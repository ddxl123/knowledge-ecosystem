# 第05章 大数定律与中心极限定理


## 5.1 大数定律

### 5.1.1 依概率收敛

$X_n \xrightarrow{P} a$：对任意 ε > 0，
$$\lim_{n \to \infty} P\{|X_n - a| < \varepsilon\} = 1$$

### 5.1.2 切比雪夫大数定律

设 X₁, X₂, … 相互独立，E(Xᵢ) = μᵢ，D(Xᵢ) ≤ C（有界），则：

$$\frac{1}{n}\sum_{i=1}^n X_i - \frac{1}{n}\sum_{i=1}^n \mu_i \xrightarrow{P} 0$$

### 5.1.3 伯努利大数定律 ⭐

设 n_A 是 n 次独立伯努利试验中事件 A 发生的次数，p = P(A)，则：

$$\frac{n_A}{n} \xrightarrow{P} p$$

> 频率依概率收敛于概率。

### 5.1.4 辛钦大数定律 ⭐⭐

设 X₁, X₂, … **独立同分布**（i.i.d.），E(Xᵢ) = μ 存在，则：

$$\bar{X}_n = \frac{1}{n}\sum_{i=1}^n X_i \xrightarrow{P} \mu$$

> 样本均值依概率收敛于总体期望。这是用样本均值估计总体均值的理论基础。

---

## 5.2 中心极限定理

### 5.2.1 林德伯格-列维（独立同分布）CLT ⭐⭐

设 X₁, X₂, … i.i.d.，E(Xᵢ) = μ，D(Xᵢ) = σ² > 0，则：

$$\frac{\sum_{i=1}^n X_i - n\mu}{\sqrt{n}\sigma} \xrightarrow{d} N(0, 1)$$

等价地：
$$\bar{X}_n \stackrel{\text{近似}}{\sim} N\left(\mu, \frac{\sigma^2}{n}\right)$$

> 不管原来什么分布，只要独立同分布且方差有限，标准化后趋于标准正态。

### 5.2.2 棣莫弗-拉普拉斯定理 ⭐

设 X ~ B(n, p)，则当 n 充分大时：

$$\frac{X - np}{\sqrt{np(1-p)}} \xrightarrow{d} N(0, 1)$$

即：
$$P\{a < X \leq b\} \approx \Phi\left(\frac{b - np}{\sqrt{np(1-p)}}\right) - \Phi\left(\frac{a - np}{\sqrt{np(1-p)}}\right)$$

> 二项分布的正态近似（n 大时）。

### 5.2.3 近似计算的修正 ⭐

用正态近似二项分布时，为提高精度，可做**连续性修正**：

$$P\{X = k\} \approx \Phi\left(\frac{k + 0.5 - np}{\sqrt{np(1-p)}}\right) - \Phi\left(\frac{k - 0.5 - np}{\sqrt{np(1-p)}}\right)$$

$$P\{X \leq k\} \approx \Phi\left(\frac{k + 0.5 - np}{\sqrt{np(1-p)}}\right)$$

---

## 5.3 题型与解题方法

### 题型一：判断收敛类型
**方法**：看条件——大数定律处理"收敛到常数"，中心极限定理处理"标准化后趋于正态"。

### 题型二：大数定律的应用
**典型题**：已知 E(X)，用 $\bar{X}_n$ 估计 E(X)。

### 题型三：中心极限定理近似计算 ⭐
**步骤**：
1. 确定 Xi 的分布（i.i.d.）
2. 计算 E(Xi) = μ，D(Xi) = σ²
3. 标准化：$Z = \frac{\sum X_i - n\mu}{\sqrt{n}\sigma}$
4. 用 Φ 函数查表

**典型例题**：
> 一批零件重量 X ~ N(50, 25)，随机取 100 个，求总重量超过 5100 的概率。
> 解：$\sum X_i \sim N(5000, 2500)$
> $P\{\sum X_i > 5100\} = P\{Z > (5100-5000)/50\} = 1 - \Phi(2) = 0.0228$

### 题型四：二项分布的正态近似 ⭐
**步骤**：
1. 验证 n 足够大（一般 n ≥ 30）
2. 计算 np 和 np(1−p)
3. 标准化后用 Φ 函数

---

## 5.4 易错点总结 ⚠️

1. **辛钦大数定律要求独立同分布**，仅独立不够
2. **中心极限定理要求方差存在**，柯西分布（无期望/方差）不适用
3. **正态近似二项分布时，np 和 n(1−p) 都不能太小**（一般 ≥ 5）
4. **连续性修正**：离散 → 连续时，区间两端各扩展 0.5
5. **大数定律给出"收敛"，中心极限定理给出"近似分布"**，不要混淆
6. **依概率收敛 vs 依分布收敛**：前者更强，后者由 CLT 给出
