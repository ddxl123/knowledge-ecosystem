# 第07章 参数估计


## 7.1 点估计

### 7.1.1 基本概念

**参数估计**：用样本统计量估计总体分布中的未知参数。

**点估计**：用一个统计量 $\hat{\theta} = \hat{\theta}(X_1, \ldots, X_n)$ 估计参数 θ。

---

## 7.2 矩估计法

### 7.2.1 原理

用样本矩替代总体矩（大数定律保证收敛）。

### 7.2.2 步骤 ⭐

1. 计算总体的前 k 阶矩 $\mu_k = E(X^k)$（含参数 θ₁, …, θₖ）
2. 列方程：$\mu_k = A_k = \frac{1}{n}\sum X_i^k$
3. 解方程得 $\hat{\theta}_k = \hat{\theta}_k(X_1, \ldots, X_n)$

### 7.2.3 典型例题

> 设 X ~ U(0, θ)，求 θ 的矩估计。
> 解：$E(X) = \theta/2$，令 $\theta/2 = \bar{X}$，得 $\hat{\theta} = 2\bar{X}$

> 设 X ~ N(μ, σ²)，μ, σ² 均未知，求矩估计。
> 解：$E(X) = \mu$ → $\hat{\mu} = \bar{X}$；$E(X^2) = \sigma^2 + \mu^2$ → $\hat{\sigma}^2 = \frac{1}{n}\sum X_i^2 - \bar{X}^2 = \frac{1}{n}\sum(X_i - \bar{X})^2$

---

## 7.3 最大似然估计法 ⭐⭐

### 7.3.1 原理

选择使样本出现概率最大的参数值。

### 7.3.2 离散型步骤

1. 写似然函数：$L(\theta) = \prod_{i=1}^n P\{X_i = x_i\} = \prod_{i=1}^n p(x_i; \theta)$
2. 取对数：$\ln L(\theta) = \sum_{i=1}^n \ln p(x_i; \theta)$
3. 求导令为零：$\frac{d \ln L}{d\theta} = 0$
4. 解出 $\hat{\theta}$（验证确实是最大值）

### 7.3.3 连续型步骤

1. 写似然函数：$L(\theta) = \prod_{i=1}^n f(x_i; \theta)$
2. 取对数后求导
3. 解出 $\hat{\theta}$

### 7.3.4 多参数情况

对每个参数分别求偏导：
$$\frac{\partial \ln L}{\partial \theta_j} = 0, \quad j = 1, \ldots, k$$

### 7.3.5 最大似然估计的不变性 ⭐

若 $\hat{\theta}$ 是 θ 的 MLE，则 $g(\hat{\theta})$ 是 $g(\theta)$ 的 MLE。

### 7.3.6 典型例题

> 设 X ~ B(1, p)，求 p 的 MLE。
> 解：$L(p) = \prod p^{x_i}(1-p)^{1-x_i} = p^{\sum x_i}(1-p)^{n-\sum x_i}$
> $\ln L = (\sum x_i)\ln p + (n - \sum x_i)\ln(1-p)$
> $\frac{d\ln L}{dp} = \frac{\sum x_i}{p} - \frac{n - \sum x_i}{1-p} = 0$
> 得 $\hat{p} = \bar{X}$

> 设 X ~ E(λ)，求 λ 的 MLE。
> 解：$L(\lambda) = \lambda^n e^{-\lambda \sum x_i}$
> $\ln L = n\ln\lambda - \lambda\sum x_i$
> $\frac{d\ln L}{d\lambda} = \frac{n}{\lambda} - \sum x_i = 0$
> 得 $\hat{\lambda} = 1/\bar{X}$

---

## 7.4 估计量的评价标准

### 7.4.1 无偏性 ⭐

$$E(\hat{\theta}) = \theta$$

**常见结论**：
- $\bar{X}$ 是 μ 的无偏估计
- $S^2 = \frac{1}{n-1}\sum(X_i - \bar{X})^2$ 是 σ² 的无偏估计
- $\frac{1}{n}\sum(X_i - \bar{X})^2$ **不是** σ² 的无偏估计（偏小，差一个系数）

### 7.4.2 有效性

若 $\hat{\theta}_1$ 和 $\hat{\theta}_2$ 都是 θ 的无偏估计，且 $D(\hat{\theta}_1) < D(\hat{\theta}_2)$，则 $\hat{\theta}_1$ 比 $\hat{\theta}_2$ 有效。

### 7.4.3 一致性（相合性）

$$\hat{\theta}_n \xrightarrow{P} \theta \quad (n \to \infty)$$

---

## 7.5 区间估计（置信区间）⭐⭐

### 7.5.1 基本概念

| 概念 | 含义 |
|------|------|
| 置信区间 | $[\hat{\theta}_L, \hat{\theta}_U]$，以概率 1−α 包含 θ |
| 置信水平 | 1−α（通常取 0.95 或 0.99） |
| 置信上/下限 | 单侧区间 |

### 7.5.2 正态总体参数的置信区间 ⭐⭐

#### (1) μ 的置信区间（σ 已知）

$$\bar{X} \pm z_{\alpha/2} \cdot \frac{\sigma}{\sqrt{n}}$$

#### (2) μ 的置信区间（σ 未知）⭐

$$\bar{X} \pm t_{\alpha/2}(n-1) \cdot \frac{S}{\sqrt{n}}$$

#### (3) σ² 的置信区间

$$\left[\frac{(n-1)S^2}{\chi^2_{\alpha/2}(n-1)}, \frac{(n-1)S^2}{\chi^2_{1-\alpha/2}(n-1)}\right]$$

### 7.5.3 两个正态总体之差 μ₁−μ₂ 的置信区间

**σ₁², σ₂² 已知**：
$$(\bar{X} - \bar{Y}) \pm z_{\alpha/2}\sqrt{\frac{\sigma_1^2}{m} + \frac{\sigma_2^2}{n}}$$

**σ₁² = σ₂² = σ² 未知**：
$$(\bar{X} - \bar{Y}) \pm t_{\alpha/2}(m+n-2) \cdot S_w\sqrt{\frac{1}{m} + \frac{1}{n}}$$

---

## 7.6 题型与解题方法

### 题型一：矩估计
**步骤**：令总体矩 = 样本矩，解方程。

### 题型二：最大似然估计
**步骤**：写 L(θ) → 取 ln L → 求导 = 0 → 解方程。

### 题型三：无偏性判断
**方法**：计算 $E(\hat{\theta})$，看是否等于 θ。

### 题型四：求置信区间 ⭐
**步骤**：
1. 确定估计的参数和已知/未知条件
2. 选择枢轴量
3. 确定分位点
4. 代入公式

---

## 7.7 易错点总结 ⚠️

1. **矩估计不唯一**：可用不同阶矩，结果可能不同
2. **MLE 不一定无偏**：如 $\hat{\sigma}^2_{MLE} = \frac{1}{n}\sum(X_i-\bar{X})^2$ 有偏
3. **S² 分母是 n−1 才无偏**，不是 n
4. **置信区间不是"θ 落在该区间的概率为 1−α"**——参数是固定的，区间是随机的
5. **σ 未知时用 t 分布**，σ 已知时用正态分布
6. **置信区间的枢轴量选择**：σ 已知用 Z，σ 未知用 T
7. **χ² 分布置信区间**：上、下分位点不要搞反
