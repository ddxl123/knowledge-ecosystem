# 第06章 数理统计基础


## 6.1 基本概念

### 6.1.1 总体与样本

| 概念 | 定义 |
|------|------|
| 总体 | 研究对象的全体（随机变量 X） |
| 个体 | 总体中的每个成员 |
| 样本 | (X₁, X₂, …, Xₙ)，从总体中随机抽取的 n 个个体 |
| 简单随机样本 | X₁, …, Xₙ 独立同分布，与总体 X 同分布 |

### 6.1.2 统计量

**定义**：样本 (X₁, …, Xₙ) 的函数 T = T(X₁, …, Xₙ)，**不含未知参数**。

### 6.1.3 常用统计量 ⭐

| 统计量 | 公式 | 含义 |
|--------|------|------|
| 样本均值 | $\bar{X} = \frac{1}{n}\sum_{i=1}^n X_i$ | 位置特征 |
| 样本方差 | $S^2 = \frac{1}{n-1}\sum_{i=1}^n (X_i - \bar{X})^2$ | 离散程度 |
| 样本标准差 | $S = \sqrt{S^2}$ | |
| 样本 k 阶原点矩 | $A_k = \frac{1}{n}\sum_{i=1}^n X_i^k$ | |
| 样本 k 阶中心矩 | $B_k = \frac{1}{n}\sum_{i=1}^n (X_i - \bar{X})^k$ | |

⚠️ **样本方差分母是 n−1**（无偏估计），不是 n！

---

## 6.2 抽样分布 ⭐⭐

### 6.2.1 χ² 分布

**定义**：设 X₁, …, Xₙ ~ N(0,1) 独立，则：
$$\chi^2 = X_1^2 + X_2^2 + \cdots + X_n^2 \sim \chi^2(n)$$

**性质**：
- E(χ²) = n，D(χ²) = 2n
- **可加性**：若 X ~ χ²(m)，Y ~ χ²(n) 独立，则 X+Y ~ χ²(m+n)
- 上侧分位点：$P\{\chi^2 > \chi^2_\alpha(n)\} = \alpha$

### 6.2.2 t 分布

**定义**：设 X ~ N(0,1)，Y ~ χ²(n) 独立，则：
$$T = \frac{X}{\sqrt{Y/n}} \sim t(n)$$

**性质**：
- 密度关于 0 对称
- n 较大时趋近标准正态
- E(T) = 0（n > 1），D(T) = n/(n−2)（n > 2）
- 上侧分位点：$P\{T > t_\alpha(n)\} = \alpha$

### 6.2.3 F 分布

**定义**：设 X ~ χ²(m)，Y ~ χ²(n) 独立，则：
$$F = \frac{X/m}{Y/n} \sim F(m, n)$$

**性质**：
- F > 0
- 若 T ~ t(n)，则 T² ~ F(1, n)
- $F_\alpha(m,n) = \frac{1}{F_{1-\alpha}(n,m)}$
- 上侧分位点：$P\{F > F_\alpha(m,n)\} = \alpha$

---

## 6.3 正态总体的抽样分布定理 ⭐⭐⭐

设 X₁, …, Xₙ ~ N(μ, σ²) i.i.d.，$\bar{X}$ 为样本均值，S² 为样本方差。

### 定理 1：样本均值的分布

$$\bar{X} \sim N\left(\mu, \frac{\sigma^2}{n}\right)$$

标准化：
$$\frac{\bar{X} - \mu}{\sigma / \sqrt{n}} \sim N(0, 1)$$

### 定理 2：样本方差与 χ²

$$\frac{(n-1)S^2}{\sigma^2} = \frac{\sum_{i=1}^n (X_i - \bar{X})^2}{\sigma^2} \sim \chi^2(n-1)$$

### 定理 3：均值与方差独立

$\bar{X}$ 与 S² 相互独立。

### 定理 4：t 分布

$$\frac{\bar{X} - \mu}{S / \sqrt{n}} \sim t(n-1)$$

### 定理 5：两个正态总体

设 X₁,…,Xₘ ~ N(μ₁,σ₁²)，Y₁,…,Yₙ ~ N(μ₂,σ₂²) 独立。

$$\frac{(\bar{X} - \bar{Y}) - (\mu_1 - \mu_2)}{\sqrt{\frac{\sigma_1^2}{m} + \frac{\sigma_2^2}{n}}} \sim N(0, 1)$$

当 σ₁² = σ₂² = σ² 时：
$$\frac{(\bar{X} - \bar{Y}) - (\mu_1 - \mu_2)}{S_w\sqrt{\frac{1}{m} + \frac{1}{n}}} \sim t(m+n-2)$$

其中 $S_w^2 = \frac{(m-1)S_1^2 + (n-1)S_2^2}{m+n-2}$

$$\frac{S_1^2 / \sigma_1^2}{S_2^2 / \sigma_2^2} \sim F(m-1, n-1)$$

---

## 6.4 题型与解题方法

### 题型一：判断抽样分布
**步骤**：
1. 确定涉及的统计量（$\bar{X}$, S², 还是两者组合）
2. 判断是否正态总体
3. 选择对应分布（正态/χ²/t/F）

### 题型二：求统计量的期望和方差
**方法**：利用定理，结合已知分布的期望和方差。

### 题型三：分位点计算
**方法**：查表或利用对称性（t 分布关于 0 对称，F 分布有倒数关系）。

---

## 6.5 易错点总结 ⚠️

1. **样本方差分母是 n−1 不是 n**
2. **χ²(n−1) 中是 n−1**，因为用了 $\bar{X}$ 估计 μ 损失了一个自由度
3. **t 分布的自由度也是 n−1**（σ 未知时用 S 代替）
4. **$\bar{X}$ 与 S² 独立**是正态总体的特殊性质
5. **F 分布有两个自由度**，注意分子分母对应的自由度
6. **F 的倒数关系**：$F_\alpha(m,n) = 1/F_{1-\alpha}(n,m)$，不是 $F_{1-\alpha}(m,n)$
