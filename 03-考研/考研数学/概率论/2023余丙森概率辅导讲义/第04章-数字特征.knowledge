# 第04章 数字特征


## 4.1 数学期望

### 4.1.1 定义

**离散型**：
$$E(X) = \sum_{k} x_k p_k$$

**连续型**：
$$E(X) = \int_{-\infty}^{+\infty} x f(x)\,dx$$

**绝对收敛**：级数/积分需绝对收敛，否则期望不存在。

### 4.1.2 随机变量函数的期望 ⭐

**离散型**：
$$E[g(X)] = \sum_k g(x_k) p_k$$

**连续型**：
$$E[g(X)] = \int_{-\infty}^{+\infty} g(x) f(x)\,dx$$

**二维**：
$$E[g(X,Y)] = \int_{-\infty}^{+\infty}\int_{-\infty}^{+\infty} g(x,y) f(x,y)\,dx\,dy$$

⚠️ **不用先求 g(X) 的分布，直接用 X 的分布算！**

### 4.1.3 期望的性质 ⭐

| 性质 | 公式 |
|------|------|
| 常数 | E(C) = C |
| 线性 | E(aX+bY) = aE(X) + bE(Y) |
| 独立时 | E(XY) = E(X)·E(Y) |
| 单调性 | X ≥ 0 ⟹ E(X) ≥ 0 |

⚠️ **线性性不要求独立！** 但 E(XY) = E(X)E(Y) 需要独立（或不相关）。

---

## 4.2 方差

### 4.2.1 定义

$$D(X) = E\{[X - E(X)]^2\} = E(X^2) - [E(X)]^2$$

**计算公式** ⭐：
$$D(X) = E(X^2) - [E(X)]^2$$

### 4.2.2 方差的性质

| 性质 | 公式 |
|------|------|
| 非负 | D(X) ≥ 0，D(X)=0 ⟺ X 为常数 |
| 常数 | D(C) = 0 |
| 倍数 | D(aX+b) = a²D(X) |
| 独立可加 | D(X+Y) = D(X) + D(Y)（X,Y 独立）|

**一般情况**：
$$D(X \pm Y) = D(X) + D(Y) \pm 2\text{Cov}(X,Y)$$

### 4.2.3 标准差

$$\sigma(X) = \sqrt{D(X)}$$

---

## 4.3 常见分布的期望与方差 ⭐⭐

| 分布 | E(X) | D(X) |
|------|------|------|
| B(1, p) | p | p(1−p) |
| B(n, p) | np | np(1−p) |
| P(λ) | λ | λ |
| Ge(p) | 1/p | (1−p)/p² |
| U(a, b) | (a+b)/2 | (b−a)²/12 |
| E(λ) | 1/λ | 1/λ² |
| N(μ, σ²) | μ | σ² |

---

## 4.4 协方差与相关系数

### 4.4.1 协方差

$$\text{Cov}(X, Y) = E(XY) - E(X)E(Y)$$

**等价定义**：
$$\text{Cov}(X, Y) = E\{[X - E(X)][Y - E(Y)]\}$$

**性质**：
- Cov(X, X) = D(X)
- Cov(X, Y) = Cov(Y, X)
- Cov(aX+b, cY+d) = ac·Cov(X, Y)
- Cov(X₁+X₂, Y) = Cov(X₁, Y) + Cov(X₂, Y)
- X, Y 独立 ⟹ Cov(X, Y) = 0（反之不然）

### 4.4.2 相关系数 ⭐⭐

$$\rho_{XY} = \frac{\text{Cov}(X,Y)}{\sqrt{D(X)}\sqrt{D(Y)}}$$

**性质**：
1. $|\rho_{XY}| \leq 1$
2. $|\rho_{XY}| = 1$ ⟺ X 与 Y 线性相关（Y = aX + b，a>0 时 ρ=1，a<0 时 ρ=−1）
3. X, Y 独立 ⟹ ρ = 0（"不相关"），反之不然

### 4.4.3 不相关 vs 独立 ⭐⭐

| | 不相关 | 独立 |
|--|--------|------|
| 定义 | ρ = 0（或 Cov = 0） | F(x,y) = F_X(x)F_Y(y) |
| 关系 | 独立 ⟹ 不相关 | 不一定 |
| 例外 | 二维正态中等价 | 仅二维正态 |

**不相关但不独立的经典例子**：X ~ U(−1, 1)，Y = X²。ρ=0 但明显不独立。

---

## 4.5 矩与协方差矩阵

### 4.5.1 矩的定义

| 名称 | 定义 | 含义 |
|------|------|------|
| k 阶原点矩 | E(Xᵏ) | k=1 即期望 |
| k 阶中心矩 | E[(X−E(X))ᵏ] | k=2 即方差 |
| k+l 阶混合原点矩 | E(XᵏYˡ) | |
| k+l 阶混合中心矩 | E[(X−EX)ᵏ(Y−EY)ˡ] | k=l=1 即协方差 |

### 4.5.2 协方差矩阵

$$\boldsymbol{C} = \begin{pmatrix} D(X) & \text{Cov}(X,Y) \\ \text{Cov}(Y,X) & D(Y) \end{pmatrix}$$

**性质**：对称、半正定。

---

## 4.6 切比雪夫不等式 ⭐

$$P\{|X - E(X)| \geq \varepsilon\} \leq \frac{D(X)}{\varepsilon^2}$$

或等价地：
$$P\{|X - E(X)| < \varepsilon\} \geq 1 - \frac{D(X)}{\varepsilon^2}$$

**意义**：不需要知道分布，只需知道期望和方差即可给出概率的界。

---

## 4.7 题型与解题方法

### 题型一：求期望和方差
**方法**：
1. 直接用定义（离散求和/连续积分）
2. 利用常见分布的结论
3. 分解为简单随机变量之和（如 X = X₁ + … + Xₙ）

### 题型二：求协方差和相关系数
**步骤**：
1. 求 E(XY)、E(X)、E(Y)
2. Cov(X,Y) = E(XY) − E(X)E(Y)
3. 求 D(X)、D(Y)
4. ρ = Cov / (√D(X)·√D(Y))

### 题型三：独立与不相关
**方法**：验证 Cov 是否为 0（不相关），再验证联合密度是否可分离（独立）。

### 题型四：切比雪夫不等式应用
**典型题型**：已知期望和方差，估计概率的范围。

---

## 4.8 易错点总结 ⚠️

1. **D(X+Y) = D(X)+D(Y) 仅当独立时成立**，一般情况下要加 2Cov
2. **E(XY) = E(X)E(Y) 需要独立或不相关**，不是总成立
3. **不相关 ≠ 独立**（二维正态除外）
4. **ρ 衡量的是线性相关程度**，非线性关系可能 ρ=0
5. **方差公式 D(X) = E(X²) − [E(X)]²** 比定义式更常用
6. **D(aX+b) = a²D(X)**，常数 b 不影响方差
7. **切比雪夫不等式是估计**，实际概率可能小很多
