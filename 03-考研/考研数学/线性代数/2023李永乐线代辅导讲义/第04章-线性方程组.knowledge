# 第04章 线性方程组


## 一、知识定义

### 1.1 线性方程组的矩阵形式
- **一般形式:** Ax = b（A为m×n系数矩阵，x为n维未知向量，b为m维常数向量）
- **齐次方程组:** Ax = 0（b = 0）
- **非齐次方程组:** Ax = b（b ≠ 0）
- **增广矩阵:** (A | b)

### 1.2 解的判定
- **有解条件:** Ax = b 有解 ⟺ r(A) = r(A|b)
- **唯一解:** r(A) = r(A|b) = n（未知数个数）
- **无穷多解:** r(A) = r(A|b) = r < n
- **无解:** r(A) ≠ r(A|b)

### 1.3 齐次方程组的解
- **必有零解:** x = 0 总是 Ax = 0 的解
- **有非零解条件:** r(A) < n（未知数个数）
  - 等价条件：|A| = 0（方阵时）
  - 等价条件：方程个数 < 未知数个数（m < n时必有非零解）

### 1.4 基础解系
- **定义:** 齐次方程组 Ax = 0 的解空间的一组基
- **个数:** 基础解系含 n - r(A) 个线性无关的解向量
- **通解:** x = k₁ξ₁ + k₂ξ₂ + … + kₙ₋ᵣξₙ₋ᵣ（k₁, …, kₙ₋ᵣ为任意常数）

---

## 二、重要性质与公式

### 2.1 齐次方程组的性质
1. **解的叠加性:** 若 ξ₁, ξ₂ 是解，则 k₁ξ₁ + k₂ξ₂ 也是解
2. **解空间:** Ax = 0 的所有解构成 n-r(A) 维向量空间
3. **有非零解的充要条件:** 系数矩阵的秩 < 未知数个数

### 2.2 非齐次方程组的性质
1. **解的结构:** Ax = b 的通解 = Ax = 0 的通解 + Ax = b 的一个特解
   - x = k₁ξ₁ + k₂ξ₂ + … + kₙ₋ᵣξₙ₋ᵣ + η*
2. **解的差:** 若 η₁, η₂ 是 Ax = b 的解，则 η₁ - η₂ 是 Ax = 0 的解
3. **解的和:** 若 η₁, η₂ 是 Ax = b 的解，则 η₁ + η₂ 一般不是 Ax = b 的解
4. **线性组合:** 若 η₁, η₂ 是 Ax = b 的解，则 k₁η₁ + k₂η₂ 是 Ax = b 的解 ⟺ k₁ + k₂ = 1

### 2.3 克莱姆法则（n个方程n个未知数）
- |A| ≠ 0 ⟺ 有唯一解 xᵢ = Dᵢ/D
- |A| = 0 ⟺ 无解或无穷多解

### 2.4 方程组之间的关系
- Ax = 0 与 Bx = 0 同解 ⟹ r(A) = r(B)（反之不成立）
- Ax = b 的解集结构由 r(A) 和 r(A|b) 完全决定

---

## 三、常考题型

### 题型一：判断解的情况
**步骤:**
1. 写出增广矩阵 (A|b)
2. 初等行变换化为阶梯形
3. 比较 r(A) 与 r(A|b)
4. 判定无解/唯一解/无穷多解

### 题型二：求齐次方程组的基础解系
**步骤:**
1. 对系数矩阵A做初等行变换，化为行最简形
2. 确定主元列和自由变量
3. 对自由变量分别取标准基向量，回代求主元变量
4. 得到 n-r(A) 个线性无关解，即为基础解系

**例题:** 求 Ax = 0 的基础解系
```
A = [1  2 -1  1]
    [2  4  1  0]
    [3  6  0  1]
```
→ 行变换后 r(A) = 2，基础解系含 4-2 = 2 个向量

### 题型三：求非齐次方程组的通解
**步骤:**
1. 求 Ax = 0 的基础解系 ξ₁, …, ξₙ₋ᵣ
2. 求 Ax = b 的一个特解 η*
3. 通解：x = k₁ξ₁ + … + kₙ₋ᵣξₙ₋ᵣ + η*

### 题型四：含参数方程组的讨论
**步骤:**
1. 对增广矩阵做初等行变换（含参数）
2. 讨论参数取值对秩的影响
3. 分情况给出解的情况

### 题型五：方程组解的性质与结构
- 利用解的叠加性
- 利用解的结构定理
- 已知部分解推断整体

### 题型六：两个方程组的公共解
**方法:**
1. 联立两个方程组 [A; B]x = [0; 0]
2. 求联立方程组的基础解系

### 题型七：同解方程组
- Ax = 0 与 Bx = 0 同解 ⟺ A与B行等价 ⟺ A的行向量组与B的行向量组等价

---

## 四、易错点提醒

⚠️ **r(A) 与 r(A|b) 的比较:** 必须用增广矩阵做行变换，不能分开

⚠️ **基础解系的个数:** 是 n - r(A) 个向量，不是 r(A) 个

⚠️ **特解的选取:** 可以取任何一个特解，不同特解不影响通解

⚠️ **齐次 vs 非齐次:**
- 齐次：解空间是子空间（对加法和数乘封闭）
- 非齐次：解集不是子空间（不包含零向量）

⚠️ **"无穷多解"≠"任意解":** 无穷多解时解仍有结构约束

⚠️ **自由变量的选取:** 取标准基向量时，注意符号和主元位置的对应

⚠️ **含参数时分类讨论:** 不要遗漏参数的特殊取值（如使某行全为零的情况）

---

## 五、真题链接

- 解的判定（选择题必考）
- 求基础解系和通解（计算题高频）
- 含参数方程组的讨论（中等难度综合题）
- 两个方程组的公共解/同解问题
- 解的结构定理的应用

---

## 六、与其他章节的联系
- ← **第01章行列式:** 克莱姆法则；|A|=0与有非零解的关系
- ← **第02章矩阵:** 初等变换化阶梯形；秩的概念
- ← **第03章向量:** 线性相关/无关与齐次方程组；线性表示与非齐次方程组
- → **第05章特征值:** 特征向量是齐次方程组 (λE-A)x = 0 的解
