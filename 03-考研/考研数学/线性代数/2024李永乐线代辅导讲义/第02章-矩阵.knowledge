# 第02章 矩阵


## 一、知识定义

### 1.1 矩阵的基本概念
- **矩阵:** m×n个数排成m行n列的数表
- **方阵:** 行数等于列数的矩阵
- **单位矩阵** E（或I）：主对角线为1，其余为0的方阵
- **零矩阵** O：所有元素为0
- **对角矩阵:** 非主对角线元素全为0
- **数量矩阵:** kE，对角线元素相同

### 1.2 特殊矩阵
- **对称矩阵:** Aᵀ = A（aᵢⱼ = aⱼᵢ）
- **反对称矩阵:** Aᵀ = -A（aᵢⱼ = -aⱼᵢ，对角线为0）
- **正交矩阵:** AᵀA = A Aᵀ = E，即 A⁻¹ = Aᵀ
- **幂等矩阵:** A² = A
- **幂零矩阵:** ∃k, Aᵏ = O
- **对合矩阵:** A² = E

---

## 二、重要性质与公式

### 2.1 矩阵运算
| 运算 | 规则 | 注意事项 |
|------|------|----------|
| 加法 | A + B = (aᵢⱼ + bᵢⱼ) | 同型矩阵才能相加 |
| 数乘 | kA = (kaᵢⱼ) | \|kA\| = kⁿ\|A\| |
| 乘法 | AB: (AB)ᵢⱼ = Σₖ aᵢₖbₖⱼ | 一般 AB ≠ BA |
| 转置 | (Aᵀ)ᵢⱼ = aⱼᵢ | (AB)ᵀ = BᵀAᵀ |

### 2.2 逆矩阵
- **定义:** AB = BA = E，则 B = A⁻¹
- **存在条件:** |A| ≠ 0（即可逆 ⟺ 满秩 ⟺ 非奇异）
- **求法:**
  - 公式法: A⁻¹ = (1/|A|) · A*（A*为伴随矩阵）
  - 初等行变换: [A | E] → [E | A⁻¹]
- **性质:**
  - (A⁻¹)⁻¹ = A
  - (kA)⁻¹ = (1/k)A⁻¹
  - (AB)⁻¹ = B⁻¹A⁻¹
  - (Aᵀ)⁻¹ = (A⁻¹)ᵀ
  - |A⁻¹| = 1/|A|

### 2.3 伴随矩阵
- **定义:** A* = (Aᵢⱼ)ᵀ（代数余子式矩阵的转置）
- **核心公式:** AA* = A*A = |A|E
- **性质:**
  - |A*| = |A|^(n-1)
  - (A*)* = |A|^(n-2) · A（n≥2）
  - (kA)* = k^(n-1) · A*
  - (AB)* = B* · A*
  - A⁻¹ = (1/|A|) · A*（当|A|≠0）
  - A* = |A| · A⁻¹（当|A|≠0）

### 2.4 初等变换与初等矩阵
- **三种初等行变换:**
  1. 交换两行 rᵢ ↔ rⱼ
  2. 某行乘非零常数 krᵢ
  3. 某行加上另一行的k倍 rᵢ + krⱼ
- **初等矩阵:** 由单位矩阵经一次初等变换得到
- **关键定理:** 对A做初等行变换 = 左乘初等矩阵；初等列变换 = 右乘初等矩阵
- **矩阵等价:** A经有限次初等变换得到B ⟺ 存在可逆P, Q使PAQ = B

### 2.5 分块矩阵
- **运算规则:** 分块后按"元素"运算，注意分块的兼容性
- **常见分块:**
  ```
  [A  O]    [A  C]
  [O  B]    [O  B]
  ```
- **逆矩阵的分块求法:**
  - 对角分块: [A O; O B]⁻¹ = [A⁻¹ O; O B⁻¹]
  - 三角分块: [A C; O B]⁻¹ = [A⁻¹  -A⁻¹CB⁻¹; O  B⁻¹]

### 2.6 矩阵的秩
- **定义:** 矩阵中非零子式的最高阶数，记为 r(A)
- **性质:**
  - 0 ≤ r(A) ≤ min(m, n)
  - r(A) = r(Aᵀ) = r(AᵀA) = r(AAᵀ)
  - r(A+B) ≤ r(A) + r(B)
  - r(AB) ≤ min(r(A), r(B))
  - 若A可逆，则 r(AB) = r(BA) = r(B)
  - **西尔维斯特不等式:** r(AB) ≥ r(A) + r(B) - n

---

## 三、常考题型

### 题型一：矩阵的运算
1. 注意矩阵乘法不满足交换律：AB ≠ BA
2. (A+B)² ≠ A² + 2AB + B²（除非AB = BA）
3. AB = O ⇏ A = O 或 B = O

### 题型二：求逆矩阵
**步骤:**
1. 计算 |A|，验证 |A| ≠ 0
2. 方法一：伴随矩阵法 A⁻¹ = (1/|A|)·A*
3. 方法二：初等行变换 [A|E] → [E|A⁻¹]
4. 方法三：分块求逆（适合特殊结构）

### 题型三：伴随矩阵相关计算
- 利用 AA* = |A|E 进行推导
- 求 A* 的逆、秩、行列式等

### 题型四：矩阵方程
- AX = B → X = A⁻¹B（A可逆时）
- XA = B → X = BA⁻¹
- AXB = C → X = A⁻¹CB⁻¹

### 题型五：矩阵的秩
**求秩方法:**
1. 初等行变换化为阶梯形，非零行数即为秩
2. 利用秩的不等式进行估计
3. 利用已知条件（如AB = O）推导秩的关系

---

## 四、易错点提醒

⚠️ **矩阵乘法不可交换:** AB ≠ BA 是常态，只有特殊情况下才相等

⚠️ **AB = O 不意味着 A=O 或 B=O:** 这与数的乘法不同

⚠️ **逆矩阵的顺序:** (AB)⁻¹ = B⁻¹A⁻¹，注意顺序反转

⚠️ **|kA| = kⁿ|A|:** 不是 k|A|，n为矩阵阶数

⚠️ **伴随矩阵的阶数:** A是n阶方阵，A*也是n阶方阵

⚠️ **秩的不等式方向:** r(AB) ≤ min(r(A), r(B)) 是"≤"不是"="

⚠️ **分块矩阵乘法:** 分块方式要保证子块的乘法有意义

---

## 五、真题链接

- 逆矩阵的求法（选择/填空高频）
- 伴随矩阵的性质与计算
- 矩阵方程的求解
- 矩阵秩的计算与证明
- 初等矩阵与初等变换的关系

---

## 六、与其他章节的联系
- ← **第01章行列式:** |AB| = |A|·|B|，逆矩阵存在的行列式判据
- → **第03章向量:** 向量组的秩 = 矩阵的秩
- → **第04章线性方程组:** 系数矩阵的秩决定解的情况
- → **第05章特征值:** 相似矩阵 P⁻¹AP = B
