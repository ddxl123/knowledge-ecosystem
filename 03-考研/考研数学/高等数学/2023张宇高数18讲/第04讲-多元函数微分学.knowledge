# 第04讲：多元函数微分学


## 知识点1：多元函数的极限与连续

**定义（二重极限）**:
$$\lim_{(x,y) \to (x_0, y_0)} f(x,y) = A$$
要求 $(x,y)$ 沿任何路径趋于 $(x_0, y_0)$ 时极限都存在且相等

**性质**:
1. 多元函数在一点连续 $\iff$ 极限值等于函数值
2. 多元函数在有界闭区域上连续 $\Rightarrow$ 有最值、介值定理成立

**常考题型**:
- 证明二重极限不存在（取不同路径）
- 求二重极限

**易错点**: 二重极限不存在只需找到两条不同路径极限不等即可

---

## 知识点2：偏导数

**定义**:
$$\frac{\partial f}{\partial x}\bigg|_{(x_0, y_0)} = \lim_{\Delta x \to 0} \frac{f(x_0 + \Delta x, y_0) - f(x_0, y_0)}{\Delta x}$$

**性质**:
1. 偏导数存在 $\not\Rightarrow$ 连续
2. 偏导数存在 $\not\Rightarrow$ 可微
3. 可微 $\Rightarrow$ 偏导数存在 $\Rightarrow$ 方向导数存在

**计算方法**: 对某变量求偏导时，将其他变量视为常数

**常考题型**:
- 求多元函数的偏导数
- 复合函数的偏导数（链式法则）
- 隐函数的偏导数

---

## 知识点3：全微分

**定义**: 若 $\Delta z = A \Delta x + B \Delta y + o(\sqrt{(\Delta x)^2 + (\Delta y)^2})$，则
$$dz = \frac{\partial z}{\partial x} dx + \frac{\partial z}{\partial y} dy$$

**可微的充分条件**: 偏导数连续 $\Rightarrow$ 可微

**性质**:
1. 全微分形式不变性：$dz = \frac{\partial z}{\partial u} du + \frac{\partial z}{\partial v} dv$（无论 $u, v$ 是自变量还是中间变量）

**常考题型**:
- 判断函数是否可微
- 求全微分
- 利用全微分进行近似计算

---

## 知识点4：复合函数求导（链式法则）

**公式**: 设 $z = f(u,v)$，$u = \varphi(x,y)$，$v = \psi(x,y)$，则
$$\frac{\partial z}{\partial x} = \frac{\partial z}{\partial u} \cdot \frac{\partial u}{\partial x} + \frac{\partial z}{\partial v} \cdot \frac{\partial v}{\partial x}$$

**常考题型**:
- 多层复合函数求偏导
- 抽象函数求偏导
- 利用全微分形式不变性

**易错点**: 链式法则中注意中间变量的个数，画好树形图

---

## 知识点5：隐函数求导

**公式（一个方程 $F(x,y,z) = 0$ 确定 $z = z(x,y)$）**:
$$\frac{\partial z}{\partial x} = -\frac{F_x}{F_z}, \quad \frac{\partial z}{\partial y} = -\frac{F_y}{F_z}$$

**公式（方程组确定的隐函数）**:
利用雅可比行列式求解

**常考题型**:
- 由方程确定的隐函数求偏导
- 由方程组确定的隐函数求偏导

---

## 知识点6：极值与条件极值

**极值的必要条件**: $f_x(x_0, y_0) = 0$，$f_y(x_0, y_0) = 0$

**极值的充分条件**: 令 $A = f_{xx}$，$B = f_{xy}$，$C = f_{yy}$
- $AC - B^2 > 0$ 且 $A > 0$：极小值
- $AC - B^2 > 0$ 且 $A < 0$：极大值
- $AC - B^2 < 0$：非极值（鞍点）
- $AC - B^2 = 0$：无法判断

**条件极值（拉格朗日乘数法）**:
求 $f(x,y,z)$ 在约束 $\varphi(x,y,z) = 0$ 下的极值：
构造 $L(x,y,z,\lambda) = f(x,y,z) + \lambda \varphi(x,y,z)$，令所有偏导数为零

**常考题型**:
- 求多元函数的极值
- 求条件极值（拉格朗日乘数法）
- 求闭区域上的最值

**易错点**: 边界上的最值需要单独讨论

**真题链接**: 复合函数偏导数和极值问题是高频考点
