# 2025王道408真题
# 领域: 考研
# 学科: 计算机/408真题
# 来源: 2025王道408真题
# 描述: 408统考历年真题核心知识点归纳，涵盖数据结构、计算机组成原理、操作系统、计算机网络高频考点

{{数据结构——线性表高频考点}}{{顺序表和链表的操作对比是必考点。头插法/尾插法建表的区别。双链表插入删除操作的指针修改顺序。循环链表的应用（约瑟夫环问题）。静态链表的概念。选择题常考时间复杂度分析。综合题常考链表算法设计题：逆置、合并、查找中间结点等。}}

{{数据结构——树与二叉树高频考点}}{{二叉树遍历序列还原二叉树是高频综合题。已知先序+中序→还原二叉树→求后序。已知后序+中序→还原二叉树→求先序。二叉排序树的建立、查找和删除。平衡二叉树的旋转操作（LL、RR、LR、RL）。赫夫曼树的构造和WPL计算。线索二叉树的构造。}}

{{数据结构——图的高频考点}}{{图的邻接矩阵和邻接表存储及转换。DFS和BFS遍历序列的判断。最小生成树的Prim和Kruskal算法步骤。Dijkstra最短路径的逐步求解。拓扑排序序列的合法性判断。关键路径的求解（求事件最早最迟时间、活动最早最迟开始时间）。AOE网的相关计算。}}

{{数据结构——排序算法的比较与分析}}{{各排序算法的时间复杂度、空间复杂度、稳定性是选择题高频考点。快速排序的分区过程和递归树分析。堆排序的建堆过程和调整过程。归并排序的合并过程。基数排序的分配收集过程。给定初始序列，判断某种排序的某趟结果。排序算法的选择题常考：比较次数、移动次数、稳定性判断。}}

{{数据结构——查找的高频考点}}{{折半查找的过程和判定树构造。ASL的计算是高频考点。B树的插入删除操作（3阶B树最常考）。散列表的构造和查找：散列函数选择、冲突处理（线性探测法最常考）、ASL计算。二叉排序树和平衡二叉树的查找效率比较。红黑树的基本概念。}}

{{组成原理——数据表示高频考点}}{{原码、反码、补码、移码之间的转换。定点数补码加减法运算和溢出判断。IEEE 754浮点数的表示（单精度：1位符号+8位阶码+23位尾数，隐含最高位1）。浮点数的规格化、舍入。海明码的编码和检错纠错。CRC校验码的计算。大小端字节序。}}

{{组成原理——存储系统高频考点}}{{SRAM和DRAM的区别是选择题必考。Cache的三种映射方式及地址划分。Cache的替换算法（LRU最常考）。Cache命中率和平均访问时间的计算。主存与Cache的地址映射计算。虚拟存储器的基本概念。TLB快表的作用。存储器扩展（位扩展、字扩展）。}}

{{组成原理——指令系统与CPU高频考点}}{{指令格式分析（操作码+地址码）。寻址方式的识别和有效地址计算。CISC和RISC的对比。指令流水线的性能分析（吞吐率、加速比）。流水线冲突的类型和解决方案。CPU数据通路分析。微程序控制器和硬布线控制器的对比。微指令格式（水平型和垂直型）。中断处理过程。}}

{{组成原理——总线与I/O高频考点}}{{总线带宽计算。总线仲裁方式的对比。I/O方式的对比：程序查询、程序中断、DMA。DMA方式中CPU和DMA控制器交替使用总线的方式。中断响应过程和中断优先级。DMA传送过程和三种传送方式。I/O接口的功能和编址方式。PCI和PCIe的基本概念。}}

{{操作系统——进程管理高频考点}}{{进程状态转换图是必考。信号量实现同步互斥是综合题高频：生产者消费者、读者写者、哲学家进餐。PV操作的分析：确定信号量的个数和初值、在正确位置插入P/V操作。死锁的必要条件和银行家算法的安全性判断。处理机调度算法（FCFS、SJF、RR、优先级、多级反馈队列）的执行过程和性能比较。}}

{{操作系统——内存管理高频考点}}{{页式存储管理的地址计算（页号、页内偏移→查页表→物理地址）。页表结构和TLB的作用。缺页中断处理和页面置换算法（FIFO、LRU、CLOCK）的手工模拟。段式和段页式存储管理。动态分区分配算法（首次适应、最佳适应等）。内存分配和回收的过程。}}

{{操作系统——文件系统与I/O高频考点}}{{文件的物理结构（连续、链接、索引）的对比和地址转换。目录结构和文件共享。磁盘调度算法（FCFS、SSTF、SCAN、C-SCAN）的手工模拟和比较。磁盘访问时间的计算。缓冲区管理。SPOOLing技术。文件保护机制。I/O控制方式的层次和比较。}}

{{计算机网络——物理层与数据链路层高频考点}}{{奈奎斯特定理和香农定理的计算题。编码方式的波形图判断（曼彻斯特编码最常考）。CSMA/CD协议的最小帧长计算和冲突检测。以太网帧格式和MAC地址。交换机的工作原理和自学习。VLAN的概念。CRC校验的计算过程。PPP协议的特点。滑动窗口协议的窗口大小和序号位数关系。}}

{{计算机网络——网络层高频考点}}{{IP地址分类和子网划分（CIDR记法）是计算题高频。子网掩码、网络地址、广播地址的计算。ARP协议的工作过程。IP数据报分片的计算（标识、标志、片偏移）。路由表的构造和分组转发过程。RIP和OSPF路由协议的对比。NAT地址转换。ICMP报文类型。IPv6的基本特点。}}

{{计算机网络——传输层高频考点}}{{TCP三次握手和四次挥手的过程及状态变化是必考。TCP报文段首部字段含义（序号、确认号、标志位）。TCP流量控制（滑动窗口机制）。TCP拥塞控制四种算法（慢开始、拥塞避免、快重传、快恢复）的窗口变化过程。cwnd和ssthresh的数值变化分析。UDP和TCP的对比。}}

{{计算机网络——应用层高频考点}}{{DNS域名解析过程（递归查询和迭代查询）。HTTP协议的特点和请求/响应格式。HTTP状态码分类。SMTP、POP3、IMAP的区别。DHCP工作过程。常用协议的端口号（HTTP-80,HTTPS-443,DNS-53,SMTP-25,POP3-110,FTP-20/21,TCP-各种,UDP-各种）。网络安全基础：对称加密和非对称加密的对比。}}

{{408真题——综合题解题策略}}{{数据结构综合题：链表操作题要注意边界条件（空表、单元素、首尾元素）。树的综合题注意遍历序列的还原方法。图的综合题注意算法的手工模拟步骤。组成原理综合题：浮点数运算题注意对阶→运算→规格化→舍入步骤。Cache题注意地址划分和命中判断。OS综合题：PV操作题注意信号量设置和P/V位置。页面置换题注意手工模拟。网络综合题：IP子网划分题注意CIDR计算。TCP拥塞控制题注意窗口变化图。}}
