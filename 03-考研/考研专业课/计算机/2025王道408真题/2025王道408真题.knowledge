# 2025王道408真题
# 领域: 考研
# 学科: 408计算机
# 来源: 王道408真题
# 描述: 2025年王道408真题核心知识点，涵盖数据结构、计算机组成原理、操作系统、计算机网络四大板块

{{知识点1}}{{顺序表与链表的对比：顺序表支持随机存取，插入删除需移动元素，时间复杂度O(n)；链表不支持随机存取，插入删除只需修改指针，时间复杂度O(1)（已知结点位置时）。顺序表适合频繁查找，链表适合频繁插入删除。考试中常考两种结构的选择题和综合应用题。}}

{{知识点2}}{{头插法与尾插法建链表：头插法建立的链表与输入顺序相反，时间复杂度O(n)；尾插法建立的链表与输入顺序相同，需要尾指针，时间复杂度O(n)。尾插法常配合头结点使用。综合题中建表算法是基础，需熟练掌握。}}

{{知识点3}}{{双链表操作的指针修改顺序：插入操作需修改四个指针（前驱的next、新结点的prior和next、后继的prior），删除操作需修改两个指针（前驱的next、后继的prior）。修改顺序不当会导致链表断裂，这是选择题常见陷阱。}}

{{知识点4}}{{循环链表与约瑟夫环：循环链表尾结点的next指向头结点。约瑟夫环问题（n个人围成圈，从第s个人开始报数，报到m的人出列）是循环链表的经典应用题，需注意指针移动和结点删除的逻辑。}}

{{知识点5}}{{栈和队列的应用：栈的应用包括括号匹配、表达式求值（后缀表达式）、递归转非递归、迷宫求解。队列的应用包括BFS、操作系统中的进程就绪队列、缓冲区管理。常考用栈模拟递归的过程分析。}}

{{知识点6}}{{二叉树遍历序列还原二叉树：已知先序+中序可唯一确定二叉树，方法是先序第一个为根，在中序中找到根的位置划分左右子树，递归处理。已知后序+中序同理，后序最后一个为根。先序+后序不能唯一确定二叉树（除非是满二叉树）。这是408综合题高频考点。}}

{{知识点7}}{{二叉排序树（BST）的查找、插入和删除：查找效率取决于树的高度，最好O(logn)，最坏O(n)（退化为链表）。插入操作沿查找路径找到叶结点位置插入。删除操作：叶子直接删，只有一个孩子用孩子替代，有两个孩子用中序前驱或后继替代。}}

{{知识点8}}{{平衡二叉树（AVL）的旋转操作：LL型→右旋，RR型→左旋，LR型→先左旋后右旋，RL型→先右旋后左旋。旋转操作是选择题和综合题的重点，需手工模拟旋转过程。平衡因子定义为左子树高度减右子树高度。}}

{{知识点9}}{{赫夫曼树与赫夫曼编码：赫夫曼树是带权路径长度（WPL）最小的二叉树。构造方法：每次选两个权值最小的结点合并。赫夫曼编码是前缀编码，用于数据压缩。常考WPL计算和编码/解码过程。注意：赫夫曼树中没有度为1的结点。}}

{{知识点10}}{{图的存储结构：邻接矩阵适合稠密图，空间O(n²)；邻接表适合稀疏图，空间O(n+e)。邻接矩阵中第i行非零元素个数=顶点i的出度（有向图）或度（无向图）。十字链表适合有向图，邻接多重表适合无向图。}}

{{知识点11}}{{DFS与BFS遍历：DFS使用栈（递归实现），深度优先，适合判断连通性、拓扑排序；BFS使用队列，广度优先，适合求最短路径（无权图）。给定图的邻接矩阵或邻接表，能手工模拟DFS和BFS序列。DFS生成树/森林与BFS生成树的区别。}}

{{知识点12}}{{最小生成树算法：Prim算法从一个顶点出发，逐步加入最小权边（适合稠密图，O(n²)）；Kruskal算法按边权排序逐边加入（适合稀疏图，O(eloge)）。需手工模拟两种算法的执行过程，判断哪些边被选中。}}

{{知识点13}}{{Dijkstra最短路径算法：求单源最短路径，不能处理负权边。逐步扩展已确定最短路径的顶点集合。每次选距离源点最近的未确定顶点，用它更新邻居的距离。需手工模拟每一步的dist和path数组变化。时间复杂度O(n²)。}}

{{知识点14}}{{拓扑排序与关键路径：拓扑排序适用于有向无环图（DAG），结果不唯一。AOE网中关键路径是从源点到汇点的最长路径，决定工程最短工期。求关键路径需计算事件最早/最迟发生时间和活动最早/最迟开始时间。关键活动的时间余量为0。}}

{{知识点15}}{{各排序算法的时间复杂度与稳定性：希尔排序、快速排序、堆排序、选择排序是不稳定的；插入排序、冒泡排序、归并排序、基数排序是稳定的。最好/最坏/平均情况的时间复杂度需牢记。空间复杂度：归并O(n)，快速O(logn)~O(n)，其余多为O(1)。}}

{{知识点16}}{{快速排序的分区过程：选枢轴元素，将小于枢轴的放左边、大于的放右边。Lomuto分区和Hoare分区的区别。快速排序平均时间O(nlogn)，最坏O(n²)（已有序时）。优化方法：三数取中选枢轴。综合题常考某趟排序结果的判断。}}

{{知识点17}}{{堆排序的建堆与调整：大根堆用于升序排序，小根堆用于降序排序。建堆过程从最后一个非叶子结点开始向下调整，时间O(n)。每趟取出堆顶后将最后一个元素放到堆顶再向下调整，时间O(logn)。总时间O(nlogn)。常考手工建堆过程。}}

{{知识点18}}{{归并排序的合并过程：将两个有序子序列合并为一个有序序列。需额外空间O(n)。二路归并每趟将相邻的有序段两两合并。时间复杂度始终为O(nlogn)。综合题可能考多路归并或归并排序与快速排序的比较。}}

{{知识点19}}{{折半查找与判定树：折半查找要求序列有序且支持随机存取（顺序表）。判定树的构造方法。ASL计算：成功ASL=各层比较次数×该层结点数之和/总结点数。折半查找时间O(logn)。判定树的形态与查找序列的有序性对应。}}

{{知识点20}}{{B树与B+树：m阶B树每个结点最多m个孩子、最少⌈m/2⌉个孩子（根结点除外）。3阶B树（2-3树）插入删除操作最常考。插入导致结点满时需分裂，删除导致结点关键字不足时需借或合并。B+树所有关键字都在叶子结点，叶子结点形成有序链表。}}

{{知识点21}}{{散列表的构造与查找：散列函数常用除留余数法。冲突处理：线性探测法（容易产生堆积）、链地址法（不会堆积）。装填因子α=表中记录数/表长。ASL计算是高频考点。线性探测法的查找成功和查找失败ASL计算方法不同。}}

{{知识点22}}{{定点数的表示与运算：原码、反码、补码、移码的转换规则。正数的原码=反码=补码；负数的反码=符号位不变其余取反，补码=反码+1。补码加减法：[A-B]补=[A]补+[-B]补。溢出判断：一位符号位时用双符号位（模4补码）判断。}}

{{知识点23}}{{IEEE 754浮点数表示：单精度32位=1位符号S+8位阶码E（移码表示，偏移量127）+23位尾数M（隐含最高位1）。真值=(-1)^S × 1.M × 2^(E-127)。特殊值：E=0,M=0表示±0；E=255,M=0表示±∞；E=255,M≠0表示NaN。双精度64位，偏移量1023。}}

{{知识点24}}{{海明码与CRC校验码：海明码检错纠错，r个校验位可检测2^r-1位数据中的错误。校验位放在2的幂次位置。CRC校验码通过模2除法求余数得到，余数作为校验码附加在数据后面。CRC只能检错不能纠错（配合ARQ协议可纠错）。}}

{{知识点25}}{{Cache的映射方式与替换策略：直接映射（每个主存块只能映射到固定的Cache行）、全相联映射（可映射到任意Cache行）、组相联映射（折中方案）。替换算法：LRU（最近最少使用）最常考。Cache命中率和平均访问时间的计算公式：t_avg=ht_c+(1-h)t_m。}}

{{知识点26}}{{Cache地址划分与命中判断：主存地址=标记+行号/组号+块内地址。直接映射：行号位数=log2(Cache行数)；组相联：组号位数=log2(组数)，组内行数=相联度。判断是否命中需比较标记位。综合题常考给定访问序列判断命中/缺失情况。}}

{{知识点27}}{{虚拟存储器与TLB：虚拟地址→查TLB→命中则直接得物理地址；未命中则查页表→可能缺页→从外存调入。TLB是页表的高速缓存。多级页表减少页表占用的内存空间。地址转换过程是综合题高频考点，需注意TLB、Cache、主存的层次关系。}}

{{知识点28}}{{指令格式与寻址方式：指令=操作码+地址码。定长操作码和扩展操作码的区别。常见寻址方式：立即寻址、直接寻址、间接寻址、寄存器寻址、寄存器间接寻址、基址寻址、变址寻址、相对寻址。有效地址的计算方法。变址寻址适合数组操作，基址寻址适合程序浮动。}}

{{知识点29}}{{CISC与RISC对比：CISC指令系统复杂，指令长度可变，寻址方式多，微程序控制；RISC指令系统精简，指令长度固定，寻址方式少，硬布线控制。RISC采用流水线技术，CISC难以流水。现代处理器多采用RISC思想。}}

{{知识点30}}{{指令流水线与冲突：流水线吞吐率=n/(k+n-1)×时钟频率，加速比≈k（理想情况）。三种冲突：结构冲突（资源竞争）、数据冲突（RAW/WAR/WAW）、控制冲突（分支指令）。解决方法：数据前推、分支预测、插入气泡。流水线性能分析是选择题高频。}}

{{知识点31}}{{总线与总线仲裁：总线带宽=总线宽度×总线频率。同步总线和异步总线的区别。总线仲裁方式：链式查询（对电路故障敏感）、计数器定时查询、独立请求（响应最快）。三种方式的优缺点对比是选择题常考。}}

{{知识点32}}{{I/O方式对比：程序查询方式CPU利用率低，程序中断方式CPU与外设可并行，DMA方式以数据块为单位传送。DMA的三种传送方式：停止CPU访存、周期挪用、交替访存。DMA传送过程：预处理→数据传送→后处理。中断响应过程和中断优先级。}}

{{知识点33}}{{进程状态与转换：就绪→运行（调度）、运行→就绪（时间片到）、运行→阻塞（请求I/O）、阻塞→就绪（I/O完成）、创建→就绪、运行→终止。五状态模型和七状态模型（加入挂起状态）。进程状态转换图是选择题必考。}}

{{知识点34}}{{处理机调度算法：FCFS（先来先服务，对短作业不利）、SJF（最短作业优先，平均等待时间最短）、RR（时间片轮转，时间片太大退化为FCFS，太小上下文切换开销大）、优先级调度（可能饥饿）、多级反馈队列（综合性能好）。各算法的手工模拟和甘特图绘制。}}

{{知识点35}}{{死锁与银行家算法：死锁四个必要条件：互斥、请求与保持、不可剥夺、循环等待。预防方法破坏任一条件。避免方法：银行家算法。安全性判断：找一个安全序列使每个进程都能完成。常考给定资源分配矩阵，判断系统是否安全，某请求能否分配。}}

{{知识点36}}{{信号量与PV操作：P操作使信号量减1，若小于0则阻塞；V操作使信号量加1，若小于等于0则唤醒一个进程。经典同步互斥问题：生产者-消费者（互斥+同步）、读者-写者（读写互斥+写写互斥）、哲学家进餐（避免死锁的策略）。综合题高频。}}

{{知识点37}}{{页式存储管理：虚拟地址=页号+页内偏移。页表存储页号到物理块号的映射。地址转换：页号查页表得物理块号，物理地址=物理块号×页面大小+页内偏移。分页不会产生外部碎片，但可能有内部碎片。多级页表和反置页表的概念。}}

{{知识点38}}{{页面置换算法：FIFO（先进先出，可能Belady异常）、LRU（最近最少使用，不会Belady异常）、CLOCK（近似LRU，用访问位）。手工模拟给定页面引用串，计算缺页次数和缺页率。分配的物理块数增加时，FIFO可能缺页率反而增加。}}

{{知识点39}}{{磁盘调度算法：FCFS（公平但效率低）、SSTF（最短寻道优先，可能饥饿）、SCAN（电梯算法，来回扫描）、C-SCAN（单向扫描，更公平）。计算各算法的总寻道长度。磁盘访问时间=寻道时间+旋转延迟+传输时间。常考手工模拟和比较。}}

{{知识点40}}{{文件物理结构：连续分配（支持随机访问，有外部碎片）、链接分配（无外部碎片，不支持随机访问）、索引分配（支持随机访问，需索引表）。混合索引（直接地址+一级间接+二级间接）的文件最大长度计算。UNIX的i-node结构是索引分配的典型。}}

{{知识点41}}{{CSMA/CD协议与以太网：CSMA/CD用于有线以太网。最小帧长=2×传播延迟×数据传输速率（保证冲突检测）。以太网帧格式：目的MAC(6)+源MAC(6)+类型(2)+数据(46~1500)+FCS(4)。最短帧长64字节。交换机基于MAC地址表转发帧，支持自学习。}}

{{知识点42}}{{IP地址与子网划分：A类(1~126)、B类(128~191)、C类(192~223)。CIDR无类域间路由：网络前缀/主机号。子网掩码与IP地址按位与得网络地址。广播地址=主机号全1。子网划分的计算题是408网络大题高频，需根据主机数确定子网位数。}}

{{知识点43}}{{IP数据报分片：当数据报长度超过MTU时需分片。标识字段相同表示同一数据报的分片。标志位MF=1表示还有分片，DF=1禁止分片。片偏移以8字节为单位。各分片的总长度、MF、DF、片偏移的计算是常考计算题。}}

{{知识点44}}{{TCP三次握手与四次挥手：三次握手：SYN→SYN+ACK→ACK。四次挥手：FIN→ACK→FIN→ACK。状态变化：CLOSED→SYN_SENT→ESTABLISHED→FIN_WAIT_1→FIN_WAIT_2→TIME_WAIT→CLOSED。TIME_WAIT等待2MSL的原因（确保ACK到达，防止旧报文干扰）。}}

{{知识点45}}{{TCP拥塞控制：慢开始（cwnd指数增长）、拥塞避免（cwnd线性增长）、快重传（收到3个重复ACK立即重传）、快恢复（ssthresh减半，cwnd=ssthresh）。cwnd和ssthresh的数值变化过程是综合题高频，需画出窗口变化图。超时→慢开始，3个重复ACK→快恢复。}}

{{知识点46}}{{TCP流量控制与滑动窗口：接收方通过rwnd通告接收窗口大小。发送方发送窗口=min(cwnd, rwnd)。停等协议窗口大小1，GBN协议发送窗口>1接收窗口=1，SR协议发送窗口>1接收窗口>1。序号位数n时，GBN窗口≤2^n-1，SR窗口≤2^(n-1)。}}

{{知识点47}}{{DNS域名解析：递归查询（本地DNS代替主机查询）和迭代查询（本地DNS逐级查询）。域名从右到左：顶级域→二级域→三级域。DNS使用UDP协议，端口号53。正向解析（域名→IP）和反向解析（IP→域名）。常用记录类型：A、CNAME、MX、NS。}}

{{知识点48}}{{HTTP协议与应用层协议：HTTP是无状态协议，使用TCP端口80。HTTPS使用TLS加密，端口443。请求方法：GET、POST、PUT、DELETE。状态码：2xx成功、3xx重定向、4xx客户端错误、5xx服务器错误。SMTP(25)发邮件，POP3(110)/IMAP收邮件，FTP(20数据/21控制)文件传输。}}

{{知识点49}}{{数据结构综合题解题策略：链表算法题注意边界条件（空表、单元素、首尾元素）。二叉树题注意递归结构和遍历序列的灵活运用。图的算法题注意手工模拟步骤的完整性。排序题注意各趟结果的判断方法。所有算法题需先写思路再写代码，注释关键步骤。}}

{{知识点50}}{{组成原理综合题解题策略：浮点数运算题按"对阶→尾数运算→规格化→舍入→溢出判断"步骤进行。Cache题先划分地址位，再逐个判断命中。流水线题画时空图分析。指令格式题先确定操作码长度和各字段含义。所有计算题注意单位换算（KB、MB、bit、Byte）。}}
