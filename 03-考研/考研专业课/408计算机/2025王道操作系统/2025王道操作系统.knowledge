# 2025王道操作系统
# 领域: 考研
# 学科: 408计算机/操作系统
# 来源: 2025王道考研操作系统
# 描述: 408统考操作系统核心知识点，涵盖进程管理、内存管理、文件系统、I/O管理等

{{操作系统基本概念}}{{操作系统是控制和管理计算机硬件与软件资源、合理组织计算机工作流程、方便用户使用的程序集合。基本特征：并发、共享、虚拟、异步。基本功能：处理机管理、存储器管理、文件管理、设备管理、用户接口。}}

{{操作系统的分类}}{{批处理操作系统（单道/多道）、分时操作系统（多路性、独立性、及时性、交互性）、实时操作系统（及时性、可靠性）、网络操作系统、分布式操作系统。微内核结构将基本功能移入内核，其他功能作为用户态服务进程运行。}}

{{进程与程序的区别}}{{进程是程序的一次执行实例，是动态的、有生命周期的；程序是静态的代码集合。进程是资源分配和调度的基本单位，具有并发性、独立性、异步性、结构性（PCB+程序段+数据段）。一个程序可对应多个进程。}}

{{进程控制块PCB}}{{PCB是进程存在的唯一标志，包含：进程标识符PID、进程状态、程序计数器、寄存器值、内存管理信息、I/O状态信息、记账信息等。系统通过PCB表（数组或链表）管理所有进程。}}

{{进程的状态转换}}{{五状态模型：创建→就绪→运行→阻塞→终止。就绪→运行（调度/分派）、运行→就绪（时间片用完）、运行→阻塞（等待事件）、阻塞→就绪（事件发生）、运行→终止。注意：不能由阻塞直接到运行。}}

{{进程的创建与终止}}{{创建过程：分配PCB、分配资源、初始化PCB、插入就绪队列。引起创建的事件：用户登录、作业调度、提供服务、应用请求。终止过程：正常结束、异常结束、外界干预。终止后回收资源、归还PCB。}}

{{进程通信方式}}{{共享存储（共享内存区）、消息传递（直接通信/间接通信即信箱）、管道通信（pipe，半双工，pipe文件在内存中）。共享存储需要同步互斥机制；消息传递由操作系统提供发送/接收原语。}}

{{线程的基本概念}}{{线程是CPU调度的基本单位，进程是资源分配的基本单位。同一进程的线程共享进程的地址空间和资源，但有独立的栈、寄存器、程序计数器。线程切换开销小于进程切换。用户级线程与内核级线程的映射模型：多对一、一对一、多对多。}}

{{处理机调度层次}}{{高级调度（作业调度/长程调度）：从外存选作业调入内存创建进程。中级调度（内存调度）：将进程挂起/解挂以调节内存中的进程数。低级调度（进程调度/短程调度）：从就绪队列选进程分配CPU。}}

{{调度算法FCFS与SJF}}{{FCFS（先来先服务）：非抢占式，对长作业有利，对短作业不利，不利于I/O繁忙型作业。SJF（短作业优先）：平均等待时间最短，但可能导致长作业饥饿；抢占式SJF即SRTF（最短剩余时间优先）。}}

{{时间片轮转与优先级调度}}{{RR（时间片轮转）：就绪队列按FCFS排列，每个进程运行一个时间片后被抢占。时间片太大退化为FCFS，太小上下文切换开销过大。优先级调度：静态优先级/动态优先级，可抢占/非抢占，可能导致低优先级饥饿，可用老化技术解决。}}

{{多级反馈队列调度}}{{多级反馈队列调度综合了RR和优先级调度。设置多个就绪队列，优先级依次降低，时间片依次增大。新进程进入最高优先级队列；若时间片内未完成则降到下一级队列。高优先级队列空时才调度低优先级队列。可能导致低优先级饥饿。}}

{{进程同步的基本概念}}{{临界资源：一次只允许一个进程使用的资源。临界区：访问临界资源的代码段。同步机制应遵循：空闲让进、忙则等待、有限等待、让权等待。同步与互斥的区别：同步是协作关系，互斥是竞争关系。}}

{{软件实现方法}}{{Peterson算法：设置flag数组和turn变量，适用于两个进程。皮特森算法满足互斥、前进、有限等待。但不适用于多进程场景。硬件方法：关中断（仅适用于内核）、TS指令（Test-and-Set）、Swap指令、CAS操作。硬件方法简单但不满足"让权等待"。}}

{{信号量机制}}{{信号量S是一个整数，P操作（wait）：S=S-1，若S<0则阻塞；V操作（signal）：S=S+1，若S≤0则唤醒一个等待进程。整型信号量不满足"让权等待"；记录型信号量通过阻塞队列实现让权等待。利用信号量实现互斥：mutex初值为1；实现同步：信号量初值为0。}}

{{经典同步问题之生产者消费者}}{{n个缓冲区的生产者-消费者问题需要三个信号量：mutex=1（互斥访问缓冲区）、empty=n（空缓冲区数）、full=0（满缓冲区数）。生产者：P(empty)→P(mutex)→放入→V(mutex)→V(full)。消费者：P(full)→P(mutex)→取出→V(mutex)→V(empty)。注意P(empty)和P(mutex)的顺序不能颠倒。}}

{{经典同步问题之读者写者}}{{读者-写者问题：读进程可共享读，写进程需独占。解决方案：读写者优先权不同。读优先：readcount计数，第一个读者P(wmutex)锁写，最后一个读者V(wmutex)解锁。写者：P(wmutex)进入写。可能导致写者饥饿。}}

{{死锁的概念与条件}}{{死锁：多个进程因竞争资源而造成的一种互相等待的僵局。死锁的四个必要条件：互斥条件、不剥夺条件、请求和保持条件、循环等待条件。四个条件同时成立才可能发生死锁。}}

{{死锁的处理策略}}{{预防：破坏四个必要条件之一（如资源一次性分配、可剥夺、有序分配）。避免：银行家算法，在分配前检查系统是否处于安全状态。检测与解除：资源分配图、死锁定理，检测到死锁后通过剥夺资源或撤销进程来解除。}}

{{银行家算法}}{{银行家算法核心数据结构：Available（可用资源向量）、Max（最大需求矩阵）、Allocation（已分配矩阵）、Need（还需要矩阵）。安全性算法：找一个Need≤Work的进程，加入安全序列，释放其资源更新Work，重复直到所有进程都加入则安全，否则不安全。}}

{{内存管理基本概念}}{{逻辑地址（相对地址/虚拟地址）与物理地址（绝对地址）。程序装入方式：绝对装入、可重定位装入（静态重定位）、动态运行时装入（动态重定位）。程序链接方式：静态链接、装入时动态链接、运行时动态链接。}}

{{内存连续分配方式}}{{单一连续分配：内存分为系统区和用户区，无外部碎片。固定分区分配：将用户区分为固定大小的分区，有内部碎片。动态分区分配：按进程需要动态分配，有外部碎片。动态分配算法：首次适应FF、最佳适应BF、最差适应WF、邻近适应NF。}}

{{基本分页存储管理}}{{将内存和进程都分为大小相等的页/块。逻辑地址=页号+页内偏移。页表：记录页号到物理块号的映射。地址变换机构根据页表将逻辑地址转为物理地址。快表TLB是页表的高速缓存，可加速地址转换。两级页表减少页表占用的连续内存。}}

{{基本分段存储管理}}{{按程序逻辑结构分为若干段，每段长度不等。段表记录段号→段基址+段长。分段便于共享和保护，但产生外部碎片。分页对用户透明，分段对用户可见。段页式管理：先分段再分页，兼具两者优点。}}

{{虚拟内存的概念}}{{虚拟内存基于局部性原理（时间局部性和空间局部性），允许程序的部分装入内存即可运行。虚拟内存容量=min(内存+外存, 2^地址位数)。实现方式：请求分页、请求分页式。特征：多次性、对换性、虚拟性。}}

{{请求分页存储管理}}{{页表增加：状态位、访问字段、修改位、外存地址。缺页中断：访问的页不在内存时产生缺页中断，由操作系统将该页从外存调入内存。若内存满则需页面置换。地址变换：先查快表→查页表→若在内存则访问→若不在则缺页中断。}}

{{页面置换算法}}{{最佳置换OPT：淘汰最长时间不被访问的页面（理论最优，不可实现）。先进先出FIFO：淘汰最先进入的页面，可能Belady异常。最近最久未使用LRU：淘汰最长时间未被访问的页面，用栈或计数器实现。时钟CLOCK：访问位为0则淘汰，为1则置0并指针下移。改进CLOCK：考虑修改位。}}

{{页面分配策略}}{{固定分配与可变分配、局部置换与全局置换。抖动（颠簸）：页面频繁调入调出，CPU利用率急剧下降。工作集：某段时间内进程实际访问的页面集合。工作集窗口大小为Δ时，工作集W(t,Δ)为t时刻前Δ次访问涉及的页面集。驻留集大小应≥工作集大小。}}

{{文件系统基本概念}}{{文件是具有符号名的一组相关信息的集合。文件的逻辑结构：无结构文件（流式文件）、有结构文件（记录式文件）：顺序文件、索引文件、索引顺序文件、直接文件（散列文件）。文件目录：FCB包含文件名、物理地址、权限等信息。}}

{{文件的物理结构}}{{连续分配：文件占连续的物理块，顺序和随机访问都快，但有外部碎片且不易扩展。链接分配：物理块链接成链，隐式链接/显式链接（FAT），无外部碎片但只能顺序访问。索引分配：索引表记录各逻辑块对应的物理块，支持随机访问；多级索引和混合索引处理大文件。}}

{{目录管理与文件共享}}{{单级目录、两级目录、树形目录、无环图目录（支持共享）。文件共享方式：硬链接（多个目录指向同一inode，链接计数）、符号链接（创建新文件存放目标路径）。文件保护：访问控制列表ACL、用户分类（Owner/Group/Other）。}}

{{磁盘调度算法}}{{磁盘访问时间=寻道时间+旋转延迟+传输时间。FCFS：公平但效率低。SSTF（最短寻道时间优先）：可能导致饥饿。SCAN（电梯算法）：单向扫描到尽头再反向。C-SCAN：单向扫描到尽头后直接回到另一端。LOOK/C-LOOK：改进SCAN/CSCAN，不必到尽头即反向/回起点。}}

{{I/O管理概述}}{{I/O设备分类：块设备（磁盘）、字符设备（键盘）。I/O控制方式：程序直接控制（CPU不断轮询）、中断驱动（CPU发出命令后处理其他事，中断时处理I/O）、DMA（直接内存访问，以数据块为单位，仅开始和结束时CPU干预）、通道（专用I/O处理器，通道程序控制）。}}

{{缓冲管理}}{{缓冲区的作用：缓和CPU与I/O设备速度不匹配、减少CPU中断频率、提高CPU与I/O并行性。缓冲技术：单缓冲、双缓冲、循环缓冲、缓冲池。缓冲池管理：空缓冲队列、输入队列、输出队列。SPOOLing技术利用缓冲和共享设备模拟独占设备实现虚拟设备。}}

{{SPOOLing假脱机技术}}{{SPOOLing组成：输入井和输出井（磁盘上）、输入缓冲区和输出缓冲区（内存中）、输入进程和输出进程。核心思想：将独占设备改造成共享设备。典型应用：共享打印机，各进程输出到输出井，由SPOOLing系统依次打印。}}

{{文件保护机制}}{{访问控制：为每个文件维护访问控制列表ACL，指定各用户/组的访问权限（读、写、执行、删除等）。口令保护：文件创建时设置口令。密码保护：对文件内容加密。UNIX系统将用户分为文件主、同组用户、其他用户三类，每类赋予rwx权限。}}
