# 2025王道操作系统
# 领域: 考研
# 学科: 408计算机
# 来源: 王道操作系统
# 描述: 考研408操作系统核心知识点

{{知识点1}}{{操作系统的定义与功能：操作系统是控制和管理计算机硬件与软件资源、合理组织计算机工作流程、为用户和应用程序提供接口的系统软件。四大功能：处理机管理、存储器管理、文件管理、设备管理。}}

{{知识点2}}{{操作系统的特征——并发、共享、虚拟、异步：并发指多个程序在同一时间间隔内交替执行；共享指系统资源供多个进程共用，分为互斥共享和同时访问；虚拟指将物理实体变为逻辑对应物；异步指进程以不可预知的速度推进。}}

{{知识点3}}{{操作系统的发展阶段：手工操作→批处理（单道/多道）→分时系统→实时系统→网络操作系统→分布式操作系统。多道批处理是现代操作系统的基础。}}

{{知识点4}}{{操作系统的运行机制与体系结构：内核态与用户态的区分。核心态执行特权指令，用户态执行非特权指令。大内核与微内核的对比：大内核效率高但结构复杂，微内核结构清晰但性能有损失。}}

{{知识点5}}{{中断与异常：中断（外中断）来自CPU外部，如I/O中断、时钟中断；异常（内中断/陷入）来自CPU内部，如缺页异常、除零错误、系统调用。中断是操作系统获得控制权的基本手段。}}

{{知识点6}}{{系统调用：用户程序通过系统调用请求操作系统内核服务。系统调用是用户态转向内核态的唯一途径（除异常和外部中断）。包括进程控制、文件操作、设备管理、通信等类系统调用。}}

{{知识点7}}{{进程的定义与特征：进程是程序的一次动态执行过程，是系统资源分配和调度的基本单位。特征：动态性（最基本）、并发性、独立性、异步性、结构性。进程由PCB、程序段、数据段组成。}}

{{知识点8}}{{进程控制块（PCB）：PCB是进程存在的唯一标志。包含进程描述信息、进程控制和管理信息、资源分配清单、处理机状态信息。系统通过PCB表（数组或链表）管理所有进程。}}

{{知识点9}}{{进程状态与转换：三态模型——就绪态、运行态、阻塞态。五态模型增加创建态和终止态。转换关系：就绪→运行（调度）、运行→就绪（时间片用完/被抢占）、运行→阻塞（等待事件）、阻塞→就绪（事件发生）。}}

{{知识点10}}{{进程控制——创建、终止、阻塞、唤醒：创建进程（fork）需分配PCB、分配资源、初始化PCB、插入就绪队列。进程终止需回收资源、撤销PCB。阻塞是进程自身主动行为，唤醒是被动的。}}

{{知识点11}}{{进程间通信——共享存储、消息传递、管道通信：共享存储区需同步互斥控制；消息传递分为直接通信和间接通信（信箱）；管道是连接读写进程的共享文件，半双工通信。}}

{{知识点12}}{{线程的基本概念：线程是CPU调度的基本单位，进程是资源分配的基本单位。同一进程的线程共享进程资源。用户级线程切换不需内核支持，开销小；内核级线程由内核管理，是CPU调度的实际单位。}}

{{知识点13}}{{处理机调度的层次：高级调度（作业调度/长程调度）——从后备队列选作业调入内存；中级调度（内存调度）——将进程调出到外存（挂起）；低级调度（进程调度/短程调度）——从就绪队列选进程分配CPU。}}

{{知识点14}}{{进程调度的时机与方式：不能调度的情况：中断处理中、在原子操作中。调度方式：非剥夺式（非抢占式）和剥夺式（抢占式）。抢占式需遵循优先权原则、短进程优先原则、时间片原则。}}

{{知识点15}}{{调度算法——FCFS与SJF：先来先服务（FCFS）对长作业有利，对短作业不利，有利于CPU繁忙型。短作业优先（SJF）平均等待时间最短，但可能导致长作业饥饿，不可抢占版本对用户不友好。}}

{{知识点16}}{{调度算法——优先级调度与时间片轮转：优先级调度分为静态和动态优先级，可抢占或非抢占。时间片轮转中时间片大小影响系统效率：太大退化为FCFS，太小切换开销过大。}}

{{知识点17}}{{调度算法——多级反馈队列调度算法：设置多个就绪队列，各队列优先级递减、时间片递增。新进程进入第一队列末尾，若时间片内未完成则降到下一队列。高优先级队列为空时才调度低优先级队列。能较好地满足各种类型进程。}}

{{知识点18}}{{进程同步的基本概念：临界资源——一次仅允许一个进程使用的资源。临界区——访问临界资源的代码段。同步需遵循空闲让进、忙则等待、有限等待、让权等待四个原则。}}

{{知识点19}}{{软件实现方法——Peterson算法和Dekker算法：Peterson算法使用flag和turn两个变量，实现进程对临界区的互斥访问。在双进程场景下满足互斥、前进和有限等待条件，但不满足让权等待。}}

{{知识点20}}{{硬件实现方法——TestAndSet与Swap指令：TSL指令（TestAndSet）是原子操作，读取旧值并设置新值。Swap指令交换两个变量值。硬件方法简单高效，但不满足让权等待，可能导致忙等。}}

{{知识点21}}{{信号量机制——整型信号量与记录型信号量：整型信号量仅有wait(S)和signal(S)操作，不满足让权等待。记录型信号量引入进程链表（阻塞队列），当资源不足时进程被阻塞并加入等待队列，满足让权等待。}}

{{知识点22}}{{利用信号量实现互斥与同步：互斥——在临界区前后加P(mutex)和V(mutex)，mutex初值为1。同步——用信号量确保执行顺序，如生产者消费者问题中，full和empty信号量控制缓冲区使用。}}

{{知识点23}}{{经典同步问题——生产者消费者问题：需三个信号量：mutex（互斥，初值1）、full（产品数，初值0）、empty（空缓冲区数，初值n）。注意P操作顺序不能颠倒（先P(empty)后P(mutex)），否则可能死锁。}}

{{知识点24}}{{经典同步问题——读者写者问题：读者优先策略：第一个读者需P(wmutex)互斥写者，后续读者只需计数。写者需等所有读者退出。可能导致写者饥饿。改进：加一个信号量限制读者数量。}}

{{知识点25}}{{经典同步问题——哲学家进餐问题：解决方案包括：至多允许四人同时拿起筷子；奇数号先拿左偶数号先拿右筷子；同时拿到两根筷子才吃（AND信号量）。核心是避免循环等待导致死锁。}}

{{知识点26}}{{管程机制：管程将共享数据及操作封装在一起，一次仅允许一个进程在管程内执行。解决信号量编写困难、易出错的问题。管程由共享数据结构、初始化代码、过程（函数）组成。引入条件变量实现同步。}}

{{知识点27}}{{死锁的概念与必要条件：死锁是多个进程因竞争资源而互相等待的僵局。四个必要条件（缺一不可）：互斥条件、不剥夺条件、请求并保持条件、循环等待条件。}}

{{知识点28}}{{死锁的预防——破坏四个必要条件：破坏互斥（使用可共享资源）、破坏不剥夺（允许剥夺资源）、破坏请求并保持（一次性申请所有资源）、破坏循环等待（按编号顺序申请资源）。各方法均有局限性。}}

{{知识点29}}{{死锁的避免——银行家算法：银行家算法通过安全性检查避免系统进入不安全状态。核心数据结构：Available（可用资源）、Max（最大需求）、Allocation（已分配）、Need（还需要）。当请求≤Need且≤Available时，尝试分配并执行安全性算法检查。}}

{{知识点30}}{{死锁的检测与解除：资源分配图和死锁定理——简化资源分配图，若能完全简化则无死锁。解除方法：资源剥夺法、撤销进程法、回退进程法。通常选择代价最小的进程撤销。}}

{{知识点31}}{{内存管理的基本概念：程序装入方式——绝对装入（编译时确定地址）、可重定位装入（装入时重定位）、动态运行时装入（运行时重定位）。程序链接方式——静态链接、装入时动态链接、运行时动态链接。}}

{{知识点32}}{{内存保护：上下限寄存器法——用两个寄存器分别存放进程上下限地址。基址+限长寄存器法——基址寄存器存放起始地址，限长寄存器存放最大偏移量。每次访问地址都需检查是否越界。}}

{{知识点33}}{{连续分配管理方式——单一连续分配、固定分区分配、动态分区分配：单一连续分配只适用于单用户单任务。固定分区将内存划分为大小固定或不等的分区，存在内部碎片。动态分区按需分配，存在外部碎片，需要紧凑技术。}}

{{知识点34}}{{动态分区分配算法：首次适应（FF）——从头查找第一个满足要求的空闲分区，简单但低地址碎片多。最佳适应（BW）——找最小满足的分区，留下小碎片。最坏适应（WF）——找最大分区。邻近适应（NF）——从上次查找结束处开始。}}

{{知识点35}}{{基本分页存储管理：将进程和内存都分成等大的页和页框（帧）。通过页表映射逻辑地址到物理地址。逻辑地址=页号+页内偏移，物理地址=页框号×页大小+页内偏移。优点：无外部碎片，缺点：页内碎片。}}

{{知识点36}}{{页表与地址变换机构：页表项包含页号和块号（页框号）。地址变换需两次访存（一次查页表，一次取数据）。用快表（TLB/联想寄存器）加速，命中时只需一次访存。快表是相联存储器，按内容并行查找。}}

{{知识点37}}{{两级页表与多级页表：页表过大时将其再分页，建立页表的页表（外层页表）。逻辑地址结构变为：外层页号+页内页号+页内偏移。解决了大页表占用连续内存空间的问题。现代OS常用多级页表。}}

{{知识点38}}{{基本分段存储管理：按程序逻辑结构分段，每段有段名和段长。逻辑地址=段号+段内偏移。段表项：段号、段长、基址。与分页区别：分页是物理单位（用户不可见），分段是逻辑单位（用户可见）；页大小固定，段大小可变。}}

{{知识点39}}{{段页式存储管理：先分段再分页。逻辑地址=段号+页号+页内偏移。每个段对应一个页表。需三次访存（段表→页表→数据），可用TLB加速。兼具分段（逻辑清晰）和分页（内存利用率高）的优点。}}

{{知识点40}}{{虚拟存储器的基本概念：基于局部性原理（时间局部性和空间局部性），程序运行时仅需将部分页面装入内存。虚拟存储器特征：多次性、对换性、虚拟性。需硬件支持（一定容量内存+外存+地址变换机构）。}}

{{知识点41}}{{请求分页管理方式——页表机制：页表项新增：状态位（是否在内存）、访问字段（记录访问次数或时间）、修改位（是否被修改过）、外存地址。调页时缺页中断，由操作系统将所需页面从外存调入内存。}}

{{知识点42}}{{页面置换算法——最佳置换（OPT）与先进先出（FIFO）：OPT选择将来最长时间不被访问的页面淘汰，理论上缺页率最低但无法实现，用于评估其他算法。FIFO选择最早进入内存的页面，简单但性能差，存在Belady异常（分配更多页框缺页率反而增加）。}}

{{知识点43}}{{页面置换算法——LRU与Clock算法：LRU（最近最久未使用）选择最近最长时间未被访问的页面，需硬件支持（计数器或栈），无Belady异常。Clock（时钟）/NRU（最近未使用）算法：访问位为0时淘汰，为1时清0并指针后移，是LRU的近似。改进型Clock考虑修改位。}}

{{知识点44}}{{页面分配策略与调入策略：固定分配局部置换、可变分配全局置换等策略。调入策略：预调页（预测性调入，成功率约50%）和请求调页（缺页时调入，常用）。从何处调入：外存分为文件区和对换区，对换区速度更快。}}

{{知识点45}}{{抖动与工作集：抖动（颠簸）——页面频繁换入换出，CPU利用率急剧下降。原因：进程分配页框过少或进程过多。工作集——某段时间内进程访问的页面集合。驻留集应大于等于工作集，否则发生抖动。}}

{{知识点46}}{{文件的基本概念与分类：文件是具有符号名的相关信息集合。按用途分：系统文件、用户文件、库文件。按数据形式分：源文件、目标文件、可执行文件。按存取控制分：只读、读写、执行文件。文件系统是OS中管理文件的软件机构。}}

{{知识点47}}{{文件的逻辑结构：无结构文件（流式文件）——如Unix中的文件。有结构文件（记录式文件）——顺序文件、索引文件、索引顺序文件、散列文件。顺序文件适合批量处理，索引文件适合随机访问，索引顺序文件综合两者优点。}}

{{知识点48}}{{文件目录：FCB（文件控制块）包含文件名、类型、大小、位置、权限、时间等。目录结构：单级目录（不允许重名）、两级目录（主文件目录MFD+用户文件目录UFD）、树形目录（绝对路径和相对路径）、无环图目录（实现文件共享，需防止删除时悬空指针）。}}

{{知识点49}}{{文件的物理结构——连续分配：文件占据连续的磁盘块。优点：顺序和随机访问都快，实现简单。缺点：外部碎片严重，文件长度不易动态扩展，需紧凑（高开销）。类似内存的动态分区分配。}}

{{知识点50}}{{文件的物理结构——链接分配：隐式链接：每个盘块含指向下一个盘块的指针，只适合顺序访问。显式链接：将所有指针集中存放在文件分配表FAT中，支持随机访问。FAT是MS-DOS的核心数据结构。}}

{{知识点51}}{{文件的物理结构——索引分配：为每个文件建立索引表，记录各逻辑块对应的物理块号。支持随机访问。多级索引和混合索引（Unix的i节点）解决大文件问题。i节点直接地址+一次间接+二次间接+三次间接寻址。}}

{{知识点52}}{{文件存储空间管理：空闲表法——类似内存动态分区，适合连续分配。空闲链表法——将空闲盘块链接。位示图法——用二进制位表示盘块状态，1为空闲/0为已分配（或相反），简单高效。成组链接法——Unix使用，将空闲块分组管理。}}

{{知识点53}}{{文件保护——访问控制：访问控制表（ACL）——为每个文件设置允许访问的用户及权限。用户组划分——将用户分为Owner/Group/Others，分别设定权限。口令与密码保护方式。Unix使用9位权限位（rwxrwxrwx）。}}

{{知识点54}}{{文件系统的层次结构：用户接口→文件目录系统→存取控制验证→逻辑文件系统→物理文件系统→辅助分配模块→设备管理模块→设备。自上而下，各层分工明确。}}

{{知识点55}}{{磁盘的结构与调度：磁盘由盘面、磁道、扇区组成。磁盘地址=柱面号+盘面号+扇区号。访问时间=寻道时间+旋转延迟+传输时间。寻道时间占比最大，是优化重点。}}

{{知识点56}}{{磁盘调度算法：FCFS——公平但效率低。SSTF（最短寻道时间优先）——可能饥饿。SCAN（电梯算法）——方向一致扫描，无饥饿。C-SCAN（循环扫描）——只单向服务，返回时快速移动。LOOK和C-LOOK是改进版，不必到达磁盘端点。}}

{{知识点57}}{{磁盘管理——磁盘初始化与引导：低级格式化（物理格式化）→磁盘分区→逻辑格式化。引导块（引导扇区/MBR）存放自举程序，开机时BIOS读取引导块将OS装入内存。}}

{{知识点58}}{{I/O管理的基本概念：I/O设备分类——按特性分块设备和字符设备；按传输速率分低速、中速、高速设备。I/O控制方式：程序直接控制→中断驱动→DMA→通道控制。效率逐步提升，CPU干预逐步减少。}}

{{知识点59}}{{I/O控制方式——程序直接控制与中断驱动：程序直接控制：CPU轮询检查设备状态，CPU利用率极低。中断驱动：设备就绪后发中断通知CPU，CPU以字（节）为单位传输，每传一次中断一次，仍有较多CPU开销。}}

{{知识点60}}{{I/O控制方式——DMA与通道：DMA（直接内存访问）：以数据块为单位传输，CPU仅在开始和结束时干预。需DMA控制器。通道：专用处理器执行通道程序，CPU只需启动通道，通道自主完成一组读写操作。效率最高。}}

{{知识点61}}{{I/O软件层次结构：用户层I/O软件→设备独立性软件→设备驱动程序→中断处理程序→硬件。设备独立性使程序与具体设备无关，通过逻辑设备名映射物理设备。设备驱动程序是OS与硬件之间的桥梁。}}

{{知识点62}}{{SPOOLing技术（假脱机技术）：利用磁盘作为缓冲，将独占设备改造为共享设备。核心组成：输入井/输出井（磁盘）、输入缓冲区/输出缓冲区（内存）、输入进程/输出进程。典型应用：打印机的SPOOLing。}}

{{知识点63}}{{缓冲区管理：单缓冲——CPU和设备交替使用缓冲区。双缓冲——CPU和设备可并行。循环缓冲——多个缓冲区循环使用。缓冲池——系统公用缓冲区队列（空缓冲队列、输入队列、输出队列），由Getbuf和Putbuf过程管理。}}

{{知识点64}}{{设备分配与回收：设备分配需考虑：设备固有属性（独占/共享/虚拟）、设备分配算法、安全性（安全分配/不安全分配）。数据结构：SDCT（系统设备表）、DCT（设备控制表）、COCT（控制器控制表）、CHCT（通道控制表），形成四级层次结构。}}

{{知识点65}}{{进程的状态转换综合辨析：运行态→阻塞态是进程主动行为（等待I/O等）；运行态→就绪态是被动行为（时间片到/被抢占）。阻塞态不能直接转换为运行态，必须先经过就绪态。创建态和终止态是进程生命周期的起止。}}

{{知识点66}}{{线程的实现方式——用户级线程与内核级线程：用户级线程在用户空间管理，切换不需内核干预，但一个线程阻塞会导致整个进程阻塞。内核级线程由OS内核管理，是CPU调度的基本单位，一个线程阻塞不影响其他线程。组合方式：多对一、一对一、多对多。}}

{{知识点67}}{{文件系统的挂载与目录检索：文件系统需挂载（安装）到目录树才能使用。目录检索方法：线性检索法（顺序遍历目录项）和Hash法（根据文件名计算哈希值直接定位）。大规模文件系统使用B+树索引提高检索效率。}}

{{知识点68}}{{文件共享——硬链接与软链接：硬链接——多个目录项指向同一i节点（索引节点），删除原文件不影响链接（引用计数）。软链接（符号链接）——创建新文件存储目标文件路径，类似快捷方式，原文件删除则链接失效。}}

{{知识点69}}{{系统安全与访问控制矩阵：访问控制矩阵以行表示主体（用户/进程），列表示客体（文件/设备），矩阵元素表示访问权限。实际实现为访问控制表（ACL，按列存储）或能力表（按行存储）。两者各有适用场景。}}

{{知识点70}}{{操作系统引导过程：POST（加电自检）→BIOS读取MBR→MBR中的引导程序加载活动分区的引导扇区→引导程序加载OS内核→内核初始化。现代UEFI替代传统BIOS，支持安全启动和GPT分区表。}}
