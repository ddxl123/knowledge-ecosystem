# 2025王道计算机组成原理
# 领域: 考研
# 学科: 408计算机/计算机组成原理
# 来源: 2025王道考研计算机组成原理
# 描述: 408统考计算机组成原理核心知识点，涵盖数据表示、运算器、存储系统、指令系统、CPU、总线、I/O等

{{计算机系统概述}}{{计算机系统=硬件+软件。冯·诺依曼机特点：存储程序、五大部件（运算器、控制器、存储器、输入设备、输出设备）、指令和数据以二进制表示、指令由操作码和地址码组成、指令在存储器中按顺序存放。现代计算机以存储器为中心。}}

{{计算机性能指标}}{{机器字长：CPU一次能处理的数据位数。主存容量：MAR位数决定寻址范围×MDR位数。CPI：执行一条指令所需的时钟周期数。MIPS：每秒百万条指令。CPU执行时间=指令数×CPI×时钟周期。FLOPS：每秒浮点运算次数。吞吐量、响应时间。}}

{{数据的表示——进制与编码}}{{进制转换：十进制↔二进制↔八进制↔十六进制。BCD码：8421码、余3码、2421码。字符编码：ASCII（7位）、汉字GB2312/GBK/Unicode。字符串存储：大端序（高位字节在低地址）、小端序（低位字节在低地址）。校验码：奇偶校验、海明码、CRC码。}}

{{定点数的表示}}{{无符号数：所有位都表示数值。有符号数：原码（最高位符号位，其余数值位）、反码（正数同原码，负数数值位取反）、补码（正数同原码，负数反码+1）、移码（补码符号位取反）。补码0唯一表示，比原码多表示一个最小负数。范围：n位补码-2^(n-1)~2^(n-1)-1。}}

{{定点数的运算}}{{补码加减法：[A+B]补=[A]补+[B]补，[A-B]补=[A]补+[-B]补。溢出判断：单符号位（两个操作数同号结果异号则溢出）、双符号位（01正溢出/10负溢出）。移位运算：算术移位保持符号位不变。乘法：原码一位乘（移位相加）、补码Booth乘法。除法：恢复余数法、不恢复余数法（加减交替法）。}}

{{浮点数的表示与运算}}{{浮点数N=M×r^E，M为尾数，E为阶码，r为基数。IEEE 754标准：单精度32位（1+8+23）、双精度64位（1+11+52）。阶码用移码表示，尾数用原码表示，隐含最高位1。规格化：左规（尾数左移阶码减）、右规（尾数右移阶码加）。运算步骤：对阶→尾数运算→规格化→舍入→溢出判断。}}

{{海明码与CRC}}{{海明码：设k个数据位，r个校验位，满足2^r≥k+r+1。每个校验位覆盖特定的数据位。可检2位错、纠1位错。CRC循环冗余校验：将数据多项式除以生成多项式，余数作为校验码附加在数据后。CRC只能检错不能纠错（除非海明码），可检测所有奇数个错误和突发错误。}}

{{存储器概述}}{{按层次：寄存器→Cache→主存→外存。按存取方式：随机存取RAM、只读ROM、顺序存取（磁带）、直接存取（磁盘）。存储器容量=存储单元数×存储字长。存取时间、存储周期（存取时间+恢复时间）、带宽。主存容量取决于MAR位数和MDR位数。}}

{{SRAM与DRAM}}{{SRAM：用触发器存储，不需刷新，速度快，集成度低，用于Cache。DRAM：用电容存储，需定期刷新（集中刷新、分散刷新、异步刷新），速度慢，集成度高，用于主存。DRAM地址线复用，分行列两次传送。刷新周期一般2ms。双端口RAM和多模块存储器可提高访存速度。}}

{{主存容量扩展}}{{位扩展：增加存储字长（数据线），多个芯片并联。字扩展：增加存储字数（地址线），用片选信号选择芯片。字位同时扩展。地址分配：用高位地址线经译码器产生片选信号。存储器与CPU连接：地址线、数据线、读写控制线、片选信号线。}}

{{高速缓冲存储器Cache}}{{Cache位于CPU和主存之间，利用局部性原理（时间+空间）。命中率h，平均访问时间=h×t_c+(1-h)×t_m。Cache与主存的映射方式：直接映射（每个主存块只能映射到Cache的一个特定位置）、全相联映射（可映射到任意位置）、组相联映射（折中）。}}

{{Cache替换算法与写策略}}{{替换算法：随机替换、先进先出FIFO、最近最少使用LRU（最常用）、最不经常使用LFU。写策略：写直达（同时写Cache和主存，开销大但一致性好）、写回法（只写Cache，被替换时才写回主存，减少写操作但需脏位标记）。Cache容量小，需高效利用。}}

{{Cache的命中与性能}}{{影响命中率的因素：Cache容量（越大命中率越高）、块大小（适当增大提高空间局部性，但太大会降低命中率）、映射方式（全相联最高但硬件复杂）、替换算法。分离Cache：指令Cache和数据Cache分开，避免冲突。多级Cache：L1速度最快容量最小，L2/L3逐渐增大。}}

{{ROM与存储器层次结构}}{{ROM类型：掩膜ROM（出厂固定）、PROM（一次编程）、EPROM（紫外线擦除）、EEPROM（电擦除）、Flash（闪存，按块擦写）。存储器层次结构原理：利用局部性原理，越靠近CPU速度越快、容量越小、每位价格越高。虚拟存储器：主存-辅存层次，对程序员透明。}}

{{虚拟存储器}}{{虚拟存储器：利用主存-外存层次，给用户提供比主存大得多的地址空间。页式虚拟存储器：虚拟地址=虚页号+页内偏移，通过页表实现虚实地址映射。TLB（快表）：页表的高速缓存，加速地址转换。缺页中断：访问的页不在主存时产生，需从外存调入。段式和段页式虚拟存储器。}}

{{指令系统基本概念}}{{指令=操作码+地址码。地址码数目：零地址（空操作、停机）、一地址（单操作数或隐含操作数）、二地址、三地址。指令字长：固定长度（便于取指）、可变长度。操作码：定长操作码（规整）、扩展操作码（灵活）。指令分类：数据传送、算术逻辑、移位、转移、输入输出、其他。}}

{{寻址方式}}{{立即寻址：操作数在指令中。直接寻址：地址码为操作数的有效地址EA。间接寻址：地址码指向的存储单元内容为EA。寄存器寻址：操作数在寄存器中。寄存器间接寻址：寄存器内容为EA。偏移寻址：基址寻址（基址寄存器+偏移量）、变址寻址（变址寄存器+偏移量）、相对寻址（PC+偏移量）。堆栈寻址。}}

{{CISC与RISC}}{{CISC（复杂指令集）：指令数目多、格式多、长度不等、寻址方式多，大多数指令需多个时钟周期，微程序控制。RISC（精简指令集）：指令数目少、格式少、长度固定、寻址方式少，大多数指令一个时钟周期完成，硬布线控制，Load/Store结构，大量寄存器，优化编译。}}

{{CPU的功能和基本结构}}{{CPU=运算器+控制器。运算器：ALU、ACC、MQ、X、PSW、通用寄存器组。控制器：PC、IR、MAR、MDR、时序系统、微操作信号发生器。CPU的功能：指令控制、操作控制、时间控制、数据加工、中断处理。寄存器分为用户可见（通用寄存器、PC）和用户不可见（MAR、MDR、IR）。}}

{{指令执行过程}}{{取指周期：PC→MAR→地址总线→存储器→MDR→IR，PC+1。间址周期（若有间接寻址）：IR中地址码→MAR→存储器→MDR→IR地址码字段。执行周期：根据操作码和寻址方式执行相应操作。中断周期：保存断点（PC入栈）、形成中断服务程序入口地址、关中断。}}

{{数据通路的功能与基本结构}}{{数据通路是数据在各功能部件之间传送的路径。内部总线：CPU内部的总线。专用数据通路：根据指令执行需要设计的专用路径，速度快但硬件复杂。CPU内部单总线结构：所有寄存器和ALU通过一条内部总线连接，简单但有冲突。三总线结构效率更高。}}

{{控制器的设计}}{{硬布线控制器：由组合逻辑电路产生微操作控制信号，速度快但设计复杂、不灵活。微程序控制器：将微操作编成微指令，存入控制存储器CM，执行指令时取出微指令产生控制信号。微指令格式：水平型（并行度高、字长长）、垂直型（类似机器指令、字长短）。}}

{{微程序控制器详细设计}}{{微程序控制器中，机器指令对应一段微程序。控制存储器CM存放所有微程序。微指令寄存器μIR存放当前微指令。微地址形成部件（微指令地址寄存器μPC）产生下一条微指令地址。后继地址方式：计数器方式、下地址字段方式。微命令编码：直接编码、字段直接编码、字段间接编码。}}

{{指令流水线}}{{将指令执行分为多个阶段并行执行。五级流水线：取指IF→译码ID→执行EX→访存MEM→写回WB。加速比=n级流水线的理想加速比接近n。吞吐率提高。流水线冲突：结构冲突（资源冲突，解决：资源冗余）、数据冲突（RAW/WAR/WAW，解决：旁路转发、插入气泡）、控制冲突（分支，解决：延迟分支、分支预测）。}}

{{总线概述}}{{总线是多个部件分时共享的传输介质。按位置：CPU内部总线、系统总线（数据总线、地址总线、控制总线）、I/O总线。按数据传输方式：并行总线、串行总线。总线仲裁：集中式（链式查询、计数器定时查询、独立请求）、分布式。总线带宽=数据线宽度×总线频率。}}

{{总线操作与时序}}{{总线传输的四个阶段：申请分配、寻址、传数、结束。同步总线：由统一时钟控制，传输速率高但灵活性差。异步总线：用握手信号控制，灵活但速率较低。半同步总线：结合两者。总线事务类型：读、写、块传输、读后写（RMW）。PCI总线、PCIe总线。}}

{{I/O系统基本概念}}{{I/O系统由I/O硬件和I/O软件组成。I/O编址方式：统一编址（内存映射I/O）和独立编址（专用I/O指令）。I/O接口（I/O控制器）：数据缓冲寄存器、状态寄存器、控制寄存器、地址译码、控制逻辑。外设通过I/O接口与主机通信。}}

{{程序查询方式与中断方式}}{{程序查询方式：CPU主动查询设备状态，效率低CPU利用率低。程序中断方式：外设准备好后发中断请求，CPU响应中断执行中断服务程序。中断过程：关中断→保存断点→识别中断源→保存现场→执行中断服务→恢复现场→开中断→返回断点。多重中断需中断屏蔽技术。}}

{{DMA方式}}{{DMA（直接内存访问）：数据传输不经过CPU，由DMA控制器直接在内存和外设之间传送。CPU仅在DMA初始化和结束时干预。传送方式：停止CPU访存、周期挪用（CPU让出一个总线周期）、DMA与CPU交替访存。DMA传送单位是块，中断方式传送单位是字。DMA优先级高于中断。}}

{{总线标准与I/O接口}}{{常见总线标准：ISA、PCI、PCIe、USB、SATA。PCIe采用串行点对点连接，带宽高。USB（通用串行总线）：支持即插即用和热插拔，4线制。I/O接口的编址：统一编址用访存指令访问，独立编址用专门I/O指令。接口分类：并行接口/串行接口。}}

{{定点数加法器设计}}{{一位全加器：和S=A⊕B⊕Cᵢₙ，进位Cₒᵤₜ=AB+(A⊕B)Cᵢₙ。串行进位加法器：各位逐级串行传递进位，速度慢。并行进位加法器（先行进位/超前进位）：同时产生各位进位，速度快但硬件复杂。组内并行、组间串行是折中方案。ALU可实现加减乘除和逻辑运算。}}

{{浮点数IEEE 754特殊值}}{{IEEE 754特殊值：+0和-0（阶码全0尾数全0）、无穷大（阶码全1尾数全0，分±∞）、NaN（阶码全1尾数非0，分安静NaN和信号NaN）、非规格化数（阶码全0尾数非0，隐含位为0）。舍入模式：向偶数舍入（默认）、向零舍入、向正无穷舍入、向负无穷舍入。}}

{{存储器交叉编址}}{{多模块存储器提高访存速度。高位交叉编址：高位地址选模块，低位地址选模块内地址，各模块地址连续，不能并行访问。低位交叉编址：低位地址选模块，高位地址选模块内地址，各模块地址交替，可并行访问。低位交叉编址带宽是单模块的m倍。体号=地址mod m，体内地址=地址÷m。}}

{{磁盘存储器}}{{磁盘由盘片、磁头、主轴、磁头臂组成。磁道→扇区→块。磁盘容量=盘面数×每面磁道数×每磁道扇区数×每扇区字节数。存取时间=寻道时间（磁头移动）+旋转延迟（平均为转半圈的时间）+数据传输时间。磁盘调度算法：FCFS、SSTF、SCAN（电梯算法）、C-SCAN。}}

{{指令格式设计}}{{操作码长度与指令数关系：定长操作码用⌈log₂n⌉位表示n条指令。扩展操作码：短操作码留给高频指令，长操作码留给低频指令，增加指令数。设计原则：高频指令短编码，低频指令长编码。地址码位数影响寻址范围：n位地址码可寻址2^n个存储单元。指令字长应为字节的整数倍。}}

{{中断系统}}{{中断分类：内中断（异常，如缺页、除零、溢出）和外中断（I/O中断、时钟中断）。中断向量：中断服务程序入口地址。中断向量表：存放所有中断向量的存储区。中断响应条件：有中断请求、CPU允许中断（开中断）、当前指令执行完毕。中断优先级：硬件故障最高，其次是软件中断和外部设备。}}

{{中断处理过程与多重中断}}{{单重中断：关中断→保存断点→识别中断源→保存现场→执行中断服务→恢复现场→开中断→返回断点。多重中断（中断嵌套）：在中断服务程序中开中断，允许更高级别中断打断当前中断。中断屏蔽字：每一位对应一个中断源，1表示屏蔽。通过改变屏蔽字可动态调整中断优先级。}}

{{流水线性能计算}}{{流水线吞吐率TP=n/(k+n-1)×Δt，n为指令数，k为级数，Δt为时钟周期。加速比S=k×n/(k+n-1)。当n→∞时，TP→1/Δt，S→k。流水线效率E=n/(k+n-1)。实际流水线时钟周期Δt=max(各段时间)+缓冲寄存器延迟。流水线越深，理论加速比越高，但冲突和开销也越大。}}

{{流水线冲突详解}}{{结构冲突：多条指令同时使用同一硬件资源，解决方法：资源冗余（如分离的指令/数据存储器）。数据冲突：RAW（写后读，最常见）、WAR（读后写）、WAW（写后写）。解决方法：数据旁路/转发（forwarding）、插入流水线气泡（stall）。编译器调度也可减少冲突。控制冲突：分支指令导致，解决方法：延迟分支、静态/动态分支预测、提前计算分支目标。}}

{{ALU运算逻辑单元}}{{ALU是算术逻辑运算的核心部件，能执行加、减、乘、除、与、或、非、异或、移位等运算。ALU的输入为两个操作数和控制信号，输出为运算结果和标志位（零标志ZF、进位标志CF、溢出标志OF、符号标志SF）。74181是经典4位ALU芯片，可级联构成多位ALU。}}

{{微程序控制的执行过程}}{{执行一条机器指令的过程：1）取指令（公共取指微程序）2）根据操作码查微程序入口地址3）依次执行该指令对应的微程序中各条微指令4）每条微指令产生一组微命令控制数据通路。微指令格式中，下地址字段指明下一条微指令地址。微程序入口地址形成：操作码左移若干位或查PROM。}}

{{通道与I/O处理机}}{{通道是专用处理器，有自己的指令系统（通道指令），可独立执行通道程序控制I/O操作。CPU只需启动通道即可。通道类型：字节多路通道（低速设备，分时共享）、数组多路通道（高速设备，交叉传输）、选择通道（一次只服务一台高速设备）。I/O处理机比通道更独立，可执行更复杂的I/O任务。}}

{{I/O接口的功能与组成}}{{I/O接口功能：数据缓冲（解决速度匹配）、信号格式转换、设备选择（地址译码）、控制命令传送、状态反馈、中断控制。接口组成：数据缓冲寄存器DBR、状态/控制寄存器、地址译码逻辑、控制逻辑、数据格式转换电路。接口按数据传送方式分为并行接口和串行接口。}}

{{总线仲裁方式}}{{集中式仲裁：链式查询（BG信号逐设备传递，优先级固定，简单但对故障敏感）、计数器定时查询（设备号由计数器决定，优先级灵活）、独立请求方式（每个设备有独立BR/BG线，速度快但线多）。分布式仲裁：各设备自行比较优先级，无中央仲裁器。仲裁考虑公平性和饥饿问题。}}

{{浮点数运算的溢出判断}}{{浮点数溢出由阶码决定。阶码上溢（正指数过大）：结果溢出，无法表示。阶码下溢（负指数过小）：结果非规格化或为0。尾数溢出通过右规解决，不算真正溢出。IEEE 754中阶码全1表示无穷大或NaN，阶码全0表示0或非规格化数。运算后需检查阶码范围来判断溢出。}}

{{数据冒险的旁路转发技术}}{{旁路转发（forwarding/bypassing）：将ALU运算结果直接转发到下一条指令的ALU输入端，避免等待写回。EX/MEM转发和MEM/WB转发。Load-use冒险：从内存加载的数据在MEM阶段才可用，下一条指令在EX阶段就需要，必须插入一个气泡（stall）。编译器可通过指令调度避免部分冒险。}}

{{分支预测技术}}{{静态预测：总是预测分支不跳转或总是跳转，编译器提示。动态预测：基于历史信息。1位预测器：上次跳转则下次预测跳转。2位预测器：需连续两次预测错误才改变方向，精度更高。分支目标缓冲器BTB：缓存分支目标地址，命中时可直接取指。返回地址栈RAS：专门预测函数返回地址。}}

{{指令级并行与超标量}}{{指令级并行ILP：多条指令同时处于执行阶段。超标量处理器：每个时钟周期发射多条指令到不同功能部件，需多个功能部件支持。乱序执行：指令不按程序顺序执行，提高并行度但需重排序缓冲区ROB保证正确性。推测执行：在分支结果确定前预先执行分支方向的指令。VLIW由编译器静态调度。}}

{{浮点数运算精度与舍入}}{{浮点数运算中的精度问题：两个相近数相减会导致有效数字丢失（灾难性抵消）。舍入误差：浮点数不能精确表示所有实数，每次运算都可能引入舍入误差。IEEE 754默认向偶数舍入（银行家舍入），减少系统性偏差。扩展精度（80位）用于中间计算以减少累积误差。比较浮点数应使用容差而非直接相等判断。}}

{{计算机系统层次与软硬件接口}}{{计算机系统层次：硬件逻辑层→微程序层→操作系统层→汇编语言层→高级语言层→应用层。软硬件接口：指令集架构ISA是软件和硬件之间的接口，定义了程序员可见的寄存器、指令集、数据类型、寻址方式、中断机制等。同一ISA可有不同实现（如Intel和AMD都实现x86 ISA）。固件（微程序）是软硬件的桥梁。}}
