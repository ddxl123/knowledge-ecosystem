# 2025王道数据结构
# 领域: 考研
# 学科: 408计算机/数据结构
# 来源: 2025王道考研数据结构
# 描述: 408统考数据结构核心知识点，涵盖线性表、树、图、查找、排序等

{{数据结构基本概念}}{{数据结构是相互之间存在一种或多种特定关系的数据元素的集合。逻辑结构：集合、线性结构、树形结构、图状结构。物理结构（存储结构）：顺序存储、链式存储、索引存储、散列存储。抽象数据类型ADT=(D,S,P)，数据对象、数据关系、基本操作。}}

{{算法及其评价}}{{算法五大特性：有穷性、确定性、可行性、输入、输出。评价指标：正确性、可读性、健壮性、效率与低存储量需求。时间复杂度：T(n)=O(f(n))，取最高阶项。常见复杂度排序：O(1)<O(logn)<O(n)<O(nlogn)<O(n²)<O(n³)<O(2ⁿ)<O(n!)。空间复杂度：S(n)=O(g(n))。}}

{{线性表的顺序存储}}{{顺序表用一组地址连续的存储单元依次存储线性表的元素。随机访问，时间O(1)。插入：平均移动n/2个元素，O(n)。删除：平均移动(n-1)/2个元素，O(n)。按值查找：平均比较(n+1)/2次，O(n)。静态分配用数组，动态分配用malloc/realloc。}}

{{线性表的链式存储}}{{单链表：每个结点包含数据域和指针域。头结点便于操作统一。头插法建表得到逆序，尾插法得到正序。按序号查找O(n)。插入删除O(1)（已知前驱时）。双链表可双向遍历。循环链表最后一个结点指向头结点。静态链表用数组模拟链表。}}

{{栈的基本概念与应用}}{{栈是后进先出LIFO的线性表。基本操作：Push、Pop、GetTop。顺序栈用数组实现，top指针。共享栈（两栈共享空间）：两个栈从两端向中间增长。链栈用链表实现。应用：括号匹配、表达式求值（后缀表达式）、递归转非递归、进制转换。}}

{{队列的基本概念}}{{队列是先进先出FIFO的线性表。顺序队列存在假溢出问题，循环队列解决。队满条件：(rear+1)%MaxSize==front（牺牲一个空间）或用size变量。队空条件：front==rear。链式队列用带头结点的单链表。双端队列：两端都可以进出。}}

{{串的模式匹配}}{{朴素模式匹配（BF算法）：最坏时间O(mn)。KMP算法：利用next函数（部分匹配表）跳过不必要的比较，时间O(m+n)。next数组：next[j]表示模式串前j-1个字符的最长相等前后缀长度。改进的nextval数组避免重复比较。}}

{{树的基本概念}}{{树是n个结点的有限集合。基本术语：根结点、叶子结点、分支结点、度（结点的子树个数）、层次、深度/高度、森林。性质：树中结点数=总度数+1；度为m的树第i层最多有m^(i-1)个结点；高度为h的m叉树最多有(m^h-1)/(m-1)个结点。}}

{{二叉树的性质}}{{五个性质：①第i层最多2^(i-1)个结点②深度为k的二叉树最多2^k-1个结点③n₀=n₂+1（叶子结点数=度为2的结点数+1）④完全二叉树结点n，则⌊log₂n⌋+1层⑤完全二叉树中，i的左孩子2i，右孩子2i+1，父结点⌊i/2⌋。}}

{{二叉树的遍历}}{{先序遍历（根左右）、中序遍历（左根右）、后序遍历（左右根）、层次遍历（队列）。先序+中序可唯一确定二叉树；后序+中序可唯一确定二叉树；先序+后序不能唯一确定。非递归中序遍历用栈。线索二叉树利用空指针指向前驱/后继。}}

{{树与森林的存储与转换}}{{双亲表示法（数组存父结点下标）、孩子表示法（数组+链表）、孩子兄弟表示法（左孩子右兄弟，本质是二叉树）。树→二叉树：兄弟连线，保留最左孩子连线，删除其他连线。森林→二叉树：各棵树先转二叉树再依次作为右子树连接。}}

{{赫夫曼树与赫夫曼编码}}{{带权路径长度WPL=∑(wᵢ×lᵢ)最小的二叉树为赫夫曼树。构造：每次选两个权值最小的结点合并为新结点（权值相加），重复直到只剩一棵树。赫夫曼编码：左0右1，从根到叶的路径即为编码。前缀编码：任一编码不是其他编码的前缀，赫夫曼编码是前缀编码。}}

{{图的基本概念}}{{图G=(V,E)，V为顶点集，E为边集。有向图/无向图、完全图、稀疏图/稠密图、连通图/强连通图、生成树。度：无向图中顶点的边数之和等于边数的2倍；有向图中入度之和=出度之和=边数。路径、回路、连通分量。}}

{{图的存储结构}}{{邻接矩阵：用二维数组存边信息，空间O(n²)。邻接表：每个顶点一个链表存邻接顶点，空间O(n+e)。十字链表（有向图）、邻接多重表（无向图）。稠密图用邻接矩阵，稀疏图用邻接表。}}

{{图的遍历}}{{深度优先搜索DFS：类似树的先序遍历，用栈或递归实现，时间O(n+e)（邻接表）或O(n²)（邻接矩阵）。广度优先搜索BFS：类似树的层次遍历，用队列实现。BFS可求无权图最短路径。图可能不连通，需对每个连通分量调用遍历。}}

{{最小生成树}}{{Prim算法：从某顶点出发，每次选离已选顶点集最近的未选顶点加入，适合稠密图，O(n²)。Kruskal算法：每次选权值最小的边，若不构成环则加入，适合稀疏图，O(e log e)。两种算法都是贪心策略，结果可能不唯一但总权值相同。}}

{{最短路径算法}}{{Dijkstra算法：求单源最短路径，贪心策略，每次选距离最小的未确定顶点，时间O(n²)，不适用于负权边。Floyd算法：求所有顶点间的最短路径，动态规划，三重循环，时间O(n³)，可处理负权边但不能处理负权回路。}}

{{拓扑排序与关键路径}}{{拓扑排序：对有向无环图DAG，每次选入度为0的顶点输出并删除其出边，结果不唯一。若不能输出所有顶点则存在环。关键路径：AOE网中从源点到汇点的最长路径。关键活动：关键路径上的活动，其最早开始=最迟开始。求关键路径需先拓扑排序。}}

{{查找的基本概念}}{{查找表：由同一类型数据元素组成的集合。关键字：数据元素中某个数据项的值。平均查找长度ASL=∑PᵢCᵢ。顺序查找：ASL=(n+1)/2，O(n)。折半查找：要求有序顺序表，ASL≈log₂(n+1)-1，O(logn)。分块查找：块间有序、块内无序。}}

{{B树与B+树}}{{B树：m阶B树每个结点最多m个子树、最少⌈m/2⌉个子树（根至少2个），关键字按升序排列。B+树：非叶子结点仅起索引作用，叶子结点包含所有关键字且链接成有序链表。B+树更适合文件系统和数据库索引，支持范围查询。}}

{{散列表}}{{散列函数H(key)将关键字映射到存储地址。冲突处理：开放定址法（线性探测、二次探测、双散列）、链地址法（拉链法）。装填因子α=表中记录数/表长。平均查找长度依赖于α，与n无直接关系。好的散列函数应使关键字均匀分布。}}

{{插入排序}}{{直接插入排序：将每个元素插入到已排序序列的正确位置。空间O(1)，时间最好O(n)最坏O(n²)，稳定。折半插入排序：用折半查找确定插入位置。希尔排序：按递减增量分组排序，不稳定，时间复杂度与增量序列有关，一般O(n^1.3)。}}

{{交换排序}}{{冒泡排序：相邻元素比较交换，每趟确定一个元素最终位置。最好O(n)，最坏O(n²)，稳定。快速排序：选基准元素分区递归。平均O(nlogn)，最坏O(n²)（已排序时），不稳定。快排是内部排序中平均性能最好的。优化：三数取中选基准。}}

{{选择排序}}{{简单选择排序：每趟选最小元素放到已排序末尾。时间始终O(n²)，不稳定。堆排序：建大根堆/小根堆，每次取堆顶元素与末尾交换再调整。时间O(nlogn)，不稳定，空间O(1)。建堆时间O(n)，调整时间O(logn)。堆排序适合大数据量。}}

{{归并排序与基数排序}}{{归并排序：将有序子序列两两合并，O(nlogn)，稳定，空间O(n)。递归实现需要辅助数组。基数排序：按关键字各位分配收集，从最低位到最高位（LSD）或反之（MSD）。时间O(d(n+r))，d为位数，r为基数，稳定。不基于比较的排序。}}

{{排序算法比较与总结}}{{稳定排序：冒泡、插入、归并、基数、计数。不稳定排序：选择、快速、希尔、堆。时间复杂度：最好O(n)的有插入和冒泡；平均O(nlogn)的有快排、归并、堆排序。空间：归并O(n)，快排O(logn)，其余O(1)。排序算法的选择取决于数据规模、是否稳定、初始状态等因素。}}

{{内部排序总结}}{{当n较小时用直接插入或简单选择；当n较大时用快排（平均最快）、堆排序（最坏O(nlogn)且空间少）或归并（稳定）。当关键字位数少且范围小时用基数排序。快排在已排序数据上退化为O(n²)，需优化基准选取。}}

{{外部排序}}{{外部排序用于数据量太大无法全部装入内存的情况。多路归并排序：将文件分段排序后多路归并。置换选择排序用于生成初始归并段。最佳归并树（赫夫曼树思想）减少归并趟数。磁盘排序关注减少I/O次数。}}
