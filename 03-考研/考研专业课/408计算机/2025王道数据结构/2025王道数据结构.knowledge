# 2025王道数据结构
# 领域: 考研
# 学科: 408计算机
# 来源: 王道数据结构

{{知识点1}}{{数据结构的基本概念：数据是信息的载体，是所有能被输入到计算机中且能被计算机程序识别和处理的符号的集合。数据元素是数据的基本单位，通常作为一个整体进行考虑和处理。数据项是构成数据元素的不可分割的最小单位。数据对象是具有相同性质的数据元素的集合。数据结构是相互之间存在一种或多种特定关系的数据元素的集合，包括逻辑结构、存储结构和数据的运算三方面。}}

{{知识点2}}{{数据的逻辑结构：集合结构中的数据元素之间除同属一个集合外无其他关系。线性结构中的数据元素之间是一对一的关系，如线性表、栈、队列。树形结构中的数据元素之间是一对多的关系，如二叉树、堆。图状结构或网状结构中的数据元素之间是多对多的关系，如图。}}

{{知识点3}}{{数据的存储结构（物理结构）：顺序存储是把逻辑上相邻的元素存储在物理位置上也相邻的存储单元中，元素之间的关系由存储单元的邻接关系来体现。链式存储不要求逻辑上相邻的元素在物理位置上也相邻，借助指示元素存储地址的指针来表示元素之间的逻辑关系。索引存储在存储元素信息的同时还建立附加的索引表。散列存储根据元素的关键字直接计算出该元素的存储地址。}}

{{知识点4}}{{算法的基本特性：有穷性是指算法必须在执行有穷步之后结束，且每一步都可在有穷时间内完成。确定性是指算法中每条指令必须有确切含义，对于相同的输入只能得出相同的输出。可行性是指算法中描述的操作都可以通过已经实现的基本运算执行有限次来实现。输入是一个算法有零个或多个输入。输出是一个算法有一个或多个输出。}}

{{知识点5}}{{算法效率的度量——时间复杂度：时间复杂度T(n)表示算法中基本操作的执行次数与问题规模n之间的关系。常见时间复杂度从小到大：O(1)<O(log₂n)<O(n)<O(nlog₂n)<O(n²)<O(n³)<O(2ⁿ)<O(n!)<O(nⁿ)。加法规则：T(n)=T₁(n)+T₂(n)=O(f(n))+O(g(n))=O(max(f(n),g(n)))。乘法规则：T(n)=T₁(n)×T₂(n)=O(f(n))×O(g(n))=O(f(n)×g(n))。}}

{{知识点6}}{{算法效率的度量——空间复杂度：空间复杂度S(n)定义为算法所耗费的存储空间与问题规模n之间的关系。算法原地工作是指算法所需的辅助空间为常量，即O(1)。程序运行时的内存空间包括存储算法本身、输入数据和辅助空间（临时变量等）。}}

{{知识点7}}{{线性表的定义：线性表是具有相同数据类型的n（n≥0）个数据元素的有限序列，其中n为表长。除第一个元素外，每个元素有且仅有一个直接前驱；除最后一个元素外，每个元素有且仅有一个直接后继。线性表的特点：表中元素个数有限；元素具有逻辑上的顺序性，各元素有先后次序；表中元素都是数据元素；表中元素的数据类型相同，即每个元素占有相同大小的存储空间。}}

{{知识点8}}{{线性表的顺序存储（顺序表）：用一组地址连续的存储单元依次存储线性表中的数据元素，使得逻辑上相邻的两个元素在物理位置上也相邻。特点：随机访问，通过首地址和元素序号可在O(1)时间内找到指定元素。存储密度高，每个结点只存储数据元素。逻辑上相邻的元素物理上也相邻，因此插入和删除操作需要移动大量元素。插入操作平均移动n/2个元素，时间复杂度O(n)；删除操作平均移动(n-1)/2个元素，时间复杂度O(n)；按位查找O(1)；按值查找O(n)。}}

{{知识点9}}{{线性表的链式存储——单链表：通过一组任意的存储单元来存储线性表中的数据元素，对每个结点，除了存放元素本身的信息外，还需要存放一个指向其后继的指针。头结点的引入使得在第一个位置上的操作与在其他位置上的操作一致，无需特殊处理。建立单链表：头插法（元素逆序）和尾插法（元素正序）。查找：按位查找O(n)，按值查找O(n)。插入和删除操作均为O(n)，因为需要先找到操作位置的前驱。}}

{{知识点10}}{{双链表：双链表中每个结点有两个指针域，一个指向直接前驱，一个指向直接后继。双链表的插入操作需修改四个指针，删除操作需修改两个指针。与单链表相比，双链表可以方便地访问前驱结点，删除指定结点的时间复杂度为O(1)（已知结点位置时）。}}

{{知识点11}}{{循环链表：循环单链表中最后一个结点的指针不是NULL，而是指向头结点，从而形成一个环。判空条件为头结点的指针是否指向自身。从任一结点出发均可遍历整个链表。循环双链表兼有循环和双链表的特点。}}

{{知识点12}}{{静态链表：借助数组来描述线性表的链式存储结构，结点也有数据域和指针域，但这里的指针是结点的相对地址（数组下标），又称游标。和顺序表一样需要预先分配一块连续的内存空间。以next=-1作为结束标志。}}

{{知识点13}}{{栈的定义和基本操作：栈是只允许在一端进行插入或删除操作的线性表，遵循后进先出（LIFO）原则。栈顶允许操作，栈底不允许。基本操作：InitStack、StackEmpty、Push、Pop、GetTop。顺序栈用数组实现，设top指针指向栈顶元素（top=-1为空）。共享栈利用栈的空间互补特性，两个栈共享同一数组空间，栈满条件为top1+1==top2。链栈通常采用单链表实现，所有操作均在链表头部进行。}}

{{知识点14}}{{栈的应用——括号匹配：从左到右依次扫描每个字符，遇到左括号入栈，遇到右括号时弹出栈顶的左括号进行匹配。若栈为空时遇到右括号则不匹配；扫描结束时栈非空则不匹配。时间复杂度O(n)，空间复杂度O(n)。}}

{{知识点15}}{{栈的应用——表达式求值（后缀表达式）：中缀转后缀使用运算符栈。规则：操作数直接输出；左括号入栈；右括号依次弹出栈顶运算符直到遇到左括号；运算符与栈顶比较优先级，若当前运算符优先级小于等于栈顶则弹出栈顶，再将当前运算符入栈。后缀表达式求值使用操作数栈，遇到运算符弹出两个操作数计算后将结果入栈。}}

{{知识点16}}{{栈的应用——递归：递归调用时系统将函数调用的参数、返回地址等信息压入栈中（递归工作栈）。递归转非递归常用栈来模拟。斐波那契数列的递归实现中，F(n)=F(n-1)+F(n-2)，递归深度为n，时间复杂度为O(2ⁿ)，可用迭代法优化为O(n)。}}

{{知识点17}}{{队列的定义和基本操作：队列是只允许在一端插入（队尾）、另一端删除（队头）的线性表，遵循先进先出（FIFO）原则。基本操作：InitQueue、QueueEmpty、EnQueue、DeQueue、GetHead。}}

{{知识点18}}{{循环队列：为解决顺序队列的"假溢出"问题，将队列的存储空间在逻辑上视为环状。队满条件：(Q.rear+1)%MaxSize==Q.front（牺牲一个存储单元）。元素个数：(Q.rear-Q.front+MaxSize)%MaxSize。入队：Q.rear=(Q.rear+1)%MaxSize；出队：Q.front=(Q.front+1)%MaxSize。也可增设size变量或flag标志来区分队空和队满。}}

{{知识点19}}{{链式队列：用单链表实现，同时带有队头指针和队尾指针。入队操作在队尾执行，出队操作在队头执行。当只有一个元素时，出队后需将队尾指针也指向头结点。不带头结点的链式队列需额外处理空队列情况。}}

{{知识点20}}{{双端队列：双端队列是允许两端都可以进行入队和出队操作的队列。输出受限的双端队列允许在一端进行插入和删除，在另一端只允许插入（或删除）。输入受限的双端队列允许在一端进行插入和删除，在另一端只允许删除（或插入）。注意：使用受限的双端队列可能产生某些特定的输出序列。}}

{{知识点21}}{{栈和队列的应用——层次遍历：利用队列实现二叉树的层次遍历。根结点入队，循环执行：出队一个结点并访问，若其左子树非空则左孩子入队，若其右子树非空则右孩子入队，直到队列为空。}}

{{知识点22}}{{特殊矩阵的压缩存储——对称矩阵：n阶对称矩阵a[i][j]=a[j][i]，只需存储下三角（或上三角）部分，包括主对角线。按行优先存储，a[i][j]（i≥j）在一维数组中的下标为k=i(i+1)/2+j（从0开始计数）；若i<j，则交换i和j。元素个数为n(n+1)/2。}}

{{知识点23}}{{特殊矩阵的压缩存储——三角矩阵：上三角矩阵的下三角部分（不包括主对角线）全为常量c，下三角矩阵的上三角部分全为常量c。存储方式与对称矩阵类似，另外用一个存储单元存放常量c。}}

{{知识点24}}{{特殊矩阵的压缩存储——三对角矩阵（带状矩阵）：三对角矩阵的非零元素集中在主对角线及其上下相邻的两条对角线上，其余元素全为零。按行优先存储，a[i][j]在一维数组中的下标k=2i+j（i、j从0开始）。总共存储3n-2个元素。}}

{{知识点25}}{{稀疏矩阵的压缩存储——三元组表：将非零元素用（行，列，值）的三元组表示，所有非零元素的三元组按行号递增排列（同行则按列号递增）。十字链表是稀疏矩阵的链式存储结构，每个非零元素用一个结点表示，同时将其链接到所在行和所在列的链表中。}}

{{知识点26}}{{串的定义和基本操作：串是由零个或多个字符组成的有限序列，又称字符串。空串长度为0。子串是串中任意个连续字符组成的子序列。主串中包含子串则称子串在主串中的位置。串的存储结构包括定长顺序存储（静态数组）、堆分配存储（动态分配）和块链存储。}}

{{知识点27}}{{朴素模式匹配算法（BF算法）：从主串的每一个位置开始，将子串与主串逐一比较。最坏时间复杂度为O(mn)，其中m为主串长度，n为模式串长度。简单直观但效率低。}}

{{知识点28}}{{KMP算法：利用已匹配的信息避免重复比较。关键在于计算next数组（失败函数）。next[j]表示当模式串中第j个字符匹配失败时，应将模式串的第next[j]个字符与主串当前位置对齐继续比较。next数组的计算：next[1]=0，next[2]=1，当第j个字符不匹配时，取next[j]=next[j-1]+1或更小的递归值。KMP算法时间复杂度O(m+n)。}}

{{知识点29}}{{KMP算法的改进——nextval数组：nextval数组在next数组基础上进一步优化，当模式串第j个字符与next[j]指向的字符相等时，将nextval[j]设为nextval[next[j]]，从而在匹配失败时能跳过更多不必要的比较。nextval数组的构建：先求next数组，然后从前往后比较，若T[next[j]]==T[j]则nextval[j]=nextval[next[j]]，否则nextval[j]=next[j]。}}

{{知识点30}}{{树的基本概念：树是n（n≥0）个结点的有限集合。n=0时称为空树。在任意一棵非空树中：有且仅有一个特定的称为根的结点；当n>1时，其余结点可分为m（m>0）个互不相交的有限集T₁、T₂、…、Tₘ，其中每个集合本身又是一棵树，称为根的子树。基本术语：结点的度（子树个数）、树的度（最大结点度数）、叶子结点（度为0）、分支结点（度>0）、层次（根为第1层）、深度/高度（最大层次）、有序树与无序树、森林。}}

{{知识点31}}{{二叉树的定义和性质：二叉树是每个结点至多只有两棵子树的有序树。性质1：非空二叉树上叶子结点数等于度为2的结点数加1，即n₀=n₂+1。性质2：第i层上至多有2^(i-1)个结点（i≥1）。性质3：深度为k的二叉树至多有2^k-1个结点。性质4：具有n个结点的完全二叉树的深度为⌊log₂n⌋+1。性质5：完全二叉树中，对结点i，左孩子为2i（若2i≤n），右孩子为2i+1（若2i+1≤n），父结点为⌊i/2⌋。}}

{{知识点32}}{{满二叉树和完全二叉树：满二叉树是每一层的结点数都达到最大值的二叉树，深度为k的满二叉树有2^k-1个结点。完全二叉树是除最后一层外，其他层的结点数都达到最大值，且最后一层的结点都连续集中在最左边。完全二叉树中度为1的结点最多只有一个（若n为奇数则无度为1的结点，若n为偶数则有一个且为左孩子）。}}

{{知识点33}}{{二叉树的存储结构：顺序存储适合完全二叉树和满二叉树，按层序编号存入数组，下标为i的结点的左孩子为2i、右孩子为2i+1、父结点为⌊i/2⌋。链式存储（二叉链表）中每个结点包含数据域、左孩子指针和右孩子指针。n个结点的二叉链表共有n+1个空指针域。三叉链表增加了父结点指针域。}}

{{知识点34}}{{二叉树的遍历——先序遍历（PreOrder）：若二叉树为空则返回；访问根结点；先序遍历左子树；先序遍历右子树。递归算法时间复杂度O(n)。非递归算法借助栈实现。}}

{{知识点35}}{{二叉树的遍历——中序遍历（InOrder）：若二叉树为空则返回；中序遍历左子树；访问根结点；中序遍历右子树。非递归算法：沿左子树入栈，直到左子树为空，弹出栈顶元素并访问，然后对右子树执行相同操作。}}

{{知识点36}}{{二叉树的遍历——后序遍历（PostOrder）：若二叉树为空则返回；后序遍历左子树；后序遍历右子树；访问根结点。非递归算法需注意：访问结点时需判断是从左子树返回还是从右子树返回，可使用标记法或双栈法。}}

{{知识点37}}{{二叉树的遍历——层序遍历：利用队列实现。将根结点入队，队列非空时循环：出队一个结点并访问，将其非空的左、右孩子依次入队。通过先序+中序、后序+中序或层序+中序可以唯一确定一棵二叉树。}}

{{知识点38}}{{由遍历序列构造二叉树：先序序列和中序序列可以唯一确定一棵二叉树——先序序列的第一个元素是根结点，在中序序列中找到根的位置可划分出左、右子树。后序序列和中序序列也能唯一确定——后序序列的最后一个元素是根。注意：先序+后序不能唯一确定一棵二叉树（除非只有一个孩子）。}}

{{知识点39}}{{线索二叉树：在二叉链表中增加两个标志域ltag和rtag。ltag=0表示lchild指向左孩子，ltag=1表示lchild指向前驱；rtag=0表示rchild指向右孩子，rtag=1表示rchild指向后继。以某种遍历次序对二叉树进行线索化。中序线索二叉树可以方便地找到中序序列的前驱和后继，且不需要栈就能实现中序遍历。}}

{{知识点40}}{{树的存储结构——双亲表示法：用一组连续空间存储树的每个结点，同时在每个结点中附设一个指示器指示其双亲结点在数组中的位置。根结点的双亲位置为-1。优点：找双亲容易O(1)。缺点：找孩子需要遍历整个数组。}}

{{知识点41}}{{树的存储结构——孩子表示法：将每个结点的孩子结点用单链表链接起来形成一个线性结构，n个结点共有n个孩子链表（叶子结点的孩子链表为空表）。n个头指针组成一个线性表。方便找孩子，找双亲需遍历。}}

{{知识点42}}{{树的存储结构——孩子兄弟表示法（二叉树表示法）：每个结点包含数据域、第一个孩子指针和右兄弟指针。可以将一棵普通树转换为二叉树。转换规则：左孩子右兄弟。森林转换为二叉树：每棵树转换为二叉树后，后一棵树作为前一棵树根的右子树。}}

{{知识点43}}{{树和森林的遍历：树的先根遍历等价于对应二叉树的先序遍历；树的后根遍历等价于对应二叉树的中序遍历。森林的先序遍历等价于对应二叉树的先序遍历；森林的中序遍历等价于对应二叉树的中序遍历。}}

{{知识点44}}{{二叉排序树（BST）：左子树上所有结点的关键字均小于根结点的关键字；右子树上所有结点的关键字均大于根结点的关键字；左、右子树又各是一棵二叉排序树。中序遍历得到递增有序序列。查找：从根开始，小于则走左，大于则走右。平均查找长度为O(log₂n)，最坏为O(n)（退化为链表）。插入操作每次都在叶子结点处进行。删除操作需考虑三种情况。}}

{{知识点45}}{{平衡二叉树（AVL树）：是一种二叉排序树，其中任意结点的左子树和右子树的高度差的绝对值不超过1（平衡因子为-1、0或1）。插入操作可能导致不平衡，需要通过旋转调整：LL旋转（右旋）、RR旋转（左旋）、LR旋转（先左旋再右旋）、RL旋转（先右旋再左旋）。AVL树的查找时间复杂度始终为O(log₂n)。含有n个结点的AVL树最大深度约为1.44log₂(n+2)。}}

{{知识点46}}{{哈夫曼树和哈夫曼编码：哈夫曼树是带权路径长度（WPL）最小的二叉树，也称最优二叉树。构造方法（哈夫曼算法）：将n个权值视为n棵只有根结点的树构成森林；每次选取权值最小的两棵树合并为一棵新树，新树根的权值为两子树根的权值之和；重复直到只剩一棵树。哈夫曼树中没有度为1的结点，n个叶子结点的哈夫曼树共有2n-1个结点。哈夫曼编码：左分支为0，右分支为1，从根到叶子的路径即为该字符的编码，是一种前缀编码，可用于数据压缩。}}

{{知识点47}}{{图的基本概念：图G由顶点集V和边集E组成，记为G=(V,E)。有向图中边是有序对<v,w>，无向图中边是无序对(v,w)。完全图：无向完全图有n(n-1)/2条边，有向完全图有n(n-1)条弧。连通图：无向图中任意两点间有路径。强连通图：有向图中任意两点互相可达。连通分量：无向图的极大连通子图。生成树：包含图中所有顶点的极小连通子图。顶点的度、入度、出度。}}

{{知识点48}}{{图的存储结构——邻接矩阵：用一个一维数组存储顶点信息，用一个二维数组（邻接矩阵）存储边的信息。无向图的邻接矩阵是对称矩阵。空间复杂度O(n²)，适合稠密图。判断两点间是否有边O(1)。}}

{{知识点49}}{{图的存储结构——邻接表：对每个顶点建立一个单链表，第i个单链表中的结点表示依附于顶点vᵢ的边（有向图中为以vᵢ为尾的弧）。无向图的空间复杂度O(n+2e)，有向图为O(n+e)，适合稀疏图。逆邻接表对每个顶点建立以该顶点为头的弧的链表。}}

{{知识点50}}{{图的存储结构——十字链表和邻接多重表：十字链表是有向图的另一种链式存储结构，每条弧对应一个结点，同时链接到弧尾顶点的出边表和弧头顶点的入边表。邻接多重表是无向图的另一种链式存储结构，每条边只用一个结点表示。}}

{{知识点51}}{{图的遍历——深度优先搜索（DFS）：类似树的先序遍历。从某个顶点v出发，访问v，然后访问v的未被访问的邻接顶点，重复直到所有与v连通的顶点都被访问。用栈（递归调用栈）实现。时间复杂度：邻接矩阵O(n²)，邻接表O(n+e)。空间复杂度O(n)。}}

{{知识点52}}{{图的遍历——广度优先搜索（BFS）：类似树的层次遍历。从某个顶点v出发，访问v，然后访问v的所有未被访问的邻接顶点，再依次访问这些邻接顶点的未被访问的邻接顶点。用队列实现。时间复杂度：邻接矩阵O(n²)，邻接表O(n+e)。BFS可用于求无权图的最短路径。}}

{{知识点53}}{{最小生成树——Prim算法：从某一个顶点开始，每次选择与当前生成树相连的权值最小的边，将新的顶点加入生成树。适合稠密图。时间复杂度O(n²)，可用最小堆优化为O(elog₂e)。}}

{{知识点54}}{{最小生成树——Kruskal算法：将所有边按权值排序，依次选择权值最小且不与已选边形成回路的边。适合稀疏图。使用并查集判断是否形成回路。时间复杂度O(elog₂e)。}}

{{知识点55}}{{最短路径——Dijkstra算法：求单源最短路径，即从源点到其他所有顶点的最短路径。采用贪心策略，每次从未确定最短路径的顶点中选择距离最小的顶点，用该顶点更新其邻接顶点的距离。时间复杂度O(n²)。不能处理负权边。可用堆优化。}}

{{知识点56}}{{最短路径——Floyd算法：求所有顶点间的最短路径。使用动态规划，逐步允许经过更多的中间顶点。三重循环：最外层为中间顶点k，内两层为起点i和终点j。若dist[i][k]+dist[k][j]<dist[i][j]则更新。时间复杂度O(n³)，空间复杂度O(n²)。可以处理负权边（但不能有负权回路）。}}

{{知识点57}}{{有向无环图（DAG）与拓扑排序：拓扑排序是将DAG中所有顶点排成一个线性序列，使得图中任意一对顶点u和v，若<u,v>∈E(G)，则u在序列中出现在v之前。方法：每次选择入度为0的顶点输出并删除其出边，重复直到所有顶点输出或发现环。可用于判断图中是否有环、任务调度等。时间复杂度O(n+e)。}}

{{知识点58}}{{关键路径：在AOE网（边表示活动的网）中，从源点到汇点的最长路径称为关键路径，关键路径上的活动为关键活动。计算：事件最早发生时间ve(j)=max{ve(i)+w(i,j)}（正向）；事件最迟发生时间vl(j)=min{vl(k)-w(j,k)}（逆向）；活动最早开始时间e(i)=ve(弧尾)；活动最迟开始时间l(i)=vl(弧头)-w(弧)；e(i)=l(i)的活动为关键活动。}}

{{知识点59}}{{查找的基本概念：查找是在数据集合中寻找满足某种条件的数据元素。平均查找长度（ASL）是衡量查找算法效率的指标，ASL=ΣPᵢCᵢ，其中Pᵢ为查找第i个元素的概率，Cᵢ为找到第i个元素所需的比较次数。}}

{{知识点60}}{{顺序查找和折半查找：顺序查找逐个比较，ASL=(n+1)/2，时间复杂度O(n)。折半查找（二分查找）要求有序顺序表，每次与中间元素比较缩小一半范围。ASL≈log₂(n+1)-1，时间复杂度O(log₂n)。折半查找的判定树是一棵平衡二叉树。注意：折半查找不适用于链表。}}

{{知识点61}}{{分块查找（索引顺序查找）：将表分为若干块，块内无序、块间有序。建立索引表记录每块的最大关键字和起始位置。先在索表中确定块（顺序或折半），再在块内顺序查找。ASL介于顺序查找和折半查找之间。}}

{{知识点62}}{{B树（B-树）：一棵m阶B树是所有结点的平衡因子均等于0的m路平衡查找树。性质：每个结点最多有m棵子树（m-1个关键字）；除根结点外每个非叶结点至少有⌈m/2⌉棵子树（⌈m/2⌉-1个关键字）；根结点至少有两棵子树（若非叶子）；所有叶子结点在同一层次上（失败结点）。查找：从根开始，在结点内顺序或折半查找关键字，确定子树后继续。插入可能导致结点分裂，删除可能导致结点合并。}}

{{知识点63}}{{B+树：B+树是B树的变体。m阶B+树中每个结点最多有m个子树；非叶结点的子树个数与关键字个数相同；非叶结点只起索引作用，不保存数据；所有叶子结点包含全部关键字及指向记录的指针，叶子结点按关键字大小顺序排列形成链表。B+树更适合文件系统和数据库的索引，支持范围查找。}}

{{知识点64}}{{散列表（哈希表）的基本概念：根据关键字直接计算出存储地址。散列函数H(key)将关键字映射到散列地址。常见的散列函数：直接定址法H(key)=a×key+b；除留余数法H(key)=key%p（p取不大于表长的最大素数）；数字分析法；平方取中法。冲突是指不同的关键字映射到相同的散列地址。}}

{{知识点65}}{{散列表的冲突处理——开放定址法：当冲突发生时，按照某种探测序列寻找下一个空闲地址。线性探测法：Hᵢ=(H(key)+i)%m，容易产生"聚集"现象。平方探测法：Hᵢ=(H(key)±i²)%m，可以避免聚集但不一定能探测到所有位置。双重散列法：Hᵢ=(H(key)+i×H₂(key))%m。}}

{{知识点66}}{{散列表的冲突处理——链地址法（拉链法）：将所有关键字为同义词的记录存储在同一线性链表中。散列表的每个单元存放链表的头指针。优点：处理冲突简单，无聚集现象，适合经常插入和删除的场景。缺点：需要额外的指针空间。}}

{{知识点67}}{{散列表的查找效率：散列表的查找效率取决于三个因素：散列函数、处理冲突的方法和装填因子α=表中记录数/表长。α越大，冲突越多。平均查找长度与α有关而非与n直接相关，这是散列表优于其他查找方法之处。查找成功的平均查找长度和查找失败的平均查找长度需要分别计算。}}

{{知识点68}}{{排序的基本概念：排序是将一组数据元素按某个关键字递增或递减排列。稳定性：若排序前后两个关键字相同的元素的相对位置不变则为稳定排序。内部排序：待排序记录全部存放在内存中。外部排序：待排序记录数量大需借助外存。}}

{{知识点69}}{{直接插入排序：将待排序元素依次插入到已排好序的有序序列中。时间复杂度：最好O(n)（已有序），最坏O(n²)（逆序），平均O(n²)。空间复杂度O(1)。稳定排序。适用于基本有序的小规模数据。}}

{{知识点70}}{{折半插入排序：在直接插入排序的基础上，用折半查找确定插入位置，减少了比较次数但移动次数不变。时间复杂度：比较次数O(nlog₂n)，移动次数O(n²)，总时间复杂度O(n²)。稳定排序。}}

{{知识点71}}{{希尔排序：先将待排序序列按增量序列分为若干子序列分别进行直接插入排序，然后逐步缩小增量重复排序，直到增量为1。增量序列的选择影响效率。时间复杂度与增量序列有关，一般认为是O(n^1.3)左右。不稳定排序。空间复杂度O(1)。}}

{{知识点72}}{{冒泡排序：反复交换相邻的逆序元素，使大的元素逐渐"冒泡"到后面。每趟确定一个元素的最终位置。时间复杂度：最好O(n)（设置标志位，已有序时一趟即可结束），最坏O(n²)，平均O(n²)。空间复杂度O(1)。稳定排序。}}

{{知识点73}}{{快速排序：选一个基准元素（pivot），将待排序序列划分为两部分，左边元素都小于基准，右边元素都大于基准，然后递归排序两部分。平均时间复杂度O(nlog₂n)，最坏时间复杂度O(n²)（已有序时退化为冒泡）。空间复杂度：平均O(log₂n)，最坏O(n)。不稳定排序。是内部排序中平均性能最好的算法。优化方法：三数取中法选基准、随机化。}}

{{知识点74}}{{简单选择排序：每趟从待排序序列中选择关键字最小的元素，放到已排序序列的末尾。比较次数始终为n(n-1)/2，与初始状态无关。时间复杂度O(n²)。空间复杂度O(1)。不稳定排序。}}

{{知识点75}}{{堆排序：利用堆这种数据结构进行排序。大根堆：每个结点的值大于等于其左右孩子的值。小根堆：每个结点的值小于等于其左右孩子的值。建堆过程：从最后一个非叶子结点开始调整（向下筛选），时间复杂度O(n)。排序过程：反复取堆顶元素（与最后一个元素交换后调整堆），时间复杂度O(nlog₂n)。总时间复杂度O(nlog₂n)。空间复杂度O(1)。不稳定排序。}}

{{知识点76}}{{归并排序：将两个或两个以上的有序表合并为一个新的有序表。二路归并排序：将序列递归地分为两半，分别排序后合并。每一趟归并时间复杂度O(n)，共需⌈log₂n⌉趟。总时间复杂度O(nlog₂n)。空间复杂度O(n)（需要辅助数组）。稳定排序。}}

{{知识点77}}{{基数排序：按关键字的各个位进行分配和收集。最低位优先（LSD）：从最低位到最高位依次排序。最高位优先（MSD）：从最高位到最低位递归排序。需要r个队列（r为基数）。时间复杂度O(d(n+r))，其中d为关键字位数。空间复杂度O(r)。稳定排序。适用于关键字位数较少且每位取值范围不大的情况。}}

{{知识点78}}{{内部排序算法的比较：从时间复杂度看，快速排序、堆排序、归并排序的平均时间复杂度为O(nlog₂n)。从空间复杂度看，归并排序需O(n)，快速排序需O(log₂n)（平均），其余为O(1)。从稳定性看，直接插入、折半插入、冒泡、归并、基数排序是稳定的；希尔、快速、简单选择、堆排序是不稳定的。n较小用直接插入或简单选择；基本有序用直接插入或冒泡；n较大用快速排序或堆排序；需要稳定用归并排序。}}

{{知识点79}}{{外部排序——多路归并：当数据量大于内存容量时，需要外部排序。基本方法是外部归并排序：生成初始归并段，然后多路归并。增大归并路数可以减少归并趟数从而减少I/O次数。k路归并使用"败者树"可以将每趟归并的比较次数从O(k)降低到O(log₂k)。}}

{{知识点80}}{{外部排序——置换-选择排序和最佳归并树：置换-选择排序用于生成长度不等的初始归并段，初始归并段的平均长度为内存工作区大小的两倍。最佳归并树是带权路径长度最短的k叉哈夫曼树，用于优化归并顺序以减少I/O总量。若(k-1)不整除(段数-1)，需补虚段。}}
