{{知识点名称}}基本性质{{知识点内容}}1. 可行域是各不等式表示的半平面的交集
2. 最值在可行域的顶点（角点）处取得
3. 若目标函数平行于可行域的某条边，则该边上所有点都是最优解
4. 求解步骤：画可行域 → 平移目标直线 → 找最优解▮{{知识点名称}}典型题型-题型1：求目标函数的最值{{知识点内容}}**解题步骤**:
1. 画出各约束条件对应的直线
2. 确定可行域（用特殊点判断半平面方向）
3. 将目标函数化为 $y = -\frac{a}{b}x + \frac{z-c}{b}$，平移求最值

**例题**: 约束条件 $\begin{cases} x + y \leq 4 \\ x \geq 0 \\ y \geq 0 \end{cases}$，求 $z = 2x + y$ 的最大值
**解析**: 可行域为三角形，顶点 $(0,0)$、$(4,0)$、$(0,4)$。$z(0,0) = 0$，$z(4,0) = 8$，$z(0,4) = 4$，最大值为8▮{{知识点名称}}典型题型-题型2：含参数的线性规划{{知识点内容}}**解题步骤**:
1. 含参数的约束条件影响可行域形状
2. 分析参数变化对可行域的影响
3. 结合目标函数求参数范围

**例题**: $x + y \leq 4$，$x \geq a$，$y \geq 0$，$z = x + 2y$ 的最大值为8，求 $a$
**解析**: 顶点 $(a, 0)$、$(4, 0)$、$(a, 4-a)$。$z(a, 0) = a$，$z(4, 0) = 4$，$z(a, 4-a) = a + 8 - 2a = 8 - a$。最大值为 $\max(a, 4, 8-a) = 8$，解得 $a = 0$▮{{知识点名称}}典型题型-题型3：非线性目标函数{{知识点内容}}**解题步骤**:
1. 将非线性目标赋予几何意义
2. 如 $\frac{y}{x}$ 表示斜率，$(x-a)^2 + (y-b)^2$ 表示距离的平方
3. 在可行域内找最优的几何位置

**例题**: 约束条件 $x + y \leq 4$，$x \geq 0$，$y \geq 0$，求 $\frac{y}{x+1}$ 的最大值
**解析**: $\frac{y}{x+1}$ 表示点 $(x, y)$ 与点 $(-1, 0)$ 连线的斜率。可行域顶点 $(0,0)$、$(4,0)$、$(0,4)$，分别计算斜率：$0$、$0$、$\frac{4}{1} = 4$，最大值为4▮{{知识点名称}}高考真题{{知识点内容}}- 2024年全国甲卷：线性规划求最值
- 2023年新高考I卷：含参数的线性规划
- 2022年全国乙卷：非线性目标函数的几何意义▮{{知识点名称}}易错点与易混点{{知识点内容}}1. 画可行域时，半平面方向用特殊点（如原点）判断
2. 最值在顶点处取得，但不要忘记验证所有顶点
3. 目标函数的斜率与可行域某边平行时，整条边都是最优解
4. 非线性目标函数要赋予几何意义，不能直接平移直线