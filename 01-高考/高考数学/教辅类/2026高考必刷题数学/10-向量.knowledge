# 向量
## 收集指南

> ⚠️ **严格范围限定：** 仅收集「向量」部分，禁止引用其他部分或其他书籍。

### 条目描述
本书2026高考必刷题数学中「向量」章节的知识点梳理，涵盖核心概念、公式定理、典型题型与易错点。

### 知识结构
- 考点1：向量的线性运算
- 考点2：向量的数量积与应用
- 考点3：向量与三角形
- 考点4：向量的坐标运算综合
- 考点5：平面向量与物理

### 待收集原子知识点
- 平面向量的基本运算：加法、减法、数乘
- 向量的数量积
- 向量的坐标运算

## 高频考点

### 考点1：向量的线性运算
**考查形式**: 向量加减、共线定理、基本定理
**解题技巧**:
- 三角形法则：AB + BC = AC
- 共线定理：向量 b 与非零向量 a 共线 ⟺ 存在唯一实数 λ 使 b = λa
- 基本定理：e₁, e₂ 不共线，则任意向量 a = λ₁e₁ + λ₂e₂（唯一表示）
- 中点向量：AM = (AB + AC)/2
- 重心向量：AG = (AB + AC + AD)/3（四面体）或 AG = (AB + AC)/3（三角形）

**典型例题**: △ABC 中，D 为 BC 中点，用 AB, AC 表示 AD。
**答案与解析**: AD = AB + BD = AB + (1/2)BC = AB + (1/2)(AC - AB) = (1/2)AB + (1/2)AC = (AB + AC)/2

### 考点2：向量的数量积与应用
**考查形式**: 求夹角、投影、判断垂直
**解题技巧**:
- a · b = |a||b|cosθ
- 坐标形式：a · b = x₁x₂ + y₁y₂
- 投影：proj_b(a) = (a · b / |b|²) · b
- a ⊥ b ⟺ a · b = 0
- |a|² = a · a

**典型例题**: a = (1, √3), b = (3, 0)，求 a 在 b 上的投影。
**答案与解析**: a · b = 3，|b| = 3，投影 = 3/3 = 1

### 考点3：向量与三角形
**考查形式**: 三角形中的向量关系、四心问题
**解题技巧**:
- 外心 O：OA = OB = OC（到三顶点等距）
- 重心 G：GA + GB + GC = 0
- 垂心 H：HA · HB = HB · HC = HC · HA
- 内心 I：到三边等距
- 欧拉线：O, G, H 三点共线，且 OG : GH = 1 : 2

**典型例题**: G 为 △ABC 重心，证明 GA + GB + GC = 0。
**答案与解析**: 设 D 为 BC 中点，AG = (2/3)AD = (2/3)(AB + AC)/2 = (AB + AC)/3。GA = -AG = -(AB + AC)/3。GB = GA + AB = (2AB - AC)/3。GC = GA + AC = (2AC - AB)/3。GA + GB + GC = [-(AB+AC) + (2AB-AC) + (2AC-AB)]/3 = 0

### 考点4：向量的坐标运算综合
**考查形式**: 向量与解析几何、函数结合
**解题技巧**:
- 向量条件 → 坐标方程 → 解析几何问题
- OA · OB 转化为 x₁x₂ + y₁y₂ → 韦达定理
- 向量模 → 距离公式
- 向量共线 → 斜率相等

**典型例题**: 椭圆 x²/4 + y² = 1，A, B 为椭圆上两点，OA ⊥ OB，求 1/OA² + 1/OB²。
**答案与解析**: 设 A(2cosα, sinα), B(2cosβ, sinβ)，OA·OB = 4cosαcosβ + sinαsinβ = 0。1/OA² + 1/OB² = 1/(4cos²α+sin²α) + 1/(4cos²β+sin²β)，利用条件化简

### 考点5：平面向量与物理
**考查形式**: 力的分解、速度合成
**解题技巧**:
- 力的合成 = 向量加法
- 力的分解 = 向量分解（正交分解或沿指定方向分解）
- 速度合成：相对速度 = 绝对速度 - 牵连速度
- 功 = 力 · 位移 = F · s（数量积）

**典型例题**: 两个力 F₁ = (3, 4), F₂ = (1, -2) 作用于物体，求合力大小。
**答案与解析**: F = F₁ + F₂ = (4, 2)，|F| = √(16 + 4) = 2√5

## 易错题型

1. **向量方向**: 共线向量可能同向或反向
2. **数量积的几何意义**: 投影是有方向的（可正可负）
3. **中点公式**: 中点坐标 = (A + B)/2，不要混淆
4. **向量与实数**: 向量不能比较大小，不能做除法

## 限时训练

1. a = (2, 1), b = (-3, 4)，求 3a + 2b。（建议用时：30秒）
   **答案**: 3(2,1) + 2(-3,4) = (6,3) + (-6,8) = (0, 11)

2. a = (1, 2), b = (2, -1)，验证 a ⊥ b。（建议用时：15秒）
   **答案**: a · b = 2 - 2 = 0，a ⊥ b ✓

3. |a| = 1, |b| = 2, a·b = -1，求 a 与 b 夹角。（建议用时：30秒）
   **答案**: cosθ = -1/2，θ = 120°

4. △ABC 中，A(1,0), B(0,2), C(3,4)，求 AB 边中线长。（建议用时：1分钟）
   **答案**: AB 中点 M(1/2, 1)，C(3,4)，CM = √[(3-1/2)² + (4-1)²] = √(25/4 + 9) = √(61/4) = √61/2
