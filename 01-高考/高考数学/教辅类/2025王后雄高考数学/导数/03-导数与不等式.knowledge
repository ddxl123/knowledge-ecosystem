# 导数与不等式

## 收集指南

> ⚠️ **严格范围限定：** 仅收集「导数与不等式」章节，禁止引用同书其他章节或其他书籍。

### 条目描述
本书2025王后雄高考数学中「导数与不等式」章节的知识点梳理，涵盖核心概念、公式定理、典型题型与易错点。

### 知识结构
- 题型1：利用导数证明不等式
- 题型2：含参不等式恒成立问题
- 题型3：双变量不等式

### 待收集原子知识点
- 导数的概念与几何意义
- 导数的运算：基本公式、四则运算、复合函数求导
- 导数与单调性：单调区间、极值
- 导数与最值：闭区间最值问题
- 导数综合应用：零点问题、不等式证明

## 基本性质
1. 常用不等式：$e^x \geq x + 1$（$x = 0$ 取等）
2. 常用不等式：$\ln x \leq x - 1$（$x = 1$ 取等）
3. 常用不等式：$x - \frac{x^2}{2} \leq \ln(1+x) \leq x$（$x > -1$）
4. 构造函数时选择合适的函数形式是关键
5. 双变量不等式常通过换元或消元转化为单变量问题

## 典型题型
### 题型1：利用导数证明不等式
**解题步骤**:
1. 构造函数 $h(x) = f(x) - g(x)$
2. 求 $h(x)$ 的最小值
3. 证明最小值 $\geq 0$

**例题**: 证明当 $x > 0$ 时，$e^x > 1 + x + \frac{x^2}{2}$
**解析**: 设 $h(x) = e^x - 1 - x - \frac{x^2}{2}$，$h'(x) = e^x - 1 - x$，$h''(x) = e^x - 1 > 0$（$x > 0$），故 $h'(x)$ 递增，$h'(x) > h'(0) = 0$，$h(x)$ 递增，$h(x) > h(0) = 0$

### 题型2：含参不等式恒成立问题
**解题步骤**:
1. 分离参数：$a > f(x)$ 恒成立 $\Leftrightarrow a > f(x)_{max}$
2. 或直接构造含参函数讨论最值
3. 注意端点和极限情况

**例题**: $f(x) = x - a\ln x \geq 1$ 对 $x > 0$ 恒成立，求 $a$ 的范围
**解析**: $f'(x) = 1 - \frac{a}{x} = \frac{x-a}{x}$。$a \leq 0$ 时 $f'(x) > 0$，$f(x)$ 递增，$f(1) = 1 \geq 1$ 成立。$a > 0$ 时 $f(x)$ 在 $x = a$ 取最小值 $a - a\ln a \geq 1$，即 $a(1 - \ln a) \geq 1$，解得 $a = 1$

### 题型3：双变量不等式
**解题步骤**:
1. 固定一个变量，研究另一个变量的函数
2. 利用单调性或最值
3. 或通过换元 $t = \frac{x_1}{x_2}$ 等转化为单变量

**例题**: $f(x) = x\ln x$，若 $0 < a < b$，证明 $f(a) + f(b) > 2f(\frac{a+b}{2})$（即 $\ln x$ 的凸性）
**解析**: 即证 $a\ln a + b\ln b > 2 \cdot \frac{a+b}{2}\ln\frac{a+b}{2}$，构造 $g(x) = x\ln x$，$g''(x) = \frac{1}{x} > 0$，$g(x)$ 为凸函数，由凸函数性质得证

## 高考真题
- 2024年新高考I卷：导数与不等式证明（压轴题）
- 2023年全国甲卷：含参恒成立与存在性问题
- 2022年新高考II卷：利用导数证明数列不等式

## 易错点与易混点
1. 构造函数后要注意定义域的限制
2. 恒成立和存在性问题的转化方向不同：$a > f(x)_{max}$ vs $a > f(x)_{min}$
3. 放缩不等式 $e^x \geq x+1$ 中取等条件是 $x = 0$，不是任意 $x$
4. 双变量问题中消元时要注意变量的约束条件
