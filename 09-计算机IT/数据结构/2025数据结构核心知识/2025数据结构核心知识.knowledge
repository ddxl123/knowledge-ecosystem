# 2025数据结构核心知识
# 领域: 计算机IT
# 学科: 数据结构
# 来源: 2025数据结构核心知识
# 描述: 数据结构核心知识点

{{知识点1}}{{数据结构的定义：数据结构是相互之间存在一种或多种特定关系的数据元素的集合，包括逻辑结构、物理结构和数据的运算三方面。}}

{{知识点2}}{{逻辑结构的分类：集合（元素间无关系）、线性结构（一对一）、树形结构（一对多）、图状/网状结构（多对多）。其中集合和图状结构属于非线性结构。}}

{{知识点3}}{{物理结构（存储结构）的分类：顺序存储（逻辑相邻物理相邻）、链式存储（用指针表示逻辑关系）、索引存储（建立索引表）、散列存储（根据关键字计算地址）。}}

{{知识点4}}{{算法的五个特性：有穷性、确定性、可行性、输入（零个或多个）、输出（一个或多个）。}}

{{知识点5}}{{算法的时间复杂度：表示算法执行时间随问题规模n增长的趋势，记作T(n)=O(f(n))。常见复杂度从低到高：O(1)<O(log n)<O(n)<O(n log n)<O(n²)<O(n³)<O(2ⁿ)<O(n!)。}}

{{知识点6}}{{算法的空间复杂度：S(n)=O(g(n))，表示算法所需额外存储空间随n增长的趋势。包括递归调用栈的空间。}}

{{知识点7}}{{线性表的定义：具有相同数据类型的n个数据元素的有限序列。除第一个和最后一个元素外，每个元素有且仅有一个前驱和一个后继。}}

{{知识点8}}{{顺序表（顺序存储）：用一组地址连续的存储单元依次存储线性表中的数据元素。优点：随机访问O(1)；缺点：插入删除需移动元素O(n)，静态分配大小固定。}}

{{知识点9}}{{顺序表插入操作平均移动次数：在位置i插入，需将第i到第n个元素后移，平均移动n/2个元素，时间复杂度O(n)。}}

{{知识点10}}{{顺序表删除操作平均移动次数：删除位置i的元素，需将第i+1到第n个元素前移，平均移动(n-1)/2个元素，时间复杂度O(n)。}}

{{知识点11}}{{单链表：每个结点包含数据域和指针域。通过指针将各结点串联。不需要连续存储空间，插入删除O(1)（已知结点位置时），但不支持随机访问。}}

{{知识点12}}{{头结点与头指针的区别：头指针是指向链表第一个结点的指针；头结点是在第一个结点之前附加的结点，其数据域可为空。使用头结点可以统一空表和非空表的操作。}}

{{知识点13}}{{双链表：每个结点有prior和next两个指针域，可双向遍历。插入操作需修改四个指针，删除操作需修改两个指针。}}

{{知识点14}}{{循环链表：最后一个结点的指针指向头结点（单循环）或头结点的next指向第一个结点（双向循环）。判断空表条件：L->next==L（带头结点单循环）。}}

{{知识点15}}{{静态链表：用数组实现链表，数组元素包含data和next（下一个元素的下标）。兼具顺序表和链表的特点，用于不支持指针的语言。}}

{{知识点16}}{{栈的定义：后进先出（LIFO）的线性表，只允许在栈顶进行插入和删除。基本操作：Push、Pop、GetTop、IsEmpty。}}

{{知识点17}}{{栈的顺序存储：用数组实现，top指针指向栈顶元素（top从0开始）或栈顶元素的下一个位置（top从-1开始）。共享栈（两栈共享空间）：两个栈从数组两端向中间增长。}}

{{知识点18}}{{栈的链式存储：链栈，栈顶在链表头部。入栈出栈都在表头操作，无需头结点。不会出现栈满上溢（除非内存耗尽）。}}

{{知识点19}}{{栈的应用——括号匹配：遇到左括号入栈，遇到右括号与栈顶左括号匹配并出栈。最终栈空则匹配成功。}}

{{知识点20}}{{栈的应用——表达式求值：中缀转后缀（逆波兰）表达式，利用栈存放运算符。后缀表达式求值用操作数栈，遇运算符弹出两个操作数计算后压栈。}}

{{知识点21}}{{栈的应用——递归转非递归：递归本质上利用了系统调用栈。可用显式栈模拟递归过程，避免栈溢出。}}

{{知识点22}}{{队列的定义：先进先出（FIFO）的线性表，队尾插入，队头删除。基本操作：EnQueue、DeQueue、GetHead、IsEmpty。}}

{{知识点23}}{{循环队列：用数组实现队列，通过取模运算使逻辑上首尾相接。队空条件：front==rear；队满条件：(rear+1)%MaxSize==front（牺牲一个存储单元）。元素个数：(rear-front+MaxSize)%MaxSize。}}

{{知识点24}}{{队列的链式存储：链队，队头删除，队尾插入。通常设置队头和队尾指针。空队列条件：front==rear（都指向头结点）。}}

{{知识点25}}{{双端队列：两端都可以进行入队和出队操作。分为输出受限（一端只能入队）和输入受限（一端只能出队）两种。}}

{{知识点26}}{{栈和队列的应用——BFS和DFS：图的广度优先搜索用队列，深度优先搜索用栈（或递归）。层序遍历二叉树用队列。}}

{{知识点27}}{{串的定义：由零个或多个字符组成的有限序列。空串长度为0，空格串是包含空格的串。子串是串中任意个连续字符组成的子序列。}}

{{知识点28}}{{KMP算法的核心思想：利用已匹配的信息避免回溯。next数组（部分匹配表）记录模式串中每个位置的最长相等前后缀长度。当匹配失败时，根据next数组跳过不必要的比较。}}

{{知识点29}}{{next数组的求法：next[0]=-1。对于位置j，next[j]等于模式串P[0..j-1]的最长相等真前缀和真后缀的长度。nextval数组是next的改进版，进一步避免多余比较。}}

{{知识点30}}{{树的定义：n个结点的有限集合（n=0为空树）。非空树有且仅有一个根结点，其余结点可分为m个互不相交的有限集，每个集合本身又是一棵树（子树）。}}

{{知识点31}}{{树的基本术语：结点的度（子结点数）、树的度（最大度）、叶子结点（度为0）、分支结点、层次（根为第1层）、树的高度/深度（最大层次）、有序树与无序树。}}

{{知识点32}}{{二叉树的定义：每个结点最多有两个子树（左子树和右子树），且有左右之分。五种基本形态：空、只有根、只有左子树、只有右子树、左右子树都有。}}

{{知识点33}}{{二叉树的性质1：第i层最多有2^(i-1)个结点（i≥1）。性质2：深度为k的二叉树最多有2^k-1个结点。性质3：叶子结点数n0 = 度为2的结点数n2 + 1，即n0 = n2 + 1。}}

{{知识点34}}{{满二叉树：深度为k且有2^k-1个结点的二叉树，每层都满。完全二叉树：除最后一层外每层都满，最后一层从左到右连续。完全二叉树中，结点i的左子结点为2i，右子结点为2i+1，父结点为⌊i/2⌋。}}

{{知识点35}}{{二叉树的顺序存储：用数组存储，适合完全二叉树。结点i的左孩子2i、右孩子2i+1、父结点⌊i/2⌋。对于普通二叉树需大量空间浪费。}}

{{知识点36}}{{二叉树的链式存储：二叉链表（lchild, data, rchild）。三叉链表增加parent指针。n个结点的二叉链表有n+1个空指针域。}}

{{知识点37}}{{二叉树的先序遍历（PreOrder）：根→左→右。中序遍历（InOrder）：左→根→右。后序遍历（PostOrder）：左→右→根。层序遍历：从上到下从左到右，用队列实现。}}

{{知识点38}}{{由遍历序列构造二叉树：已知先序+中序可唯一确定二叉树；已知后序+中序可唯一确定二叉树。但先序+后序不能唯一确定（除非是满二叉树等特殊情况）。}}

{{知识点39}}{{线索二叉树：利用空的lchild和rchild域存放遍历序列中的前驱和后继指针。增加ltag和rtag标志位区分孩子指针和线索。中序线索二叉树便于中序遍历，无需栈和递归。}}

{{知识点40}}{{树的存储结构——双亲表示法：数组存储，每个结点包含data和parent（父结点下标）。求父结点O(1)，求子结点需遍历O(n)。}}

{{知识点41}}{{树的存储结构——孩子表示法：每个结点的孩子用单链表存储。适合求子结点，但求父结点需遍历。}}

{{知识点42}}{{树的存储结构——孩子兄弟表示法（二叉树表示法）：每个结点包含第一个孩子指针和下一个兄弟指针。可将任意树转换为二叉树。}}

{{知识点43}}{{树转换为二叉树：每个结点的左指针指向其第一个孩子，右指针指向其下一个兄弟。转换后根结点没有右子树。}}

{{知识点44}}{{森林与二叉树的转换：森林中每棵树转换为二叉树后，第一棵树的根作为新二叉树的根，其余树的根依次作为前一棵树根的右子树。}}

{{知识点45}}{{哈夫曼树（最优二叉树）：带权路径长度WPL最小的二叉树。构造方法：每次选权值最小的两个结点合并，新结点权值为两者之和，重复直到只剩一个根结点。}}

{{知识点46}}{{哈夫曼编码：利用哈夫曼树进行前缀编码。左分支为0，右分支为1（或反之）。从根到叶子的路径即为该字符的编码。前缀编码保证任一编码不是其他编码的前缀，可唯一解码。}}

{{知识点47}}{{图的定义：G=(V,E)，V是顶点的有限非空集合，E是边的有限集合。有向图的边用<vi,vj>表示，无向图用(vi,vj)表示。}}

{{知识点48}}{{图的基本术语：完全图（无向：n(n-1)/2条边；有向：n(n-1)条弧）、稀疏图与稠密图、权与网、邻接点、度（无向图：度之和=2|E|；有向图：入度之和=出度之和=|E|）。}}

{{知识点49}}{{图的连通性：无向图中任意两点连通则为连通图。连通分量是无向图的极大连通子图。有向图中任意两点互相可达为强连通图。}}

{{知识点50}}{{图的邻接矩阵存储：用n×n矩阵A，A[i][j]=1（或权值）表示vi到vj有边。空间O(n²)，适合稠密图。无向图的邻接矩阵是对称的。}}

{{知识点51}}{{图的邻接表存储：每个顶点建立一个单链表，链接其所有邻接点。空间O(n+e)，适合稀疏图。有向图的邻接表便于求出度，逆邻接表便于求入度。}}

{{知识点52}}{{图的十字链表：有向图的存储结构，结合邻接表和逆邻接表。每条弧一个结点，每个顶点一个结点。可同时求入度和出度。}}

{{知识点53}}{{图的邻接多重表：无向图的存储结构。每条边一个结点，避免邻接表中一条边存储两次的问题。}}

{{知识点54}}{{图的深度优先搜索（DFS）：类似树的先序遍历。从某顶点出发，访问后递归访问其未访问的邻接点。时间复杂度：邻接矩阵O(n²)，邻接表O(n+e)。}}

{{知识点55}}{{图的广度优先搜索（BFS）：类似树的层序遍历。从某顶点出发，访问所有邻接点后再依次访问它们的邻接点。用队列实现。时间复杂度同DFS。}}

{{知识点56}}{{最小生成树——Prim算法：从某顶点开始，每次选择连接已选顶点集和未选顶点集的最小权边。时间O(n²)，适合稠密图。}}

{{知识点57}}{{最小生成树——Kruskal算法：每次选择权值最小且不构成回路的边。用并查集判断是否构成回路。时间O(e log e)，适合稀疏图。}}

{{知识点58}}{{最短路径——Dijkstra算法：求单源最短路径，适用于无负权边的图。每次选距离最小的未确定顶点，用其更新邻接点的距离。时间O(n²)或O((n+e)log n)（用优先队列）。不能处理负权边。}}

{{知识点59}}{{最短路径——Floyd算法：求所有顶点间的最短路径，适用于有负权边但无负权回路的图。动态规划思想：D^(k)[i][j] = min(D^(k-1)[i][j], D^(k-1)[i][k]+D^(k-1)[k][j])。时间O(n³)。}}

{{知识点60}}{{拓扑排序：对有向无环图（DAG）的顶点进行线性排序，使得对每条边<u,v>，u排在v前面。方法：每次选入度为0的顶点输出并删除相关边。可判断图中是否有环。}}

{{知识点61}}{{关键路径：在AOE网（边表示活动的有向网）中，从源点到汇点的最长路径。关键活动是关键路径上的活动，其延迟会导致整个工程延期。求法：求最早发生时间ve和最迟发生时间vl，ve=vl的顶点在关键路径上。}}

{{知识点62}}{{查找的基本概念：在数据集合中找出特定元素。平均查找长度ASL = Σ(Pi×Ci)，Pi为查找第i个元素的概率，Ci为查找该元素的比较次数。}}

{{知识点63}}{{顺序查找：从表的一端逐个比较。对n个元素的ASL=(n+1)/2。可设置哨兵免去边界判断。适用于顺序表和链表。}}

{{知识点64}}{{折半查找（二分查找）：要求有序顺序表。每次与中间元素比较，缩小一半搜索范围。ASL≈log₂(n+1)-1。不适用于链表（需随机访问）。}}

{{知识点65}}{{折半查找的判定树：是一棵平衡二叉树（不一定是完全二叉树）。树高为⌈log₂(n+1)⌉。查找成功最多比较树的高度次。}}

{{知识点66}}{{分块查找（索引顺序查找）：将表分成若干块，块内无序，块间有序。先在索引表中确定所在块，再在块内顺序查找。ASL介于顺序查找和折半查找之间。}}

{{知识点67}}{{B树（B-树）：m阶B树是多路平衡查找树。每个结点最多m个子树，最少⌈m/2⌉个子树（根至少2个）。所有叶子在同一层。关键字在结点内有序排列。}}

{{知识点68}}{{B树的查找：从根开始，在结点内顺序或折半查找关键字，确定子树方向。查找效率取决于树高，h ≤ log⌈m/2₎((n+1)/2)+1。}}

{{知识点69}}{{B树的插入：在叶子结点插入，若关键字数超过m-1则分裂，中间关键字上移至父结点。分裂可能向上传递直到根结点（根分裂则树高增1）。}}

{{知识点70}}{{B+树：与B树的区别：所有关键字都在叶子结点，叶子结点用链表相连便于范围查找；非叶子结点仅起索引作用。更适合文件系统和数据库索引。}}

{{知识点71}}{{散列表（哈希表）：根据关键字直接计算存储地址。哈希函数H(key)将关键字映射到地址空间。冲突（碰撞）：不同关键字映射到同一地址。}}

{{知识点72}}{{常见的哈希函数：除留余数法H(key)=key%p（p取不大于表长的最大素数）、直接定址法、数字分析法、平方取中法。}}

{{知识点73}}{{冲突处理——开放定址法：线性探测（逐个向后查找空位）、二次探测（按平方序列探测）、双重散列（用第二个哈希函数确定步长）。线性探测容易产生"堆积"。}}

{{知识点74}}{{冲突处理——链地址法（拉链法）：每个地址建立一个链表，冲突的元素链接到同一链表中。适合频繁插入删除，不会产生堆积。}}

{{知识点75}}{{散列表的装填因子α=表中记录数/表长。α越大冲突越多。开放定址法要求α<1，链地址法可以α>1。平均查找长度与α有关。}}

{{知识点76}}{{排序的稳定性：若排序前关键字相同的元素相对位置在排序后不变，则称该排序算法是稳定的。稳定的排序：冒泡、插入、归并、基数、计数、桶排序。不稳定的排序：选择、快速、希尔、堆排序。}}

{{知识点77}}{{内部排序与外部排序：内部排序是数据全部在内存中排序；外部排序是数据量太大需借助外存排序。}}

{{知识点78}}{{冒泡排序：相邻元素两两比较，每趟将最大（或最小）元素"冒泡"到末尾。最好O(n)（已有序，设标志位提前结束），最坏和平均O(n²)。稳定排序。}}

{{知识点79}}{{简单选择排序：每趟选最小元素放到已排序序列末端。比较次数始终为n(n-1)/2，时间O(n²)。不稳定（如序列5,5,2）。移动次数少。}}

{{知识点80}}{{直接插入排序：将未排序元素插入到已排序序列的正确位置。最好O(n)，最坏和平均O(n²)。稳定排序。对基本有序的数据效率高。}}

{{知识点81}}{{希尔排序：分组插入排序，逐步缩小增量。时间复杂度与增量序列有关，约为O(n^1.3)到O(n²)。不稳定排序。}}

{{知识点82}}{{快速排序：选一个基准元素（pivot），将序列分为小于和大于基准的两部分，递归排序。平均O(n log n)，最坏O(n²)（已有序时退化）。不稳定排序。实际应用中最快的比较排序。}}

{{知识点83}}{{快速排序的优化：三数取中法选pivot避免最坏情况；小规模数据切换为插入排序；尾递归优化减少栈深度。}}

{{知识点84}}{{归并排序：将两个有序子序列合并为一个有序序列。递归实现：分治思想，时间O(n log n)，空间O(n)。稳定排序。适用于外部排序。}}

{{知识点85}}{{堆排序：利用堆（完全二叉树）进行排序。建堆O(n)，每次取堆顶后调整O(log n)。总时间O(n log n)，空间O(1)。不稳定排序。}}

{{知识点86}}{{大根堆与小根堆：大根堆中每个结点的值≥其子结点的值，堆顶为最大值；小根堆中每个结点的值≤其子结点的值，堆顶为最小值。}}

{{知识点87}}{{堆的调整：将堆顶取出后，将最后一个元素放到堆顶，然后向下调整（与较大的子结点交换），维护堆性质。时间O(log n)。}}

{{知识点88}}{{基数排序：按位分配收集，从最低位到最高位（LSD）或反之（MSD）。对d位r进制数，时间O(d(n+r))。稳定排序，不需要比较。适用于关键字位数较少的整数排序。}}

{{知识点89}}{{计数排序：统计每个值出现的次数，确定每个值的最终位置。时间O(n+k)，k为值域范围。稳定排序。适用于值域较小的情况。}}

{{知识点90}}{{桶排序：将元素分配到若干桶中，每个桶内单独排序，最后合并。平均O(n+k)，最坏O(n²)。适用于数据均匀分布的情况。}}

{{知识点91}}{{排序算法的比较：时间复杂度方面，快排/归并/堆排平均O(n log n)；空间方面，快排O(log n)（栈），归并O(n)，堆排O(1)；稳定性方面，归并和冒泡稳定，快排和堆排不稳定。}}

{{知识点92}}{{外部排序——多路归并：数据分段排序后写入外存，再多路归并。使用败者树（或胜者树）减少关键字比较次数，每趟归并的比较次数从O(km)降到O(m log k)。}}

{{知识点93}}{{败者树与胜者树：胜者树中每个非叶子结点存放比赛的胜者（较小值），败者树存放败者而胜者继续向上比较。败者树在多路归并中更高效，因为新元素只需与父结点比较。}}

{{知识点94}}{{置换-选择排序：用于生成初始归并段。在内存中维护一个最小堆，依次输出堆顶（必须≥上次输出值），直到堆顶小于上次输出值时该段结束。可生成长度大于内存的初始归并段。}}

{{知识点95}}{{红黑树：自平衡二叉搜索树。每个结点为红色或黑色，满足：根为黑、红结点的子结点为黑、从任一结点到其叶子的路径上黑结点数相同。插入删除后通过旋转和变色恢复平衡。查找插入删除均为O(log n)。}}

{{知识点96}}{{AVL树（平衡二叉树）：任意结点的左右子树高度差不超过1。通过LL、RR、LR、RL四种旋转操作维护平衡。查找效率O(log n)。比红黑树平衡性更严格，插入删除时旋转可能更频繁。}}

{{知识点97}}{{并查集：用于处理不相交集合的合并与查询。用树形结构表示集合，每个集合用一棵树表示，根结点为集合代表。路径压缩和按秩合并可将操作均摊时间优化至O(α(n))，α为反阿克曼函数。}}

{{知识点98}}{{字典树（Trie树）：用于字符串集合的存储和查找。每个结点代表一个字符，从根到叶子的路径代表一个字符串。查找时间O(m)（m为字符串长度），与集合大小无关。常用于自动补全和拼写检查。}}

{{知识点99}}{{跳表（Skip List）：在有序链表基础上增加多级索引。通过随机化决定每个元素的层数。期望查找时间O(log n)，实现简单，是平衡树的替代方案。Redis的有序集合底层使用跳表。}}

{{知识点100}}{{布隆过滤器：一种概率型数据结构，用于判断元素是否在集合中。使用多个哈希函数和位数组。可能产生假阳性（误判存在），但不会产生假阴性（误判不存在）。空间效率极高，常用于缓存穿透防护。}}
