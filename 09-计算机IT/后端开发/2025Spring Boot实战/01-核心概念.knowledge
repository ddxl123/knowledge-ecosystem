# 核心概念

## 自动配置
- @SpringBootApplication = @SpringBootConfiguration + @EnableAutoConfiguration + @ComponentScan
- 自动配置原理：spring.factories/AutoConfiguration.imports注册配置类
- 条件注解：@ConditionalOnClass、@ConditionalOnProperty、@ConditionalOnMissingBean
- 配置优先级：命令行参数 > 系统环境变量 > application.properties > 默认值
- 自定义Starter：创建自动配置模块，META-INF注册

## 依赖注入
- @Component、@Service、@Repository、@Controller：注册Bean
- @Autowired：自动注入（构造器注入推荐）
- @Qualifier：指定注入的Bean名称
- @Value：注入配置值，@Value("${server.port}")
- @ConfigurationProperties：类型安全的配置绑定

## Web开发
- @RestController = @Controller + @ResponseBody
- @GetMapping/@PostMapping/@PutMapping/@DeleteMapping
- @PathVariable：路径参数；@RequestParam：查询参数
- @RequestBody：请求体JSON反序列化
- ResponseEntity：自定义响应状态码和头信息

## AOP面向切面
- @Aspect：切面类
- 切点表达式：@Pointcut("execution(* com.example.service.*.*(..))")
- 通知类型：@Before、@After、@AfterReturning、@AfterThrowing、@Around
- 应用场景：日志记录、权限校验、性能监控、事务管理
- 自定义注解+AOP：注解驱动的横切关注点

## 配置管理
- application.properties / application.yml
- 多环境配置：application-{profile}.yml，spring.profiles.active
- 配置加密：Jasypt加密敏感配置
- 配置中心：Nacos、Apollo、Spring Cloud Config
- 外部化配置：环境变量、命令行参数、配置中心
