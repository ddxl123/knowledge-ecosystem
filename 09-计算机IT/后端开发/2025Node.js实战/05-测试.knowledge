# 测试

## 测试框架
- Jest：零配置测试框架，内置断言、Mock、覆盖率
- Mocha + Chai：灵活的测试框架+断言库
- Vitest：Vite生态测试框架，兼容Jest API
- 测试文件：*.test.js 或 *.spec.js
- describe/it/test：组织测试用例

## 单元测试
- 断言：expect(value).toBe(expected)、toEqual、toContain
- Mock：jest.fn()、jest.mock('module')
- 异步测试：async/await、.resolves/.rejects
- 快照测试：expect(component).toMatchSnapshot()
- 测试覆盖：--coverage生成覆盖率报告

## API测试
- Supertest：HTTP接口测试
- request(app).get('/api/users').expect(200)
- 请求体：.send({ name: 'Tom' })
- 响应验证：.expect(res => { expect(res.body.name).toBe('Tom') })
- 与Express/Express Router配合

## 数据库测试
- 测试数据库：独立测试数据库，避免污染开发数据
- beforeEach清理数据：await User.deleteMany({})
- 测试数据工厂：faker.js生成随机数据
- 事务回滚：测试后回滚数据库变更
- 内存数据库：mongodb-memory-server

## E2E测试
- Playwright：跨浏览器E2E测试
- Puppeteer：Chrome无头浏览器测试
- Cypress：前端E2E测试框架
- 场景：用户注册登录、表单提交、页面导航
- CI集成：GitHub Actions自动运行
