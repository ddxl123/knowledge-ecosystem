# 认证与安全

## JWT认证
- jsonwebtoken：jwt.sign(payload, secret, { expiresIn })
- 验证：jwt.verify(token, secret)
- 中间件验证：Authorization: Bearer <token>
- Refresh Token：长期Token刷新短期Access Token
- Token黑名单：Redis存储已注销Token

## Passport.js
- 策略模式：LocalStrategy、JWTStrategy、OAuthStrategy
- app.use(passport.initialize())
- passport.authenticate('jwt', { session: false })
- 本地策略：验证用户名密码
- OAuth策略：Google、GitHub、微信登录

## 密码安全
- bcrypt：密码哈希，bcrypt.hash(password, 10)
- 验证：bcrypt.compare(password, hashedPassword)
- 密码策略：最小长度、复杂度要求
- 防暴力破解：登录失败次数限制、账户锁定
- 密码重置：邮件发送重置链接

## OAuth2.0
- 授权码流程：用户授权→获取code→换取Token
- passport-google-oauth20：Google登录
- passport-github2：GitHub登录
- 自建OAuth2服务器：node-oauth2-server
- PKCE：移动端安全增强

## API安全
- CORS：cors中间件精确配置
- Rate Limiting：express-rate-limit
- Helmet：设置安全HTTP头
- XSS防护：输入消毒、输出编码
- SQL注入：使用ORM/参数化查询
