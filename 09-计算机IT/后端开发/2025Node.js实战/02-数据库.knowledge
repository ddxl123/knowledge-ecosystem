# 数据库操作

## MongoDB与Mongoose
- Schema定义：new Schema({ name: String, age: Number })
- Model：mongoose.model('User', schema)
- CRUD：create、find、findById、findByIdAndUpdate、findByIdAndDelete
- 验证：required、enum、min/max、自定义验证器
- 中间件：pre/post钩子（save、remove）

## 关系与嵌入
- 嵌入式：将关联数据嵌入文档
- 引用式：ObjectId引用其他集合，populate()填充
- 虚拟属性：schema.virtual('fullName').get(function() { ... })
- 索引：schema.index({ email: 1 }, { unique: true })
- 聚合管道：Model.aggregate([{ $match: ... }, { $group: ... }])

## SQL数据库
- Sequelize：支持MySQL/PostgreSQL/SQLite
- 模型定义：sequelize.define('User', { name: DataTypes.STRING })
- 关联：hasOne、hasMany、belongsTo、belongsToMany
- 迁移：sequelize-cli创建和执行迁移
- 原始查询：sequelize.query('SELECT * FROM users')

## Prisma ORM
- Schema定义：prisma/schema.prisma文件
- prisma generate：生成类型安全的客户端
- CRUD：prisma.user.create/findMany/update/delete
- 关系：@relation定义模型关系
- 迁移：prisma migrate dev，自动SQL迁移

## 缓存与Redis
- ioredis：Redis客户端
- 缓存策略：查询结果缓存、Session存储
- 发布订阅：redis.subscribe/publish
- 限流：滑动窗口、令牌桶实现
- 分布式锁：Redlock算法
