# 模型

## 模型定义
- 继承models.Model：class Article(models.Model) {}
- 字段类型：CharField、IntegerField、TextField、DateField、BooleanField、FileField
- 字段选项：max_length、default、null、blank、unique、choices、db_index
- 主键：自动添加id字段，或primary_key=True自定义
- __str__方法：对象的可读表示

## 关系字段
- ForeignKey：多对一关系，on_delete级联策略（CASCADE/SET_NULL/PROTECT）
- ManyToManyField：多对一对关系，自动创建中间表
- OneToOneField：一对一关系
- related_name：反向查询名称
- through：自定义中间表（额外字段）

## 查询API
- QuerySet惰性求值：链式调用，执行时才查询数据库
- 过滤：filter()、exclude()、get()
- 条件：__exact、__contains、__in、__gt、__lt、__isnull、__range
- 排序：order_by('-created_at')
- 聚合：aggregate(Avg('score'), Count('id'))
- 分组：annotate(Count('category'))

## 迁移
- makemigrations：生成迁移文件
- migrate：执行迁移到数据库
- 迁移文件：operations列表，CreateModel/AlterField/AddField
- 数据迁移：RunPython执行数据转换
- 冲突处理：--merge合并冲突迁移

## 高级特性
- 自定义Manager：objects = ArticleManager()
- 模型方法：自定义业务逻辑方法
- 抽象基类：abstract = True，不创建数据库表
- 代理模型：proxy = True，不创建新表，修改行为
- 信号：pre_save、post_save、pre_delete、post_delete
