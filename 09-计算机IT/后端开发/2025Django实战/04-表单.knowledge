# 表单

## 表单定义
- class ArticleForm(forms.Form): title = forms.CharField(max_length=100)
- ModelForm：class ArticleForm(forms.ModelForm): model = Article
- 字段类型：CharField、EmailField、IntegerField、ChoiceField、FileField
- 字段选项：required、initial、widget、label、help_text、error_messages
- 清洗方法：clean_title()、clean()（全局验证）

## 表单处理
- GET请求：显示空表单 form = ArticleForm()
- POST请求：验证和保存 form = ArticleForm(request.POST)
- is_valid()：验证表单数据
- cleaned_data：获取清洗后的数据
- save()：ModelForm保存到数据库

## 表单渲染
- {{ form.as_p }}、{{ form.as_table }}、{{ form.as_ul }}
- 手动渲染：{{ form.title }}、{{ form.title.label_tag }}、{{ form.title.errors }}
- Widget定制：attrs={'class': 'form-control'}
- CSRF保护：{% csrf_token %}

## 文件上传
- FileField/MultipleFileField
- request.FILES获取上传文件
- 存储：FileSystemStorage、S3Boto3Storage
- 图片处理：Pillow库
- 上传大小限制：DATA_UPLOAD_MAX_MEMORY_SIZE

## 表单集
- formset_factory：管理多个表单实例
- ModelFormSet：基于模型的表单集
- inlineformset_factory：关联模型的表单集
- 管理表单：增删改多个对象
- extra/max_num：控制初始和最大表单数
