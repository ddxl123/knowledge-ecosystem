# 2025计算机图形学入门
# 领域: 计算机IT
# 学科: 计算机图形学
# 来源: 2025计算机图形学入门
# 描述: 计算机图形学核心知识点，涵盖渲染管线、光照模型、纹理映射、几何变换、着色器、光线追踪等基础概念

{{图形学概述}}{{计算机图形学是研究如何利用计算机生成、处理和显示图形的学科。核心任务包括几何建模、渲染、动画和交互。应用领域覆盖游戏开发、影视特效、虚拟现实、科学可视化、CAD/CAM等。2025年，实时渲染和AI辅助图形生成是两大前沿方向。}}

{{渲染管线概述}}{{渲染管线（Rendering Pipeline）是GPU将3D场景转换为2D图像的流水线过程。主要阶段包括：应用程序阶段（CPU端）、几何阶段（顶点处理、图元装配、光栅化）和光栅化阶段（片段着色、帧缓冲操作）。理解管线是掌握图形学的基础。}}

{{顶点着色器}}{{顶点着色器（Vertex Shader）是渲染管线中第一个可编程阶段，对每个顶点独立执行。主要任务包括：顶点位置变换（模型空间→裁剪空间）、法线变换、纹理坐标计算、逐顶点光照等。输入为顶点属性（位置、法线、UV等），输出变换后的顶点。}}

{{片段着色器}}{{片段着色器（Fragment Shader / Pixel Shader）计算每个像素片段的最终颜色。接收插值后的顶点数据，执行纹理采样、光照计算、阴影判断等。是实现各种视觉效果的核心阶段，性能直接影响渲染帧率。}}

{{图元装配与光栅化}}{{图元装配将顶点组装为基本图元（三角形、线段、点）。光栅化器将图元转换为离散的片段（fragments），确定哪些像素被图元覆盖，并对顶点属性进行插值。三角形是GPU最高效处理的基本图元。}}

{{坐标系统变换}}{{3D渲染涉及多个坐标空间的变换链：模型空间→世界空间→观察空间→裁剪空间→NDC→屏幕空间。核心变换矩阵包括Model矩阵（物体变换）、View矩阵（相机变换）、Projection矩阵（投影变换），简称MVP变换。}}

{{齐次坐标}}{{齐次坐标用N+1维表示N维点，3D点表示为(x,y,z,w)。w=1表示点，w=0表示向量。齐次坐标统一了平移、旋转、缩放等仿射变换为矩阵乘法，使得变换可以级联组合。透视除法（除以w）实现透视投影效果。}}

{{投影变换}}{{投影分为正交投影和透视投影。正交投影保持平行线平行，用于CAD/工程图；透视投影模拟人眼近大远小效果，产生灭点。透视投影通过非线性z映射实现深度缓冲，近平面映射到0，远平面映射到1。}}

{{深度缓冲}}{{深度缓冲（Z-Buffer）存储每个像素的深度值，通过深度测试解决可见性问题。标准深度缓冲精度非线性分布，近处精度高远处精度低，可能导致z-fighting（深度冲突）。反向Z缓冲（Reverse Z）可改善远处精度分布。}}

{{Phong光照模型}}{{Phong光照模型将表面光照分解为三个分量：环境光（Ambient，模拟间接光照）、漫反射（Diffuse，Lambert余弦定律）、镜面反射（Specular，高光）。公式：I = Ka*La + Kd*Ld*(N·L) + Ks*Ls*(R·V)^n。简单高效，广泛用于实时渲染。}}

{{Blinn-Phong光照模型}}{{Blinn-Phong是Phong模型的改进，用半程向量H替代反射向量R，高光计算变为(N·H)^n。优势：当光源和观察者都在无穷远时避免逐像素计算反射向量，GPU效率更高。OpenGL固定管线默认使用此模型。}}

{{PBR物理渲染}}{{基于物理的渲染（Physically Based Rendering）遵循能量守恒和菲涅尔方程。核心概念：金属度（Metallic）、粗糙度（Roughness）、法线贴图。微表面理论假设表面由微小镜面组成，粗糙度决定微表面法线分布。Disney BRDF和Cook-Torrance模型是主流PBR实现。}}

{{着色器语言GLSL}}{{GLSL（OpenGL Shading Language）是OpenGL的着色器语言，语法类似C。支持向量/矩阵类型（vec4, mat4）、内置函数（normalize, dot, mix）、varying/attribute/uniform变量传递。HLSL用于DirectX，WGSL用于WebGPU，SPIR-V为中间表示。}}

{{顶点缓冲对象VBO}}{{VBO（Vertex Buffer Object）将顶点数据存储在GPU显存中，避免每帧从CPU传输数据。通过glGenBuffers创建，glBindBuffer绑定，glBufferData上传。结合VAO（Vertex Array Object）管理顶点属性布局，是现代OpenGL的核心数据管理方式。}}

{{索引缓冲}}{{索引缓冲（Index Buffer / EBO）存储顶点索引，避免重复存储共享顶点。一个立方体8个顶点而非36个（6面×2三角×3顶点）。使用glDrawElements代替glDrawArrays进行索引绘制，显著减少内存占用和顶点着色器调用次数。}}

{{纹理映射}}{{纹理映射将2D图像贴附到3D表面，通过UV坐标（0-1范围）建立表面点与纹理像素的对应。纹理过滤方式：最近邻（锯齿明显）、双线性插值（平滑）、各向异性过滤（倾斜表面质量好）。Mipmap预计算多级缩小纹理，提高远距离采样质量。}}

{{法线贴图}}{{法线贴图（Normal Map）用RGB编码表面法线方向，在不增加几何复杂度的情况下模拟凹凸细节。切线空间法线贴图（蓝紫色调）可复用于不同朝向的表面。片段着色器中将法线从切线空间变换到世界空间后参与光照计算。}}

{{阴影映射}}{{阴影映射（Shadow Mapping）从光源视角渲染深度图，然后在相机渲染时比较片段深度与深度图值判断是否在阴影中。问题包括：阴影痤疮（通过bias偏移解决）、锯齿边缘（PCF软阴影缓解）、透视走级联阴影映射（CSM）改善。}}

{{环境光遮蔽AO}}{{环境光遮蔽（Ambient Occlusion）模拟环境光被几何体遮挡的效果，在缝隙、角落处产生自然暗化。SSAO（屏幕空间AO）是最常用的实时方法，采样像素周围深度判断遮蔽程度。2025年GTAO（Ground Truth AO）成为主流高质量实时AO方案。}}

{{帧缓冲对象FBO}}{{帧缓冲对象（Frame Buffer Object）允许离屏渲染到纹理，是实现后处理效果的基础。可附加颜色附件、深度附件、模板附件。常见应用：高斯模糊、Bloom辉光、HDR色调映射、屏幕空间反射等。延迟渲染需要多个FBO存储G-Buffer。}}

{{延迟渲染}}{{延迟渲染（Deferred Rendering）分两步：先渲染G-Buffer存储位置、法线、材质等信息，再对每个光源逐像素计算光照。优势：光照复杂度与场景几何解耦，支持大量动态光源。劣势：显存占用大、不支持透明物体、MSAA兼容性差。2025年前向+延迟混合管线更常见。}}

{{Instanced渲染}}{{实例化渲染（Instanced Rendering）一次绘制调用渲染大量相似物体（如草地、树木、粒子）。通过glDrawArraysInstanced/glDrawElementsInstanced调用，每个实例可用gl_InstanceID获取唯一属性。结合实例属性缓冲可为每个实例传递不同变换/颜色，极大减少Draw Call。}}

{{曲面细分}}{{曲面细分（Tessellation）在GPU上动态增加几何细节，包括三个阶段：细分控制着色器（决定细分级别）、细分器（生成新顶点）、细分评估着色器（计算最终位置）。用于地形LOD、角色细节增强等，比CPU端细分更高效。}}

{{几何着色器}}{{几何着色器（Geometry Shader）可增删改图元，输入完整图元（如三角形），输出任意图元。典型应用：粒子系统billboard、线条粗细分割、毛发生成、阴影体构建。性能不如计算着色器灵活，2025年趋向被Mesh Shader替代。}}

{{Mesh Shader}}{{Mesh Shader是新一代几何处理管线，替代传统顶点+几何着色器。支持GPU驱动的几何剔除和LOD选择，每个工作组处理一组meshlet（小网格块）。NVIDIA Turing架构引入，2025年已广泛支持。显著提升复杂场景渲染效率。}}

{{计算着色器}}{{计算着色器（Compute Shader）通用GPU计算单元，不绑定渲染管线。可执行任意并行计算：粒子模拟、图像后处理、GPU排序、BVH构建等。通过工作组（workgroup）和共享内存（shared memory）实现高效并行。是2025年GPU编程的核心能力。}}

{{光线追踪基础}}{{光线追踪（Ray Tracing）从相机发射光线，计算与场景的交点，递归追踪反射/折射光线。核心操作：光线-三角形求交、BVH加速结构遍历。相比光栅化，自然支持软阴影、反射、折射、全局光照，但计算量大。2025年硬件RT Core加速成为标配。}}

{{BVH加速结构}}{{包围体层次结构（BVH）将场景几何体组织为树形结构，节点用AABB包围盒包裹。遍历时先测试包围盒，不相交则跳过整棵子树，将O(n)求交降低为O(log n)。构建质量（SAH启发式）和遍历效率是光线追踪性能的关键。}}

{{光追与光栅化混合}}{{2025年主流渲染引擎采用光栅化+光追混合方案：光栅化处理主可见性（快速），光追处理反射、阴影、全局光照等需要光线追踪的效果。典型应用：RTX DI（直接照明光追）、RTX GI（间接照明），平衡画质与性能。}}

{{延迟光照与前向渲染对比}}{{前向渲染（Forward Rendering）每个物体一次pass处理所有光源，光源多时开销大。延迟渲染分离几何和光照阶段，适合多光源场景。Forward+在前向基础上添加光源剔除，兼顾透明物体支持。2025年根据场景特性混合使用多种管线。}}

{{HDR与色调映射}}{{HDR（高动态范围）渲染使用浮点帧缓冲存储超出0-1范围的亮度值，保留亮部和暗部细节。色调映射（Tone Mapping）将HDR映射到显示器可显示的LDR范围。常用曲线：Reinhard、ACES、Filmic。Bloom效果通常在HDR空间计算。}}

{{抗锯齿技术}}{{抗锯齿（Anti-Aliasing）消除边缘锯齿。MSAA在光栅化阶段多重采样；FXAA/后处理AA速度快但模糊；TAA（时间抗锯齿）利用前帧信息，2025年最主流；DLSS/FSR利用AI超分辨率在低分辨率渲染后放大，兼顾画质与性能。}}

{{透明度渲染}}{{透明物体渲染挑战：需要从后到前排序（画家算法），或使用OIT（顺序无关透明度）。OIT方案包括：深度剥离（多pass）、per-pixel linked lists（显存开销大）、Moment-Based OIT。2025年混合OIT方案在游戏引擎中逐渐普及。}}

{{粒子系统}}{{粒子系统用大量小粒子模拟烟雾、火焰、水花等效果。每个粒子有位置、速度、生命值、颜色等属性。GPU粒子系统利用计算着色器更新粒子状态，Instanced渲染绘制。2025年流体模拟（SPH、FLIP）与粒子系统结合更紧密。}}

{{骨骼动画}}{{骨骼动画用骨骼层级变换驱动网格顶点。每个顶点关联多根骨骼（蒙皮权重），最终位置为各骨骼变换的加权混合。动画数据存储关键帧，插值播放。2025年神经网络动画（motion matching、生成式动画）与传统骨骼动画融合。}}

{{场景图与空间管理}}{{场景图组织场景对象的层级关系，支持变换继承。空间加速结构（BVH、Octree、BSP、Grid）用于快速剔除不可见物体。视锥体剔除（Frustum Culling）跳过相机外的物体，遮挡剔除（Occlusion Culling）跳过被遮挡的物体。}}

{{着色器编译与链接}}{{着色器从源码编译为GPU指令的过程：源码→预处理→编译→链接为程序对象。常见优化：uniform缓冲对象（UBO）批量传递参数、着色器变体管理（uber shader分支）、运行时编译缓存。2025年Shader Permutation管理是引擎优化重点。}}

{{图形API对比}}{{主流图形API：OpenGL（易学，2025年维护模式）、Vulkan（低开销，显式控制）、DirectX 12（Windows平台主力）、Metal（Apple平台）、WebGPU（浏览器标准，2025年广泛支持）。现代API强调多线程命令录制、显式资源管理、管线状态对象（PSO）。}}

{{实时渲染优化策略}}{{2025年实时渲染优化核心策略：GPU-driven rendering（GPU端剔除和调度）、虚拟几何（Nanite技术，自动LOD）、虚拟纹理（按需加载纹理页）、可变速率着色（VRS，边缘降低着色率）、异步计算（光追与光栅化并行）。帧预算通常16.67ms（60fps）。}}

{{渲染方程}}{{渲染方程（Rendering Equation）描述光在场景中的传输：Lo(p,ωo) = Le(p,ωo) + ∫fr(p,ωi,ωo)Li(p,ωi)(n·ωi)dωi。出射光=自发光+入射光经BRDF加权的积分。路径追踪、辐照度缓存、光子映射等全局光照算法都是对此方程的不同近似求解。}}

{{全局光照GI}}{{全局光照（Global Illumination）考虑光线多次反弹的间接照明。方法包括：光线追踪（精确但慢）、辐照度缓存/探针（预计算间接光）、屏幕空间GI（SSGI，实时但信息有限）、光子映射、辐射度方法。2025年Lumen（UE5）和RTXGI代表实时GI前沿。}}

{{材质系统}}{{现代材质系统由参数化材质属性组成：反照率（Albedo）、金属度、粗糙度、法线、自发光、遮蔽、高度等贴图。材质图（Material Graph）可视化编辑器允许美术组合节点生成着色器代码。2025年材质分层（Layered Material）支持复杂材质混合。}}

{{纹理压缩}}{{纹理压缩减少显存占用和带宽消耗。常用格式：BC1（RGB，4:1压缩）、BC3（RGBA，带alpha）、BC5（法线贴图双通道）、BC7（高质量RGBA）、ASTC（移动端灵活块大小）。2025年BC7和ASTC为主流，BC6H用于HDR纹理。}}

{{图像后处理流水线}}{{后处理在渲染完成后对图像进行全屏处理：Bloom提取并模糊亮区、色调映射将HDR转LDR、FXAA/TAA抗锯齿、景深（DOF）模拟镜头散焦、运动模糊、镜头畸变/色散。通过FBO链式执行，每步读取上一步结果。}}

{{GPU内存管理}}{{GPU显存管理是性能关键。资源类型：纹理、缓冲区、渲染目标。管理策略：资源池化（避免频繁分配释放）、流式加载（按需上传）、内存别名（不同生命周期资源复用同一内存）。2025年ReBAR（Resizable BAR）允许CPU直接访问全量显存。}}

{{多线程渲染}}{{现代图形API支持多线程命令录制：多线程同时构建命令列表，最后提交到GPU。Vulkan的Secondary Command Buffer、DX12的Bundle支持此模式。线程模型：每线程一个命令池、工作窃取、帧并行。2025年引擎普遍实现多线程提交，减少CPU端瓶颈。}}

{{WebGPU与Web图形}}{{WebGPU是2025年浏览器图形新标准，替代WebGL。提供类Vulkan的现代API：显式管线状态、计算着色器、存储缓冲。WGSL着色器语言。支持Web端实时光追（实验阶段）。与Three.js、Babylon.js等框架集成，推动Web端高质量3D体验。}}

{{AI与图形学融合}}{{2025年AI深度融入图形学：神经辐射场（NeRF）从照片重建3D场景、3D Gaussian Splatting实时渲染、AI超分辨率（DLSS/FSR/XeSS）、神经纹理压缩、生成式AI创建3D资产和材质。AI正在改变传统图形管线的多个环节。}}
