# 2025设计模式入门
# 领域: 计算机IT
# 学科: 设计模式
# 来源: 2025设计模式入门
# 描述: 设计模式核心知识点

{{设计模式定义}}{{设计模式（Design Pattern）是对软件设计中反复出现的问题所提出的通用解决方案。它不是可以直接复制粘贴的代码，而是描述了在特定场景下如何组织类和对象以解决特定设计问题的模板。设计模式提高了代码的可复用性、可扩展性和可维护性。}}

{{设计模式的起源}}{{设计模式的概念最早由建筑师 Christopher Alexander 提出，后被 GoF（Gang of Four，四人帮）——Erich Gamma、Richard Helm、Ralph Johnson 和 John Vlissides 在 1994 年的《Design Patterns: Elements of Reusable Object-Oriented Software》一书中系统化，收录了 23 个经典设计模式，奠定了面向对象设计模式的基础。}}

{{设计模式三大分类}}{{设计模式按目的分为三大类：（1）创建型模式（Creational Patterns）——处理对象创建机制，如工厂方法、抽象工厂、单例、建造者、原型；（2）结构型模式（Structural Patterns）——处理类与对象的组合，如适配器、桥接、组合、装饰器、外观、享元、代理；（3）行为型模式（Behavioral Patterns）——处理对象间的通信与职责分配，如责任链、命令、迭代器、中介者、备忘录、观察者、状态、策略、模板方法、访问者。}}

{{开闭原则 OCP}}{{开闭原则（Open/Closed Principle）由 Bertrand Meyer 提出：软件实体（类、模块、函数等）应该对扩展开放，对修改关闭。即在不修改现有代码的情况下，通过添加新代码来扩展功能。实现方式通常依赖抽象和多态，例如通过接口或抽象类定义契约，新增功能只需添加新的实现类。}}

{{里氏替换原则 LSP}}{{里氏替换原则（Liskov Substitution Principle）：子类型必须能够替换其基类型而不影响程序的正确性。即任何使用父类对象的地方，都应该能够透明地使用子类对象。违反 LSP 的典型例子：正方形继承长方形，重写 setWidth/setHeight 导致行为不一致。}}

{{单一职责原则 SRP}}{{单一职责原则（Single Responsibility Principle）：一个类应该只有一个引起它变化的原因。每个类只负责一项职责。违反 SRP 的表现：一个类既负责业务逻辑又负责数据持久化。拆分后，每个类职责清晰，修改一个功能不会影响另一个。}}

{{接口隔离原则 ISP}}{{接口隔离原则（Interface Segregation Principle）：客户端不应该被迫依赖它不使用的接口。应该将臃肿的接口拆分为更小、更具体的接口。例如，将一个大接口 `Worker` 拆分为 `Workable`、`Eatable`、`Sleepable`，让不同角色只实现需要的接口。}}

{{依赖倒置原则 DIP}}{{依赖倒置原则（Dependency Inversion Principle）：高层模块不应该依赖低层模块，二者都应该依赖抽象；抽象不应该依赖细节，细节应该依赖抽象。核心是面向接口编程。例如，业务层不直接依赖 MySQL 实现，而是依赖数据库接口，可随时替换为 PostgreSQL。}}

{{单一职责原则 SRP 实战}}{{SRP 实战示例：一个 `UserService` 类同时处理用户注册、发送邮件和记录日志，违反了 SRP。重构为 `UserService`（注册）、`EmailService`（发邮件）、`LoggerService`（记日志），每个类只有一个变化的原因，修改邮件发送逻辑不会影响用户注册。}}

{{创建型模式概述}}{{创建型模式关注对象的创建机制，将对象的创建与使用分离。包含五种模式：工厂方法（Factory Method）、抽象工厂（Abstract Factory）、单例（Singleton）、建造者（Builder）、原型（Prototype）。核心思想是：让创建什么对象、如何创建、何时创建变得灵活可控。}}

{{工厂方法模式}}{{工厂方法模式（Factory Method）定义一个创建对象的接口，让子类决定实例化哪个类。将对象的创建延迟到子类。例如：`ShapeFactory` 有 `createShape()` 方法，`CircleFactory` 返回 `Circle`，`RectangleFactory` 返回 `Rectangle`。优点是符合开闭原则，新增产品只需新增工厂。}}

{{抽象工厂模式}}{{抽象工厂模式（Abstract Factory）提供一个接口，用于创建一系列相关或相互依赖的对象，而无需指定具体类。例如：`GUIFactory` 创建 `Button` 和 `TextBox`，`WindowsFactory` 创建 Windows 风格组件，`MacFactory` 创建 Mac 风格组件。适用于需要保证产品族一致性的场景。}}

{{单例模式}}{{单例模式（Singleton）确保一个类只有一个实例，并提供全局访问点。实现方式：（1）饿汉式——类加载时创建，线程安全；（2）懒汉式——首次使用时创建，需加锁保证线程安全；（3）双重检查锁——减少锁开销；（4）枚举单例——最简洁，天然防序列化和反射攻击。应用场景：配置管理器、数据库连接池、日志对象。}}

{{建造者模式}}{{建造者模式（Builder）将复杂对象的构建过程与表示分离，使同样的构建过程可以创建不同的表示。适用于对象有多个可选参数的场景。例如：`QueryBuilder` 链式调用 `select().from().where().orderBy()` 逐步构建 SQL。2025 年在 API 设计和配置对象构建中非常流行。}}

{{原型模式}}{{原型模式（Prototype）通过复制已有实例来创建新对象，而非通过 new 关键字。分为浅拷贝（只复制基本类型，引用类型共享）和深拷贝（完全独立的副本）。适用于创建成本高、需要大量相似对象的场景。Java 中实现 `Cloneable` 接口，JavaScript 中常用 `Object.create()` 或展开运算符。}}

{{结构型模式概述}}{{结构型模式关注类和对象的组合方式，描述如何将类或对象组合成更大的结构。包含七种模式：适配器、桥接、组合、装饰器、外观、享元、代理。核心思想是：通过组合而非继承来实现灵活的结构扩展。}}

{{适配器模式}}{{适配器模式（Adapter）将一个类的接口转换成客户端期望的另一个接口，使原本不兼容的类可以协同工作。分为类适配器（通过继承）和对象适配器（通过组合）。例如：将旧的 `LegacyPrinter` 适配为新的 `ModernPrinter` 接口。在集成第三方库或遗留系统时非常常用。}}

{{装饰器模式}}{{装饰器模式（Decorator）动态地为对象添加额外职责，比继承更灵活。装饰器实现与被装饰对象相同的接口，并持有一个指向被装饰对象的引用。经典应用：Java I/O 流 `BufferedInputStream(FileInputStream(...))`。可以层层嵌套，按需组合功能。}}

{{代理模式}}{{代理模式（Proxy）为另一个对象提供代理以控制对它的访问。类型：（1）虚拟代理——延迟加载大对象；（2）保护代理——控制访问权限；（3）远程代理——为远程对象提供本地代表；（4）缓存代理——缓存请求结果。2025 年在微服务、API 网关、AOP 编程中广泛使用。}}

{{外观模式}}{{外观模式（Facade）为复杂子系统提供一个统一的高层接口，使子系统更容易使用。不改变子系统，只是简化调用。例如：`ComputerFacade` 封装了 CPU、内存、硬盘的启动步骤，客户端只需调用 `computer.start()`。适用于简化复杂库或框架的使用。}}

{{组合模式}}{{组合模式（Composite）将对象组合成树形结构以表示"部分-整体"的层次关系，使客户端对单个对象和组合对象的使用具有一致性。经典应用：文件系统（文件和文件夹）、UI 组件树（按钮和面板）、组织架构（员工和部门）。}}

{{桥接模式}}{{桥接模式（Bridge）将抽象部分与实现部分分离，使它们可以独立变化。通过组合代替多层继承。例如：`Shape`（抽象）持有一个 `Color`（实现）引用，`Circle` + `Red` 和 `Circle` + `Blue` 可自由组合，避免类爆炸。适用于有两个以上独立变化维度的场景。}}

{{享元模式}}{{享元模式（Flyweight）通过共享技术有效地支持大量细粒度的对象。将对象的状态分为内部状态（可共享，不变）和外部状态（不可共享，变化）。经典应用：String 常量池、数据库连接池、文本编辑器中的字符对象共享。适用于大量相似对象占用过多内存的场景。}}

{{行为型模式概述}}{{行为型模式关注对象之间的职责分配和通信方式。包含十一种模式：责任链、命令、迭代器、中介者、备忘录、观察者、状态、策略、模板方法、访问者、解释器。核心思想是：让对象之间的协作更加灵活、松耦合。}}

{{观察者模式}}{{观察者模式（Observer）定义对象间的一对多依赖关系，当一个对象状态改变时，所有依赖它的对象都会收到通知并自动更新。也称为发布-订阅模式。经典应用：事件系统、消息队列、Vue/React 的响应式系统。2025 年在事件驱动架构、WebSocket 实时通信中依然是核心模式。}}

{{策略模式}}{{策略模式（Strategy）定义一系列算法，将每个算法封装起来，并使它们可以互相替换。让算法独立于使用它的客户端变化。例如：`SortStrategy` 接口有 `QuickSort`、`MergeSort`、`BubbleSort` 等实现，运行时可切换。在支付方式选择、折扣计算、验证规则等场景广泛应用。}}

{{模板方法模式}}{{模板方法模式（Template Method）在父类中定义算法骨架，将某些步骤延迟到子类实现。子类可以重写特定步骤，但不能改变算法整体结构。例如：`DataMiner` 定义 `mine()` 流程：openFile → extractData → parseData → analyzeData，`PDFMiner` 和 `CSVMiner` 分别实现具体的提取和解析步骤。}}

{{状态模式}}{{状态模式（State）允许对象在其内部状态改变时改变其行为，看起来像是改变了其类。将每个状态封装为独立的类。例如：订单状态（待支付→已支付→已发货→已完成），每种状态下 `pay()`、`ship()`、`complete()` 方法行为不同。适用于状态转换逻辑复杂的场景。}}

{{命令模式}}{{命令模式（Command）将请求封装为对象，使你可以用不同的请求参数化客户端，支持请求的排队、日志记录和撤销操作。包含 Command、Receiver、Invoker 三个角色。经典应用：编辑器的撤销/重做、宏命令、任务队列。在 GUI 按钮回调和后端任务处理中非常常见。}}

{{责任链模式}}{{责任链模式（Chain of Responsibility）将请求沿着处理者链传递，每个处理者决定自己处理还是传给下一个。发送者和接收者解耦。经典应用：Web 中间件（Express/Koa）、日志级别过滤、审批流程（组长→经理→总监）、异常处理链。2025 年在中间件架构中极为常见。}}

{{迭代器模式}}{{迭代器模式（Iterator）提供一种方法顺序访问聚合对象中的各个元素，而不暴露其内部表示。所有语言的 `for-each`、`Iterator` 接口都基于此模式。在 2025 年，JavaScript 的 Generator（`function*`）和异步迭代器（`for await...of`）是该模式的现代演进。}}

{{中介者模式}}{{中介者模式（Mediator）用一个中介对象封装一系列对象之间的交互，使对象不需要显式地互相引用，降低耦合度。经典应用：聊天室（ChatRoom 中介各 User）、航空管制系统（塔台中介各飞机）、UI 组件间的协调。2025 年在微服务编排和事件总线中有广泛应用。}}

{{备忘录模式}}{{备忘录模式（Memento）在不破坏封装性的前提下，捕获一个对象的内部状态，并在之后恢复它。包含三个角色：Originator（原发器）、Memento（备忘录）、Caretaker（管理者）。经典应用：游戏存档、编辑器撤销、事务回滚。关键是要控制备忘录的访问权限。}}

{{访问者模式}}{{访问者模式（Visitor）将算法与对象结构分离，在不修改元素类的前提下定义新操作。通过双分派实现。适用于对象结构稳定但需要频繁添加新操作的场景。经典应用：编译器 AST 遍历（不同类型节点有不同的代码生成逻辑）、文档导出为不同格式。}}

{{设计模式与 SOLID 的关系}}{{SOLID 原则是设计模式的指导思想。工厂模式体现了依赖倒置和开闭原则；策略模式体现了开闭原则和单一职责；装饰器模式体现了开闭原则（对扩展开放）；代理模式体现了单一职责。掌握 SOLID 原则是理解和正确使用设计模式的前提。}}

{{2025 年设计模式新趋势}}{{2025 年设计模式的新趋势：（1）函数式设计模式崛起——不可变数据、纯函数、高阶函数替代部分 OOP 模式；（2）响应式模式——RxJS、Reactive Streams 中的观察者模式演进；（3）Serverless 架构下的事件驱动模式；（4）AI 辅助代码生成中设计模式的自动化应用；（5）微服务中的 Saga、CQRS、Event Sourcing 等分布式设计模式。}}

{{设计模式学习建议}}{{学习设计模式的建议：（1）先掌握 SOLID 原则，再学具体模式；（2）从最常用的模式开始——策略、工厂、观察者、单例、装饰器；（3）结合实际项目理解，不要死记 UML 图；（4）注意模式之间的区别和联系，如策略 vs 状态、代理 vs 装饰器；（5）不要过度设计，简单问题不需要模式；（6）阅读优秀开源项目的源码，看模式如何落地。}}

{{设计模式的反模式警示}}{{设计模式的常见误用：（1）过度设计——简单场景硬套模式，增加不必要的复杂性；（2）为模式而模式——不理解问题本质就套用模式；（3）单例滥用——全局状态导致紧耦合和测试困难；（4）忽视语言特性——现代语言已有内置解决方案（如 Python 装饰器语法糖）。记住：设计模式是手段，解决问题才是目的。}}
