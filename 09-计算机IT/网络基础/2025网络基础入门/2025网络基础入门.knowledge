# 2025网络基础入门
# 领域: 计算机IT
# 学科: 网络基础
# 来源: 2025网络基础入门
# 描述: 网络基础核心知识点

{{OSI七层模型概述}}{{OSI模型将网络通信分为七层：物理层(第1层)负责比特传输；数据链路层(第2层)负责帧传输和MAC寻址；网络层(第3层)负责IP寻址和路由；传输层(第4层)负责端到端连接和数据可靠性；会话层(第5层)管理会话建立与维护；表示层(第6层)处理数据格式转换与加密；应用层(第7层)为用户应用提供网络服务接口。自上而下封装，自下而上解封装。}}

{{物理层功能与设备}}{{物理层是OSI第1层，负责在物理媒介上传输原始比特流。定义电气、机械、时序接口规范。主要设备包括集线器(Hub)、中继器(Repeater)、网线(Cat5e/Cat6/Cat6a)、光纤等。传输介质分为有线(双绞线、同轴电缆、光纤)和无线(无线电波、微波、红外)。}}

{{数据链路层功能}}{{数据链路层负责将网络层的数据报封装成帧(Frame)，通过MAC地址实现同一网络内的节点间通信。主要协议包括以太网(Ethernet)、PPP、HDLC。设备为交换机(Switch)。该层分为两个子层：LLC(逻辑链路控制)和MAC(介质访问控制)。帧格式包含目的MAC、源MAC、类型/长度、数据、FCS校验。}}

{{网络层功能与IP协议}}{{网络层负责在不同网络之间转发数据包，核心协议是IP协议。主要功能包括逻辑寻址(IP地址)、路由选择、分片与重组。IPv4地址32位，IPv6地址128位。主要设备是路由器(Router)。该层还包含ICMP(ping/traceroute)、ARP(IP转MAC)、IGMP(组播管理)等协议。}}

{{传输层TCP与UDP}}{{传输层提供端到端的数据传输服务。TCP是面向连接的可靠传输协议，通过三次握手建立连接、四次挥手断开，提供流量控制、拥塞控制、数据重传。UDP是无连接的不可靠传输协议，开销小、延迟低，适用于视频流、DNS查询、游戏等实时场景。TCP端口号范围0-65535，其中0-1023为知名端口。}}

{{TCP三次握手过程}}{{TCP建立连接需要三次握手：1)客户端发送SYN包(seq=x)；2)服务端回复SYN+ACK包(seq=y, ack=x+1)；3)客户端发送ACK包(ack=y+1)。三次握手确保双方都具备收发能力，防止已失效的连接请求到达服务端造成资源浪费。四次挥手则是因为TCP全双工，双方需分别关闭各自的发送通道。}}

{{IP地址分类}}{{IPv4地址分为五类：A类(1.0.0.0-126.255.255.255，默认/8)用于大型网络；B类(128.0.0.0-191.255.255.255，默认/16)用于中型网络；C类(192.0.0.0-223.255.255.255，默认/24)用于小型网络；D类(224.0.0.0-239.255.255.255)用于组播；E类保留。特殊地址：127.0.0.1为本地回环，0.0.0.0表示任意地址，255.255.255.255为广播地址。}}

{{私有IP地址范围}}{{RFC 1918定义了三段私有IP地址：A类10.0.0.0/8(10.0.0.0-10.255.255.255)；B类172.16.0.0/12(172.16.0.0-172.31.255.255)；C类192.168.0.0/16(192.168.0.0-192.168.255.255)。私有地址不可直接在互联网上路由，需通过NAT转换。局域网、家庭网络广泛使用这些地址段。}}

{{子网掩码与CIDR}}{{子网掩码用于区分IP地址中的网络部分和主机部分。CIDR(无类域间路由)使用斜线记法，如/24表示前24位为网络位。子网划分可以将一个大网络分成多个小网络，提高IP利用率。计算可用主机数公式：2^n - 2(n为主机位数，减去网络地址和广播地址)。VLSM允许不同子网使用不同掩码长度。}}

{{子网划分实例}}{{将192.168.1.0/24划分为4个子网：192.168.1.0/26(可用192.168.1.1-62)、192.168.1.64/26(可用65-126)、192.168.1.128/26(可用129-190)、192.168.1.192/26(可用193-254)。每个子网62个可用主机地址。子网掩码均为255.255.255.192(/26)。划分步骤：确定借位数→计算子网数→确定每个子网范围。}}

{{路由基础概念}}{{路由是网络层的核心功能，决定数据包从源到目的的最佳路径。路由表存储路由信息，包含目的网络、下一跳、出接口、度量值(Metric)。路由类型：直连路由(自动)、静态路由(手动配置)、动态路由(协议学习)。默认路由(0.0.0.0/0)是"最后手段"路由，当无更精确匹配时使用。}}

{{动态路由协议}}{{动态路由协议分为距离矢量型和链路状态型。RIP(距离矢量)使用跳数为度量，最大15跳，收敛慢。OSPF(链路状态)使用带宽为度量，支持大规模网络，收敛快。EIGRP(混合型)是Cisco私有协议。BGP用于自治系统(AS)之间的路由，是互联网的核心路由协议。OSPF使用Dijkstra算法计算最短路径树。}}

{{交换机工作原理}}{{交换机工作在数据链路层，通过MAC地址表进行帧转发。工作流程：1)收到帧后学习源MAC与端口的映射；2)查找目的MAC；3)若找到则单播转发到对应端口，未找到则泛洪到所有端口(除入端口)。交换机端口模式：Access(接终端)、Trunk(交换机间互联，承载多VLAN)、Hybrid。MAC地址表有老化时间(默认300秒)。}}

{{VLAN虚拟局域网}}{{VLAN将一个物理交换机逻辑上划分为多个广播域，隔离广播流量。802.1Q标准在以太网帧中插入4字节VLAN标签(Tag)，包含12位VLAN ID(1-4094)。Access端口属于单个VLAN，Trunk端口可承载多个VLAN。VLAN间通信需要三层设备(路由器或三层交换机)。常用VLAN 1为默认VLAN，不建议用于用户数据。}}

{{DNS域名系统}}{{DNS将域名解析为IP地址，是互联网的"电话簿"。查询类型：递归查询(客户端→本地DNS)、迭代查询(本地DNS→各级DNS服务器)。记录类型：A记录(域名→IPv4)、AAAA(域名→IPv6)、CNAME(别名)、MX(邮件交换)、NS(域名服务器)、PTR(反向解析)、TXT(文本记录)。DNS使用UDP 53端口(查询)和TCP 53(区域传送)。}}

{{DNS解析流程}}{{浏览器输入域名后的解析流程：1)浏览器DNS缓存；2)操作系统DNS缓存和hosts文件；3)本地DNS服务器(递归解析器)；4)根DNS服务器→顶级域DNS(如.com)→权威DNS。解析结果会在各级缓存中保存，直到TTL过期。dig和nslookup是常用的DNS诊断工具。1.1.1.1和8.8.8.8是知名公共DNS服务器。}}

{{DHCP动态主机配置协议}}{{DHCP自动为网络设备分配IP地址等网络参数。工作过程(DORA)：1)Discover(客户端广播发现)；2)Offer(服务器提供IP)；3)Request(客户端请求确认)；4)Acknowledge(服务器确认分配)。分配的信息包括IP地址、子网掩码、默认网关、DNS服务器。租约到期前客户端需续约。DHCP使用UDP 67(服务器)和68(客户端)端口。}}

{{NAT网络地址转换}}{{NAT将私有IP地址转换为公有IP地址，解决IPv4地址不足问题。类型：静态NAT(一对一映射)、动态NAT(从地址池分配)、PAT/NAPT(端口地址转换，多对一，最常用)。NAT隐藏了内网拓扑，提供一定安全性。缺点是破坏端到端通信，影响P2P应用。企业网络和家庭路由器广泛使用PAT。}}

{{ARP地址解析协议}}{{ARP将IP地址解析为MAC地址，工作在数据链路层和网络层之间。工作过程：主机广播ARP请求(包含目标IP)→目标主机单播回复ARP响应(包含自己的MAC)。结果缓存在ARP表中(超时约20分钟)。免费ARP用于检测IP冲突。ARP欺骗是常见的网络攻击方式，可通过ARP绑定或DAI防护。}}

{{HTTP与HTTPS协议}}{{HTTP是应用层协议，用于Web浏览器与服务器之间的通信，默认端口80。HTTP/1.1引入持久连接和管线化；HTTP/2引入多路复用、头部压缩、服务器推送；HTTP/3基于QUIC(UDP)协议，进一步降低延迟。HTTPS = HTTP + TLS加密，默认端口443。TLS握手过程包括证书验证、密钥协商、对称加密通信。}}

{{常见应用层协议与端口}}{{常用协议端口：HTTP(80)、HTTPS(443)、FTP(20数据/21控制)、SSH(22)、Telnet(23)、SMTP(25)、DNS(53)、DHCP(67/68)、POP3(110)、IMAP(143)、SNMP(161/162)、LDAP(389)、HTTPS(443)、RDP(3389)、MySQL(3306)。记住常用端口号是网络排查的基本功。}}

{{ICMP协议与ping/traceroute}}{{ICMP(Internet控制消息协议)用于网络诊断和错误报告。ping命令发送ICMP Echo Request，接收Echo Reply，测试连通性和延迟。traceroute/tracert通过发送TTL递增的包来追踪数据包路径，每经过一个路由器TTL减1，归零时返回ICMP超时消息。ICMP还有目标不可达、重定向等消息类型。}}

{{网络安全基础防火墙}}{{防火墙是网络安全的第一道防线，分为包过滤防火墙(基于IP/端口规则)、状态检测防火墙(跟踪连接状态)、应用层防火墙(深度包检测DPI)、下一代防火墙(NGFW，集成IPS、应用识别等)。部署位置通常在网络边界。ACL(访问控制列表)是防火墙规则的基础，分为标准ACL和扩展ACL。}}

{{VPN虚拟专用网络}}{{VPN在公共网络上建立加密隧道，实现安全的远程访问。主要类型：IPSec VPN(网络层加密，适合站点互联)、SSL/TLS VPN(应用层加密，适合远程访问)、WireGuard(新型轻量VPN协议)。隧道模式封装整个原始包，传输模式仅加密有效载荷。企业广泛使用VPN实现远程办公和分支机构互联。}}

{{无线网络WiFi基础}}{{WiFi基于IEEE 802.11标准。主要版本：802.11n(WiFi4,最高600Mbps,2.4/5GHz)、802.11ac(WiFi5,最高6.9Gbps,5GHz)、802.11ax(WiFi6,最高9.6Gbps,2.4/5GHz,OFDMA)、802.11be(WiFi7,最高46Gbps,6GHz,MLO)。安全协议：WEP(已淘汰)→WPA→WPA2(AES-CCMP)→WPA3(SAE)。2.4GHz穿墙强但干扰多，5GHz速度快但覆盖小。}}

{{网络拓扑结构}}{{常见网络拓扑：总线型(所有设备共享一条总线，已淘汰)、星型(所有设备连到中心节点，最常用)、环型(设备形成环形连接)、树型(层次结构)、网状型(设备间多条路径，冗余高)、混合型。现代企业网络通常采用三层架构：核心层(高速转发)、汇聚层(策略控制)、接入层(终端接入)。}}

{{STP生成树协议}}{{STP(IEEE 802.1D)防止交换机环路导致的广播风暴。工作原理：选举根桥→确定根端口→确定指定端口→阻塞冗余端口。RSTP(802.1w)收敛更快(秒级)。MSTP(802.1s)支持多实例。端口状态：阻塞→监听→学习→转发。生成树是交换网络的基础协议，理解它对排查网络环路问题至关重要。}}

{{CIDR与路由聚合}}{{CIDR(无类域间路由)允许任意长度的子网掩码，打破了传统ABC类的限制。路由聚合(超网)将多个连续子网合并为一条路由条目，减小路由表规模。例如192.168.0.0/24和192.168.1.0/24可聚合为192.168.0.0/23。聚合要求地址连续且对齐。CIDR极大缓解了IPv4地址耗尽问题。}}

{{IPv6基础}}{{IPv6使用128位地址，地址空间约3.4×10^38个。地址格式为8组4位十六进制数，如2001:0db8::1。IPv6取消了NAT、广播(改用组播)、头部校验和。新特性：SLAAC(无状态自动配置)、更大的报文头、流标签、IPSec内置。表示方法可省略前导零、用::压缩连续零组。IPv6是未来网络发展的方向。}}

{{网络排障常用命令}}{{网络排障必备命令：ping(测试连通性)、traceroute/tracert(追踪路由路径)、ipconfig/ifconfig(查看网络配置)、nslookup/dig(DNS解析测试)、netstat/ss(查看网络连接和端口)、arp -a(查看ARP表)、tcpdump/Wireshark(抓包分析)、telnet/nc(测试端口连通性)。排障遵循自下而上：物理→数据链路→网络→传输→应用。}}

{{TCP/IP四层模型}}{{TCP/IP模型是实际使用的网络模型，分为四层：网络接口层(对应OSI物理层+数据链路层)、网际层(对应OSI网络层)、传输层(对应OSI传输层)、应用层(对应OSI会话层+表示层+应用层)。每层都有代表性协议：网际层(IP/ICMP/ARP)、传输层(TCP/UDP)、应用层(HTTP/DNS/FTP/SMTP)。}}

{{数据封装与解封装过程}}{{发送数据时自上而下封装：应用数据→TCP/UDP段(Segment，加端口号)→IP包(Packet，加IP地址)→以太网帧(Frame，加MAC地址)→比特流(Bit)。接收数据时自下而上解封装，逐层剥离头部。每一层只处理自己层的头部信息，对上层数据透明传输。PDU(协议数据单元)在各层有不同名称。}}

{{网关与默认网关}}{{网关是连接不同网络的"关口"，通常是路由器接口的IP地址。默认网关是主机访问非本网段目的地时的默认出口。主机发送数据时，先判断目标IP是否在同一子网：若是则直接发送，否则发给默认网关。网关必须与主机在同一子网内。企业网络中网关通常由三层交换机或路由器充当。}}

{{端口与套接字}}{{端口号标识主机上的不同应用进程。知名端口(0-1023)：HTTP=80, HTTPS=443, SSH=22等。注册端口(1024-49151)：MySQL=3306等。动态端口(49152-65535)：客户端临时使用。Socket(套接字)= IP地址 + 端口号，唯一标识网络中的一个通信端点。TCP连接由四元组标识：源IP、源端口、目的IP、目的端口。}}

{{网络性能指标}}{{衡量网络性能的关键指标：带宽(最大传输速率，bps)、吞吐量(实际传输速率)、延迟(数据从源到目的的时间)、抖动(延迟的变化量)、丢包率(丢失包占总包的比例)、往返时间RTT(Round-Trip Time)。带宽和延迟是最核心的两个指标。优化方法包括QoS(服务质量)、负载均衡、CDN(内容分发网络)。}}

{{QoS服务质量}}{{QoS对不同类型的网络流量提供差异化服务保障。主要机制：分类与标记(DSCP/CoS)、队列调度(PQ优先队列、WFQ加权公平队列、CBWFQ基于类的加权公平队列)、流量整形与监管(令牌桶算法)、拥塞避免(WRED加权随机早期检测)。VoIP、视频会议等实时业务通常需要高优先级的QoS保障。}}

{{网络设备对比}}{{集线器(Hub)：物理层，共享带宽，半双工，已淘汰。交换机(Switch)：数据链路层，MAC寻址，独享带宽，全双工。路由器(Router)：网络层，IP寻址，连接不同网络。三层交换机：兼具交换和路由功能。防火墙：安全策略执行。无线AP：提供WiFi接入。各设备各司其职，共同构建完整网络。}}
