# 2025Rust入门教程
# 领域: 计算机IT
# 学科: Rust
# 来源: 2025Rust入门教程
# 描述: Rust编程语言核心知识点

{{Rust语言概述}}{{Rust 是由 Mozilla 开发的系统级编程语言，以"安全、并发、高性能"为核心设计目标。它的所有权系统在编译期消除空指针、数据竞争等问题，无需垃圾回收。Rust 广泛用于操作系统、WebAssembly、嵌入式开发、网络服务等领域。2025年 Rust 生态已非常成熟，被 Linux 内核、Android 等重大项目采纳。}}

{{Cargo包管理工具}}{{Cargo 是 Rust 的构建系统和包管理器。常用命令：`cargo new` 创建项目、`cargo build` 编译、`cargo run` 编译并运行、`cargo test` 运行测试、`cargo doc` 生成文档。`Cargo.toml` 是项目配置文件，声明依赖、版本、特性等。crates.io 是 Rust 的官方包仓库。}}

{{变量与可变性}}{{Rust 中变量默认不可变（immutable）。使用 `let` 声明变量，`let mut` 声明可变变量。不可变性是 Rust 安全性的基石之一。可以使用遮蔽（shadowing）重新绑定同名变量，遮蔽允许改变类型，而 `mut` 不允许。}}

{{基本数据类型}}{{Rust 的基本类型包括：标量类型（整数 i8-i128/u8-u128、浮点 f32/f64、布尔 bool、字符 char）和复合类型（元组 tuple、数组 array）。整数溢出在 debug 模式会 panic，release 模式会回绕。`usize` 和 `isize` 取决于平台（32位或64位）。}}

{{函数与闭包}}{{函数用 `fn` 定义，参数必须标注类型，返回值类型用 `->` 指定。Rust 中表达式（expression）和语句（statement）有明确区分：表达式返回值，语句不返回。闭包是一种匿名函数，使用 `|参数|` 语法，可以捕获环境中的变量，分为 `Fn`、`FnMut`、`FnOnce` 三种 trait。}}

{{所有权系统概述}}{{所有权是 Rust 最核心的特性。三条规则：1) 每个值有且只有一个所有者；2) 同一时间只能有一个所有者；3) 所有者离开作用域时值被丢弃（drop）。当赋值或传参时，默认发生所有权转移（move），而非复制。这在编译期保证了内存安全，无需垃圾回收器。}}

{{引用与借用}}{{引用（reference）允许你使用值但不获取所有权。`&T` 是不可变引用（可以同时存在多个），`&mut T` 是可变引用（同一时间只能有一个）。借用规则：不能同时存在可变引用和不可变引用。引用必须始终有效（不能悬垂引用）。这些规则在编译期由借用检查器（borrow checker）强制执行。}}

{{生命周期基础}}{{生命周期（lifetime）是 Rust 保证引用有效性的机制。用 `'a` 语法标注，表示引用的有效范围。函数签名中多个引用参数时，编译器需要知道返回值的生命周期与哪个参数关联。生命周期省略规则（lifetime elision）在大多数简单场景下自动推断，无需手动标注。}}

{{生命周期进阶}}{'static 生命周期表示引用在整个程序运行期间有效，如字符串字面量。结构体包含引用时必须标注生命周期。生命周期子类型化（subtyping）允许较短的生命周期被用在需要较长生命周期的地方。HRTB（Higher-Ranked Trait Bounds）如 `for<'a> Fn(&'a T) -> &'a U` 用于更复杂的场景。}}

{{结构体（struct）}}{{Rust 中结构体用 `struct` 定义，有三种形式：具名字段结构体、元组结构体（tuple struct）、单元结构体（unit struct）。使用 `impl` 块为结构体定义方法，`&self` 表示不可变借用，`&mut self` 表示可变借用，`self` 表示获取所有权。结构体支持字段简写初始化和更新语法 `..other`。}}

{{枚举与Option}}{{Rust 的枚举（enum）非常强大，每个变体可以携带不同类型的数据。`Option<T>` 是标准库枚举，用于表示值可能存在（`Some(T)`）或不存在（`None`），替代了 null 的概念。模式匹配可以优雅地处理枚举的所有变体，编译器确保不会遗漏任何情况。}}

{{模式匹配（match）}}{{`match` 是 Rust 的核心控制流结构，必须穷举所有可能。模式可以匹配字面量、变量、通配符、结构体解构、枚举变体、引用等。`if let` 和 `while let` 提供了单分支匹配的简写。`matches!` 宏用于布尔场景。模式匹配在编译期保证穷尽性，极大减少逻辑错误。}}

{{Result与错误处理}}{{Rust 使用 `Result<T, E>` 类型处理可能失败的操作，而非异常。`Ok(T)` 表示成功，`Err(E)` 表示失败。`?` 操作符可以提前返回错误，简化错误传播。`panic!` 宏用于不可恢复的错误。自定义错误类型通常实现 `std::error::Error` trait。anyhow 和 thiserror 是常用的错误处理库。}}

{{trait系统}}{{trait 定义共享行为，类似其他语言的接口。使用 `trait` 定义，`impl Trait for Type` 实现。trait 可以有默认实现。常用标准库 trait：`Display`、`Debug`、`Clone`、`Copy`、`PartialEq`、`Eq`、`PartialOrd`、`Ord`、`Hash`、`Default`、`Iterator`。`derive` 宏可以自动派生常用 trait。}}

{{泛型（generics）}}{{泛型允许编写适用于多种类型的代码。函数、结构体、枚举、方法都可以使用泛型。Rust 的泛型是零成本抽象——编译器会为每种具体类型生成专门代码（单态化，monomorphization）。泛型约束通过 trait bound 语法 `T: Trait` 或 `where` 子句实现。}}

{{trait对象与动态分发}}{{`dyn Trait` 创建 trait 对象，实现运行时多态（动态分发）。使用 `Box<dyn Trait>` 或 `&dyn Trait` 存储。与泛型的静态分发不同，trait 对象通过 vtable 在运行时查找方法，有少量性能开销。不能与返回 `Self` 或使用关联常量的方法一起使用。}}

{{集合类型}}{{标准库提供三种核心集合：`Vec<T>` 动态数组、`HashMap<K, V>` 哈希映射、`String` UTF-8 字符串。`Vec` 支持 `push`、`pop`、索引访问、迭代。`HashMap` 使用 `insert`、`get`、`entry` API。集合使用所有权语义，存入集合会转移所有权（除非类型实现了 `Copy`）。}}

{{字符串类型}}{{Rust 有 `String`（堆分配、可增长）和 `&str`（字符串切片、不可变引用）两种主要字符串类型。字符串字面量是 `&str` 类型。`String` 和 `&str` 之间可以互相转换（`to_string()`、`as_str()`、`&s`）。Rust 字符串是 UTF-8 编码，不能通过索引直接访问字符，需使用 `.chars()` 或 `.bytes()` 迭代。}}

{{迭代器（Iterator）}}{{迭代器 trait `Iterator` 定义了 `next` 方法，返回 `Option<Item>`。`for` 循环语法糖自动调用迭代器。适配器方法（`map`、`filter`、`zip`、`enumerate`、`take`、`skip` 等）链式组合，消费器方法（`collect`、`sum`、`count`、`any`、`all`、`fold`）产生最终结果。迭代器是惰性求值的，常比手动循环更高效。}}

{{智能指针}}{{Rust 提供多种智能指针：`Box<T>` 在堆上分配数据；`Rc<T>` 引用计数智能指针，允许多个所有者（单线程）；`RefCell<T>` 提供内部可变性，运行时检查借用规则。`Arc<T>` 是 `Rc` 的线程安全版本。`Deref` trait 允许智能指针像引用一样使用，`Drop` trait 控制析构行为。}}

{{并发编程概述}}{{Rust 通过所有权系统在编译期防止数据竞争。`std::thread::spawn` 创建线程，`move` 闭包转移所有权到新线程。`Mutex<T>` 保护共享数据，`Arc<T>` 在线程间共享所有权。`RwLock<T>` 允许多个读取者或一个写入者。`Send` 和 `Sync` trait 标记类型的线程安全性。}}

{{消息传递（channel）}}{{Rust 使用 `mpsc`（multiple producer, single consumer）通道实现线程间消息传递。`mpsc::channel()` 创建通道，返回发送端 `tx` 和接收端 `rx`。`tx.send()` 发送消息，`rx.recv()` 接收消息。发送端可以克隆实现多生产者。通道是所有权转移的——发送后原值不可再使用。}}

{{async/await异步编程}}{{Rust 的 `async/await` 提供零成本异步抽象。`async fn` 返回 `Future`，`.await` 驱动执行。需要运行时（如 tokio、async-std）来执行异步任务。`tokio::spawn` 创建异步任务。`select!` 宏并发等待多个 future。`async` 块和 `async move` 块用于创建匿名 future。Pin 类型用于自引用结构的安全处理。}}

{{模块系统与可见性}}{{Rust 的模块系统使用 `mod` 声明模块，`pub` 控制可见性。默认所有项（函数、结构体等）是私有的。`use` 关键字引入路径到当前作用域。`pub(crate)` 限制为当前 crate 可见。模块可以写在同一文件或独立文件中（`mod.rs` 或同名目录）。`Cargo.toml` 中的 `[dependencies]` 引入外部 crate。}}

{{Trait的高级用法}}{{关联类型（associated type）在 trait 中定义类型占位符，如 `Iterator` 的 `Item`。默认泛型参数和运算符重载通过 `std::ops` trait 实现。supertraits 要求实现某 trait 前必须先实现另一个（如 `trait PrettyPrint: Display`）。扩展 trait（extension trait）为已有类型添加方法。}}

{{宏（macro）}}{{Rust 宏分为声明式宏（`macro_rules!`）和过程宏（procedural macros）。声明式宏使用模式匹配生成代码。过程宏包括：derive 宏（自动实现 trait）、属性宏（自定义属性）、函数式宏（函数调用语法）。常用内置宏：`println!`、`vec!`、`format!`、`todo!`、`unimplemented!`、`dbg!`。}}

{{闭包与函数指针}}{{函数指针类型 `fn(T) -> U` 是零大小的，可以强制转换为闭包。闭包实现了 `Fn`、`FnMut`、`FnOnce` 中的一个或多个 trait，取决于如何捕获变量。`move` 关键字强制闭包获取捕获变量的所有权。闭包作为参数时推荐用泛型约束（`impl Fn`）而非 trait 对象，以获得静态分发的性能。}}

{{测试（Testing）}}{{Rust 内置测试框架。`#[test]` 标注测试函数，`#[cfg(test)]` 模块条件编译。`cargo test` 运行测试。断言宏：`assert!`、`assert_eq!`、`assert_ne!`。`should_panic` 属性测试预期的 panic。`Result<T, E>` 风格的测试函数支持 `?` 操作符。`cargo test -- --test-threads=1` 控制并发。集成测试放在 `tests/` 目录。}}

{{Cargo工作空间}}{{工作空间（workspace）允许在单个仓库管理多个相关 crate。根目录 `Cargo.toml` 定义 `[workspace]` 和 `members` 列表。共享同一 `Cargo.lock` 和输出目录。子 crate 可以互相依赖。适用于大型项目的模块化拆分。`cargo build --workspace` 构建所有成员。}}

{{特征（attribute）与条件编译}}{{Rust 使用 `#[attribute]` 语法标注项。常用属性：`#[derive(...)]` 自动派生、`#[cfg(...)]` 条件编译、`#[allow(...)]` 抑制警告、`#[deprecated]` 标记废弃、`#[inline]` 建议内联、`#[must_use]` 标记返回值不应忽略。条件编译支持 `target_os`、`target_arch`、`feature` 等谓词。}}

{{类型别名与newtype模式}}{{`type` 关键字创建类型别名，如 `type Result<T> = std::result::Result<T, MyError>`，简化复杂类型签名。newtype 模式用元组结构体包裹已有类型（`struct Meters(f64)`），提供类型安全和封装，可以在外部类型上实现外部 trait（孤儿规则的绕过）。}}

{{Drop trait与RAII}}{{Rust 使用 RAII（Resource Acquisition Is Initialization）模式管理资源。实现 `Drop` trait 的 `drop` 方法在值离开作用域时自动调用。`std::mem::drop` 可以提前释放资源。不能手动调用 `drop` 方法（编译器会报错）。`ManuallyDrop` 允许手动控制 drop 时机。这确保了文件句柄、锁、网络连接等资源的安全释放。}}

{{unsafe Rust}}{{`unsafe` 关键字允许执行编译器无法验证安全性的操作：解引用裸指针、调用 unsafe 函数、访问/修改可变静态变量、实现 unsafe trait。`unsafe` 不是关闭所有安全检查，只是开放这五种能力。应尽量将 unsafe 代码封装在安全的抽象接口中，最小化 unsafe 块的范围。}}

{{Trait bound与where子句}}{{泛型约束通过 trait bound 语法 `fn foo<T: Display + Clone>(t: T)` 指定。`where` 子句提供更清晰的写法，尤其在复杂约束时。约束可以包括生命周期 bound（`T: 'a`）。`impl Trait` 语法是 trait bound 的语法糖。`dyn Trait` 用于动态分发场景。多重约束使用 `+` 连接。}}

{{标准库常用trait总结}}{{
- Display：格式化输出（`{}`）；Debug：调试输出（`{:?}`）
- Clone：深拷贝；Copy：隐式复制（栈上数据）
- PartialEq/Eq：相等比较；PartialOrd/Ord：排序比较
- Hash：哈希计算；Default：默认值
- From/Into：类型转换；TryFrom/TryInto：可能失败的转换
- AsRef/AsMut：引用转换；Deref/DerefMut：解引用
- Add/Sub/Mul/Div 等：算术运算符重载
}}

{{错误处理最佳实践}}{{
- 库代码用 `Result<T, E>` 返回错误，不要 panic
- 可恢复错误用 Result，不可恢复错误用 panic!
- 自定义错误类型实现 `std::error::Error` + `Display` + `Debug`
- 使用 `thiserror` 简化自定义错误定义
- 使用 `anyhow::Result` 用于应用程序级错误处理
- `?` 操作符自动调用 `From::from` 转换错误类型
- `.unwrap()` 和 `.expect()` 仅用于原型或确定不会失败的场景
}}

{{Pin与自引用结构}}{{
Pin<P> 保证指针 P 指向的值不会被移动（除非 T: Unpin）。
这是 async/await 的基础，因为异步函数生成的 Future 可能是自引用的。
大多数类型实现了 Unpin（可以安全移动），需要 pin 住的类型使用 `PhantomPinned` 标记。
`pin!` 宏（标准库）和 `pin-project` crate 简化 Pin 的使用。
}}
