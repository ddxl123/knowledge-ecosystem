# 神经网络基础

## 感知机与神经元
- 感知机：最简单的神经网络，y = f(wx + b)
- 激活函数：Sigmoid、Tanh、ReLU、LeakyReLU、GELU
- ReLU优势：缓解梯度消失、计算简单、稀疏激活
- 多层感知机(MLP)：输入层→隐藏层→输出层
- 万能近似定理：足够宽的单隐藏层可逼近任意连续函数

## 前向传播
- 线性变换：z = Wx + b
- 激活：a = σ(z)
- 逐层计算：输入→第一层→第二层→...→输出
- 输出层：分类用Softmax，回归用线性
- Softmax：将输出转为概率分布，各项和为1

## 反向传播
- 链式法则：计算损失对每个参数的梯度
- 梯度下降：w = w - lr * ∂L/∂w
- 批量梯度下降(BGD)、随机梯度下降(SGD)、小批量(Mini-batch)
- 学习率：过大震荡不收敛，过小收敛慢
- 反向传播是从输出层向输入层逐层计算梯度

## 优化器
- SGD+Momentum：加入动量加速收敛
- AdaGrad：自适应学习率，累积梯度平方
- RMSprop：AdaGrad改进，指数衰减平均
- Adam：Momentum+RMSprop，最常用优化器
- AdamW：权重衰减解耦的Adam

## 训练技巧
- 权重初始化：Xavier（Sigmoid）、He（ReLU）
- 批归一化(BatchNorm)：加速训练、稳定梯度
- Dropout：随机丢弃神经元，防止过拟合
- 学习率调度：StepLR、CosineAnnealing、Warmup
- 早停(Early Stopping)：验证集损失不再下降时停止
