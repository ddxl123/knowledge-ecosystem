# 卷积神经网络

## 卷积层
- 卷积操作：滤波器(卷积核)在输入上滑动计算
- 特征提取：低层（边缘、纹理）→高层（物体部件、整体）
- 参数：卷积核大小、步长(stride)、填充(padding)
- 输出尺寸：(W-F+2P)/S + 1
- 权重共享：同一卷积核在整个输入上共享参数

## 池化层
- 最大池化：取区域最大值，保留显著特征
- 平均池化：取区域平均值，保留整体信息
- 作用：降低空间维度、减少参数、平移不变性
- 全局平均池化(GAP)：整个特征图取平均，替代全连接层
- 自适应池化：AdaptiveAvgPool2d，输出固定大小

## 经典网络
- LeNet：最早的CNN，手写数字识别
- AlexNet：ReLU、Dropout、GPU训练，ImageNet突破
- VGG：3x3小卷积核堆叠，网络加深
- GoogLeNet/Inception：多尺度卷积并行，1x1卷积降维
- ResNet：残差连接，解决深层网络退化，可达152层

## 现代CNN架构
- DenseNet：密集连接，特征复用
- EfficientNet：复合缩放（深度、宽度、分辨率）
- MobileNet：深度可分离卷积，移动端轻量模型
- ShuffleNet：通道混洗，移动端高效模型
- ConvNeXt：用现代训练技巧改进传统CNN

## 应用场景
- 图像分类：识别图片中的物体类别
- 目标检测：YOLO、Faster R-CNN，定位+分类
- 语义分割：FCN、U-Net，像素级分类
- 图像生成：GAN、StyleGAN
- 人脸识别：ArcFace、FaceNet
