# 深度学习入门
# 领域: 计算机IT
# 学科: 人工智能与深度学习
# 来源: 深度学习入门（2025版）
# 描述: 深度学习基础理论与实践，涵盖神经网络、卷积网络、循环网络、Transformer及主流框架应用

{{神经网络基础}}{{神经网络由输入层、隐藏层和输出层组成。每个神经元接收输入、加权求和、通过激活函数产生输出。常用激活函数：Sigmoid(0-1)、Tanh(-1~1)、ReLU(max(0,x))、Leaky ReLU、GELU、Swish。感知机是最简单的神经网络，多层感知机(MLP)可解决非线性问题。万能近似定理证明足够宽的单隐层网络可逼近任意连续函数。}}

{{反向传播算法}}{{反向传播(Backpropagation)是训练神经网络的核心算法。前向传播计算输出和损失，反向传播利用链式法则计算梯度。梯度下降优化参数：θ = θ - α·∂L/∂θ。变体：SGD(随机梯度下降)、Momentum(动量)、RMSprop(自适应学习率)、Adam(结合动量和自适应学习率，最常用)。学习率调度策略：StepLR、CosineAnnealing、Warmup。}}

{{损失函数}}{{回归任务：MSE(均方误差)、MAE(平均绝对误差)、Huber Loss(结合MSE和MAE)。分类任务：Cross-Entropy(交叉熵)、Binary Cross-Entropy(二分类)、Focal Loss(解决类别不平衡)。正则化损失：L1正则(Lasso，产生稀疏解)、L2正则(Ridge，权重衰减)。损失函数的选择影响模型训练和收敛特性。}}

{{正则化技术}}{{过拟合是深度学习的核心挑战。Dropout：训练时随机丢弃神经元(通常p=0.5)，测试时缩放输出。Batch Normalization：对每个mini-batch标准化，加速训练、允许更大学习率。Layer Normalization：对单个样本的所有特征标准化，常用于Transformer。权重衰减(Weight Decay)：L2正则化防止权重过大。Early Stopping：监控验证集损失，及时停止训练。数据增强扩充训练数据。}}

{{卷积神经网络(CNN)}}{{CNN专门处理网格结构数据(如图像)。卷积层：卷积核在输入上滑动提取局部特征，参数共享减少参数量。池化层(Max/Average Pooling)：降低空间维度，增强平移不变性。经典架构演进：LeNet→AlexNet→VGGNet(小卷积核堆叠)→GoogLeNet(Inception多尺度)→ResNet(残差连接，解决梯度消失)→DenseNet→EfficientNet(复合缩放)。}}

{{循环神经网络(RNN)}}{{RNN处理序列数据，具有记忆能力。隐藏状态h_t = f(W_h·h_{t-1} + W_x·x_t)。梯度消失/爆炸问题限制了长期依赖学习。LSTM(长短期记忆)：引入遗忘门、输入门、输出门和细胞状态，有效解决长期依赖。GRU(门控循环单元)：简化版LSTM，合并门控机制，参数更少。双向RNN同时利用前向和后向信息。}}

{{Transformer架构}}{{Transformer基于自注意力机制(Self-Attention)，完全抛弃循环和卷积。核心组件：Multi-Head Attention(多头注意力)、Position Encoding(位置编码)、Feed-Forward Network(前馈网络)、Layer Normalization、残差连接。Attention计算：Attention(Q,K,V) = softmax(QK^T/√d_k)V。Encoder-Decoder架构，编码器和解码器各由N层堆叠。并行计算效率远超RNN。}}

{{注意力机制}}{{注意力机制让模型聚焦于输入的关键部分。Self-Attention中Q、K、V均由输入线性变换得到。Multi-Head Attention将注意力分成多个头，捕获不同子空间的信息。Cross-Attention用于解码器关注编码器输出。注意力权重可视化可解释模型决策。复杂度为O(n²)，长序列计算代价大。优化方案：Sparse Attention、Linear Attention、Flash Attention。}}

{{生成对抗网络(GAN)}}{{GAN由生成器G和判别器D组成，二者对抗训练。G生成假数据试图欺骗D，D区分真假数据。训练目标：min_G max_D V(D,G)。训练不稳定是主要挑战：模式崩溃、训练振荡。改进：WGAN(Wasserstein距离)、DCGAN(卷积架构)、StyleGAN(风格控制)、Conditional GAN(条件生成)。应用：图像生成、风格迁移、超分辨率、数据增强。}}

{{变分自编码器(VAE)}}{{VAE结合深度学习和概率图模型。编码器将输入映射到潜在空间的分布(均值和方差)，解码器从潜在变量重建输入。损失函数 = 重建损失 + KL散度(正则化潜在空间)。重参数化技巧使采样可微分。VAE学习平滑的潜在空间，可用于插值生成。β-VAE解耦潜在表示。与GAN相比，VAE生成图像较模糊但训练更稳定。}}

{{目标检测}}{{目标检测定位并分类图像中的物体。两阶段方法：R-CNN系列(R-CNN→Fast R-CNN→Faster R-CNN)，先生成候选区域再分类。单阶段方法：YOLO系列(YOLOv1~YOLOv11)和SSD，直接预测边界框和类别，速度更快。关键概念：IoU(交并比)、NMS(非极大值抑制)、Anchor(锚框)、FPN(特征金字塔网络)。评估指标：mAP(平均精度均值)。}}

{{语义分割}}{{语义分割对图像每个像素进行分类。FCN(全卷积网络)首次实现端到端像素级预测。U-Net：编码器-解码器+跳跃连接，医学图像分割经典架构。DeepLab系列：空洞卷积(Atrous Convolution)增大感受野、ASPP(空间金字塔池化)、CRF后处理。Transformer分割：SegFormer、Mask2Former。评估指标：mIoU(平均交并比)、Dice系数。}}

{{自然语言处理(NLP)基础}}{{NLP核心任务：分词、词性标注、命名实体识别(NER)、文本分类、机器翻译、问答系统、文本生成。文本表示演进：One-hot→TF-IDF→Word2Vec(CBOW/Skip-gram)→GloVe→ELMo(上下文相关)→BERT(预训练微调范式)。Tokenizer分词方法：BPE(Byte Pair Encoding)、WordPiece、SentencePiece。}}

{{预训练语言模型}}{{BERT：双向Transformer编码器，MLM(掩码语言模型)+NSP(下一句预测)预训练，微调适配下游任务。GPT系列：自回归Transformer解码器，GPT-1→GPT-2→GPT-3→GPT-4，规模不断扩大。T5：统一文本到文本框架。RoBERTa、ALBERT、DeBERTa等改进变体。预训练+微调(Fine-tuning)成为NLP标准范式。提示学习(Prompt Learning)减少微调需求。}}

{{大语言模型(LLM)}}{{LLM指参数量巨大的语言模型(通常>10B)。涌现能力(Emergent Abilities)：规模增大后出现的能力(如思维链推理)。In-Context Learning：无需微调，通过示例学习。RLHF(人类反馈强化学习)对齐模型行为。SFT(监督微调)和DPO(直接偏好优化)是其他对齐方法。推理优化：KV Cache、量化(INT8/INT4)、推测解码、Flash Attention。}}

{{深度学习框架}}{{PyTorch：动态计算图，Pythonic风格，研究社区首选。TensorFlow：静态图(TF2支持动态图)，工业部署成熟。核心概念：Tensor(张量)、autograd(自动微分)、nn.Module(模型定义)、DataLoader(数据加载)、Optimizer(优化器)。训练流程：定义模型→定义损失和优化器→训练循环(forward→loss→backward→step)→评估。GPU加速通过CUDA实现。}}

{{迁移学习}}{{迁移学习利用预训练模型的知识解决新任务。特征提取：冻结预训练模型参数，仅训练新分类头。微调(Fine-tuning)：解冻部分或全部层，以小学习率继续训练。领域自适应：减小源域和目标域的分布差异。模型库：Hugging Face Hub、TorchVision、TIMM。迁移学习在数据稀缺场景特别有效。}}

{{模型部署与优化}}{{模型部署到生产环境需要优化。模型压缩：剪枝(去除不重要权重)、知识蒸馏(大模型指导小模型)、量化(降低数值精度FP32→INT8)。推理引擎：ONNX Runtime、TensorRT(NVIDIA)、OpenVINO(Intel)、CoreML(Apple)。服务化部署：TorchServe、Triton Inference Server、FastAPI+uvicorn。边缘部署：TensorFlow Lite、NCNN。}}

{{强化学习基础}}{{强化学习中Agent通过与Environment交互学习最优策略。核心要素：State(状态)、Action(动作)、Reward(奖励)、Policy(策略)、Value Function(价值函数)。马尔可夫决策过程(MDP)形式化描述。Q-Learning：学习动作价值函数。Deep Q-Network(DQN)：用神经网络逼近Q函数。Policy Gradient：直接优化策略。Actor-Critic结合策略和价值方法。PPO是常用的稳定策略优化算法。}}

{{扩散模型}}{{扩散模型(Diffusion Model)是当前最先进的生成模型。前向过程：逐步向数据添加高斯噪声直到纯噪声。反向过程：学习去噪，从噪声恢复数据。DDPM(去噪扩散概率模型)奠定了理论基础。采样加速：DDIM、DPM-Solver。Stable Diffusion：潜在空间扩散+文本条件控制，实现文本生成图像。ControlNet添加空间控制条件。扩散模型在图像、音频、视频生成中超越GAN。}}
