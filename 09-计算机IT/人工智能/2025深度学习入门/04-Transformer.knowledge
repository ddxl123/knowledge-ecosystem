# Transformer

## 自注意力机制
- Query、Key、Value：输入映射到三个向量空间
- 注意力分数：Attention(Q,K,V) = softmax(QK^T/√d_k)V
- 缩放因子√d_k：防止点积过大导致Softmax梯度消失
- 多头注意力：多个注意力头并行，捕捉不同子空间信息
- 自注意力复杂度：O(n²)，n为序列长度

## Transformer架构
- Encoder：多头自注意力 + 前馈网络 + 残差连接 + LayerNorm
- Decoder：掩码自注意力 + 编码器-解码器注意力 + 前馈网络
- 位置编码：正弦/余弦函数或可学习的位置编码
- 残差连接：缓解深层网络训练困难
- LayerNorm：稳定训练，加速收敛

## BERT
- 双向编码器：同时利用左右上下文
- 预训练任务：掩码语言模型(MLM) + 下一句预测(NSP)
- 微调：在预训练基础上针对下游任务微调
- 变体：RoBERTa、ALBERT、DeBERTa
- 应用：文本分类、问答、命名实体识别

## GPT系列
- 自回归语言模型：从左到右生成
- 预训练：预测下一个token
- GPT-3：175B参数，少样本学习能力
- InstructGPT/ChatGPT：RLHF对齐人类偏好
- GPT-4：多模态，更强推理能力

## 现代Transformer
- Vision Transformer(ViT)：图像分块+Transformer
- 多模态模型：CLIP、DALL-E、GPT-4V
- 高效Transformer：Flash Attention、稀疏注意力、线性注意力
- 状态空间模型：Mamba，线性复杂度替代Transformer
- 混合专家(MoE)：条件计算，增大模型容量
