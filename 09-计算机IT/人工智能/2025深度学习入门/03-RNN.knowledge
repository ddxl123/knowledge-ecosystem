# 循环神经网络

## RNN基础
- 序列数据：文本、语音、时间序列
- 循环结构：隐藏状态h_t = f(x_t, h_{t-1})
- 参数共享：不同时刻使用相同的权重矩阵
- 展开：将循环结构展开为前馈网络
- 应用：语言模型、机器翻译、语音识别

## 梯度问题
- 梯度消失：长序列中梯度指数衰减，无法学习长期依赖
- 梯度爆炸：梯度指数增长，训练不稳定
- 梯度裁剪：clip_grad_norm_限制梯度范数
- 解决方案：LSTM、GRU门控机制

## LSTM
- 三个门：遗忘门、输入门、输出门
- 细胞状态：长期记忆载体，门控选择性读写
- 遗忘门：决定丢弃哪些旧信息
- 输入门：决定存储哪些新信息
- 输出门：决定输出哪些信息

## GRU
- 简化版LSTM：重置门和更新门
- 更新门：控制旧信息保留程度
- 重置门：控制旧信息参与新候选值计算的程度
- 参数更少，训练更快
- 性能与LSTM相当

## 序列模型
- 多对多：序列到序列（机器翻译），Encoder-Decoder
- 多对一：序列分类（情感分析）
- 一对多：序列生成（图像描述）
- 注意力机制：加权关注输入序列不同位置
- 双向RNN：同时利用前向和后向信息
