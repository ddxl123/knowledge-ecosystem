# 2025Kubernetes入门
# 领域: 计算机IT
# 学科: Kubernetes
# 来源: 2025Kubernetes入门
# 描述: Kubernetes容器编排核心知识点

{{Kubernetes概述}}{{Kubernetes（简称K8s）是Google开源的容器编排平台，用于自动化部署、扩展和管理容器化应用。它将容器组织为逻辑单元，方便管理和发现。K8s最初源于Google内部的Borg系统，2014年开源，现由CNCF（云原生计算基金会）维护。2025年K8s已成为云原生领域的事实标准。}}

{{Kubernetes核心架构}}{{K8s采用主从架构，分为控制平面（Control Plane）和工作节点（Node）。控制平面包括API Server、etcd、Scheduler、Controller Manager；工作节点包括kubelet、kube-proxy和容器运行时。API Server是所有组件的通信中心，etcd存储集群状态数据。}}

{{Kubernetes API Server}}{{API Server是K8s的前端接口，所有操作（kubectl、Dashboard、内部组件通信）都通过它进行。它负责认证、授权、准入控制和数据验证。默认监听6443端口（HTTPS），是集群的核心枢纽。RESTful风格，所有资源都通过API对象表示。}}

{{etcd}}{{etcd是一个分布式键值存储系统，保存K8s集群的所有状态数据。它是集群的单一数据源（Single Source of Truth）。生产环境建议部署3或5个节点的etcd集群以保证高可用。注意：etcd数据丢失意味着集群状态丢失，需要定期备份。}}

{{Scheduler调度器}}{{Scheduler负责将未调度的Pod分配到合适的Node上。调度过程分为过滤（Filtering）和打分（Scoring）两个阶段。过滤排除不满足条件的节点，打分对剩余节点排序选择最优。可通过nodeSelector、affinity、taint/toleration等机制影响调度决策。}}

{{Controller Manager}}{{Controller Manager运行多个控制器进程，包括ReplicaSet Controller、Node Controller、Job Controller等。每个控制器通过API Server监听资源变化，将实际状态驱动到期望状态。这是K8s声明式管理的核心机制——你声明期望状态，控制器负责实现。}}

{{Node节点}}{{Node（也叫Worker Node）是运行容器化应用的工作机器。每个Node上运行kubelet、kube-proxy和容器运行时。Node可以是物理机或虚拟机。通过kubectl get nodes可查看集群节点状态，节点状态包括Ready、NotReady、SchedulingDisabled等。}}

{{kubelet}}{{kubelet是运行在每个Node上的代理程序，负责管理Pod的生命周期。它通过API Server接收PodSpec，确保其中描述的容器健康运行。kubelet还负责执行存活探针（livenessProbe）和就绪探针（readinessProbe），并向API Server汇报节点和Pod状态。}}

{{kube-proxy}}{{kube-proxy是运行在每个Node上的网络代理，负责实现Service的网络规则。它维护节点上的网络规则（iptables或IPVS），将发往Service的流量转发到后端Pod。2025年推荐使用IPVS模式，性能优于iptables模式。}}

{{Pod基础概念}}{{Pod是K8s中最小的可部署单元，包含一个或多个紧密关联的容器。同一Pod中的容器共享网络命名空间（可通过localhost互相访问）和存储卷。通常一个Pod运行一个主容器，可附加sidecar容器（如日志收集、代理）。Pod是临时性的，不应直接创建，应通过Deployment等控制器管理。}}

{{Pod生命周期}}{{Pod有多种状态：Pending（等待调度或拉取镜像）、Running（至少一个容器运行中）、Succeeded（所有容器正常退出）、Failed（至少一个容器失败）、Unknown（状态无法获取）。Pod启动时依次执行Init容器、postStart钩子，终止时执行preStop钩子和SIGTERM信号处理。}}

{{Init容器}}{{Init容器（Init Container）在主容器启动前按顺序运行，用于初始化环境。常见用途：等待依赖服务就绪、拉取配置文件、执行数据库迁移。Init容器失败会阻止Pod启动。每个Init容器必须成功完成后，下一个才会开始。}}

{{ReplicaSet}}{{ReplicaSet确保指定数量的Pod副本持续运行。它通过标签选择器匹配Pod，当实际副本数与期望不符时自动创建或删除Pod。通常不直接使用ReplicaSet，而是通过Deployment间接管理。ReplicaSet是Deployment的底层实现机制。}}

{{Deployment核心}}{{Deployment是K8s中最常用的控制器，用于声明式管理无状态应用。它管理ReplicaSet和Pod，支持滚动更新、回滚、扩缩容。创建Deployment时需指定replicas、selector、template等字段。Deployment确保应用始终处于期望状态，是生产环境部署应用的首选方式。}}

{{Deployment滚动更新}}{{滚动更新（Rolling Update）是Deployment默认的更新策略。更新时逐步用新版Pod替换旧版Pod，通过maxSurge（最大超出数）和maxUnavailable（最大不可用数）控制更新节奏。默认maxSurge=25%，maxUnavailable=25%。这样可以实现零停机更新，保证服务持续可用。}}

{{Deployment回滚}}{{Deployment支持版本历史记录，可通过kubectl rollout undo回滚到上一版本或指定版本。默认保留10个历史版本（可通过revisionHistoryLimit配置）。查看历史：kubectl rollout history deployment/xxx。回滚本质是将Deployment的PodTemplate恢复到指定版本。}}

{{Namespace命名空间}}{{Namespace用于将集群资源划分为逻辑隔离的组，适合多团队或多环境（dev/staging/prod）共享集群。K8s默认有default、kube-system、kube-public等命名空间。资源名在同一Namespace内唯一，不同Namespace可重名。通过kubectl -n xxx指定命名空间。}}

{{Label标签}}{{Label是附加到K8s对象上的键值对，用于组织和选择资源。Service通过标签选择器找到后端Pod，Deployment通过标签管理Pod。标签不提供唯一性，一个对象可有多个标签。常用标签：app、version、environment、tier等。}}

{{Selector选择器}}{{Selector是基于标签的资源过滤机制。等值选择器：app=nginx；集合选择器：environment in (production, qa)。Service和ReplicaSet使用selector确定它们管理的Pod集合。正确使用标签和选择器是K8s资源管理的关键。}}

{{Service概述}}{{Service为一组Pod提供稳定的网络访问入口。Pod IP是临时的（重建会变），而Service提供固定的虚拟IP（ClusterIP）和DNS名称。Service通过标签选择器关联后端Pod，实现负载均衡。定义Service时需指定ports和selector。}}

{{ClusterIP Service}}{{ClusterIP是默认的Service类型，仅在集群内部可访问。它分配一个集群内部虚拟IP，kube-proxy负责将流量转发到后端Pod。适用于内部微服务之间的通信。headless Service（clusterIP: None）不分配虚拟IP，直接返回Pod IP列表，适合有状态应用。}}

{{NodePort Service}}{{NodePort在每个Node上开放一个固定端口（30000-32767），外部可通过<NodeIP>:<NodePort>访问服务。NodePort自动创建ClusterIP。适合开发测试，生产环境通常使用Ingress替代。端口范围可通过--service-node-port-range参数调整。}}

{{LoadBalancer Service}}{{LoadBalancer类型自动创建云厂商的负载均衡器（如AWS ELB、阿里云SLB），将外部流量转发到Service。这是在云环境中暴露服务的标准方式。LoadBalancer自动创建NodePort和ClusterIP。在裸机环境需要MetalLB等方案提供支持。}}

{{ExternalName Service}}{{ExternalName类型将Service映射到外部DNS名称（如database.example.com），通过CNAME记录实现。不使用代理，适合将外部服务引入K8s服务发现体系。常用于跨集群或混合云场景中引用外部资源。}}

{{ConfigMap}}{{ConfigMap用于存储非敏感的配置数据，以键值对形式存在。可以作为环境变量、命令行参数或文件挂载到Pod中。支持从文件或目录创建。ConfigMap更新后，挂载的文件会自动更新（有延迟），但环境变量需要重启Pod。不应用于存储敏感信息。}}

{{Secret}}{{Secret用于存储敏感信息，如密码、token、SSH密钥。数据以Base64编码存储（注意：Base64不是加密）。使用方式类似ConfigMap：环境变量或文件挂载。启用静态加密（EncryptionConfiguration）可保护etcd中的Secret数据。2025年建议结合外部密钥管理（如Vault）使用。}}

{{Volume卷}}{{Volume用于Pod中容器间共享数据和持久化存储。emptyDir：Pod级别临时存储，Pod删除后数据丢失。hostPath：挂载Node文件系统。nfs：网络文件系统。云存储：awsElasticBlockStore、gcePersistentDisk等。PV和PVC是K8s推荐的存储管理抽象。}}

{{PersistentVolume持久卷}}{{PersistentVolume（PV）是集群级别的存储资源，由管理员预先创建或通过StorageClass动态创建。PV独立于Pod的生命周期，支持多种后端：NFS、Ceph、云存储等。PV有访问模式（ReadWriteOnce/ReadOnlyMany/ReadWriteMany）和回收策略（Retain/Delete/Recycle）。}}

{{PersistentVolumeClaim持久卷声明}}{{PersistentVolumeClaim（PVC）是用户对存储的请求。用户通过PVC声明所需的存储大小和访问模式，K8s自动绑定匹配的PV。PVC实现了存储的供需分离——用户不需要关心存储的具体实现细节。动态供应通过StorageClass自动创建PV满足PVC。}}

{{StorageClass存储类}}{{StorageClass定义存储的"类别"，支持动态PV供应。管理员定义不同的StorageClass（如fast-ssd、slow-hdd），用户在PVC中指定storageClassName。每个StorageClass指定供应者（provisioner）、参数和回收策略。默认StorageClass可通过注解设置。}}

{{Ingress概述}}{{Ingress是管理外部HTTP/HTTPS访问集群服务的API对象。它提供基于域名和路径的路由规则、TLS终止、负载均衡等功能。Ingress需要Ingress Controller（如Nginx Ingress、Traefik）才能工作。相比LoadBalancer Service，Ingress可以用一个公网IP暴露多个服务。}}

{{Ingress Controller}}{{Ingress Controller是实现Ingress规则的组件，监听Ingress资源变化并配置底层负载均衡器。常见选择：Nginx Ingress Controller（最广泛使用）、Traefik（支持自动服务发现）、Istio Gateway。安装方式通常通过Helm chart。2025年Gateway API逐步取代传统Ingress。}}

{{Ingress路由规则}}{{Ingress支持基于主机名（host）和路径（path）的路由。例如：app.example.com/api → api-service，app.example.com/web → web-service。支持精确匹配（Exact）、前缀匹配（Prefix）和实现特定的正则匹配。TLS配置通过引用Secret实现HTTPS终止。}}

{{Gateway API}}{{Gateway API是Ingress的演进版本（2025年GA），提供更丰富的路由能力。核心资源：GatewayClass（基础设施提供者）、Gateway（负载均衡配置）、HTTPRoute（路由规则）。支持请求头匹配、流量分割、请求改写等高级特性。被Istio、Envoy等广泛支持。}}

{{HorizontalPodAutoscaler水平自动扩缩容}}{{HPA根据CPU、内存或自定义指标自动调整Pod副本数。定义时指定最小/最大副本数和目标指标值（如CPU使用率50%）。HPA通过Metrics Server获取指标数据，定期计算期望副本数。支持自定义指标（如QPS、队列长度）和多指标组合。}}

{{Metrics Server}}{{Metrics Server是K8s集群级别的资源指标聚合器，为HPA和kubectl top提供CPU和内存使用数据。它从kubelet的Summary API收集指标数据。安装后可使用kubectl top nodes/pods查看资源使用情况。注意：Metrics Server提供实时数据，不存储历史数据。}}

{{VerticalPodAutoscaler垂直自动扩缩容}}{{VPA自动调整Pod的CPU和内存请求/限制。有三种模式：Off（仅推荐）、Initial（仅在创建时应用）、Recreates（驱逐并重建Pod）。适用于无法水平扩展的有状态应用。2025年推荐结合Goldilocks工具使用，获取最佳资源配置建议。}}

{{DaemonSet}}{{DaemonSet确保在每个（或指定）Node上运行一个Pod副本。适用于节点级守护进程：日志收集器（Fluentd）、监控代理（Prometheus Node Exporter）、网络插件（Calico）。新增Node时自动创建Pod，Node删除时Pod被垃圾回收。支持滚动更新。}}

{{StatefulSet}}{{StatefulSet用于管理有状态应用（如数据库、消息队列），提供稳定的网络标识（pod-0、pod-1）和持久存储。Pod按顺序创建和删除，每个Pod有独立的PVC。适用于需要稳定标识、有序部署和持久存储的场景。Headless Service为StatefulSet提供DNS记录。}}

{{Job和CronJob}}{{Job创建一个或多个Pod执行一次性任务，确保指定数量的成功完成。parallelism控制并行Pod数，completions控制成功完成数。CronJob按Cron定时调度Job，适合批处理、备份、报告等定时任务。支持并发策略（Allow/Forbid/Replace）和历史限制。}}

{{RBAC权限控制}}{{RBAC（基于角色的访问控制）是K8s的授权机制。核心概念：Role（命名空间级权限）、ClusterRole（集群级权限）、RoleBinding（将Role绑定到用户/组）、ClusterRoleBinding（集群级绑定）。遵循最小权限原则，避免使用cluster-admin角色。通过kubectl auth can-i检查权限。}}

{{ServiceAccount}}{{ServiceAccount为Pod中的进程提供身份标识。默认每个命名空间有default ServiceAccount。创建专用SA并绑定Role，可精确控制Pod的API访问权限。SA的token自动挂载到Pod（可通过automountServiceAccountToken: false禁用）。2025年推荐使用TokenRequest API的短期token。}}

{{NetworkPolicy网络策略}}{{NetworkPolicy定义Pod间的网络访问规则，实现网络隔离。默认所有Pod可互相通信。通过ingress（入站）和egress（出站）规则限制流量。选择器决定策略适用的Pod，支持基于Pod标签、Namespace标签和IP CIDR的规则。需要CNI插件支持（如Calico、Cilium）。}}

{{Helm包管理器}}{{Helm是K8s的包管理器，通过Chart打包应用。Chart包含模板化的K8s清单文件，通过values.yaml自定义配置。常用命令：helm install、helm upgrade、helm rollback、helm list。2025年Helm 3已成为K8s应用部署的事实标准。Artifact Hub提供丰富的公共Chart。}}

{{Helm Chart结构}}{{典型的Helm Chart目录结构：Chart.yaml（元数据）、values.yaml（默认配置）、templates/（K8s清单模板）、templates/NOTES.txt（安装后提示）。模板使用Go模板语法，支持条件判断、循环、函数。helpers.tpl定义可复用的模板片段。可通过helm create快速生成Chart骨架。}}

{{kubectl基础命令}}{{kubectl是K8s的命令行工具。基础操作：kubectl get（查看资源）、kubectl describe（详细信息）、kubectl create/apply（创建/更新资源）、kubectl delete（删除资源）、kubectl logs（查看日志）、kubectl exec（进入容器）。推荐使用kubectl apply而非create，支持声明式管理。}}

{{kubectl高级技巧}}{{实用技巧：-o jsonpath自定义输出格式，--field-selector按字段筛选，kubectl port-forward端口转发调试服务，kubectl cp在容器和本地间拷贝文件，kubectl top查看资源使用，kubectl debug创建调试容器。alias k=kubectl 可提高效率。JSONPath和Go模板支持复杂查询。}}

{{kubectl apply与声明式管理}}{{kubectl apply使用三路合并（three-way merge）策略，比较本地配置、线上状态和last-applied-annotation，智能更新资源。推荐将所有K8s配置存入Git仓库（GitOps）。相比kubectl replace/patch，apply更安全，不会覆盖未管理的字段。dry-run=client可预览变更。}}

{{资源请求与限制}}{{resources字段控制Pod的CPU和内存使用。requests（请求）：调度依据，确保Node有足够资源；limits（限制）：运行时上限，超限可能被OOMKilled或限流。最佳实践：设置合理的requests，limits设为requests的1.5-2倍。推荐使用VPA工具自动优化。}}

{{探针机制}}{{K8s提供三种探针：livenessProbe（存活探针）检测容器是否存活，失败则重启容器；readinessProbe（就绪探针）检测容器是否就绪接收流量，失败则从Service端点移除；startupProbe（启动探针）用于慢启动应用，成功前其他探针不会执行。支持HTTP、TCP、gRPC和命令探测方式。}}

{{Taint与Toleration}}{{Taint应用于Node，排斥不匹配的Pod；Toleration应用于Pod，允许被调度到有对应Taint的Node。效果类型：NoSchedule（不调度新Pod）、PreferNoSchedule（尽量不调度）、NoExecute（驱逐已有Pod）。常用于专用节点（GPU）、节点维护等场景。kubectl taint nodes node1 key=value:NoSchedule。}}

{{Node Affinity节点亲和性}}{{nodeAffinity通过标签表达式约束Pod调度到特定Node。requiredDuringSchedulingIgnoredDuringExecution（硬性要求）和preferredDuringSchedulingIgnoredDuringExecution（软性偏好）。比nodeSelector更灵活，支持复杂的标签匹配规则。topologyKey可实现跨区域/机架分布。}}

{{Pod Affinity与Anti-Affinity}}{{podAffinity/podAntiAffinity控制Pod间的调度关系。亲和性将相关Pod调度到一起（如Web和Cache），反亲和性将Pod分散到不同Node（如多副本高可用）。支持required和preferred两种约束级别。结合topology.kubernetes.io/zone实现跨可用区分布。}}

{{Kustomize配置管理}}{{Kustomize是K8s原生的配置管理工具，无需模板引擎。通过kustomization.yaml定义base和overlay，实现环境差异化配置。kubectl apply -k支持原生Kustomize。适合不需要Helm复杂性的场景。支持patches、configMapGenerator、secretGenerator等特性。2025年与ArgoCD深度集成。}}

{{Kubernetes事件与日志}}{{kubectl get events查看集群事件，按时间排序，包含资源创建、调度、错误等信息。Pod日志：kubectl logs <pod> [-c container]，-f实时跟踪，--previous查看崩溃前日志。集中式日志方案：EFK（Elasticsearch+Fluentd+Kibana）或PLG（Promtail+Loki+Grafana）。}}

{{Kubernetes监控方案}}{{主流监控栈：Prometheus（指标采集）+ Grafana（可视化）。Prometheus通过ServiceMonitor（Prometheus Operator）自动发现监控目标。关键指标：节点CPU/内存/磁盘、Pod资源使用、API Server延迟、etcd健康。kube-prometheus-stack是2025年最流行的K8s监控方案。}}

{{Istio服务网格}}{{Istio是K8s最流行的服务网格，提供流量管理、安全和可观测性。核心组件：Envoy Sidecar代理、Istiod控制平面。功能：金丝雀发布、故障注入、重试超时、mTLS加密、分布式追踪。2025年Ambient Mesh模式减少Sidecar开销，简化部署。}}

{{Kubernetes安全最佳实践}}{{安全要点：1.启用RBAC并遵循最小权限原则；2.使用NetworkPolicy限制网络访问；3.扫描容器镜像漏洞（Trivy/Snyk）；4.启用Pod Security Standards（替代PodSecurityPolicy）；5.加密etcd数据；6.定期更新K8s版本；7.使用非root用户运行容器；8.限制资源使用防止DoS。}}

{{Pod Security Standards}}{{Pod Security Standards（PSS）替代已废弃的PodSecurityPolicy，定义三个安全级别：Privileged（无限制）、Baseline（基本限制，阻止已知特权升级）、Restricted（严格限制，遵循安全最佳实践）。通过Namespace标签强制执行，2025年推荐对生产命名空间使用Restricted级别。}}

{{GitOps工作流}}{{GitOps以Git仓库为基础设施和应用配置的唯一事实来源。ArgoCD和Flux是主流的GitOps工具，自动将Git中的K8s配置同步到集群。工作流：代码变更→Git提交→自动同步→集群更新。优势：版本化、可审计、易回滚。2025年GitOps已成为K8s运维最佳实践。}}

{{ArgoCD}}{{ArgoCD是声明式GitOps持续交付工具，支持自动/手动同步、多集群管理、应用回滚、健康状态监控。通过Web UI或CLI管理应用。支持Kustomize、Helm、Jsonnet等配置工具。Application CRD定义Git仓库到K8s集群的映射关系。2025年已成GitOps事实标准。}}

{{Kubernetes集群升级策略}}{{集群升级需要谨慎规划。控制平面先升级，然后升级工作节点。策略：原地升级（In-Place）或滚动替换（Rolling Replacement）。云托管集群（EKS/GKE/ACK）通常提供一键升级。升级前：备份etcd、检查兼容性、升级插件。建议每个版本升级跨度不超过3个小版本。}}

{{kubectl debug调试}}{{kubectl debug（K8s 1.25+）创建临时调试容器附加到运行中的Pod。可共享进程命名空间调试crashlooping容器。支持节点调试：kubectl debug node/xxx -it --image=busybox。比直接exec更安全，不需要在应用镜像中安装调试工具。2025年已成标准调试方式。}}

{{Kubernetes多集群管理}}{{多集群场景：多云部署、灾备、边缘计算。管理方案：kubectx快速切换集群上下文，Cluster API声明式管理集群生命周期，Submariner实现跨集群网络，Liqo实现集群资源联邦。2025年vCluster提供轻量级虚拟集群，适合多租户场景。}}

{{Kubernetes成本优化}}{{成本优化策略：1.右置资源（设置合理的requests/limits）；2.使用Spot/抢占式实例；3.HPA自动扩缩容；4.集群自动伸缩（Cluster Autoscaler/Karpenter）；5.清理未使用的资源；6.使用Kubecost/OpenCost监控成本；7.命名空间资源配额；8.镜像优化减小体积。}}

{{Karpenter节点自动伸缩}}{{Karpenter是AWS开源的K8s节点自动伸缩器（2025年已支持多云），替代传统Cluster Autoscaler。直接与云API交互，快速创建最优规格节点。支持Spot实例、节点合并（Consolidation）、过期回收。响应速度秒级，比CAS更适合弹性工作负载。}}

{{容器运行时接口CRI}}{{CRI（Container Runtime Interface）是K8s与容器运行时之间的标准接口。2025年主流运行时：containerd（默认）、CRI-O。Docker已从K8s 1.24起不再作为直接运行时（dockershim移除），但containerd底层仍兼容Docker镜像。gVisor和Kata Containers提供安全沙箱运行时。}}

{{Kubernetes故障排查流程}}{{系统化排查：1.kubectl get pods检查Pod状态；2.kubectl describe pod查看事件和错误；3.kubectl logs查看容器日志；4.kubectl exec进入容器调试；5.kubectl get events查看集群事件；6.检查Service/Endpoint关联；7.kubectl top检查资源使用；8.检查NetworkPolicy和RBAC规则。}}

{{CNI容器网络接口}}{{CNI（Container Network Interface）负责Pod网络配置。主流CNI插件：Calico（支持网络策略、广泛应用）、Cilium（基于eBPF、高性能）、Flannel（简单轻量）、Weave。选择考虑：性能、网络策略支持、多集群网络、IPv6支持。2025年Cilium因eBPF技术成为热门选择。}}

{{2025年Kubernetes新特性}}{{2025年值得关注的K8s特性：1.Gateway API正式GA；2.原生Sidecar容器支持（K8s 1.28+）；3.改进的HPA和VPA；4.Pod Security Admission增强；5.结构化日志标准化；6.KMS v2加密增强；7.用户命名空间支持；8.更高效的API Server。持续关注kubernetes.io/blog获取最新动态。}}
