# Kubernetes 基础


## 收集指南

> ⚠️ **严格范围限定：** 仅收集本章（第6章 K8s）相关内容，禁止跨章节引用。

### 条目描述
本文件是《2025Docker容器化实战》中关于「K8s」的知识原子文件，属于云计算方向。

### 知识结构
- K8s 架构
- 核心资源对象
- 部署与服务
- 配置管理
- 运维操作

### 待收集原子知识点
- Master/Node 架构
- Pod/Deployment/Service
- Ingress 与负载均衡
- ConfigMap/Secret
- kubectl 常用命令

## 核心知识点

### 一、K8s 架构

- **Control Plane（控制平面）：**
  - `kube-apiserver`：API 入口，所有操作的统一接口
  - `etcd`：分布式键值存储，保存集群状态
  - `kube-scheduler`：将 Pod 调度到合适的 Node
  - `kube-controller-manager`：执行控制循环

- **Worker Node（工作节点）：**
  - `kubelet`：管理节点上的 Pod 生命周期
  - `kube-proxy`：实现 Service 的网络代理
  - `Container Runtime`：运行容器（containerd/CRI-O）

### 二、核心资源对象

```yaml
# Pod（最小部署单元）
apiVersion: v1
kind: Pod
metadata:
  name: myapp
  labels:
    app: myapp
spec:
  containers:
    - name: myapp
      image: myapp:v1
      ports:
        - containerPort: 8080
      resources:
        requests:
          memory: "128Mi"
          cpu: "250m"
        limits:
          memory: "256Mi"
          cpu: "500m"
      livenessProbe:
        httpGet:
          path: /health
          port: 8080
        initialDelaySeconds: 10
        periodSeconds: 30
      readinessProbe:
        httpGet:
          path: /ready
          port: 8080
```

```yaml
# Deployment（管理 Pod 副本）
apiVersion: apps/v1
kind: Deployment
metadata:
  name: myapp
spec:
  replicas: 3
  selector:
    matchLabels:
      app: myapp
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: myapp
    spec:
      containers:
        - name: myapp
          image: myapp:v1
          ports:
            - containerPort: 8080
```

### 三、Service 与 Ingress

```yaml
# Service（服务发现与负载均衡）
apiVersion: v1
kind: Service
metadata:
  name: myapp-svc
spec:
  selector:
    app: myapp
  ports:
    - port: 80
      targetPort: 8080
  type: ClusterIP  # ClusterIP/NodePort/LoadBalancer

---
# Ingress（HTTP 路由）
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: myapp-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - host: myapp.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: myapp-svc
                port:
                  number: 80
```

- **Service 类型：**
  - `ClusterIP`：集群内部访问（默认）
  - `NodePort`：通过节点端口外部访问
  - `LoadBalancer`：云厂商负载均衡器
  - `ExternalName`：CNAME 记录

### 四、ConfigMap 与 Secret

```yaml
# ConfigMap
apiVersion: v1
kind: ConfigMap
metadata:
  name: myapp-config
data:
  APP_ENV: "production"
  DB_HOST: "mysql.default.svc.cluster.local"
  nginx.conf: |
    server { listen 80; ... }

---
# Secret
apiVersion: v1
kind: Secret
metadata:
  name: myapp-secret
type: Opaque
data:
  DB_PASSWORD: cGFzc3dvcmQ=  # base64 编码
  API_KEY: c2VjcmV0a2V5

# 在 Pod 中使用
spec:
  containers:
    - name: myapp
      envFrom:
        - configMapRef:
            name: myapp-config
        - secretRef:
            name: myapp-secret
```

### 五、kubectl 常用命令

```bash
# 集群信息
kubectl cluster-info
kubectl get nodes

# 资源管理
kubectl apply -f deployment.yaml    # 创建/更新
kubectl delete -f deployment.yaml  # 删除
kubectl get pods -o wide            # 查看 Pod
kubectl describe pod myapp          # Pod 详情
kubectl logs myapp -f               # 查看日志
kubectl exec -it myapp -- /bin/bash # 进入容器

# 扩缩容
kubectl scale deployment myapp --replicas=5
kubectl autoscale deployment myapp --min=3 --max=10 --cpu-percent=80

# 滚动更新
kubectl set image deployment/myapp myapp=myapp:v2
kubectl rollout status deployment/myapp
kubectl rollout undo deployment/myapp  # 回滚

# 命名空间
kubectl get namespaces
kubectl create namespace staging
kubectl config set-context --current --namespace=staging
```
