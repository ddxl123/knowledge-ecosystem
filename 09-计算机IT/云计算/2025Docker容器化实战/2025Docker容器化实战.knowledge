# Docker容器化实战
# 领域: 计算机IT
# 学科: 云计算与容器技术
# 来源: Docker容器化实战（2025版）
# 描述: Docker容器技术从入门到实战，涵盖镜像管理、容器编排、网络配置及生产环境部署

{{Docker基本概念}}{{Docker是基于Linux内核特性（Namespace隔离、Cgroups资源限制、UnionFS分层文件系统）的容器化平台。核心组件包括：Docker Engine（守护进程+CLI）、镜像(Image,只读模板)、容器(Container,运行实例)、仓库(Registry,镜像存储)。容器相比虚拟机更轻量，共享宿主机内核，启动速度快、资源占用少。}}

{{Docker安装与配置}}{{支持Linux、macOS、Windows平台。Linux通过包管理器安装docker-ce；macOS/Windows使用Docker Desktop。配置镜像加速器（/etc/docker/daemon.json中设置registry-mirrors）。Docker守护进程通过systemctl管理。非root用户使用docker需加入docker用户组。cgroup driver推荐使用systemd。}}

{{镜像管理命令}}{{docker images列出本地镜像；docker pull拉取镜像（指定tag如nginx:1.25）；docker rmi删除镜像；docker tag打标签；docker push推送镜像；docker save/load导出导入镜像为tar文件；docker inspect查看镜像详情。镜像采用分层存储，相同层可共享，节省存储空间。镜像ID基于SHA256哈希。}}

{{容器生命周期管理}}{{docker run创建并启动容器（-d后台、-it交互式、--name命名、-p端口映射、-v卷挂载、--restart重启策略）；docker start/stop/restart控制容器；docker rm删除容器（-f强制）；docker ps查看运行中容器（-a所有）；docker logs查看日志（-f跟踪）；docker exec在运行容器中执行命令（-it进入交互Shell）；docker cp在容器和主机间复制文件。}}

{{Dockerfile详解}}{{Dockerfile是镜像构建的指令文件。常用指令：FROM(基础镜像)、RUN(执行命令)、COPY/ADD(复制文件)、WORKDIR(工作目录)、ENV(环境变量)、EXPOSE(声明端口)、CMD(默认命令)、ENTRYPOINT(入口点)、ARG(构建参数)、USER(运行用户)、HEALTHCHECK(健康检查)。最佳实践：合并RUN减少层数、使用多阶段构建、合理排序指令利用缓存、使用.dockerignore排除无关文件。}}

{{Docker数据卷}}{{数据卷(Volume)是Docker持久化数据的机制。docker volume create创建卷；-v或--mount挂载卷到容器。三种挂载方式：Volume(Docker管理，推荐)、Bind Mount(直接挂载主机目录)、tmpfs(内存临时文件系统)。数据卷独立于容器生命周期，容器删除后数据仍保留。命名卷便于在多个容器间共享数据。}}

{{Docker网络模型}}{{Docker网络驱动：bridge(默认，容器间桥接)、host(共享主机网络栈)、none(无网络)、overlay(跨主机通信)、macvlan(MAC地址分配)。docker network create创建自定义网络。容器间通过服务名进行DNS解析（自定义网络中）。端口映射-p host:container将容器端口暴露到主机。docker network connect/disconnect动态管理容器网络。}}

{{Docker Compose}}{{Docker Compose用于定义和运行多容器应用。docker-compose.yml文件定义services、networks、volumes。常用命令：docker compose up(-d后台)启动、docker compose down停止并删除、docker compose logs查看日志、docker compose ps查看状态、docker compose exec执行命令。支持环境变量(.env文件)、依赖关系(depends_on)、健康检查(healthcheck)、资源限制(deploy.resources)。}}

{{镜像仓库与分发}}{{Docker Hub是默认公共仓库。私有仓库：Harbor(企业级，含UI、扫描、签名)、Registry(官方轻量方案)。docker login登录仓库；docker push推送镜像。镜像命名：registry/namespace/repository:tag。Harbor提供项目管理、漏洞扫描(Trivy)、镜像签名(Notary)、RBAC权限控制、镜像复制等功能。}}

{{Docker安全实践}}{{安全原则：最小权限运行(非root用户)、镜像安全扫描(trivy/grype)、使用可信基础镜像、只读文件系统(--read-only)、限制资源(--memory/--cpus)、seccomp/apparmor安全配置。Docker Content Trust(DCT)启用镜像签名验证。docker scout进行镜像漏洞分析。避免使用--privileged特权模式。网络隔离使用自定义bridge网络。}}

{{容器编排基础}}{{容器编排解决大规模容器管理问题。Kubernetes(k8s)是主流编排平台，核心概念：Pod(最小调度单元)、Service(服务发现与负载均衡)、Deployment(声明式部署)、ConfigMap/Secret(配置管理)、Namespace(资源隔离)。Docker Swarm是Docker原生编排方案，较简单但功能有限。docker stack deploy使用Compose文件部署Swarm服务。}}

{{多阶段构建}}{{多阶段构建(multi-stage build)减小最终镜像体积。在Dockerfile中使用多个FROM指令，前一阶段用于编译构建，后一阶段仅复制编译产物到精简基础镜像。例如Go应用：第一阶段golang镜像编译二进制，第二阶段scratch或alpine镜像仅包含二进制文件。可使用--target指定构建到哪个阶段。}}

{{Docker日志与监控}}{{日志驱动：json-file(默认)、syslog、journald、fluentd、awslogs等。docker logs查看容器日志。生产环境推荐集中日志方案(ELK/Loki)。监控：cAdvisor收集容器指标，Prometheus+Grafana可视化。docker stats实时查看容器资源使用。健康检查(HEALTHCHECK)监控容器应用状态。docker events监听Docker守护进程事件。}}

{{CI/CD中的Docker}}{{Docker在CI/CD中的应用：构建阶段在Docker中编译测试，推送镜像到仓库，部署阶段拉取并运行。常用CI工具：GitHub Actions、GitLab CI、Jenkins。镜像标签策略：语义版本(v1.2.3)、Git SHA、latest。DinD(Docker in Docker)和DooD(Docker outside of Docker)两种CI中使用Docker的方式。自动化镜像构建与安全扫描集成。}}

{{容器存储驱动}}{{存储驱动管理镜像分层和容器可写层。overlay2(推荐)是主流驱动，基于OverlayFS。devicemapper、aufs、btrfs等为其他选项。容器可写层使用Copy-on-Write(CoW)策略，读取时直接读下层，写入时才复制到可写层。存储驱动影响容器性能和特性支持。/var/lib/docker是Docker数据存储根目录。}}

{{Docker环境变量与配置管理}}{{docker run -e设置环境变量；--env-file从文件加载。Docker Compose中environment和env_file配置。敏感信息使用Docker Secrets(Swarm模式)或外部密钥管理。配置文件可通过volume挂载或COPY到镜像。环境变量注入是容器配置外部化的常用方式。配置变更无需重建镜像。}}

{{容器资源限制}}{{cgroups限制容器资源：--memory限制内存(--memory-swap总内存)、--cpus限制CPU份额、--blkio-weight限制IO权重、--pids-limit限制进程数。OOM(Out of Memory)时容器可能被杀死。docker inspect查看资源限制设置。Kubernetes中通过resources.requests和resources.limits配置。资源限制是多租户环境的必要安全措施。}}

{{Docker与微服务}}{{Docker是微服务架构的理想载体。每个服务独立打包为镜像，独立部署扩展。服务间通过API(REST/gRPC)或消息队列通信。Docker Compose用于本地开发多服务环境。服务发现通过DNS(自定义网络)或Consul/etcd。配置管理使用环境变量或配置中心。链路追踪(Jaeger/Zipkin)监控微服务调用链。}}

{{Docker在生产环境中的最佳实践}}{{生产环境注意事项：使用固定版本标签(非latest)、实施镜像安全扫描、配置资源限制、设置重启策略(--restart unless-stopped)、使用健康检查、日志集中收集、监控告警、定期更新基础镜像、使用只读文件系统、网络隔离、秘密管理、优雅停机(SIGTERM处理)、镜像瘦身(alpine/distroless)。}}

{{Docker Desktop与开发环境}}{{Docker Desktop适用于macOS和Windows，提供图形化界面、Kubernetes集成、Volume管理、Extensions市场。Dev Environments支持一键创建开发环境。docker context管理多个Docker端点。WSL2后端(Windows)提供接近原生Linux性能。Testcontainers在测试中使用容器化依赖服务。}}

{{Kubernetes基础与Docker集成}}{{Kubernetes集群由Master(Node)和Worker(Node)组成。核心资源：Pod、Deployment、Service(ClusterIP/NodePort/LoadBalancer)、Ingress。kubectl命令行管理集群。kubectl apply -f声明式部署。ConfigMap和Secret管理配置。PV/PVC持久化存储。HPA自动扩缩容。Helm是Kubernetes包管理器。kubectl logs/exec调试Pod。}}
