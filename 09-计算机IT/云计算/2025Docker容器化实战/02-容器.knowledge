# Docker容器

## 容器生命周期
- docker run：创建并启动容器
- docker start/stop/restart：启动/停止/重启
- docker pause/unpause：暂停/恢复
- docker rm：删除容器
- docker exec：在运行中容器内执行命令

## 容器运行
- -d：后台运行（detached模式）
- -p 8080:80：端口映射（宿主:容器）
- -v /host:/container：卷挂载
- -e KEY=VALUE：环境变量
- --name：容器命名
- --network：指定网络
- --restart：重启策略（always/unless-stopped/on-failure）

## 容器资源限制
- --memory：内存限制（--memory=512m）
- --cpus：CPU限制（--cpus=1.5）
- --blkio-weight：IO权重
- --pids-limit：进程数限制
- cgroups：Linux控制组，实现资源隔离

## 容器日志与监控
- docker logs：查看容器日志
- --log-driver：日志驱动（json-file/syslog/fluentd）
- docker stats：实时资源使用统计
- docker inspect：容器详细信息
- 健康检查：HEALTHCHECK指令

## 容器安全
- 非root用户：USER指令指定运行用户
- 只读文件系统：--read-only
- 能力限制：--cap-drop ALL --cap-add
- seccomp配置：限制系统调用
- 镜像扫描：运行前扫描漏洞
