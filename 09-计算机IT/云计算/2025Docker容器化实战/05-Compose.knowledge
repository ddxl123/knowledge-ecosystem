# Docker Compose

## 基础配置
- docker-compose.yml：YAML格式定义多容器应用
- 三大部分：services、volumes、networks
- 版本：Compose Specification（无需version字段）
- docker compose up/down：启动/停止所有服务
- docker compose ps/logs：查看状态/日志

## 服务定义
- image/build：使用镜像或Dockerfile构建
- ports：端口映射
- volumes：卷挂载
- environment/env_file：环境变量
- depends_on：服务依赖关系
- restart：重启策略

## 网络与卷
- 默认网络：所有服务在同一网络，服务名即DNS
- 自定义网络：networks定义
- 外部网络：external: true使用已有网络
- 命名卷：volumes定义，持久化数据
- 匿名卷：自动创建

## 高级特性
- 健康检查：healthcheck配置
- 资源限制：deploy.resources.limits
- 配置和密钥：configs、secrets
- 扩展字段：x-前缀的YAML锚点
- 多文件：-f指定多个compose文件

## 实战配置
- Web应用栈：Nginx + Node.js + MongoDB + Redis
- 开发环境：热重载、调试端口暴露
- CI/CD：测试环境一键启动
- 环境隔离：dev/staging/prod不同配置
- 网络安全：内部网络不暴露端口
