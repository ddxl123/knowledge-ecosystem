# Docker网络

## 网络模式
- bridge（默认）：桥接网络，容器通过虚拟网桥通信
- host：使用宿主机网络，性能好但隔离性差
- none：无网络，完全隔离
- overlay：跨主机容器通信（Swarm/K8s）
- macvlan：容器获得独立MAC地址

## 自定义网络
- docker network create：创建自定义bridge网络
- 自定义网络支持DNS解析：容器名即域名
- docker network connect/disconnect：连接/断开网络
- 子网和网关配置：--subnet、--gateway
- 网络隔离：不同网络的容器默认不通信

## 容器间通信
- 同一网络：容器名DNS解析直接通信
- link（旧方式）：--link container_name
- 端口映射：-p宿主端口:容器端口
- 环境变量：Docker自动注入linked容器信息
- 服务发现：Docker内置DNS服务器

## 网络驱动
- bridge：单机容器通信
- overlay：跨主机通信，VXLAN隧道
- macvlan：直接使用物理网络
- none：禁用网络
- 第三方插件：Weave、Calico、Flannel

## 网络调试
- docker network inspect：查看网络详情
- docker exec + ping/curl：容器内网络测试
- tcpdump：抓包分析
- iptables：Docker自动配置的NAT规则
- DNS调试：nslookup、dig
