# Docker镜像

## 镜像基础
- 镜像：只读模板，包含运行应用所需的一切（代码、运行时、库、环境变量）
- 分层存储：镜像由多个只读层叠加，共享基础层
- Union FS：联合文件系统，将多个目录挂载到同一位置
- 镜像ID：SHA256哈希值唯一标识
- 标签：name:tag格式，如nginx:latest、node:18-alpine

## Dockerfile
- FROM：基础镜像，必须是第一条指令
- RUN：执行命令，每条RUN创建一层
- COPY/ADD：复制文件到镜像（ADD支持URL和解压）
- CMD/ENTRYPOINT：容器启动命令
- ENV/ARG：环境变量/构建参数

## Dockerfile最佳实践
- 多阶段构建：FROM ... AS builder → FROM ... COPY --from=builder
- 减少层数：合并RUN命令，&&链接
- 使用.dockerignore：排除不需要的文件
- 选择小基础镜像：alpine、distroless
- 固定版本：避免使用latest标签

## 镜像管理
- docker build：构建镜像
- docker images：列出本地镜像
- docker pull/push：拉取/推送镜像
- docker tag：给镜像打标签
- docker rmi：删除镜像

## 镜像仓库
- Docker Hub：官方公共仓库
- 私有仓库：Harbor、阿里云ACR、AWS ECR
- 镜像扫描：Trivy、Snyk扫描安全漏洞
- 镜像签名：Docker Content Trust验证镜像完整性
- 自动构建：GitHub/CI集成自动构建推送
