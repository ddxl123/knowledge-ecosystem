# 2025操作系统核心知识
# 领域: 计算机IT
# 学科: 操作系统
# 来源: 2025操作系统核心知识
# 描述: 操作系统核心知识点

{{知识点1}}{{操作系统的基本功能包括哪些？}}
操作系统的基本功能包括：处理器管理（进程/线程调度）、存储器管理（内存分配与回收）、设备管理（I/O设备分配与控制）、文件管理（文件存储、检索和保护）、用户接口（命令接口和程序接口）。操作系统是计算机硬件与应用程序之间的桥梁，负责资源的合理分配和高效利用。

{{知识点2}}{{操作系统的四大特征是什么？}}
操作系统的四大特征是：①并发性——多个程序在宏观上同时推进；②共享性——系统资源被多个并发进程共同使用；③虚拟性——通过技术手段将一个物理实体变为若干逻辑上的对应物；④异步性——程序执行走走停停，以不可预知的速度推进。其中并发和共享是最基本的两个特征，互为存在条件。

{{知识点3}}{{进程与程序的本质区别是什么？}}
进程是程序的一次动态执行过程，是系统资源分配和调度的基本单位；程序是静态的指令集合，是永久存在的。区别在于：①进程是动态的，程序是静态的；②进程有生命周期（创建→就绪→运行→阻塞→终止），程序没有；③一个程序可对应多个进程，一个进程可执行多个程序；④进程有PCB（进程控制块），程序没有独立的控制结构。

{{知识点4}}{{进程控制块（PCB）包含哪些信息？}}
PCB是操作系统用于管理进程的核心数据结构，包含：①进程标识符PID——唯一标识进程；②进程状态——就绪/运行/阻塞等；③程序计数器PC——下一条指令地址；④寄存器值——保存CPU上下文；⑤内存管理信息——页表、段表指针等；⑥I/O状态信息——已打开文件、分配设备等；⑦记账信息——CPU使用时间、时间片等；⑧调度信息——优先级、调度队列指针等。

{{知识点5}}{{进程的五状态模型是什么？}}
进程五状态模型：①新建态（New）——进程正在被创建；②就绪态（Ready）——已获得除CPU外的所有资源，等待调度；③运行态（Running）——占有CPU，正在执行；④阻塞态/等待态（Blocked/Waiting）——等待某事件（如I/O完成）发生；⑤终止态（Terminated）——进程执行完毕或被终止。状态转换：新建→就绪，就绪→运行（调度），运行→就绪（时间片用完），运行→阻塞（请求I/O），阻塞→就绪（I/O完成），运行→终止。

{{知识点6}}{{进程与线程的区别是什么？}}
①调度单位不同：传统OS中进程是独立调度的基本单位，线程是CPU调度的基本单位；②资源分配不同：进程是资源分配的基本单位，同一进程的各线程共享该进程的资源（地址空间、打开文件等）；③开销不同：进程创建/切换开销大（需切换地址空间），线程开销小（共享地址空间，只需保存少量寄存器）；④通信方式不同：进程间通信需要IPC机制，线程间可直接读写共享变量；⑤独立性不同：进程间相互独立，线程间耦合度高。

{{知识点7}}{{用户级线程与内核级线程的区别和联系是什么？}}
用户级线程（ULT）：在用户空间实现，由线程库管理，内核不可见，创建/切换无需内核干预，开销小，但一个ULT阻塞会导致整个进程阻塞。内核级线程（KLT）：由内核管理，创建/切换需系统调用，开销较大，但一个KLT阻塞不影响其他KLT。多线程模型：①多对一模型——多个ULT映射到一个KLT；②一对一模型——每个ULT对应一个KLT；③多对多模型——多个ULT映射到多个KLT。现代操作系统普遍采用一对一或多对多模型。

{{知识点8}}{{进程间通信（IPC）的主要方式有哪些？}}
①共享内存——多个进程共享一块内存区域，需要同步机制保护，速度最快；②消息传递——通过系统调用send/receive发送消息，分为直接通信和间接通信（信箱）；③管道通信——半双工通信，分为普通管道（父子进程间）和命名管道（无亲缘关系进程间）；④信号量——用于同步和互斥的计数器；⑤套接字——可用于不同机器间的进程通信；⑥信号——异步通知机制，用于通知进程某事件发生。

{{知识点9}}{{处理机调度的三个层次分别是什么？}}
①高级调度（作业调度/长程调度）——从外存后备队列中选择作业调入内存，创建进程，频率最低；②中级调度（内存调度）——将暂时不能运行的进程调至外存（挂起），或从外存重新调入内存，频率中等；③低级调度（进程调度/短程调度）——从就绪队列中选择进程分配CPU，频率最高。三个层次配合工作，确保系统资源的高效利用。

{{知识点10}}{{常见进程调度算法有哪些？各有什么特点？}}
①FCFS（先来先服务）——非抢占，简单公平，但可能产生"护航效应"（短作业被长作业阻塞）；②SJF（最短作业优先）——平均等待时间最短，但可能导致长作业饥饿；③SRTF（最短剩余时间优先）——SJF的抢占版本；④RR（时间片轮转）——适合分时系统，时间片太大退化为FCFS，太小切换开销大；⑤优先级调度——可能导致低优先级饥饿，可用老化（aging）解决；⑥多级反馈队列——综合以上优点，设置多个优先级队列，进程可在队列间移动。

{{知识点11}}{{什么是上下文切换？其开销包括哪些？}}
上下文切换是CPU从一个进程/线程切换到另一个的过程。步骤：①保存当前进程的寄存器、PC、PSW等到其PCB；②将新进程的上下文从PCB加载到CPU寄存器。开销包括：①直接开销——保存/恢复寄存器、切换地址空间（刷新TLB/页表缓存）、切换内核栈；②间接开销——缓存失效（指令缓存、数据缓存冷启动）、TLB未命中带来的额外内存访问。上下文切换频率是影响系统性能的重要因素。

{{知识点12}}{{临界区问题及解决方案是什么？}}
临界区是访问共享资源的代码段。临界区问题需满足：①互斥——同一时刻只有一个进程在临界区；②前进——如果没有进程在临界区，等待进入的进程能进入；③有限等待——进程等待进入的时间有限。解决方案：①Peterson算法——软件方案，适用于两个进程；②硬件方案——禁用中断（单CPU有效）、原子指令（test_and_set、compare_and_swap）；③信号量——P/V操作；④管程——高级同步原语。

{{知识点13}}{{信号量机制如何解决同步与互斥问题？}}
信号量是一个整型变量，只能通过两个原子操作P（wait/down）和V（signal/up）访问。P操作：S--，若S<0则阻塞当前进程；V操作：S++，若S≤0则唤醒一个阻塞进程。互斥问题：初始化mutex=1，临界区前P(mutex)，临界区后V(mutex)。同步问题：初始化S=0，先执行的代码段后V(S)，后执行的代码段前P(S)。经典应用：生产者-消费者问题、读者-写者问题、哲学家进餐问题。

{{知识点14}}{{生产者-消费者问题的标准解法是什么？}}
需要三个信号量：mutex=1（互斥访问缓冲区）、empty=N（空缓冲区数）、full=0（满缓冲区数）。生产者：P(empty)→P(mutex)→放入产品→V(mutex)→V(full)。消费者：P(full)→P(mutex)→取出产品→V(mutex)→V(empty)。注意P(empty/full)必须在P(mutex)之前，否则可能死锁（生产者在缓冲区满时持有mutex等待empty，消费者无法获取mutex）。

{{知识点15}}{{死锁的四个必要条件是什么？}}
①互斥条件——资源一次只能被一个进程使用；②请求与保持条件——进程已持有至少一个资源，又请求新资源，请求被阻塞时保持已有资源不释放；③不可抢占条件——已分配的资源不能被强制抢占，只能由持有者主动释放；④循环等待条件——存在一个进程等待的循环链，每个进程等待下一个进程持有的资源。四个条件缺一不可，破坏任一条件即可预防死锁。

{{知识点16}}{{死锁的处理策略有哪些？}}
①死锁预防——破坏死锁四个必要条件之一（如：资源一次性分配破坏"请求与保持"；允许抢占破坏"不可抢占"；有序资源分配法破坏"循环等待"）；②死锁避免——在资源分配时动态检查是否安全（银行家算法）；③死锁检测与恢复——允许死锁发生，定期检测（资源分配图化简），发现后恢复（终止进程或资源抢占）；④死锁忽略——如鸵鸟策略，多数通用OS采用此法（如Windows、Linux），因为死锁发生的概率低。

{{知识点17}}{{银行家算法的工作原理是什么？}}
银行家算法是死锁避免算法，用于判断资源分配是否安全。核心数据结构：Available（可用资源向量）、Max（最大需求矩阵）、Allocation（已分配矩阵）、Need=Max-Allocation（还需要矩阵）。安全算法步骤：①初始化Work=Available，Finish[i]=false；②找一个满足Need[i]≤Work且Finish[i]=false的进程；③执行：Work+=Allocation[i]，Finish[i]=true；④若所有Finish=true则安全，否则不安全。安全序列可能有多条，只要存在一条即安全。

{{知识点18}}{{内存管理中连续分配方式有哪些？}}
①单一连续分配——内存分为系统区和用户区，用户区只分配给一个进程，无外部碎片，有内部碎片；②固定分区分配——用户区分为若干固定大小的分区，每个分区装入一个进程，存在内部碎片；③动态分区分配——按进程实际需要分配，无内部碎片但有外部碎片。动态分区分配算法：首次适应（FF）——从头找第一个够大的空闲区；最佳适应（BF）——找最小的能满足的空闲区（易产生小碎片）；最坏适应（WF）——找最大的空闲区（大空闲区很快用完）。

{{知识点19}}{{分页存储管理的基本原理是什么？}}
将进程的逻辑地址空间等分为若干页（Page），将物理内存等分为若干与页大小相同的页框/帧（Frame）。通过页表建立页到页框的映射。逻辑地址由页号和页内偏移组成：页号=逻辑地址/页大小，页内偏移=逻辑地址%页大小。物理地址=页框号×页大小+页内偏移。优点：无外部碎片，内存利用率高；缺点：有少量内部碎片（最后一页），需要页表开销。

{{知识点20}}{{快表（TLB）是什么？如何加速地址转换？}}
TLB（Translation Lookaside Buffer）是存储在MMU中的高速缓存，存储最近使用的页号到页框号的映射。地址转换过程：①先查TLB，命中则直接获得页框号（约1个时钟周期）；②TLB未命中则查内存中的页表，获得映射后存入TLB。有效访问时间EAT=TLB命中率×(TLB访问时间+内存访问时间)+(1-TLB命中率)×(TLB访问时间+2×内存访问时间)。TLB命中率通常>99%，可显著降低地址转换开销。多级页表也能减少页表占用的连续内存。

{{知识点21}}{{多级页表为什么必要？如何工作？}}
单级页表需要连续存储，对于32位系统（4KB页大小）需要约4MB连续内存存放页表（1M个页表项×4B），64位系统问题更严重。多级页表将页表再分页：逻辑地址分为多级页号+页内偏移，每级页表只占用一个页框，无需连续存储。如两级页表：一级页表→二级页表→页框。优势：①不需要连续大块内存；②未使用的虚拟地址空间不需要分配页表（节省内存）。代价：增加访存次数（每多一级多一次访存），但TLB缓解了这一问题。

{{知识点22}}{{页面置换算法有哪些？各有什么特点？}}
①OPT（最佳置换）——替换将来最长时间不被使用的页面，理论最优但无法实现；②FIFO（先进先出）——替换最先进入的页面，简单但可能产生Belady异常（增加页框数反而缺页率上升）；③LRU（最近最少使用）——替换最长时间未被访问的页面，性能接近OPT，需要硬件支持（计数器或栈），无Belady异常；④Clock（时钟算法）——FIFO改进，使用访问位，指针扫描，访问位为0则替换，为1则置0后继续扫描；⑤LFU（最不经常使用）——替换访问次数最少的页面。

{{知识点23}}{{什么是抖动（Thrashing）？如何解决？}}
抖动是指页面频繁调入调出，CPU利用率急剧下降的现象。原因：进程获得的页框数太少，不能容纳其工作集，导致不断缺页。解决方案：①工作集模型——跟踪进程的工作集大小（在时间窗口Δ内访问的页面集合），保证分配的页框数≥工作集大小；②页框分配策略——采用局部分配策略，限制每个进程的页框数下限；③交换调度——当检测到抖动时，挂起某些进程以释放页框；④增加物理内存。

{{知识点24}}{{什么是局部性原理？与虚拟内存的关系是什么？}}
局部性原理包括：①时间局部性——刚被访问的存储单元可能很快被再次访问（如循环变量）；②空间局部性——刚被访问的存储单元附近的单元可能很快被访问（如数组遍历）。虚拟内存正是基于局部性原理：程序运行时只将当前需要的页面装入内存，其余留在外存。由于局部性，缺页率可以控制在较低水平，使得"部分加载"成为可能，实现了内存的虚拟扩展——逻辑上可用内存远大于物理内存。

{{知识点25}}{{段式存储管理与页式存储管理的区别是什么？}}
①分段：按逻辑意义划分（代码段、数据段、栈段等），段长可变；分页：按物理大小划分，页大小固定。②分段面向用户/程序员，分页面向系统/硬件。③段有段名和段号，便于共享和保护；页无逻辑意义。④分段产生外部碎片（段间），分页产生内部碎片（页内）。⑤段表项：段号、段长、基址；页表项：页号、页框号。⑥段式共享方便（共享段号），页式共享复杂（需共享多个页面）。现代OS常用段页式：先分段再分页。

{{知识点26}}{{文件的逻辑结构有哪些？}}
①无结构文件（流式文件）——文件由一串字节流组成，Unix/Linux采用此方式，灵活简单；②有结构文件（记录式文件）——由一组记录组成。记录式文件又分为：顺序文件——记录按关键字排列，适合批量处理；索引文件——建立索引表，支持随机访问；索引顺序文件——将记录分组，组间顺序，组内索引，兼顾顺序和随机；哈希文件——通过哈希函数直接定位记录，查找速度最快。

{{知识点27}}{{文件的物理结构（磁盘分配方式）有哪些？}}
①连续分配——文件占据连续磁盘块，支持顺序和随机访问，速度快但有外部碎片且文件不能动态增长；②链接分配——文件的磁盘块通过指针链接，隐式链接（每块末尾存下一块地址）不支持随机访问，显式链接（FAT表）支持随机访问；③索引分配——为每个文件建立索引表，支持随机访问，单级索引受限于索引块大小，多级索引和混合索引（Unix inode采用）解决大文件问题。

{{知识点28}}{{Unix inode的混合索引结构是什么？}}
Unix/Linux的inode采用混合索引方式存储文件的磁盘块地址：①直接指针——12个，直接指向数据块，适合小文件（12×4KB=48KB）；②一级间接指针——1个，指向一个索引块，索引块中存储数据块地址（可存1024个地址，4MB）；③二级间接指针——1个，指向一个索引块，该索引块再指向索引块（4GB）；④三级间接指针——1个，可达4TB。这种设计使得小文件访问快（直接指针），大文件也能支持。

{{知识点29}}{{文件目录管理的作用和结构是什么？}}
文件目录是文件名到文件控制信息（FCB/inode）的映射。目录结构：①单级目录——所有文件在同一目录，不允许重名；②两级目录——主文件目录（MFD）+用户文件目录（UFD），不同用户可有同名文件；③树形目录——多级层次结构，路径名唯一标识文件（绝对路径/相对路径），Unix/Linux、Windows采用；④无环图目录——支持文件共享（多个目录指向同一文件），需要处理删除时的悬挂指针问题。

{{知识点30}}{{文件保护机制有哪些？}}
①访问控制表（ACL）——为每个文件维护一个列表，记录哪些用户/组有哪些权限（读/写/执行）；②用户权限位——Unix风格的owner/group/other各三位rwx权限（如chmod 755）；③口令——文件设置口令，访问时需验证；④加密——文件内容加密存储，读取时解密。访问矩阵是通用模型：行为主体（用户/进程），列为客体（文件/设备），矩阵元素为访问权限。实际中常用ACL（按列存储）或能力表（按行存储）来实现。

{{知识点31}}{{磁盘调度算法有哪些？}}
①FCFS——按请求到达顺序服务，公平但移动臂移动距离大；②SSTF（最短寻道时间优先）——选择离当前磁头最近的请求，平均寻道时间短但可能饥饿；③SCAN（电梯算法）——磁头单方向移动到尽头再反向，兼顾效率和公平；④C-SCAN（循环扫描）——只单方向服务，到达尽头后快速返回起点，更公平；⑤LOOK——SCAN改进，磁头不必到尽头，只需到该方向最远请求即可反向；⑥C-LOOK——C-SCAN的LOOK改进版。

{{知识点32}}{{I/O控制方式有哪些？各有什么特点？}}
①程序直接控制方式——CPU不断轮询I/O状态，CPU利用率极低；②中断驱动方式——I/O完成后发中断通知CPU，CPU利用率提高但每次传输以字/字节为单位；③DMA方式（直接存储器访问）——由DMA控制器控制数据在内存和设备间直接传输，以数据块为单位，CPU只需在传输开始和结束时干预；④通道方式——通道是专用处理器，可执行通道程序，实现CPU、通道、I/O设备三者并行工作，进一步解放CPU。

{{知识点33}}{{SPOOLing技术（假脱机技术）是什么？}}
SPOOLing（Simultaneous Peripheral Operations On-Line）将独占设备改造为共享设备的技术。组成：①输入井——模拟输入设备，暂存输入数据；②输出井——模拟输出设备，暂存输出数据；③输入缓冲区和输出缓冲区；④输入进程和输出进程。典型应用：打印机SPOOLing——多个进程的打印请求先输出到磁盘输出井，打印守护进程按序输出到打印机。效果：每个进程认为自己独占打印机，实际是共享的，提高了设备利用率和系统效率。

{{知识点34}}{{缓冲区的作用和类型有哪些？}}
缓冲区的作用：①缓和CPU与I/O设备速度不匹配的矛盾；②减少对CPU的中断频率；③解决数据粒度不匹配问题；④提高CPU和I/O设备的并行性。类型：①单缓冲——一个缓冲区，CPU和设备交替使用；②双缓冲——两个缓冲区，CPU和设备可并行工作（生产者-消费者模式）；③循环缓冲——多个缓冲区组成环形队列；④缓冲池——系统公用缓冲区，包含空缓冲队列、输入队列、输出队列，通过getbuf和releasebuf操作管理。

{{知识点35}}{{什么是虚拟设备？与物理设备的区别？}}
虚拟设备是通过SPOOLing等技术，将物理独占设备（如打印机）模拟为逻辑共享设备。每个进程认为自己独占设备，实际上通过缓冲和调度共享物理设备。虚拟设备的好处：①提高设备利用率；②提高系统并发度；③方便用户使用。设备独立性（设备无关性）是另一概念：用户程序使用逻辑设备名，系统负责将逻辑设备映射到物理设备，使程序不受具体物理设备变化的影响。

{{知识点36}}{{磁盘的物理结构和性能参数有哪些？}}
磁盘由多个盘片组成，每个盘片有两个磁头面。磁道（Track）——盘面上的同心圆；扇区（Sector）——磁道的弧段，是磁盘读写的最小单位；柱面（Cylinder）——所有盘面上同一位置磁道的集合。性能参数：①寻道时间——磁头移动到目标磁道的时间（最重要，约占总时间70%）；②旋转延迟——等待目标扇区旋转到磁头下的时间（平均为旋转半圈的时间）；③数据传输时间——读写数据的时间。磁盘访问时间=寻道时间+旋转延迟+传输时间。

{{知识点37}}{{RAID（磁盘冗余阵列）的各级别有什么特点？}}
RAID 0——条带化，无冗余，性能最高但无容错；RAID 1——镜像，数据完全复制，容错最好但成本高；RAID 2——位级条带化+海明码纠错（已过时）；RAID 3——字节级条带化+专用奇偶校验盘；RAID 4——块级条带化+专用奇偶校验盘（奇偶盘成为瓶颈）；RAID 5——块级条带化+分布式奇偶校验（最常用，兼顾性能和容错）；RAID 6——双分布式奇偶校验，可容忍两块盘同时故障。RAID 10（1+0）——先镜像再条带化，兼顾性能和可靠性。

{{知识点38}}{{操作系统中的用户态和内核态是什么？如何切换？}}
用户态——进程运行在用户空间，权限受限，不能直接访问硬件和内核数据；内核态——运行在内核空间，拥有全部权限。切换场景：①系统调用——用户程序主动请求内核服务（如read/write/fork），通过trap/软中断陷入内核；②异常——用户程序执行出错（如缺页异常、除零）；③外部中断——I/O完成、时钟中断等，被动切换到内核。切换过程：保存用户态上下文→切换到内核栈→执行内核代码→恢复上下文→返回用户态。现代CPU通过特权级（ring0-ring3）实现。

{{知识点39}}{{系统调用的过程是什么？为什么需要系统调用？}}
系统调用是用户程序请求操作系统内核服务的接口。需要系统调用的原因：用户态不能直接访问硬件和内核资源，必须通过内核代为完成。过程：①用户程序将参数放入指定寄存器/栈；②执行trap指令（如int 0x80或syscall），CPU从用户态切换到内核态；③内核根据系统调用号查系统调用表，跳转到对应处理函数；④内核执行服务，将结果写入指定位置；⑤执行iret/sysret指令，返回用户态。系统调用是操作系统对外提供的程序接口（API的核心）。

{{知识点40}}{{什么是虚拟内存？其实现方式有哪些？}}
虚拟内存是让程序认为自己拥有连续的、足够大的内存空间的技术，实际上物理内存可能远小于逻辑地址空间。实现方式：①请求分页——程序运行时按需调入页面，不在内存的页面留在外存（交换区），访问时产生缺页中断；②请求分段——按段调入调出；③请求段页式——结合两者。关键技术：①页表中的存在位标识页面是否在内存；②缺页中断处理——从外存调入页面，可能需要置换；③页面置换算法决定淘汰哪个页面。虚拟内存使得大程序能在小内存中运行。

{{知识点41}}{{微内核架构与宏内核架构的区别是什么？}}
宏内核（Monolithic Kernel）——将进程管理、内存管理、文件系统、设备驱动等所有功能都在内核中实现（如Linux）。优点：性能好，模块间直接调用；缺点：内核庞大，一个模块出错可能导致整个系统崩溃。微内核（Microkernel）——内核只保留最基本功能（进程通信、低级调度、中断处理），其他服务（文件系统、设备驱动）作为用户态进程运行（如Minix、QNX、macOS的Mach）。优点：模块化好，可靠性高，一个服务崩溃不影响系统；缺点：进程间通信频繁，性能略低。混合内核（Hybrid Kernel）结合两者优点（如Windows NT、macOS XNU）。

{{知识点42}}{{进程同步中的经典问题——哲学家进餐问题及解法？}}
五个哲学家围坐圆桌，每人左右各有一根筷子（共5根），需同时拿到左右两根才能进餐。问题：如何避免死锁和饥饿？解法：①限制同时进餐人数（最多4人同时尝试）；②奇数哲学家先拿左边，偶数先拿右边（破坏循环等待）；③使用一个互斥信号量限制同时拿起筷子的哲学家数量；④AND型信号量——同时申请两个资源（左右筷子），要么都获得要么都不获得。

{{知识点43}}{{什么是中断？中断的分类和处理过程？}}
中断是CPU暂停当前执行，转去处理紧急事件，处理完后返回继续执行的机制。分类：①硬件中断（外部中断）——由外部设备触发，分为可屏蔽中断（如I/O完成）和不可屏蔽中断（如电源故障）；②软件中断（异常/陷阱）——由CPU内部事件触发，分为故障（fault，如缺页）、自陷（trap，如系统调用）、终止（abort，如硬件故障）。处理过程：①关中断；②保存断点（PC和PSW）；③识别中断源；④保护现场；⑤执行中断服务程序；⑥恢复现场；⑦开中断；⑧返回断点继续执行。

{{知识点44}}{{内存管理中的伙伴系统是什么？}}
伙伴系统是一种内存分配算法，用于管理空闲物理内存块。原理：将内存按2的幂次大小划分，初始为一个大块（如1024页）。当请求n页时，找到最小的2^k≥n的空闲块，如果该块不存在则递归地将更大的块分裂为两个"伙伴"（大小相等的两半），直到满足需求。释放时，检查释放块的伙伴是否空闲，如果空闲则合并为更大的块，递归合并直到伙伴不空闲或达到最大块。优点：减少外部碎片，合并速度快。Linux内核使用伙伴系统管理物理页框。

{{知识点45}}{{文件系统中的日志技术（Journaling）是什么？}}
日志文件系统（如ext3/ext4/NTFS）在写入数据前，先将操作记录到日志区域（journal/log），操作完成后才标记日志为已完成。好处：①崩溃恢复——系统崩溃后只需重放日志中未完成的操作，无需扫描整个文件系统（传统文件系统如ext2需要fsck全盘扫描，耗时极长）；②数据一致性——保证元数据（甚至数据）的一致性。日志模式：journal（数据和元数据都写日志，最安全）、ordered（只记元数据日志，但保证数据先于元数据写入）、writeback（只记元数据日志，不保证顺序，性能最好）。

{{知识点46}}{{什么是设备独立性？如何实现？}}
设备独立性是指应用程序使用逻辑设备名访问设备，系统负责将逻辑设备映射到物理设备。好处：①程序与设备解耦，设备更换不影响程序；②提高设备利用率（一个逻辑设备可映射到多个物理设备）。实现：①逻辑设备表（LUT）——维护逻辑设备名到物理设备名和驱动程序的映射；②设备驱动程序层——屏蔽不同硬件差异，提供统一接口；③I/O子系统——统一管理设备的分配、回收和访问控制。现代OS通过设备文件（/dev/xxx）实现设备独立性。

{{知识点47}}{{什么是工作集模型？如何防止抖动？}}
工作集是进程在时间窗口（Δ）内访问的页面集合，记为WS(t,Δ)。工作集模型用于预防抖动：①系统跟踪每个进程的工作集大小；②保证进程的页框分配≥工作集大小；③如果所有进程的工作集总和>物理页框数，说明内存不足，需挂起某些进程。操作系统通过"驻留集"管理分配给每个进程的页框数。策略：①固定分配——进程获得固定数量的页框；②可变分配——根据缺页率动态调整页框数（缺页率高则增加，低则减少）。配合局部置换策略效果最佳。

{{知识点48}}{{操作系统的安全机制有哪些？}}
①身份认证——验证用户身份（口令、生物识别、多因素认证）；②访问控制——基于ACL或角色的访问控制（RBAC），限制用户对资源的访问权限；③内存保护——通过基址/界限寄存器或MMU的页表权限位防止进程越界访问；④加密——文件加密、磁盘加密（BitLocker/LUKS）保护数据机密性；⑤审计日志——记录系统操作，用于事后追溯；⑥特权分离——最小权限原则，用户态/内核态隔离；⑦地址空间布局随机化（ASLR）——防止缓冲区溢出攻击。

{{知识点49}}{{现代操作系统的新特性——容器和虚拟化技术？}}
容器技术（如Docker）：基于OS级虚拟化，多个容器共享同一内核，通过命名空间（namespace）实现资源隔离（PID/网络/文件系统等），通过控制组（cgroup）限制资源使用。优势：启动快、开销小、密度高。虚拟化技术（如VMware/KVM/Xen）：通过Hypervisor在硬件上运行多个完整虚拟机，每个VM有独立内核。类型1（裸金属型）直接运行在硬件上，类型2（宿主型）运行在宿主OS上。两者区别：容器轻量但隔离性弱，VM重量但隔离性强。Kubernetes是容器编排的事实标准。

{{知识点50}}{{实时操作系统（RTOS）有什么特点？}}
实时操作系统强调任务的时间确定性。分类：①硬实时——必须在严格截止时间内完成（如飞行控制、医疗设备），错过截止时间就是系统失败；②软实时——偶尔错过截止时间可接受（如视频播放、在线游戏）。RTOS特点：①确定性——可预测的响应时间；②可抢占式调度——高优先级任务可立即抢占低优先级任务；③优先级驱动调度——如速率单调调度（RMS，静态优先级）和最早截止时间优先（EDF，动态优先级）；④快速中断响应。常见RTOS：VxWorks、FreeRTOS、RT-Linux。
