# 算法导论精要
# 领域: 计算机IT
# 学科: 算法与数据结构
# 来源: 算法导论精要（2025版）
# 描述: 经典算法与数据结构核心知识，涵盖排序、搜索、图算法、动态规划及算法设计思想

{{算法复杂度分析}}{{时间复杂度衡量算法执行时间随输入规模增长的趋势。常见复杂度：O(1)常数 < O(log n)对数 < O(n)线性 < O(n log n)线性对数 < O(n²)平方 < O(2ⁿ)指数。大O表示法描述上界。空间复杂度衡量额外使用的内存。摊还分析：分摊单次操作的平均代价。最好/最坏/平均情况分析。主定理(Master Theorem)分析递归复杂度。}}

{{数组与链表}}{{数组：连续内存，O(1)随机访问，O(n)插入删除。动态数组(如C++ vector)自动扩容(通常2倍)。链表：非连续内存，O(1)插入删除(已知位置)，O(n)查找。单链表、双链表、循环链表。链表操作：反转、合并、检测环(快慢指针)、找中间节点。数组和链表的选择取决于访问模式。}}

{{栈与队列}}{{栈(Stack)：后进先出(LIFO)，push/pop/peek操作，O(1)。应用：函数调用栈、表达式求值、括号匹配、DFS。队列(Queue)：先进先出(FIFO)，enqueue/dequeue操作，O(1)。应用：BFS、任务调度、缓冲区。双端队列(Deque)：两端都可操作。优先队列(Priority Queue)：基于堆实现，O(log n)插入和删除最大/最小元素。}}

{{哈希表}}{{哈希表通过哈希函数将键映射到数组索引，实现O(1)平均查找/插入/删除。冲突解决：链地址法(拉链法)、开放寻址法(线性探测/二次探测/双重哈希)。负载因子=元素数/桶数，超过阈值需要扩容(再哈希)。好的哈希函数：均匀分布、计算快速。应用：字典、缓存、去重、频率统计。布隆过滤器(Bloom Filter)空间效率更高的概率数据结构。}}

{{树结构}}{{二叉树：每个节点最多两个子节点。遍历：前序(根左右)、中序(左根右)、后序(左右根)、层序(BFS)。二叉搜索树(BST)：左<根<右，O(log n)平均查找。AVL树：严格平衡(高度差≤1)，旋转操作。红黑树：近似平衡，工程中广泛使用(如Java TreeMap)。B树/B+树：多路平衡树，数据库索引核心结构。Trie(字典树)：字符串前缀匹配。}}

{{堆(Heap)}}{{二叉堆：完全二叉树实现的优先队列。最大堆(父≥子)和最小堆(父≤子)。数组存储：parent=i/2, left=2i, right=2i+1。插入：上浮(sift-up)，O(log n)。删除堆顶：下沉(sift-down)，O(log n)。建堆：O(n)。堆排序：O(n log n)，原地但不稳定。应用：Top-K问题、中位数维护(双堆)、合并K个有序列表。斐波那契堆：理论更优的摊还复杂度。}}

{{排序算法}}{{比较排序：冒泡O(n²)、选择O(n²)、插入O(n²)、归并O(n log n)稳定、快速O(n log n)平均(不稳定)、堆排序O(n log n)原地。快排核心：分区(partition)，选pivot将数组分为两部分。优化：三数取中、随机化、小数组切换插入排序。非比较排序：计数排序O(n+k)、基数排序O(d(n+k))、桶排序O(n)。排序稳定性在多关键字排序中重要。}}

{{搜索算法}}{{线性搜索O(n)：逐个比较。二分搜索O(log n)：有序数组，比较中间元素缩小范围。变体：查找第一个/最后一个等于目标的位置、查找第一个大于等于目标的位置。跳跃搜索、插值搜索、指数搜索。哈希搜索O(1)平均。BST搜索O(log n)。深度优先搜索(DFS)：递归/栈。广度优先搜索(BFS)：队列，最短路径(无权图)。}}

{{图的基本概念}}{{图G=(V,E)，顶点集和边集。有向图vs无向图。加权图vs无权图。度(degree)：入度+出度。存储方式：邻接矩阵(空间O(V²))、邻接表(空间O(V+E))。连通性：连通分量、强连通分量(SCC)。拓扑排序：DAG的线性排列(Kahn算法/DFS)。图的遍历：DFS(递归/栈)、BFS(队列)。二分图判定(染色法)。}}

{{最短路径算法}}{{Dijkstra算法：非负权图单源最短路径，贪心+优先队列，O((V+E)log V)。Bellman-Ford：支持负权边，O(VE)，可检测负权环。Floyd-Warshall：所有顶点对最短路径，动态规划O(V³)。SPFA：Bellman-Ford的队列优化。BFS：无权图最短路径。DAG上最短路径：拓扑排序+松弛。A*搜索：启发式搜索，带估价函数。}}

{{最小生成树}}{{最小生成树(MST)：连通加权无向图中权值和最小的生成树。Kruskal算法：贪心，按边权排序，依次选择不形成环的边(并查集判断)，O(E log E)。Prim算法：从一个顶点开始，每次选择连接已选集合和未选集合的最小权边(优先队列优化)，O(E log V)。切分定理：切分的最小权横跨边属于MST。Borůvka算法。}}

{{动态规划}}{{动态规划(DP)适用于最优子结构和重叠子问题。步骤：定义状态→状态转移方程→初始条件→计算顺序→最终答案。经典问题：斐波那契数列、爬楼梯、背包问题(0-1/完全/多重)、最长公共子序列(LCS)、最长递增子序列(LIS)、编辑距离、矩阵链乘法、区间DP。记忆化搜索(自顶向下)vs制表法(自底向上)。状态压缩DP。}}

{{贪心算法}}{{贪心策略：每步选择局部最优，期望得到全局最优。适用条件：贪心选择性质+最优子结构。经典问题：活动选择问题、分数背包、Huffman编码、Dijkstra最短路径、Kruskal/Prim最小生成树、区间调度问题。贪心与DP的区别：贪心不回溯，DP考虑所有子问题。正确性证明：交换论证、贪心 stays ahead。}}

{{回溯算法}}{{回溯法系统地搜索解空间，遇到不满足条件的分支则回退。模板：选择→约束检查→递归→撤销选择。经典问题：N皇后、数独求解、全排列、组合总和、子集生成、图着色。剪枝优化：可行性剪枝、最优性剪枝。时间复杂度通常为指数级，但剪枝可大幅减少搜索空间。与DFS的关系：回溯是DFS+剪枝。}}

{{分治算法}}{{分治策略：分解(Divide)→解决(Conquer)→合并(Combine)。经典应用：归并排序、快速排序、二分搜索、大整数乘法(Karatsuba)、最近点对、Strassen矩阵乘法。主定理(Master Theorem)：T(n) = aT(n/b) + f(n)，根据f(n)与n^(log_b a)的关系得出复杂度。分治适合子问题独立的场景。}}

{{字符串算法}}{{字符串匹配：暴力O(nm)、KMP(O(n+m)，利用前缀函数避免重复比较)、Rabin-Karp(哈希匹配)、Boyer-Moore(坏字符+好后缀规则)。后缀数组：所有后缀排序，用于最长重复子串、最长公共子串。Trie(前缀树)：高效前缀匹配。AC自动机：多模式匹配。Manacher算法：最长回文子串O(n)。Z算法。}}

{{位运算技巧}}{{位运算基础：AND(&)、OR(|)、XOR(^)、NOT(~)、左移(<<)、右移(>>)。技巧：n & (n-1)消除最低位1、n & (-n)获取最低位1、判断奇偶(n&1)、交换两数(XOR)。位掩码：集合操作(并集|、交集&、补~)。状态压缩DP用整数表示子集。Brian Kernighan计数1的个数。常用：判断2的幂、不用临时变量交换。}}

{{NP完全理论}}{{P类问题：多项式时间可解。NP类问题：多项式时间可验证。NP完全(NPC)：NP中最难的问题，所有NP问题可多项式归约到它。经典NPC问题：旅行商问题(TSP)、图着色、背包问题(整数解)、子集和、SAT。NP-hard：至少和NPC一样难(不要求属于NP)。近似算法：为NP-hard问题提供近似解。启发式算法。}}

{{高级数据结构}}{{并查集(Union-Find)：集合合并和查询，路径压缩+按秩合并，近O(1)。线段树(Segment Tree)：区间查询和修改，O(log n)。树状数组(Binary Indexed Tree/Fenwick Tree)：前缀和查询和单点修改，O(log n)。跳表(Skip List)：概率平衡的有序数据结构。红黑树、AVL树的工程实现。持久化数据结构。}}
