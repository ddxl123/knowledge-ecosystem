# 贪心算法


## 收集指南

> ⚠️ **严格范围限定：** 仅收集本章（第5章 贪心）相关内容，禁止跨章节引用。

### 条目描述
本文件是《2025算法导论精要》中关于「贪心」的知识原子文件，属于算法方向。

### 知识结构
- 贪心基本思想
- 经典贪心问题
- 区间调度
- 哈夫曼编码
- 贪心与 DP 对比

### 待收集原子知识点
- 贪心选择性质
- 活动选择/区间合并
- 分糖果/任务分配
- 最优编码
- 贪心正确性证明

## 核心知识点

### 一、贪心基本思想

- **贪心算法特点：**
  - 每一步选择当前最优解（局部最优）
  - 希望局部最优能导致全局最优
  - 不回溯，效率高

- **适用条件：**
  - 贪心选择性质：通过局部最优选择能达到全局最优
  - 最优子结构：问题的最优解包含子问题的最优解

- **与动态规划的区别：**
  - 贪心：每步局部最优，不考虑子问题
  - DP：考虑所有子问题，选择最优组合

### 二、区间调度问题

```python
# 活动选择：选择最多的不重叠活动
def activity_selection(activities):
    # 按结束时间排序
    activities.sort(key=lambda x: x[1])
    selected = [activities[0]]
    for act in activities[1:]:
        if act[0] >= selected[-1][1]:  # 开始时间 >= 上一个结束时间
            selected.append(act)
    return selected

# 区间合并
def merge_intervals(intervals):
    intervals.sort(key=lambda x: x[0])
    merged = [intervals[0]]
    for start, end in intervals[1:]:
        if start <= merged[-1][1]:
            merged[-1][1] = max(merged[-1][1], end)
        else:
            merged.append([start, end])
    return merged

# 用最少的点覆盖所有区间
def min_points_to_cover(intervals):
    intervals.sort(key=lambda x: x[1])
    points = [intervals[0][1]]
    for start, end in intervals[1:]:
        if start > points[-1]:
            points.append(end)
    return points
```

### 三、经典贪心问题

```python
# 分糖果：每个孩子有胃口值，每个糖果有大小
def find_content_children(g, s):
    g.sort()  # 胃口值排序
    s.sort()  # 糖果大小排序
    child = cookie = 0
    while child < len(g) and cookie < len(s):
        if s[cookie] >= g[child]:
            child += 1
        cookie += 1
    return child

# 任务调度（冷却时间 n）
def least_interval(tasks, n):
    freq = Counter(tasks)
    max_freq = max(freq.values())
    max_count = sum(1 for v in freq.values() if v == max_freq)
    return max(len(tasks), (max_freq - 1) * (n + 1) + max_count)

# 跳跃游戏
def can_jump(nums):
    max_reach = 0
    for i, jump in enumerate(nums):
        if i > max_reach:
            return False
        max_reach = max(max_reach, i + jump)
    return True

# 加油站问题
def can_complete_circuit(gas, cost):
    if sum(gas) < sum(cost):
        return -1
    start = tank = 0
    for i in range(len(gas)):
        tank += gas[i] - cost[i]
        if tank < 0:
            start = i + 1
            tank = 0
    return start
```

### 四、哈夫曼编码

```python
import heapq
from collections import Counter

class HuffmanNode:
    def __init__(self, char=None, freq=0, left=None, right=None):
        self.char = char
        self.freq = freq
        self.left = left
        self.right = right
    
    def __lt__(self, other):
        return self.freq < other.freq

def huffman_encoding(text):
    freq = Counter(text)
    
    # 构建最小堆
    heap = [HuffmanNode(char, f) for char, f in freq.items()]
    heapq.heapify(heap)
    
    # 构建哈夫曼树
    while len(heap) > 1:
        left = heapq.heappop(heap)
        right = heapq.heappop(heap)
        parent = HuffmanNode(freq=left.freq + right.freq, left=left, right=right)
        heapq.heappush(heap, parent)
    
    # 生成编码表
    codes = {}
    def build_codes(node, code=""):
        if node.char:
            codes[node.char] = code or "0"
        else:
            build_codes(node.left, code + "0")
            build_codes(node.right, code + "1")
    build_codes(heap[0])
    
    return codes
```

### 五、贪心正确性证明

- **交换论证：** 假设存在更优解，通过交换证明贪心解不差于它
- **归纳证明：** 证明每步贪心选择不改变最优性
- **反证法：** 假设贪心不是最优，推导矛盾

- **贪心不适用的场景：**
  - 背包问题（物品不可分割时贪心可能不是最优）
  - 旅行商问题
  - 需要考虑所有组合的问题
