# 搜索算法

## 二分查找
- 前提：有序数组
- 时间复杂度：O(log n)
- 模板：left=0, right=n-1, while left<=right, mid=(left+right)/2
- 变体：查找第一个/最后一个等于目标的位置
- 浮点二分：精度控制 while right-left > eps

## 哈希表
- 哈希函数：将键映射到数组下标
- 冲突解决：链地址法、开放寻址法
- 负载因子：元素数/桶数，超过阈值需要扩容
- 平均O(1)查找/插入/删除
- 应用：去重、计数、缓存、两数之和

## 树搜索
- 二叉搜索树(BST)：左<根<右，O(log n)平均
- BST退化：有序插入导致链表，O(n)
- 平衡二叉树：AVL树、红黑树，保证O(log n)
- B树/B+树：多路搜索树，数据库索引使用
- 前缀树(Trie)：字符串搜索，自动补全

## 图搜索
- BFS广度优先：队列实现，找最短路径（无权图）
- DFS深度优先：栈/递归实现，探索所有路径
- BFS应用：层序遍历、最短路径、社交网络距离
- DFS应用：连通分量、拓扑排序、回溯
- 双向BFS：两端同时搜索，加速查找

## 高级搜索
- A*算法：启发式搜索，f(n)=g(n)+h(n)
- 二分答案：将判定问题转化为搜索问题
- 搜索剪枝：提前排除不可能的分支
- 记忆化搜索：缓存已计算结果，避免重复
- 迭代加深DFS：限制搜索深度逐步增加
