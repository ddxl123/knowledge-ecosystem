# 排序算法


## 收集指南

> ⚠️ **严格范围限定：** 仅收集本章（第1章 排序）相关内容，禁止跨章节引用。

### 条目描述
本文件是《2025算法导论精要》中关于「排序」的知识原子文件，属于算法方向。

### 知识结构
- 比较排序
- 非比较排序
- 排序算法比较
- 排序稳定性
- 实际应用选择

### 待收集原子知识点
- 冒泡/选择/插入排序
- 快速/归并/堆排序
- 计数/桶/基数排序
- 时间空间复杂度
- 排序算法选择策略

## 核心知识点

### 一、基础比较排序 O(n²)

```python
# 冒泡排序：相邻比较交换，每轮将最大值冒泡到末尾
def bubble_sort(arr):
    n = len(arr)
    for i in range(n - 1):
        swapped = False
        for j in range(n - 1 - i):
            if arr[j] > arr[j + 1]:
                arr[j], arr[j + 1] = arr[j + 1], arr[j]
                swapped = True
        if not swapped: break  # 优化：无交换则已有序
    return arr

# 选择排序：每轮选择最小值放到已排序末尾
def selection_sort(arr):
    n = len(arr)
    for i in range(n):
        min_idx = i
        for j in range(i + 1, n):
            if arr[j] < arr[min_idx]:
                min_idx = j
        arr[i], arr[min_idx] = arr[min_idx], arr[i]
    return arr

# 插入排序：将每个元素插入到已排序部分的正确位置
def insertion_sort(arr):
    for i in range(1, len(arr)):
        key = arr[i]
        j = i - 1
        while j >= 0 and arr[j] > key:
            arr[j + 1] = arr[j]
            j -= 1
        arr[j + 1] = key
    return arr
```

### 二、高效比较排序 O(n log n)

```python
# 快速排序：选基准，分区，递归
def quick_sort(arr):
    if len(arr) <= 1:
        return arr
    pivot = arr[len(arr) // 2]
    left = [x for x in arr if x < pivot]
    middle = [x for x in arr if x == pivot]
    right = [x for x in arr if x > pivot]
    return quick_sort(left) + middle + quick_sort(right)

# 原地快排
def quick_sort_inplace(arr, low, high):
    if low < high:
        pi = partition(arr, low, high)
        quick_sort_inplace(arr, low, pi - 1)
        quick_sort_inplace(arr, pi + 1, high)

def partition(arr, low, high):
    pivot = arr[high]
    i = low - 1
    for j in range(low, high):
        if arr[j] <= pivot:
            i += 1
            arr[i], arr[j] = arr[j], arr[i]
    arr[i + 1], arr[high] = arr[high], arr[i + 1]
    return i + 1

# 归并排序：分治，合并两个有序数组
def merge_sort(arr):
    if len(arr) <= 1:
        return arr
    mid = len(arr) // 2
    left = merge_sort(arr[:mid])
    right = merge_sort(arr[mid:])
    return merge(left, right)

def merge(left, right):
    result = []
    i = j = 0
    while i < len(left) and j < len(right):
        if left[i] <= right[j]:
            result.append(left[i]); i += 1
        else:
            result.append(right[j]); j += 1
    result.extend(left[i:])
    result.extend(right[j:])
    return result

# 堆排序
def heap_sort(arr):
    n = len(arr)
    # 建最大堆
    for i in range(n // 2 - 1, -1, -1):
        heapify(arr, n, i)
    # 逐个提取最大值
    for i in range(n - 1, 0, -1):
        arr[0], arr[i] = arr[i], arr[0]
        heapify(arr, i, 0)
    return arr
```

### 三、非比较排序 O(n)

```python
# 计数排序：统计每个值出现次数
def counting_sort(arr):
    if not arr: return arr
    max_val = max(arr)
    count = [0] * (max_val + 1)
    for num in arr:
        count[num] += 1
    result = []
    for i, c in enumerate(count):
        result.extend([i] * c)
    return result

# 基数排序：按位排序（从低位到高位）
def radix_sort(arr):
    max_val = max(arr)
    exp = 1
    while max_val // exp > 0:
        arr = counting_sort_by_digit(arr, exp)
        exp *= 10
    return arr
```

### 四、排序算法比较

| 算法 | 平均时间 | 最坏时间 | 空间 | 稳定性 |
|------|---------|---------|------|--------|
| 冒泡排序 | O(n²) | O(n²) | O(1) | 稳定 |
| 选择排序 | O(n²) | O(n²) | O(1) | 不稳定 |
| 插入排序 | O(n²) | O(n²) | O(1) | 稳定 |
| 快速排序 | O(n log n) | O(n²) | O(log n) | 不稳定 |
| 归并排序 | O(n log n) | O(n log n) | O(n) | 稳定 |
| 堆排序 | O(n log n) | O(n log n) | O(1) | 不稳定 |
| 计数排序 | O(n+k) | O(n+k) | O(k) | 稳定 |
| 基数排序 | O(d·n) | O(d·n) | O(n+k) | 稳定 |

### 五、排序算法选择策略

- **小数据（n < 50）：** 插入排序（常数因子小）
- **一般场景：** 快速排序（平均最优）
- **需要稳定性：** 归并排序
- **内存受限：** 堆排序（原地排序）
- **整数且范围小：** 计数排序
- **整数且位数少：** 基数排序
- **几乎有序：** 插入排序（O(n)）
- **Python 内置：** Timsort（归并+插入的混合，稳定 O(n log n)）
