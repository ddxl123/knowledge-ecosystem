# 排序算法

## 比较排序
- 冒泡排序：O(n²)，相邻比较交换，稳定
- 选择排序：O(n²)，每轮选最小放前面，不稳定
- 插入排序：O(n²)，逐个插入已排序部分，稳定，小数据量高效
- 归并排序：O(n log n)，分治法，稳定，需额外空间O(n)
- 快速排序：O(n log n)平均，O(n²)最坏，不稳定，原地排序

## 堆排序与计数排序
- 堆排序：O(n log n)，不稳定，原地排序
- 建堆：自底向上，O(n)
- 最大堆/最小堆：父节点大于/小于子节点
- 计数排序：O(n+k)，非比较排序，稳定，适合小范围整数
- 基数排序：O(d(n+k))，按位排序，稳定
- 桶排序：O(n)平均，将数据分到桶中分别排序

## 排序算法比较
- 时间复杂度：快排/归并/堆排 O(n log n) 优于 冒泡/选择/插入 O(n²)
- 空间复杂度：快排O(log n)、归并O(n)、堆排O(1)
- 稳定性：归并、插入、冒泡、计数排序稳定；快排、堆排、选择排序不稳定
- 选择原则：小数据量用插入排序，通用用快排，需要稳定用归并
- 内置排序：大多数语言使用混合排序（TimSort = 归并+插入）

## 快速排序详解
- 分区：选pivot，小的放左边，大的放右边
- pivot选择：首元素、尾元素、随机、三数取中
- 最坏情况：已排序数组，退化为O(n²)
- 优化：随机化、三路快排（处理大量重复元素）
- 应用最广泛的排序算法

## 排序应用
- Top K问题：最小堆/快速选择算法，O(n)
- 第K大元素：快速选择算法，O(n)平均
- 合并有序数组：双指针，O(n+m)
- 逆序对计数：归并排序变体
- 自定义排序：比较函数/Comparator
