# 动态规划

## DP基础
- 最优子结构：问题的最优解包含子问题的最优解
- 重叠子问题：子问题被重复计算
- 状态定义：dp[i]表示什么
- 状态转移方程：dp[i]与dp[i-1]等的关系
- 初始条件和边界

## 经典DP问题
- 斐波那契数列：dp[i] = dp[i-1] + dp[i-2]
- 爬楼梯：dp[i] = dp[i-1] + dp[i-2]
- 最长公共子序列(LCS)：dp[i][j] = dp[i-1][j-1]+1 或 max(dp[i-1][j],dp[i][j-1])
- 最长递增子序列(LIS)：O(n log n)二分优化
- 编辑距离：dp[i][j]表示word1前i个到word2前j个的最少操作

## 背包问题
- 0-1背包：每个物品只能选一次，dp[i][w] = max(dp[i-1][w], dp[i-1][w-wi]+vi)
- 完全背包：每个物品可选无限次，dp[j] = max(dp[j], dp[j-wi]+vi)
- 多重背包：每个物品有限个
- 空间优化：滚动数组，一维dp
- 背包变体：恰好装满、方案计数

## 区间DP
- 定义：dp[i][j]表示区间[i,j]的最优解
- 枚举分割点：dp[i][j] = min/max(dp[i][k] + dp[k+1][j] + cost)
- 经典问题：矩阵链乘法、石子合并、戳气球
- 遍历顺序：先短区间后长区间
- 时间复杂度：通常O(n³)

## DP优化技巧
- 状态压缩DP：用位运算表示状态集合
- 树形DP：在树结构上的动态规划
- 数位DP：按位处理数字的DP
- 斜率优化/单调队列优化：降低时间复杂度
- 记忆化搜索：自顶向下的递归+缓存
