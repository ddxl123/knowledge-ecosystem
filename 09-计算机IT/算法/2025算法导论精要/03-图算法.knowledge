# 图算法

## 图的表示
- 邻接矩阵：二维数组，O(1)查询边，空间O(V²)
- 邻接表：数组+链表/列表，空间O(V+E)
- 边列表：存储所有边，适合稀疏图
- 有向图vs无向图：无向图是特殊的有向图
- 加权图vs无权图：边上带权重

## 最短路径
- Dijkstra算法：单源最短路径，非负权图，O((V+E)logV)
- Bellman-Ford：单源最短路径，支持负权边，O(VE)
- Floyd-Warshall：全源最短路径，动态规划，O(V³)
- SPFA：Bellman-Ford队列优化，平均O(E)
- DAG最短路径：拓扑排序+松弛

## 最小生成树
- Kruskal算法：按边权排序，依次加入不构成环的边，并查集判环
- Prim算法：从任意顶点开始，每次加入最小权边
- 时间复杂度：Kruskal O(E log E)，Prim O(E log V)
- 应用：网络布线、聚类、近似算法
- 唯一性：边权不同时唯一

## 拓扑排序
- 有向无环图(DAG)的线性排序
- Kahn算法：入度为0的节点入队
- DFS方法：后序遍历的逆序
- 应用：任务调度、课程安排、编译依赖
- 环检测：拓扑排序无法完成说明有环

## 并查集
- 操作：Find（查找根节点）、Union（合并集合）
- 路径压缩：Find时将节点直接连到根
- 按秩合并：将矮树合并到高树下
- 时间复杂度：近似O(α(n))，α为反阿克曼函数
- 应用：连通分量、Kruskal算法判环
