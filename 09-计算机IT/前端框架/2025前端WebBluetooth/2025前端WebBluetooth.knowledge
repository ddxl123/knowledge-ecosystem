# 2025前端WebBluetooth
# 领域: 计算机IT
# 学科: 前端框架
{{知识点1}}{{Web Bluetooth API 概述：Web Bluetooth API 是一组允许网页通过 JavaScript 与低功耗蓝牙（BLE）设备进行通信的 Web 标准接口。它基于 GATT（Generic Attribute Profile）协议，使 Web 应用能够发现、连接并与蓝牙外设交换数据。该 API 由 Web Bluetooth Community Group 维护，目前仍属于实验性技术（Experimental）。}}
{{知识点2}}{{navigator.bluetooth 入口：Web Bluetooth API 通过 navigator.bluetooth 属性暴露 Bluetooth 对象，这是所有蓝牙操作的入口点。该属性仅在安全上下文（HTTPS）中可用。调用方式为 const bluetooth = navigator.bluetooth;。}}
{{知识点3}}{{Bluetooth 接口：Bluetooth 接口提供查询蓝牙可用性和请求设备访问的方法。核心方法包括 requestDevice()（请求用户选择蓝牙设备）、getDevices()（获取已授权设备列表）和 getAvailability()（查询蓝牙是否可用）。}}
{{知识点4}}{{requestDevice() 方法：Bluetooth.requestDevice(options) 是 Web Bluetooth API 最核心的方法，它触发浏览器的设备选择器对话框，让用户选择一个蓝牙设备。返回一个 Promise，解析为 BluetoothDevice 对象。需要用户交互（transient activation）才能调用。}}
{{知识点5}}{{requestDevice 的 filters 参数：filters 是一个数组，包含多个过滤器对象。每个过滤器可指定 services（GATT 服务 UUID 数组）、name（精确设备名称）、namePrefix（名称前缀）、manufacturerData（制造商数据）和 serviceData（服务数据）。设备必须匹配过滤器中所有指定的属性才会出现在选择器中。}}
{{知识点6}}{{optionalServices 参数：optionalServices 是 requestDevice 的可选参数，指定设备可能支持的额外 GATT 服务 UUID。即使设备未广播这些服务，连接后仍可访问。这很重要，因为只能访问在 filters.services 或 optionalServices 中列出的服务。}}
{{知识点7}}{{acceptAllDevices 参数：当设置 acceptAllDevices: true 时，设备选择器将列出所有蓝牙设备而非仅匹配过滤器的设备。使用此选项时应省略 filters，且必须设置 optionalServices 以访问设备数据。适用于设备未广播足够信息用于过滤的场景。}}
{{知识点8}}{{exclusionFilters 参数：exclusionFilters 数组用于排除特定设备。其元素与 filters 使用相同的属性格式，匹配的设备将被排除在选择结果之外。这在 acceptAllDevices 为 true 时特别有用。}}
{{知识点9}}{{BluetoothDevice 接口：BluetoothDevice 表示一个蓝牙设备对象。核心只读属性包括 id（设备唯一标识字符串）、name（人类可读的设备名称）和 gatt（设备的 BluetoothRemoteGATTServer 引用）。它继承自 EventTarget。}}
{{知识点10}}{{BluetoothDevice.forget() 方法：forget() 方法允许网页主动撤销对已授权设备的访问权限。这是用户隐私保护的重要机制，调用后该 origin 将不再能访问该设备，直到用户重新授权。}}
{{知识点11}}{{BluetoothDevice.watchAdvertisements()：watchAdvertisements() 方法让页面开始监听设备的蓝牙广播包。返回一个 Promise。配合 advertisementreceived 事件使用，可以获取设备的广播数据、RSSI 信号强度等信息。}}
{{知识点12}}{{gattserverdisconnected 事件：当与蓝牙设备的 GATT 连接断开时，BluetoothDevice 会触发 gattserverdisconnected 事件。开发者应监听此事件以处理意外断连，通常需要实现重连逻辑。}}
{{知识点13}}{{BluetoothRemoteGATTServer 接口：BluetoothRemoteGATTServer 代表远程设备上的 GATT 服务器。核心属性 connected（布尔值，表示脚本环境是否已连接）和 device（对 BluetoothDevice 的引用）。提供了 connect()、disconnect()、getPrimaryService() 和 getPrimaryServices() 方法。}}
{{知识点14}}{{GATT Server connect() 方法：BluetoothRemoteGATTServer.connect() 方法建立与远程蓝牙设备的 GATT 连接。返回一个 Promise，解析为 BluetoothRemoteGATTServer 对象。这是在 requestDevice 获取设备后的下一步操作，连接成功后才能访问服务和特征。}}
{{知识点15}}{{GATT Server disconnect() 方法：BluetoothRemoteGATTServer.disconnect() 断开与设备的连接。注意 connected 属性在脚本断开时为 false，但用户代理可能仍保持物理连接。断开后可再次调用 connect() 重新连接。}}
{{知识点16}}{{getPrimaryService() 方法：BluetoothRemoteGATTServer.getPrimaryService(uuid) 返回一个 Promise，解析为指定 UUID 的 BluetoothRemoteGATTService 主服务对象。如果设备不支持该服务，则 Promise 被拒绝。}}
{{知识点17}}{{getPrimaryServices() 方法：BluetoothRemoteGATTServer.getPrimaryServices() 返回一个 Promise，解析为设备上所有主服务的数组。可以传入可选的 UUID 参数来过滤返回的服务列表。}}
{{知识点18}}{{BluetoothRemoteGATTService 接口：BluetoothRemoteGATTService 代表 GATT 服务器提供的一个服务。只读属性包括 device（所属 BluetoothDevice）、isPrimary（是否为主服务）和 uuid（服务 UUID 字符串）。提供 getCharacteristic() 和 getCharacteristics() 方法来访问特征。}}
{{知识点19}}{{getCharacteristic() 方法：BluetoothRemoteGATTService.getCharacteristic(uuid) 返回一个 Promise，解析为指定 UUID 的 BluetoothRemoteGATTCharacteristic 对象。如果服务中不包含该特征，则 Promise 被拒绝。}}
{{知识点20}}{{getCharacteristics() 方法：BluetoothRemoteGATTService.getCharacteristics() 返回一个 Promise，解析为该服务下所有特征的数组。可传入可选的 UUID 参数进行过滤。用于发现服务提供的所有可用特征。}}
{{知识点21}}{{BluetoothRemoteGATTCharacteristic 接口：BluetoothRemoteGATTCharacteristic 代表一个 GATT 特征，是提供外设服务详细信息的基本数据元素。只读属性包括 service（所属服务）、uuid（特征 UUID）、properties（特征属性对象）和 value（当前缓存的特征值，类型为 DataView）。}}
{{知识点22}}{{Characteristic readValue() 方法：readValue() 返回一个 Promise，解析为包含特征值副本的 DataView。读取成功后会同时更新 value 缓存属性。这是从蓝牙设备获取数据的主要方式之一。}}
{{知识点23}}{{Characteristic writeValue() 方法：writeValue(value) 将 ArrayBuffer 中的字节写入特征值，返回一个 Promise。根据特征的属性，可能使用有响应或无响应写入。还有更明确的 writeValueWithResponse() 和 writeValueWithoutResponse() 方法。}}
{{知识点24}}{{startNotifications() 方法：startNotifications() 订阅特征值变化通知，返回一个 Promise。订阅后，当特征值变化时会触发 characteristicvaluechanged 事件。这是实时接收蓝牙设备数据（如心率、温度）的关键方法。}}
{{知识点25}}{{stopNotifications() 方法：stopNotifications() 取消对特征值变化的通知订阅，返回一个 Promise。不再需要接收数据时应调用此方法以节省资源和电量。}}
{{知识点26}}{{characteristicvaluechanged 事件：当特征值通过通知或指示更新时，BluetoothRemoteGATTCharacteristic 触发此事件。通过 event.target.value 获取更新后的 DataView。这是实现实时数据流的核心事件机制。}}
{{知识点27}}{{BluetoothCharacteristicProperties 接口：该接口提供特征的属性信息。每个属性都是只读布尔值：read（可读）、write（可写且有响应）、writeWithoutResponse（可写且无响应）、notify（支持通知）、indicate（支持指示）、broadcast（支持广播）、authenticatedSignedWrites（支持签名写入）、reliableWrite（支持可靠写入）和 writableAuxiliaries（辅助描述符可写）。}}
{{知识点28}}{{BluetoothRemoteGATTDescriptor 接口：BluetoothRemoteGATTDescriptor 代表 GATT 描述符，提供关于特征值的附加信息。只读属性包括 characteristic（所属特征）、uuid（描述符 UUID）和 value（缓存值）。方法包括 readValue() 和 writeValue()。}}
{{知识点29}}{{GATT 信息模型层次结构：GATT 数据组织为层次结构：顶层是 GATT Server → Service（服务）→ Characteristic（特征）→ Descriptor（描述符）。每个服务和特征通过 UUID 标识。标准 UUID 如 heart_rate（0x180D）、battery_service（0x180F）等。}}
{{知识点30}}{{GATT Blocklist（阻断列表）：为保护安全，Web Bluetooth 规范维护了一个 GATT 阻断列表，列出不允许网页访问的敏感服务、特征和描述符。例如 HID（Human Interface Device）服务被阻断，防止网页记录键盘输入。}}
{{知识点31}}{{安全上下文要求：Web Bluetooth API 仅在安全上下文（HTTPS 或 localhost）中可用。在 HTTP 页面上调用会抛出 SecurityError。这是保护蓝牙通信不被中间人攻击的基本要求。}}
{{知识点32}}{{Permissions Policy 集成：Web Bluetooth 受 Permissions Policy 指令 bluetooth 控制。默认允许列表为 self，即仅允许同源嵌套帧访问，第三方内容默认被阻止。跨源访问需同时在 HTTP 头和 iframe 中指定允许的来源。}}
{{知识点33}}{{Permissions API 查询权限状态：可通过 navigator.permissions.query({ name: "bluetooth" }) 查询蓝牙权限状态，返回 granted（已授权）、denied（已拒绝）或 prompt（需用户确认）三种状态。用于在调用 requestDevice 前检查权限。}}
{{知识点34}}{{getDevices() 方法：Bluetooth.getDevices() 返回一个 Promise，解析为该 origin 已获授权的 BluetoothDevice 数组，包括已关机或超出范围的设备。不需要用户交互即可调用，适合在页面加载时恢复之前的设备连接。}}
{{知识点35}}{{BLE 角色与连接模式：蓝牙低功耗（BLE）定义了多种角色。Web Bluetooth API 中网页以 Central（中心）角色运行，连接到 Peripheral（外围）设备上的 GATT Server。还支持 Broadcaster（广播者）和 Observer（观察者）角色用于单向通信。}}
{{知识点36}}{{蓝牙设备标识符隐私：每个蓝牙设备有 48 位 MAC 地址（BD_ADDR）。为保护隐私，LE 设备可使用随机可解析或不可解析的私有地址（约每 15 分钟更换）。Web Bluetooth API 建议对不同脚本使用不同的设备 ID，防止跨站点跟踪用户。}}
{{知识点37}}{{典型连接流程：Web Bluetooth 的标准使用流程为：①navigator.bluetooth.requestDevice() 选择设备 → ②device.gatt.connect() 建立 GATT 连接 → ③server.getPrimaryService() 获取服务 → ④service.getCharacteristic() 获取特征 → ⑤characteristic.readValue() 读取或 startNotifications() 订阅数据。}}
{{知识点38}}{{心率监测器示例：典型的 Web Bluetooth 用例是连接心率监测器。通过过滤 heart_rate 服务发现设备，获取 heart_rate_measurement 特征并订阅通知，解析 DataView 获取心率值、能量消耗和 RR 间隔等数据。}}
{{知识点39}}{{DataView 数据解析：蓝牙特征值以 DataView 对象返回，需要手动解析二进制数据。使用 getUint8()、getUint16()、getInt16() 等方法按偏移量读取。注意字节序（littleEndian 参数），BLE 通常使用小端序。}}
{{知识点40}}{{浏览器兼容性：Web Bluetooth API 在 Chrome/Edge 56+、Opera 43+ 中支持较好。Safari 和 Firefox 的支持有限或需要手动启用。Android Chrome 支持良好，iOS 上支持有限。截至 2025 年，该 API 仍标记为实验性技术。}}
{{知识点41}}{{Web Bluetooth 与 WebUSB 的对比：Web Bluetooth 专注于低功耗蓝牙 GATT 通信，而 WebUSB 用于 USB 设备访问。两者都要求 HTTPS、用户授权和安全上下文。Web Bluetooth 更适合 IoT 和健康设备场景，WebUSB 适合硬件调试和开发工具。}}
{{知识点42}}{{制造商数据过滤：requestDevice 的 manufacturerData 过滤器通过 companyIdentifier（蓝牙规范中的制造商编号）匹配设备。可选的 dataPrefix 和 mask 参数用于精确匹配广播包中的制造商自定义数据字节。}}
{{知识点43}}{{服务数据过滤：serviceData 过滤器匹配 BLE 广播包中的服务数据字段。需要指定 service（服务 UUID），可选 dataPrefix 和 mask 进行字节级匹配。适用于识别包含特定服务数据的自定义设备。}}
{{知识点44}}{{错误处理：Web Bluetooth 可能抛出的异常包括 TypeError（无效参数组合）、NotFoundError（未找到匹配设备）、SecurityError（安全上下文不允许）和 NetworkError（连接或通信失败）。应使用 try-catch 或 Promise.catch() 进行妥善处理。}}
{{知识点45}}{{BluetoothUUID 辅助方法：BluetoothUUID 提供静态方法将服务/特征/描述符的名称或短 UUID 转换为完整的 128 位 UUID。如 BluetoothUUID.getService('heart_rate') 返回 '0000180d-0000-1000-8000-00805f9b34fb'。可在 requestDevice 的 services 参数中直接使用名称字符串。}}
{{知识点46}}{{电源管理与性能：BLE 设计用于低功耗，Web Bluetooth 利用此特性。但频繁的读写操作和持续的通知订阅会消耗设备电量。最佳实践包括：不需要时停止通知、减少不必要的轮询、及时断开不用的连接。}}
{{知识点47}}{{数据写入策略：写入特征值时，writeWithResponse 确保设备确认收到（可靠但较慢），writeWithoutResponse 不等待确认（更快但可能丢失）。应根据特征的 properties 选择合适的方式，某些特征只支持其中一种。}}
{{知识点48}}{{跨域与 iframe 限制：Web Bluetooth 默认仅允许同源访问。第三方 iframe 需要父页面通过 Permissions-Policy HTTP 头显式授权 bluetooth 权限。这防止了嵌入的恶意内容未经授权访问用户的蓝牙设备。}}
{{知识点49}}{{Content Security Policy 建议：规范建议使用 CSP（Content Security Policy）减少 XSS 攻击风险。即使用户信任某 origin，该服务器可能被入侵或存在 XSS 漏洞，导致恶意代码获得蓝牙设备访问权。CSP 能降低但不能完全消除此风险。}}
{{知识点50}}{{恢复已授权设备：使用 getDevices() 可在页面加载时获取之前授权的设备，结合 connect() 可实现"零点击"重连体验。这比每次调用 requestDevice() 要求用户选择设备更流畅，但需注意设备可能已超出范围或关机。}}
