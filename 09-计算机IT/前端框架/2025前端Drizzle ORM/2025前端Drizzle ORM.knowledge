# 2025前端Drizzle ORM
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}{{Drizzle ORM 是什么？Drizzle ORM 是一个无头（Headless）TypeScript ORM 库，设计理念是轻量、类型安全、无依赖、serverless-ready。它是唯一同时提供 SQL-like 查询 API 和关系查询 API（Relational Queries）的 ORM，让开发者在 TypeScript 中以接近原生 SQL 的方式操作数据库，同时获得完整的类型推断。}}

{{知识点2}}{{Drizzle 的核心优势——"If you know SQL, you know Drizzle"。与其他 ORM 不同，Drizzle 不抽象 SQL，而是拥抱 SQL，API 设计与 SQL 语法高度一致。这意味着开发者无需学习额外的查询语言，学习曲线极低。同时 Drizzle 支持 SQL 模板标签（sql template tag）来编写任意原生 SQL 片段。}}

{{知识点3}}{{Drizzle ORM 零依赖设计。Drizzle ORM 运行时依赖为 0，使其非常适合 serverless 和 edge 运行环境（如 Cloudflare Workers、Vercel Edge Functions）。包体积极小，不会对部署包大小产生显著影响。}}

{{知识点4}}{{Drizzle ORM 支持的数据库方言。Drizzle 是方言特定（dialect-specific）的 ORM，目前支持 PostgreSQL、MySQL、SQLite、SingleStore、MSSQL、CockroachDB 六种数据库方言，每种方言有独立的导入路径，如 drizzle-orm/pg-core、drizzle-orm/mysql-core、drizzle-orm/sqlite-core。}}

{{知识点5}}{{Schema 定义——pgTable 基本用法。在 Drizzle 中，使用 pgTable 定义 PostgreSQL 表的 schema。示例：import { pgTable, serial, text, integer } from 'drizzle-orm/pg-core'; export const users = pgTable('users', { id: serial('id').primaryKey(), name: text('name').notNull(), age: integer('age') })。TypeScript 键名默认作为数据库列名。}}

{{知识点6}}{{列名别名映射。如果希望 TypeScript 中使用 camelCase 而数据库使用 snake_case，可以在列定义中传入数据库列名作为第一个参数：varchar('first_name')。也可以在 Drizzle 初始化时设置 casing: 'snake_case' 选项实现自动映射，无需逐列手动设置别名。}}

{{知识点7}}{{Schema 文件组织方式。Drizzle 支持将 schema 定义放在单个 schema.ts 文件中，也支持分散到多个文件中。在 drizzle.config.ts 中指定 schema 路径，可以是单个文件或目录。Drizzle-Kit 会递归扫描目录中的所有 Drizzle 模型定义。}}

{{知识点8}}{{列类型与约束。Drizzle 提供丰富的列类型定义方法，包括 notNull()（非空约束）、unique()（唯一约束）、default()（默认值）、primaryKey()（主键）、generatedAlwaysAsIdentity()（自增主键）、references()（外键引用）等链式调用。}}

{{知识点9}}{{外键引用与级联操作。使用 references() 方法定义外键，第二个参数可指定级联动作：{ onDelete: 'cascade', onUpdate: 'set null' }。支持的级联动作包括 cascade、restrict、no action、set null、set default。}}

{{知识点10}}{{Timestamps 等公共列复用。可以将 created_at、updated_at 等公共列定义为独立对象，然后在多个表中通过展开运算符 ...timestamps 复用，实现 DRY 原则。这是 Drizzle 纯 TypeScript 定义带来的天然优势。}}

{{知识点11}}{{枚举类型（Enum）。PostgreSQL 方言使用 pgEnum 定义枚举：export const rolesEnum = pgEnum('roles', ['guest', 'user', 'admin'])，然后在表定义中作为列类型使用：role: rolesEnum().default('guest')。}}

{{知识点12}}{{PostgreSQL Schema（数据库命名空间）。Drizzle 支持 PostgreSQL 的 schema 功能（类似数据库文件夹），使用 pgSchema 定义：export const customSchema = pgSchema('custom')，然后在其中定义表：export const users = customSchema.table('users', { ... })。}}

{{知识点13}}{{Drizzle 数据库实例初始化。不同方言使用不同的初始化函数，如 import { drizzle } from 'drizzle-orm/node-postgres'。初始化时传入连接信息和可选的 schema：const db = drizzle({ connection: process.env.DATABASE_URL, schema })。支持多种驱动程序如 node-postgres、postgres.js、mysql2、better-sqlite3 等。}}

{{知识点14}}{{Select 基本查询。db.select().from(users) 返回所有行所有列。Drizzle 总是显式列出列名而非使用 SELECT *，这是为了保证字段顺序的一致性。返回类型自动从表定义推断，包含列的可空性信息。}}

{{知识点15}}{{部分列选择（Partial Select）。可以传入选择对象指定需要的列：db.select({ id: users.id, name: users.name }).from(users)。也支持任意表达式作为选择字段：db.select({ id: users.id, lowerName: sql`lower(${users.name})` }).from(users)，需用 sql<Type> 指定返回类型。}}

{{知识点16}}{{条件过滤（WHERE 子句）。使用 drizzle-orm 提供的操作符函数：eq（等于）、ne（不等于）、gt（大于）、gte（大于等于）、lt（小于）、lte（小于等于）。例如：db.select().from(users).where(eq(users.id, 42))。所有值自动参数化，防止 SQL 注入。}}

{{知识点17}}{{逻辑组合过滤。使用 and() 和 or() 组合多个条件：and(eq(users.id, 42), eq(users.name, 'Dan'))。使用 not() 取反条件。也支持直接使用 sql 模板标签编写复杂条件。}}

{{知识点18}}{{高级过滤操作符。Drizzle 提供丰富的过滤操作符：isNull/isNotNull（空值判断）、inArray/notInArray（IN 查询）、between/notBetween（范围查询）、like/ilike/notIlike（模糊匹配）、exists/notExists（子查询存在判断）、arrayContains/arrayContained/arrayOverlaps（PostgreSQL 数组操作）。}}

{{知识点19}}{{排序与分页。使用 .orderBy(asc(field)) 或 .orderBy(desc(field)) 排序，支持多字段排序。使用 .limit(n) 限制行数，.offset(n) 跳过行数，实现分页。MSSQL 方言使用 .fetch() 和 .top() 替代 limit。}}

{{知识点20}}{{DISTINCT 查询。db.selectDistinct() 返回去重结果。PostgreSQL 还支持 selectDistinctOn([users.id]) 按指定列去重。与 select() 用法一致，只是结果中不含重复行。}}

{{知识点21}}{{Insert 插入操作。基本语法：db.insert(users).values({ name: 'Andrew' })。支持批量插入：db.insert(users).values([{ name: 'A' }, { name: 'B' }])。使用 typeof users.$inferInsert 获取插入类型，用于类型安全的插入函数封装。}}

{{知识点22}}{{Returning 子句。PostgreSQL 和 SQLite 支持 .returning() 返回插入/更新/删除的行：db.insert(users).values({ name: 'Dan' }).returning()。支持部分返回：.returning({ insertedId: users.id })。MySQL 使用 .$returningId() 获取自增主键。}}

{{知识点23}}{{Upsert（冲突处理）。db.insert(users).values({ id: 1, name: 'John' }).onConflictDoNothing() 忽略冲突。.onConflictDoUpdate({ target: users.id, set: { name: 'John' } }) 冲突时更新。支持 targetWhere 和 setWhere 精细控制冲突条件。MySQL 使用 .onDuplicateKeyUpdate() 方法。}}

{{知识点24}}{{Update 更新操作。db.update(users).set({ name: 'Mr. Dan' }).where(eq(users.name, 'Dan'))。set 中值为 undefined 的字段会被忽略，设置为 null 则更新为 NULL。支持 sql 模板标签作为值：set({ updatedAt: sql`NOW()` })。支持 .limit() 和 .orderBy()。}}

{{知识点25}}{{Update...From 语法。Drizzle 支持 PostgreSQL 的 UPDATE...FROM 语法，允许在更新时关联其他表：db.update(users).set({ cityId: cities.id }).from(cities).where(and(eq(cities.name, 'Seattle'), eq(users.name, 'John')))。PostgreSQL 方言还可 returning 关联表的列。}}

{{知识点26}}{{Delete 删除操作。db.delete(users).where(eq(users.name, 'Dan')) 删除符合条件的行。支持 .returning() 返回被删除的行。支持 .orderBy() 和 .limit()（MySQL/SQLite）。}}

{{知识点27}}{{JOIN 查询。Drizzle 支持所有 SQL JOIN 类型：.leftJoin()、.rightJoin()、.innerJoin()、.fullJoin()、.crossJoin()，以及对应的 Lateral 变体（leftJoinLateral 等）。JOIN 结果类型自动推断，可空性根据 JOIN 类型正确处理。}}

{{知识点28}}{{JOIN 时的部分选择。JOIN 时可使用部分选择获取扁平化结果：db.select({ userId: users.id, petId: pets.id }).from(users).leftJoin(pets, eq(users.id, pets.ownerId))。需要注意 LEFT JOIN 的列可能为 null，使用 sql<Type | null> 指定正确类型。}}

{{知识点29}}{{表别名与自连接。使用 alias() 函数创建表别名：const parent = alias(users, 'parent')，然后用于自连接：db.select().from(users).leftJoin(parent, eq(parent.id, users.parentId))。Drizzle 自动处理类型推断和结果结构。}}

{{知识点30}}{{聚合查询。使用 sql 标签配合聚合函数：sql`cast(count(${users.id}) as int)`。使用 .groupBy() 分组，.having() 过滤分组。cast(... as int) 在 PostgreSQL 中是必要的，因为 count() 返回 bigint 被视为字符串。}}

{{知识点31}}{{子查询（Subquery）。使用 .as() 创建子查询：const sq = db.select().from(users).where(eq(users.id, 42)).as('sq')，然后在主查询或 JOIN 中使用：db.select().from(sq)。子查询可嵌入任何表可使用的位置。}}

{{知识点32}}{{CTE（WITH 子句）。使用 db.$with('name').as(query) 创建 CTE：const sq = db.$with('sq').as(db.select().from(users).where(eq(users.id, 42)))，然后 db.with(sq).select().from(sq)。CTE 中可包含 INSERT、UPDATE、DELETE 语句。}}

{{知识点33}}{{Relation 关系定义。Drizzle 使用 relations() 函数定义表间关系，与外键独立，是应用层抽象。one() 定义一对一关系，many() 定义一对多关系。关系定义不影响数据库 schema，可用于不支持外键的数据库。}}

{{知识点34}}{{关系查询 API（Relational Queries）。初始化时传入 schema 后使用 db.query.tableName.findMany({ with: { relation: true } }) 查询嵌套关系数据。支持 findMany() 和 findFirst()。始终生成单条 SQL 语句，适合 serverless 场景。}}

{{知识点35}}{{关系查询的嵌套与过滤。with 支持无限嵌套：db.query.users.findMany({ with: { posts: { with: { comments: true } } } })。每个层级都支持 where 过滤、columns 部分选择、limit/offset、orderBy 排序。}}

{{知识点36}}{{关系查询的 columns 选择。使用 columns 属性控制返回的列：db.query.posts.findMany({ columns: { id: true, content: true }, with: { comments: true } })。true 表示只包含这些列，false 表示排除这些列。同时存在时 false 被忽略。}}

{{知识点37}}{{关系查询的 extras 自定义字段。使用 extras 添加计算字段：db.query.users.findMany({ extras: { loweredName: sql`lower(${users.name})`.as('lowered_name') } })。必须使用 .as() 指定别名，否则类型变为 DrizzleTypeError。支持回调语法用于复杂表达式。}}

{{知识点38}}{{事务（Transactions）。使用 db.transaction(async (tx) => { ... }) 执行事务。tx 对象拥有与 db 相同的查询接口。支持嵌套事务（自动使用 savepoints）。支持 tx.rollback() 手动回滚。事务可返回值。}}

{{知识点39}}{{事务隔离级别配置。PostgreSQL 支持配置隔离级别、访问模式和延迟性：db.transaction(async (tx) => { ... }, { isolationLevel: 'read committed', accessMode: 'read write', deferrable: true })。不同方言有不同的配置选项。}}

{{知识点40}}{{Drizzle-Kit 迁移工具。Drizzle-Kit 是 Drizzle 的 CLI 迁移管理工具，提供多个命令：drizzle-kit generate（生成 SQL 迁移文件）、drizzle-kit migrate（应用迁移）、drizzle-kit push（直接推送 schema 变更）、drizzle-kit pull（从数据库拉取 schema）。}}

{{知识点41}}{{drizzle-kit generate + migrate 工作流。这是推荐的生产环境迁移流程。generate 命令对比当前 schema 与上一次迁移的差异，生成包含 snapshot.json 和 migration.sql 的迁移目录。migrate 命令读取迁移文件并应用到数据库。支持重命名提示。}}

{{知识点42}}{{drizzle-kit push 快速原型推送。push 命令直接将 schema 变更推送到数据库，不生成迁移文件。适合快速原型开发，但不适合需要版本控制和回滚的生产环境。它会自动对比差异并生成 ALTER 语句。}}

{{知识点43}}{{drizzle-kit pull 数据库优先。pull 命令从现有数据库读取 schema 并生成 TypeScript 定义文件。适合数据库优先（database-first）工作流，即数据库是 schema 的源头，代码从数据库反向生成。}}

{{知识点44}}{{运行时迁移应用。除了 CLI 迁移，Drizzle 还支持在应用启动时通过代码应用迁移：import { migrate } from 'drizzle-orm/node-postgres/migrator'; await migrate(db, { migrationsFolder: './drizzle' })。适合 serverless 部署和零停机部署场景。}}

{{知识点45}}{{Drizzle 配置文件（drizzle.config.ts）。使用 defineConfig 定义配置，指定 dialect（postgresql/mysql/sqlite/turso）、schema 路径、out 迁移输出目录、dbCredentials 数据库连接信息等。配置文件是 Drizzle-Kit 命令的输入源。}}

{{知识点46}}{{SQL 模板标签（sql template tag）。sql 模板标签允许在 Drizzle 查询中嵌入原生 SQL 片段，同时保持参数化安全。例如：sql`lower(${users.name})` 会将 users.name 替换为正确的列引用并参数化。支持泛型 sql<Type> 指定返回类型。}}

{{知识点47}}{{Prepared Statements（预编译语句）。使用 .prepare('name') 创建预编译语句，配合 placeholder('paramName') 定义参数占位符。执行时传入参数：prepared.execute({ param: value })。预编译语句可大幅提升重复查询的性能，数据库会缓存查询计划。}}

{{知识点48}}{{批量插入与性能优化。Drizzle 的批量插入 db.insert(users).values([...array]) 在底层会生成单条 INSERT 语句。对于大批量数据，建议分批插入（每批 1000-5000 条）。预编译语句在批量场景中尤其有效。}}

{{知识点49}}{{类型推断系统。Drizzle 提供两个核心类型推断：typeof table.$inferSelect 推断查询返回类型，typeof table.$inferInsert 推断插入数据类型。列的 nullability、默认值等信息都会被正确推断，确保类型安全。}}

{{知识点50}}{{与 Next.js / Nuxt 等框架集成。Drizzle ORM 完美适配现代全栈框架。在 Next.js App Router 中，可在 server components 和 API routes 中直接使用。配合 Neon、PlanetScale、Turso 等 serverless 数据库，实现全栈 TypeScript 类型安全。}}

{{知识点51}}{{Drizzle Studio 数据库浏览器。Drizzle Kit 内置 Drizzle Studio，可通过 npx drizzle-kit studio 启动本地 Web 界面，可视化浏览数据库数据、表结构和关系。支持数据编辑，是开发调试的利器。}}

{{知识点52}}{{drizzle-kit export 命令。export 命令将 Drizzle schema 输出为 SQL 语句到控制台或文件。可配合 Atlas 等外部迁移工具使用，实现更灵活的迁移工作流。适合需要与其他工具链集成的团队。}}
