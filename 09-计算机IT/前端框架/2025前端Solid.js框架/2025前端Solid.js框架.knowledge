# 2025前端Solid.js框架
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}{{Solid.js 是什么：Solid.js 是一个声明式的、高效的 JavaScript 库，用于构建用户界面。与 React 等框架不同，它不使用虚拟 DOM（Virtual DOM），而是将模板编译为真实的 DOM 节点，并通过细粒度的响应式更新来操作它们。由 Ryan Carniato 创建，经过五年以上的研究和开发。}}

{{知识点2}}{{Solid.js 的核心设计理念：Solid.js 遵循"声明式数据"理念，将状态建模为响应式原语系统。组件是普通 JavaScript 函数，只运行一次来设置视图（render-once 模型）。状态变化时，只有依赖该状态的代码才会重新执行，而非整个组件重新渲染。}}

{{知识点3}}{{createSignal 基础：createSignal 是 Solid.js 最基础的响应式原语，创建一个由 getter（访问器）和 setter（设置器）组成的响应式状态。用法：const [count, setCount] = createSignal(0)。getter 是一个函数，调用 count() 获取当前值；setter 用于更新值。}}

{{知识点4}}{{Signal 的拉取式响应模型：Signal 使用拉取式（pull-based）响应模型。读取（reads）操作轻量级，写入（writes）触发依赖追踪和副作用重新执行。这种模型针对频繁读取、不频繁写入的场景进行了优化。}}

{{知识点5}}{{SignalOptions 配置：createSignal 接受可选的 options 对象，包含 name（调试工具中显示的名称，仅开发模式）、equals（自定义比较函数，默认使用引用相等 ===，设为 false 则每次更新都触发）和 internal（标记为内部信号，不显示在开发工具中）。}}

{{知识点6}}{{createEffect 副作用：createEffect 创建一个响应式计算，自动追踪函数内访问的响应式值（如信号）。当任何依赖变化时，函数会重新执行。初始运行在当前渲染阶段完成后、浏览器绘制前执行。后续运行在依赖变化时触发。}}

{{知识点7}}{{createEffect 执行时序：初始运行在所有同步代码完成后、DOM 元素创建后、浏览器绘制前执行。Ref 在首次运行前已设置。多个依赖在同一批次变化时，effect 只运行一次。多个 effect 之间的运行顺序不保证。effect 总是在同一更新周期内的纯计算（如 memo）之后运行。}}

{{知识点8}}{{createEffect 与 SSR：effect 在服务端渲染（SSR）期间不会运行，在客户端首次水合（hydration）期间也不会运行。这确保了服务端和客户端的行为一致性。}}

{{知识点9}}{{createRenderEffect 渲染效果：与 createEffect 不同，createRenderEffect 在渲染阶段同步运行。在组件的同步执行过程中，render effect 会立即执行，而 createEffect 会被调度到渲染完成后执行。适合需要在 DOM 更新时立即执行的操作。}}

{{知识点10}}{{createStore 状态管理：Store 是 Solid.js 的状态管理原语，提供集中式的方式来处理共享数据。与 signal 追踪单个值不同，store 通过只更新变化的属性来维持细粒度的响应性，适合管理复杂状态。}}

{{知识点11}}{{Store 的 Proxy 机制：Store 使用 JavaScript 的 Proxy 机制，将响应性扩展到嵌套属性。创建 store 时不会立即设置信号追踪，信号是惰性创建的——只有在追踪作用域内访问时才会建立。}}

{{知识点12}}{{Store 的路径语法（Path Syntax）：更新 store 值的最佳方式是使用 setter 的路径语法，格式为 setStore(key, newValue)。支持字符串键精确目标、数组键选择多个属性、过滤函数基于动态条件访问，以及范围对象 {from, to, by} 操作数组切片。}}

{{知识点13}}{{Store 数组操作：路径语法提供了强大的数组操作能力。追加元素可用展开运算符（替换整个数组触发所有依赖更新）或路径语法（直接赋值到新索引，仅触发新元素的依赖更新，更高效）。支持批量修改多个元素、动态值赋值、条件过滤更新。}}

{{知识点14}}{{Store 多重设置器语法：使用数组语法可同时设置多个索引的属性，如 setStore("users", [2, 7, 10], "loggedIn", false)。范围语法 {from, to} 设置连续范围，{from, to, by} 支持步长。多重设置器调用自动包裹在 batch 中，所有元素一次更新后再触发 effect。}}

{{知识点15}}{{createContext 上下文：Context 提供了一种无需逐层传递 props 即可通过组件树传递数据的方式。使用 createContext() 创建上下文对象，通过 Provider 的 value 属性传递值，子组件使用 useContext() 消费上下文。适用于全局应用状态或多个组件频繁访问的信息。}}

{{知识点16}}{{Context 使用场景：当有大型组件树需要共享状态时使用 Context。可以避免 prop drilling（属性逐层传递）。但如果只是避免几层传递，调整组件层次结构可能更简单。Signal 可以直接导入使用，往往是最简单的方案。}}

{{知识点17}}{{Context 自定义工具函数：当应用包含多个 context 对象时，可创建自定义的 Provider 组件和 useContext 包装函数来提高可读性。例如创建 useCounter() 工具函数，避免每次都要导入 useContext 并传入 context 对象。}}

{{知识点18}}{{Solid.js 组件模型：在 Solid.js 中，组件就是普通函数——返回 DOM 节点。组件函数只执行一次（render-once），之后的更新完全由响应式系统驱动。这与 React 的函数组件每次渲染都重新执行形成鲜明对比。}}

{{知识点19}}{{自动依赖追踪：Solid.js 的响应式系统自动追踪依赖。在响应式上下文中访问一个信号就自动订阅了它，无需手动声明依赖数组。这消除了 React 中 useEffect 依赖数组遗漏导致的 bug。}}

{{知识点20}}{{细粒度 DOM 更新：Solid.js 编译器将 JSX 编译为高效的真实 DOM 操作。静态 HTML 被提取为模板，动态部分通过细粒度的反应式更新。只有依赖变化的具体 DOM 节点会更新，而非整个组件子树。}}

{{知识点21}}{{Solid.js 与 React 的关键区别：Solid.js 组件只执行一次（render-once），React 每次状态变化都重新执行组件函数。Solid.js 不使用虚拟 DOM，直接操作真实 DOM。Solid.js 使用 getter 函数（count()）而非直接值访问。Solid.js 的依赖追踪是自动的，无需手动指定。}}

{{知识点22}}{{Solid.js 性能优势：Solid.js 的性能几乎与优化过的原生 JavaScript 不可区分。在 JS Framework Benchmark 中表现优异。体积小且完全支持 tree-shaking。服务端渲染同样快速。经过五年多的精心工程设计，专注于性能优化。}}

{{知识点23}}{{Derived State 派生状态：在 Solid.js 中创建派生状态非常简单——只需将表达式包裹在函数中。例如 const doubleCount = () => count() * 2。这个函数在追踪作用域中被调用时自动建立依赖关系。}}

{{知识点24}}{{Solid.js JSX 编译：Solid.js 使用 babel-preset-solid 处理 JSX。TypeScript 配置需要设置 jsx: "preserve" 和 jsxImportSource: "solid-js"。编译器将 JSX 转换为真实的 DOM 模板创建和细粒度的插入操作。}}

{{知识点25}}{{Solid.js 项目初始化：使用 npx degit solidjs/templates/js my-app（JavaScript）或 npx degit solidjs/templates/ts my-app（TypeScript）快速创建项目。项目基于 Vite 构建。也可手动安装 babel-preset-solid 和 solid-js。}}

{{知识点26}}{{Solid.js 现代框架特性：Solid.js 提供了完整的现代框架功能，包括 JSX、Fragments、Context、Portals、Suspense、流式 SSR、渐进式水合（Progressive Hydration）、Error Boundaries 和并发渲染。}}

{{知识点27}}{{Solid.js Web Component 支持：Solid.js 对 Web Component 友好，可以通过 solid-element 包来创建自定义元素（Custom Elements）。这使得 Solid.js 组件可以与其他框架或无框架项目互操作。}}

{{知识点28}}{{Solid.js 同构渲染：Solid.js 支持同构渲染，即同一组件可以在客户端和服务端渲染。支持 Node LTS 以及最新的 Deno 和 Cloudflare Worker 运行时。服务端渲染支持流式传输和渐进式水合。}}

{{知识点29}}{{Solid.js 自定义渲染器：Solid.js 支持编写自定义渲染器（Custom Renderers），使得 Solid 可以在任何环境中使用，不仅限于浏览器 DOM。这为移动端、Canvas、WebGL 等场景提供了可能性。}}

{{知识点30}}{{Solid.js 生态系统：Solid.js 拥有不断增长的生态系统。核心库 solid-js 提供响应式原语，solid-js/web 提供 DOM 相关功能。社区提供 solid-primitives（自定义原语集合）、Kobalte（组件库）、solid-labels（构建时工具）等。}}

{{知识点31}}{{Solid.js 调试支持：Solid.js 天然可调试——DOM 中的 div 就是真实的 div，可直接使用浏览器开发者工具检查渲染。支持 Solid DevTools 进行更深入的响应式系统调试，包括信号追踪、依赖关系可视化等。}}

{{知识点32}}{{onCleanup 清理函数：onCleanup 是 Solid.js 的生命周期钩子，在组件销毁或 effect 重新运行前执行清理操作。用于取消订阅、清除定时器、释放资源等。在 SSR 期间不会执行。}}

{{知识点33}}{{onMount 挂载钩子：onMount 在组件首次渲染到 DOM 后执行一次。等效于在 createEffect 中运行但没有追踪任何依赖。适合执行只需要运行一次的初始化代码，如 API 调用或第三方库初始化。}}

{{知识点34}}{{batch 批量更新：batch 函数将多个状态更新合并为一次更新，避免中间状态触发不必要的 effect 执行。在 batch 块内的所有信号和 store 更新完成后，才会统一触发依赖的 effect。Store 的多重设置器语法自动使用 batch。}}

{{知识点35}}{{untrack 非追踪访问：untrack 函数允许在响应式上下文中读取信号值而不建立依赖关系。当你需要访问某个值但不希望它触发重新执行时使用。例如 untrack(() => count()) 读取 count 但不订阅它。}}

{{知识点36}}{{Solid.js 事件处理：Solid.js 使用事件委托（Event Delegation）来优化事件处理。对于 JSX 中的事件处理器如 onClick，Solid.js 会在根级别委托事件而非在每个元素上单独绑定。支持原生事件绑定（on:click）和委托事件绑定。}}

{{知识点37}}{{Show 条件渲染：Show 是 Solid.js 的控制流组件，用于条件渲染。当 when 属性为真时渲染子组件，否则渲染 fallback。与 JavaScript 的三元运算符不同，Show 组件在条件切换时更好地管理组件生命周期。}}

{{知识点38}}{{For 列表渲染：For 组件是 Solid.js 渲染列表的推荐方式。它为数组中的每个项目调用回调函数，并在数组变化时高效地更新 DOM。与 map 不同，For 只更新变化的项目而非重新渲染整个列表。}}

{{知识点39}}{{Suspense 异步处理：Suspense 组件用于处理异步数据加载。当子组件中的 Resource 正在加载时，显示 fallback 内容。支持流式 SSR，可以在服务端逐步发送 HTML 片段到客户端。是 Solid.js 并发渲染能力的基础。}}

{{知识点40}}{{createResource 异步数据：createResource 用于管理异步数据源。它接受一个数据获取函数和可选的源信号，返回一个包含 data、loading、error 状态的 Resource 对象。与 Suspense 配合使用，实现声明式的异步数据加载。}}

{{知识点41}}{{SolidStart 全栈框架：SolidStart 是 Solid.js 的全栈元框架，类似于 Next.js 之于 React。提供文件系统路由、服务端渲染、API 路由、中间件支持等功能。基于 Vinxi 构建，集成了 Vite 和 Nitro。}}

{{知识点42}}{{Solid.js 浏览器兼容性：Solid.js 支持最近 2 年的现代浏览器，包括 Firefox、Safari、Chrome 和 Edge（桌面和移动端）。不支持 IE 等已淘汰的浏览器。服务端支持 Node LTS 以及最新版本的 Deno 和 Cloudflare Workers。}}

{{知识点43}}{{Signal 的 equals 选项详解：默认使用引用相等（===）比较。设为 false 时每次 set 都触发更新，适合手动控制更新的场景。传入自定义函数 (prev, next) => boolean 时，返回 true 表示相等（不更新），返回 false 表示不同（触发更新）。函数必须是纯函数。}}

{{知识点44}}{{Store 读写分离优势：将 store 的读取（getter）和写入（setter）能力分离，提供了有价值的调试优势。这种分离便于追踪和控制哪些组件在访问或修改值，有助于维护大型应用的状态管理。}}

{{知识点45}}{{Nested Store 嵌套存储：Store 支持创建嵌套存储来帮助设置嵌套属性。例如 const [users, setUsers] = createStore(store.users)，通过 setUsers 的更改会同步更新 store.users。这种派生 store 读取时也与原始 store 保持同步。}}

{{知识点46}}{{Solid.js 与 D3 集成：由于 Solid.js 提供对 DOM 的直接访问（bare-metal 最小抽象），可以轻松集成 D3 等原生 JavaScript 库。不需要像虚拟 DOM 框架那样进行复杂的桥接处理。}}

{{知识点47}}{{createMemo 记忆化计算：createMemo 创建一个记忆化的计算值，只在其依赖变化时重新计算。与派生信号（() => count() * 2）不同，memo 会缓存结果，避免在多处使用时重复计算。适合计算成本较高的派生值。}}

{{知识点48}}{{splitProps 属性分割：splitProps 工具函数用于将组件的 props 分割为多组，方便将一部分 props 传递给子组件或 DOM 元素，另一部分留给自身使用。这在创建包装组件（wrapper component）时非常有用。}}

{{知识点49}}{{mergeProps 合并属性：mergeProps 将多个属性对象合并为一个响应式属性对象。与 Object.assign 不同，mergeProps 保持响应性——如果源对象是信号或 store，合并后的属性会自动跟踪变化。}}

{{知识点50}}{{Portal 门户渲染：Portal 组件将子元素渲染到 DOM 树中的不同位置，通常用于模态框、工具提示、弹出菜单等需要脱离父组件 DOM 层级的场景。Solid.js 的 Portal 支持指定目标容器。}}
