# 2025前端前端CSS Values and Units
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}{{CSS 值和单位概述：CSS 每个声明由属性/值对组成，值可以是整数、关键字、函数或多种项目的组合。有些值带单位，有些不带。CSS Values and Units 模块定义了 CSS 属性接受的数据类型——值和单位，还定义了用于定义每个 CSS 属性和函数有效值集的形式语法（值定义语法）。}}

{{知识点2}}{{整数（Integer）类型：整数是一个或多个十进制数字（0-9），如 1024 或 -55。整数前面可以有 + 或 - 符号，符号与数字之间不能有空格。整数是 CSS 中最基本的数值类型之一，用于 z-index、flex-grow 等属性。}}

{{知识点3}}{{数字（Number）类型：数字表示实数，可以有也可以没有小数点和小数部分，如 0.255、128 或 -1.2。数字前面也可以有 + 或 - 符号。数字类型用于 opacity、scale 等属性。}}

{{知识点4}}{{维度（Dimension）类型：维度是带有单位的数字，如 45deg、100ms 或 10px。单位标识符不区分大小写，数字和单位之间不能有空格。CSS 用维度指定长度、角度、时间、频率、弹性值和分辨率。}}

{{知识点5}}{{绝对长度单位：绝对长度单位固定为物理长度，包括 cm（厘米）、in（英寸，1in=96px）、mm（毫米）、pc（派卡，1pc=1/6in）、pt（磅，1pt=1/72in）、px（像素，1px=1/96in）和 Q（四分之一毫米，1Q=1/40cm）。这些单位更适合固定尺寸的媒体输出，如打印。}}

{{知识点6}}{{px 像素单位详解：px 是最常用的 CSS 长度单位。1px 等于 1/96 英寸。px 是一个视觉角度单位，在高 DPI 屏幕上，1 个 CSS 像素可能对应多个设备像素。当长度值为 0 时，单位标识符可以省略。}}

{{知识点7}}{{em 单位详解：em 是本地字体相对长度单位，相对于元素自身的字体大小。如果用于 font-size 属性，则相对于继承的字体大小。1em 等于当前元素的 font-size 值。嵌套使用 em 会导致复合增长问题，例如在 font-size: 1.1em 的元素内再设置 font-size: 1.1em，实际大小为 1.21em。}}

{{知识点8}}{{rem 单位详解：rem 是根字体相对长度单位，相对于根元素（通常是 html 元素）的字体大小。与 em 不同，rem 不受嵌套影响，在整个文档中保持一致的参考基准。浏览器默认根字体大小为 16px，因此 1rem = 16px。rem 是响应式设计中最推荐的字体大小单位。}}

{{知识点9}}{{视口单位（Viewport Units）概述：视口单位长度相对于浏览器视口的尺寸。vw 是视口宽度的 1%，vh 是视口高度的 1%。vmin 取视口较小尺寸的 1%，vmax 取视口较大尺寸的 1%。vi 和 vb 分别对应根元素内联轴和块轴方向的 1%。}}

{{知识点10}}{{动态视口单位（Dynamic Viewport Units）：dvh、dvw 等动态视口单位相对于动态变化的视口大小，会随移动浏览器地址栏的显示/隐藏而变化。dvh = 1% 动态视口高度，dvw = 1% 动态视口宽度。相比传统 vh/vw，动态视口单位能更准确地反映移动端可见区域。}}

{{知识点11}}{{大视口和小视口单位：lvh、lvw 是大视口单位，始终使用最大的视口尺寸（地址栏隐藏时的尺寸）。svh、svw 是小视口单位，始终使用最小的视口尺寸（地址栏显示时的尺寸）。这些单位提供更稳定的参考基准，适合需要固定布局的场景。}}

{{知识点12}}{{ch 单位详解：ch 是本地字体相对长度单位，等于元素字体中窄字形 "0"（零，U+0030）的平均字符前进宽度。ch 常用于限制文本行宽，例如 max-width: 65ch 被认为是最佳阅读行宽，约可容纳 65 个英文字符。}}

{{知识点13}}{{ex 和 cap 单位：ex 相对于元素字体的 x 高度（小写字母 x 的高度），cap 相对于元素字体的大写字母标称高度。这些单位在排版精确控制中有用，但实际使用较少。cap 单位在需要对齐大写字母时特别有用。}}

{{知识点14}}{{ic 单位详解：ic 是本地字体相对长度单位，等于元素字体中全角字形 "水"（CJK 水字，U+6C34）的平均字符前进宽度。ic 单位特别适用于中日韩（CJK）文本排版，可以精确控制与 CJK 字符宽度相关的布局。}}

{{知识点15}}{{lh 和 rlh 单位：lh 相对于元素的行高，rlh 相对于根元素的行高。这些单位在垂直节奏（vertical rhythm）排版中非常有用，可以确保元素尺寸与文本行高保持一致的倍数关系。}}

{{知识点16}}{{根字体相对单位族：包括 rcap（根元素大写字母高度）、rch（根元素 "0" 字符宽度）、rem（根元素字体大小）、rex（根元素 x 高度）、ric（根元素全角字符宽度）、rlh（根元素行高）。这些单位提供相对于根元素的稳定参考，不受嵌套影响。}}

{{知识点17}}{{容器查询单位（Container Query Units）：cqw、cqh、cqi、cqb、cqmin、cqmax 是 CSS 容器查询模块定义的单位。cqw = 查询容器宽度的 1%，cqh = 查询容器高度的 1%，cqi = 容器内联尺寸的 1%，cqb = 容器块尺寸的 1%。这些单位让组件能根据其容器而非视口进行响应式布局。}}

{{知识点18}}{{百分比（Percentage）类型：百分比表示某个其他值的一部分。每个允许百分比的属性都会定义百分比所参考的量。例如 width: 50% 参考的是父元素的计算宽度。百分比可以与维度混合使用，如 length-percentage 类型允许同时接受长度和百分比值。}}

{{知识点19}}{{角度单位：CSS 支持四种角度单位。deg（度）：一个完整圆为 360 度。grad（梯度）：一个完整圆为 400 梯度。rad（弧度）：一个完整圆为 2π 弧度。turn（圈）：一个完整圆为 1 圈。turn 单位在动画旋转中特别直观，如 rotate(0.5turn) 表示旋转半圈。}}

{{知识点20}}{{时间单位：CSS 支持两种时间单位。s（秒）和 ms（毫秒），1s = 1000ms。时间值在 animation-duration、transition-duration 等属性中使用。与长度单位不同，时间单位的标识符是必需的（即使是 0 也需要写 0s 或 0ms）。}}

{{知识点21}}{{频率单位：CSS 支持 Hz（赫兹）和 kHz（千赫兹）两个频率单位，1kHz = 1000Hz。频率单位在 CSS 中极少使用，主要在语音合成相关的属性中出现。单位标识符不区分大小写，1Hz 也可以写成 1hz 或 1HZ。}}

{{知识点22}}{{弹性单位（Flex Unit）：fr 是 CSS Grid 布局中定义的弹性单位，表示网格容器中的弹性长度。例如 grid-template-columns: 1fr 2fr 表示第二列宽度是第一列的两倍。fr 单位按比例分配剩余空间，是 Grid 布局的核心特性之一。}}

{{知识点23}}{{分辨率单位：CSS 支持三种分辨率单位。dpi（每英寸点数）、dpcm（每厘米点数）和 dppx/x（每像素点数）。分辨率单位主要用于 @media 查询中，如 @media (min-resolution: 2dppx) 用于检测高 DPI 屏幕。1dppx 等于 96dpi。}}

{{知识点24}}{{calc() 函数详解：calc() 允许在 CSS 属性值中使用加减乘除数学运算。最常用于混合不同单位的值，如 calc(100% - 32px)。calc() 支持嵌套使用，可以与 CSS 自定义属性结合。在 calc() 中，加减号两侧必须有空格，乘除则没有此限制。}}

{{知识点25}}{{min() 和 max() 函数：min() 接受一组逗号分隔的值并返回最小值，如 width: min(500px, 50%)。max() 返回最大值，如 width: max(500px, 50%)。这两个函数常用于在不同单位间进行比较，实现响应式约束。}}

{{知识点26}}{{clamp() 函数详解：clamp() 接受三个参数——最小值、首选值和最大值，语法为 clamp(min, preferred, max)。如果首选值小于最小值返回最小值，大于最大值返回最大值，否则返回首选值。例如 font-size: clamp(1rem, 2.5vw, 2rem) 实现响应式字体大小。}}

{{知识点27}}{{CSS 颜色值类型概述：CSS 支持多种颜色表示方法，包括命名颜色（如 rebeccapurple）、十六进制（如 #f09）、RGB（如 rgb(255 0 153)）、HSL（如 hsl(150 30% 60%)）、HWB（如 hwb(12 50% 0%)）、Lab、LCH、Oklab、OkLCh 等。每种方法都有不同的适用场景。}}

{{知识点28}}{{RGB 颜色函数：rgb() 函数使用红、绿、蓝三个通道定义颜色，每个通道值范围为 0-255 或 0%-100%。新语法使用空格分隔，支持透明度如 rgb(255 0 153 / 80%)。十六进制颜色是 RGB 的简写形式，#rgb 为 3 位简写，#rrggbb 为完整 6 位形式。}}

{{知识点29}}{{HSL 颜色函数：hsl() 使用色相（Hue, 0-360度）、饱和度（Saturation, 0%-100%）和亮度（Lightness, 0%-100%）定义颜色。HSL 比 RGB 更直观，便于调整颜色的明暗和饱和度。新语法如 hsl(150 30% 60% / 0.8) 支持透明度。}}

{{知识点30}}{{现代颜色空间：Lab 和 LCH 基于 CIELAB 色彩空间，提供感知均匀的颜色表示。oklab() 和 oklch() 基于 Oklab 色彩空间，进一步改善了感知均匀性。这些现代色彩空间在创建色阶、颜色插值时能产生更自然的结果。}}

{{知识点31}}{{相对颜色语法：CSS 相对颜色允许基于现有颜色生成新颜色。语法为 hsl(from red 240deg s l)，其中 from 关键字指定源颜色，后面定义输出颜色各通道的值。可以使用 calc() 修改通道，如 lch(from blue calc(l + 20) c h) 将蓝色变亮。}}

{{知识点32}}{{color-mix() 函数：color-mix() 用于混合两种颜色，语法为 color-mix(in <color-space>, color1, color2)。可以指定混合比例和颜色空间，如 color-mix(in oklch, red 40%, blue) 将红色和蓝色按 40:60 混合。默认混合比例为 50:50。}}

{{知识点33}}{{light-dark() 函数：light-dark() 接受两个颜色参数，第一个用于浅色主题，第二个用于深色主题。如 light-dark(white, black) 在浅色模式下使用白色，深色模式下使用黑色。该函数需要配合 color-scheme 属性使用。}}

{{知识点34}}{{currentColor 关键字：currentColor 代表元素的 color 属性值，允许在通常不接受颜色值的属性上使用当前颜色。例如 border: 1px solid currentColor。如果 currentColor 用于 color 属性本身，则取继承的 color 值。这是实现主题一致性的常用技巧。}}

{{知识点35}}{{颜色插值：颜色插值发生在渐变、过渡和动画中。插值时颜色首先转换到指定的颜色空间（默认为 Oklab），然后对每个分量进行线性插值。可以通过 color-interpolation-method 覆盖默认的插值颜色空间。缺失分量（none）在插值中有特殊处理规则。}}

{{知识点36}}{{CSS 全局关键字值：CSS 定义了几个全局关键字。initial 将属性重置为初始值，inherit 使属性从父元素继承，unset 如果属性自然继承则等同于 inherit 否则等同于 initial，revert 将属性值回退到用户代理样式表的值，revert-layer 回退到上一个级联层的值。}}

{{知识点37}}{{值定义语法（Value Definition Syntax）：CSS 使用形式化语法定义每个属性接受的有效值集合。语法使用组合子（combinators）如 |（或）、&&（且需全部出现）、||（且至少一个出现）和 []（分组）来描述值的组合方式。理解值定义语法有助于阅读 CSS 规范。}}

{{知识点38}}{{interpolate-size 属性：这是一个较新的 CSS 属性，用于控制 auto 等关键字值在尺寸过渡和动画中的插值行为。它使得 height: auto 也能参与 CSS 过渡动画，解决了长期以来 CSS 无法对 auto 高度进行动画的痛点。}}

{{知识点39}}{{CSS 数学函数高级功能：除了基本的 calc/min/max/clamp，CSS 还支持高级数学函数。round() 按舍入策略计算值，mod() 取模，rem() 取余数，abs() 取绝对值，sign() 取符号。三角函数包括 sin()、cos()、tan()、asin()、acos()、atan()、atan2()。还有 exp()、log()、pow()、sqrt()、hypot() 等。}}

{{知识点40}}{{attr() 函数：attr() 允许在 CSS 中引用 HTML 元素的属性值。例如 content: attr(data-label) 可以将 data-label 属性的值作为伪元素的内容。在 CSS Values Level 5 中，attr() 的功能得到了大幅扩展，支持更多属性类型。}}

{{知识点41}}{{random() 和 progress() 函数：random() 是 CSS Values Level 5 中引入的函数，允许在 CSS 中生成随机值。progress() 函数用于计算一个值在某个范围内的进度。sibling-count() 和 sibling-index() 函数返回元素在兄弟元素中的计数和索引。这些是 CSS 的新能力。}}

{{知识点42}}{{if() 函数：if() 是 CSS 条件值函数，允许根据条件选择不同的值。这使得 CSS 可以根据条件（如媒体查询结果或容器查询结果）动态选择属性值，减少了对 @media 规则的依赖，让样式声明更加灵活和内联。}}

{{知识点43}}{{尺寸和布局中的混合类型：CSS 允许某些属性接受混合类型的值，如 <length-percentage> 同时接受长度和百分比。<frequency-percentage>、<angle-percentage>、<time-percentage> 也是类似的混合类型。这在 calc() 中特别有用，可以组合使用如 calc(100% - 20px)。}}

{{知识点44}}{{位置值（Position Value）：position 值类型用于定义对象在定位区域内的 2D 位置，主要用于 background-position 等属性。可以使用关键字（top、right、bottom、left、center）、长度值或百分比的组合来指定位置。}}

{{知识点45}}{{CSS 值序列化：CSSOM API 在序列化颜色和其他值时使用标准化的字符串表示。理解值的序列化规则有助于通过 JavaScript 操作 CSS 值时避免意外行为。例如 rgb(255,0,0) 会被序列化为 rgb(255, 0, 0)。}}

{{知识点46}}{{CSS 值的继承与级联：在 CSS 级联和继承中，initial 将属性设为规范定义的初始值，inherit 强制从父元素继承，unset 是两者的智能组合——自然继承的属性用 inherit 行为，否则用 initial 行为。revert 和 revert-layer 提供更高级的回退机制。}}

{{知识点47}}{{ratio 数据类型：ratio 表示两个数值之间的比率关系，主要用于媒体查询中的 aspect-ratio 条件。语法为 <number> / <number>，如 16 / 9。在 CSS 中 aspect-ratio 属性也使用此类型，如 aspect-ratio: 16 / 9。}}

{{知识点48}}{{字符串（String）数据类型：CSS 字符串用引号包围，支持单引号和双引号。字符串中可以使用反斜杠转义特殊字符。字符串类型主要用于 content 属性、@font-face 中的 font-family 等场景。}}
