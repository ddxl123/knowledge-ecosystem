# 2025前端错误边界与异常处理
# 领域: 计算机IT
# 学科: 前端框架
# 来源: 2025前端错误边界与异常处理
# 描述: 前端错误边界与异常处理核心知识点

{{Error Boundary概念}}{{Error Boundary是React中用于捕获子组件树渲染期间错误的组件，防止整个应用因单个组件错误而崩溃。Error Boundary无法捕获事件处理器、异步代码、服务端渲染及Error Boundary自身的错误。错误边界在生产环境中替代了渲染时的白屏崩溃，提供优雅降级的用户体验。}}

{{getDerivedStateFromError静态方法}}{{static getDerivedStateFromError(error)是React Error Boundary的静态生命周期方法，在子组件抛出错误后被调用，用于渲染备用UI。该方法接收error参数，返回一个对象来更新state，使组件在下次渲染时展示降级界面。它在渲染阶段调用，因此不允许副作用操作。}}

{{componentDidCatch生命周期方法}}{{componentDidCatch(error, info)在子组件抛出错误后调用，通常用于将错误信息上报到错误监控服务。参数error是抛出的错误对象，info包含componentStack字段提供组件栈追踪信息。该方法在提交阶段调用，允许执行副作用如发送错误报告。与getDerivedStateFromError配合使用构成完整的Error Boundary机制。}}

{{React 19错误处理钩子}}{{React 19引入了createRoot的新回调选项：onUncaughtError、onCaughtError和onRecoverableError。onUncaughtError捕获未被Error Boundary捕获的错误，onCaughtError处理已被Error Boundary捕获的错误，onRecoverableError处理React自动恢复的错误。这些钩子配合Sentry.reactErrorHandler()实现更精细的错误捕获和上报。}}

{{Sentry.ErrorBoundary组件}}{{@sentry/react提供的ErrorBoundary组件封装了React原生Error Boundary，内置错误上报能力。使用方式为<Sentry.ErrorBoundary fallback={<p>出错了</p>}>包裹应用内容。支持fallback、beforeCapture、showDialog等配置项。当子组件抛出错误时自动将错误信息发送到Sentry服务端。}}

{{window.onerror全局错误捕获}}{{window.addEventListener('error', handler)或window.onerror可捕获未被try-catch处理的JavaScript运行时错误。onerror处理器接收5个参数：message（错误消息）、source（脚本URL）、lineno（行号）、colno（列号）、error（Error对象）。addEventListener方式接收单个ErrorEvent对象。返回true可阻止错误在控制台显示，但脚本仍会停止执行。}}

{{unhandledrejection事件}}{{window.addEventListener('unhandledrejection', handler)用于捕获未处理的Promise拒绝。事件类型为PromiseRejectionEvent，包含promise（被拒绝的Promise对象）和reason（拒绝原因）两个只读属性。该事件可被cancelable，调用preventDefault()可阻止默认的控制台错误输出。注意跨域脚本的Promise拒绝不会触发此事件以防止数据泄露。}}

{{rejectionhandled事件}}{{window的rejectionhandled事件在Promise被拒绝后、延迟绑定catch处理器时触发。与unhandledrejection配合使用可实现完整的Promise错误处理生命周期：先通过unhandledrejection捕获未处理拒绝，再通过rejectionhandled检测后续是否补上了处理逻辑。常用于错误监控中区分真正未处理和延迟处理的Promise错误。}}

{{Sentry SDK初始化配置}}{{Sentry.init()必须在应用入口文件中作为第一个导入执行。核心配置包括dsn（数据源名称）、integrations（集成插件数组）、tracesSampleRate（采样率，1.0为100%捕获）、tracePropagationTargets（追踪传播目标URL正则数组）、enableLogs（日志功能开关）。建议创建独立的instrument.js文件并在main入口最顶部导入。}}

{{Sentry BrowserTracing集成}}{{Sentry.browserTracingIntegration()是Sentry的浏览器端性能追踪集成，自动捕获页面加载、导航和用户交互的性能数据。配置tracesSampleRate控制采样比例，生产环境建议设置为0.1-0.2以平衡性能和数据量。tracePropagationTargets用于控制哪些URL的请求需要进行分布式追踪传播。}}

{{Sentry Session Replay集成}}{{Sentry.replayIntegration()提供会话回放功能，录制用户操作过程的视频级回放。配置replaysSessionSampleRate控制会话采样率（如0.1表示10%的会话），replaysOnErrorSampleRate控制错误时采样率（1.0表示所有出错会话都录制）。有助于快速定位和复现用户遇到的问题。}}

{{Sentry User Feedback集成}}{{Sentry.feedbackIntegration()允许在用户遇到错误时收集反馈。用户可描述问题经过并提供上下文信息，帮助开发者理解问题。支持colorScheme配置（system/light/dark），可与Error Boundary的fallback UI集成展示反馈表单。}}

{{错误恢复策略-自动重试}}{{错误恢复策略之一是自动重试机制。在Error Boundary中维护重试计数器，当子组件出错时提供"重试"按钮，点击后重置组件状态重新渲染。适用于临时性错误如网络请求失败。需设置最大重试次数防止无限循环，一般建议3次。可结合指数退避策略增加重试间隔。}}

{{错误恢复策略-降级UI}}{{降级UI是Error Boundary的核心功能，当组件出错时展示备用界面替代崩溃的白屏。降级UI应包含：错误友好的提示信息、重试操作按钮、返回首页链接、可选的错误详情展开。设计原则是不暴露技术细节给用户，同时提供足够的恢复路径。可根据错误类型展示不同的降级UI。}}

{{错误恢复策略-组件隔离}}{{通过在应用不同层级放置Error Boundary实现错误隔离。页面级Error Boundary捕获整页错误，组件级Error Boundary隔离单个功能模块。这样某个组件的错误不会影响其他正常模块的渲染。推荐在路由层级、功能模块层级和关键组件层级分别设置Error Boundary。}}

{{try-catch与Error Boundary的区别}}{{try-catch用于捕获命令式JavaScript代码中的错误，适用于事件处理器、异步代码等。Error Boundary用于声明式捕获React组件树渲染过程中的错误。两者互补：try-catch处理过程式逻辑错误，Error Boundary处理声明式渲染错误。Error Boundary无法替代try-catch，反之亦然。}}

{{事件处理器中的错误处理}}{{React事件处理器中的错误不会被Error Boundary捕获，需要使用try-catch处理。在事件回调函数内部使用try-catch包裹可能出错的逻辑，catch中可更新组件状态显示错误信息。这是React设计决定：Error Boundary仅捕获渲染阶段的错误，事件处理器属于用户交互阶段。}}

{{异步代码错误处理}}{{async/await和Promise中的错误需要通过try-catch或.catch()方法处理。在React组件中，异步错误可通过setState更新组件状态来展示错误UI。未捕获的Promise拒绝会触发全局unhandledrejection事件。建议在全局层面监听unhandledrejection作为最后防线，同时在业务代码中做好局部错误处理。}}

{{Error Boundary与Suspense的协作}}{{Error Boundary与React Suspense可组合使用形成优雅的加载-错误处理模式。常见模式为<ErrorBoundary><Suspense fallback={<Loading/>}><Component/></Suspense></ErrorBoundary>。Suspense处理异步加载状态，Error Boundary处理错误状态。这种组合提供了完整的组件加载生命周期管理。}}

{{错误边界中的错误日志记录}}{{componentDidCatch是记录错误日志的理想位置。应记录的信息包括：error对象（错误消息和堆栈）、info.componentStack（组件栈）、当前路由、用户信息、环境信息（开发/生产）、时间戳。推荐集成Sentry、LogRocket等专业错误监控平台，同时保留本地日志作为备用。}}

{{自定义Error Boundary Hook}}{{useErrorBoundary是react-error-boundary库提供的Hook，允许在函数组件中使用Error Boundary的reset功能。返回resetBoundary函数可在自定义错误恢复逻辑中调用。配合useEffect监听props变化可实现自动化错误重置。社区库react-error-boundary提供了useErrorBoundary和withErrorBoundary等便捷API。}}

{{Vue 3错误处理机制}}{{Vue 3提供了app.config.errorHandler全局错误处理器，可捕获组件渲染函数和观察器中的错误。组件级使用onErrorCaptured生命周期钩子捕获子组件错误，类似React的Error Boundary。错误沿组件链向上传播，直到被某个onErrorCaptued捕获或到达全局处理器。返回false可阻止错误继续向上传播。}}

{{Angular ErrorHandler}}{{Angular通过ErrorHandler类提供全局错误处理机制。默认的ErrorHandler将错误记录到控制台。通过创建自定义ErrorHandler继承ErrorHandler类并重写handleError方法，可实现全局错误上报。使用providers注册自定义ErrorHandler替代默认实现。Angular还提供了HttpInterceptor处理HTTP请求错误。}}

{{Svelte错误处理}}{{Svelte 4.x通过onErrorEvent组件属性处理错误。Svelte 5引入了更完善的错误处理机制。SvelteKit提供了+error.svelte文件用于页面级错误展示，以及handleError服务端Hook处理服务端错误。Svelte的错误处理相对轻量级，主要依赖标准JavaScript的try-catch机制。}}

{{Next.js错误处理}}{{Next.js 13+的App Router引入了error.tsx文件作为路由段的错误边界，自动包裹该段的子组件。error.tsx必须是客户端组件（'use client'），接收error和reset两个props。not-found.tsx处理404错误。global-error.tsx处理根布局中的错误。中间件中的错误通过error.tsx全局捕获。}}

{{Source Map与错误定位}}{{生产环境代码经过压缩混淆后，错误堆栈中的行号列号与源码不匹配。Source Map文件记录了编译后代码与源码的映射关系，配合错误监控平台可还原到源码级别的错误位置。Sentry支持上传Source Map实现准确的错误定位。建议在CI/CD流程中自动生成并上传Source Map。}}

{{错误监控最佳实践-采样策略}}{{生产环境错误监控需要合理的采样策略。错误事件通常100%捕获（tracesSampleRate: 1.0）。性能追踪数据建议10%-20%采样以减少数据量和成本。Session Replay建议按需采样：10%常规会话，100%出错会话。高频重复错误应去重合并，避免数据膨胀。根据业务流量动态调整采样率。}}

{{错误监控最佳实践-告警规则}}{{合理配置告警规则是错误监控的关键。建议按以下维度配置：错误率突增告警（如5分钟内错误率超过1%）、新错误首次出现告警、关键路径错误即时告警（如支付、登录流程）、特定错误类型告警（如API超时）。告警应分级，避免告警疲劳。集成Slack、飞书等协作工具实现团队协同处理。}}

{{网络请求错误处理}}{{网络请求错误是前端最常见的异常类型。需要处理：HTTP状态码错误（4xx/5xx）、网络连接失败、请求超时、CORS错误。使用axios拦截器或fetch封装统一处理，区分网络错误和业务错误。超时配置建议30秒以内，关键接口可设置更短超时。离线时应展示友好的离线提示页面。}}

{{CSS和资源加载错误处理}}{{图片、字体、CSS等静态资源加载失败可通过元素的onerror事件捕获。img标签的onerror可设置备用图片。CSS加载失败可通过监听link标签的onerror事件或document.styleSheets检测。动态import()失败需要catch处理，展示加载失败提示。建议为关键资源提供CDN降级方案和本地缓存。}}

{{Web Worker错误处理}}{{Web Worker中的错误不会冒泡到主线程。通过worker.onerror事件监听Worker中的未捕获错误。worker.onmessageerror监听消息解析错误。Worker内部建议使用try-catch包裹业务逻辑，通过postMessage将错误信息发送回主线程处理。SharedWorker和Service Worker同样需要独立的错误处理机制。}}

{{CSP违规错误处理}}{{Content Security Policy（CSP）违规可通过SecurityPolicyViolationEvent事件监听。document.addEventListener('securitypolicyviolation', handler)可捕获违规信息，包括violatedDirective、blockedURI等字段。CSP违规通常意味着XSS攻击尝试或配置错误，应记录并告警。可通过report-uri或report-to指令将违规报告发送到指定端点。}}

{{错误边界性能影响}}{{Error Boundary本身对性能的影响极小。getDerivedStateFromError和componentDidCatch仅在子组件出错时触发，正常渲染时无额外开销。但过度嵌套Error Boundary会增加组件树深度，可能影响React协调性能。建议在关键隔离点设置Error Boundary，而非每个组件都包裹。Sentry等监控SDK的错误上报使用异步请求，不阻塞渲染。}}

{{生产环境与开发环境的错误处理差异}}{{开发环境中React默认展示详细的错误覆盖层（Error Overlay），包含错误堆栈和组件栈信息。生产环境中Error Boundary的降级UI替代错误覆盖层。Sentry建议仅在生产环境初始化，避免开发环境产生噪音数据。开发环境应使用严格模式（Strict Mode）提前发现潜在问题。可通过process.env.NODE_ENV区分环境。}}

{{错误分类与优先级}}{{前端错误可按严重程度分类：P0-应用崩溃白屏（需立即处理）、P1-核心功能不可用（如支付失败）、P2-非核心功能异常（如某个小功能报错）、P3-用户无感知的错误（如监控数据丢失）。不同级别的错误对应不同的告警和响应SLA。错误分类有助于团队合理分配资源处理问题。}}

{{错误监控数据隐私合规}}{{错误监控涉及用户数据收集，需遵守GDPR、CCPA等隐私法规。Sentry提供sendDefaultPii配置控制是否发送用户IP和请求头。应避免在错误日志中记录用户敏感信息（密码、token、身份证号等）。Sentry支持数据脱敏和beforeSend钩子过滤敏感数据。Session Replay应脱敏输入框内容。用户有权请求删除其错误数据。}}

{{微前端错误处理策略}}{{微前端架构中错误处理需要多层隔离。子应用内部使用各自的Error Boundary捕获渲染错误。基座应用使用全局错误处理捕获子应用加载和通信错误。使用Sentry的Tags区分不同子应用的错误来源。子应用间的错误不应相互影响。建议每个子应用独立配置错误监控，同时汇总到统一的错误管理平台。}}

{{服务端渲染(SSR)错误处理}}{{SSR场景下错误处理更复杂。服务端渲染时的错误不会被客户端Error Boundary捕获，需要在服务端使用try-catch包裹渲染逻辑。Next.js的error.tsx自动处理客户端和服务端的错误边界。SSR错误可降级到客户端渲染（Client-Side Rendering）作为恢复策略。需注意服务端和客户端错误监控的差异性。}}

{{PWA离线错误处理}}{{Progressive Web App需要特殊处理离线场景下的错误。Service Worker的fetch事件应处理网络请求失败情况，返回缓存内容或离线页面。navigator.onLine检测网络状态变化，及时更新UI提示用户。后台同步（Background Sync）失败应记录并在网络恢复后重试。离线状态下的操作应本地缓存，待恢复后同步。}}

{{错误监控平台对比}}{{2025年主流前端错误监控平台包括：Sentry（开源、功能全面、支持多框架）、Datadog RUM（全栈监控集成）、LogRocket（会话回放为主）、Bugsnag（稳定性评分）、Rollbar（实时错误追踪）。选择标准包括：SDK体积、采样能力、Source Map支持、告警灵活性、团队协作功能、价格。Sentry因开源和功能全面性仍是社区首选。}}

{{错误处理测试策略}}{{确保错误处理逻辑的正确性需要专门的测试。使用React Testing Library测试Error Boundary：渲染会抛错的组件，验证降级UI是否展示。Mock API请求返回错误状态码测试错误提示。使用Cypress或Playwright测试用户可感知的错误恢复流程。使用MSW（Mock Service Worker）模拟各种网络异常场景。}}

{{渐进增强的错误处理}}{{渐进增强理念下，错误处理应从基础功能开始逐步增强。基础层：JavaScript全局错误捕获（window.onerror + unhandledrejection）。框架层：Error Boundary或等价机制。业务层：接口错误统一处理。监控层：集成错误监控平台。每一层独立工作，某层失败不影响其他层。确保即使监控SDK加载失败，基础错误处理仍有效。}}
