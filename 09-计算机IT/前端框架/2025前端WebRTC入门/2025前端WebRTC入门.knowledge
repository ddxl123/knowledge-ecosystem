# 2025前端WebRTC入门
# 领域: 计算机IT
# 学科: 前端框架
# 来源: 2025前端WebRTC入门
# 描述: WebRTC核心知识点，涵盖RTCPeerConnection、信令、ICE、音视频传输、数据通道、屏幕共享等

{{WebRTC定义与概述}}
WebRTC（Web Real-Time Communication）是一组支持浏览器之间进行实时音视频通信和任意数据交换的开放标准API。无需安装插件或第三方软件，即可在对等端之间建立点对点连接。WebRTC由W3C和IETF标准化，主要包括RTCPeerConnection、RTCDataChannel和MediaStream等核心接口。截至2025年，所有主流浏览器均已全面支持WebRTC。

{{WebRTC核心架构}}
WebRTC的架构由三个核心层组成：1）媒体层：负责音视频的采集、编码、传输和渲染；2）传输层：基于ICE/STUN/TURN实现NAT穿透，使用SRTP传输音视频，SCTP传输数据；3）信令层：WebRTC本身不定义信令协议，需要通过外部信令服务器（如WebSocket、HTTP）交换SDP和ICE候选。这种分层设计使WebRTC既灵活又强大。

{{RTCPeerConnection概述}}
RTCPeerConnection是WebRTC的核心接口，代表本地设备与远程对等端之间的连接。它负责建立和维护点对点连接，处理音视频流的传输、ICE候选的收集和交换、以及连接状态的管理。创建方式：new RTCPeerConnection(configuration)，其中configuration可包含ICE服务器配置、证书等参数。

{{RTCPeerConnection构造函数}}
RTCPeerConnection()构造函数接受一个可选的RTCConfiguration配置对象。主要配置项包括：iceServers（STUN/TURN服务器数组）、iceTransportPolicy（传输策略：all或relay）、bundlePolicy（捆绑策略：balanced、max-compat、max-bundle）、rtcpMuxPolicy（RTCP复用策略）。示例：const pc = new RTCPeerConnection({ iceServers: [{ urls: 'stun:stun.l.google.com:19302' }] })。

{{RTCPeerConnection连接状态}}
RTCPeerConnection维护多个状态属性：connectionState（new/connecting/connected/disconnected/failed/closed）、iceConnectionState（new/checking/connected/completed/failed/disconnected/closed）、iceGatheringState（new/gathering/complete）、signalingState（stable/have-local-offer/have-remote-offer/have-local-pranswer/have-remote-pranswer/closed）。开发者应监听connectionstatechange事件来跟踪连接状态变化。

{{信令机制（Signaling）}}
信令是WebRTC连接建立前的协商过程，用于交换会话描述信息（SDP）和网络连接信息（ICE候选）。WebRTC标准本身不规定信令协议，开发者可自由选择WebSocket、HTTP长轮询、Socket.io等方案。信令流程：创建Offer→通过信令服务器发送→对方创建Answer→回传→双方交换ICE候选→连接建立。

{{会话描述协议（SDP）}}
SDP（Session Description Protocol）是WebRTC中用于描述多媒体会话的文本协议。SDP包含：媒体类型（audio/video）、编解码器信息（H.264、VP8、VP9、AV1等）、传输地址、带宽信息等。在WebRTC中通过RTCSessionDescription对象表示，包含type（offer/answer）和sdp（SDP文本）两个属性。Offer由发起方创建，Answer由应答方创建。

{{SDP Offer/Answer模型}}
WebRTC使用Offer/Answer模型进行会话协商。流程：1）调用方调用createOffer()生成本地SDP描述；2）调用setLocalDescription()设置本地描述；3）通过信令服务器将Offer发送给对方；4）对方调用setRemoteDescription()设置远程描述；5）对方调用createAnswer()生成应答；6）对方setLocalDescription()并回传Answer；7）调用方setRemoteDescription()。完成后双方就媒体参数达成一致。

{{ICE框架}}
ICE（Interactive Connectivity Establishment）是WebRTC用于NAT穿透的框架。ICE收集所有可用的网络传输地址（本地IP、STUN反射地址、TURN中继地址），形成ICE候选对，通过连通性检查找到最佳传输路径。ICE解决了NAT和防火墙环境下P2P连接困难的问题。

{{ICE候选（ICE Candidates）}}
ICE候选是可能的网络传输地址，分为三种类型：1）Host候选：本地网络接口地址；2）Server Reflexive（srflx）候选：通过STUN服务器获取的公网地址；3）Relay候选：通过TURN服务器获取的中继地址。ICE代理会逐步收集这些候选并通过onicecandidate事件返回，需通过信令服务器交换给对方。

{{STUN服务器}}
STUN（Session Traversal Utilities for NAT）服务器帮助客户端发现其公网IP和端口。当客户端位于NAT后面时，STUN服务器通过映射关系告知客户端其外部可达地址。STUN服务器轻量且免费，能解决大多数对称NAT以外的NAT场景。常用的免费STUN服务器：stun:stun.l.google.com:19302。

{{TURN服务器}}
TURN（Traversal Using Relays around NAT）服务器在P2P连接失败时充当中继服务器，转发音视频数据。当双方位于对称NAT或严格防火墙后时，直接P2P连接不可行，TURN服务器作为备选方案确保连接可用。TURN服务器带宽消耗大，需要部署和运维成本。实际项目中应优先尝试STUN直连，仅在失败时降级到TURN中继。

{{ICE代理与连通性检查}}
ICE代理在收集到本地和远程ICE候选后，会生成候选对（candidate pairs）并进行连通性检查。检查过程：向每个候选对发送STUN绑定请求，收到响应即表示该路径可达。ICE代理根据优先级选择最佳候选对，优先选择延迟低、带宽高的直连路径。连通性检查完成后，ICE状态变为completed。

{{获取用户媒体流（getUserMedia）}}
navigator.mediaDevices.getUserMedia(constraints)用于获取用户的摄像头和麦克风输入。constraints参数指定请求的媒体类型及其约束条件。返回Promise，解析为MediaStream对象。constraints示例：{ video: { width: 1280, height: 720, frameRate: 30 }, audio: true }。调用时浏览器会弹出权限请求对话框，用户同意后才能获取媒体流。

{{getUserMedia约束条件}}
getUserMedia的约束条件（Constraints）支持精细的媒体控制。视频约束包括：width/height（分辨率）、frameRate（帧率）、facingMode（前置/后置摄像头）、deviceId（指定设备）。音频约束包括：echoCancellation（回声消除）、noiseSuppression（噪声抑制）、autoGainControl（自动增益）、sampleRate（采样率）。使用ideal值可设置理想值而非强制值。

{{媒体流（MediaStream）}}
MediaStream表示来自音视频源的媒体数据流，包含一个或多个MediaStreamTrack。主要属性：id（唯一标识）、active（是否活跃）、tracks（所有轨道数组）。主要方法：getTracks()、getAudioTracks()、getVideoTracks()、addTrack()、removeTrack()。事件：onaddtrack、onremovetrack。MediaStream可直接赋给HTML的video/audio元素的srcObject属性进行播放。

{{媒体轨道（MediaStreamTrack）}}
MediaStreamTrack是媒体流中的单个轨道，代表一个音频或视频源。主要属性：kind（audio/video）、id、label、enabled（是否启用）、readyState（live/ended）。方法：clone()、stop()、getSettings()、getCapabilities()、applyConstraints()。通过enabled属性可以静音或关闭视频而不中断连接。

{{向连接添加媒体轨道}}
使用RTCPeerConnection.addTrack()方法将MediaStreamTrack添加到连接中。addTrack(track, stream)接受轨道和可选的流作为参数，返回RTCRtpSender对象。也可以使用addTransceiver()方法创建收发器，支持更灵活的"先添加轨道后设置方向"的用法。在接收端，track事件会在新轨道到达时触发。

{{屏幕共享（Screen Sharing）}}
navigator.mediaDevices.getDisplayMedia(constraints)用于捕获屏幕、窗口或浏览器标签页。返回Promise，解析为MediaStream。主要选项：video（必须为true）、audio（可选，捕获系统音频）。高级选项：preferCurrentTab（优先当前标签页）、selfBrowserSurface（是否允许选择当前标签页）、monitorTypeSurfaces（是否包含整个屏幕）。屏幕共享流可像摄像头流一样添加到RTCPeerConnection中传输。

{{屏幕共享高级选项}}
getDisplayMedia支持多个高级配置：controller（CaptureController实例，用于进一步控制捕获会话）、surfaceSwitching（是否允许运行时切换共享的标签页）、systemAudio（是否包含系统音频）、windowAudio（窗口音频共享选项：exclude/window/system）。这些选项可提供更好的用户体验，如标签页切换控制和音频共享粒度控制。

{{RTCDataChannel概述}}
RTCDataChannel提供浏览器之间的双向数据传输通道，可传输任意类型的数据（文本、二进制）。基于SCTP协议，支持可靠和不可靠传输模式。每个RTCPeerConnection最多支持约65534个数据通道。数据通道适用于：文件传输、游戏状态同步、聊天消息、实时协作编辑等低延迟数据交换场景。

{{创建数据通道}}
通过RTCPeerConnection.createDataChannel(label, options)创建数据通道。label是通道名称。options配置项：ordered（是否保序，默认true）、maxPacketLifeTime（最大传输时间ms）、maxRetransmits（最大重传次数）、protocol（子协议）、negotiated（是否由应用层协商，默认false）。设置ordered:false且maxRetransmits:0可实现类似UDP的不可靠传输。

{{数据通道传输模式}}
数据通道支持多种传输模式：1）可靠有序传输（ordered:true，默认）：类似TCP，保证消息按序到达；2）可靠无序传输（ordered:false, maxRetransmits未设置）：消息必达但不保证顺序；3）部分可靠传输（maxRetransmits或maxPacketLifeTime设置）：允许丢包以换取低延迟；4）完全不可靠（maxRetransmits:0）：类似UDP，不重传。根据应用需求选择合适的传输模式。

{{数据通道发送与接收}}
发送数据使用channel.send(data)，支持String、Blob、ArrayBuffer、ArrayBufferView类型。接收数据监听message事件：channel.onmessage = (event) => { /* event.data */ }。binaryType属性决定接收二进制数据时的类型（'blob'或'arraybuffer'）。注意：需在setRemoteDescription之后创建的数据通道才能发送数据。

{{数据通道状态管理}}
数据通道的readyState有四种状态：connecting（正在建立连接）、open（已就绪，可发送数据）、closing（正在关闭）、closed（已关闭）。主要事件：open（通道就绪可发送）、message（收到数据）、close（通道关闭）、error（发生错误）、bufferedamountlow（缓冲区低于阈值）。应监听open事件确认通道可用后再发送数据。

{{ICE服务器配置}}
RTCPeerConnection的iceServers配置指定STUN/TURN服务器。格式：{ urls: 'stun:stun.example.com:3478' }或{ urls: 'turn:turn.example.com:3478', username: 'user', credential: 'pass' }。可配置多个服务器作为备选。iceTransportPolicy可设为'relay'强制使用TURN中继。生产环境建议部署自有TURN服务器以保证连接可靠性和安全性。

{{WebRTC连接建立完整流程}}
完整的WebRTC连接流程：1）双方创建RTCPeerConnection实例并配置ICE服务器；2）调用方获取本地媒体流并添加轨道；3）调用方createOffer()→setLocalDescription()→发送Offer；4）被调用方setRemoteDescription()→获取媒体流→createAnswer()→setLocalDescription()→发送Answer；5）调用方setRemoteDescription()；6）双方交换ICE候选（通过onicecandidate事件收集）；7）ICE连通性检查完成，连接建立。

{{连接关闭与资源释放}}
正确关闭WebRTC连接至关重要。步骤：1）停止所有MediaStreamTrack（track.stop()）；2）关闭所有RTCDataChannel（channel.close()）；3）调用RTCPeerConnection.close()关闭连接；4）移除video/audio元素的srcObject引用。未正确释放资源会导致摄像头指示灯常亮、内存泄漏等问题。建议在页面beforeunload事件中处理清理逻辑。

{{WebRTC安全机制}}
WebRTC内置多层安全保护：1）强制HTTPS：getUserMedia仅在安全上下文中可用；2）DTLS加密：所有数据传输通过DTLS加密；3）SRTP：音视频通过SRTP（加密的RTP）传输；4）权限控制：需用户明确授权才能访问摄像头和麦克风；5）ICE候选项中的IP地址经过混淆处理。开发者还应实施应用层认证和授权机制。

{{WebRTC统计信息}}
RTCPeerConnection.getStats()返回RTCStatsReport，包含丰富的连接统计数据。可用于监控：音视频码率、丢包率、RTT延迟、编解码器信息、ICE候选对详情、数据通道吞吐量等。典型应用场景：实时监控通话质量、自适应码率调整、故障诊断。getStats()可传入特定track或sender来获取单个轨道的统计。

{{adapter.js兼容性库}}
adapter.js是WebRTC的兼容性垫片库，由WebRTC社区维护。它封装了不同浏览器之间的API差异（Chrome、Firefox、Safari等），使开发者无需关心浏览器兼容性问题。2025年虽然主流浏览器已高度统一，但adapter.js仍然推荐使用以处理边缘情况和新API的渐进支持。

{{WebRTC在前端框架中的应用}}
现代前端框架（React、Vue、Angular）中集成WebRTC的模式：1）封装自定义Hook/Composable管理连接生命周期；2）使用Context/Provider管理全局媒体状态；3）将音视频元素与框架的声明式渲染结合；4）使用WebRTC事件驱动更新UI状态。推荐将WebRTC逻辑与UI逻辑分离，便于测试和维护。

{{常见WebRTC应用场景}}
WebRTC的典型应用场景：1）视频会议：1对1或多人音视频通话；2）直播连麦：主播与观众实时互动；3）在线教育：师生实时音视频互动与屏幕共享；4）远程协作：屏幕共享、文件传输、实时编辑；5）游戏：低延迟数据通道同步游戏状态；6）IoT：浏览器与智能设备的实时通信；7）P2P文件传输：利用数据通道实现高速文件共享。

{{WebRTC多人通话架构}}
多人WebRTC通话通常采用两种架构：1）Mesh架构：每两个参与者之间建立P2P连接，适合3-5人小规模通话，但连接数随人数平方增长；2）SFU（Selective Forwarding Unit）架构：所有参与者向中心服务器发送媒体流，服务器选择性转发给其他参与者，适合大规模通话。生产环境推荐使用SFU方案，如mediasoup、Janus、Jitsi等开源媒体服务器。

{{WebRTC与WebSocket的关系}}
WebRTC和WebSocket是互补的技术。WebSocket提供全双工的客户端-服务器通信，适合信令传输、状态同步等场景。WebRTC提供点对点的音视频和数据传输，适合实时媒体通信。典型的WebRTC应用中，WebSocket用于信令（交换SDP和ICE候选），WebRTC用于实际的媒体和数据传输。两者分工明确，各司其职。

{{WebRTC调试技巧}}
WebRTC调试工具：1）chrome://webrtc-internals：Chrome内置的WebRTC调试页面，查看连接状态、统计信息、SDP详情；2）about:webrtc：Firefox的等效工具；3）getStats() API：编程方式获取统计数据；4）Wireshark：抓包分析RTP/SRTP/DTLS流量；5）console日志：监听各类事件打印状态变化。调试时重点关注ICE状态、信令状态和连接状态的变化顺序。

{{WebRTC 2025新特性与趋势}}
2025年WebRTC的重要发展：1）Insertable Streams / Encoded Transform：支持端到端加密和自定义媒体处理；2）WebCodecs API：提供更底层的编解码器控制，与WebRTC配合实现自定义媒体管线；3）WASM编解码：使用WebAssembly实现自定义编解码器；4）WebTransport：新的传输协议，可与WebRTC互补；5）QUIC集成：基于QUIC的WebRTC数据传输标准化进行中；6）AI增强：实时语音降噪、背景虚化等AI能力的浏览器原生集成。
