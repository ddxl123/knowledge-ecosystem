# 2025前端前端CSS Scope
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}{{@scope 是什么？@scope 是 CSS 的一个 at-rule（@规则），允许将样式规则限定在文档的特定部分范围内，通过选择器描述上界和下界来定义作用域。它属于 CSS Cascading and Level 6 规范的一部分。}}

{{知识点2}}{{@scope 的基本语法——独立块形式：@scope (scope-root) to (scope-limit) { ... }。scope-root 定义作用域的上界（包含该元素），scope-limit 定义作用域的下界（排除该元素），形成一个"甜甜圈"形状的作用域范围。}}

{{知识点3}}{{@scope 的内联形式：当 <style> 元素放在某个 HTML 元素内部时，可以省略 prelude，直接写 @scope { ... }，此时作用域自动限定为该 <style> 元素的父元素。也可以写 @scope to (scope-limit) { ... } 来只指定下界。}}

{{知识点4}}{{甜甜圈作用域（Donut Scope）：@scope 最独特的特性之一是支持设置下界限制。例如 @scope (.article-body) to (figure) 表示样式只作用于 .article-body 内部、但不在 <figure> 内部的元素，形成一个环形作用域。}}

{{知识点5}}{{上界和下界的包含性：默认情况下上界是包含的（inclusive），下界是排除的（exclusive）。可以通过组合选择器改变：@scope (.root > *) to (.limit) 让上界排除；@scope (.root) to (.limit > *) 让下界包含；@scope (.root > *) to (.limit > *) 让上界排除、下界包含。}}

{{知识点6}}{{:scope 伪类在 @scope 中的用法：在 @scope 块内，:scope 伪类可以直接选中作用域根元素。例如 @scope (.card) { :scope { background: white; } } 等同于给 .card 设置白色背景。}}

{{知识点7}}{{@scope 内的特异性（Specificity）：在 @scope 规则内部，裸选择器和嵌套选择器 & 的行为等同于前面加了 :where(:scope)，即 :where() 的零特异性。因此 @scope (.card) { img { } } 中 img 的特异性仅为 0-0-1。}}

{{知识点8}}{{:scope 的特异性：显式使用 :scope 会增加类级别特异性 0-1-0。例如 @scope (.card) { :scope img { } } 的特异性为 0-1-1（0-1-0 + 0-0-1）。}}

{{知识点9}}{{作用域邻近度（Scoping Proximity）：@scope 为 CSS 级联引入了新标准——作用域邻近度。当两个作用域的样式冲突时，距离作用域根 DOM 层级跳数更少的那个获胜。这优先于源码顺序但低于 !important、layer 和特异性。}}

{{知识点10}}{{作用域邻近度解决嵌套主题问题：当 .light-theme 和 .dark-theme 主题嵌套时，最内层元素会根据距离最近的主题根来决定样式，而非依赖源码顺序，从而正确应用主题。}}

{{知识点11}}{{@scope 与 CSS 级联的关系：@scope 引入的 scoping proximity 位于 CSS 级联的特异性和源码顺序之间。优先级从低到高：源码顺序 < 作用域邻近度 < 特异性 < layers < !important。}}

{{知识点12}}{{@scope 不完全隔离继承：@scope 限制选择器匹配范围，但不影响 CSS 继承属性。可继承属性（如 color、font-family）仍会传播到作用域限制之外的子元素。}}

{{知识点13}}{{选择器列表（Selector List）支持：@scope 的上界和下界都支持选择器列表。例如 @scope (.hero, .body) to (figure) 表示样式适用于 .hero 或 .body 内的元素，但排除 <figure> 内部。}}

{{知识点14}}{{:scope 在下界中的特殊用法：下界可以使用 :scope 表达与根的特定关系。例如 @scope (.body) to (:scope > figure) 表示只有直接子元素的 <figure> 才是下界限制。}}

{{知识点15}}{{:scope 引用外部元素：下界选择器可以通过 :scope 引用作用域根在外部的位置。例如 @scope (.body) to (.feature :scope figure) 表示只有当 :scope 在 .feature 内部时，figure 才作为下界。}}

{{知识点16}}{{@scope 内的选择器不能逃逸子树：@scope 内的选择器不能匹配到作用域根之外的元素。例如 :scope + p 是无效的，因为它会选到作用域根的兄弟元素。}}

{{知识点17}}{{@scope 与 Shadow DOM 的对比：Shadow DOM 提供真正的 DOM 级隔离，而 @scope 提供选择器级别的作用域限定。@scope 不创建新的 DOM 树，也不阻断全局样式继承，但使用更简单、无需自定义元素。}}

{{知识点18}}{{@scope 与 BEM 方法论的关系：BEM 通过命名约定（.block__element--modifier）避免样式冲突，@scope 通过语言层面的作用域限制达到类似效果。@scope 可以替代 BEM 的命名约束，减少类名冗长。}}

{{知识点19}}{{@scope 与 CSS Modules 的对比：CSS Modules 在构建时生成唯一类名实现隔离，@scope 是原生 CSS 方案。@scope 无需构建工具，但 CSS Modules 的隔离更彻底（包括防止继承溢出）。两者可互补使用。}}

{{知识点20}}{{@scope 的浏览器支持状态（截至2025）：Chrome 118+、Edge 118+、Safari 17.4+、Firefox 146+ 已全面支持。所有主流浏览器均已支持 @scope，可以放心在生产环境使用。}}

{{知识点21}}{{@scope 与 :where() 的零特异性特性：@scope 内部使用 :where(:scope) 前缀机制意味着作用域选择器本身不增加特异性。这使得样式更容易被覆盖，但显式使用 :scope 伪类时会增加 0-1-0 特异性。}}

{{知识点22}}{{内联 @scope 的使用场景：在 Web Components 或微前端中，可以将 <style>@scope { ... }</style> 直接放在组件模板内，实现零配置的样式隔离，无需 Shadow DOM。}}

{{知识点23}}{{@scope 实现组件样式隔离：通过 @scope (.my-component) { ... } 可以将样式限制在特定组件类内，防止全局样式污染。配合下界限制还可以防止嵌套子组件被误影响。}}

{{知识点24}}{{@scope 解决第三方样式冲突：引入第三方组件库时，可以用 @scope 限制其样式不泄漏到应用其他部分。例如 @scope (.third-party-widget) { ... } 包裹第三方样式。}}

{{知识点25}}{{@scope 的嵌套使用：@scope 可以与 CSS Nesting 结合使用，在嵌套语法中进一步细化作用域，实现更精细的样式控制。}}

{{知识点26}}{{@scope 的形式化语法：@scope = @scope <scope-boundaries>? { <block-contents> }。其中 <scope-boundaries> = [ (<scope-start>) ]? [ to (<scope-end>) ]?，<scope-start> 和 <scope-end> 都是 <selector-list>。}}

{{知识点27}}{{@scope 与 CSS Cascade Layers 的关系：@scope 作用于同一 cascade layer 内时，scoping proximity 作为额外的级联判断标准生效。在不同 layer 之间，layer 优先级高于 scoping proximity。}}

{{知识点28}}{{@scope 的实际应用——主题系统：利用 scoping proximity 特性，可以构建自动响应嵌套层级的主题系统，内层主题始终胜出，无需手动管理特异性或 z-index 式的优先级。}}

{{知识点29}}{{@scope 的实际应用——文章排版：限定文章内容区的样式不影响侧边栏或页脚。例如 @scope (.article-content) { p { line-height: 1.8; } } 只影响文章正文段落。}}

{{知识点30}}{{@scope 的实际应用——卡片组件嵌套：当卡片组件可以递归嵌套时，@scope 确保每层卡片的样式只作用于自身内容，不穿透到内部嵌套的卡片中。}}

{{知识点31}}{{@scope 与 style scoped 属性的区别：早期 HTML 有 <style scoped> 属性，已被废弃。@scope 是 CSS 层面的全新实现，功能更强（支持下界限制），已被标准化并获主流浏览器支持。}}

{{知识点32}}{{@scope 在设计系统中的价值：设计系统中组件经常多层嵌套（如 Button > Icon > Badge），@scope 可以精确控制每层的样式范围，避免深层嵌套中的样式意外覆盖。}}

{{知识点33}}{{@scope 的可访问性考虑：使用 @scope 不影响元素的可访问性语义，仅控制样式匹配。辅助技术仍能正常识别被作用域限制的元素。}}

{{知识点34}}{{@scope 的性能特性：@scope 在浏览器样式计算阶段生效，与传统选择器相比不会有显著性能差异。浏览器只需额外检查元素是否在作用域范围内。}}

{{知识点35}}{{@scope 渐进增强策略：对于需要支持旧浏览器的场景，可以将 @scope 规则放在 @supports at-rule 中检测，或使用 PostCSS 插件进行编译降级。}}

{{知识点36}}{{@scope 的多重作用域根示例：@scope (.theme-a, .theme-b) to (.nested-theme) { ... } 可以同时定义多个作用域根，所有匹配的根元素共享相同的样式规则和下界限制。}}

{{知识点37}}{{@scope 对 CSS-in-JS 方案的影响：@scope 作为原生 CSS 作用域方案，减少了对 styled-components 或 emotion 等 CSS-in-JS 库的依赖，有助于减少运行时 JavaScript 开销。}}

{{知识点38}}{{@scope 规范状态：@scope 属于 W3C CSS Cascading and Inheritance Level 6 规范（Working Draft），由 Miriam Suzanne 主导推动，已在 2023 年底前在 Chrome、Safari 中实现，2024 年 Firefox 跟进支持。}}
