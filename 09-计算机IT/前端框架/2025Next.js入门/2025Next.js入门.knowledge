# 2025Next.js入门
# 领域: 计算机IT
# 学科: 前端框架
# 来源: 2025Next.js入门
# 描述: Next.js核心知识点

{{Next.js是什么}}{{Next.js是基于React的全栈Web框架，由Vercel开发维护。它在React基础上提供了路由、渲染策略、数据获取、优化等开箱即用的能力，让开发者无需从零配置即可构建生产级应用。2025年最新版本为Next.js 15，引入了Turbopack稳定版、改进的缓存策略等特性。}}

{{App Router核心概念}}{{App Router是Next.js 13+引入的新一代路由系统，基于文件系统约定。在app目录下，每个文件夹代表一个路由段，page.tsx定义页面UI，layout.tsx定义共享布局，loading.tsx定义加载状态，error.tsx定义错误边界。App Router取代了传统的pages目录路由，支持React Server Components、嵌套布局、流式渲染等现代特性。}}

{{文件系统路由}}{{Next.js使用文件系统作为路由约定。app/about/page.tsx对应/about路由，app/blog/[slug]/page.tsx对应动态路由/blog/:slug。支持静态路由、动态路由[slug]、可选动态路由[[...slug]]、路由组(folderName)、平行路由(@name)、拦截路由((..)path)等多种路由模式。}}

{{Server Components（服务器组件）}}{{Server Components是App Router的默认组件类型。服务器组件在服务端渲染，不发送JavaScript到客户端，减少bundle大小。它们可以直接访问数据库、文件系统等服务器资源。在app目录下的组件默认是Server Component，如需客户端交互需显式标注"use client"。}}

{{Client Components（客户端组件）}}{{在文件顶部添加"use client"指令即可声明客户端组件。Client Components可以使用useState、useEffect等React Hooks，处理浏览器API、事件监听等交互逻辑。关键原则：尽可能将组件保持为Server Component，只在需要交互时才转为Client Component，并尽量将客户端组件推到组件树的叶子节点。}}

{{渲染策略概述}}{{Next.js支持多种渲染策略：SSR（服务端渲染）在请求时生成HTML；SSG（静态站点生成）在构建时生成HTML；ISR（增量静态再生）结合两者优势；Streaming支持流式传输页面内容。App Router默认使用Server Components，根据数据获取方式自动选择最佳渲染策略。}}

{{SSR服务端渲染}}{{SSR在每次请求时在服务器上执行组件渲染，生成完整HTML发送给客户端。适用于内容频繁变化或需要个性化数据的页面。在App Router中，使用不缓存的数据获取方式（如fetch不设置缓存、使用cookies/headers等动态API）会自动触发SSR。}}

{{SSG静态站点生成}}{{SSG在构建时（build time）预渲染页面为静态HTML，部署后可直接由CDN分发，速度极快。适用于内容不频繁变化的页面，如博客文章、营销页面。在App Router中，默认情况下不使用动态API的Server Component会在构建时静态渲染。}}

{{ISR增量静态再生}}{{ISR允许在构建后按需或定时更新静态页面，无需重新部署。通过在fetch请求中设置revalidate选项实现：fetch('...', { next: { revalidate: 3600 } })表示最多3600秒重新验证。也可使用revalidatePath或revalidateTag在服务端操作后按需触发页面重新生成。}}

{{Streaming流式渲染}}{{Streaming允许服务器逐步将页面HTML发送到客户端，而非等待整个页面渲染完成。Next.js通过loading.tsx和React Suspense实现流式渲染：先显示加载状态，数据就绪后自动替换为实际内容。这显著改善了TTFB（首字节时间）和用户感知加载速度。}}

{{数据获取fetch}}{{App Router中推荐在Server Component直接使用fetch获取数据。Next.js扩展了原生fetch API，添加了缓存和重新验证选项：{ cache: 'force-cache' }用于缓存（等同SSG），{ cache: 'no-store' }禁用缓存（等同SSR），{ next: { revalidate: N } }用于ISR。相同URL的fetch在渲染期间会自动去重。}}

{{Server Actions（服务器操作）}}{{Server Actions是定义在服务端的异步函数，可直接在Client或Server Component中调用，无需手动创建API端点。通过在函数上方添加"use server"指令声明。常用于表单提交、数据变更等场景。Next.js自动处理序列化、错误处理和渐进增强，支持与form的action属性直接配合使用。}}

{{API Routes（路由处理器）}}{{在app目录下创建route.ts文件即可定义API端点。导出对应HTTP方法的函数：GET、POST、PUT、DELETE等。例如app/api/users/route.ts处理/api/users请求，app/api/users/[id]/route.ts处理/api/users/:id。支持Request和Response Web标准API，也可使用NextResponse便捷方法。}}

{{中间件Middleware}}{{在项目根目录创建middleware.ts定义中间件，在请求完成前执行逻辑。可实现重定向、改写路径、认证检查、请求头修改等功能。使用NextResponse的redirect、rewrite、setHeaders等方法。通过matcher配置限定中间件作用范围，避免全局执行影响性能。}}

{{Layout布局系统}}{{layout.tsx定义路由段的共享UI，在导航时不会重新渲染，保留状态。支持嵌套布局：app/layout.tsx是根布局（必须），app/dashboard/layout.tsx是/dashboard的子布局。根布局必须包含<html>和<body>标签。布局可以接收children和params等参数。}}

{{Loading和Error处理}}{{loading.tsx在页面数据加载时自动显示，配合Suspense实现流式UI。error.tsx定义路由段的错误边界，必须是Client Component，接收error和reset参数。not.tsx处理404状态。global-error.tsx捕获根布局中的错误。这些约定文件让错误处理和加载状态管理变得声明式。}}

{{Metadata元数据管理}}{{通过导出metadata对象或generateMetadata函数来管理页面的SEO元数据。支持title、description、openGraph、twitter等丰富配置。静态元数据直接导出对象，动态元数据通过异步函数根据params或数据生成。Next.js自动处理meta标签的合并和继承。}}

{{Link组件导航}}{{使用next/link组件实现客户端导航，避免整页刷新。Link组件预获取可视区域内链接的页面资源，实现近乎即时的页面切换。基本用法：<Link href="/about">About</Link>。支持动态路由、查询参数、滚动控制、替换历史记录等选项。App Router中Link不再需要包裹<a>标签。}}

{{图片优化Image}}{{next/image组件提供自动图片优化：懒加载、响应式尺寸、WebP/AVIF格式转换、防止CLS（布局偏移）。支持本地图片和远程图片。关键属性：width/height或fill（填充父容器）、alt替代文本、priority标记首屏图片跳过懒加载、sizes定义响应式断点。}}

{{字体优化Font}}{{next/font模块支持自定义字体零布局偏移加载。支持Google Fonts自动优化和本地字体导入。使用next/font/google或next/font/local，在组件中调用返回的函数获取className。字体在构建时自动下载并内联，消除外部字体请求的CLS问题。}}

{{样式方案CSS Modules}}{{Next.js原生支持CSS Modules，将.module.css文件自动转换为局部作用域样式，避免类名冲突。在组件中import styles from './style.module.css'后通过className={styles.container}使用。也支持全局CSS（在layout.tsx中import）、Tailwind CSS集成等方案。}}

{{环境变量}}{{Next.js支持.env.local、.env.development、.env.production等环境变量文件。服务端变量默认不暴露给客户端。客户端变量必须以NEXT_PUBLIC_前缀开头，会被内联到浏览器bundle中。在server端代码中通过process.env访问，在middleware和路由处理器中同理。}}

{{Route Handlers高级用法}}{{Route Handlers支持请求流、流式响应、Cookies和Headers操作。通过NextRequest和NextResponse提供丰富API。支持Request.cookies和Response.cookies读写Cookie，Request.headers读取请求头。可返回JSON、HTML、流式数据等多种响应格式。配合revalidateTag和revalidatePath实现缓存失效。}}

{{数据缓存策略}}{{Next.js的缓存分多层：请求记忆化（同一渲染中相同fetch自动去重）、数据缓存（跨请求持久化fetch结果）、全路由缓存（静态渲染结果缓存）、路由缓存（客户端导航缓存）。通过revalidate、cache选项、动态函数调用来控制缓存行为。理解缓存层次对性能优化至关重要。}}

{{Parallel Routes平行路由}}{{平行路由允许在同一布局中同时渲染多个页面。通过@folder约定定义命名插槽，在layout.tsx中作为props接收。常用于仪表板、模态框等场景。支持独立的loading和error处理。在模态框场景中，平行路由可以让模态框有独立URL，支持浏览器前进后退。}}

{{Intercepting Routes拦截路由}}{{拦截路由允许在当前路由上下文中加载另一个路由的内容，常用于模态框展示图片或详情页。语法：(..)拦截上一层、(...)拦截根目录、(.…)拦截同级。结合平行路由使用，实现类似Instagram的点击图片弹出详情、同时URL变化的效果。}}

{{Serverless Functions部署}}{{Next.js的API Routes和Server Actions在Vercel上自动部署为Serverless Functions。每个路由处理器独立扩展，按需冷启动。本地开发使用next dev运行在Node.js进程中。生产部署时，静态页面由CDN分发，动态路由由Edge Runtime或Node.js Runtime处理。}}

{{Vercel部署}}{{Vercel是Next.js的官方推荐部署平台。支持零配置部署：连接Git仓库后自动构建和部署。提供预览部署（Preview Deployments）、生产部署、域名管理、环境变量配置。支持Edge Functions、Serverless Functions、图片优化等Next.js全功能。也支持Docker自托管部署。}}

{{中间件应用场景}}{{中间件的典型应用场景包括：基于角色的认证路由保护、A/B测试（通过cookie分组）、国际化（根据Accept-Language或cookie重写路径）、地理围栏（基于请求IP重定向到区域站点）、请求日志记录、CORS头设置等。注意中间件中不能使用Node.js API，只能使用Web API。}}

{{Turbopack构建工具}}{{Turbopack是Next.js内置的新一代构建工具，基于Rust编写，比Webpack快数倍。在Next.js 15中已进入稳定状态。通过next dev --turbopack启用。它采用增量计算架构，只重新编译变化的部分，极大加速了大型项目的开发启动和热更新速度。未来将逐步取代Webpack。}}

{{数据变更模式}}{{在Next.js中处理数据变更（增删改）的标准模式：使用Server Actions定义变更逻辑，在组件中通过form的action或事件处理器调用。变更完成后调用revalidatePath或revalidateTag触发缓存更新和页面重新渲染。结合useFormStatus和useOptimistic实现乐观更新，提供即时反馈的用户体验。}}

{{Authentication认证集成}}{{Next.js与主流认证方案（NextAuth.js/Auth.js、Clerk、Supabase Auth等）集成良好。典型模式：中间件做路由保护，Server Actions处理登录/注册逻辑，Server Component读取session数据。Auth.js是Next.js官方推荐方案，提供OAuth、邮箱登录、JWT策略等，与App Router深度集成。}}

{{数据库集成}}{{Server Components可以直接连接数据库，无需API层。常用方案：Prisma（类型安全ORM）、Drizzle（轻量ORM）、Kysely（类型安全查询构建器）。Server Actions中可直接执行数据库写操作。注意使用连接池（如PgBouncer、Prisma Accelerate）避免Serverless环境的连接数问题。}}

{{测试策略}}{{Next.js应用测试建议：使用Jest + Testing Library做单元和组件测试；Playwright或Cypress做端到端测试；next/jest提供Jest配置预设。Server Component测试可通过API调用测试或使用msw模拟数据。Server Actions测试直接调用函数验证结果。CI中集成测试确保质量。}}

{{性能优化原则}}{{Next.js性能优化核心原则：1）优先使用Server Components减少客户端JS；2）合理使用缓存策略平衡新鲜度和性能；3）使用loading.tsx实现流式加载；4）Image组件自动优化图片；5）动态导入（dynamic import）按需加载重组件；6）合理使用revalidate控制ISR策略；7）监控Core Web Vitals指标。}}

{{从Pages Router迁移}}{{从Pages Router迁移到App Router的要点：逐步迁移而非一步到位，两套路由可共存。pages/_app.tsx → app/layout.tsx，pages/_document.tsx → app/layout.tsx中的<html>/<body>。getServerSideProps/getStaticProps → 直接在Server Component中fetch。API Routes → Route Handlers。useRouter从next/navigation导入。}}
