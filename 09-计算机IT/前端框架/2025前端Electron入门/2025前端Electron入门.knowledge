# 2025前端Electron入门
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}
Electron是一个使用JavaScript、HTML和CSS构建跨平台桌面应用程序的框架。它将Chromium和Node.js嵌入到二进制文件中，允许开发者维护一个JavaScript代码库，创建可在Windows、macOS和Linux上运行的应用程序，无需原生开发经验。知名应用如VS Code、Slack、Discord均使用Electron构建。

{{知识点2}}
Electron的核心架构继承自Chromium的多进程模型。开发者主要接触两种进程：主进程（Main Process）和渲染进程（Renderer Process）。这种设计类似于Chrome浏览器本身的架构，一个主进程管理多个渲染进程，每个窗口对应一个独立的渲染进程。

{{知识点3}}
主进程是Electron应用的入口点，每个应用只有一个主进程。它运行在Node.js环境中，可以使用require模块和所有Node.js API。主进程的主要职责包括：创建和管理应用窗口（BrowserWindow）、控制应用生命周期（app模块）、调用原生桌面功能（菜单、对话框、托盘图标等）。

{{知识点4}}
渲染进程负责渲染Web内容。每个BrowserWindow实例都在独立的渲染进程中运行。渲染进程中的代码遵循Web标准，使用HTML、CSS和JavaScript构建用户界面。渲染进程默认不能直接访问Node.js API，需要通过预加载脚本（Preload Script）来桥接。

{{知识点5}}
BrowserWindow模块是Electron中创建和控制浏览器窗口的核心类。通过实例化BrowserWindow可以创建应用窗口，支持设置窗口大小、标题、webPreferences等配置。每个BrowserWindow实例在独立的渲染进程中加载Web页面，通过webContents对象与主进程交互。

{{知识点6}}
预加载脚本（Preload Script）在渲染进程的Web内容加载之前执行。它运行在渲染进程上下文中，但拥有访问Node.js API的特权。预加载脚本通过BrowserWindow构造函数的webPreferences选项指定，是主进程和渲染进程之间通信的重要桥梁。

{{知识点7}}
上下文隔离（Context Isolation）是Electron 12以来默认启用的安全特性。它确保预加载脚本和Electron内部逻辑在与网页分离的上下文中运行。这意味着预加载脚本中设置的window属性对网页不可见，防止网站访问Electron内部API或预加载脚本的特权功能。

{{知识点8}}
contextBridge模块用于在上下文隔离启用的情况下，安全地将API从预加载脚本暴露到渲染进程。使用contextBridge.exposeInMainWorld方法可以将指定的API对象挂载到渲染进程的window全局对象上，同时保持安全隔离。不应直接暴露整个ipcRenderer对象，而应为每个IPC消息提供独立的方法。

{{知识点9}}
进程间通信（IPC）是Electron中主进程和渲染进程之间交互的核心机制。IPC使用开发者定义的"通道"（channels）通过ipcMain和ipcRenderer模块传递消息。通道名称是任意的，且支持双向通信。

{{知识点10}}
渲染进程到主进程的单向IPC通信模式：使用ipcRenderer.send发送消息，主进程通过ipcMain.on监听。这种模式适用于从UI调用主进程API的场景，如程序化改变窗口标题、触发系统通知等。

{{知识点11}}
渲染进程到主进程的双向IPC通信模式：使用ipcRenderer.invoke发送请求，主进程通过ipcMain.handle处理并返回结果。这种模式适用于需要等待主进程返回数据的场景，如打开文件对话框、读取系统信息等。invoke/handle模式返回Promise，支持异步操作。

{{知识点12}}
主进程到渲染进程的通信：主进程通过win.webContents.send向渲染进程发送消息，渲染进程使用ipcRenderer.on监听。这种方式常用于主进程主动推送数据或事件到渲染进程，如系统事件通知、后台数据更新等。

{{知识点13}}
Electron应用的生命周期通过app模块管理。关键事件包括：ready（应用初始化完成）、window-all-closed（所有窗口关闭）、activate（macOS上点击Dock图标）。macOS上窗口全部关闭时不退出应用是常见做法，通过检查process.platform !== 'darwin'实现跨平台兼容。

{{知识点14}}
Electron Forge是Electron官方推荐的一体化打包和分发工具。它将@electron/packager、@electron/osx-sign、electron-winstaller等工具整合为统一接口，支持代码签名、安装程序生成和发布。通过npx create-electron-app@latest my-app可快速创建新项目。

{{知识点15}}
Electron Forge支持四种官方模板：webpack、webpack-typescript、vite、vite-typescript。这些模板内置了打包插件和开发服务器，为生产环境打包JavaScript代码提供现代前端工具链支持。推荐使用这些模板初始化项目以获得最佳开发体验。

{{知识点16}}
使用Electron Forge打包应用的流程：npm run package将应用代码与Electron二进制文件捆绑到文件夹；npm run make基于打包结果为每个配置的maker生成平台特定的分发文件（如macOS的.app、Windows的MSI安装程序、Linux的.deb等）。

{{知识点17}}
代码签名是分发桌面应用的重要安全步骤。macOS在应用打包层面进行签名，Windows则对分发安装程序签名。Electron Forge的forge.config.js中可配置osxSign和osxNotarize选项，macOS还需要通过Apple的notarytool进行公证。

{{知识点18}}
Electron应用的安全最佳实践包括：仅加载安全内容（HTTPS）、不为远程内容启用Node.js集成、在所有渲染器中启用上下文隔离、启用进程沙箱、定义内容安全策略（CSP）、不使用shell.openExternal处理不受信任的内容、使用最新版本的Electron。

{{知识点19}}
内容安全策略（CSP）是Electron应用安全的重要防线。通过在HTML中设置Content-Security-Policy meta标签或HTTP头，可以限制脚本、样式和其他资源的加载来源。推荐使用限制性规则如script-src 'self'，防止XSS攻击。

{{知识点20}}
Electron Fiddle是Electron维护者提供的沙盒应用，用于学习和原型开发。它可以快速实验Electron API，与文档集成良好，文档中的示例可通过"Open in Electron Fiddle"按钮直接在Fiddle中打开和运行。

{{知识点21}}
应用分发格式因平台而异。macOS支持.app和DMG格式，Windows支持MSI安装程序和可移植EXE，Linux支持deb、rpm、AppImage等格式。Electron Forge通过Maker插件支持这些格式的生成和配置。

{{知识点22}}
自动更新是桌面应用的重要功能。Electron生态中常用electron-updater配合electron-builder或Forge的发布功能实现。应用发布后，客户端可通过检查更新服务器获取新版本，下载并安装更新，实现无缝升级体验。

{{知识点23}}
主进程可访问丰富的原生API模块：dialog（文件/消息对话框）、menu（应用菜单和上下文菜单）、tray（系统托盘图标）、shell（系统默认应用操作）、clipboard（剪贴板操作）、globalShortcut（全局快捷键）、notification（系统通知）等。

{{知识点24}}
窗口自定义是Electron应用的重要能力。BrowserWindow支持创建无边框窗口（frameless）、透明窗口（transparent）、始终置顶窗口（alwaysOnTop）、全屏窗口等。可通过titleBarStyle设置自定义标题栏，实现完全自定义的窗口外观。

{{知识点25}}
父子窗口和模态窗口：通过parent选项创建子窗口，子窗口始终显示在父窗口上方。设置parent和modal选项可创建模态窗口，禁用父窗口交互。macOS上模态窗口显示为附加在父窗口上的sheet。

{{知识点26}}
WebContentsView（替代已废弃的BrowserView）用于在窗口中嵌入额外的Web内容。每个WebContentsView有自己的渲染进程和webContents对象，适用于需要在同一窗口中显示多个Web页面的场景，如侧边栏、工具面板等。

{{知识点27}}
会话（Session）管理控制Electron中网络请求和Cookie的行为。通过session模块可以设置代理、拦截请求、管理Cookie、处理权限请求等。ses.setPermissionRequestHandler可控制Web内容的权限请求（如摄像头、麦克风、通知等）。

{{知识点28}}
Electron的开发环境搭建：需要Node.js（推荐v18+）和npm/yarn/pnpm。基本项目结构包含main.js（主进程入口）、preload.js（预加载脚本）、index.html（渲染进程页面）、package.json（项目配置）。开发时使用electron .命令启动应用。

{{知识点29}}
Electron与前端框架集成：Electron的渲染进程中可使用任何前端框架（React、Vue、Svelte、Angular等）。通过Forge的vite或webpack模板，可直接使用现代前端工具链进行开发和热重载。主进程保持纯Node.js代码，渲染进程使用框架构建UI。

{{知识点30}}
Electron的发布周期跟随Chromium的发布节奏，大约每8周发布一个新的主要版本。每个主版本通常只支持最新的3个主要版本的安全更新。开发时应关注Electron的发布时间线，及时更新以获取安全补丁和新功能。

{{知识点31}}
调试Electron应用：主进程可使用Node.js调试器或VS Code的调试功能；渲染进程可使用Chrome DevTools（通过win.webContents.openDevTools()打开）。Electron Forge提供开发模式下的热重载和调试支持。

{{知识点32}}
进程沙箱（Process Sandboxing）是Electron的安全机制之一。沙箱化的渲染进程中，预加载脚本可访问的Node.js API受到限制，进一步降低安全风险。即使渲染进程被攻击，沙箱也能限制攻击者的能力范围。

{{知识点33}}
Electron应用的性能优化：合理使用webPreferences中的backgroundThrottling选项控制后台标签页的资源消耗；使用paintWhenInitiallyHidden优化窗口首次显示；避免在主进程中执行耗时操作；合理管理窗口生命周期，及时销毁不需要的BrowserWindow实例。

{{知识点34}}
系统托盘（Tray）功能允许Electron应用在系统托盘区域显示图标和菜单。通过Tray模块创建托盘图标，配合Menu模块构建右键菜单，可实现应用最小化到托盘、快速操作入口等功能，是桌面应用的常见交互模式。

{{知识点35}}
全局快捷键（GlobalShortcut）模块允许注册系统级快捷键，即使应用不在前台也能响应键盘输入。适用于媒体控制、快速唤起等场景。需要注意在应用退出时注销已注册的快捷键，避免冲突。
