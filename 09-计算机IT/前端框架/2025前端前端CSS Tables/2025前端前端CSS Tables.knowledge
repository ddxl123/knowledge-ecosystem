# 2025前端前端CSS Tables
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}{{CSS Tables 模块概述：CSS Tables 模块定义了如何布局表格数据的样式。它适用于 HTML <table> 元素，也适用于任何具有表格相关 display 值的元素。默认情况下，表格以二维网格形式渲染，单元格按连续的行和列排列。}}

{{知识点2}}{{border-collapse 属性：border-collapse 属性设置表格内单元格是共享边框还是各自拥有独立边框。可选值为 collapse（共享边框）和 separate（独立边框）。默认值为 separate。该属性可继承，适用于 table 和 inline-table 元素。}}

{{知识点3}}{{border-collapse: collapse 行为：当设置为 collapse 时，相邻单元格共享边框（折叠边框表格渲染模型）。此时 border-style 的 inset 值表现为 ridge，outset 表现为 groove。折叠边框模型下 border-spacing 属性无效。}}

{{知识点4}}{{border-collapse: separate 行为：当设置为 separate 时，相邻单元格拥有各自独立的边框（分离边框表格渲染模型）。单元格之间的距离由 border-spacing 属性定义。empty-cells 属性仅在此模式下生效。}}

{{知识点5}}{{border-spacing 属性：border-spacing 属性定义相邻单元格之间的间距。可接受一个或两个值：一个值同时设置水平和垂直间距；两个值分别设置水平间距和垂直间距。默认值为 0。该属性可继承，仅在 border-collapse: separate 时生效。}}

{{知识点6}}{{border-spacing 与表格边缘：border-spacing 的值也会应用在表格的外边缘。表格边框与首行/末行或首列/末列单元格之间的距离是对应方向的 border-spacing 加上表格对应 padding 之和。}}

{{知识点7}}{{table-layout 属性：table-layout 属性定义表格的布局算法。可选值为 auto（自动布局）和 fixed（固定布局）。默认值为 auto。该属性不可继承，适用于 table 和 inline-table 元素。}}

{{知识点8}}{{table-layout: auto 行为：使用自动表格布局算法，表格和单元格的宽度会根据内容自动调整。大多数浏览器默认使用此算法。列宽由最宽的不可断行内容决定，需要遍历全部内容后才能确定。}}

{{知识点9}}{{table-layout: fixed 行为：使用固定表格布局算法，渲染速度更快，因为水平布局仅取决于表格宽度、列宽和边框/间距，不依赖单元格内容。需要配合 width 属性显式指定表格宽度，否则回退为 auto。}}

{{知识点10}}{{固定布局列宽确定规则：在 fixed 布局中，列宽按以下优先级确定：1）具有显式宽度的 <col> 元素；2）首行中具有显式宽度的单元格；3）从剩余可用空间中分配。整个表格可在首行下载分析后即渲染。}}

{{知识点11}}{{empty-cells 属性：empty-cells 属性设置没有可见内容的表格单元格是否显示边框和背景。可选值为 show（显示）和 hide（隐藏）。默认值为 show。该属性可继承，仅在 border-collapse: separate 时有效。}}

{{知识点12}}{{caption-side 属性：caption-side 属性定义表格标题（<caption>）相对于表格的位置。可选值为 top（表格上方）和 bottom（表格下方）。默认值为 top。该属性可继承，适用于 table-caption 元素。}}

{{知识点13}}{{caption-side 逻辑值：CSS 逻辑属性模块定义了 inline-start 和 inline-end 两个逻辑值，分别将标题定位在表格的行内起始和末尾边缘，但目前浏览器支持有限。}}

{{知识点14}}{{表格相关 display 值：CSS 的 display 属性包含多个表格相关的值：table、inline-table、table-row、table-cell、table-column、table-column-group、table-header-group、table-footer-group、table-row-group。这些值使非表格元素也能表现出表格的布局行为。}}

{{知识点15}}{{display: table 与 display: inline-table：display: table 使元素表现类似 <table>，生成块级表格容器；display: inline-table 使元素表现类似 <table> 但为行内级。两者都会生成匿名的表格行和单元格来包裹子元素。}}

{{知识点16}}{{表格内部 display 值：table-row-group 对应 <tbody>、table-header-group 对应 <thead>、table-footer-group 对应 <tfoot>、table-row 对应 <tr>、table-cell 对应 <td>/<th>、table-column 对应 <col>、table-column-group 对应 <colgroup>。}}

{{知识点17}}{{vertical-align 在表格中的行为：vertical-align 属性在表格单元格中的行为与在普通块级元素中不同。在 table-cell 中，vertical-align 控制单元格内容的垂直对齐方式，可选 top、middle、bottom、baseline 等值。}}

{{知识点18}}{{表格单元格的垂直对齐 - baseline：baseline 是 table-cell 的默认 vertical-align 值。同一行中所有 baseline 对齐的单元格共享同一基线，这意味着行的高度由最高单元格或文本基线决定。}}

{{知识点19}}{{表格的 width 和 height 设置：表格可以设置 width 和 height。当 table-layout: fixed 时，width 决定表格总宽度；当 table-layout: auto 时，width 为最小宽度。单元格的 width 设置在 fixed 布局中决定列宽。}}

{{知识点20}}{{表格单元格的 padding：每个表格单元格可以独立设置 padding，用于控制内容与边框之间的间距。padding 在表格布局中不参与列宽计算（fixed 布局中），但在 auto 布局中会影响内容宽度。}}

{{知识点21}}{{表格边框样式：表格和单元格都可以设置 border 样式。在 collapse 模式下，相邻单元格的边框会合并；在 separate 模式下，每个单元格拥有独立边框。border-style 的 inset 和 outset 在 collapse 模式下会变为 ridge 和 groove。}}

{{知识点22}}{{表格条纹效果：使用 nth-child 选择器可以实现表格行的条纹效果。例如 tr:nth-child(even) { background: #ccc; } 可以将偶数行设置为灰色背景，提高表格的可读性。}}

{{知识点23}}{{表格标题样式：<caption> 元素可以通过 CSS 设置样式，包括背景色、padding、字体等。caption-side 属性控制标题位于表格上方或下方。标题是表格的一部分但不参与表格行/列的布局。}}

{{知识点24}}{{col 和 colgroup 样式限制：<col> 和 <colgroup> 元素的 CSS 样式支持非常有限。仅 border（仅在 border-collapse: collapse 时）、background、width 和 visibility 属性有效，其他属性（如 padding、margin）不生效。}}

{{知识点25}}{{表格的 overflow 处理：当 table-layout: fixed 且设置了明确的 width 时，单元格可以使用 overflow 属性来处理溢出内容。配合 white-space: nowrap 和 text-overflow: ellipsis 可以实现文本截断效果。}}

{{知识点26}}{{CSS Tables vs Grid/Flexbox：CSS Tables 是较早的布局方案。现代布局推荐使用 CSS Grid（二维布局）和 Flexbox（一维布局）。但表格特有的 border-collapse、单元格合并等特性在数据展示场景中仍有不可替代的优势。}}

{{知识点27}}{{display: table 实现居中布局：在 Flexbox 和 Grid 普及之前，display: table 配合 margin: auto 是实现垂直水平居中的常用技巧。父元素设为 display: table，子元素设为 display: table-cell 加 vertical-align: middle。}}

{{知识点28}}{{表格的可访问性：表格应使用语义化 HTML 元素（<thead>、<tbody>、<tfoot>、<th>、scope 属性等）来确保屏幕阅读器的正确解析。<th> 元素的 scope 属性指定表头是应用于行（row）还是列（col）。}}

{{知识点29}}{{表格响应式设计：表格在移动端的响应式处理方式包括：使用 overflow-x: auto 使表格可水平滚动；使用 display: block 将行和单元格转为块级元素重排；或使用 data-label 属性配合 CSS 伪元素显示列标题。}}

{{知识点30}}{{表格的 visibility: collapse：visibility: collapse 用于 table-row、table-column、table-row-group 和 table-column-group 元素时，会移除行或列但保持表格其余部分的布局不变。这与 display: none 不同，后者完全移除元素。}}

{{知识点31}}{{CSS Table Module Level 3 草案：W3C 的 CSS Table Module Level 3 规范正在开发中，旨在扩展表格布局和渲染的高级功能。目前仍处于草案阶段，尚未准备好实现。现有稳定标准基于 CSS 2.2。}}

{{知识点32}}{{表格的 text-align 属性：text-align 属性在 table-cell 元素上设置内容的水平对齐方式。可选 left、right、center、justify 等值。也可以在 table 元素上设置，通过继承应用到所有单元格。}}

{{知识点33}}{{表格中的 rowspan 和 colspan：虽然 rowspan 和 colspan 是 HTML 属性而非 CSS 属性，但它们影响 CSS 表格布局。跨行/跨列的单元格会影响周围单元格的位置和尺寸计算，在 fixed 布局中需要特别注意。}}

{{知识点34}}{{表格的 border-spacing 动画：border-spacing 属性支持动画（by computed value type），可以通过 CSS transition 或 animation 实现间距变化的平滑过渡效果。而 border-collapse 和 empty-cells 不支持动画（discrete）。}}

{{知识点35}}{{表格单元格的背景和边框叠加：在 border-collapse: separate 模式下，表格的 background 会在单元格间距处显示。border-spacing 区域会显示表格的背景色。在 collapse 模式下，单元格背景直接相邻。}}

{{知识点36}}{{使用 display: table 实现等高列：将父元素设为 display: table、子元素设为 display: table-cell，所有子元素会自动等高，无需使用 JavaScript 或额外的 CSS hack。这是 CSS Tables 布局的经典应用场景之一。}}

{{知识点37}}{{表格的 position 和 z-index：表格行（<tr>）不支持 position: relative，但单元格（<td>/<th>）支持。在 collapse 模式下，z-index 可以控制相邻边框的叠加顺序。}}

{{知识点38}}{{表格的 max-width 和 min-width：在 table-layout: auto 模式下，max-width 限制表格最大宽度，min-width 设置最小宽度。在 fixed 模式下，max-width 和 min-width 会影响表格的实际宽度计算。}}
