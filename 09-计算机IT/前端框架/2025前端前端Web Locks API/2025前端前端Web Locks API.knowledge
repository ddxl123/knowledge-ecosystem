# 2025前端前端Web Locks API
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}{{Web Locks API 是什么：Web Locks API 是一个浏览器原生API，允许在多个标签页、窗口或Worker之间对共享资源进行协调访问。它通过"锁"的抽象概念，确保在同一源(origin)内同一时间只有一个执行上下文可以持有某个特定资源的独占访问权，从而避免竞态条件。}}

{{知识点2}}{{W3C 规范状态：Web Locks API 由 W3C 维护，规范地址为 https://w3c.github.io/web-locks/。该API自2022年3月起已广泛可用(Widely Available)，属于Baseline特性，主流浏览器均已支持。}}

{{知识点3}}{{安全上下文要求：Web Locks API 仅在安全上下文(Secure Contexts)中可用，即必须通过 HTTPS 协议访问。HTTP环境下无法使用该API。}}

{{知识点4}}{{Web Workers 支持：Web Locks API 在 Web Workers 中同样可用。Worker中通过 WorkerNavigator.locks 属性获取 LockManager 实例，与主线程的锁共享同一源的锁管理空间。}}

{{知识点5}}{{LockManager 接口：LockManager 是 Web Locks API 的核心接口，提供两个实例方法：request() 用于请求锁，query() 用于查询当前锁状态。通过 navigator.locks 属性获取 LockManager 实例。}}

{{知识点6}}{{Navigator.locks 属性：Navigator 接口的 locks 只读属性返回一个 LockManager 对象。这是进入 Web Locks API 的主入口点，通过 navigator.locks 即可开始使用锁功能。}}

{{知识点7}}{{WorkerNavigator.locks 属性：WorkerNavigator 接口同样具有 locks 只读属性，在 Web Worker 中返回 LockManager 对象，使 Worker 也能参与锁的协调。}}

{{知识点8}}{{Lock 接口：Lock 接口表示一个锁对象，具有两个只读属性：name（锁名称）和 mode（锁模式）。Lock 实例在 request() 的回调中接收，或通过 query() 返回的锁信息中获取。}}

{{知识点9}}{{Lock.name 属性：Lock.name 是只读属性，返回创建锁时传入的名称字符串。锁名由开发者选择，用于标识被协调的抽象资源，例如 "net_db_sync"、"my_resource" 等。}}

{{知识点10}}{{Lock.mode 属性：Lock.mode 是只读属性，返回锁的访问模式，值为 "exclusive"（独占）或 "shared"（共享）。不指定时默认为 "exclusive"。}}

{{知识点11}}{{LockManager.request() 方法签名：request(name, callback) 或 request(name, options, callback)。name 是锁名称字符串，options 是可选的配置对象，callback 是获取锁后执行的回调函数。返回一个 Promise。}}

{{知识点12}}{{request() 的回调机制：回调函数在锁被授予时调用，锁在回调返回后自动释放。通常使用 async 函数作为回调，这样锁会在异步函数完全执行完毕后才释放。}}

{{知识点13}}{{request() 返回值：request() 方法返回一个 Promise，该 Promise 在锁释放后以回调函数的返回值 resolve；如果请求被中止则 reject。可以通过 await 等待整个锁生命周期完成。}}

{{知识点14}}{{独占模式(exclusive)：独占锁是默认模式。同一时间同一名称的锁只能有一个独占持有者。当独占锁被持有时，任何其他同名锁请求都必须排队等待。适用于写操作等互斥场景。}}

{{知识点15}}{{共享模式(shared)：共享锁允许多个持有者同时持有同一名称的锁。当共享锁被持有时，其他共享锁请求可以被授予，但独占锁请求必须等待。适用于读操作等可并发场景。}}

{{知识点16}}{{读写者模式(Readers-Writer Pattern)：通过 shared 和 exclusive 模式组合可实现经典的读写者模式。多个读操作使用 shared 模式并发执行，写操作使用 exclusive 模式独占执行。类似 IndexedDB 的 readonly/readwrite 事务语义。}}

{{知识点17}}{{ifAvailable 选项：设置 ifAvailable: true 时，锁请求只在锁可立即获取时才被授予。如果无法立即获取，回调函数会收到 null 而非 Lock 实例，从而避免等待。使用时需在回调中检查 lock 是否为 null。}}

{{知识点18}}{{steal 选项：设置 steal: true 时，会释放当前持有的同名锁，将锁授予新请求者，并跳过排队中的请求。这是一个危险操作，因为之前在锁内运行的代码可能与新持有者产生冲突，需谨慎使用。}}

{{知识点19}}{{signal 选项：通过传入 AbortSignal（AbortController 的 signal 属性）可以中止排队中的锁请求。如果在锁被授予前 AbortController 被 abort，请求会被丢弃并抛出 AbortError。常用于实现锁请求超时。}}

{{知识点20}}{{超时锁请求模式：结合 AbortController 和 setTimeout 可实现锁请求超时。创建 AbortController，用 setTimeout 在指定时间后调用 abort()，将 controller.signal 传入 request() 的 signal 选项，捕获 AbortError 处理超时。}}

{{知识点21}}{{LockManager.query() 方法：query() 方法返回一个 Promise，resolve 为包含 held（已持有的锁）和 pending（排队中的锁请求）两个数组的对象。这是一个快照，反映调用时刻的锁管理器状态。}}

{{知识点22}}{{query() 返回的 LockInfo 对象：每个 LockInfo 包含三个属性：name（锁名称）、mode（锁模式 "exclusive"/"shared"）、clientId（请求锁的上下文唯一标识，与 Client.id 相同）。}}

{{知识点23}}{{锁的作用域(Origin Scoping)：锁按源(origin)隔离。来自 https://example.com 的锁与 https://example.org:8080 的锁完全独立，互不影响。不同源之间的同名锁不会产生冲突。}}

{{知识点24}}{{锁的队列机制：当一个锁被持有时，对同名锁的新请求会进入队列。队列中的第一个请求会在当前锁释放后被授予。请求按照先进先出(FIFO)顺序处理。}}

{{知识点25}}{{锁的自动释放：锁在回调函数返回后自动释放，无需手动调用释放方法。这是 Web Locks API 的设计特点，避免了忘记释放锁导致的死锁问题。如果回调抛出异常，锁同样会被释放。}}

{{知识点26}}{{长时间持有锁的模式：通过返回一个由外部控制 resolve/reject 的 Promise，可以实现任意时长持有锁。使用 Promise.withResolvers() 创建 Promise，将该 Promise 作为回调返回值，锁会一直持有直到 resolve 或 reject 被调用。}}

{{知识点27}}{{Deadlock（死锁）问题：死锁发生在多个锁请求互相等待无法继续的情况。例如：标签页1持有锁A并请求锁B，标签页2持有锁B并请求锁A，双方都无法继续。Web Locks API 中的死锁仅影响锁本身及相关代码，不会影响浏览器或其他标签页。}}

{{知识点28}}{{避免死锁的策略：可以通过以下方式避免死锁：1）不嵌套锁请求；2）始终按固定顺序请求多个锁；3）为锁请求设置超时(signal 选项)；4）使用 ifAvailable 避免无限等待。}}

{{知识点29}}{{Leader Election 模式：Web Locks API 天然支持领导者选举模式。多个标签页尝试获取同一个锁，只有一个能成功成为"领导者"。常用于确保只有一个标签页执行后台同步、推送通知处理等任务。}}

{{知识点30}}{{锁名称规范：锁名称以连字符(-)开头是不允许的，会抛出 NotSupportedError。此外，steal 和 ifAvailable 不能同时为 true，signal 也不能与 steal 或 ifAvailable 同时使用，否则会抛出 NotSupportedError。}}

{{知识点31}}{{request() 的异常类型：InvalidStateError（文档不完全活跃时）、SecurityError（无法获取锁管理器时）、NotSupportedError（参数组合不合法时）、AbortError（signal 被中止时）。这些异常会导致返回的 Promise 被 reject。}}

{{知识点32}}{{与 IndexedDB 的对比：IndexedDB 的事务自动提供类似锁的机制（readonly 可并发，readwrite 独占），但仅限于单个数据库。Web Locks API 更通用，可用于协调任意跨标签页的资源共享场景，不限于数据库操作。}}

{{知识点33}}{{与 Service Worker 的配合：Web Locks API 可与 Service Worker 配合使用。Service Worker 中通过 self.navigator.locks 访问 LockManager，可用于协调多个客户端（页面）之间的资源访问，例如确保只有一个客户端执行缓存更新。}}

{{知识点34}}{{await 与线性代码流：由于 request() 返回 Promise，可以在 async 函数中使用 await 实现线性代码流。先执行不需要锁的操作，await 获取锁后执行需要保护的操作，锁释放后继续执行后续代码。}}

{{知识点35}}{{多个锁请求的组合模式：可以在一个 async 函数中请求多个不同的锁，实现复杂的资源协调。但需要注意锁的获取顺序一致性以避免死锁。每个 request() 调用都是独立的，可以分别设置不同的选项。}}

{{知识点36}}{{实际应用场景-离线数据同步：多个标签页同时打开时，使用 Web Locks API 确保只有一个标签页执行网络数据与 IndexedDB 的同步操作，避免数据重复写入或冲突。这是 Web Locks API 最典型的使用场景之一。}}

{{知识点37}}{{实际应用场景-单实例任务：确保某些任务（如推送通知处理、后台数据清理、定时任务触发）在同一源内只执行一次，即使用户打开了多个标签页。通过锁名称标识任务，只有一个标签页能获取锁并执行。}}

{{知识点38}}{{实际应用场景-资源独占访问：对某些需要独占访问的资源（如文件系统API、串口API、USB设备等）进行协调，确保同一时间只有一个上下文在操作硬件或文件资源。}}

{{知识点39}}{{浏览器兼容性：Web Locks API 自2022年3月起广泛可用。Chrome 69+、Firefox 96+、Safari 15.4+、Edge 79+ 均支持。属于Baseline Widely Available特性，可在生产环境放心使用。}}

{{知识点40}}{{调试技巧-使用 query() 诊断：当锁请求长时间未被授予时，可以调用 navigator.locks.query() 查看当前所有已持有和排队中的锁，帮助定位哪个锁被占用、被谁占用、处于什么模式，是调试锁相关问题的利器。}}

{{知识点41}}{{与 SharedArrayBuffer 的区别：SharedArrayBuffer 提供的是底层内存共享，需要配合 Atomics 进行同步。Web Locks API 是高级抽象，基于异步回调，不涉及共享内存，更适合协调应用层面的资源访问。}}

{{知识点42}}{{锁的传递性：锁不可传递给其他上下文。一个标签页获取的锁只能在该标签页的回调中使用，不能通过 postMessage 发送给其他标签页或 Worker。锁的生命周期严格绑定到请求它的执行上下文。}}

{{知识点43}}{{Promise.withResolvers() 配合模式：ES2024 引入的 Promise.withResolvers() 与 Web Locks API 配合使用非常优雅。const { promise, resolve, reject } = Promise.withResolvers() 创建 Promise，将 promise 作为回调返回值，外部通过 resolve/reject 控制锁的释放时机。}}

{{知识点44}}{{性能考量：Web Locks API 的锁操作是异步的，不会阻塞主线程。锁的获取和释放都通过微任务(microtask)调度，对页面性能影响极小。但应避免在高频操作中频繁请求锁，以免造成不必要的排队延迟。}}

{{知识点45}}{{锁名称最佳实践：使用有意义的、描述资源的字符串作为锁名称，例如 "sync-database"、"user-profile-update"。避免使用过于通用的名称（如 "lock"），以减少不必要的锁竞争。锁名称是全局的（在同源内），应具有良好的命名空间管理。}}
