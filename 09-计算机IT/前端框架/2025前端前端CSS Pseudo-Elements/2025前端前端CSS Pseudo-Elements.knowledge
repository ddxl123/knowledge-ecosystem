# 2025前端前端CSS Pseudo-Elements
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}{{CSS伪元素（Pseudo-Elements）是CSS选择器的一种，用于选取元素的特定部分或创建不存在于DOM中的虚拟元素。伪元素以双冒号（::）语法书写，如::before和::after，以区别于使用单冒号（:）的伪类（Pseudo-Classes）。最早的四个伪元素（::before、::after、::first-line、::first-letter）也兼容单冒号写法，但推荐使用双冒号以符合CSS3规范。}}

{{知识点2}}{{::before伪元素在所选元素内容之前创建一个内联盒子（inline box）作为该元素的第一个子元素。它常通过content属性添加装饰性内容，如图标、引号或其他装饰。::before不能应用于替换元素（如<img>），因为替换元素的内容由外部资源决定。如果content属性未指定或值为normal/none，则::before不会被渲染。}}

{{知识点3}}{{::after伪元素在所选元素内容之后创建一个内联盒子作为该元素的最后一个子元素。与::before类似，它通过content属性添加装饰性内容。::after同样不能应用于替换元素。当content属性未指定或值为normal/none时，::after不渲染，表现如同display: none。}}

{{知识点4}}{{content属性是::before和::after伪元素的核心，决定了伪元素的生成内容。它可以接受字符串值（如content: "→ "）、URL（如content: url("icon.png")）、attr()函数（如content: attr(href)）、计数器counter()/counters()、引号关键字（open-quote/close-quote）以及图片值（渐变、image-set等）。设为none时不生成伪元素，normal时对::before和::after等同于none。}}

{{知识点5}}{{::first-line伪元素选中块级元素第一行的文本内容，可以改变第一行的字体、颜色、背景等样式。第一行的实际长度取决于元素宽度、文档宽度和字体大小等多种因素。可使用的属性仅限于字体相关属性、背景相关属性、color、word-spacing、letter-spacing、text-decoration、text-transform、line-height、text-shadow和vertical-align等。}}

{{知识点6}}{{::first-letter伪元素选中块级元素第一行的第一个字母、数字或符号字符。前置或紧跟第一个字母的标点符号也会被包含在匹配中。::first-letter支持比::first-line更多的属性，包括所有字体属性、背景属性、margin、padding、border、color、text-decoration、text-shadow、text-transform、letter-spacing、word-spacing、line-height、box-shadow和float等。常用于实现首字下沉（drop cap）效果。}}

{{知识点7}}{{::selection伪元素用于设置用户选中文本时的高亮样式。仅支持有限的CSS属性：color、background-color、text-decoration及其相关属性、text-shadow以及-webkit-text-stroke相关属性。background-image会被忽略。::selection遵循特殊的高亮伪元素继承模型，从父元素的高亮伪元素和父元素本身双重继承。}}

{{知识点8}}{{::marker伪元素选中列表项（display: list-item的元素）的标记框，通常包含项目符号或编号。可用于<li>和<summary>等元素。支持的属性包括所有动画和过渡属性、字体属性、color、content、counter相关属性、quotes、direction相关属性和white-space。可以自定义列表标记的样式和内容。}}

{{知识点9}}{{::placeholder伪元素用于样式化表单输入框的占位符文本。大多数浏览器默认使用半透明或浅灰色显示占位符文本。可使用的属性与::first-line相同（字体属性、背景属性、color等）。需要注意可访问性：占位符文本不应替代<label>元素，且需确保足够的颜色对比度。}}

{{知识点10}}{{::backdrop伪元素用于样式化在顶层（top layer）中渲染的元素的背景遮罩。出现在以下场景：使用Fullscreen API进入全屏的元素、通过showModal()显示的<dialog>元素、通过showPopover()显示的弹出框元素。多个顶层元素按后进先出（LIFO）堆叠，每个元素都有自己的::backdrop。::backdrop不继承任何属性。}}

{{知识点11}}{{::file-selector-button伪元素选中<input type="file">中的文件选择按钮。可以通过CSS完全自定义按钮的外观。为了兼容旧浏览器，可能需要同时使用::-webkit-file-upload-button和::-ms-browse前缀。该伪元素是一个完整的元素，字体和颜色不会自动从input元素继承。}}

{{知识点12}}{{::cue伪元素用于样式化WebVTT视频字幕（captions）和提示文本。语法为::cue或::cue(<selector>)，其中selector可以匹配字幕文本中的内部标签（如<c>、<i>、<b>、<u>等）。背景属性单独应用于每条字幕，而其他属性则应用于整个字幕集合。支持的属性包括背景、color、font、line-height、opacity、outline、text-decoration、text-shadow等。}}

{{知识点13}}{{::highlight()伪元素用于创建自定义文本高亮效果，通过CSS Custom Highlight API配合使用。需要先通过JavaScript创建Highlight对象并注册到CSS.highlights中，然后通过::highlight(<name>)选择器样式化。与::selection不同，::highlight()是通过编程方式创建的，适用于搜索结果高亮、代码语法高亮等场景。}}

{{知识点14}}{{::target-text伪元素用于样式化通过URL片段标识符（fragment identifier）定位的目标文本。当用户点击带有锚点的链接时，浏览器会滚动到目标元素，::target-text允许为这段文本设置特殊的高亮样式。属于高亮伪元素类别。}}

{{知识点15}}{{::grammar-error伪元素选中浏览器认为有语法错误的文本段落。::spelling-error伪元素选中浏览器认为拼写错误的文本。这两个都属于高亮伪元素，支持的样式属性有限，通常只有color、background-color和text-decoration等。}}

{{知识点16}}{{::slotted()伪元素用于选中Web Components中放置在slot中的元素。它只能在Shadow DOM的样式中使用，语法为::slotted(<selector>)。例如::slotted(p)会选择所有被插入slot的<p>元素。这是Shadow DOM样式化机制的重要组成部分。}}

{{知识点17}}{{::part()伪元素用于选中Shadow DOM中具有匹配part属性的元素。例如，如果Shadow DOM中的元素有part="label"，外部样式可以通过::part(label)来样式化它。与::slotted()不同，::part()可以从Shadow DOM外部使用，提供了更灵活的组件样式化方式。}}

{{知识点18}}{{伪元素不能独立存在，必须依附于一个"原始元素"（originating element）。伪元素必须出现在复杂或复合选择器的最后部分。例如p::first-line有效，但::first-line > *无效，因为不能选择伪元素的子元素。可以基于原始元素的当前状态选择伪元素，如p:hover::first-line。}}

{{知识点19}}{{在CSS选择器列表中，如果包含无效选择器，整个样式块都会被忽略。这意味着伪元素选择器写法错误会导致同规则块中的其他样式也失效。需要注意选择器的正确书写格式。}}

{{知识点20}}{{::before和::after伪元素默认与父元素共享相同的层叠上下文。如果没有显式设置z-index，::after的生成内容会出现在::before之上，因为::after在DOM流中渲染得更晚。可以通过设置position和z-index来控制伪元素的层叠顺序。}}

{{知识点21}}{{伪元素对无障碍性有重要影响。MDN明确指出，使用::before和::after添加的内容不应被屏幕阅读器可靠地访问。因此，不建议使用伪元素添加对理解页面内容至关重要的信息。装饰性内容（如图标、引号装饰）使用伪元素是可接受的。}}

{{知识点22}}{{当::before或::after的display属性设为list-item时，它们的行为类似列表项<li>，因此可以生成自己的::marker伪元素。这可以通过嵌套选择器如ul::before::marker来样式化。这是CSS伪元素嵌套的典型用法，支持在::before和::after上嵌套::marker。}}

{{知识点23}}{{content属性的attr()函数可以从HTML元素属性中提取值作为伪元素的内容。例如a::after { content: " (" attr(href) ")"; }可以在链接后显示其href值。这对于打印样式表特别有用，可以在链接旁显示目标URL。attr()函数在CSS Values Level 5中得到扩展，可以用于更多CSS属性。}}

{{知识点24}}{{content属性支持CSS计数器功能。counter()函数显示指定名称的计数器值，counters()函数显示嵌套计数器的值，用指定字符串分隔。配合counter-reset和counter-increment属性，可以实现自动编号、章节编号等复杂编号系统。计数器可以指定list-style-type来改变显示格式。}}

{{知识点25}}{{content属性支持引号相关的关键字：open-quote（插入开始引号）、close-quote（插入结束引号）、no-open-quote和no-close-quote。引号样式由quotes属性定义，可以为不同嵌套层级指定不同的引号对，支持多语言的引号约定。}}

{{知识点26}}{{content属性支持替代文本（alternative text）功能，使用斜杠（/）分隔。例如content: url("icon.png") / "描述文本";。这为通过content插入的图像提供了无障碍替代文本，屏幕阅读器可以朗读这段文字，类似HTML中img标签的alt属性。}}

{{知识点27}}{{::details-content伪元素用于样式化<details>元素中可展开/收起的内容部分。这是较新的伪元素，允许开发者独立控制折叠区域的样式，包括过渡动画效果，增强了原生折叠组件的定制能力。}}

{{知识点28}}{{::picker()伪元素用于样式化表单控件的弹出选择器部分，例如自定义<select>元素的下拉选择器。::picker-icon伪元素则选中表单控件中的选择器图标（如select关闭时的向下箭头）。::checkmark伪元素选中自定义select中当前选中<option>内的勾选标记。这些都是2025年新增的表单相关伪元素。}}

{{知识点29}}{{::scroll-button()、::scroll-marker和::scroll-marker-group是与滚动相关的伪元素。::scroll-button()创建可控制滚动容器滚动的按钮。::scroll-marker创建滚动标记（滚动目标按钮）。::scroll-marker-group在滚动容器前后生成容器来包含scroll-marker伪元素。这些是CSS Overflow Level 5规范中的新特性。}}

{{知识点30}}{{::view-transition、::view-transition-group()、::view-transition-image-pair()、::view-transition-new()和::view-transition-old()是View Transitions API相关的伪元素。它们用于控制页面过渡动画中各层的样式，支持创建平滑的单页和多页面过渡效果。}}

{{知识点31}}{{非标准的WebKit前缀伪元素在实际开发中广泛使用。::-webkit-scrollbar系列用于自定义滚动条样式（包括track、thumb等）。::-webkit-progress-bar和::-webkit-progress-value用于自定义进度条。::-webkit-slider-thumb和::-webkit-slider-runnable-track用于自定义滑块。Mozilla前缀::-moz-range-thumb等也有类似功能。}}

{{知识点32}}{{高亮伪元素（Highlight Pseudo-elements）遵循特殊的继承模型。当为高亮伪元素设置样式时，它们从两个来源继承：父元素（遵循普通继承）和父元素的高亮伪元素（遵循高亮继承）。这意味着子元素的高亮文本会组合两个来源的属性。例如，如果父元素的::selection设置了background-color，子元素的::selection会继承这个值，除非被显式覆盖。}}

{{知识点33}}{{::column伪元素用于选中多栏布局（multi-column layout）中的每个列片段。这是CSS多栏布局模块的一部分，允许为每列设置独立的样式。虽然规范已定义，但浏览器支持程度有限。}}

{{知识点34}}{{伪元素的CSS动画和过渡：::before和::after等树状伪元素（Tree-Abiding pseudo-elements）支持完整的CSS动画和过渡。::marker伪元素也支持所有动画和过渡属性。这使得列表标记、生成内容等可以有平滑的过渡效果。但::first-line和::first-letter等排版伪元素的动画支持有限。}}

{{知识点35}}{{使用::before和::after创建纯CSS图标和装饰效果是常见的前端技巧。通过设置content为空字符串，利用border创建三角形、利用background和border-radius创建圆形、利用transform旋转和缩放，可以创建各种复杂的视觉效果而无需额外的HTML元素或图片资源。例如待办列表中的对勾标记就可以纯CSS实现。}}

{{知识点36}}{{伪元素在打印样式表中特别有用。可以使用::before在链接后插入URL（通过attr(href)），使用::after在表格单元格后添加内容，或使用::first-letter创建首字下沉效果。content属性的counter功能也常用于打印时的章节自动编号和页码引用。}}

{{知识点37}}{{使用data-*自定义属性配合::after的attr()函数，可以创建纯CSS的工具提示（tooltip）效果。通过span[data-description]:hover::after { content: attr(data-description); }即可在悬停时显示提示文本，无需JavaScript。配合:focus选择器还可以支持键盘用户的可访问性。}}

{{知识点38}}{{::placeholder的可访问性注意事项：占位符文本不应替代<label>元素。占位符在用户输入时会消失，且在Windows高 contrast模式下可能与用户输入的文字样式相同。更好的做法是在input外部提供提示文本，并通过aria-describedby建立关联，确保提示始终可见且对屏幕阅读器可访问。}}
