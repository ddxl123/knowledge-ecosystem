# 2025前端前端面试题集
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}{{JavaScript闭包（Closure）}}
闭包是指函数能够记住并访问其词法作用域，即使函数在其词法作用域之外执行。闭包由函数和函数创建时的词法环境组成。常见应用场景：数据封装/私有变量、函数工厂、回调与定时器、模块模式。经典面试题：for循环中的闭包问题——用let声明或IIFE解决。闭包可能导致内存泄漏，因为被引用的变量不会被垃圾回收。

{{知识点2}}{{JavaScript原型链与继承}}
每个JavaScript对象都有一个__proto__指向其构造函数的prototype对象，形成原型链。当访问对象属性时，沿原型链向上查找直到null。ES6的class语法是原型链的语法糖。继承方式：原型链继承、构造函数继承、组合继承、寄生组合继承（最优）、ES6 class extends。instanceof运算符沿原型链检查。

{{知识点3}}{{JavaScript事件循环（Event Loop）}}
事件循环是JS处理异步的核心机制。执行栈（Call Stack）执行同步代码，微任务队列（Promise.then、MutationObserver）优先于宏任务队列（setTimeout、setInterval、I/O）。每次宏任务执行完后清空所有微任务，再执行下一个宏任务。async/await本质上是Promise的语法糖，await之后的代码进入微任务队列。Node.js有额外的阶段（timers、poll、check）。

{{知识点4}}{{JavaScript作用域与变量提升}}
作用域分为全局作用域、函数作用域、块级作用域（let/const）。var声明有变量提升（提升至函数顶部，初始化为undefined），let/const有暂时性死区（TDZ）——声明前访问会报错。const声明的对象引用不可变，但对象内部属性可变。箭头函数没有自己的this、arguments、super，不能用作构造函数。

{{知识点5}}{{JavaScript类型系统与类型转换}}
七种原始类型：Number、String、Boolean、Undefined、Null、Symbol、BigInt。typeof对null返回"object"（历史遗留bug）。隐式类型转换规则：==比较时会进行类型转换，===严格比较不转换。[] == false为true，[] + {}为"[object Object]"。NaN === NaN为false，用Number.isNaN()判断。推荐始终使用===。

{{知识点6}}{{Promise与异步编程}}
Promise有三种状态：pending、fulfilled、rejected，状态不可逆。Promise.all()全部成功才返回，Promise.race()返回最先完成的，Promise.allSettled()等待所有完成，Promise.any()返回第一个成功的。手写Promise是高频面试题，需实现then链式调用、异步执行、状态管理。async/await是语法糖，用try-catch处理错误。

{{知识点7}}{{深拷贝与浅拷贝}}
浅拷贝只复制一层：Object.assign()、展开运算符{...obj}、Array.prototype.slice()。深拷贝递归复制所有层级：structuredClone()（现代API）、JSON.parse(JSON.stringify())（丢失函数/undefined/Symbol/循环引用）。手写深拷贝需处理：循环引用（WeakMap标记）、特殊对象（Date/RegExp）、原型链保持。

{{知识点8}}{{防抖（Debounce）与节流（Throttle）}}
防抖：事件触发后延迟执行，若在延迟期间再次触发则重新计时。适用于搜索输入、窗口resize。节流：固定时间间隔内只执行一次。适用于滚动事件、按钮防重复点击。实现要点：防抖用setTimeout+clearTimeout，节流用时间戳或setTimeout标志位。面试常考手写实现。

{{知识点9}}{{this指向问题}}
this在运行时确定，非定义时。规则优先级：new绑定 > 显式绑定（call/apply/bind）> 隐式绑定（对象调用）> 默认绑定（严格模式undefined，非严格window）。箭头函数继承外层this，不可改变。class中的方法默认不会自动绑定this，需在constructor中bind或使用箭头函数属性。

{{知识点10}}{{ES6+核心特性}}
解构赋值、模板字符串、箭头函数、let/const、Symbol、Set/Map、WeakSet/WeakMap、Proxy/Reflect、迭代器（Iterator）与生成器（Generator）、可选链（?.）、空值合并（??）、Promise.allSettled、动态import、Top-level await。Map与Object的区别：Map键可以是任意类型，有size属性，保持插入顺序。

{{知识点11}}{{React Hooks核心原理}}
useState：函数组件的状态管理，基于闭包和链表实现。useEffect：副作用处理，依赖数组控制执行时机，返回清理函数。useRef：跨渲染周期持久化引用，不触发重渲染。useMemo/useCallback：性能优化，缓存计算结果/函数引用。useContext：跨组件共享状态。自定义Hook：逻辑复用，本质是函数。

{{知识点12}}{{React Fiber架构}}
Fiber是React 16+的协调引擎。将渲染工作拆分为可中断的小单元（Fiber节点），通过链表结构实现。双缓存机制：current tree（屏幕显示）和workInProgress tree（构建中）。优先级调度：Concurrent Mode允许高优先级更新打断低优先级更新。requestIdleCallback思想，但React自己实现了调度器（Scheduler）。

{{知识点13}}{{React虚拟DOM与Diff算法}}
虚拟DOM是JS对象描述UI结构。Diff算法三大策略：同层比较（不跨层级）、类型不同直接替换、通过key标识同层节点。Reconciliation过程：从根节点递归比较，标记增删改操作，批量提交到真实DOM。React 18引入自动批处理（Automatic Batching），多个setState自动合并更新。

{{知识点14}}{{React性能优化}}
使用React.memo避免不必要的重渲染；useMemo缓存计算结果；useCallback缓存函数引用；虚拟列表（react-window/react-virtualized）处理大列表；代码分割（React.lazy + Suspense）；避免内联函数和对象字面量作为props；使用key正确标识列表项；Concurrent特性（useTransition、useDeferredValue）。

{{知识点15}}{{React状态管理方案}}
Context API：适合低频更新的全局状态。Redux：单一数据源、纯函数reducer、不可变更新，Redux Toolkit简化了使用。Zustand：轻量级，基于hooks，无boilerplate。Jotai：原子化状态管理。React Query/SWR：服务端状态管理，缓存、乐观更新。选择标准：项目规模、团队熟悉度、性能需求。

{{知识点16}}{{Vue 3响应式原理}}
Vue 3使用Proxy替代Vue 2的Object.defineProperty。Proxy能监听属性的增删、数组变化，无需hack数组方法。reactive()创建深度响应式对象，ref()创建基本类型的响应式引用（通过.value访问）。依赖收集通过WeakMap→Map→Set的数据结构，effect函数在执行时触发getter进行track，数据变化时通过trigger通知更新。

{{知识点17}}{{Vue 3 Composition API}}
setup()函数是Composition API的入口。ref/reactive创建响应式数据；computed计算属性；watch/watchEffect侦听副作用；provide/inject跨层级传递；生命周期钩子（onMounted、onUpdated、onUnmounted等）。与Options API对比：更好的逻辑复用（composables）、TypeScript支持、代码组织更灵活。

{{知识点18}}{{Vue虚拟DOM与编译优化}}
Vue 3的编译器做了大量优化：静态提升（hoistStatic）——静态节点只创建一次；补丁标记（PatchFlag）——标记动态绑定类型，diff时只比较变化部分；块树（Block Tree）——扁平化追踪动态节点，跳过静态内容。Vue 3的diff算法使用最长递增子序列优化节点移动。

{{知识点19}}{{Vue Router路由机制}}
Hash模式：基于URL的hash变化，兼容性好，不发送请求。History模式：基于HTML5 History API，URL美观但需要服务端配合。导航守卫：全局守卫（beforeEach）、路由守卫（beforeEnter）、组件守卫（beforeRouteEnter等）。路由懒加载：() => import('./Component.vue')。动态路由匹配、嵌套路由、编程式导航。

{{知识点20}}{{TypeScript核心类型系统}}
基础类型：string、number、boolean、any、unknown、never、void。接口（interface）与类型别名（type）的区别：interface可合并声明，type支持联合/交叉类型。泛型：<T>创建类型参数，约束（extends）、默认类型。条件类型：T extends U ? X : Y。映射类型：Partial<T>、Required<T>、Pick<T,K>、Omit<T,K>。infer关键字在条件类型中提取类型。

{{知识点21}}{{CSS盒模型与BFC}}
标准盒模型：width/width只包含内容区。IE盒模型（怪异模式）：width/width包含content+padding+border。box-sizing: border-box切换为IE盒模型。BFC（块级格式化上下文）触发条件：overflow不为visible、float、position为absolute/fixed、display为inline-block/flex/grid。BFC特性：内部浮动参与高度计算、外边距不穿透。

{{知识点22}}{{CSS Flexbox布局}}
容器属性：flex-direction（主轴方向）、flex-wrap（换行）、justify-content（主轴对齐）、align-items（交叉轴对齐）、align-content（多行对齐）。项目属性：flex-grow（放大比例）、flex-shrink（缩小比例）、flex-basis（初始大小）、align-self（单独对齐）。flex: 1等同于flex: 1 1 0%。面试常考：圣杯布局、骰子布局。

{{知识点23}}{{CSS Grid布局}}
二维布局系统。容器属性：grid-template-columns/rows（定义行列）、gap（间距）、grid-area（命名区域）。fr单位：按比例分配空间。auto-fill/auto-fit：自动响应式列数。place-items：简写align-items和justify-content。Grid vs Flex：Grid适合二维布局，Flex适合一维布局。面试常考：复杂网格布局实现。

{{知识点24}}{{浏览器渲染流程}}
解析HTML→构建DOM树；解析CSS→构建CSSOM树；合并为渲染树（Render Tree）；布局（Layout/Reflow）计算几何信息；绘制（Paint）生成绘制记录；合成（Composite）GPU合成图层。重排（Reflow）比重绘（Repaint）代价更大。优化：批量DOM操作、使用transform/opacity做动画（仅触发合成）、will-change提示。

{{知识点25}}{{网络请求与HTTP协议}}
HTTP/1.1：持久连接、管线化（但有队头阻塞）。HTTP/2：多路复用、头部压缩、服务端推送、二进制帧。HTTP/3：基于QUIC（UDP），解决TCP队头阻塞。状态码：2xx成功、3xx重定向（301永久/302临时/304缓存）、4xx客户端错误、5xx服务端错误。HTTPS：TLS加密，握手过程（非对称加密交换密钥，对称加密传输数据）。

{{知识点26}}{{浏览器缓存机制}}
强缓存：Cache-Control（max-age/no-cache/no-store）、Expires。协商缓存：Last-Modified/If-Modified-Since、ETag/If-None-Match。优先级：Cache-Control > Expires > ETag > Last-Modified。304 Not Modified表示资源未变化。Service Worker缓存：可编程控制缓存策略，实现离线访问。面试常考：从输入URL到页面展示的完整流程。

{{知识点27}}{{跨域解决方案}}
同源策略：协议+域名+端口相同。解决方案：CORS（服务端设置Access-Control-Allow-Origin，预检请求OPTIONS）、JSONP（利用script标签，只支持GET）、代理服务器（开发环境webpack-dev-server、生产环境Nginx反向代理）、postMessage（跨窗口通信）、WebSocket（不受同源限制）。简单请求与预检请求的区别。

{{知识点28}}{{Web安全：XSS与CSRF}}
XSS（跨站脚本攻击）：存储型（恶意脚本存入服务器）、反射型（URL参数注入）、DOM型（前端直接操作DOM）。防御：输入转义、CSP（内容安全策略）、HttpOnly Cookie。CSRF（跨站请求伪造）：利用用户已登录状态发起恶意请求。防御：SameSite Cookie、CSRF Token、验证Referer/Origin。2025年还需关注供应链攻击。

{{知识点29}}{{前端工程化：Webpack与Vite}}
Webpack：模块打包器，核心概念——entry、output、loader、plugin。构建流程：初始化→编译→输出。常用loader：babel-loader、css-loader、file-loader。常用plugin：HtmlWebpackPlugin、MiniCssExtractPlugin。Tree Shaking：基于ES Module静态分析。Vite：基于ESM的开发服务器，开发环境无需打包，使用esbuild预构建依赖，Rollup生产构建。HMR更快。

{{知识点30}}{{前端性能优化策略}}
加载优化：代码分割、懒加载、资源预加载（preload/prefetch）、CDN加速、Gzip/Brotli压缩、HTTP/2多路复用。渲染优化：减少重排重绘、虚拟列表、requestAnimationFrame、Web Worker。缓存策略：强缓存+协商缓存、Service Worker。监控：Lighthouse、Core Web Vitals（LCP/FID/CLS）、Performance API。首屏优化：SSR/SSG、骨架屏。

{{知识点31}}{{手写call/apply/bind}}
call：将函数设为对象的临时方法执行，接收参数列表。apply：同call但接收参数数组。bind：返回新函数并绑定this，支持柯里化。实现要点：处理this为原始值的情况（包装为对象）、处理new调用bind返回函数的情况、参数拼接。面试高频手写题。

{{知识点32}}{{手写EventEmitter（发布订阅模式）}}
实现on（订阅）、emit（发布）、off（取消订阅）、once（一次性订阅）。内部维护事件名到回调数组的映射。注意once的实现：包装回调函数，在执行后自动off。发布订阅模式vs观察者模式的区别：发布订阅有事件中心解耦。

{{知识点33}}{{前端路由实现原理}}
Hash路由：监听hashchange事件，通过location.hash获取路径。History路由：监听popstate事件，使用pushState/replaceState修改URL而不刷新页面。History模式需要服务端配置fallback到index.html。核心实现：路由表（path→component映射）、导航守卫、动态路由匹配、嵌套路由。

{{知识点34}}{{Web Worker与多线程}}
Web Worker在后台线程运行JS，不阻塞主线程。通过postMessage通信，数据通过结构化克隆传递（或Transferable转移所有权）。SharedWorker可被多个页面共享。Service Worker是特殊的Worker，用于拦截网络请求、推送通知、后台同步。Worker不能直接操作DOM。2025年还关注Atomics和SharedArrayBuffer。

{{知识点35}}{{前端测试策略}}
单元测试：Jest/Vitest测试独立函数/组件。集成测试：Testing Library测试组件交互。E2E测试：Playwright/Cypress模拟用户操作。测试金字塔：大量单元测试→适量集成测试→少量E2E测试。Mock策略：mock API请求、mock浏览器API。快照测试：检测UI变化。TDD（测试驱动开发）流程。

{{知识点36}}{{微前端架构}}
将大型前端应用拆分为多个独立子应用。方案：iframe（简单但隔离好）、single-spa（路由劫持加载子应用）、qiankun（基于single-spa，提供JS/CSS沙箱）、Module Federation（Webpack 5，运行时共享模块）。核心挑战：应用隔离（JS沙箱、CSS隔离）、通信机制、公共依赖管理、路由管理。

{{知识点37}}{{算法：数组与字符串}}
高频题：两数之和（哈希表O(n)）、三数之和（排序+双指针）、最长无重复子串（滑动窗口）、合并两个有序数组（双指针从后往前）、最大子数组和（动态规划）。字符串：反转字符串、回文判断、字符串转整数（atoi）、最长公共前缀。时间复杂度和空间复杂度分析是面试必考。

{{知识点38}}{{算法：链表操作}}
反转链表（迭代/递归）、合并两个有序链表、环形链表检测（快慢指针）、删除倒数第N个节点（双指针间隔N）、链表中点（快慢指针）、两个链表的交点。技巧：dummy头节点简化边界处理、快慢指针法、递归与迭代的转换。时间O(n)空间O(1)是常见要求。

{{知识点39}}{{算法：树与递归}}
二叉树遍历：前序/中序/后序（递归+迭代）、层序遍历（BFS）。经典题：最大深度、对称二叉树、路径总和、二叉搜索树验证、最近公共祖先。递归三要素：定义函数含义、终止条件、递推关系。BFS用队列，DFS用栈或递归。平衡二叉树判断、序列化与反序列化也是高频题。

{{知识点40}}{{算法：动态规划}}
核心思想：将大问题分解为子问题，存储子问题结果避免重复计算。经典题：斐波那契数列、爬楼梯、零钱兑换、最长公共子序列（LCS）、最长递增子序列（LIS）、编辑距离、背包问题。解题步骤：定义状态→状态转移方程→初始条件→计算顺序。空间优化：滚动数组降维。

{{知识点41}}{{WebSocket与实时通信}}
WebSocket是全双工通信协议，建立在HTTP升级握手之上。与HTTP轮询对比：实时性更好、减少不必要的请求头开销。心跳机制：定期发送ping/pong保持连接。应用场景：聊天应用、实时数据推送、协同编辑。Socket.IO封装了WebSocket并提供降级方案。SSE（Server-Sent Events）是单向的服务器推送替代方案。

{{知识点42}}{{Monorepo与包管理}}
Monorepo将多个项目放在一个仓库中。工具：Lerna（管理发布）、Turborepo（构建缓存与并行）、pnpm workspace（依赖管理，硬链接节省空间）。优点：代码复用、原子提交、统一版本管理。缺点：仓库体积大、CI复杂。pnpm通过内容寻址存储和硬链接解决node_modules膨胀问题。

{{知识点43}}{{SSR与SSG渲染策略}}
SSR（服务端渲染）：首屏快、SEO友好，但服务器压力大。SSG（静态生成）：构建时生成HTML，适合内容型网站。ISR（增量静态再生）：结合SSG和SSR，按需重新生成。Next.js App Router（React Server Components）：组件默认在服务端执行，减少客户端JS体积。Nuxt 3：Vue生态的全栈框架。流式渲染（Streaming SSR）渐进式发送HTML。
