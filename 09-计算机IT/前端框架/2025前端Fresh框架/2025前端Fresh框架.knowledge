# 2025前端Fresh框架
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}
Fresh 是一个基于 Deno 的轻量、快速、可扩展的全栈 Web 框架，构建于 Web 标准之上。它专注于服务端渲染（SSR），默认不向浏览器发送任何 JavaScript，只有通过 Island 机制显式声明的交互部分才会被水合（hydration）到客户端。其核心理念是"简单、易用、高效"。

{{知识点2}}
Fresh 采用 Island Architecture（岛屿架构），这是其最核心的设计理念。页面大部分是静态 HTML，只有需要交互的部分（称为"岛屿"）才会加载 JavaScript 进行客户端水合。这种方式极大地减少了发送到浏览器的 JS 体积，提升页面加载性能。

{{知识点3}}
Fresh 基于 Preact 构建，Preact 是 React 的轻量级替代品（约 3KB）。所有路由组件默认在服务端渲染为 Preact 组件，生成纯 HTML 发送给浏览器。这使得 Fresh 的渲染速度非常快。

{{知识点4}}
Fresh 使用 Preact Signals 作为响应式状态管理方案。Signals 是 Preact 的响应式原语，当信号值变化时，读取该信号的组件会自动重新渲染，无需 setState 或手动订阅。可以使用 useSignal 创建组件内状态，使用 computed 派生计算值。

{{知识点5}}
Fresh 采用文件系统路由（File-based Routing）。在 routes/ 目录下放置文件即自动注册对应的 URL 路由。文件名即路由路径，支持 index.ts 映射根路径，[slug].ts 映射动态参数，[...path].ts 映射通配符路由。

{{知识点6}}
Fresh 的项目结构包含标准目录：routes/ 存放路由文件，islands/ 存放需要客户端交互的岛屿组件，components/ 存放纯服务端组件，static/ 存放静态资源（CSS、图片等）。main.ts 是服务端入口文件，client.ts 是客户端入口文件。

{{知识点7}}
Fresh 通过 define.page() 定义页面组件，通过 define.handlers() 定义路由处理器。处理器可以在服务端获取数据并通过 page() 函数传递给页面组件的 props.data 属性，实现类型安全的数据传递。

{{知识点8}}
Fresh 的中间件（Middleware）系统允许在路由执行前进行链式处理。中间件接收 Context 对象，可用于设置 HTTP 头、记录日志、进行身份验证等。中间件通过 .use() 方法注册，也可以通过文件系统中的 _middleware.ts 文件定义。

{{知识点9}}
Fresh 内置了多种开箱即用的中间件：cors() 用于设置跨域头部、csrf() 用于 CSRF 防护、csp() 用于内容安全策略、trailingSlashes() 用于强制尾部斜杠。这些中间件直接从 fresh 包导入使用。

{{知识点10}}
Fresh 的 Islands（岛屿）组件定义在 islands/ 目录下，文件名需使用 PascalCase 或 kebab-case。岛屿组件同时在服务端和客户端渲染——服务端生成初始 HTML，客户端加载 JavaScript 实现交互功能。

{{知识点11}}
Fresh 支持向 Island 组件传递 props，但只能传递可序列化的值。支持的类型包括：原始类型（string、number、boolean、bigint）、Date、URL、RegExp、Map、Set、Uint8Array、JSX Elements、Preact Signals 以及 Temporal 对象等。不支持传递函数或类实例。

{{知识点12}}
Fresh 支持嵌套 Islands，即一个岛屿组件内部可以包含其他岛屿组件。在嵌套场景下，子岛屿表现为普通 Preact 组件，但仍会接收序列化后的 props。这使得可以在同一页面中灵活组合静态和交互部分。

{{知识点13}}
Fresh 可以将服务端渲染的 JSX 作为 props 传递给 Island 组件。这是一个强大的特性，允许在服务端预渲染复杂内容，然后作为子元素传递给需要交互的岛屿组件。

{{知识点14}}
Fresh 使用 IS_BROWSER 常量来区分服务端和客户端环境。在岛屿组件中，可以通过 import { IS_BROWSER } from "fresh/runtime" 获取该布尔值，在服务端为 false，在浏览器为 true。这用于保护仅客户端可用的 API（如 EventSource、navigator.getUserMedia）。

{{知识点15}}
Fresh 的路由匹配优先级：静态路由（精确路径匹配如 /about）优先于动态路由（模式匹配如 /posts/:id）。动态路由按注册顺序匹配，第一个匹配的路由获胜。HEAD 请求在没有专用处理器时自动回退到 GET 处理器。

{{知识点16}}
Fresh 支持完整的 URLPattern 语法进行路径匹配。通过 App 实例的方法注册路由：.get()、.post()、.put()、.delete()、.head()、.patch()、.options() 分别对应 HTTP 方法，.all() 匹配所有方法。如果路由不匹配请求方法，返回 405 Method Not Allowed。

{{知识点17}}
Fresh 的文件系统路由支持路由组（Route Groups），使用括号命名的目录（如 (marketing)）不会影响 URL 路径，但可以用于分组路由并应用不同的布局或中间件。还支持 (_islands) 目录就近放置岛屿组件，以及 (_components) 目录放置仅被附近路由使用的组件。

{{知识点18}}
Fresh 的路由文件支持导出 RouteConfig 配置对象，其中 routeOverride 可以指定自定义的 URL Pattern 替代文件路径推导的模式。还可以通过导出 css 数组为特定路由加载额外的 CSS 文件。

{{知识点19}}
Fresh 的布局系统（Layouts）通过 _layout.tsx 文件实现。布局自动按目录树嵌套——从 _app.tsx（最外层 HTML 壳）到各级 _layout.tsx，再到页面组件。每层布局接收 Component 子组件以及其他 props 如 state 和 url。

{{知识点20}}
Fresh 的布局支持异步操作，可以在渲染前获取数据。通过 skipInheritedLayouts 配置可以跳过从父目录继承的布局，适用于登录页、全屏仪表盘等需要完全不同 UI 的场景。还可以在布局文件本身设置 skipInheritedLayouts 来实现独立的区块外壳。

{{知识点21}}
Fresh 的 Partials（部分内容更新）机制允许页面的特定区域在不刷新整个页面的情况下更新。通过在 HTML 元素上添加 f-client-nav 属性并用 <Partial name="..."> 组件包裹需要更新的区域来启用。当用户点击链接时，Fresh 从服务器获取新页面并仅更新匹配的部分区域。

{{知识点22}}
Fresh 的 Partials 支持三种替换模式：replace（默认，替换全部内容）、prepend（在现有内容前插入）、append（在现有内容后追加）。使用 prepend 或 append 模式时必须添加 key 属性，以便 Preact 区分新旧子元素。

{{知识点23}}
Fresh 支持 f-partial 属性优化部分请求。在链接上设置 f-partial="/optimized-route"，Fresh 会导航到 href 指定的页面 URL，但从 f-partial 指定的 URL 获取更新内容。这可以创建高度优化的路由，只返回需要的部分内容，避免渲染完整页面。

{{知识点24}}
Fresh 的 Partials 支持加载指示器功能。通过 _freshIndicator 属性附加到触发动态导航的元素上，当请求开始时 value 设为 true，完成或失败时设为 false。通常与 Preact Signals 配合使用，自动触发组件重新渲染以显示加载状态。

{{知识点25}}
Fresh 集成了浏览器原生的 View Transitions API，只需在 _app.tsx 的 body 元素上添加 f-client-nav 和 f-view-transition 属性即可启用。所有部分导航（链接点击、表单提交、前进后退）都会自动应用过渡动画，默认为交叉淡入淡出效果。

{{知识点26}}
Fresh 的 View Transitions 可以通过 CSS 自定义动画，使用 ::view-transition-old 和 ::view-transition-new 伪元素。还可以通过 view-transition-name CSS 属性为特定元素分配独立的过渡效果，实现导航栏固定不动而内容区域滑动的效果。这是渐进增强——不支持的浏览器仍正常工作。

{{知识点27}}
Fresh 内置 WebSocket 支持。最简单的方式是使用 app.ws() 注册 WebSocket 端点，定义 open、message、close 事件处理器。也可以在路由处理器中使用 ctx.upgrade() 升级 HTTP 连接，支持托管模式（传入事件处理器对象）和裸模式（获取原始 WebSocket 对象）。

{{知识点28}}
Fresh 支持多种部署方式。推荐使用 Deno Deploy 一键部署，自动创建分支预览和收集指标。也可以打包为 Docker 容器部署到任何云平台，或使用 deno compile 编译为单一二进制文件。Fresh 2 会在构建步骤中生成 _fresh/server.js 作为服务端入口。

{{知识点29}}
Fresh 内置 OpenTelemetry 支持。自动为中间件执行、路由处理器执行、页面渲染、静态文件服务、懒加载路由等关键操作创建追踪跨度。无需修改代码，只需配置导出器。在 Deno Deploy 上使用 Fresh 预设时会自动收集追踪数据。

{{知识点30}}
Fresh 的 OpenTelemetry 支持客户端追踪关联。当启用导出器时，Fresh 自动在每个渲染页面的 head 中注入 W3C Trace Context 的 meta 标签（traceparent），使客户端 OpenTelemetry 工具可以将浏览器性能追踪关联到服务端跨度，实现端到端可观测性。

{{知识点31}}
Fresh 的 CSP（内容安全策略）中间件支持基于 nonce 的严格安全模式。启用 useNonce 后，Fresh 自动为每个请求生成唯一的 nonce 值，注入到所有内联 script 和 style 标签的 nonce 属性中，并在 CSP 头部使用 'nonce-{value}' 替代 'unsafe-inline'。

{{知识点32}}
Fresh 的序列化系统支持循环引用和重复引用。如果同一个对象或信号在 props 树中出现多次，它只会被序列化一次，客户端反序列化时会恢复所有引用关系。这对于共享状态的多个岛屿组件非常重要。

{{知识点33}}
Fresh 的 Signals 序列化机制：服务端通过 .peek() 读取信号当前值并序列化；客户端将值包装回 live signal() 调用。如果同一个信号对象传递给多个岛屿，它只序列化一次，所有岛屿在客户端接收相同的信号实例，保持状态同步。

{{知识点34}}
Fresh 可以在文件系统的任意目录中放置 (_islands) 目录来就近定义岛屿组件，这使得岛屿组件可以与使用它们的路由组件放在同一目录下，提高代码的组织性和可维护性。

{{知识点35}}
Fresh 的 Handler 函数支持导出单一函数处理所有 HTTP 方法，也支持导出 handlers 对象按方法名分别处理 GET、POST 等。处理器中可通过 ctx.params 访问路由参数，通过 ctx.state 访问中间件设置的状态，通过 ctx.render() 渲染 JSX。

{{知识点36}}
Fresh 支持异步页面组件，即在 define.page() 中直接编写异步数据获取逻辑，无需单独定义处理器。这对于不需要处理器和组件之间类型安全数据桥梁的简单页面非常方便。还可以通过 throw HttpError(404) 抛出 HTTP 错误。

{{知识点37}}
Fresh 通过 ctx.state 实现中间件到处理器/组件的状态传递。中间件可以在 ctx.state 上设置任意属性（如用户会话信息），下游的处理器和页面组件都可以访问这些状态。App 实例支持泛型参数来定义 state 的类型。

{{知识点38}}
Fresh 的 App 类是框架的核心入口，通过链式调用配置应用：.use() 注册中间件、.get/.post 等注册路由、.fsRoutes() 导入文件系统路由、.ws() 注册 WebSocket 端点。支持 basePath 配置基础路径前缀。

{{知识点39}}
Fresh 使用 Vite 作为构建工具，项目中包含 vite.config.ts 配置文件。通过 fresh() Vite 插件可以配置路由目录等选项。Deno Deploy 会在部署时自动运行 deno task build（即 vite build）完成构建步骤。

{{知识点40}}
Fresh 的环境变量通过 Deno.env.get() 访问。需要在客户端（岛屿代码）使用的环境变量必须以 FRESH_PUBLIC_ 为前缀，非公开变量仅在服务端可用，确保敏感信息不会泄露到浏览器。

{{知识点41}}
Fresh 非常适合 AI 辅助开发。其小巧的 API 面积和基于文件的约定使 AI 代理可以用最少的上下文脚手架路由、添加中间件和构建功能，因为框架简单且可预测。官方文档明确推荐将其作为 AI 辅助开发的理想框架。

{{知识点42}}
Fresh 的典型使用场景包括：Web API、电子商务商店、作品集网站、落地页和文档站、CRUD 应用等以服务端渲染为主的项目。不适合构建单页应用（SPA），因为 SPA 需要大量客户端 JavaScript。

{{知识点43}}
Fresh 的官方宣传案例显示，巴西顶级电商平台 deco.cx 使用 Fresh 构建其店铺前端，实现了 5 倍的页面加载速度提升和 30% 的转化率提升。Fresh 也是 deno.com 和 Deno Deploy 官网的底层框架。

{{知识点44}}
Fresh 2.x 版本引入了程序化路由注册 API（new App()），与文件系统路由并存。通过 App 实例的链式方法可以直接注册路由和中间件，提供了更大的灵活性。两种方式可以混合使用。

{{知识点45}}
Fresh 支持 Web Components（自定义元素），但必须在客户端注册，因为 customElements.define() 是浏览器 API。通常在岛屿组件中使用 useEffect 注册自定义元素，并在服务端返回空的占位 HTML。第三方 Web Component 库（如 Shoelace）同样适用此模式。
