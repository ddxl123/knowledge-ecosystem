# 2025前端Fullscreen API
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}{{Fullscreen API 概述：Fullscreen API 是一组允许网页元素以全屏模式显示的 Web API。它没有自己独立的接口，而是通过扩展 Document、Element 等现有接口来提供全屏功能，包括方法、属性和事件处理器。规范由 WHATWG 维护，地址为 fullscreen.spec.whatwg.org。}}

{{知识点2}}{{Element.requestFullscreen() 方法：这是进入全屏模式的核心方法，定义在 Element 接口上。调用后会异步请求将指定元素（及其所有后代元素）置于全屏模式，移除浏览器 UI 元素及其他应用。返回一个 Promise，在全屏激活成功时 resolve，失败时 reject。}}

{{知识点3}}{{requestFullscreen() 的选项参数：该方法接受一个可选的 options 对象，包含三个属性：keyboardLock（控制键盘锁定模式，可选 "none"/"browser"）、navigationUI（控制是否显示导航 UI，可选 "auto"/"hide"/"show"）、screen（指定全屏显示在哪个屏幕上，接受 ScreenDetailed 对象）。}}

{{知识点4}}{{Document.exitFullscreen() 方法：这是退出全屏模式的方法，定义在 Document 接口上。调用后请求将当前处于全屏模式的元素恢复为窗口模式。返回一个 Promise，在完全退出全屏后 resolve。如果存在多个全屏元素，此方法只退出最顶层的一个。}}

{{知识点5}}{{Document.fullscreenElement 属性：这是一个只读属性，返回当前正在以全屏模式显示的 Element 对象。如果没有元素处于全屏模式则返回 null。如果有多个元素处于全屏模式，返回最近请求全屏的那个（最顶层）。修改此属性不会报错，但 setter 是空操作。}}

{{知识点6}}{{Document.fullscreenEnabled 属性：这是一个只读属性（布尔值），指示当前文档是否可以进入全屏模式。当页面没有窗口化插件且所有包含该文档的 iframe 都设置了 allowfullscreen 属性时为 true。在请求全屏前应先检查此属性。}}

{{知识点7}}{{fullscreenchange 事件：当浏览器进入或退出全屏模式后立即触发此事件。事件发送给正在转换全屏状态的 Element，然后冒泡到 Document。此事件不可取消。通过检查 document.fullscreenElement 的值来判断是进入还是退出全屏。}}

{{知识点8}}{{fullscreenerror 事件：当尝试将元素切换到全屏模式或退出全屏模式时发生错误，会触发此事件。常见原因包括 Permissions Policy 限制、元素不兼容、缺少用户激活等。在 Chrome 中不会生成警告日志，但 Firefox 会在控制台输出错误原因。}}

{{知识点9}}{{用户激活要求（Transient User Activation）：requestFullscreen() 需要用户交互才能触发。必须在事件处理器中调用（如 click、keydown 等），否则请求会被拒绝。这是安全措施，防止网站未经用户同意强制全屏。}}

{{知识点10}}{{兼容的全屏元素：要进入全屏的元素必须满足以下条件：必须是标准 HTML 元素或 svg/math 元素；不能是 dialog 元素；必须位于顶级文档中或在设置了 allowfullscreen 属性的 iframe 内。}}

{{知识点11}}{{键盘锁定模式（Keyboard Lock）：requestFullscreen() 的 keyboardLock 选项设为 "browser" 时启用。在该模式下，浏览器会将更多键盘事件转发给应用，而不是由浏览器或系统处理。例如可让 Esc 键用于打开游戏菜单而非退出全屏。需配合 event.preventDefault() 使用。}}

{{知识点12}}{{keyboardLock 的安全限制：某些键组合如 Ctrl+Alt+Delete（Windows）无法被拦截，因为涉及系统控制或隐私风险。浏览器鼓励提供额外机制退出全屏键盘锁定模式，大多数浏览器使用长按 Esc 来退出键盘锁定状态。}}

{{知识点13}}{{navigationUI 选项：控制全屏时是否显示导航 UI。"hide" 表示隐藏浏览器导航界面，整个屏幕用于显示元素；"show" 表示显示页面导航控件；"auto" 让浏览器自行决定（默认值）。}}

{{知识点14}}{{screen 选项（多显示器支持）：通过 requestFullscreen() 的 screen 参数可以指定元素在哪个屏幕上全屏显示。需要配合 Window.getScreenDetails() 获取 ScreenDetails 对象，再从中选取目标 ScreenDetailed 对象。这是一个较新的特性。}}

{{知识点15}}{{CSS :fullscreen 伪类：匹配当前处于全屏模式的元素。如果多个元素同时全屏，会匹配所有全屏元素。常用于在全屏状态下调整样式，如改变背景色、布局等。属于 Selectors Level 4 规范。}}

{{知识点16}}{{CSS ::backdrop 伪类：当元素处于全屏模式时，浏览器会在该元素后面绘制一个背景层。可以通过 ::backdrop 伪元素来设置这个背景的样式，例如改变背景颜色或透明度。}}

{{知识点17}}{{Top Layer（顶层）概念：当元素进入全屏模式时，它会被放置在浏览器的"顶层"中。顶层中的元素不受普通文档流和 z-index 的影响，始终显示在页面其他内容之上。多个全屏元素按请求时间堆叠。}}

{{知识点18}}{{权限策略（Permissions Policy）控制：全屏功能通过 Permissions Policy 的 "fullscreen" 指令控制。默认允许列表为 "self"，意味着只允许顶级文档和同源嵌套上下文使用全屏。第三方内容需要服务器设置 Permissions-Policy 头并在 iframe 上添加 allow="fullscreen" 属性。}}

{{知识点19}}{{Permissions-Policy 头配置示例：要在第三方 iframe 中允许全屏，服务器需设置头 Permissions-Policy: fullscreen=(self b.example.com)，同时 iframe 元素需添加 allow="fullscreen" 属性。使用 Permissions API 可以测试全屏权限的 grant/deny/prompt 状态。}}

{{知识点20}}{{iframe 中的全屏：嵌入的 iframe 内容默认不能使用全屏。父页面需要在 iframe 标签上设置 allowfullscreen 属性（旧写法）或 allow="fullscreen" 属性（新写法）来允许嵌入内容请求全屏。}}

{{知识点21}}{{Promise 化的 API：现代 Fullscreen API 的 requestFullscreen() 和 exitFullscreen() 都返回 Promise，支持 async/await 语法。可以使用 .then()/.catch() 或 try/catch 来处理成功和失败。这是对旧版回调式 API 的改进。}}

{{知识点22}}{{requestFullscreen() 的错误类型：Promise reject 时可能抛出 TypeError（文档非活跃、元素不在文档中、权限不允许、元素与文档相同、popover 已显示等情况）或 NotSupportedError（browser keyboardLock 不被支持时）。}}

{{知识点23}}{{全屏与用户退出：用户始终可以通过按 Esc 键或 F11 键退出全屏，无需等待程序化退出。此外，导航到其他页面、切换标签页、使用 Alt+Tab 切换应用也会自动退出全屏模式。开发者应在 UI 中告知用户这些退出方式。}}

{{知识点24}}{{全屏切换模式的经典实现：通过检查 document.fullscreenElement 是否为 null 来判断当前状态。若为 null 则调用 element.requestFullscreen() 进入全屏，否则调用 document.exitFullscreen() 退出。这是最常见的全屏切换模式。}}

{{知识点25}}{{Shadow DOM 中的全屏支持：ShadowRoot 也提供 fullscreenElement 属性。当全屏元素位于 Shadow DOM 内部时，可以通过 shadowRoot.fullscreenElement 来获取全屏元素，这为 Web Components 提供了全屏支持。}}

{{知识点26}}{{Document.fullscreen 已废弃属性：旧版 API 中的 document.fullscreen 布尔属性已被废弃，取而代之的是 document.fullscreenElement。新代码应使用 fullscreenElement 来判断是否处于全屏状态。}}

{{知识点27}}{{移动端浏览器的视口缩放问题：某些移动浏览器在全屏模式下会忽略 viewport meta 标签设置并阻止用户缩放。例如，捏合缩放手势在全屏模式下可能无效，即使在非全屏模式下该页面支持缩放。开发者需要注意此行为差异。}}

{{知识点28}}{{全屏事件的判断逻辑：fullscreenchange 事件本身不提供是进入还是退出全屏的信息。需要在事件处理器中检查 document.fullscreenElement 的值：如果非 null 则是进入全屏，如果为 null 则是退出全屏。}}

{{知识点29}}{{事件监听方式：可以通过 addEventListener("fullscreenchange", handler) 或设置 onfullscreenchange 属性来监听全屏变化事件。fullscreenerror 事件同理，使用 "fullscreenerror" 事件名或 onfullscreenerror 属性。}}

{{知识点30}}{{requestFullscreen() 需要异步检测：由于 requestFullscreen() 是异步操作，不能在调用后立即检查 fullscreenElement。应使用返回的 Promise 或监听 fullscreenchange 事件来确认全屏状态变化。}}

{{知识点31}}{{全屏视频播放的典型应用：Fullscreen API 最常见的应用场景之一是视频播放器。视频元素调用 requestFullscreen() 后配合 :fullscreen 伪类可以实现全屏播放器的自适应布局和控件样式调整。}}

{{知识点32}}{{检查全屏可用性的最佳实践：在请求全屏前应依次检查：1) document.fullscreenEnabled 是否为 true；2) 当前是否已有全屏元素（document.fullscreenElement）；3) 请求必须在用户交互事件处理器中调用。三步检查可最大程度避免运行时错误。}}

{{知识点33}}{{旧版浏览器前缀 API：早期浏览器使用带前缀的 API，如 webkitRequestFullScreen()、mozRequestFullScreen()、webkitExitFullscreen() 等。现代浏览器已统一到无前缀的标准 API，但在某些旧项目中可能仍会遇到带前缀的写法。}}

{{知识点34}}{{全屏与对话框元素的限制：dialog 元素不能直接调用 requestFullscreen() 进入全屏。如果尝试对 dialog 元素调用，Promise 会 reject 并抛出 TypeError。这是规范中的明确限制。}}

{{知识点35}}{{多元素全屏堆叠机制：当多个元素同时处于全屏模式时，它们都在 Top Layer 中按请求时间堆叠。最近请求的元素显示在最上面。Document.fullscreenElement 返回最顶层元素。exitFullscreen() 只移除最顶层元素，露出下层元素。Esc 键则退出所有全屏元素。}}

{{知识点36}}{{Fullscreen API 的规范状态：Fullscreen API 由 WHATWG 维护，规范地址为 https://fullscreen.spec.whatwg.org/。所有现代浏览器（Chrome、Firefox、Safari、Edge）均已实现标准 API。2025 年状态为成熟稳定的 Web 标准。}}

{{知识点37}}{{全屏与画中画的关系：Fullscreen API 和 Picture-in-Picture API 是两个独立的 API。全屏将元素扩展到整个屏幕，而画中画将视频缩小为浮动窗口。两者可以组合使用但互不依赖，各有独立的进入/退出事件和状态检查方法。}}

{{知识点38}}{{全屏状态下的键盘事件处理：在全屏模式下，浏览器通常保留 Esc 键和 F11 键用于退出全屏。如果需要拦截这些键（如游戏场景），必须使用 keyboardLock: "browser" 选项并配合 event.preventDefault()，否则按键的默认行为无法被阻止。}}

{{知识点39}}{{Fullscreen API 与安全上下文：Fullscreen API 在安全上下文（HTTPS）下工作最佳。虽然规范没有强制要求 HTTPS，但 Permissions Policy 等安全特性在非安全上下文中可能受到更严格的限制。生产环境应始终使用 HTTPS。}}

{{知识点40}}{{全屏模式下的 CSS 布局策略：元素进入全屏后默认占据整个屏幕。推荐使用 :fullscreen 伪类设置全屏专属样式，如 display: flex、object-fit: contain 等，确保内容在不同屏幕尺寸下正确显示。可结合 ::backdrop 设置全屏背景。}}
