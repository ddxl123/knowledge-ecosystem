# 2025前端前端CSS Colors
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}{{CSS颜色概述：CSS Colors模块定义了颜色、颜色类型、颜色混合、透明度，以及如何将这些颜色和效果应用于HTML内容。该模块包含两个核心CSS属性：color和opacity，但超过20个CSS和SVG属性、CSS图像、at-rules和@media规则都依赖于这两个属性。规范来源为CSS Color Module Level 4和Level 5。}}

{{知识点2}}{{命名颜色（Named Colors）：CSS定义了超过160个标准命名颜色关键字，如red、blue、orange、cornflowerblue、rebeccapurple等。所有命名颜色关键字不区分大小写，使用RGB模型并关联sRGB色彩空间。特殊关键字包括transparent（完全透明）和currentColor（继承当前color属性值）。}}

{{知识点3}}{{currentColor关键字：currentColor代表元素的color属性值，允许在通常不接受color值的属性上使用该颜色。如果currentColor用作color属性的值，则它从color属性的继承值中获取值。常用于border-color、background-color等属性中实现颜色统一。}}

{{知识点4}}{{系统颜色（System Colors）：CSS定义了一组系统颜色关键字，如AccentColor、ButtonFace、ButtonText、Canvas、CanvasText、Field、FieldText、GrayText、Highlight、HighlightText、LinkText、Mark、MarkText、SelectedItem、SelectedItemText、VisitedText等，这些颜色反映用户、浏览器或操作系统的默认颜色选择。}}

{{知识点5}}{{十六进制颜色表示法（Hex Color）：以#开头，支持#rgb（3位简写）、#rrggbb（6位完整）、#rgba（4位带透明度简写）和#rrggbbaa（8位带透明度完整）四种格式。每个分量用十六进制数表示，范围00-FF（对应0-255）。简写时每位数字自动重复，如#f00等同于#ff0000。}}

{{知识点6}}{{RGB函数表示法：rgb()函数接受3或4个参数——红、绿、蓝分量和可选的alpha通道。每个分量可以是0-255的数值或0%-100%的百分比。alpha通道可用百分比（0%-100%）或小数（0.0-1.0）表示。现代语法使用空格分隔：rgb(255 0 153 / 80%)，旧版逗号语法仍兼容。}}

{{知识点7}}{{HSL函数表示法：hsl()使用色相（Hue）、饱和度（Saturation）、亮度（Lightness）三元组描述颜色。色相是角度值（0deg-360deg），0deg为红色，经过黄、绿、青、蓝、品红后回到360deg红色。饱和度为百分比，100%完全饱和，0%为灰度。亮度从0%（黑）到100%（白）。支持可选alpha通道。}}

{{知识点8}}{{HWB函数表示法：hwb()使用色相（Hue）、白度（Whiteness）、黑度（Blackness）描述颜色。色相坐标系统与hsl()相同。白度和黑度为百分比，允许选择色相后混入白色和/或黑色。语法示例：hwb(12 50% 0%)或hwb(194 0% 0% / 0.5)。}}

{{知识点9}}{{Lab颜色函数：lab()使用CIELAB色彩空间，参数为亮度（Lightness，百分比）、A轴（红绿轴）和B轴（黄蓝轴）。CIELAB是感知均匀的色彩空间，设计为人眼感知的近似。示例：lab(50% 40 59.5)。支持可选alpha通道。}}

{{知识点10}}{{LCH颜色函数：lch()使用CIELAB色彩空间的柱坐标表示，参数为亮度（Lightness）、色度（Chroma）和色相（Hue）。色度表示颜色的鲜艳程度，色相为角度值。示例：lch(52.2% 72.2 50)。相比lab()更直观，因为色相是角度值。}}

{{知识点11}}{{Oklab颜色函数：oklab()使用Oklab色彩空间，由Björn Ottosson设计，旨在比CIELAB更好地感知均匀。参数为亮度（百分比）、A轴和B轴。示例：oklab(59% 0.1 0.1)。Oklab是现代CSS颜色处理的重要基础。}}

{{知识点12}}{{OkLCh颜色函数：oklch()是Oklab色彩空间的柱坐标表示，参数为亮度（Lightness）、色度（Chroma）和色相（Hue）。示例：oklch(60% 0.15 50)。OkLCh是CSS颜色插值的默认色彩空间，在现代CSS中被广泛推荐使用。}}

{{知识点13}}{{color()函数：color()函数允许使用预定义色彩空间指定颜色，支持sRGB、srgb-linear、display-p3、a98-rgb、prophoto-rgb、rec2020、xyz、xyz-d50、xyz-d65等色彩空间。语法为color(color-space value1 value2 value3)。这使得CSS能够表达超出sRGB色域的广色域颜色。}}

{{知识点14}}{{color-mix()颜色混合函数：color-mix()允许在指定色彩空间中混合两种颜色。语法为color-mix(in color-space, color1, color2)，可选指定每种颜色的比例。例如color-mix(in oklch, red 40%, blue)会在OkLCh空间中混合40%红色和60%蓝色。这是CSS原生的颜色混合能力。}}

{{知识点15}}{{相对颜色（Relative Colors）：相对颜色语法允许基于现有颜色输出新颜色。任何颜色函数都可以接受origin color后跟from关键字，然后定义输出颜色的各通道值。例如hsl(from red 240deg s l)将红色的色相改为240度，lch(from blue calc(l + 20) c h)将蓝色亮度增加20%。}}

{{知识点16}}{{light-dark()函数：light-dark()接受两个颜色参数，第一个用于亮色方案（light color-scheme），第二个用于暗色方案（dark color-scheme）。语法：light-dark(white, black)。需要在CSS中通过color-scheme属性或meta标签声明支持亮暗模式才能生效。}}

{{知识点17}}{{contrast-color()函数：contrast-color()用于指定一个颜色，函数会返回与之形成对比的颜色。主要用于确保文本在背景色上的可读性。目前该函数仍在规范讨论中，浏览器支持有限。}}

{{知识点18}}{{device-cmyk()函数：device-cmyk()用于指定CMYK（青、品红、黄、黑）颜色，主要用于打印场景。目前尚无浏览器支持此功能。语法支持传统逗号分隔语法和现代语法。}}

{{知识点19}}{{透明度与Alpha通道：CSS颜色支持alpha通道控制透明度。所有颜色函数都可通过/分隔符添加alpha值，如rgb(255 0 0 / 0.5)或hsl(0 100% 50% / 80%)。十六进制支持4位（#rgba）和8位（#rrggbbaa）格式。alpha值为0%完全透明，100%完全不透明。}}

{{知识点20}}{{透明关键字transparent：transparent是CSS预定义的颜色关键字，等同于rgba(0, 0, 0, 0)，即完全透明的黑色。在颜色过渡和动画中，transparent常用于实现淡入淡出效果。}}

{{知识点21}}{{缺失颜色分量（Missing Components）：在非传统逗号语法中，任何颜色函数的分量都可以指定为none关键字表示缺失。缺失分量在非插值场景下等效于零值（0、0%或0deg）。在颜色插值中，缺失分量有特殊语义——可从另一颜色中获取对应值。}}

{{知识点22}}{{颜色插值（Color Interpolation）：颜色插值发生在渐变、过渡和动画中。插值时颜色首先转换到指定色彩空间，然后线性插值各分量。默认插值色彩空间为Oklab，可通过color-interpolation-method覆盖。不同色彩空间的插值会产生不同的视觉效果。}}

{{知识点23}}{{跨色彩空间插值中的缺失分量处理：当在不同色彩空间中插值时，缺失分量通过类比分量传递。类比关系为：R↔X（红）、G↔Y（绿）、B↔Z（蓝）、L↔L（亮度）、C↔S（色彩丰富度）、H↔H（色相）、a↔a、b↔b。处理步骤包括零值替换、转换到插值色彩空间、重置类比缺失分量、从另一颜色补充。}}

{{知识点24}}{{CSS颜色属性color：color属性设置元素的文本内容的前景色。它是CSS中最基本的颜色属性，接受所有<color>值。该属性是继承的，子元素会继承父元素的文本颜色，除非显式覆盖。}}

{{知识点25}}{{opacity属性：opacity属性设置元素的整体透明度，值范围为0（完全透明）到1（完全不透明）。与alpha通道不同，opacity影响整个元素包括其所有子元素和背景。opacity值小于1的元素会创建新的层叠上下文。}}

{{知识点26}}{{色彩空间（Color Space）概念：色彩空间是颜色的数学表示系统。sRGB是Web标准色彩空间，覆盖约35%的可见色域。display-p3是苹果推广的广色域空间，比sRGB大约大50%。CIELAB和Oklab是感知均匀色彩空间，适合颜色计算。不同色彩空间的选择影响颜色混合和插值的结果。}}

{{知识点27}}{{颜色插值方法（color-interpolation-method）：CSS允许在渐变和颜色混合中指定插值色彩空间，语法为in color-space。例如linear-gradient(in oklch, red, blue)会在OkLCh空间中插值。不同插值空间产生不同中间色：在sRGB中插值红到蓝会经过暗紫色，在OkLCh中则更明亮均匀。}}

{{知识点28}}{{CSS颜色与可访问性：WCAG 2.2强烈建议不要仅使用颜色传达特定信息、动作或结果。需要确保文本与背景的对比度满足最低要求（AA级：普通文本4.5:1，大文本3:1；AAA级：普通文本7:1，大文本4.5:1）。色盲用户约占男性人口的8%，设计时应考虑颜色无障碍。}}

{{知识点29}}{{color-scheme属性：color-scheme属性告诉浏览器元素应使用哪种配色方案渲染，可选值包括light、dark、normal等。配合light-dark()函数使用，可实现自动适配系统亮暗模式。可在html元素上设置color-scheme: light dark来全局启用。}}

{{知识点30}}{{接受颜色值的CSS属性：许多CSS属性接受<color>值，包括background-color、border-color、outline-color、box-shadow、text-shadow、text-decoration-color、text-emphasis-color、column-rule-color、caret-color、accent-color、scrollbar-color等。此外SVG属性如fill、stroke、flood-color、lighting-color、stop-color也接受颜色值。}}

{{知识点31}}{{颜色在渐变中的应用：CSS渐变函数（linear-gradient、radial-gradient、conic-gradient）中的颜色停靠点使用<color>值。通过指定插值色彩空间可以改变渐变的中间色过渡效果。conic-gradient特别适合创建色轮效果，结合oklch插值可获得感知均匀的渐变。}}

{{知识点32}}{{CSS颜色与Canvas API：Canvas 2D API的fillStyle和strokeStyle属性接受CSS颜色字符串，包括所有现代颜色函数。这使得Canvas绘图可以使用与CSS相同的颜色表示法，包括oklch()、lab()等新函数，前提是浏览器支持。}}

{{知识点33}}{{@color-profile at规则：CSS Colors模块引入了@color-profile at规则及其组件rendering-intent和src描述符，用于定义自定义颜色配置文件。目前尚无浏览器支持此功能。该功能将允许精确控制打印和显示设备的颜色表现。}}

{{知识点34}}{{CSS颜色值序列化：CSSOM API将颜色值序列化为标准化字符串表示。现代语法中，rgb()和hsl()使用空格分隔的新语法序列化。浏览器开发者工具中的颜色值显示遵循此序列化规范。}}

{{知识点35}}{{颜色函数的现代语法 vs 传统语法：现代CSS颜色函数使用空格分隔参数并用/分隔alpha值（如rgb(255 0 0 / 0.5)），传统语法使用逗号分隔（如rgb(255, 0, 0, 0.5)）。现代语法支持none关键字表示缺失分量，传统语法不支持。新项目推荐使用现代语法。}}

{{知识点36}}{{color-gamut媒体查询：@media (color-gamut: srgb|p3|rec2020)媒体查询允许检测设备支持的色域范围，从而为广色域设备提供更丰富的颜色。display-p3颜色仅在支持p3色域的设备上才能正确显示，其他设备会回退到最接近的sRGB颜色。}}

{{知识点37}}{{forced-colors媒体查询与forced-color-adjust：@media (forced-colors: active)检测浏览器是否启用了强制颜色模式（如Windows高对比度模式）。forced-color-adjust属性控制元素在强制颜色模式下的行为，可选值包括auto、none、preserve-parent-color。}}

{{知识点38}}{{动态范围限制：dynamic-range-limit属性和dynamic-range-limit-mix()函数用于控制HDR内容的高动态范围颜色显示。这在支持HDR的显示器上允许开发者控制高亮度颜色的表现。}}

{{知识点39}}{{accent-color属性：accent-color属性设置表单控件（如复选框、单选按钮、范围滑块）的强调颜色。它接受任何<color>值，并自动计算适当的对比色用于控件的前景元素。简化了自定义表单样式的工作。}}

{{知识点40}}{{scrollbar-color属性：scrollbar-color属性设置滚动条轨道和滑块的颜色，接受两个<color>值：第一个为滑块颜色，第二个为轨道颜色。提供了跨浏览器自定义滚动条外观的标准化方法。}}
