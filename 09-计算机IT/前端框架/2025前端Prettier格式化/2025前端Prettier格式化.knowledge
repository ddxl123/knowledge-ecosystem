# 2025前端Prettier格式化
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}
Prettier 是一个固执己见（opinionated）的代码格式化工具，支持 JavaScript、JSX、Angular、Vue、Flow、TypeScript、CSS/Less/SCSS、HTML、JSON、GraphQL、Markdown、YAML 等多种语言。它通过解析代码为 AST 并重新打印，消除原始格式差异，确保整个代码库风格一致。

{{知识点2}}
Prettier 的核心工作原理是将代码解析为抽象语法树（AST），然后根据配置规则和行宽限制重新打印代码。它不关心原始格式，只关注语法正确性，从而保证输出结果的一致性。

{{知识点3}}
Print Width（行宽）选项通过 --print-width 或 printWidth 配置，默认值为 80。它不是硬性最大行长度限制，而是 Prettier 尽力接近的目标值。推荐不超过 80 个字符以保持可读性。与 ESLint 的 max-len 不同，printWidth 指定的是首选行长度。

{{知识点4}}
Tab Width（缩进宽度）通过 --tab-width 或 tabWidth 配置，默认值为 2。指定每个缩进级别使用的空格数。.editorconfig 文件中的 indent_size 或 tab_width 设置也会被 Prettier 读取（除非被覆盖）。

{{知识点5}}
Tabs 选项通过 --use-tabs 或 useTabs 配置，默认值为 false。设为 true 时使用制表符缩进而非空格。Prettier 使用 SmartTabs 模式：用制表符缩进，但用空格进行对齐（如三元表达式对齐）。

{{知识点6}}
Semicolons（分号）选项通过 --no-semi 或 semi 配置，默认值为 true。设为 false 时，只在可能引发 ASI（自动分号插入）失败的行首添加分号。

{{知识点7}}
Quotes（引号）选项通过 --single-quote 或 singleQuote 配置，默认值为 false（使用双引号）。设为 true 时优先使用单引号。当字符串中某种引号数量更多时，Prettier 会使用较少的引号类型来避免转义。JSX 引号由单独的 jsxSingleQuote 控制。

{{知识点8}}
Trailing Commas（尾逗号）选项通过 --trailing-comma 或 trailingComma 配置。在 Prettier v3.0.0 中默认值从 "es5" 改为 "all"。可选值："all"（所有可能的位置，包括函数参数）、"es5"（ES5 合法位置）、"none"（不添加）。"all" 需要 ES2017+ 运行环境或降级编译。

{{知识点9}}
Bracket Spacing（括号间距）通过 --no-bracket-spacing 或 bracketSpacing 配置，默认值为 true。true 时输出 { foo: bar }，false 时输出 {foo: bar}。

{{知识点10}}
Arrow Function Parentheses（箭头函数括号）通过 --arrow-parens 配置，默认值为 "always"。"always" 始终添加括号如 (x) => x，"avoid" 可省略如 x => x。v2.0.0 默认从 "avoid" 改为 "always"，因为始终使用括号便于后续添加类型注解、额外参数等。

{{知识点11}}
Quote Props（属性引号）通过 --quote-props 配置，默认值为 "as-needed"。可选值："as-needed"（仅必要时加引号）、"consistent"（对象中有一个需要引号则全部加引号）、"preserve"（保留原始输入）。在 Angular、TypeScript、Flow 和 Vue 中，数字属性名不会被去引号。

{{知识点12}}
JSX Quotes（JSX 引号）通过 --jsx-single-quote 或 jsxSingleQuote 配置，默认值为 false。独立于普通引号设置，控制 JSX 属性使用单引号还是双引号。

{{知识点13}}
Bracket Same Line（括号同行）通过 --bracket-same-line 或 bracketSameLine 配置，默认值为 false。控制多行 HTML/JSX/Vue 元素的闭合 > 是否放在最后一行末尾（true）还是单独一行（false）。v2.4.0 起替代了已废弃的 jsxBracketSameLine。

{{知识点14}}
Object Wrap（对象换行）是 v3.5.0 新增的选项，通过 --object-wrap 或 objectWrap 配置，默认值为 "preserve"。"preserve" 保持原始换行行为（如果左花括号和第一个属性之间有换行则保持多行），"collapse" 尽可能压缩为单行。

{{知识点15}}
Prettier 支持多种配置文件格式，按优先级排列：package.json 中的 "prettier" 键、.prettierrc（JSON/YAML）、.prettierrc.json/.yml/.yaml/.json5、.prettierrc.js/.ts（ESM 或 CJS）、.prettierrc.mjs/.mts/.cjs/.cts、.prettierrc.toml。配置文件从被格式化文件的位置向上搜索直到找到或未找到。

{{知识点16}}
Prettier 不支持全局配置。这是有意为之，确保项目在不同计算机间保持一致行为，团队成员获得相同的格式化结果。

{{知识点17}}
配置覆盖（Overrides）允许为特定文件扩展名、文件夹或文件设置不同配置。在配置文件中使用 overrides 数组，每个覆盖项必须包含 files 字段（字符串或字符串数组），可选 excludeFiles 字段排除文件。

{{知识点18}}
Prettier 的 Parser（解析器）选项自动根据文件路径推断，无需手动设置。内置解析器包括：babel、babel-flow、babel-ts、flow、typescript、espree、meriyah、acorn、css、scss、less、json、json5、jsonc、json-stringify、graphql、markdown、mdx、html、vue、angular、lwc、mjml、yaml 等。v3.0.0 移除了自定义解析器 API，改用插件机制。

{{知识点19}}
.prettierignore 文件用于排除不需要格式化的文件和文件夹，使用 gitignore 语法。默认忽略 .git、.jj、.sl、.svn、.hg 和 node_modules 目录。项目根目录的 .gitignore 规则也会被 Prettier 遵循。--with-node-modules 选项可包含 node_modules。

{{知识点20}}
行内忽略注释用于跳过特定代码段的格式化。JavaScript 使用 // prettier-ignore，JSX 使用 {/* prettier-ignore */}，HTML 使用 <!-- prettier-ignore -->，CSS 使用 /* prettier-ignore */，Markdown 使用 <!-- prettier-ignore-start/end --> 范围忽略。

{{知识点21}}
HTML 中还有 <!-- prettier-ignore-attribute --> 用于忽略特定属性的格式化，可以指定属性名如 <!-- prettier-ignore-attribute (mouseup) --> 来只忽略特定属性。

{{知识点22}}
CLI 核心命令：prettier --write . 格式化当前目录所有文件；prettier --check . 检查格式（CI 中常用，格式不符返回退出码 1）；prettier --list-different 或 -l 打印格式不同的文件名。退出码：0=全部格式正确，1=有格式问题，2=Prettier 自身出错。

{{知识点23}}
--debug-check 选项用于检查格式化是否可能改变代码正确性，不能与 --write 一起使用。--find-config-path 找到配置文件路径，--config 指定配置文件路径，--no-config 忽略配置文件使用默认设置。

{{知识点24}}
--cache 选项启用格式化缓存，缓存键包括 Prettier 版本、选项、Node.js 版本和文件元数据/内容。默认缓存路径为 ./node_modules/.cache/prettier/.prettier-cache。--cache-strategy 可选 metadata（更快）或 content（默认，适合 git 操作场景）。

{{知识点25}}
--config-precedence 选项定义配置文件与 CLI 选项的优先级关系：cli-override（默认，CLI 优先）、file-override（配置文件优先）、prefer-file（有配置文件则忽略 CLI 选项）。这为编辑器集成提供了灵活性。

{{知识点26}}
--stdin-filepath 选项用于从标准输入格式化时指定文件路径，使 Prettier 能正确推断解析器。例如：cat abc.css | prettier --stdin-filepath abc.css。

{{知识点27}}
Prettier 的插件系统允许扩展支持的语言和格式化规则。Prettier 自身的语言实现也基于插件 API。核心包内置 JavaScript 和 Web 相关语言，其他语言需安装插件。通过配置文件的 "plugins" 数组、CLI 的 --plugin 选项或 API 的 plugins 选项加载。

{{知识点28}}
官方及社区插件丰富，包括：@prettier/plugin-php、@prettier/plugin-ruby、@prettier/plugin-xml、prettier-plugin-astro、prettier-plugin-svelte、prettier-plugin-java、prettier-plugin-kotlin、prettier-plugin-rust、prettier-plugin-sh（Shell）、prettier-plugin-sql、prettier-plugin-solidity、prettier-plugin-toml、prettier-plugin-prisma 等。

{{知识点29}}
Prettier 插件开发需要导出五个核心属性：languages（语言定义数组，需包含 name 和 parsers）、parsers（将代码字符串转为 AST）、printers（将 AST 转为 Prettier 中间表示）、options（自定义选项）、defaultOptions（默认选项）。parse 函数签名为 parse(text, options) => AST | Promise<AST>。

{{知识点30}}
Prettier 与 EditorConfig 集成良好。会自动读取 .editorconfig 文件并转换对应属性：charset→utf-8、end_of_line→行尾符、indent_style→制表符/空格、indent_size→缩进宽度、max_line_length→行宽。.prettierrc 等配置会覆盖 .editorconfig 的设置。

{{知识点31}}
Experimental Ternaries 是实验性选项，通过 --experimental-ternaries 或 experimentalTernaries 配置，默认 false。启用后使用 "curious ternaries" 风格，问号跟在条件后面，是未来可能成为默认行为的新格式。

{{知识点32}}
Experimental Operator Position 通过 --experimental-operator-position 配置，默认 "end"。设为 "start" 时，二元表达式换行时运算符放在新行开头而非上一行末尾。

{{知识点33}}
Range 选项（rangeStart/rangeEnd）允许只格式化文件的特定字符偏移范围。范围会向后扩展到语句起始行，向前扩展到语句结束。CLI 使用 --range-start 和 --range-end。

{{知识点34}}
推荐在项目中使用 .prettierignore 文件配合 prettier --write . 命令，确保只格式化需要格式化的文件。配合 pre-commit hook（如 husky + lint-staged）可在提交前自动格式化，最大程度减少 CI 因格式问题失败。

{{知识点35}}
Prettier 与 ESLint 的关系：Prettier 负责代码格式化（formatting），ESLint 负责代码质量检查（linting）。推荐使用 eslint-config-prettier 禁用 ESLint 中与 Prettier 冲突的格式规则，以及 eslint-plugin-prettier 将 Prettier 作为 ESLint 规则运行（但新推荐是分开运行）。

{{知识点36}}
TypeScript 配置文件支持需要 Node.js >= 22.6.0，且 Node.js v24.3.0 之前需要 --experimental-strip-types 标志。可通过 NODE_OPTIONS="--experimental-strip-types" 或直接 node --experimental-strip-types 来启用。

{{知识点37}}
Prettier v3.0.0 的重大变更：trailingComma 默认值从 "es5" 改为 "all"；自定义解析器 API 移除，改用插件系统；整体转向 ESM。升级时需注意这些破坏性变更对现有项目的影响。

{{知识点38}}
--with-node-modules 选项让 Prettier 格式化 node_modules 中的文件（默认忽略）。--ignore-unknown 或 -u 选项在使用通配符模式时忽略未知文件类型。--no-error-on-unmatched-pattern 防止模式无匹配时报错。

{{知识点39}}
Prettier 的格式化哲学：尽量减少选项，避免风格之争。核心理念是"一个团队只有一种格式"。格式化不影响 AST（抽象语法树），即不改变代码语义，只改变视觉呈现。

{{知识点40}}
在 CI/CD 流水线中，推荐使用 prettier --check . 检查格式。配合 --cache 选项可加速增量检查。缓存策略 metadata 使用文件时间戳判断变更，content 使用文件内容哈希判断变更。删除缓存可手动执行 rm ./node_modules/.cache/prettier/.prettier-cache。
