# 2025CSS进阶技巧
# 领域: 计算机IT
# 学科: 前端框架
# 来源: 2025CSS进阶技巧
# 描述: CSS进阶技巧核心知识点

{{CSS Grid布局基础}}
CSS Grid是一种二维布局系统，使用 display: grid 创建网格容器。通过 grid-template-columns 和 grid-template-rows 定义列和行的轨道大小。使用 grid-column 和 grid-item 属性控制子元素在网格中的位置和跨度。Grid布局适合复杂的页面整体布局，能够同时控制行和列。

{{Grid的fr单位与repeat函数}}
fr（fraction）单位表示可用空间的一比例份额。grid-template-columns: 1fr 2fr 1fr 表示三列按1:2:1分配空间。repeat()函数简化重复定义：repeat(3, 1fr) 等价于 1fr 1fr 1fr。repeat(auto-fill, minmax(250px, 1fr)) 可实现响应式自动换行布局。

{{Grid区域命名与模板}}
使用 grid-template-areas 属性为网格区域命名，子元素通过 grid-area 引用名称放置。例如：grid-template-areas: "header header" "sidebar main" "footer footer"。这种方式使布局语义清晰，代码可读性极强，特别适合页面骨架布局。

{{Grid对齐控制}}
Grid提供多层对齐：justify-items / align-items 控制所有子项在单元格内的对齐；justify-content / align-content 控制整个网格在容器内的对齐；子项可用 justify-self / align-self 覆盖。常用值：start、end、center、stretch。

{{Container Queries容器查询}}
容器查询允许组件根据其父容器的尺寸而非视口尺寸来响应式调整样式。使用 container-type: inline-size 定义容器，然后用 @container 规则编写条件样式。例如：@container (min-width: 500px) { ... }。这使得组件在不同上下文中能自适应展示。

{{Container Queries的命名容器}}
一个页面可能有多个容器，使用 container-name 属性为容器命名，然后在 @container 规则中指定目标容器名。例如：@container sidebar (min-width: 300px) { ... }。避免查询到意外的祖先容器，确保组件样式精确可控。

{{Container Style Queries}}
Container Style Queries允许基于容器的CSS自定义属性值来应用样式。使用 @container style(--theme: dark) 来匹配。虽然目前浏览器支持有限，但它为基于主题/状态的组件样式切换提供了纯CSS方案。

{{:has()选择器}}
:has() 是CSS的"父选择器"，弥补了CSS长期缺失的能力。语法：.card:has(img) 选择包含图片的卡片。.form:has(:invalid) 选择包含无效输入的表单。:has() 可以实现基于子元素状态改变父元素样式的场景，替代大量JavaScript逻辑。

{{:has()的高级用法}}
:has() 可以与其它选择器组合实现复杂逻辑：.sidebar:has(+ .main) 选择后面紧跟main的sidebar；body:has(.modal[open]) .app 选择模态框打开时的app内容区域。支持链式组合：:has(.a):has(.b) 表示同时包含a和b。

{{CSS Nesting原生嵌套}}
CSS Nesting允许在选择器内部嵌套子选择器，类似Sass/Less但无需预处理器。使用 & 引用父选择器。例如：.card { color: black; & .title { font-size: 20px; } &:hover { opacity: 0.9; } }。原生嵌套减少了选择器重复，提高可读性。

{{CSS Nesting的&符号}}
& 符号在CSS Nesting中表示父选择器的直接替换。.nav { & > li { } } 等价于 .nav > li { }。当省略 & 时，浏览器会自动将其视为后代选择器（空格连接）。注意：& 必须出现在嵌套选择器的开头，否则语法无效。

{{Subgrid子网格}}
Subgrid允许子元素继承父Grid的轨道定义。子容器设置 display: grid 后，用 grid-template-columns: subgrid 或 grid-template-rows: subgrid 继承父网格轨道。典型场景：卡片列表中每张卡片的标题、内容、底部按钮需要跨卡片对齐。

{{Subgrid的实际应用场景}}
在产品卡片列表中，父容器定义列轨道，每个卡片作为子grid使用 subgrid 继承列轨道，卡片内部的标题、价格、按钮区域可以跨卡片精确对齐。也可以用 subgrid 继承行轨道，确保不同高度内容区域的基线对齐。

{{CSS动画之@keyframes}}
@keyframes 定义关键帧动画序列。通过 from/to 或百分比定义中间状态。结合 animation 属性控制播放：animation: slideIn 0.5s ease-in-out forwards。animation-fill-mode 控制动画结束后元素保持的状态：forwards（保持终态）、backwards（应用初始帧）、both。

{{CSS过渡动画transition}}
transition 是最常用的动画方式，定义属性变化时的平滑过渡。语法：transition: property duration timing-function delay。可以同时过渡多个属性：transition: transform 0.3s, opacity 0.2s。transition-behavior: allow-discrete 允许离散属性（如 display）也能参与过渡。

{{CSS View Transitions API}}
View Transitions API提供页面内或跨页面的平滑过渡效果。使用 document.startViewTransition() 触发，CSS中用 ::view-transition伪元素控制过渡样式。view-transition-name 为元素命名，浏览器自动捕获前后状态并生成过渡动画。支持多元素独立过渡。

{{跨文档视图过渡Cross-Document View Transitions}}
在MPA（多页应用）中，通过CSS的 @view-transition { types: slide; } 和同源页面间的导航，可以实现页面切换的平滑过渡。配合 view-transition-name 命名，浏览器会自动在页面间捕获匹配元素并生成过渡动画。

{{CSS @scope作用域规则}}
@scope 允许定义样式的作用范围，限定选择器只在特定DOM子树内生效。语法：@scope (.card) to (.card-footer) { ... }，其中 .card 是作用域根，.card-footer 是作用域边界（排除区域）。解决了BEM等命名方法的样式隔离问题。

{{CSS @layer层叠层}}
@layer 允许显式控制CSS的层叠顺序，而非依赖源码顺序。定义：@layer base, components, utilities; 然后 @layer components { .btn { ... } }。层叠优先级从左到右递增。第三方CSS可以放入 @layer reset { ... } 避免覆盖业务样式。

{{CSS @property自定义属性类型}}
@property 允许为CSS自定义属性注册类型、初始值和继承行为。例如：@property --hue { syntax: '<number>'; initial-value: 0; inherits: false; }。注册后的变量可以在过渡和动画中平滑插值，解决了普通CSS变量无法过渡的问题。

{{@property实现渐变动画}}
利用 @property 将渐变的色值注册为可插值类型，可以实现渐变色的平滑动画。例如注册 --color-start 和 --color-end 为 color 类型，然后在 @keyframes 中改变它们的值，背景渐变会平滑过渡而非瞬间切换。

{{CSS color-mix()函数}}
color-mix() 在两个颜色之间按比例混合。语法：color-mix(in srgb, red 50%, blue)。支持多种色彩空间：srgb、lab、oklch 等。典型用途：生成主色的浅色变体（hover、disabled状态），无需定义额外的颜色变量。

{{oklch色彩空间}}
oklch(L C H) 是感知均匀的色彩空间，L为亮度(0-1)，C为色度，H为色相角度。相比HSL，oklch在相同亮度值下视觉亮度一致，避免了HSL中黄色和蓝色亮度差异大的问题。2025年CSS中推荐使用oklch进行颜色设计系统构建。

{{light-dark()颜色函数}}
light-dark() 为暗色/亮色模式提供简洁的颜色切换语法。例如：color: light-dark(#333, #eee)。需要配合 color-scheme: light dark 使用。比 prefers-color-scheme 媒体查询更简洁，适合在组件级别快速切换主题色。

{{CSS Nesting中的@规则}}
CSS Nesting支持在嵌套中直接使用 @media、@supports 等@规则。例如：.card { font-size: 14px; @media (min-width: 768px) { font-size: 16px; } }。这使得响应式样式与组件样式紧密组织在一起，提高了代码的上下文关联性。

{{CSS Scroll-Driven Animations}}
滚动驱动动画通过 animation-timeline 属性将动画绑定到滚动进度。scroll() 绑定到最近的滚动容器，view() 绑定到元素在视口中的可见性。例如：animation: fade-in linear; animation-timeline: view(); 可实现元素进入视口时的渐显效果。

{{滚动进度条实现}}
使用 Scroll-Driven Animations 实现页面滚动进度条：@keyframes progress { from { width: 0; } to { width: 100%; } }。进度条元素设置 animation: progress linear; animation-timeline: scroll(); 即可，无需JavaScript监听scroll事件。

{{CSS :nth-child(of S)语法}}
:nth-child(of S) 扩展了 :nth-child 的能力，允许只在匹配选择器S的子元素中计数。例如：li:nth-child(2 of .highlight) 选择第二个带 .highlight 类的li元素，而非第二个li恰好带highlight。解决了旧语法中"第N个匹配元素"的难题。

{{CSS text-wrap: balance和pretty}}
text-wrap: balance 让浏览器自动平衡多行文本的行长，使每行长度接近，常用于标题。text-wrap: pretty 优化段落文本的换行，避免孤字（orphans）和不美观的断行。两者都是纯CSS的排版增强，无需手动调整。

{{CSS @starting-style}}
@starting-style 定义元素首次渲染或从 display:none 变为可见时的初始样式。例如：dialog { @starting-style { opacity: 0; transform: translateY(20px); } }。配合 transition 可实现元素首次出现时的入场动画，无需额外JS触发。

{{CSS Popover API与样式}}
HTML的popover属性配合CSS可以实现原生弹出层。:popover-open 伪类匹配已打开的弹出层。::backdrop 伪元素控制背景遮罩。使用 anchor() 函数和 position-anchor 属性可以将弹出层锚定到触发按钮，实现纯CSS的tooltip和dropdown。

{{CSS Anchor Positioning}}
锚点定位允许元素相对于页面上另一个"锚点"元素定位。使用 anchor-name 定义锚点，position-anchor 引用锚点。anchor() 函数在 inset 属性中获取锚点边缘位置。position-try-fallbacks 定义当空间不足时的备选定位策略。

{{CSS函数式伪类:is()和:where()}}
:is() 和 :where() 接受选择器列表，简化复杂选择器。:is(.card, .tile) .title 匹配card或tile内的title。区别：:is() 的特异性取参数中最高特异性，:where() 的特异性始终为0。:where() 适合做可被轻松覆盖的默认样式。

{{CSS @counter-style}}
@counter-style 允许自定义列表计数器样式，超越内置的 decimal、disc 等。定义 symbols、prefix、suffix、system 等属性。例如创建 emoji 计数器：@counter-style emoji { system: cyclic; symbols: "🔴" "🟢" "🔵"; }，列表使用 list-style-type: emoji。

{{CSS高亮伪元素::highlight}}
::highlight() 伪元素允许为通过 JavaScript 创建的自定义高亮范围设置样式。通过 CSS.highlights API 注册高亮范围，然后用 ::highlight(search-result) { background: yellow; color: black; } 设置样式。替代了传统的包裹span方式，不修改DOM结构。

{{CSS Exclusive Accordion独占手风琴}}
使用原生HTML的 <details name="group"> 属性，同name的details元素互斥展开，实现纯CSS/HTML的手风琴效果。浏览器原生支持，无需JavaScript。结合 details::details-content 伪元素可以添加展开/收起的过渡动画。

{{CSS Scroll Snap滚动吸附}}
scroll-snap-type: x mandatory 或 y proximity 在滚动容器上启用吸附。子元素设置 scroll-snap-align: start/center/end 定义吸附对齐点。配合 scroll-snap-stop: always 可以实现"逐项"滚动效果，常用于轮播图和全屏滚动页面。

{{CSS :focus-visible}}
:focus-visible 仅在键盘导航（Tab键）聚焦时显示焦点样式，鼠标点击时不显示。解决了 :focus 在鼠标交互时也显示蓝色边框的问题。推荐用法：:focus-visible { outline: 2px solid blue; outline-offset: 2px; }，既保证无障碍又不影响视觉。

{{CSS logical properties逻辑属性}}
逻辑属性用 start/end 替代 left/right/top/bottom，自动适配书写方向。margin-inline-start 替代 margin-left，padding-block 替代 padding-top/bottom，border-inline-end 替代 border-right。支持LTR和RTL语言的国际化布局，无需额外CSS。

{{CSS aspect-ratio宽高比}}
aspect-ratio 属性直接定义元素的宽高比，如 aspect-ratio: 16/9。替代了传统的 padding-top 百分比hack。常见用途：视频容器、图片占位、正方形头像。设为 auto 时，对于有固有尺寸的元素（如图片），使用固有宽高比。

{{CSS gap属性}}
gap 属性为 Grid 和 Flexbox 容器统一设置子项间距。row-gap 和 column-gap 可分别设置。gap: 20px 30px 表示行间距20px，列间距30px。比 margin 更优雅，不需要处理首尾子项的多余外边距，2025年已成为间距控制的首选方式。

{{CSS :user-valid和:user-invalid}}
:user-valid 和 :user-invalid 仅在用户实际交互后才生效，解决了 :valid/:invalid 在页面加载时就立即应用样式的问题。用户输入后才显示验证状态，提供更好的表单体验。比旧的 :valid 伪类更适合实际表单交互场景。
