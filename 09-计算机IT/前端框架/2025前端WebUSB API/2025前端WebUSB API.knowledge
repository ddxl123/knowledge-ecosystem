# 2025前端WebUSB API
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}
WebUSB API 是什么：WebUSB API 是一个 Web 标准 API，允许网页通过 JavaScript 直接与 USB 设备通信，无需安装原生驱动程序。它由 WICG（Web Incubator Community Group）维护，规范地址为 https://wicg.github.io/webusb/ 。WebUSB 为非标准化 USB 设备提供了 Web 访问途径，硬件厂商可以通过 Web 提供设备访问能力，而无需开发独立的原生 API。

{{知识点2}}
WebUSB 的核心目的：USB 设备通常分为键盘、鼠标、视频设备等标准设备类，由操作系统类驱动支持。但大量非标准化 USB 设备需要自定义驱动，WebUSB 旨在让这些设备能够通过 Web 页面直接访问，消除了用户搜索和安装驱动的需求。

{{知识点3}}
WebUSB 的安全上下文要求：WebUSB API 仅在安全上下文（HTTPS）中可用。这是为了防止中间人攻击窃取 USB 通信数据。在本地开发环境中，localhost 也被视为安全上下文。

{{知识点4}}
WebUSB 的浏览器兼容性：截至 2025 年，WebUSB API 在 Chrome、Edge、Opera 等 Chromium 内核浏览器中获得支持。Firefox 和 Safari 尚未实现该 API。使用前需要检查浏览器兼容性表。该 API 被标记为实验性技术（Experimental）。

{{知识点5}}
navigator.usb 入口点：通过 navigator.usb 属性可以获取 USB 对象实例，它是 WebUSB API 的入口点。USB 接口继承自 EventTarget，提供了查找和连接 USB 设备的方法。示例：const usb = navigator.usb;

{{知识点6}}
USB.requestDevice() 方法：USB.requestDevice() 方法返回一个 Promise，解析为 USBDevice 实例。调用此方法会触发浏览器的设备配对流程，弹出设备选择对话框让用户选择要连接的 USB 设备。该方法需要用户交互（Transient user activation）才能调用。

{{知识点7}}
requestDevice() 的 filters 参数：requestDevice() 接受一个 options 对象，其中 filters 数组用于指定设备过滤条件。每个过滤器对象可包含以下属性：vendorId（厂商 ID）、productId（产品 ID）、classCode（设备类代码）、subclassCode（子类代码）、protocolCode（协议代码）、serialNumber（序列号）。

{{知识点8}}
requestDevice() 多设备选择：可以指定多个过滤器对象来匹配多种设备。过滤器数量不等于显示的设备数量——浏览器会列出所有匹配的已连接设备。例如指定两个 productId，如果连接了三台匹配设备，用户会在对话框中看到三台设备。

{{知识点9}}
USB.getDevices() 方法：USB.getDevices() 返回一个 Promise，解析为已配对的 USBDevice 数组。与 requestDevice() 不同，getDevices() 不会触发配对对话框，只返回之前已授权的设备。适用于在页面重新加载后恢复设备连接。

{{知识点10}}
USB connect 事件：当之前配对的 USB 设备连接到主机时，USB 对象会触发 connect 事件。事件类型为 USBConnectionEvent，包含 device 属性指向新连接的 USBDevice。可用于监听设备热插拔。

{{知识点11}}
USB disconnect 事件：当已配对的 USB 设备从主机断开时，USB 对象会触发 disconnect 事件。事件类型同样为 USBConnectionEvent。可用于清理资源或提示用户设备已移除。

{{知识点12}}
USBDevice 接口概述：USBDevice 接口提供了已配对 USB 设备的元数据访问和控制方法。它是 WebUSB API 中最核心的接口，包含了设备信息属性和数据传输方法。

{{知识点13}}
USBDevice 设备标识属性：USBDevice 提供了多个只读属性用于标识设备：vendorId（USB 官方分配的厂商 ID）、productId（制造商定义的产品 ID）、manufacturerName（制造商名称）、productName（产品名称）、serialNumber（序列号）。

{{知识点14}}
USBDevice 设备分类属性：deviceClass、deviceSubclass、deviceProtocol 三个属性用于识别 USB 设备类型以加载合适的驱动。它们对应 USB 描述符中的 bDeviceClass、bDeviceSubclass、bDeviceProtocol 字段。

{{知识点15}}
USBDevice 版本属性：设备版本通过 deviceVersionMajor、deviceVersionMinor、deviceVersionSubminor 三个属性以语义化版本号表示。USB 协议版本通过 usbVersionMajor、usbVersionMinor、usbVersionSubminor 表示。

{{知识点16}}
USBDevice.open() 方法：open() 方法返回一个 Promise，在设备会话启动后解析。这是与设备通信的第一步，必须先打开设备才能进行后续操作。如果设备已被其他程序占用，可能会抛出错误。

{{知识点17}}
USBDevice.close() 方法：close() 方法返回一个 Promise，在所有已打开的接口被释放且设备会话结束后解析。使用完毕后应调用 close() 释放设备资源，使其他程序可以访问该设备。

{{知识点18}}
USBDevice.forget() 方法：forget() 方法不仅关闭设备会话，还会重置设备的授权权限。调用后，下次使用需要重新通过 requestDevice() 进行配对授权。适用于用户明确要求断开设备的场景。

{{知识点19}}
USBDevice.selectConfiguration() 方法：selectConfiguration() 方法用于选择 USB 设备的指定配置。USB 设备可能有多个配置，每个配置定义了不同的功能集合。返回 Promise 在配置选择完成后解析。

{{知识点20}}
USBDevice.claimInterface() 方法：claimInterface() 方法请求对指定接口的独占访问权。在进行数据传输之前，必须先声明（claim）接口。返回 Promise 在接口被成功声明后解析。同一时间只有一个页面可以声明某个接口。

{{知识点21}}
USBDevice.releaseInterface() 方法：releaseInterface() 方法释放之前声明的接口，解除独占访问。在切换功能或关闭设备前应释放接口。返回 Promise 在接口释放后解析。

{{知识点22}}
USBDevice.selectAlternateInterface() 方法：selectAlternateInterface() 方法用于选择接口的替代配置。USB 接口可以有多个替代设置（alternate settings），不同的替代设置可能有不同的端点配置和功能特性。

{{知识点23}}
USBDevice.controlTransferIn() 方法：controlTransferIn() 方法向 USB 设备发送控制传输请求并接收数据。返回 Promise，解析为 USBInTransferResult 对象。控制传输用于发送命令或查询设备状态，是 USB 通信中最基础的传输类型。

{{知识点24}}
USBDevice.controlTransferOut() 方法：controlTransferOut() 方法通过控制传输向 USB 设备发送数据。返回 Promise，解析为 USBOutTransferResult 对象。与 controlTransferIn 配对使用，实现双向控制通信。

{{知识点25}}
USBDevice.transferIn() 方法：transferIn() 方法用于从 USB 设备接收批量（bulk）或中断（interrupt）数据。返回 Promise，解析为 USBInTransferResult 对象。需要指定端点号和要接收的字节数。

{{知识点26}}
USBDevice.transferOut() 方法：transferOut() 方法用于向 USB 设备发送批量或中断数据。返回 Promise，解析为 USBOutTransferResult 对象。接受端点号和要发送的数据（Uint8Array 或 ArrayBuffer）作为参数。

{{知识点27}}
USBDevice.isochronousTransferIn() 方法：isochronousTransferIn() 方法用于接收等时传输数据，适用于时间敏感的信息传输（如音频/视频流）。返回 Promise，解析为 USBIsochronousInTransferResult 对象。等时传输不保证数据完整性但保证传输时序。

{{知识点28}}
USBDevice.isochronousTransferOut() 方法：isochronousTransferOut() 方法用于发送等时传输数据。返回 Promise，解析为 USBIsochronousOutTransferResult 对象。适用于需要定期传输且允许少量丢包的场景。

{{知识点29}}
USBDevice.reset() 方法：reset() 方法重置 USB 设备，取消所有正在进行的操作并拒绝其 Promise。返回 Promise 在设备重置完成后解析。当设备出现异常状态时可用于恢复。

{{知识点30}}
USBDevice.clearHalt() 方法：clearHalt() 方法清除端点的停止（halt）条件。当端点进入错误状态时，需要先清除 halt 条件才能恢复通信。返回 Promise 在条件清除后解析。

{{知识点31}}
USBDevice.opened 属性：opened 是只读布尔属性，指示是否已与配对的 USB 设备建立会话。在调用 open() 后为 true，调用 close() 后为 false。可用于检查设备当前连接状态。

{{知识点32}}
USBDevice.configuration 属性：configuration 是只读属性，返回当前选中的 USBConfiguration 对象。如果尚未选择配置则为 null。通过 selectConfiguration() 方法设置。

{{知识点33}}
USBDevice.configurations 属性：configurations 是只读数组，包含设备支持的所有 USBConfiguration 对象。每个配置代表设备的一种工作模式，包含不同的接口集合。

{{知识点34}}
USBConfiguration 接口：USBConfiguration 接口描述 USB 设备的特定配置及其支持的接口。关键属性包括：configurationValue（配置值）、configurationName（配置名称）、interfaces（USBInterface 数组）。一个设备可以有多个配置，但同时只能激活一个。

{{知识点35}}
USBInterface 接口：USBInterface 接口描述 USB 设备提供的一个接口。接口代表设备的一个功能，实现特定协议，可能包含用于双向通信的端点。关键属性包括：interfaceNumber（接口编号）、alternate（当前替代配置）、alternates（替代配置数组）、claimed（是否已声明）。

{{知识点36}}
USBAlternateInterface 接口：USBAlternateInterface 接口描述接口的一种替代配置。不同替代设置可以有不同的端点和功能特性。通过 USBDevice.selectAlternateInterface() 切换。属性包括 interfaceClass、interfaceSubclass、interfaceProtocol 和 endpoints。

{{知识点37}}
USBEndpoint 接口：USBEndpoint 接口描述 USB 设备的一个端点。端点代表进出设备的单向数据流。关键属性：endpointNumber（端点编号，1-15）、direction（方向，"in" 或 "out"）、type（类型，"bulk"/"interrupt"/"isochronous"）、packetSize（数据包大小）。

{{知识点38}}
端点类型详解：USB 端点有三种传输类型——bulk（批量）：可靠的大数据传输，可能被其他流量抢占；interrupt（中断）：可靠的小数据传输，有专用总线时间；isochronous（等时）：不可靠的周期性传输，错过截止时间数据会被丢弃，适用于音视频流。

{{知识点39}}
USBInTransferResult 和 USBOutTransferResult：USBInTransferResult 表示从设备到主机的数据传输结果，USBOutTransferResult 表示从主机到设备的传输结果。两者都有 status 属性（"ok"、"stall"、"babble"）和 data 属性（接收/发送的数据 DataView）。

{{知识点40}}
USB 端点发现实战：在实际使用中，设备端点布局可能需要运行时发现。以 USB CDC 串口设备为例，需要遍历 configuration.interfaces，找到 interfaceClass 为 10（USB_CDC_CLASS）的接口，再从其 endpoints 中筛选 type 为 "bulk" 的端点，根据 direction 确定 inEndpoint 和 outEndpoint。

{{知识点41}}
WebUSB 与 WebHID API 的区别：标准 USB 设备类（键盘、鼠标等）可通过 WebHID API 访问。WebUSB 主要用于非标准化 USB 设备，提供更底层的 USB 协议访问。WebHID 更高层抽象，WebUSB 更底层控制。

{{知识点42}}
WebUSB 的使用流程：典型的 WebUSB 使用流程为：1) 检查浏览器支持 navigator.usb；2) 调用 requestDevice() 选择设备；3) 调用 device.open() 打开设备；4) 调用 selectConfiguration() 选择配置；5) 调用 claimInterface() 声明接口；6) 使用 transferIn/transferOut 传输数据；7) 操作完成后 releaseInterface() 和 close()。

{{知识点43}}
WebUSB 的安全限制：WebUSB 有多重安全限制：必须在安全上下文（HTTPS）下运行；需要用户交互才能触发设备选择；设备选择需要用户显式授权；某些设备类（如 HID、大容量存储）可能被浏览器阻止访问以防止安全风险。

{{知识点44}}
WebUSB 在 Web Workers 中的支持：WebUSB API 可在 Web Workers 中使用，这意味着耗时的 USB 通信可以在后台线程中执行，不会阻塞主线程的 UI 渲染。这在处理大量数据传输时非常有用。

{{知识点45}}
WebUSB 的 Arduino 应用示例：WebUSB 最常见的应用场景之一是与 Arduino 微控制器通信。通过指定 vendorId: 0x2341（Arduino 的厂商 ID），可以连接 Arduino 设备。示例：navigator.usb.requestDevice({ filters: [{ vendorId: 0x2341 }] }).then(device => { console.log(device.productName); })。

{{知识点46}}
WebUSB 的连接管理最佳实践：使用 WebUSB 时应注意：始终在 finally 块中调用 releaseInterface() 和 close() 释放资源；监听 connect/disconnect 事件处理设备热插拔；使用 try-catch 处理设备访问错误；考虑使用 forget() 允许用户撤销授权。

{{知识点47}}
WebUSB 的传输结果处理：处理数据传输时应检查 result.status：status 为 "ok" 表示传输成功；"stall" 表示端点被停止，需要 clearHalt()；"babble" 表示接收数据超过预期长度。对于等时传输，还需要检查每个数据包的状态。

{{知识点48}}
WebUSB 的 USBConnectionEvent：USBConnectionEvent 是 connect 和 disconnect 事件的事件对象，继承自 Event。它包含一个 device 属性，指向触发事件的 USBDevice 实例。注意 device 属性仅在 connect 事件中完全可用。

{{知识点49}}
WebUSB 与其他 Web 硬件 API：WebUSB 是 Web 硬件访问 API 家族的一员，与 Web Serial API（串口）、Web Bluetooth API（蓝牙）、WebHID API（人机接口设备）并列。选择哪个 API 取决于设备的连接方式和通信协议。

{{知识点50}}
WebUSB 的未来展望：截至 2025 年，WebUSB 仍为实验性 API，尚未成为所有主流浏览器的标配。但随着 WebAssembly 和 PWA 的发展，WebUSB 在物联网（IoT）、硬件调试、固件更新等领域的应用前景广阔。建议在生产环境使用前做好兼容性降级方案。
