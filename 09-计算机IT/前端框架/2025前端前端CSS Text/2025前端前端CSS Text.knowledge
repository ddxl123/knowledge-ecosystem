# 2025前端前端CSS Text
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}{{CSS Text模块概述：CSS Text模块定义了文本操作方式，包括换行、对齐、空白处理和文本转换。规范包括CSS Text Module Level 3和Level 4。涵盖的核心属性有：text-align、text-transform、text-indent、letter-spacing、word-spacing、word-break、overflow-wrap、hyphens、white-space、text-wrap、text-overflow、text-shadow、text-decoration、line-break、tab-size、hanging-punctuation等。这些属性共同控制网页中文本的渲染和排版行为。}}

{{知识点2}}{{text-align属性：控制块级容器中行内内容的水平对齐方式。值包括：start（默认，根据书写方向决定）、end、left、right、center、justify（两端对齐）、match-parent。该属性可继承，适用于块级容器。justify会使文本两端对齐，但最后一行除外。对于阅读障碍用户，两端对齐的不一致词间距可能造成困扰。}}

{{知识点3}}{{text-align-last属性：控制强制换行后最后一行文本的对齐方式。当text-align为justify时，最后一行默认左对齐，使用text-align-last可以单独控制。值包括：auto、start、end、left、right、center、justify。常与text-align:justify配合使用。}}

{{知识点4}}{{text-transform属性：控制文本的大小写转换。值包括：none（默认）、capitalize（首字母大写）、uppercase（全大写）、lowercase（全小写）、full-width（全角字符）、full-size-kana（全尺寸假名）、math-auto（数学斜体）。该属性可继承，支持语言特定的大小写映射规则，如土耳其语的i/İ和ı/I区分、德语ß转SS、荷兰语ij二连字等。}}

{{知识点5}}{{text-transform语言特性：text-transform会考虑语言特定的大小写映射规则。例如：土耳其语中i和ı是不同的字母；德语ß在大写时变为SS；荷兰语ij二连字在capitalize时两个字母都大写；希腊语元音在全大写时去掉重音符号；爱尔兰语某些前缀字母保持小写。语言通过HTML的lang属性定义。}}

{{知识点6}}{{letter-spacing属性：控制字符之间的额外间距。值为normal或长度值。正数增加间距，负数减少间距。注意：CSS Text Level 3新增了百分比值支持，百分比相对于空格字符宽度。该属性可继承，应用于所有元素及::first-letter和::first-line伪元素。对于阿拉伯语等连写文字，不应使用letter-spacing。}}

{{知识点7}}{{word-spacing属性：控制单词之间的额外间距。与letter-spacing类似但作用于单词间。默认值为normal，可使用长度值或百分比值（相对于空格字符宽度）。该属性可继承。在两端对齐文本中，浏览器可能会进一步调整单词间距。}}

{{知识点8}}{{text-indent属性：设置块容器中文本行前的缩进量。支持长度值和百分比值（相对于容器的内联方向尺寸）。关键修饰符：each-line（影响首行和强制换行后的行）、hanging（反转缩进，除首行外都缩进）。负值可创建悬挂缩进效果。常用于段落首行缩进，使用相邻兄弟组合符可跳过标题后的首段。}}

{{知识点9}}{{text-overflow属性：控制溢出文本如何向用户显示。值包括：clip（默认，直接截断）、ellipsis（显示省略号…）、自定义字符串。使用前提：必须同时设置overflow:hidden和white-space:nowrap。支持双值语法（分别控制左右两端溢出）。CSS Text Level 4新增了fade值和fade()函数用于渐隐效果。}}

{{知识点10}}{{white-space属性：控制空白字符的处理和换行行为。现在是简写属性，包含white-space-collapse和text-wrap-mode。关键字值：normal（折叠空白，自动换行）、pre（保留空白，不换行）、pre-wrap（保留空白，自动换行）、pre-line（折叠空白，保留换行符）、nowrap（折叠空白，不换行）、break-spaces（保留空白，行尾空格可换行）。}}

{{知识点11}}{{white-space详细行为表：normal折叠新行和空格/制表符，自动换行，移除行尾空格；pre保留新行和空格/制表符，不换行，保留行尾空格；pre-wrap保留新行和空格/制表符，自动换行，行尾空格悬挂；pre-line保留新行但折叠空格/制表符，自动换行，移除行尾空格。tab-size属性可配置制表符宽度（默认8个空格）。}}

{{知识点12}}{{white-space的新变化：white-space作为简写属性是相对较新的特性。nowrap现在被解释为text-wrap-mode的值，break-spaces被解释为white-space-collapse的值。新增了wrap和collapse等组合关键字。规范还定义了white-space-trim子属性（尚未实现）。}}

{{知识点13}}{{text-wrap属性（CSS Text Level 4）：简写属性，包含text-wrap-mode和text-wrap-style。值：wrap（默认，正常换行）、nowrap（不换行）、balance（平衡各行字符数）、pretty（优化排版质量）、stable（编辑时保持前面行稳定）。这是2024-2025年最重要的CSS文本新特性之一。}}

{{知识点14}}{{text-wrap:balance：平衡各行字符数以提升排版质量和可读性。适用于标题、字幕、引用等短文本。由于计算开销大，浏览器限制了影响的行数（Chrome限制6行，Firefox限制10行）。对于标题类元素特别推荐使用。}}

{{知识点15}}{{text-wrap:pretty：使用更慢但更优的排版算法，减少孤行（orphans）。适用于正文等较长文本段落。性能开销较大，应仅在布局质量比速度更重要时使用。Chrome在2023年引入此特性。}}

{{知识点16}}{{text-wrap:stable：编辑内容时，确保正在编辑行之前的行保持稳定，不会因当前行的变化而重新换行。适用于contenteditable元素，改善编辑体验。}}

{{知识点17}}{{text-decoration属性：简写属性，控制文本装饰效果。包含四个子属性：text-decoration-line（装饰线类型）、text-decoration-color（颜色）、text-decoration-style（样式）、text-decoration-thickness（粗细）。装饰线类型：underline、overline、line-through、blink、spelling-error、grammar-error。装饰样式：solid、double、dotted、dashed、wavy。}}

{{知识点18}}{{text-decoration继承特性：文本装饰会跨越后代文本元素绘制。如果父元素设置了text-decoration:underline，子元素无法通过text-decoration:none移除装饰。但子元素可以添加额外的装饰（如overline）。text-decoration-thickness可设置为auto、from-font、长度值或百分比。}}

{{知识点19}}{{text-shadow属性：为文本添加阴影效果。语法：text-shadow: offset-x offset-y blur-radius color。支持多个阴影（逗号分隔），前到后绘制。可选值：color（颜色，可在偏移值前后）、offset-x和offset-y（必需，水平和垂直偏移）、blur-radius（可选，模糊半径）。当偏移均为0时，阴影在文本正后方。}}

{{知识点20}}{{word-break属性：控制单词内如何断行以防止溢出。值：normal（默认断行规则）、break-all（在任何两个非CJK字符间可断行）、keep-all（CJK文本不断行）、auto-phrase（实验性，语言分析优化断行）、break-word（已弃用，等同于overflow-wrap:anywhere + word-break:normal）。}}

{{知识点21}}{{word-break:break-all vs overflow-wrap:break-word：break-all会在文本即将溢出的位置强制断行，即使整个单词能放在一行上也会断行。overflow-wrap仅在整行无法容纳整个单词时才在任意位置断行。break-all更激进，适用于长URL或无空格文本；overflow-wrap更保守，优先保持单词完整性。}}

{{知识点22}}{{overflow-wrap属性（原word-wrap）：控制浏览器是否在单词内断行以防止溢出。值：normal（仅在正常断行点换行）、anywhere（可在任意位置断行，影响min-content尺寸计算）、break-word（同anywhere但不影响min-content计算）。仅在整行无法容纳整个单词时才触发断行，比word-break更温和。}}

{{知识点23}}{{hyphens属性：控制单词断行时是否自动添加连字符。值：none（不使用连字符）、manual（默认，仅在显式建议的断行点）、auto（浏览器自动在合适位置断行）。需要设置HTML lang属性以启用正确的语言连字符规则。浏览器支持因语言而异。}}

{{知识点24}}{{连字符相关字符：U+00AD（SHY，软连字符）是不可见的断行提示，仅在需要断行时显示连字符。U+2010（HYPHEN，硬连字符）始终显示。HTML中使用&shy;插入软连字符。<wbr>元素导致的换行不添加连字符。hyphenate-character属性可指定替代的连字符字符。}}

{{知识点25}}{{line-break属性：控制换行的严格程度，主要影响CJK文本和标点符号处理。值：auto（默认）、loose（宽松规则，适合短行）、normal（正常规则）、strict（严格规则，禁止在某些标点后换行）。对于CJK文本排版非常重要。}}

{{知识点26}}{{tab-size属性：控制制表符（U+0009）的宽度。默认值为8。可设置为长度值（如4px）或数字（表示空格字符数，如4）。常用于代码块和<pre>元素的格式化显示。该属性可继承。}}

{{知识点27}}{{hanging-punctuation属性：控制标点符号是否悬挂到行框外。值：none（默认）、first（行首标点悬挂）、force-end（强制行尾标点悬挂）、allow-end（允许行尾标点悬挂）、last（行尾标点悬挂）。用于改善排版美感，使边缘对齐更整齐。目前浏览器支持有限。}}

{{知识点28}}{{text-autospace属性（CSS Text Level 4）：控制CJK字符与非CJK字符之间的自动间距。用于改善中英文混排的可读性。值：normal（默认，根据需要添加间距）、no-autospace（不添加）等。这是改善中日韩文字混排体验的重要新特性，2024-2025年浏览器开始支持。}}

{{知识点29}}{{text-spacing-trim属性（CSS Text Level 4）：控制CJK标点符号周围的间距修剪。用于压缩全角标点的多余间距，使排版更紧凑美观。值包括normal、space-all、space-first、trim-start等。是改善中文排版质量的重要新特性。}}

{{知识点30}}{{writing-mode与文本方向：writing-mode控制文本流方向，值：horizontal-tb（默认，水平从上到下）、vertical-rl（垂直从右到左）、vertical-lr（垂直从左到右）。direction属性控制行内方向：ltr（从左到右）、rtl（从右到左）。unicode-bidi处理双向文本。text-orientation控制竖排文本中字符的方向。}}

{{知识点31}}{{text-combine-upright属性：将多个字符组合在一个竖排字符空间中。值：none（默认）、all（将所有字符组合）。主要用于竖排CJK文本中的数字横排显示，如竖排文本中的年份"2025"。}}

{{知识点32}}{{text-orientation属性：控制竖排文本中字符的方向。值：mixed（默认，CJK字符竖排，拉丁字符旋转）、upright（所有字符竖排）、sideways（所有字符侧向旋转90度）。与writing-mode:vertical-*配合使用。}}

{{知识点33}}{{text-size-adjust属性：控制移动设备上文本大小的自动调整。值：none（禁用自动调整）、auto（默认，允许浏览器调整）、百分比值。用于防止移动浏览器在横屏时放大文本。-webkit-text-size-adjust是其带前缀版本。}}

{{知识点34}}{{orphans和widows属性：控制分页或分列时的最小行数。orphans设置页面底部元素必须保留的最少行数（默认2），widows设置页面顶部元素必须保留的最少行数（默认2）。在打印样式和多列布局中非常有用。}}

{{知识点35}}{{text-justify属性：控制text-align:justify时使用的对齐算法。值：auto（浏览器选择）、inter-word（在单词间增加间距）、inter-character（在字符间增加间距）、none（禁用对齐）。inter-character提供更均匀的间距但性能较低。}}

{{知识点36}}{{initial-letter属性：控制首字母下沉效果。语法：initial-letter: normal | <number> <integer>?。第一个数字控制首字母大小（占几行高），第二个数字控制下沉行数。例如initial-letter: 3 2;表示首字母占3行高、下沉2行。浏览器支持仍在发展中。}}

{{知识点37}}{{CSS Text Level 4新特性汇总：2025年CSS Text Module Level 4引入的重要新特性包括：text-wrap:balance/pretty/stable改善排版质量；text-autospace改善中英文混排；text-spacing-trim压缩CJK标点间距；white-space成为简写属性并新增子属性；text-overflow新增fade值；word-break新增auto-phrase值。这些特性代表了CSS文本排版的重大进步。}}

{{知识点38}}{{文本溢出处理完整策略：处理文本溢出需要三层配合：1）white-space/text-wrap控制是否换行（nowrap禁止换行）；2）word-break/overflow-wrap控制单词内断行（break-word或anywhere）；3）text-overflow控制溢出视觉提示（ellipsis显示省略号）。典型单行省略：overflow:hidden; white-space:nowrap; text-overflow:ellipsis。}}

{{知识点39}}{{text-shadow与box-shadow的区别：text-shadow作用于文本字符，支持offset-x、offset-y、blur-radius和color，不支持spread-radius和inset。box-shadow作用于元素盒模型，支持spread-radius和inset。text-shadow可继承，box-shadow不可继承。text-shadow适用于::first-letter和::first-line伪元素。}}

{{知识点40}}{{CSS文本排版最佳实践：1）标题使用text-wrap:balance提升排版质量；2）正文使用text-wrap:pretty减少孤行；3）使用text-align:start/end替代left/right以支持RTL语言；4）长URL使用overflow-wrap:anywhere防止溢出；5）CJK混排使用text-autospace；6）避免对大段文本使用text-transform:uppercase；7）可编辑区域使用text-wrap:stable防止重排；8）使用lang属性启用正确的语言特性。}}
