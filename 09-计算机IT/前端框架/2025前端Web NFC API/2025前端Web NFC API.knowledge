# 2025前端Web NFC API
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}{{Web NFC API 是什么}}
Web NFC API 是一个 Web 标准 API，允许 Web 应用通过 NFC（近场通信）技术与兼容的 NFC 设备（如 NFC 标签）交换数据。它基于轻量级的 NDEF（NFC Data Exchange Format）消息格式，使浏览器能够读取和写入 NFC 标签上的数据。该 API 由 W3C Web NFC 社区组维护，目前仍属于实验性技术（Experimental）。

{{知识点2}}{{Web NFC API 的核心接口}}
Web NFC API 包含三个核心接口：NDEFReader 用于读写 NFC 标签，NDEFMessage 表示 NDEF 消息，NDEFRecord 表示 NDEF 记录。此外还有 NDEFReadingEvent 接口用于处理读取事件。这些接口共同构成了完整的 NFC 数据交互能力。

{{知识点3}}{{NDEFReader 接口概述}}
NDEFReader 是 Web NFC API 的核心接口，用于从兼容的 NFC 设备读取数据和向其写入数据。当 NFC 设备进入读取器的磁场感应范围内时，NDEFReader 可以与其通信。它继承自 EventTarget 接口，支持事件监听机制。

{{知识点4}}{{NDEFReader 构造函数}}
通过 new NDEFReader() 可以创建一个新的 NDEFReader 实例。该构造函数不需要参数，创建后即可调用 scan() 和 write() 方法进行 NFC 操作。

{{知识点5}}{{NDEFReader.scan() 方法}}
NDEFReader.scan() 方法激活读取设备并返回一个 Promise。当 NFC 标签读取操作被调度时 Promise 解析，如果遇到硬件或权限错误则拒绝。该方法会触发权限提示（如果 "nfc" 权限尚未授予）。调用 scan() 是开始监听 NFC 标签的前提。

{{知识点6}}{{NDEFReader.write() 方法}}
NDEFReader.write() 方法尝试将 NDEF 消息写入 NFC 标签，返回一个 Promise。当消息成功写入标签时 Promise 解析，如果遇到硬件或权限错误则拒绝。同样会触发权限提示。写入前需要有标签进入感应范围。

{{知识点7}}{{NDEFReader reading 事件}}
当从兼容的 NFC 设备获得新的读取数据时，NDEFReader 会触发 reading 事件。该事件的类型为 NDEFReadingEvent，包含读取到的 NDEF 消息和设备序列号。可以通过 onreading 属性或 addEventListener 监听此事件。

{{知识点8}}{{NDEFReader readingerror 事件}}
当 NFC 标签在读取设备附近但无法被读取时，会触发 readingerror 事件。这通常发生在标签格式不兼容或数据损坏的情况下。可以通过 onreadingerror 属性监听此事件以进行错误处理。

{{知识点9}}{{NDEFMessage 接口}}
NDEFMessage 接口表示从 NFC 标签读取的或可以写入 NFC 标签的 NDEF 消息内容。一个 NDEF 消息由一个或多个 NDEF 记录（NDEFRecord）组成。可以通过构造函数创建，也可以从 NDEFReadingEvent.message 属性获取。

{{知识点10}}{{NDEFMessage 构造函数}}
NDEFMessage() 构造函数接受一个 NDEFRecord 数组作为参数，创建并初始化一个新的 NDEFMessage 对象。用于构造要写入 NFC 标签的消息内容。

{{知识点11}}{{NDEFMessage.records 属性}}
NDEFMessage.records 是只读属性，返回消息中包含的 NDEF 记录列表（NDEFRecord 数组）。这是访问消息内容的主要入口，通过遍历 records 可以获取所有记录的数据。

{{知识点12}}{{NDEFRecord 接口概述}}
NDEFRecord 接口表示可以包含在 NDEF 消息中的单条 NDEF 记录。每条记录包含类型信息、标识符和数据载荷。NDEFRecord 提供了读取和解析这些数据的属性和方法。

{{知识点13}}{{NDEFRecord.recordType 属性}}
NDEFRecord.recordType 是只读属性，返回记录的类型。标准类型包括："empty"（空记录）、"text"（文本）、"url"（URL）、"smart-poster"（智能海报）、"absolute-url"（绝对URL）、"mime"（MIME类型）、"unknown"（未知）。也可以是外部类型名称，格式为"域名:自定义类型名"。

{{知识点14}}{{NDEFRecord.mediaType 属性}}
NDEFRecord.mediaType 是只读属性，返回记录的 MIME 类型。当 recordType 不等于 "mime" 时，该值为 null。对于 MIME 类型的记录，可以据此判断载荷数据的格式（如 application/json、image/png 等）。

{{知识点15}}{{NDEFRecord.id 属性}}
NDEFRecord.id 是只读属性，返回记录的标识符，可以是绝对或相对 URL，用于标识记录。注意标识符的唯一性仅由记录的生成者保证，NFC 系统不强制全局唯一。

{{知识点16}}{{NDEFRecord.data 属性}}
NDEFRecord.data 是只读属性，返回一个 DataView 对象，包含记录载荷的原始字节数据。这是获取记录实际内容的核心属性，可以根据 recordType 和 mediaType 来决定如何解析这些原始数据。

{{知识点17}}{{NDEFRecord.encoding 属性}}
NDEFRecord.encoding 是只读属性，返回文本载荷的编码方式（如 "utf-8"、"utf-16" 等）。当记录不是文本类型时返回 null。对于 text 类型记录，用于正确解码文本内容。

{{知识点18}}{{NDEFRecord.lang 属性}}
NDEFRecord.lang 是只读属性，返回文本载荷的语言标识（如 "en"、"zh-CN" 等）。如果未指定语言则返回 null。对于多语言应用场景，可以根据此属性选择合适的文本内容。

{{知识点19}}{{NDEFRecord.toRecords() 方法}}
NDEFRecord.toRecords() 方法将 NDEFRecord.data 转换为子记录序列。这允许解析可能包含嵌套记录的记录类型的载荷，如智能海报（smart-poster）和外部类型（external type）记录。返回 NDEFRecord 数组。

{{知识点20}}{{NDEFReadingEvent 接口}}
NDEFReadingEvent 接口表示从 NDEFReader 获得新的 NFC 读取时触发的事件对象。它继承自 Event 接口，包含 message 和 serialNumber 两个只读属性，用于获取读取到的消息和设备信息。

{{知识点21}}{{NDEFReadingEvent.message 属性}}
NDEFReadingEvent.message 是只读属性，返回包含接收到的消息的 NDEFMessage 对象。通过该属性可以访问所有读取到的 NDEF 记录，是获取 NFC 数据的主要途径。

{{知识点22}}{{NDEFReadingEvent.serialNumber 属性}}
NDEFReadingEvent.serialNumber 是只读属性，返回设备的序列号，用于防冲突和识别。如果设备没有可用的序列号，则返回空字符串。可用于区分不同的 NFC 标签。

{{知识点23}}{{安全上下文要求}}
Web NFC API 仅在安全上下文（Secure Contexts）中可用，即必须通过 HTTPS 提供服务。这是出于安全考虑，防止恶意网站通过 NFC 读取敏感数据或向 NFC 标签写入恶意内容。本地开发环境（localhost）也被视为安全上下文。

{{知识点24}}{{权限模型}}
Web NFC API 使用 Permissions API 的 "nfc" 权限。首次调用 scan() 或 write() 方法时会触发权限提示，用户需要明确授予 NFC 访问权限。权限状态可以通过 navigator.permissions.query({ name: "nfc" }) 查询。

{{知识点25}}{{浏览器兼容性现状}}
截至 2025 年，Web NFC API 主要在 Chromium 内核浏览器（Chrome、Edge 等）的 Android 版本中获得支持。桌面浏览器和 Safari/Firefox 尚未支持。该 API 仍标记为实验性技术，生产环境使用需谨慎评估兼容性。

{{知识点26}}{{平台限制 - 仅限 Android}}
Web NFC API 目前仅在 Android 平台的 Chromium 浏览器中可用。这是因为 NFC 硬件访问需要操作系统的底层支持，而 iOS 的 NFC 访问受到 Apple 的严格限制，桌面设备通常不具备 NFC 硬件。

{{知识点27}}{{NDEF 记录类型 - text 类型}}
text 类型记录用于存储文本数据。创建时需指定 recordType: "text"，data 为文本字符串。读取时通过 record.data 获取 DataView，再通过 TextDecoder 解码。encoding 属性指定编码，lang 属性指定语言。

{{知识点28}}{{NDEF 记录类型 - url 类型}}
url 类型记录用于存储 URL 地址。创建时指定 recordType: "url"，data 为 URL 字符串。读取时通过 TextDecoder 解码 data 即可获得 URL。适用于 NFC 标签触发浏览器打开特定网页的场景。

{{知识点29}}{{NDEF 记录类型 - mime 类型}}
mime 类型记录用于存储带 MIME 类型的任意数据。创建时指定 recordType: "mime"，mediaType 指定 MIME 类型（如 "application/json"），data 为对应的二进制或文本数据。适用于传输结构化数据。

{{知识点30}}{{NDEF 记录类型 - smart-poster 类型}}
smart-poster 类型记录实现 NFC 智能海报标准，可以包含嵌套的 URL、文本和其他记录。通常用于存储一个主 URL 以及关联的元数据（如标题、图标等）。使用 toRecords() 方法解析嵌套内容。

{{知识点31}}{{NDEF 记录类型 - external 类型}}
external 类型（外部类型）允许应用自定义 NDEF 记录类型。格式为 "域名:类型名"，如 "example.com:mytype"。域名部分应为应用拥有或控制的域名，确保类型名称的全局唯一性。

{{知识点32}}{{写入 NFC 标签的最佳实践}}
推荐在 reading 事件回调中执行 write() 操作，而非直接调用。这是因为写入需要标签在感应范围内，而 reading 事件保证了标签的存在。先读取标签内容进行验证，确认是目标标签后再写入，避免误写。

{{知识点33}}{{处理读写冲突}}
当需要在读取的同时进行写入时，应使用 ignoreRead 标志位控制读取处理逻辑。在写入期间设置 ignoreRead = true 跳过 reading 事件处理，写入完成后恢复。这避免了写入过程中被读取事件干扰。

{{知识点34}}{{Web NFC API 的应用场景}}
主要应用场景包括：NFC 标签读写（产品信息、票务）、设备间数据交换、物联网设备配对、智能海报交互、身份验证与门禁系统、移动支付辅助等。Web NFC 使这些场景可以通过纯 Web 应用实现，无需原生 App。

{{知识点35}}{{与传统 NFC 开发的对比}}
相比原生 Android NFC 开发（使用 Android NFC API），Web NFC API 更加轻量和易用，但功能也更受限。Web NFC 仅支持 NDEF 格式，不支持低级 NFC 操作（如 ISO-DEP、NDEF-formatted raw access）。原生 API 可以处理更多 NFC 标签类型和协议。

{{知识点36}}{{NDEF 消息格式基础}}
NDEF（NFC Data Exchange Format）是 NFC 论坛定义的标准数据格式。一个 NDEF 消息由一个或多个 NDEF 记录组成，每条记录包含头部信息（类型、ID、载荷长度）和载荷数据。NDEF 是 Web NFC API 唯一支持的数据格式。

{{知识点37}}{{错误处理策略}}
Web NFC API 的错误处理应覆盖：硬件不可用（设备无 NFC）、权限被拒绝、标签不可读/不可写、写入超时等场景。scan() 和 write() 返回的 Promise 在这些情况下会 reject，应使用 try-catch 或 .catch() 进行捕获和用户提示。

{{知识点38}}{{生命周期管理}}
NDEFReader 的 scan() 一旦成功，将持续监听 NFC 标签直到页面关闭或 reader 被垃圾回收。不需要重复调用 scan()。如果需要停止监听，可以使用 AbortController 的 abort() 方法来取消扫描。

{{知识点39}}{{AbortController 集成}}
NDEFReader.scan() 支持 AbortSignal 选项，可以传入 AbortController 的 signal 来控制扫描的生命周期。调用 controller.abort() 可以优雅地停止 NFC 扫描，这在组件卸载或用户取消操作时非常有用。

{{知识点40}}{{DataView 数据解析}}
NDEFRecord.data 返回 DataView 对象，需要根据记录类型进行不同的解析。文本数据使用 TextDecoder 解码，二进制数据可直接操作 ArrayBuffer，JSON 数据先解码为字符串再 JSON.parse。了解 DataView 和 ArrayBuffer 的使用是处理 NFC 数据的基础。
