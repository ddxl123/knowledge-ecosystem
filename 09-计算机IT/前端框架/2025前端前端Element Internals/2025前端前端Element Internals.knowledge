# 2025前端前端Element Internals
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}{{ElementInternals 是什么？}}
ElementInternals 是 Web 平台提供的一个接口（Interface），属于 Web Components 规范的一部分。它为自定义元素（Custom Elements）提供了与原生 HTML 表单元素相仿的内部能力，包括表单关联、表单验证、无障碍语义以及自定义状态等功能。通过这个接口，开发者可以让自定义元素像原生表单控件一样参与表单提交和验证。

{{知识点2}}{{ElementInternals 的获取方式：attachInternals()}}
ElementInternals 对象不能通过 new 构造函数创建，只能通过 HTMLElement.prototype.attachInternals() 方法获取。调用方式为 `this.attachInternals()`，通常在自定义元素的构造函数中调用，返回的 ElementInternals 实例被保存在类的私有属性中供后续使用。

{{知识点3}}{{formAssociated 静态属性}}
要使自定义元素成为表单关联元素（form-associated custom element），必须在自定义元素类上设置静态属性 `static formAssociated = true`。只有设置了此属性，ElementInternals 的表单相关方法（如 setFormValue、setValidity 等）才可正常使用，否则会抛出 NotSupportedError 异常。

{{知识点4}}{{form 只读属性}}
ElementInternals.form 是一个只读属性，返回与该自定义元素关联的 HTMLFormElement。当自定义元素被放置在 <form> 标签内或通过 label 关联到表单时，可以通过此属性获取所属表单的引用，实现对表单的编程控制。

{{知识点5}}{{shadowRoot 只读属性}}
ElementInternals.shadowRoot 是一个只读属性，返回与该元素关联的 ShadowRoot 对象。即使 Shadow DOM 以 closed 模式创建，通过 attachInternals() 获取的 ElementInternals 仍然可以访问该元素的 Shadow Root，这为内部逻辑提供了安全的 DOM 访问途径。

{{知识点6}}{{setFormValue() 方法}}
setFormValue(value) 或 setFormValue(value, state) 方法用于设置自定义元素提交给服务器的值。第一个参数 value 是提交值（可以是 File、string 或 FormData），第二个参数 state 是可选的用户输入状态，用于表单恢复时重新显示用户之前输入的内容。例如日期选择器中 value 可能是标准化格式 "2019-03-15"，而 state 可以是用户原始输入 "3/15/2019"。

{{知识点7}}{{setFormValue 的 value 与 state 的区别}}
在 setFormValue(value, state) 中，value 是经过处理后适合提交给服务器的数据，而 state 保存的是用户实际输入的原始格式。当浏览器需要恢复表单状态时（如页面后退），会使用 state 来还原用户界面，而非 value。

{{知识点8}}{{setValidity() 方法}}
setValidity(flags, message?, anchor?) 方法用于设置自定义元素的验证状态。flags 是一个字典对象，包含各种验证标志；message 是可选的验证失败消息（当有标志为 true 时必填）；anchor 是可选的 HTMLElement，用于用户代理报告表单提交问题时定位到的元素。要表示元素通过所有验证，传入空对象 {} 即可。

{{知识点9}}{{setValidity 的验证标志（flags）}}
setValidity 接受的 flags 对象可包含以下布尔属性：valueMissing（缺少必填值）、typeMismatch（类型不匹配）、patternMismatch（正则不匹配）、tooLong（超出最大长度）、tooShort（低于最小长度）、rangeUnderflow（低于最小值）、rangeOverflow（超出最大值）、stepMismatch（步长不匹配）、badInput（无法解析的输入）、customError（自定义错误）。任一为 true 时元素将匹配 :invalid 伪类。

{{知识点10}}{{checkValidity() 方法}}
checkValidity() 方法检查元素是否满足所有约束验证规则，返回布尔值。如果返回 false，还会在元素上触发可取消的 invalid 事件。此方法仅在元素为表单关联元素时可用，否则抛出 NotSupportedError 异常。

{{知识点11}}{{reportValidity() 方法}}
reportValidity() 方法与 checkValidity() 类似，但除了检查有效性外，还会向用户报告验证失败（通常显示浏览器的默认验证气泡提示）。它在元素上触发 invalid 事件并返回布尔值，适合在表单提交时使用以提供用户可见的反馈。

{{知识点12}}{{validity 只读属性}}
ElementInternals.validity 返回一个 ValidityState 对象，表示该元素当前的各个验证状态。该对象包含 valueMissing、typeMismatch、patternMismatch、tooLong、tooShort、rangeUnderflow、rangeOverflow、stepMismatch、stepMismatch、badInput、customError 等布尔属性，可以逐个检查具体的验证问题。

{{知识点13}}{{willValidate 只读属性}}
willValidate 是一个只读布尔属性，如果该元素是可提交的表单元素且为约束验证的候选对象，则返回 true。例如 disabled 的元素 willValidate 返回 false。该属性可用于判断元素是否参与表单验证流程。

{{知识点14}}{{validationMessage 只读属性}}
validationMessage 是一个只读字符串属性，包含该元素的验证消息。当元素验证通过时返回空字符串；当调用 setValidity 或 setCustomValidity 设置了验证错误时，返回对应的错误消息文本。

{{知识点15}}{{labels 只读属性}}
labels 返回一个 NodeList，包含所有关联到该自定义元素的 <label> 元素。这使得自定义表单控件可以通过 label 元素获取焦点和描述，与原生表单元素的 label 关联行为一致。

{{知识点16}}{{states 属性与 CustomStateSet}}
ElementInternals.states 返回一个 CustomStateSet 对象，它是一个 SetLike 的集合，允许自定义元素定义自己的内部状态。通过 `this.internals.states.add('my-state')` 添加状态后，可以在 CSS 中通过 `:--my-state` 选择器匹配该状态，实现自定义元素的样式化。

{{知识点17}}{{自定义状态的 CSS 选择器语法}}
当使用 CustomStateSet 添加自定义状态后，CSS 中可以通过 `:--状态名` 语法选择具有该状态的元素。例如 `my-element:--checked` 可以匹配 states 中包含 'checked' 的 my-element 元素。注意状态名中的大小写会被转换为小写。

{{知识点18}}{{ARIA 无障碍属性支持}}
ElementInternals 包含大量 ARIA 相关属性，如 role、ariaLabel、ariaChecked、ariaDisabled、ariaExpanded、ariaHidden 等。这些属性允许自定义元素定义默认的无障碍语义，确保屏幕阅读器等辅助技术能正确理解和播报自定义元素的角色和状态。

{{知识点19}}{{role 属性}}
ElementInternals.role 是一个字符串属性，用于设置自定义元素的 ARIA 角色。例如 `this._internals.role = 'checkbox'` 可以将自定义元素的默认无障碍角色设为复选框。页面作者仍可通过 HTML 的 role 属性覆盖此默认值。

{{知识点20}}{{ariaChecked 属性}}
ariaChecked 反映元素的"选中"状态，常用于复选框、单选按钮等具有选中状态的控件。在自定义复选框中，`this._internals.ariaChecked = 'false'` 设置默认值为未选中，当状态改变时同步更新此属性以保持无障碍语义正确。

{{知识点21}}{{创建表单关联自定义元素的完整步骤}}
创建表单关联自定义元素的步骤：1) 在类上声明 static formAssociated = true；2) 在构造函数中调用 this.attachInternals()；3) 通过 attributeChangedCallback 等生命周期钩子调用 setFormValue 更新值；4) 使用 setValidity 实现验证逻辑；5) 通过 ARIA 属性设置无障碍语义。

{{知识点22}}{{ElementInternals 在 HTML 表单提交中的行为}}
当表单关联的自定义元素参与表单提交时，浏览器会读取通过 setFormValue 设置的 value，将其作为表单数据的一部分发送。如果元素验证失败（checkValidity 返回 false），表单提交会被阻止，类似于原生表单元素的约束验证行为。

{{知识点23}}{{NotSupportedError 异常}}
当调用 ElementInternals 的表单相关方法（setFormValue、setValidity、checkValidity、reportValidity）时，如果自定义元素类没有声明 static formAssociated = true，会抛出 NotSupportedError DOMException。这是 ElementInternals 最常见的运行时错误之一。

{{知识点24}}{{ElementInternals 与 Shadow DOM 的关系}}
ElementInternals 提供了 shadowRoot 属性来安全地访问 Shadow DOM，即使 Shadow Root 以 closed 模式创建。这解决了一个常见问题：closed 模式的 Shadow Root 通过 element.shadowRoot 返回 null，但 ElementInternals 仍然可以访问它，为内部组件逻辑提供了可靠的 DOM 访问。

{{知识点25}}{{ElementInternals 的浏览器兼容性}}
ElementInternals API 从 2023 年 3 月起在主流浏览器中广泛可用（Baseline: Widely available）。Chrome 77+、Firefox 79+、Safari 16.4+、Edge 79+ 均支持。setFormValue、setValidity、checkValidity、reportValidity 等核心方法也达到了广泛的跨浏览器支持。

{{知识点26}}{{自定义元素的表单验证与 :invalid/:valid 伪类}}
通过 setValidity 设置验证标志后，自定义元素会匹配 :invalid 和 :valid CSS 伪类，与原生表单元素行为一致。例如设置 valueMissing: true 后，元素匹配 :invalid，开发者可以使用 CSS 为无效状态添加视觉反馈。

{{知识点27}}{{setValidity 的 anchor 参数}}
setValidity 方法的第三个参数 anchor 是一个 HTMLElement，用于浏览器在报告验证错误时定位到特定元素。anchor 必须是该自定义元素的 shadow-including descendant（影子包含后代），否则会抛出 NotFoundError 异常。这对于将错误提示关联到 Shadow DOM 内部的特定输入元素非常有用。

{{知识点28}}{{setFormValue 传入 null 的行为}}
调用 setFormValue(null) 表示该自定义元素没有要提交的值，元素将从表单数据中排除。这类似于原生复选框未选中时不参与表单数据提交的行为。在自定义复选框示例中，未选中时调用 this._internals.setFormValue(null)。

{{知识点29}}{{ElementInternals 与 HTML Standard 的规范位置}}
ElementInternals 在 WHATWG HTML Living Standard 中定义于 §4.13.7 "Element Internals" 部分，具体包括 §4.13.7.1 The ElementInternals interface、§4.13.7.2 Shadow root access、§4.13.7.3 Form-associated custom elements、§4.13.7.4 Accessibility semantics 和 §4.13.7.5 Custom state pseudo-class 五个子章节。

{{知识点30}}{{内置定制元素（Customized Built-in Elements）与 ElementInternals}}
ElementInternals 主要用于自治自定义元素（Autonomous Custom Elements）。对于内置定制元素（如扩展 HTMLButtonElement），也可以使用 attachInternals()，但其表单关联行为继承自被扩展的原生元素，使用场景和限制有所不同。

{{知识点31}}{{ElementInternals 与 AOM（Accessibility Object Model）}}
ElementInternals 中的 ARIA 属性是 Accessibility Object Model (AOM) 计划的一部分。其目标是允许自定义元素以编程方式定义默认的无障碍语义，而无需依赖 HTML 属性。这些默认值可以被页面作者通过 role 和 aria-* HTML 属性覆盖。

{{知识点32}}{{实际应用：自定义日期选择器}}
构建自定义日期选择器时，使用 ElementInternals 的典型模式：setFormValue("2025-01-15", "1/15/2025")，其中 value 为标准化日期格式供服务器处理，state 为用户输入的原始格式供浏览器恢复表单状态时使用。同时通过 setValidity 设置日期范围验证。

{{知识点33}}{{实际应用：自定义文件上传组件}}
在自定义文件上传组件中，可以将 File 对象通过 setFormValue(file) 传递给表单。ElementInternals 支持 File 类型作为 value 参数，使得自定义文件上传组件能像原生 <input type="file"> 一样参与表单数据提交。

{{知识点34}}{{clearValidity 行为}}
要清除元素的验证错误状态，只需调用 setValidity({}) 传入空对象即可。这会将所有验证标志重置为 false，元素重新匹配 :valid 伪类。无需传递 message 参数。

{{知识点35}}{{strong native semantics 概念}}
自定义元素作者应明确声明哪些无障碍语义是"强原生语义"（strong native semantics），即不应被用户覆盖的部分。例如自定义复选框的 role 和 ariaChecked 通常应作为强原生语义，阻止使用者将其 role 改为 button 等不匹配的角色。

{{知识点36}}{{ElementInternals 的局限性}}
ElementInternals 虽然强大，但仍有一些局限：不支持自定义表单控件的原生弹出式 UI（如日期选择器的下拉面板）；自定义验证气泡的样式受限；某些原生行为（如输入法交互）难以完全复制。开发者需要在自定义实现和原生行为之间权衡。
