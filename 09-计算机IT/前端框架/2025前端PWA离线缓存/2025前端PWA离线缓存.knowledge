# 2025前端PWA离线缓存
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}{{PWA（Progressive Web App）是一类使用 Web 技术构建但提供类似原生应用体验的应用。PWA的核心能力之一是离线运行，这通过Service Worker、Cache API和Fetch API三大技术实现。Service Worker作为独立线程运行，拦截网络请求并决定是从缓存还是网络获取资源。}}

{{知识点2}}{{Service Worker是一种特殊的Web Worker，运行在与主线程分离的独立线程中。它充当Web应用的可编程网络代理，能够拦截和处理网络请求，管理缓存响应，实现离线功能。Service Worker无法直接访问DOM，通过postMessage与主线程通信。}}

{{知识点3}}{{Service Worker生命周期包含三个阶段：注册（register）、安装（install）和激活（activate）。注册通过navigator.serviceWorker.register()完成；安装阶段通过install事件预缓存资源；激活阶段通过activate事件清理旧版本缓存。新版本的Service Worker在所有旧版本控制的页面关闭后才会激活。}}

{{知识点4}}{{Service Worker的注册代码示例：通过navigator.serviceWorker.register('/sw.js', { scope: '/' })注册，scope参数指定控制范围。注册前应使用'serviceWorker' in navigator进行特性检测，确保浏览器支持。注册的文件路径相对于origin而非应用根目录。}}

{{知识点5}}{{Service Worker仅在安全上下文（HTTPS）下运行，localhost被视为安全源用于开发。注册失败的常见原因包括：非HTTPS环境、文件路径错误、scope超出Worker路径范围、隐私浏览模式限制等。}}

{{知识点6}}{{Cache API提供持久化的Request/Response对象对存储机制。CacheStorage接口管理所有命名的Cache对象，可通过caches.open(name)打开缓存，cache.add(url)和cache.addAll(urls)批量添加资源，cache.match(request)查找匹配响应，cache.put(request, response)手动存储。}}

{{知识点7}}{{Cache API的关键特性：缓存中的条目不会自动过期，需要手动管理；浏览器对每个源有存储配额限制，可通过navigator.storage.estimate()查询使用情况；Cache API不遵循HTTP缓存头部；Response对象只能消费一次，存储到缓存前需使用response.clone()克隆。}}

{{知识点8}}{{Cache-First（缓存优先）策略是最常用的离线缓存策略。流程：先检查缓存中是否有匹配资源，有则直接返回；没有则发起网络请求，成功后将响应存入缓存并返回。适用于不常变化的静态资源如HTML、CSS、JS、图片等，能提供最快的响应速度。}}

{{知识点9}}{{Network-First（网络优先）策略：优先从网络获取资源，网络请求失败时回退到缓存。适用于需要最新数据的场景如API请求、动态内容。实现时通过fetch()发起请求，catch块中调用caches.match()返回缓存版本。}}

{{知识点10}}{{Stale-While-Revalidate（缓存优先且后台更新）策略：先从缓存返回响应，同时在后台发起网络请求更新缓存。这样用户能立即得到响应（可能稍旧），同时缓存保持相对新鲜。适合需要平衡响应速度和数据新鲜度的场景。}}

{{知识点11}}{{Cache-Only（仅缓存）策略：所有资源只从缓存获取，不进行任何网络请求。适用于在install事件中已完全预缓存的资源，确保完全离线可用。如果缓存中没有匹配资源则返回错误响应。}}

{{知识点12}}{{Network-Only（仅网络）策略：所有请求都直接发送到网络，不使用缓存。适用于不需要离线支持的请求，如分析跟踪、非GET请求等。这是浏览器的默认行为，当没有Service Worker拦截时的表现。}}

{{知识点13}}{{预缓存（Precaching）是在Service Worker的install事件中预先获取并缓存应用所需的关键资源。通过event.waitUntil()确保缓存操作完成，失败则安装失败。预缓存的资源通常包括应用Shell（HTML骨架、CSS、核心JS），确保应用首次离线时仍能正常渲染。}}

{{知识点14}}{{Service Worker的fetch事件在应用请求任何受控资源时触发，包括页面导航和子资源加载。通过event.respondWith()方法可以返回自定义Response，从而实现缓存拦截。如果不调用respondWith()，请求将正常发送到网络。}}

{{知识点15}}{{缓存版本管理是PWA维护的重要环节。通过为缓存命名添加版本号（如'myapp-v1'、'myapp-v2'），在activate事件中删除旧版本缓存。使用caches.keys()获取所有缓存名称，遍历并删除不在预期列表中的旧缓存，防止存储空间浪费。}}

{{知识点16}}{{Response对象的克隆（clone）是Cache API使用中的关键概念。由于Response是流式的，只能被消费一次，因此需要同时将响应返回给浏览器和存入缓存时，必须先response.clone()再分别使用。克隆失败会抛出TypeError。}}

{{知识点17}}{{缓存匹配算法考虑Vary头部。当使用cache.match()查找匹配时，不仅比较请求URL，还检查响应的Vary头部指定的请求头字段。这意味着同一URL可能因不同请求头（如Accept-Language）而有多个缓存版本。}}

{{知识点18}}{{Background Sync API允许应用注册后台同步任务，当设备恢复网络连接时自动执行。通过navigator.serviceWorker.ready获取ServiceWorkerRegistration，调用registration.sync.register('tag-name')注册。Service Worker监听sync事件，根据tag执行相应任务。如果任务失败，浏览器可能有限次数地重试。}}

{{知识点19}}{{Periodic Background Sync API允许Service Worker按周期性时间表在后台同步数据。注册方式类似Background Sync，但使用periodicsync事件。浏览器可能因设备电量、使用频率等因素调整同步频率。适用于定期更新缓存内容、预取新数据等场景。}}

{{知识点20}}{{Background Fetch API用于处理长时间后台下载任务，如下载大文件、视频等。与Background Sync不同，Background Fetch在主应用和Service Worker都关闭后仍可继续。浏览器会显示持久UI元素提示用户下载进度，用户可随时取消。完成后通过backgroundfetchsuccess等事件通知Service Worker。}}

{{知识点21}}{{Workbox是Google推出的PWA工具库，简化Service Worker和缓存策略的实现。提供workbox-strategies模块封装常用策略（CacheFirst、NetworkFirst、StaleWhileRevalidate等），workbox-precaching模块处理预缓存，workbox-routing模块管理路由匹配。2025年Workbox已成熟稳定，是最主流的PWA工具。}}

{{知识点22}}{{Workbox的运行时缓存通过registerRoute()方法配置路由规则。可基于URL模式、请求方法、请求目的地等条件匹配请求，对不同类型的资源应用不同缓存策略。例如对图片使用CacheFirst策略，对API请求使用NetworkFirst策略。}}

{{知识点23}}{{Workbox预缓存通过构建工具（如webpack插件workbox-webpack-plugin）自动生成预缓存清单（precache manifest）。清单包含资源URL和内容哈希，Service Worker安装时根据清单预缓存所有资源。内容哈希确保只有文件变更时才重新下载，实现增量更新。}}

{{知识点24}}{{缓存存储限制因浏览器而异。Chrome对每个源的缓存配额通常为可用磁盘空间的一定百分比（一般不超过源可用空间的50%）。可通过navigator.storage.estimate()获取quota和usage信息。当存储空间不足时，浏览器可能自动清除缓存数据。}}

{{知识点25}}{{IndexedDB是PWA中存储结构化数据的另一种选择，与Cache API互补。Cache API适合存储网络请求/响应对，IndexedDB适合存储应用数据、用户配置等结构化数据。两者都可在Service Worker中使用，共同构成PWA的完整离线存储方案。}}

{{知识点26}}{{Service Worker更新机制：浏览器在以下情况检查更新——导航到Service Worker作用域内的页面、定期检查（通常每24小时）、调用registration.update()手动触发。新版本下载后经历install→waiting→activate流程，可通过self.skipWaiting()跳过等待阶段立即激活。}}

{{知识点27}}{{clients.claim()方法使新激活的Service Worker立即控制所有打开的客户端页面，无需用户刷新。通常在activate事件处理器中调用。这对于确保所有页面立即使用新的缓存策略非常重要，但需注意可能导致页面状态不一致。}}

{{知识点28}}{{缓存失效策略需要根据资源类型选择：静态资源（CSS/JS/图片）使用内容哈希文件名配合长期缓存；API数据使用带过期时间的Network-First策略；用户生成内容使用Network-Only策略。避免缓存敏感数据如认证token和个人信息。}}

{{知识点29}}{{Service Worker调试工具：Chrome DevTools的Application面板提供Service Worker状态查看、缓存内容检查、离线模拟等功能。可使用self.clients.matchAll()查看受控客户端，chrome://serviceworker-internals/提供详细日志。console日志在Service Worker面板中查看。}}

{{知识点30}}{{PWA的manifest.json文件定义应用的元数据，包括name、short_name、start_url、display（推荐standalone）、icons（至少192x192和512x512）、theme_color和background_color。manifest配合Service Worker使应用可被"安装"到设备主屏幕。}}

{{知识点31}}{{缓存策略混合使用是生产环境的最佳实践。通常对应用Shell使用Cache-First预缓存，对静态资源使用Stale-While-Revalidate，对API请求使用Network-First，对第三方资源使用Cache-First运行时缓存。不同类型资源需要不同策略来平衡性能和新鲜度。}}

{{知识点32}}{{Service Worker的作用域（scope）决定了它能控制哪些页面的请求。默认作用域是Service Worker脚文件所在目录。通过注册时的scope参数或Service-Worker-Allowed响应头可以扩大作用域，但不能超出Worker脚本所在源。一个源可以有多个Service Worker，各自控制不同路径。}}

{{知识点33}}{{缓存预取（Runtime Caching）与预缓存（Precaching）的区别：预缓存在Service Worker安装时完成，资源确定且有限；运行时缓存在请求发生时按需进行，资源动态且可能无限。运行时缓存需要设置最大条目数或过期时间防止缓存无限增长。}}

{{知识点34}}{{Fetch API的Request和Response接口是PWA缓存的基础。Request对象包含url、method、headers等属性；Response对象包含status、headers、body等属性。两者都支持clone()方法。Service Worker可以通过修改Request或构造新的Response来实现高级缓存逻辑。}}

{{知识点35}}{{Service Worker中的事件类型包括：install（安装时触发，用于预缓存）、activate（激活时触发，用于清理旧缓存）、fetch（网络请求时触发，用于拦截和响应）、sync（后台同步触发）、push（推送消息触发）、message（主线程消息触发）、periodicsync（周期同步触发）。}}

{{知识点36}}{{离线回退页面设计是PWA用户体验的重要部分。当缓存和网络都不可用时，Service Worker应返回一个预缓存的离线回退页面（fallback.html），告知用户当前处于离线状态并提供基本功能。这比返回网络错误页面的体验好得多。}}

{{知识点37}}{{PWA缓存的安全注意事项：缓存API仅在安全上下文（HTTPS）下可用；不要缓存敏感用户数据；定期清理过期缓存防止存储泄露；使用Cache-Control头部配合Service Worker控制缓存行为；注意跨域资源的CORS设置影响缓存能力。}}

{{知识点38}}{{CacheStorage.match()方法可跨所有缓存查找匹配响应，无需指定缓存名称。而Cache.match()仅在单个缓存中查找。当使用多个命名缓存组织不同类型资源时，match()的跨缓存搜索能力简化了查找逻辑。两者都支持ignoreSearch选项忽略查询参数。}}

{{知识点39}}{{PWA在2025年的浏览器支持情况：所有主流浏览器（Chrome、Edge、Firefox、Safari）均支持Service Worker和Cache API。Safari对部分高级特性如Background Sync、Periodic Background Sync支持有限。应使用特性检测而非浏览器检测来适配。}}

{{知识点40}}{{Service Worker中的waitUntil()方法延长事件的生命周期，确保异步操作（如缓存写入）在Service Worker被终止前完成。传入一个Promise，浏览器会等待该Promise解决后才停止Service Worker。Chrome中Service Worker空闲30秒后可能被停止，waitUntil最长等待5分钟。}}
