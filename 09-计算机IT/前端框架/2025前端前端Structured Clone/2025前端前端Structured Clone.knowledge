# 2025前端前端Structured Clone
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}{{Structured Clone Algorithm（结构化克隆算法）是HTML规范中定义的一种用于深拷贝复杂JavaScript对象的序列化/反序列化算法。它能递归遍历输入对象，同时维护已访问引用的映射表，以安全处理循环引用。}}

{{知识点2}}{{structuredClone()是该算法的标准全局API，定义于HTML规范中。语法为structuredClone(value, options?)，返回value的深拷贝副本。可在Window和WorkerGlobalScope上使用。}}

{{知识点3}}{{结构化克隆算法的核心使用场景包括：1）调用structuredClone()进行显式深拷贝；2）通过postMessage()在Worker间传递数据；3）使用IndexedDB存储对象；4）其他需要跨上下文传递数据的Web API。}}

{{知识点4}}{{structuredClone()支持循环引用。算法通过维护已访问引用的映射表，在遇到循环引用时正确重建引用关系，而非无限递归。例如对象的itself属性指向自身时，克隆后的对象也会正确保持这一环形结构。}}

{{知识点5}}{{structuredClone()支持transfer选项，允许转移（而非复制）Transferable对象。转移后原对象将不可用（被detach），这是一种零拷贝的高效资源传递方式。用法：structuredClone(value, { transfer: [buffer] })。}}

{{知识点6}}{{结构化克隆算法不支持Function对象。尝试克隆函数会抛出DataCloneError异常。这是因为函数可能包含闭包、作用域链等不可序列化的运行时状态。}}

{{知识点7}}{{DOM节点不能被结构化克隆。尝试克隆DOM元素同样会抛出DataCloneError。DOM节点绑定到特定文档上下文，无法安全地跨上下文复制。}}

{{知识点8}}{{RegExp对象在克隆时lastIndex属性不会被保留。lastIndex是RegExp实例的可变状态属性，克隆后会重置为0。}}

{{知识点9}}{{结构化克隆不会保留属性描述符（property descriptors）、setter、getter等元数据特性。例如原始对象中通过属性描述符标记为只读的属性，在克隆副本中默认变为可读写。}}

{{知识点10}}{{结构化克隆不会遍历或复制原型链。克隆结果是纯粹的数据副本，不保留原始对象的原型继承关系，克隆后的对象的原型是Object.prototype。}}

{{知识点11}}{{类的私有成员（Class private elements）不会被结构化克隆复制。虽然内置类型的内部字段可能被保留，但用户定义的#private字段不可克隆。}}

{{知识点12}}{{支持的JavaScript基本类型包括：undefined、null、boolean、number、bigint、string。注意symbol类型不被支持。}}

{{知识点13}}{{支持的JavaScript复合类型包括：Array、Object（仅普通对象字面量）、Map、Set、Date、RegExp、Error类型、ArrayBuffer、TypedArray、DataView。}}

{{知识点14}}{{支持的Error类型包括：Error、EvalError、RangeError、ReferenceError、SyntaxError、TypeError、URIError。浏览器必须序列化name和message属性，并预期序列化stack、cause等其他有意义的属性。}}

{{知识点15}}{{AggregateError的结构化克隆支持已在部分浏览器中实现，但规范层面仍在推进中（参考whatwg/html#5749 PR）。}}

{{知识点16}}{{支持的Web/API类型包括Blob、File、FileList、ImageData、ImageBitmap、CryptoKey、DOMException等数十种平台对象。这些类型在Web IDL中通过[Serializable]扩展属性标记。}}

{{知识点17}}{{支持的Web/API类型还包括DOMMatrix、DOMMatrixReadOnly、DOMPoint、DOMPointReadOnly、DOMQuad、DOMRect、DOMRectReadOnly等几何类型。}}

{{知识点18}}{{支持的Web/API类型还包括媒体相关类型：AudioData、VideoFrame、EncodedAudioChunk、EncodedVideoChunk、RTCEncodedAudioFrame、RTCEncodedVideoFrame。}}

{{知识点19}}{{支持的Web/API类型还包括文件系统相关类型：FileSystemHandle、FileSystemFileHandle、FileSystemDirectoryHandle。}}

{{知识点20}}{{支持的Web/API类型还包括WebGPU相关类型：GPUCompilationInfo、GPUCompilationMessage、GPUPipelineError。以及WebTransportError、RTCCertificate、CropTarget、FencedFrameConfig等。}}

{{知识点21}}{{Transferable对象是可以跨Agent转移资源的对象。转移操作是不可逆且非幂等的——对象一旦被转移，原对象将不再可用。ArrayBuffer是最常见的Transferable对象。}}

{{知识点22}}{{可转移的对象列表包括：ArrayBuffer、AudioData、ImageBitmap、MediaSourceHandle、MediaStreamTrack、MessagePort、MIDIAccess、OffscreenCanvas、ReadableStream、RTCDataChannel、TransformStream、VideoFrame、WritableStream等。}}

{{知识点23}}{{TypedArray（如Uint8Array、Int32Array）本身是可序列化的但不是可转移的。要转移TypedArray的数据，需要转移其底层的ArrayBuffer（即typedArray.buffer）。}}

{{知识点24}}{{structuredClone()抛出DataCloneError（DOMException）的情况：当输入值的任何部分不可序列化时，如包含Function或DOM节点。}}

{{知识点25}}{{结构化克隆与JSON.parse(JSON.stringify())的区别：1）结构化克隆支持循环引用，JSON不支持；2）结构化克隆支持Map、Set、TypedArray、Blob等，JSON不支持；3）结构化克隆支持Date、RegExp的正确克隆；4）JSON会丢失undefined、函数、symbol等。}}

{{知识点26}}{{结构化克隆是浏览器内部实现的序列化/反序列化过程。序列化步骤（StructuredSerializeInternal）将对象转为与realm无关的中间表示，反序列化步骤（StructuredDeserialize）在目标realm中重建对象。}}

{{知识点27}}{{HTML规范定义了三种序列化变体：StructuredSerialize（标准序列化）、StructuredSerializeForStorage（用于持久化存储，可能更严格）、StructuredSerializeWithTransfer（带转移列表的序列化）。}}

{{知识点28}}{{自定义对象要支持结构化克隆，需要在Web IDL接口上使用[Serializable]扩展属性，并定义序列化步骤（serialization steps）和反序列化步骤（deserialization steps）。}}

{{知识点29}}{{序列化步骤负责将平台对象的数据写入中间Record，反序列化步骤负责从中间Record恢复数据到新创建的平台对象实例中。两组步骤通常是对应对称的。}}

{{知识点30}}{{在继承场景下，只有对象的主要接口（primary interface）参与序列化/反序列化。如果涉及继承，继承链中每个标记了[Serializable]的接口都需要定义独立的序列化/反序列化步骤。}}

{{知识点31}}{{structuredClone()的polyfill方案：可使用core-js库（github.com/zloirock/core-js）或@ungap/structured-clone包。但在支持原生API的现代浏览器中无需polyfill。}}

{{知识点32}}{{浏览器兼容性：structuredClone()从Chrome 98、Firefox 94、Safari 15.4、Node.js 17开始支持。已成为现代浏览器的标准能力，无需额外依赖。}}

{{知识点33}}{{结构化克隆在Web Worker通信中的应用：postMessage()内部使用结构化克隆算法在主线程和Worker线程间传递数据。结合transfer选项可以零拷贝传递大ArrayBuffer。}}

{{知识点34}}{{结构化克隆在IndexedDB中的应用：当通过IndexedDB的put()或add()存储对象时，对象会通过结构化克隆的"存储变体"（StructuredSerializeForStorage）进行序列化。}}

{{知识点35}}{{结构化克隆的性能特征：相比手动递归深拷贝，浏览器原生的结构化克隆经过高度优化，通常性能更好。但对于简单对象，浅拷贝（Object.assign、展开运算符）更快。}}

{{知识点36}}{{CommonJS polyfill（@ungap/structured-clone）与浏览器原生实现的差异：polyfill可能不支持所有可序列化类型（如Web API类型），且性能可能不如原生实现。}}

{{知识点37}}{{转移操作的实用场景：1）将大型ArrayBuffer从主线程传到Worker进行后台处理；2）将OffscreenCanvas转移到Worker进行离屏渲染；3）验证数据前克隆并转移buffer防止意外修改。}}

{{知识点38}}{{结构化克隆的memory参数：内部序列化算法维护一个memory映射表来追踪已序列化的对象引用，这对于正确处理循环引用和共享引用至关重要。这是实现深拷贝而非无限递归的关键机制。}}
