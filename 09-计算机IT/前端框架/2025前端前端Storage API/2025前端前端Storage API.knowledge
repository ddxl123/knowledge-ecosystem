# 2025前端前端Storage API
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}
Web Storage API 是浏览器提供的客户端存储方案，包含 localStorage 和 sessionStorage 两种机制。它们都通过 window 对象暴露，返回 Storage 实例，支持按 key-value 方式存储字符串数据。Web Storage 同步执行，适合存储少量数据。

{{知识点2}}
localStorage 按源（origin）分区存储，同一源下的所有页面共享同一份数据。数据持久化存储，关闭浏览器后重新打开仍然存在。存储容量一般为 5-10MB（因浏览器而异）。适合存储用户偏好、主题设置等需要长期保存的数据。

{{知识点3}}
sessionStorage 按源和浏览器标签页分区存储，每个标签页拥有独立的存储空间。关闭标签页后数据即被销毁。同一源的不同标签页之间 sessionStorage 互不共享。适合存储临时性数据，如表单草稿、页面状态等。

{{知识点4}}
Storage 接口提供了核心方法：length 属性返回存储项数量；key(n) 返回第 n 个键名；getItem(key) 获取值；setItem(key, value) 设置值；removeItem(key) 删除项；clear() 清空所有数据。所有值都以字符串形式存储，非字符串会被自动转换。

{{知识点5}}
Web Storage 的操作是同步的，会阻塞 JavaScript 执行。处理大量数据时可能影响页面响应性能。对于大数据量或性能敏感场景，应考虑使用异步的 IndexedDB 替代方案。

{{知识点6}}
StorageEvent 事件在存储区域发生变化时触发，可用于跨标签页通信。当一个标签页修改 localStorage 时，其他同源标签页会收到 storage 事件。事件包含 key、oldValue、newValue、url 等属性，但触发事件的当前标签页不会收到该事件。

{{知识点7}}
IndexedDB 是浏览器内置的低级客户端存储 API，用于存储大量结构化数据，包括文件和 Blob。它是一个事务型数据库系统，使用索引实现高性能查询。支持存储任何可通过结构化克隆算法处理的 JavaScript 对象。

{{知识点8}}
IndexedDB 操作全部异步执行，不会阻塞主线程。通过事件驱动模型（IDBRequest）与应用通信。支持在 Web Worker 中使用。适合存储大量数据（数百 MB 甚至更大），是 Web Storage 的大容量替代方案。

{{知识点9}}
IndexedDB 遵循同源策略，每个源拥有独立的数据库。需要指定数据库 schema，通过 indexedDB.open() 打开连接，然后在事务中进行数据的读写操作。数据库版本升级时通过 onupgradeneeded 事件处理 schema 变更。

{{知识点10}}
IDBFactory 是 IndexedDB 的入口对象，通过 window.indexedDB 或 WorkerGlobalScope.indexedDB 访问。核心方法包括 open() 打开数据库、deleteDatabase() 删除数据库、databases() 列出所有数据库。

{{知识点11}}
IDBDatabase 代表一个数据库连接。通过 version 属性获取数据库版本，createObjectStore() 创建对象存储，deleteObjectStore() 删除对象存储，transaction() 创建事务，close() 关闭连接。还提供 onversionchange 和 onclose 事件。

{{知识点12}}
IDBTransaction 代表一个事务，控制对对象存储的访问范围和权限。事务类型包括 readonly（只读）、readwrite（读写）和 versionchange（版本升级）。事务具有原子性，所有操作要么全部成功，要么全部回滚。

{{知识点13}}
IDBObjectStore 类似关系数据库中的表，存储一组可通过主键访问的数据。核心方法包括 add() 添加记录、put() 添加或更新记录、get() 获取记录、delete() 删除记录、clear() 清空、count() 计数、getAll() 获取全部。

{{知识点14}}
IDBIndex 为对象存储提供辅助索引，允许通过非主键字段查询数据。可以创建唯一索引或非唯一索引。使用索引比直接遍历对象存储更高效。通过 createIndex() 在 onupgradeneeded 中创建。

{{知识点15}}
IDBCursor 用于遍历对象存储和索引中的记录。支持前进（next）、后退（prev）等遍历方向。IDBCursorWithValue 扩展了游标功能，在遍历时同时返回当前值。配合 IDBKeyRange 可实现范围查询。

{{知识点16}}
IDBKeyRange 定义键的查询范围，支持多种模式：bound(下界, 上界) 闭区间、only(key) 单个键、lowerBound(下界) 大于等于、upperBound(上界) 小于等于。可以通过 open 参数控制是否包含边界。

{{知识点17}}
Cache API 提供持久化的 Request/Response 对象对存储机制，主要用于 Service Worker 的离线缓存策略。缓存中的数据长期存在，不会自动过期。一个源可以有多个命名的 Cache 对象，需要手动管理和清理。

{{知识点18}}
Cache API 的核心方法包括：match() 查找第一个匹配的响应、matchAll() 查找所有匹配的响应、add() 获取 URL 并缓存、addAll() 批量缓存多个 URL、put() 直接缓存请求-响应对、delete() 删除缓存项、keys() 获取所有缓存键。

{{知识点19}}
Cache API 仅在安全上下文（HTTPS）中可用，同时支持 window 和 Service Worker 作用域。不遵循 HTTP 缓存头，需自行实现缓存策略。键匹配算法依赖响应的 VARY 头。使用 StorageManager.estimate() 检查缓存配额。

{{知识点20}}
CacheStorage 是 Cache 对象的容器，通过 caches 全局属性访问。提供 open() 打开或创建命名缓存、has() 检查缓存是否存在、delete() 删除缓存、keys() 列出所有缓存名称、match() 跨所有缓存查找匹配。

{{知识点21}}
StorageManager 接口（Storage API）用于管理存储持久化权限和估算可用存储空间。通过 navigator.storage 访问。方法包括 estimate() 返回 usage 和 quota 信息、persist() 请求持久化存储、persisted() 查询是否已持久化、getDirectory() 获取 OPFS 目录。

{{知识点22}}
StorageManager.estimate() 返回的 quota 表示源可用的总存储空间（通常远大于 localStorage），usage 表示已使用量。不同浏览器和设备的配额不同，一般桌面浏览器允许数百 MB 到数 GB。可用于动态调整缓存策略。

{{知识点23}}
StorageManager.persist() 请求浏览器将站点数据标记为持久化，不会因存储压力被自动清除。需要用户授权或站点满足特定条件（如已安装为 PWA、用户频繁访问等）。persisted() 可查询是否已获得持久化权限。

{{知识点24}}
Storage Access API 解决第三方 iframe 中 cookie 和存储访问被浏览器限制的问题。通过 document.requestStorageAccess() 请求访问未分区的第三方 cookie，document.hasStorageAccess() 检查是否已有访问权限。保护用户隐私的同时允许合法的跨站存储访问。

{{知识点25}}
分区 Cookie（Partitioned Cookies）是浏览器隐私保护的替代方案。嵌入资源在每个顶级站点下获得独立的 Cookie 存储空间，无法跨站跟踪用户。与 Storage Access API 互补，不需要用户授权，但 Cookie 不会在站点间自动同步。

{{知识点26}}
Cookie 与 Web Storage 的区别：Cookie 有大小限制（约 4KB），会随 HTTP 请求自动发送到服务器；Web Storage 仅在客户端，容量更大（5-10MB），不会自动发送。Cookie 支持过期时间、域和路径控制、HttpOnly 和 Secure 标记等安全属性。

{{知识点27}}
浏览器存储配额与淘汰策略：localStorage 一般 5-10MB；sessionStorage 类似但生命周期更短；IndexedDB 和 Cache API 共享配额，通常为磁盘空间的 50% 左右。浏览器可能在存储压力下自动清除数据，持久化存储需显式请求。

{{知识点28}}
客户端存储安全注意事项：不要在 localStorage 中存储敏感信息（如 token），因为它易受 XSS 攻击；敏感数据应使用 HttpOnly Cookie。Web Storage 仅限同源访问，但仍可能被同一源下的恶意脚本读取。始终对存储的数据进行验证和清理。

{{知识点29}}
IndexedDB 常用封装库：idb（Jake Archibald）提供轻量 Promise 包装；Dexie.js 提供简洁的查询语法和索引支持；localForage 提供类似 localStorage 的简单 API，底层使用 IndexedDB；Lovefield 提供类 SQL 查询能力。

{{知识点30}}
OPFS（Origin Private File System）是 File System Access API 的一部分，通过 StorageManager.getDirectory() 访问。提供文件系统的读写能力，支持文件和目录操作。在 Worker 中使用同步 API（createSyncAccessHandle）性能更佳。适合大文件存储和流式处理。

{{知识点31}}
Web Storage 的同源策略：源由协议（http/https）、域名和端口号组成。file:// 协议的行为因浏览器而异。子域名之间不共享存储（a.example.com ≠ b.example.com）。同源的所有页面共享 localStorage，但 sessionStorage 按标签页隔离。

{{知识点32}}
Web Storage 容量检测方法：可以通过 try-catch 捕获 QuotaExceededError 来检测是否超出限制。逐步写入数据直到抛出异常来估算实际容量。StorageEvent 的 storageArea 属性标识受影响的存储类型。

{{知识点33}}
IndexedDB 事务的最佳实践：尽量使用 readonly 事务提高并发性；保持事务活跃（避免长时间等待），浏览器可能自动中止不活跃的事务；在 onupgradeneeded 中完成所有 schema 变更；使用事务的 oncomplete 和 onerror 处理结果。

{{知识点34}}
Service Worker 缓存策略与 Cache API 配合：Cache-First 策略优先读缓存，适合静态资源；Network-First 策略优先网络，适合动态内容；Stale-While-Revalidate 策略先返回缓存同时更新，适合频繁更新的资源。通过 Cache API + Fetch API 实现离线优先应用。

{{知识点35}}
Broadcast Channel API 可作为 Web Storage 事件的替代方案实现跨标签页通信。创建同名频道后，通过 postMessage 发送消息，所有同源标签页的频道实例都能接收。支持结构化数据传输，比 storage 事件更灵活。

{{知识点36}}
存储配额管理的最佳实践：使用 StorageManager.estimate() 监控使用情况；在接近配额时清理过期数据；对大数据使用 IndexedDB 而非 localStorage；使用 Cache API 存储网络资源；实现 LRU（最近最少使用）淘汰策略自动管理缓存大小。

{{知识点37}}
隐私模式下的存储行为：隐身/无痕模式中 localStorage 类似 sessionStorage，关闭窗口即清除。IndexedDB 和 Cache API 在隐私模式下仍可用但可能有更严格的配额限制。开发者应处理存储不可用或被清除的场景，使用 try-catch 包裹存储操作。

{{知识点38}}
structured clone algorithm 是 IndexedDB 存储数据的基础机制。支持复制大多数 JavaScript 类型：普通对象、Array、Date、RegExp、Map、Set、ArrayBuffer、Blob、File 等。不支持 Function、DOM 节点、属性描述符等。比 JSON 序列化更强大，可处理循环引用和二进制数据。
