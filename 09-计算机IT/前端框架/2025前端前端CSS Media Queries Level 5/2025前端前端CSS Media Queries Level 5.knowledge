# 2025前端前端CSS Media Queries Level 5
# 领域: 计算机IT
# 学科: 前端框架
{{知识点1}}{{CSS Media Queries Level 5 是 W3C 媒体查询规范的最新版本，扩展了 Level 4，新增了多个媒体特性（media features）和逻辑操作符，使开发者能更精确地根据用户偏好、设备能力和环境条件来应用样式。规范草案地址为 drafts.csswg.org/mediaqueries-5/。}}
{{知识点2}}{{@media 语法基础：媒体查询由可选的媒体类型（media type）和多个媒体特性表达式组成，可用逻辑操作符 and、not、or、only 组合。媒体类型包括 all、print、screen，大多数旧类型（如 tty、tv、handheld）已在 Level 4 中废弃。}}
{{知识点3}}{{prefers-color-scheme 是 Level 5 最重要的新增特性之一，用于检测用户偏好的颜色方案。取值为 light（浅色主题，默认）和 dark（深色主题）。广泛支持，自 2020 年起跨浏览器可用。}}
{{知识点4}}{{prefers-color-scheme 实战用法：通常先定义浅色主题作为默认，再用 @media (prefers-color-scheme: dark) 覆盖为深色变体。也可以反过来先定义深色再用 light 覆盖。配合 CSS color-scheme 属性和 meta name="color-scheme" 使用效果最佳。}}
{{知识点5}}{{prefers-color-scheme 在嵌入式元素中的应用：可用于 SVG（必须以 <img> 嵌入而非内联）和 iframe 中，根据父元素的 color-scheme 设置样式。支持跨域 SVG 和 iframe 元素。}}
{{知识点6}}{{prefers-reduced-motion 用于检测用户是否启用了减少动画的系统设置。取值为 no-preference（无偏好）和 reduce（减少运动）。自 2020 年起广泛支持。对有前庭运动障碍（vestibular motion disorders）的用户至关重要。}}
{{知识点7}}{{prefers-reduced-motion 最佳实践：当检测到 reduce 时，应将缩放、平移等触发动觉不适的动画替换为更温和的动画（如溶解效果），或完全移除动画。@media (prefers-reduced-motion: reduce) 可等价简写为 @media (prefers-reduced-motion)。}}
{{知识点8}}{{prefers-contrast 用于检测用户是否请求增加或减少相邻颜色之间的对比度。取值：no-preference（无偏好）、more（更高对比度）、less（更低对比度）、custom（自定义配色，匹配 forced-colors: active 用户的调色板）。自 2022 年起广泛支持。}}
{{知识点9}}{{scripting 是 Level 5 新增的媒体特性，用于检测 JavaScript 是否可用。取值：none（脚本完全不可用）、initial-only（仅在初始页面加载时启用）、enabled（脚本可用且活跃）。自 2023 年 12 月起基线可用。}}
{{知识点10}}{{scripting 注意事项：该检测基于浏览器用户设置，某些浏览器扩展通过不同技术实现脚本屏蔽，在此情况下 scripting 媒体特性可能无法按预期工作。}}
{{知识点11}}{{forced-colors 用于检测浏览器是否处于强制颜色模式（如 Windows 高对比度模式）。取值：none（未激活）和 active（已激活）。在 active 模式下，浏览器通过 CSS 系统颜色关键字提供调色板，并可能触发 prefers-color-scheme。}}
{{知识点12}}{{forced-colors 对 CSS 属性的影响：在强制颜色模式下，color、background-color、text-decoration-color、border-color、outline-color 等属性的作者级值被忽略，由浏览器从系统颜色中选择。box-shadow 和 text-shadow 强制为 none，非 url 型 background-image 也强制为 none。}}
{{知识点13}}{{forced-color-adjust 属性与 forced-colors 配合使用：设置 forced-color-adjust: none 可阻止强制颜色值应用，恢复作者样式；设置 preserve-parent-color 可在 color 不继承时保持父级颜色行为。}}
{{知识点14}}{{dynamic-range 用于测试用户代理和输出设备支持的亮度、对比度和色彩深度的组合能力。取值：standard（匹配任何视觉设备）和 high（高峰值亮度、高对比度率、色彩深度大于 24 位）。自 2022 年起广泛支持。}}
{{知识点15}}{{dynamic-range 注意：该特性测试的是设备是否具有高动态范围能力，而非该能力是否处于激活状态。某些 HDR 设备可能需要编程激活或用户手动开启。}}
{{知识点16}}{{video-dynamic-range 用于测试视频平面的动态范围能力。与 dynamic-range 类似，但专门针对视频平面。某些用户代理（包括许多电视）在两个独立平面（双平面）中分别渲染视频和图形，该特性用于检测视频平面的特性。取值同样为 standard 和 high。}}
{{知识点17}}{{display-mode 用于检测 Web 应用的显示模式。取值：browser（常规浏览器标签页）、fullscreen（全屏）、minimal-ui（最小 UI 的独立应用）、standalone（独立应用）、picture-in-picture（画中画浮动窗口）、window-controls-overlay（桌面应用风格窗口控件覆盖）。}}
{{知识点18}}{{display-mode 与 PWA 的关系：渐进式 Web 应用可通过 manifest 中的 display 成员设置显示模式。display-mode 媒体特性识别实际应用的模式（可能与请求的不同）。适用于顶层浏览上下文和所有子浏览上下文。}}
{{知识点19}}{{inverted-colors 用于检测用户代理或操作系统是否反转了所有颜色。取值：none（正常显示）和 inverted（已反转）。颜色反转可能产生阴影变高亮等副作用，可据此调整样式。目前仅 Safari 支持。}}
{{知识点20}}{{update 媒体特性用于测试输出设备修改已渲染内容外观的频率。取值：none（渲染后不可更新，如纸质打印）、slow（可动态更新但不够流畅，如电子书阅读器）、fast（可流畅更新，如电脑屏幕）。自 2023 年 9 月起广泛支持。}}
{{知识点21}}{{prefers-reduced-transparency 用于检测用户是否启用了减少透明度的系统设置。取值：no-preference 和 reduce。有助于提高某些用户的对比度和可读性。目前仍为实验性技术，支持有限。系统设置路径：Windows 在个性化 > 颜色 > 透明效果，macOS 在辅助功能 > 显示 > 减少透明度。}}
{{知识点22}}{{color-gamut 用于检测设备支持的近似色彩范围。取值：srgb（支持 sRGB 色域或更大）、p3（支持 Display P3 色域或更大）、rec2020（支持 ITU-R BT.2020 色域或更大）。P3 包含 sRGB，Rec2020 包含 P3。自 2023 年起广泛支持。}}
{{知识点23}}{{媒体查询的逻辑操作符：and 用于组合多个条件（所有为真才匹配），not 用于取反整个查询，or 用于任一条件为真即匹配（Level 4 新增），only 用于防止旧浏览器错误应用样式。逗号分隔等价于 or 操作。}}
{{知识点24}}{{Level 5 引入了更灵活的范围语法：可以用 width >= 30em 替代 min-width: 30em，用 width <= 50em 替代 max-width: 50em。支持 >、<、>=、<=、= 比较运算符。例如 @media (30em <= width <= 50em) 等价于 @media (min-width: 30em) and (max-width: 50em)。}}
{{知识点25}}{{not 操作符在 Level 5 中的行为增强：可以对单个媒体特性使用括号进行否定，如 not (hover) 而非否定整个查询。@media screen and not (hover) 表示否定 hover 但不否定 screen 媒体类型。}}
{{知识点26}}{{or 操作符在 Level 5 中的使用：不能与 and 和 not 在同一层级混用，需要通过括号分组或使用逗号替代。例如 @media ((color) and (hover)) or (monochrome) 是合法的。}}
{{知识点27}}{{用户代理客户端提示（User Agent Client Hints）：某些媒体查询有对应的 HTTP 头，可请求为特定媒体需求预优化的内容。包括 Sec-CH-Prefers-Color-Scheme 和 Sec-CH-Prefers-Reduced-Motion。这些是服务端响应式设计的重要工具。}}
{{知识点28}}{{hover 和 any-hover 媒体特性：hover 检测主要输入机制是否支持悬停，any-hover 检测是否有任何可用输入机制支持悬停。取值为 none 和 hover。对触摸设备与鼠标设备的区分非常有用。}}
{{知识点29}}{{pointer 和 any-pointer 媒体特性：pointer 检测主要输入机制的精确度，any-pointer 检测是否有任何输入机制是精确的。取值：none（无指针）、coarse（粗略，如触摸屏）、fine（精确，如鼠标）。}}
{{知识点30}}{{overflow-block 和 overflow-inline 媒体特性：overflow-block 检测输出设备如何处理沿块轴溢出视口的内容（none、scroll、paged），overflow-inline 检测沿内联轴溢出的内容是否可滚动（none、scroll）。}}
{{知识点31}}{{horizontal-viewport-segments 和 vertical-viewport-segments 是 Level 5 新增的媒体特性，用于检测设备是否有指定数量的水平或垂直视口分段。主要针对折叠屏设备等可分段视口的场景。}}
{{知识点32}}{{device-posture 媒体特性用于检测设备当前的物理姿态，即视口是处于平坦还是折叠状态。定义在 Device Posture API 中，配合折叠屏设备使用。}}
{{知识点33}}{{shape 媒体特性用于检测设备的形状，区分矩形和圆形显示器。取值为 rect（矩形）和 round（圆形），适用于智能手表等圆形屏幕设备。}}
{{知识点34}}{{display-mode: picture-in-picture 是 Level 5 新增的值，配合 Document Picture-in-Picture API 使用。可用于为画中画窗口提供专门的样式，如结合 prefers-color-scheme 提供浅色和深色配色方案。}}
{{知识点35}}{{媒体查询的安全与隐私考虑：媒体查询能提供设备能力信息，可能被滥用于构建设备指纹。浏览器可能会模糊返回值以防止精确识别。同时，未匹配的样式表虽仍会下载（优先级较低），但不会应用，直到查询结果变为 true。}}
{{知识点36}}{{媒体查询中的单位选择：建议使用 em 而非 px 作为媒体查询中的长度单位。当用户更改浏览器文本大小时，em 单位能更好地适应变化，提供更好的无障碍访问体验。}}
{{知识点37}}{{prefers-reduced-data 媒体特性用于检测用户是否请求消耗更少网络流量的 Web 内容。这是一个较新的特性，帮助用户在有限数据连接下获得优化体验。}}
{{知识点38}}{{scan 媒体特性用于检测显示器输出是逐行扫描（progressive）还是隔行扫描（interlaced）。取值为 progressive 和 interlaced，主要针对电视等设备。}}
{{知识点39}}{{Media Queries Level 5 规范状态：该规范由 W3C CSS 工作组维护，各特性处于不同成熟度阶段。prefers-color-scheme、prefers-reduced-motion、prefers-contrast、dynamic-range、forced-colors 等已广泛支持；inverted-colors、video-dynamic-range 支持有限；prefers-reduced-transparency 仍为实验性。}}
{{知识点40}}{{实战最佳实践：(1) 优先使用语义化的偏好特性而非设备尺寸断点；(2) 组合多个媒体查询创建精确条件；(3) 使用 CSS 自定义属性配合媒体查询实现主题切换；(4) 使用 range 语法简化宽度/高度断点写法；(5) 始终考虑无障碍访问需求。}}
