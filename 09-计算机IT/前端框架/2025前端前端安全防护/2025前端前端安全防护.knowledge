# 2025前端前端安全防护
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}{{XSS（跨站脚本攻击）概述}}
XSS（Cross-Site Scripting）是一种代码注入攻击，攻击者将恶意脚本注入到网页中，在其他用户的浏览器中执行。XSS可导致账户劫持、会话窃取、数据泄露、钓鱼攻击等严重后果。XSS分为三类：反射型（Reflected XSS）、存储型（Stored XSS）和DOM型（DOM-based XSS）。OWASP将其列为十大Web安全风险之一，是前端安全防护中最核心的威胁。

{{知识点2}}{{反射型XSS}}
反射型XSS是最常见的XSS类型。攻击者将恶意代码作为URL参数发送给受害者，服务器将参数值直接嵌入到HTML响应中返回。当用户点击恶意链接时，浏览器执行注入的脚本。防御措施包括：对所有用户输入进行输出编码（Output Encoding）、使用CSP策略限制脚本执行、验证和过滤输入参数。反射型XSS不会持久化存储，每次攻击都需要用户点击特制链接。

{{知识点3}}{{存储型XSS}}
存储型XSS是最危险的XSS类型。恶意脚本被永久存储在目标服务器上（如数据库、评论区、论坛帖子等），当其他用户访问包含恶意脚本的页面时自动执行。与反射型不同，存储型XSS不需要诱导用户点击特定链接。防御措施包括：对所有用户生成内容进行HTML实体编码、使用白名单过滤HTML标签和属性、部署CSP策略、定期审计存储内容。

{{知识点4}}{{DOM型XSS}}
DOM型XSS完全在客户端发生，不涉及服务器端。攻击通过操纵DOM环境在受害者浏览器中执行恶意代码。典型场景包括使用innerHTML、document.write、eval等不安全的DOM操作直接处理用户输入。URL片段（#后的部分）也可被利用。防御措施包括：使用textContent代替innerHTML、避免eval和document.write、对URL参数进行验证和编码、使用安全的DOM操作API。

{{知识点5}}{{CSRF（跨站请求伪造）概述}}
CSRF（Cross-Site Request Forgery）攻击利用用户已认证的会话，在用户不知情的情况下以用户身份执行未授权操作。攻击者通过诱导用户访问恶意页面，该页面自动向目标站点发送请求，浏览器会自动附带Cookie中的会话信息。CSRF可导致资金转移、密码修改、权限提升等操作。XSS可以绕过所有CSRF防护机制，因此必须同时防御XSS。

{{知识点6}}{{CSRF Token防护}}
CSRF Token是防御CSRF攻击的核心机制。服务器为每个用户会话生成唯一的随机Token，嵌入到表单或请求头中。服务器验证每个状态变更请求中的Token是否匹配。Token应为密码学安全的随机值，每个会话或每次请求生成一次。同步器令牌模式（Synchronizer Token Pattern）是最推荐的方法。对于无状态应用，可使用双重提交Cookie模式（Double Submit Cookie）。

{{知识点7}}{{SameSite Cookie属性}}
SameSite是Cookie的安全属性，可限制Cookie在跨站请求中的发送。三个值：Strict（完全不发送跨站Cookie）、Lax（GET导航时发送）、None（允许跨站发送，必须配合Secure属性）。Lax是现代浏览器的默认值，可有效防御大部分CSRF攻击。SameSite不能单独作为CSRF防护，应与CSRF Token配合使用。设置为None时必须同时设置Secure=true。

{{知识点8}}{{Content Security Policy（CSP）}}
CSP是一种额外的安全层，通过HTTP响应头Content-Security-Policy或meta标签定义，控制浏览器可以加载和执行哪些资源。CSP可防御XSS（限制内联脚本和外部脚本加载）、点击劫持（frame-ancestors指令）、数据注入攻击等。CSP不能替代安全编码实践，应作为纵深防御的补充。推荐使用CSP Level 3规范，支持nonce和hash机制。

{{知识点9}}{{CSP指令详解}}
关键CSP指令包括：default-src（默认资源策略）、script-src（脚本加载控制）、style-src（样式控制）、img-src（图片控制）、connect-src（XHR/fetch/WebSocket控制）、font-src（字体控制）、frame-ancestors（控制可嵌入的来源，替代X-Frame-Options）、form-action（表单提交目标控制）、base-uri（base标签限制）、object-src（插件控制）。推荐设置object-src 'none'和base-uri 'self'。

{{知识点10}}{{CSP nonce与hash机制}}
nonce（Number used once）是每个请求生成的随机值，通过script-src 'nonce-xxx'允许特定脚本执行。hash机制通过计算脚本内容的SHA-256等哈希值来白名单特定脚本。nonce适合动态内容，hash适合静态内容。使用nonce时必须确保每次请求生成不同值且不可预测。避免使用unsafe-inline，它会允许所有内联脚本，大大削弱CSP防护效果。

{{知识点11}}{{CSP Report-Only模式}}
Content-Security-Policy-Report-Only头允许部署CSP而不实际执行，仅在违规时发送报告。这是部署严格CSP前的重要过渡步骤。可同时使用CSP（执行）和CSP-Report-Only（监控）两套策略。通过report-to或report-uri指令配置违规报告的接收端点。报告包含违规URL、被阻止的资源URL、违反的指令等信息，帮助逐步完善CSP策略。

{{知识点12}}{{输出编码（Output Encoding）}}
输出编码是防御XSS的基础技术。根据输出上下文选择正确的编码方式：HTML上下文使用HTML实体编码（& < > " '）、HTML属性上下文使用属性编码、JavaScript上下文使用\xHH格式编码、URL上下文使用百分号编码、CSS上下文使用CSS编码。现代框架（React、Vue、Angular）默认提供自动编码，但需注意框架的安全逃逸口（如dangerouslySetInnerHTML）。

{{知识点13}}{{HTML净化（HTML Sanitization）}}
当需要允许用户输入HTML内容时（如富文本编辑器），必须使用HTML净化库。净化过程移除危险标签（script、iframe、object等）和危险属性（on*事件处理器、javascript:协议等），保留安全的HTML结构。推荐使用DOMPurify库，它基于浏览器原生DOM API实现，性能好且安全可靠。避免使用正则表达式进行HTML过滤，容易被绕过。

{{知识点14}}{{子资源完整性（SRI）}}
Subresource Integrity（SRI）通过在script或link标签上添加integrity属性，确保从CDN加载的资源未被篡改。浏览器计算资源的加密哈希值并与integrity属性声明的值比对，不匹配则阻止加载。格式：integrity="sha384-xxx"。可配合CSP的require-sri-for指令强制所有脚本使用SRI。SRI不能保护动态生成的内容，且一旦资源更新必须同步更新哈希值。

{{知识点15}}{{HTTPS与TLS}}
HTTPS是Web安全的基础，通过TLS加密传输数据，防止中间人攻击、数据窃听和篡改。现代浏览器已将HTTPS作为默认要求，HTTP页面会被标记为"不安全"。HSTS（HTTP Strict Transport Security）头强制浏览器只通过HTTPS访问站点，格式：Strict-Transport-Security: max-age=31536000; includeSubDomains; preload。应将站点加入HSTS预加载列表。

{{知识点16}}{{同源策略（Same-Origin Policy）}}
同源策略是浏览器的核心安全机制，限制不同源（协议+域名+端口）之间的资源访问。一个源的脚本不能读取另一个源的DOM、Cookie或发送XHR请求。CORS（Cross-Origin Resource Sharing）通过HTTP头机制允许服务器声明哪些外部源可以访问资源。正确配置CORS非常重要，避免使用Access-Control-Allow-Origin: *（通配符）暴露敏感API。

{{知识点17}}{{CORS安全配置}}
安全的CORS配置应遵循最小权限原则：仅允许必要的源访问、限制允许的HTTP方法、限制允许的请求头、避免在Access-Control-Allow-Credentials为true时使用通配符源。预检请求（OPTIONS）用于复杂跨域请求的安全验证。开发环境和生产环境应使用不同的CORS策略，生产环境绝不使用宽松配置。

{{知识点18}}{{点击劫持（Clickjacking）防御}}
点击劫持通过透明iframe覆盖在诱饵页面上，诱导用户点击隐藏的按钮或链接。防御措施包括：使用CSP的frame-ancestors指令限制可嵌入的来源（替代已废弃的X-Frame-Options）、frame-ancestors 'self'只允许同源嵌入、frame-ancestors 'none'完全禁止嵌入。X-Frame-Options的DENY和SAMEORIGIN仍被广泛支持但已被CSP取代。

{{知识点19}}{{前端依赖供应链安全}}
前端项目依赖大量第三方npm包，供应链攻击是重大安全威胁。攻击者通过劫持维护者账号、发布恶意更新等方式注入恶意代码。防御措施包括：使用lock文件锁定依赖版本、定期审计依赖（npm audit）、使用SRI验证CDN资源完整性、限制package.json中的版本范围、使用Socket.dev等工具检测恶意包、审查新依赖的维护者信誉和下载量。

{{知识点20}}{{安全的Cookie配置}}
Cookie安全属性配置：HttpOnly（禁止JavaScript访问，防御XSS窃取Cookie）、Secure（仅通过HTTPS传输）、SameSite（限制跨站发送）、设置合理的过期时间（避免永久Cookie）、限制Cookie的作用域（Path和Domain属性）。敏感会话Cookie必须设置HttpOnly和Secure。避免在Cookie中存储敏感数据，使用Session ID引用服务端存储的会话数据。

{{知识点21}}{{JSONP安全风险}}
JSONP（JSON with Padding）通过动态创建script标签实现跨域数据获取，绕过同源策略。JSONP存在严重安全风险：无法验证响应内容、易受XSS攻击、响应可被注入恶意代码。应使用CORS替代JSONP进行跨域请求。如果必须使用JSONP，必须验证回调函数名、限制允许的来源、对返回数据进行严格校验。现代Web API应完全避免JSONP。

{{知识点22}}{{前端数据存储安全}}
浏览器存储（localStorage、sessionStorage、IndexedDB）的数据可被同源的任何JavaScript访问，存在XSS泄露风险。敏感数据（如Token、密码、个人信息）不应存储在localStorage中。推荐方案：Access Token使用内存变量存储（页面刷新后失效）、Refresh Token使用HttpOnly Cookie存储、使用sessionStorage存储非敏感的临时数据。定期清理过期数据。

{{知识点23}}{{JWT（JSON Web Token）安全}}
JWT是常用的无状态认证方案。安全注意事项：不在JWT Payload中存储敏感信息（Base64可解码）、使用强密钥签名（RS256优于HS256）、设置合理的过期时间、实现Token刷新机制、防止JWT重放攻击（使用jti声明）、验证所有Header字段（防algorithm confusion攻击）。前端应将JWT存储在内存中而非localStorage，通过HttpOnly Cookie传输更安全。

{{知识点24}}{{前端输入验证}}
前端输入验证是安全的第一道防线但不能替代后端验证。验证策略包括：类型检查（数字、字符串、邮箱格式等）、长度限制、范围限制、白名单字符集、正则表达式模式匹配。使用HTML5原生验证属性（required、pattern、min、max）配合JavaScript自定义验证。前端验证的主要目的是用户体验和减少无效请求，安全验证必须在后端重复执行。

{{知识点25}}{{安全的innerHTML使用}}
innerHTML是XSS的常见入口。安全替代方案：使用textContent设置纯文本内容、使用createElement和appendChild创建DOM元素、使用DOMParser解析HTML字符串。如果必须使用innerHTML，必须先对用户输入进行HTML实体编码或使用DOMPurify净化。React的dangerouslySetInnerHTML、Vue的v-html指令都需要开发者手动确保内容安全。

{{知识点26}}{{eval与动态代码执行风险}}
eval()、Function构造函数、setTimeout/setInterval的字符串参数、new Function()等都会将字符串作为代码执行，是严重的安全隐患。攻击者可注入恶意代码通过这些函数执行。防御措施：完全避免eval、使用JSON.parse替代eval解析JSON、使用安全的模板引擎、CSP中禁止unsafe-eval。现代框架和构建工具已大大减少了使用eval的场景。

{{知识点27}}{{第三方脚本安全管理}}
第三方脚本（分析、广告、聊天插件等）是重大安全风险源。防护措施：使用SRI验证脚本完整性、通过CSP限制脚本来源、使用sandbox iframe隔离第三方内容、定期审查第三方脚本权限、使用Permissions API限制功能访问、考虑使用Web Worker隔离执行。对于高安全要求的场景，可使用代理服务器转发第三方脚本内容。

{{知识点28}}{{安全HTTP响应头}}
除CSP外的重要安全响应头：X-Content-Type-Options: nosniff（防止MIME类型嗅探）、X-XSS-Protection: 0（禁用浏览器内置XSS过滤器，避免引入新漏洞）、Referrer-Policy（控制Referer信息泄露）、Permissions-Policy（限制浏览器功能访问如摄像头、麦克风）、X-Download-Options: noopen（IE专用，防止直接执行下载文件）。

{{知识点29}}{{安全的表单设计}}
表单安全设计要点：使用HTTPS提交表单数据、对所有输入进行服务端验证、使用CSRF Token保护表单、设置autocomplete="off"用于敏感字段（如验证码）、使用隐藏字段时验证其值不被篡改、限制文件上传类型和大小、对密码字段使用适当的输入类型、实现防重复提交机制、使用tokenization替代直接传输信用卡等敏感信息。

{{知识点30}}{{Web存储API安全}}
Web Storage API（localStorage/sessionStorage）的安全限制：数据仅按源隔离（同源共享）、无过期机制（localStorage）、无HttpOnly保护（JavaScript可直接访问）、存储容量有限（约5-10MB）。安全实践：不存储敏感数据、实现应用层加密、定期清理、监控存储异常使用。IndexedDB支持更大的存储但同样面临JavaScript可访问的安全风险。

{{知识点31}}{{WebSocket安全}}
WebSocket安全防护：使用wss://（加密连接）、验证Origin头防止跨站WebSocket劫持、实施消息级别的认证和授权、限制连接速率防止DoS攻击、验证消息格式和内容、设置超时断开机制、使用CSP的connect-src指令限制WebSocket连接目标。WebSocket不自动附带Cookie，需要手动管理认证状态。

{{知识点32}}{{Fetch API与XHR安全}}
使用Fetch API和XMLHttpRequest的安全实践：验证响应Content-Type、不自动解析不可信响应为HTML、使用credentials选项控制Cookie发送策略、验证请求来源（检查Origin和Referer头）、实现请求超时和错误处理、避免将响应内容直接插入DOM。CORS预检请求提供了跨域请求的安全验证机制。

{{知识点33}}{{前端安全编码清单}}
前端安全编码核心清单：所有用户输入必须经过验证和编码、使用现代框架的自动转义功能、避免innerHTML和eval、配置严格的CSP策略、使用HTTPS和HSTS、实施CSRF防护、正确配置Cookie安全属性、使用SRI验证外部资源、定期审计依赖安全、实施安全HTTP响应头、最小权限原则、安全的错误处理（不泄露敏感信息）。

{{知识点34}}{{OWASP Top 10前端相关风险}}
OWASP Top 10中与前端直接相关的风险：A03:2021注入（XSS属于注入类）、A01:2021访问控制失效（前端权限控制）、A05:2021安全配置错误（CSP、安全头配置不当）、A08:2021软件和数据完整性故障（供应链攻击、SRI缺失）、A02:2021加密失败（HTTPS配置不当）。前端开发者应熟悉OWASP Top 10并在开发中主动防范这些风险。

{{知识点35}}{{前端安全测试方法}}
前端安全测试包括：使用OWASP ZAP或Burp Suite进行自动化扫描、手动测试XSS注入点、使用ESLint安全插件（eslint-plugin-security）进行静态分析、CSP评估工具检测策略缺陷、npm audit检查依赖漏洞、Lighthouse安全审计、浏览器DevTools的Security面板检查混合内容和证书问题。安全测试应集成到CI/CD流程中实现自动化。

{{知识点36}}{{2025年前端安全趋势}}
2025年前端安全关键趋势：AI驱动的安全威胁增加（如AI生成的钓鱼攻击）、供应链安全成为重点（npm生态持续受到攻击）、Trusted Types API普及（防御DOM XSS）、CSP Level 3成为标准实践、Passkey/WebAuthn替代传统密码认证、隐私法规驱动的前端隐私保护技术、WebAssembly安全审计需求增长、浏览器沙箱机制持续增强。前端安全正从被动防御转向主动安全架构设计。

{{知识点37}}{{Trusted Types API}}
Trusted Types是浏览器原生API，旨在防止DOM XSS攻击。它强制要求将危险的DOM操作（innerHTML、eval等）的输入必须是Trusted Types对象而非原始字符串。通过CSP的require-trusted-types-for 'script'指令启用。开发者必须创建TrustedHTML、TrustedScript等类型的值才能传递给危险API。Google已在其产品中大规模部署Trusted Types，显著降低了DOM XSS的发生率。

{{知识点38}}{{Passkey与WebAuthn前端集成}}
Passkey基于WebAuthn标准，提供无密码认证方案，消除密码泄露和钓鱼攻击风险。前端通过navigator.credentials.create()和navigator.credentials.get() API与认证器交互。Passkey使用公钥密码学，私钥安全存储在用户设备上。前端集成要点：处理浏览器兼容性、提供降级方案、安全存储认证会话、正确处理认证器响应。Passkey是2025年最重要的认证安全趋势之一。

{{知识点39}}{{前端错误处理与信息安全}}
安全的错误处理避免信息泄露：不向用户暴露详细的错误堆栈和内部路径、使用通用错误消息替代技术细节、实施全局错误捕获但不暴露敏感信息、区分开发环境和生产环境的错误详细程度、监控和记录错误但保护用户隐私、使用window.onerror和unhandledrejection事件捕获未处理异常。错误日志应脱敏处理后再发送到监控服务。

{{知识点40}}{{前端安全架构设计原则}}
前端安全架构核心原则：纵深防御（多层安全机制叠加）、最小权限（仅请求和使用必要的权限）、默认安全（安全默认配置）、失败安全（异常情况下的安全降级）、职责分离（前端验证+后端验证双重保障）、安全左移（在开发早期引入安全检查）。架构设计应考虑威胁建模、攻击面最小化、安全监控和应急响应机制。
