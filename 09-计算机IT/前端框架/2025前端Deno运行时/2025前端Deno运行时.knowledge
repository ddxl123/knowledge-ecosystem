# 2025前端Deno运行时
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}{{Deno是由Ryan Dahl（Node.js创始人）创建的开源JavaScript/TypeScript/WebAssembly运行时，基于V8引擎、Rust和Tokio构建。发音为/ˈdiːnoʊ/（dee-no），定位为"下一代JavaScript运行时"，旨在简化JavaScript开发并弥补Node.js的设计缺陷。}}

{{知识点2}}{{Deno默认安全——程序在Deno中运行时默认没有文件系统、网络或环境变量的访问权限。必须通过命令行标志（如--allow-read、--allow-net）或运行时权限提示显式授予，这与Node.js默认给予所有脚本完全系统访问形成鲜明对比。}}

{{知识点3}}{{Deno原生支持TypeScript，无需额外安装或配置。内置TypeScript编译器可直接运行.ts文件，支持JSX和TSX，还支持类型检查（deno check命令），运行时默认跳过类型检查以提高性能，可用--check标志启用。}}

{{知识点4}}{{Deno遵循Web标准API，尽可能在服务端实现标准Web API（如fetch、Request、Response、WebSocket等）。Deno积极参与TC39和WinterCG标准组织，推动Web标准发展，最大化浏览器与服务器之间的代码复用。}}

{{知识点5}}{{Deno内置完整工具链，包括代码检查器（deno lint）、代码格式化器（deno fmt，基于dprint）、测试运行器（deno test）、文档生成器（deno doc）、独立可执行文件编译器（deno compile）等，无需安装额外依赖。}}

{{知识点6}}{{Deno完全兼容Node.js和npm。通过npm:说明符可直接导入npm包（如import { Hono } from "npm:hono"），支持package.json自动识别，无需npm install即可使用npm生态中的库。}}

{{知识点7}}{{Deno使用ECMAScript模块（ESM）作为默认模块系统，要求本地导入必须包含完整文件扩展名。支持JSON模块导入（with { type: "json" }）、WebAssembly模块直接导入、以及data:URL导入。}}

{{知识点8}}{{Deno支持三种第三方模块来源：jsr:说明符（推荐的JSR注册表）、npm:说明符（npm包）、https:// URL（远程模块）。推荐使用JSR（jsr.io）作为第三方模块注册表，包括Deno标准库。}}

{{知识点9}}{{Deno的权限系统包括细粒度的权限控制：--allow-read/-R（文件读取）、--allow-write/-W（文件写入）、--allow-net/-N（网络访问）、--allow-env（环境变量）、--allow-run（子进程）等，可指定路径或主机进行精确控制。}}

{{知识点10}}{{Deno支持--deny-*标志进行权限拒绝，优先级高于--allow-*标志。例如--allow-net --deny-net=example.com允许网络访问但禁止访问特定域名。还支持--allow-all/-A标志授予所有权限（禁用安全沙箱）。}}

{{知识点11}}{{deno.json是Deno的项目配置文件（支持.json和.jsonc），可配置TypeScript编译器选项、linter规则、formatter设置、任务定义、导入映射、权限、锁定文件等。Deno会自动从当前目录或父目录检测此文件。}}

{{知识点12}}{{deno.json的imports字段是导入映射（Import Map），可将裸说明符映射到URL或文件路径。例如将"@std/assert"映射到"jsr:@std/assert@^1.0.0"，简化依赖管理。还支持目录映射（如"@/"映射到"./"）。}}

{{知识点13}}{{deno.json的tasks字段定义自定义命令，通过deno task执行，类似package.json的scripts字段。例如定义"start": "deno run --allow-net --watch dev.ts"，然后用deno task start运行。}}

{{知识点14}}{{Deno内置测试运行器，使用Deno.test()函数定义测试。支持同步和异步测试、测试步骤（t.step）、测试钩子（beforeAll/beforeEach/afterEach/afterAll）、测试过滤（--filter）、并行执行（--parallel）、快速失败（--fail-fast）等功能。}}

{{知识点15}}{{Deno测试支持三种内置报告器：pretty（默认，详细可读）、dot（简洁输出）、junit（JUnit XML格式，适用于CI/CD集成）。支持忽略测试（ignore）、聚焦测试（only）、以及条件测试（如仅在特定操作系统运行）。}}

{{知识点16}}{{Deno支持编译为独立可执行文件（deno compile），支持交叉编译到其他平台。例如deno compile --allow-net server.ts可生成无需Deno运行时的独立二进制文件，方便分发部署。}}

{{知识点17}}{{Deno使用锁定文件（deno.lock）确保依赖完整性，记录依赖的精确版本和完整性哈希。默认启用，可在deno.json中通过"lock": false禁用，或通过"lock": {"frozen": true}冻结依赖变更。}}

{{知识点18}}{{Deno支持CommonJS模块。.cjs扩展名的文件自动识别为CommonJS，也支持package.json中的"type": "commonjs"配置。还支持require(ESM)（要求ESM模块不包含顶层await），以及从ESM导入CommonJS模块。}}

{{知识点19}}{{Deno的nodeModulesDir配置控制node_modules目录行为："none"（不使用本地node_modules，使用全局缓存）、"auto"（自动创建和维护）、"manual"（用户手动维护）。默认在有package.json时使用"manual"模式。}}

{{知识点20}}{{Deno支持工作区（Workspaces），允许在单个仓库中管理多个相关包。工作区根目录的deno.json可定义成员包，共享依赖和配置，适合monorepo项目结构。}}

{{知识点21}}{{Deno Deploy是Deno的无服务器云平台，支持在云端运行JavaScript/TypeScript应用。新版本基于Deno 2.0，支持Next.js/Astro/SvelteKit等框架的一流支持、CDN缓存、环境变量管理、GitHub集成部署等。}}

{{知识点22}}{{Deno KV是Deno内置的键值存储API，提供分布式数据存储能力。无需额外配置即可使用，支持原子操作、最终一致性读取，可在本地和Deno Deploy上使用，适合存储会话、配置等数据。}}

{{知识点23}}{{Deno支持WebSocket、HTTP/2、HTTPS等高性能网络协议，开箱即用。提供Deno.serve() API用于快速创建HTTP服务器，支持自动响应体压缩，性能在基准测试中与Node.js相当甚至更优。}}

{{知识点24}}{{Deno支持Unstable特性，通过deno.json中的unstable字段或--unstable-*命令行标志启用。这些特性仍在开发中，不属于稳定API，如unstable-detect-cjs（自动检测CommonJS模块）等。}}

{{知识点25}}{{Deno内置LSP（Language Server Protocol）支持，可集成到VS Code等编辑器中。提供智能代码补全、类型检查、诊断信息、快速修复、代码导航等功能，提升开发体验。}}

{{知识点26}}{{Deno支持deno doc命令生成模块文档，支持JSDoc注释解析。还支持deno info命令检查ES模块及其所有依赖的信息，包括模块图、缓存位置等。}}

{{知识点27}}{{Deno的deno add命令可自动添加依赖到deno.json的imports字段。例如deno add jsr:@luca/cases会自动获取最新版本并配置导入映射。还有deno remove、deno outdated等依赖管理命令。}}

{{知识点28}}{{Deno支持deno bench基准测试工具，用于性能测试和基准比较。支持deno coverage生成测试覆盖率报告，帮助开发者了解测试覆盖情况。}}

{{知识点29}}{{Deno支持Jupyter Notebook集成（deno jupyter），可在Jupyter环境中使用JavaScript和TypeScript进行交互式编程和数据分析，拓展了Deno在数据科学领域的应用。}}

{{知识点30}}{{Deno支持import.meta.filename和import.meta.dirname替代Node.js中的__filename和__dirname。process全局对象可用但建议显式导入node:process，Buffer需从node:buffer导入，推荐使用Uint8Array替代。}}

{{知识点31}}{{Deno支持条件导出解析，满足的条件包括["deno", "node", "import", "default"]。可通过--conditions标志扩展条件列表，如--conditions development,react-server，用于React Server Components等场景。}}

{{知识点32}}{{Deno的权限审计功能通过DENO_AUDIT_PERMISSIONS环境变量启用，输出JSONL格式的审计日志，记录所有权限访问。结合DENO_TRACE_PERMISSIONS可添加堆栈跟踪信息，用于安全审计和调试。}}

{{知识点33}}{{Deno支持符号链接安全检查。读取或写入通过符号链接时，基于符号链接位置而非目标路径检查权限。防止通过符号链接进行权限提升，对敏感系统路径（/proc、/dev、/sys）有额外限制。}}

{{知识点34}}{{Fresh是Deno的官方Web框架，基于Preact构建，完全兼容Vite。采用Islands架构实现服务端渲染（SSR），只在需要的地方加载客户端JavaScript，最大限度减少运行时开销，适合构建高性能网站。}}

{{知识点35}}{{Deno支持data:URL模块导入，允许在不创建单独文件的情况下导入内容。格式为data:[<media type>][;base64],<data>，支持JavaScript和TypeScript模块，适用于测试、原型设计和动态生成模块。}}

{{知识点36}}{{Deno支持deno x命令运行npm或JSR包，类似npx的功能。例如deno run -A npm:create-next-app@latest等同于npx create-next-app@latest，提供跨注册表的包执行能力。}}

{{知识点37}}{{Deno 2.x版本实现了与Node.js的高度兼容，支持大多数Node.js API（通过node:说明符导入）、CommonJS模块、package.json、以及npm生态中的大量包。这使得从Node.js迁移到Deno变得非常平滑。}}

{{知识点38}}{{Deno支持deno create命令从模板快速创建新项目，以及deno init命令初始化新项目。还支持deno upgrade命令升级Deno到最新版本，保持开发环境更新。}}

{{知识点39}}{{Deno的性能基于V8引擎和Rust实现，使用Tokio异步运行时处理I/O操作。在网络性能方面，Deno与Node.js不相上下，某些场景下甚至更优，特别是在处理大量并发连接时。}}

{{知识点40}}{{Deno支持deno deploy命令行工具，可直接从CLI管理和发布项目到Deno Deploy云平台。支持即时回滚、日志查看、环境变量管理、GitHub自动部署等功能，简化部署流程。}}
