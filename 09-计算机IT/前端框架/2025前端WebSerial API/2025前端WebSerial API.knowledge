# 2025前端WebSerial API
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}
{{Web Serial API概述}}
Web Serial API 是一组允许网站与连接到用户计算机的串行设备进行通信的 Web API。它提供了连接到操作系统要求通过串口（而非USB或蓝牙HID）通信的设备的能力。典型应用场景包括3D打印机、Arduino等微控制器板的通信。该 API 由 WICG（Web Incubator Community Group）维护规范，规范地址为 https://wicg.github.io/serial/。

{{知识点2}}
{{核心接口：Serial}}
Serial 接口是 Web Serial API 的主要入口，提供了查找和连接串口的属性和方法。它包含两个核心实例方法：requestPort() 用于请求用户选择一个串口设备，getPorts() 用于获取当前源（origin）已有权限访问的已连接串口数组。Serial 接口还支持通过事件冒泡接收 SerialPort 的 connect 和 disconnect 事件。

{{知识点3}}
{{核心接口：SerialPort}}
SerialPort 接口提供了对主机设备上串口的访问能力。它没有自己的构造函数，只能通过 Serial 接口的方法获取实例。SerialPort 提供三个只读属性（connected、readable、writable）和六个实例方法（open、close、getInfo、setSignals、getSignals、forget），以及 connect 和 disconnect 两个事件。

{{知识点4}}
{{Navigator.serial 入口属性}}
navigator.serial 是一个只读属性，返回 Serial 对象，作为 Web Serial API 的入口点。每次获取该属性时返回的是同一个 Serial 实例。同样 WorkerNavigator.serial 也返回 Serial 对象，使得 Web Worker 中也能使用该 API。

{{知识点5}}
{{Serial.requestPort() 方法}}
requestPort() 方法返回一个 Promise，解析为用户选择的 SerialPort 实例。该方法必须通过用户激活（transient activation）调用，即用户必须与页面进行交互（如点击按钮）后才能调用。可传入 filters 参数，通过 usbVendorId、usbProductId、bluetoothServiceClassId 等过滤特定设备。若用户未选择端口，Promise 会以 NotFoundError 拒绝。

{{知识点6}}
{{Serial.getPorts() 方法}}
getPorts() 方法返回一个 Promise，解析为 SerialPort 对象数组，代表已连接到主机且当前源有权限访问的串口。该方法通常在页面加载时调用，用于恢复之前已授权的端口连接。若 Permissions-Policy 阻止了该功能，返回的 Promise 会以 SecurityError 拒绝。

{{知识点7}}
{{SerialPort.open() 方法}}
open() 方法返回一个 Promise，在串口打开时解析。baudRate 参数是必需的，指定串口通信的波特率。可选参数包括：bufferSize（读写缓冲区大小，默认255）、dataBits（数据位，7或8，默认8）、flowControl（流控制，"none"或"hardware"，默认"none"）、parity（奇偶校验，"none"/"even"/"odd"，默认"none"）、stopBits（停止位，1或2，默认1）。若端口已打开则抛出 InvalidStateError。

{{知识点8}}
{{SerialPort.close() 方法}}
close() 方法返回一个 Promise，在串口关闭时解析。调用 close() 前必须确保 readable 和 writable 的锁已释放（即已调用 releaseLock()）。在持续读取数据的循环中，需要先调用 reader.cancel() 来强制 reader.read() 解析为 { value: undefined, done: true }，然后在循环中调用 reader.releaseLock()，最后才能调用 port.close()。

{{知识点9}}
{{SerialPort.readable 属性}}
readable 是一个只读属性，返回一个 ReadableStream，用于从连接的设备接收数据。从该流读取的数据块是 Uint8Array 实例。只要端口处于打开状态且未遇到致命错误，该属性就非 null。典型的读取模式是使用 getReader() 获取读取器，在 while(true) 循环中调用 reader.read()。

{{知识点10}}
{{SerialPort.writable 属性}}
writable 是一个只读属性，返回一个 WritableStream，用于向连接的设备发送数据。写入该流的数据块必须是 ArrayBuffer、TypedArray 或 DataView 的实例。只要端口处于打开状态且未遇到致命错误，该属性就非 null。典型的写入模式是使用 getWriter() 获取写入器，调用 writer.write() 发送数据后调用 writer.releaseLock()。

{{知识点11}}
{{SerialPort.connected 属性}}
connected 是一个只读布尔属性，指示端口是否与设备逻辑连接。对于有线串口，物理连接时为 true；对于无线串口（如蓝牙 RFCOMM），设备在范围内且有活跃连接时为 true。当无线设备超出范围时，端口自动关闭但仍保持逻辑连接（connected 为 true），此时可尝试重新打开。若用户主动断开（如通过操作系统控制面板），connected 为 false，不应尝试重新打开。

{{知识点12}}
{{SerialPort.getInfo() 方法}}
getInfo() 方法返回一个包含设备标识信息的对象。返回对象包含三个可选属性：usbVendorId（USB 设备厂商 ID，无符号短整型）、usbProductId（USB 产品 ID，无符号短整型）、bluetoothServiceClassId（蓝牙服务类 ID，无符号长整型或字符串）。非 USB 或蓝牙设备对应的属性为 undefined。

{{知识点13}}
{{SerialPort.setSignals() 方法}}
setSignals() 方法在端口上设置控制信号，返回一个 Promise。可接受的选项包括：dataTerminalReady（DTR 信号，布尔值）、requestToSend（RTS 信号，布尔值）、break（中断信号，布尔值）。若端口未打开则抛出 InvalidStateError，若无法设置信号则抛出 NetworkError。

{{知识点14}}
{{SerialPort.getSignals() 方法}}
getSignals() 方法返回一个 Promise，解析为包含端口当前控制信号状态的对象。返回对象包含四个属性：clearToSend（CTS，指示对端是否可以发送数据）、dataCarrierDetect（DCD，载波检测信号）、dataSetReady（DSR，设备是否准备好收发数据）、ringIndicator（RI，振铃指示信号）。若端口未打开则抛出 InvalidStateError。

{{知识点15}}
{{SerialPort.forget() 方法}}
forget() 方法返回一个 Promise，在访问权限被撤销时解析。调用后会"忘记"该设备，重置之前设置的权限，使调用站点无法再与该端口通信。适用于教育应用等共享计算机场景，清理大量累积的用户权限以改善用户体验。

{{知识点16}}
{{connect 事件}}
connect 事件在端口与设备建立逻辑连接后触发。对于有线串口，发生在物理连接时；对于无线串口（如蓝牙 RFCOMM），发生在建立活跃连接时。该事件通过事件冒泡传播到 Serial 实例。可在 SerialPort 对象上监听（port.addEventListener("connect", ...)），也可在 Serial 对象上监听所有新连接的端口。

{{知识点17}}
{{disconnect 事件}}
disconnect 事件在之前逻辑连接的端口不再连接时触发。该事件通过事件冒泡传播到 Serial 实例。可在 SerialPort 对象上监听特定端口的断开，也可在 Serial 对象（navigator.serial）上监听所有端口的断开事件。event.target 指向断开的 SerialPort 对象。

{{知识点18}}
{{Permissions-Policy serial 指令}}
HTTP Permissions-Policy 头的 serial 指令控制当前文档是否允许使用 Web Serial API 与串口设备通信。默认允许列表为 self（即同源）。当策略阻止该功能时，Serial.requestPort() 和 Serial.getPorts() 会返回以 SecurityError 拒绝的 Promise。语法格式为：Permissions-Policy: serial=<allowlist>;

{{知识点19}}
{{安全上下文要求}}
Web Serial API 仅在安全上下文（Secure Context）中可用，即必须通过 HTTPS 访问。这是浏览器对敏感硬件 API 的基本安全要求，防止中间人攻击篡改与串口设备的通信数据。localhost 等特殊源也被视为安全上下文。

{{知识点20}}
{{用户激活（Transient Activation）要求}}
Serial.requestPort() 方法必须在用户激活（transient activation）期间调用。这意味着用户必须与页面进行手势交互（如点击按钮）后才能调用该方法。这是为了确保用户明确知情并同意网站访问其串口设备，防止网站在用户不知情的情况下弹出设备选择对话框。

{{知识点21}}
{{波特率（Baud Rate）配置}}
波特率是 open() 方法的必需参数，指定串口通信的数据传输速率，单位为比特每秒（bps）。常用波特率包括：9600（Arduino 默认）、115200（高速通信常见）、38400、19200 等。必须是一个正的非零值。波特率必须与连接的设备匹配，否则会导致通信乱码或失败。

{{知识点22}}
{{数据位、停止位与奇偶校验}}
串口通信帧格式由三个参数控制：dataBits（数据位）为每帧的数据位数，可选 7 或 8，默认 8；stopBits（停止位）为帧末尾的停止位数，可选 1 或 2，默认 1；parity（奇偶校验）可选 "none"（无校验）、"even"（偶校验）或 "odd"（奇校验），默认 "none"。最常见的配置是 8N1（8 数据位、无校验、1 停止位）。

{{知识点23}}
{{硬件流控制（Flow Control）}}
flowControl 参数控制串口的流控制方式，可选 "none"（无流控制，默认）或 "hardware"（硬件流控制）。硬件流控制使用 RTS/CTS 信号线来协调数据传输，防止数据溢出。当设备处理速度跟不上数据传输速率时，应启用硬件流控制。

{{知识点24}}
{{缓冲区大小（Buffer Size）配置}}
open() 方法的 bufferSize 参数指定读写缓冲区的大小，类型为无符号长整型，默认值为 255 字节。增大缓冲区可以减少读写操作的频率，提高大量数据传输时的效率，但会增加内存占用。需根据实际应用场景和设备特性进行调整。

{{知识点25}}
{{读取串口数据的典型模式}}
读取串口数据使用 ReadableStream 的典型模式：外层 while 循环检查 port.readable 是否为非 null（处理非致命错误），内层 while(true) 循环调用 reader.read() 持续读取数据。当 done 为 true 时表示读取被取消。finally 块中必须调用 reader.releaseLock() 释放锁。读取到的 value 是 Uint8Array 实例。

{{知识点26}}
{{写入串口数据的典型模式}}
写入串口数据使用 WritableStream：通过 port.writable.getWriter() 获取写入器，调用 writer.write() 发送数据。写入的数据必须是 ArrayBuffer、TypedArray 或 DataView 类型。写入字符串时需使用 TextEncoder 转换为 Uint8Array。写入完成后应调用 writer.releaseLock() 释放写入器的锁。

{{知识点27}}
{{TextEncoder 处理字符串数据}}
由于 WritableStream 要求写入的数据为二进制类型，发送字符串时需要使用 TextEncoder 将其转换为 Uint8Array。示例：const encoder = new TextEncoder(); await writer.write(encoder.encode("PING"));。接收数据时可使用 TextDecoder 将 Uint8Array 转换回字符串：const decoder = new TextDecoder(); const text = decoder.decode(value);

{{知识点28}}
{{TransformStream 高级数据处理}}
对于复杂的数据处理场景，可以使用 TransformStream 对串口数据进行流式转换。TransformStream 可以在读取和写入之间插入数据解析、编码转换、协议封装等处理逻辑。关闭使用 TransformStream 的串口比直接使用 reader/writer 更复杂，需要参考 Chrome 开发者文档中的专门指南。

{{知识点29}}
{{安全关闭串口的正确模式}}
安全关闭串口需要遵循特定步骤：1）设置 keepReading 标志为 false；2）调用 reader.cancel() 强制 reader.read() 解析；3）等待读取循环退出并释放锁；4）调用 port.close()。如果在读取循环中直接关闭端口，会因为 readable 流仍被锁定而失败。使用 async/await 模式可以更优雅地处理这个过程。

{{知识点30}}
{{requestPort 过滤器选项}}
requestPort() 的 filters 参数支持多种过滤方式：usbVendorId（USB 厂商 ID，无符号短整型）按厂商过滤；usbProductId（USB 产品 ID，无符号短整型）按产品过滤；bluetoothServiceClassId（蓝牙服务类 ID）按蓝牙服务过滤。还可以使用 allowedBluetoothServiceClassIds 参数允许特定的自定义蓝牙 RFCOMM 服务。不传 filters 则显示所有可用设备。

{{知识点31}}
{{蓝牙 RFCOMM 支持}}
Web Serial API 支持通过蓝牙 RFCOMM（Radio Frequency Communication）协议连接的串口设备。蓝牙串口使用 SPP（Serial Port Profile）进行标准化通信，但某些设备使用自定义 RFCOMM 服务。getInfo() 方法返回的 bluetoothServiceClassId 可用于识别蓝牙设备。requestPort() 的 allowedBluetoothServiceClassIds 参数可允许用户选择自定义服务的蓝牙设备。

{{知识点32}}
{{USB 设备厂商与产品 ID 过滤}}
通过 USB 实现的串口设备可以使用 usbVendorId 和 usbProductId 进行精确过滤。USB Implementors Forum（USB-IF）为每个厂商分配唯一的 Vendor ID，厂商为其产品分配 Product ID。例如 Arduino Uno 的 Vendor ID 为 0x2341，Product ID 为 0x0043 或 0x0001。使用过滤器可以限制设备选择对话框只显示特定设备。

{{知识点33}}
{{串口控制信号详解}}
串口控制信号是硬件级别的通信协调机制。setSignals() 可设置：DTR（Data Terminal Ready，数据终端就绪，告诉设备主机已准备好）、RTS（Request To Send，请求发送，用于硬件流控制）、Break（中断信号，强制发送持续低电平）。getSignals() 可读取：CTS（Clear To Send，允许发送）、DCD（Data Carrier Detect，载波检测）、DSR（Data Set Ready，数据设备就绪）、RI（Ring Indicator，振铃指示）。

{{知识点34}}
{{与 WebUSB 和 WebHID 的关系}}
Web Serial API 与 WebUSB API 和 WebHID API 共同构成了一组允许网站与外设通信的 API 集合。WebUSB 用于直接 USB 通信，WebHID 用于人机接口设备（如键盘、鼠标、游戏手柄），而 Web Serial API 用于需要操作系统串口驱动的设备。三者的选择取决于设备的通信协议和驱动方式。

{{知识点35}}
{{错误处理最佳实践}}
Web Serial API 中的常见错误包括：InvalidStateError（端口已打开或未打开时操作不当）、NetworkError（端口打开失败或信号设置失败）、SecurityError（权限策略阻止或用户拒绝授权）、NotFoundError（用户未选择端口）。建议使用 try-catch 包裹所有串口操作，并在外层循环中处理非致命错误（如临时断开），在内层循环中处理数据读取。

{{知识点36}}
{{Worker 支持与 WorkerNavigator.serial}}
Web Serial API 支持在 Web Worker 中使用，通过 WorkerNavigator.serial 属性访问 Serial 对象。这允许在后台线程中进行串口通信，避免阻塞主线程的 UI 渲染。对于需要持续读取大量串口数据或实时处理的场景，使用 Worker 可以显著改善用户体验。

{{知识点37}}
{{浏览器兼容性}}
截至 2025 年，Web Serial API 在 Chromium 内核浏览器（Chrome 89+、Edge 89+、Opera 75+）中获得支持。Safari 和 Firefox 尚未实现该 API。该 API 标记为实验性技术（Experimental），使用前应仔细检查浏览器兼容性表。在不支持的浏览器中，navigator.serial 为 undefined，可通过特性检测判断支持情况。

{{知识点38}}
{{特性检测方法}}
检测浏览器是否支持 Web Serial API 可以通过检查 navigator.serial 是否存在：if ("serial" in navigator) { /* 支持 */ } else { /* 不支持 */ }。这是一种可靠的特性检测方式，因为支持该 API 的浏览器会将 Serial 对象挂载到 navigator.serial 上。

{{知识点39}}
{{典型应用场景}}
Web Serial API 的典型应用场景包括：1）与 Arduino、ESP32 等微控制器通信，上传固件或读取传感器数据；2）控制 3D 打印机，发送 G-code 指令；3）与 GPS 模块、条码扫描器等外设通信；4）工业自动化中的 PLC 通信；5）教育领域中的 BBC micro:bit 编程；6）调试嵌入式设备的串口终端。

{{知识点40}}
{{权限模型与用户隐私}}
Web Serial API 采用严格的权限模型保护用户隐私：1）必须通过 HTTPS 安全上下文访问；2）requestPort() 需要用户激活（点击等手势）；3）用户必须在设备选择对话框中明确选择设备；4）权限按源（origin）隔离，不同网站的权限互不影响；5）可通过 forget() 方法主动撤销权限；6）Permissions-Policy 头可由服务端控制。这些措施确保网站无法在用户不知情的情况下访问串口设备。

{{知识点41}}
{{完整的连接-读写-关闭流程}}
使用 Web Serial API 的完整流程：1）检查 navigator.serial 是否存在；2）在用户点击事件中调用 navigator.serial.requestPort() 获取端口；3）调用 port.open({ baudRate: 9600 }) 打开端口；4）使用 port.readable 和 port.writable 进行数据读写；5）完成通信后安全关闭：取消读取器、释放锁、调用 port.close()。整个过程中需要监听 connect 和 disconnect 事件处理设备热插拔。

{{知识点42}}
{{数据类型与编码处理}}
Web Serial API 的数据传输使用二进制格式。发送端：字符串需通过 TextEncoder 编码为 Uint8Array，数字数据可直接使用 DataView 或 TypedArray。接收端：read() 返回的 value 始终是 Uint8Array，需通过 TextDecoder 解码为字符串，或通过 DataView 解析二进制协议数据。处理二进制协议时建议使用 DataView 的方法（getUint8、getUint16 等）进行精确的字节级操作。

{{知识点43}}
{{热插拔与自动重连策略}}
Web Serial API 支持设备热插拔检测。通过监听 navigator.serial 的 connect 和 disconnect 事件可以响应设备的连接和断开。对于无线设备（如蓝牙串口），当设备超出范围时端口自动关闭但 connected 仍为 true，此时可实现自动重连逻辑。对于有线设备，物理断开后需要用户重新授权。建议实现重连机制时添加指数退避和最大重试次数限制。

{{知识点44}}
{{WICG 规范状态}}
Web Serial API 的规范由 WICG（Web Incubator Community Group）维护，地址为 https://wicg.github.io/serial/。该 API 目前处于 W3C 社区组报告阶段，尚未成为正式的 W3C 推荐标准。规范定义了 Serial 接口、SerialPort 接口及其所有方法、属性和事件的详细行为。浏览器实现遵循该规范，但某些细节可能随版本更新而变化。

{{知识点45}}
{{与传统桌面串口工具的对比}}
Web Serial API 使得浏览器应用能够替代传统的桌面串口工具（如 PuTTY、CoolTerm、Arduino IDE 串口监视器）。优势包括：无需安装桌面软件、跨平台一致性、可构建复杂的 Web IDE 和仪表板。局限性包括：浏览器支持有限、无法访问某些底层串口功能、性能可能不如原生应用。对于简单调试场景，Web Serial API 已足够；对于复杂的工业应用，可能仍需原生方案。
