# 2025前端GSAP动画
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}
GSAP（GreenSock Animation Platform）是一个高性能的JavaScript动画库，其核心（Core）包含了创建快速、响应式动画所需的一切。额外功能如拖拽、滚动动画、变形等通过插件（Plugins）提供，核心保持精简，按需加载插件即可。

{{知识点2}}
gsap.to()是最常用的补间动画方法，用于定义目标值（终点状态）。GSAP会自动获取当前值作为起始值，无需手动定义。例如：gsap.to(".box", { rotation: 27, x: 100, duration: 1 })。可以动画化任何对象的任何属性，不限于CSS或DOM。

{{知识点3}}
gsap.from()定义起始值，动画从指定值过渡到当前状态，非常适合元素入场动画。例如：gsap.from(".class", { opacity: 0, y: 100, duration: 1 })。注意from()补间默认immediateRender为true，可能导致闪屏问题。

{{知识点4}}
gsap.fromTo()同时定义起始值和结束值，提供最精确的控制。当需要明确指定动画两端状态时使用，避免from()和to()的隐式值推断问题。

{{知识点5}}
duration（持续时间）属性控制动画时长，单位为秒，默认值为0.5。delay属性控制动画开始前的延迟时间，同样以秒为单位。合理设置这两个属性是控制动画节奏的基础。

{{知识点6}}
ease（缓动函数）控制动画的速率变化，赋予动画特定的质感和个性。默认缓动为"power1.out"。常用缓动包括"power1/2/3/4.inOut"、"elastic"、"back"、"bounce"、"none"等。可通过gsap.defaults()全局修改默认缓动。

{{知识点7}}
easeReverse属性（3.15.0新增）控制动画反向播放时的缓动曲线。设置为true时反向使用相同缓动，或指定不同缓动如"back.out"。GSAP会从播放头改变方向的精确位置重新计算缓动，适用于播放头在动画中途改变方向的情况。

{{知识点8}}
stagger（交错）属性为多个目标元素设置错开的起始时间。例如stagger: 0.1表示每个元素间隔0.1秒开始动画。还支持更复杂的配置对象，包括from、grid、axis、amount等高级参数，实现网格交错等效果。

{{知识点9}}
repeat属性控制动画重复次数，repeat: 1表示播放两次（初始+1次重复），repeat: -1为无限重复。repeatDelay设置重复间的等待时间。repeatRefresh: true使重复动画每次迭代重新记录起始/结束值，适用于动态值如random()。

{{知识点10}}
yoyo属性设为true时，每隔一次重复循环反向播放，产生来回摆动效果。配合repeat使用可创建持续的来回动画，如呼吸灯、浮动效果等。

{{知识点11}}
gsap.timeline()创建时间线，作为补间动画的容器，实现精确的序列编排。相比使用delay手动控制时序，时间线更灵活——调整任一动画时长后，后续动画自动调整。支持repeat、yoyo、delay等全局属性。

{{知识点12}}
时间线的位置参数（Position Parameter）是构建复杂动画时序的核心。支持绝对时间（如3）、相对时间（"+=1"/"-=1"）、标签名（"myLabel"）、标签相对（"myLabel+=2"）、以及"<"（最近添加动画的起点）和">"（最近添加动画的终点）等语法。

{{知识点13}}
时间线的defaults属性可为子动画设置继承的默认值，避免重复代码。例如：gsap.timeline({defaults: {duration: 1, ease: "power2.in"}})，其内所有to/from等方法自动继承这些默认参数。

{{知识点14}}
Tween的回调函数系统包括onStart、onUpdate、onComplete、onRepeat、onReverseComplete、onInterrupt等。每个回调都支持对应的Params数组传参。callbackScope可统一设置所有回调的this指向。

{{知识点15}}
ScrollTrigger是GSAP最强大的插件之一，用于创建基于滚动的动画。可将动画绑定到特定元素，仅在元素进入视口时播放。支持scrub（滚动擦洗）、pin（固定元素）、snap（吸附）等高级功能。通过gsap.registerPlugin(ScrollTrigger)注册。

{{知识点16}}
ScrollTrigger的scrub属性将动画直接链接到滚动条。scrub: true表示动画进度与滚动位置同步，scrub: 1表示动画需要1秒"追上"滚动位置，产生平滑延迟效果。配合snap可吸附到时间线标签或自定义进度值。

{{知识点17}}
ScrollTrigger的pin属性可在滚动期间固定元素位置，自动添加padding使下方内容正确衔接。start和end属性定义触发区域，支持关键字（top/center/bottom）、百分比、像素及相对值（"+=300px"）。anticipatePin可预防快速滚动时的固定延迟。

{{知识点18}}
ScrollTrigger的containerAnimation属性用于在垂直滚动控制的水平滚动区域内触发动画。容器动画必须使用线性缓动（ease: "none"），且基于containerAnimation的ScrollTrigger不支持pinning和snapping。

{{知识点19}}
ScrollTrigger丰富的回调系统包括onEnter、onLeave、onEnterBack、onLeaveBack、onToggle、onUpdate、onScrubComplete、onRefresh。配合ScrollTrigger.create()可创建独立触发器，利用回调执行任何自定义逻辑。

{{知识点20}}
gsap.context()（3.11.0新增）收集函数内创建的所有GSAP动画和ScrollTrigger，支持一次性revert()或kill()。可选传入Element/Ref作为scope，限定选择器文本的作用范围。在React中尤为重要，用于组件卸载时清理动画。

{{知识点21}}
useGSAP()是React专用Hook，封装了gsap.context()并自动处理动画清理。在React组件中使用GSAP时推荐使用此Hook，替代手动管理context和useEffect清理逻辑。

{{知识点22}}
Flip插件（3.9.0新增）实现FLIP动画技术（First, Last, Invert, Play），在DOM结构大幅变化后实现无缝过渡。三步流程：Flip.getState()记录当前状态→执行DOM变更→Flip.from(state, options)动画过渡。支持嵌套变换、flex/grid布局、交错动画。

{{知识点23}}
Flip.from()支持absolute选项在动画期间使用position: absolute，解决flex/grid布局问题。fade选项实现元素间的交叉淡入淡出。onEnter/onLeave回调处理元素进入/离开场景。Flip.fit()将一个元素重新定位到另一个元素上方。

{{知识点24}}
Draggable插件提供简单的方式使DOM元素可拖拽、旋转、抛掷。支持触摸事件，GPU加速，可通过bounds限制拖拽范围。集成InertiaPlugin实现基于动量的惯性运动，拖拽释放后元素平滑减速停止。

{{知识点25}}
MotionPathPlugin让元素沿路径运动。路径可以是SVG path元素、SVG路径数据字符串、或x/y坐标数组。支持autoRotate自动沿路径方向旋转，align将目标精确对齐到路径上，start/end定义路径上的起止进度。convertToPath()可将SVG基础图形转换为路径。

{{知识点26}}
TextPlugin逐字符（或逐词）替换DOM元素的文本内容。支持speed自动调整时长、delimiter分隔符设置、newClass/oldClass区分新旧文本样式、type: "diff"仅动画差异部分、rtl从右到左动画。动画可倒放恢复原文本。

{{知识点27}}
GSAP的lazy渲染优化：当补间首次渲染并读取起始值时，GSAP会延迟写入值到当前tick末尾，避免浏览器不喜欢的读/写/读/写布局抖动。默认开启（零时长补间除外），可通过lazy: false禁用。

{{知识点28}}
overwrite属性控制补间冲突处理。true立即杀死同目标的所有补间，"auto"仅杀死冲突属性的补间（默认），false不使用覆写策略。合理使用overwrite避免多个动画同时操作同一属性导致的冲突。

{{知识点29}}
immediateRender控制动画是否在创建时立即渲染。to()默认false（等待下一tick），from()/fromTo()默认true。设为true可强制立即渲染。在有delay的动画中需注意此属性的行为差异。

{{知识点30}}
GSAP的matchMedia()方法配合媒体查询为不同屏幕尺寸创建不同的动画设置。响应式动画开发的核心工具，可在断点处自动切换或清理动画配置。

{{知识点31}}
GSAP的全局方法：gsap.set()立即设置属性值（无动画），gsap.delayedCall()延迟执行函数，gsap.killTweensOf()杀死特定目标的所有补间，gsap.getById()通过ID查找补间实例，gsap.getProperty()获取元素当前属性值。

{{知识点32}}
GSAP支持链式调用，时间线方法如.to()、.from()、.fromTo()、.add()、.addLabel()等返回时间线自身。例如：tl.to(".box", {x:100}).to(".box", {y:50}).addLabel("end")。

{{知识点33}}
GSAP动画性能优化要点：使用transform属性（x/y/scale/rotation）代替top/left获得GPU加速；利用will-change提示浏览器；使用batch或containerAnimation减少ScrollTrigger实例；利用lazy渲染避免布局抖动。

{{知识点34}}
GSAP与CSS动画的对比优势：精确控制（play/pause/reverse/seek/progress）、时间线编排、跨浏览器一致性、丰富的缓动函数、强大的回调系统、滚动驱动动画、以及对任意JS对象属性的动画能力。

{{知识点35}}
GSAP的安装方式包括：CDN引入（<script>标签）、npm安装（npm install gsap）、以及通过构建工具（Vite/Webpack/Rollup）的ES模块导入。免费版包含核心和部分常用插件，Club GreenSock会员可获得更多高级插件如ScrollSmoother、MorphSVG等。

{{知识点36}}
ScrollSmoother是基于原生滚动技术的平滑滚动工具，与ScrollTrigger无缝集成。提供平滑滚动效果而无需scroll-jacking，可与CSS scroll snapping结合使用。通过scrollerProxy()方法可集成第三方平滑滚动库。

{{知识点37}}
InertiaPlugin为Draggable提供惯性/动量效果。用户抛掷元素后，运动基于动量平滑减速。可控制阻力、最大/最小持续时间等参数，创造自然的物理感交互体验。

{{知识点38}}
GSDevTools是GSAP的可视化调试工具，提供时间线GUI控制面板，可拖拽播放头、调整速度、检查动画。通过id属性标记的补间会在面板中显示，极大提升复杂动画的调试效率。

{{知识点39}}
GSAP的autoAlpha属性是opacity和visibility的智能组合。设为0时同时设置opacity:0和visibility:hidden，从0动画回来时在可见前先设置visibility:visible，解决了纯opacity动画中元素仍可交互的问题。

{{知识点40}}
GSAP的keyframes语法允许在一个补间中定义多个关键帧。例如：gsap.to(".box", {keyframes: [{x:100, duration:1}, {y:100, duration:1}, {rotation:360, duration:1}]})。每个关键帧可独立设置duration、ease等属性，简化多阶段动画代码。
