# 2025前端前端Streams API
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}{{Streams API是Web平台上用于处理流式数据的标准API，允许JavaScript逐块处理网络资源，而无需等待整个资源下载完成。数据被拆分为小的chunks逐步处理，实现更高效的内存使用和更快的首字节处理时间。}}

{{知识点2}}{{流（Stream）的核心思想是将资源拆分为小块（chunks），然后逐块处理。浏览器在接收媒体资源时已经在使用这种方式——视频会缓冲播放，图片会渐进加载。Streams API将这种能力开放给了JavaScript开发者。}}

{{知识点3}}{{Streams API包含三种主要流类型：ReadableStream（可读流）、WritableStream（可写流）和TransformStream（转换流）。这三种流类型可以组合使用，形成强大的数据处理管道。}}

{{知识点4}}{{ReadableStream表示一个可读的数据流，数据从底层源（underlying source）流出。可以通过fetch()的Response.body获取，也可以通过构造函数自定义创建。支持两种读取模式：默认读取器和BYOB读取器。}}

{{知识点5}}{{ReadableStream支持两种底层源类型：Push源和Pull源。Push源会主动推送数据（如视频流、WebSocket），Pull源需要显式请求数据（如fetch请求获取文件）。}}

{{知识点6}}{{ReadableStream的构造函数接受一个包含start、pull和cancel方法的underlyingSource对象。start方法在流创建时调用，pull方法在消费者需要更多数据时调用，cancel方法在流被取消时调用。}}

{{知识点7}}{{ReadableStream.locked属性返回布尔值，表示流是否被锁定到某个读取器。同一时间只能有一个读取器读取流，获取读取器后流即被锁定。}}

{{知识点8}}{{ReadableStream.getReader()方法创建一个读取器并锁定流。锁定期间无法获取其他读取器，直到当前读取器释放。读取器通过read()方法逐块读取数据，返回{done, value}格式的结果。}}

{{知识点9}}{{ReadableStream.read()返回的Promise有三种状态：有数据时返回{value: chunk, done: false}；流关闭时返回{value: undefined, done: true}；流出错时Promise被reject。}}

{{知识点10}}{{ReadableStream.cancel(reason)方法用于取消流，表示消费者不再对该流感兴趣。返回一个Promise，在流被取消后resolve。reason参数会传递给底层源。}}

{{知识点11}}{{ReadableStream.tee()方法将一个可读流分叉为两个相同的ReadableStream实例，两个分支可以被独立读取。适用于Service Worker中同时将响应流式传输到浏览器和缓存的场景。}}

{{知识点12}}{{ReadableStream.pipeTo(destination)方法将当前可读流通过管道连接到一个WritableStream。返回Promise，在管道完成时resolve，出错时reject。这是管道链的终点操作。}}

{{知识点13}}{{ReadableStream.pipeThrough(transformStream)方法将流通过TransformStream进行转换，提供链式调用的方式。可以对数据进行编码、解码、压缩等转换操作。返回转换后的ReadableStream。}}

{{知识点14}}{{ReadableStream实现了async iterable协议，支持使用for await...of语法异步迭代流中的chunks。这是消费流的最简洁方式。迭代期间流会被锁定，循环退出后锁定释放。}}

{{知识点15}}{{使用for await...of遍历ReadableStream时，默认退出循环会取消流。如需在退出循环后继续使用流，可传入{preventCancel: true}参数：stream.values({preventCancel: true})。}}

{{知识点16}}{{ReadableStream.from(iterable)是静态方法，可将可迭代对象（Array、Set、Map）或异步可迭代对象（async generator）转换为ReadableStream。是将迭代器转为流的便捷方式。}}

{{知识点17}}{{WritableStream提供向目的地（sink）写入流式数据的标准抽象，内置背压（backpressure）和队列管理机制。通过getWriter()获取写入器来写入数据。}}

{{知识点18}}{{WritableStream的构造函数接受一个underlyingSink对象，包含start、write、close和abort方法。write方法处理每个写入的chunk，close方法在流正常关闭时调用，abort方法在流异常终止时调用。}}

{{知识点19}}{{WritableStream.getWriter()方法返回WritableStreamDefaultWriter实例并锁定流。锁定期间只有一个写入器可以写入数据。写入器通过write(chunk)方法逐块写入数据。}}

{{知识点20}}{{WritableStream.abort(reason)方法中止流，立即将流置于错误状态，丢弃所有排队中的写入操作。WritableStream.close()方法正常关闭流，等待所有排队的写入完成后再关闭。}}

{{知识点21}}{{WritableStream.locked属性指示流是否被锁定到某个写入器。WritableStream是可传输对象（Transferable Object），可以在Web Workers中使用。}}

{{知识点22}}{{TransformStream表示一个转换流，数据在通过管道链时被转换。由一个WritableStream（输入端）和一个ReadableStream（输出端）组成，可传递给pipeThrough()方法。}}

{{知识点23}}{{TransformStream的构造函数接受transformer对象，包含start、transform和flush方法。transform方法接收输入chunk和controller，通过controller.enqueue()输出转换后的数据。}}

{{知识点24}}{{TransformStream.readable属性是转换流的可读端，TransformStream.writable属性是转换流的可写端。数据从writable端写入，经过转换后从readable端读出。TransformStream没有实例方法，只通过属性操作。}}

{{知识点25}}{{TransformStream是Transferable Object，可在Web Workers中使用。常见用途包括：编解码视频帧、压缩/解压数据、XML到JSON的格式转换、加密/解密数据流等。}}

{{知识点26}}{{背压（Backpressure）是流的核心概念，用于调节读写速度。当流的消费者处理速度跟不上生产者时，通过背压信号告知生产者减慢速度，防止内存溢出和数据积压。}}

{{知识点27}}{{背压通过high water mark（高水位线）和desired size（期望大小）来实现。公式：high water mark - 队列中chunks的总大小 = desired size。当desired size降至零或以下时，表示生产速度过快。}}

{{知识点28}}{{队列策略（Queuing Strategy）决定如何根据队列状态发出背压信号。内置两种策略：ByteLengthQueuingStrategy（基于字节长度）和CountQueuingStrategy（基于chunk数量）。}}

{{知识点29}}{{管道链（Pipe Chain）是将多个流串联的结构。起始端是原始源（original source），经过零或多个TransformStream转换，最终连接到终端接收器（ultimate sink）。pipeTo()连接终点，pipeThrough()连接中间转换。}}

{{知识点30}}{{ReadableStream支持字节流（byte stream）模式，使用BYOB（Bring Your Own Buffer）读取器。BYOB读取器允许将数据直接读入开发者提供的缓冲区，减少内存拷贝，提高性能。}}

{{知识点31}}{{ReadableByteStreamController是字节流的控制器，提供对字节流状态和内部队列的控制。ReadableStreamBYOBRequest表示BYOB读取器的拉取请求。}}

{{知识点32}}{{ReadableStreamDefaultController提供对普通可读流的控制能力，包括close()关闭流、enqueue(chunk)入队数据和error(e)发出错误信号。desiredSize属性返回消费者期望的chunk大小。}}

{{知识点33}}{{在Service Worker中使用Streams API可以实现流式响应处理。例如从服务器获取响应后同时流式传输到浏览器和Service Worker缓存，需要使用tee()分叉流。}}

{{知识点34}}{{使用async/await配合ReadableStream的标准模式：通过while(true)循环调用reader.read()，检查done标志处理结束条件，对value进行业务处理。比递归Promise链更清晰易读。}}

{{知识点35}}{{Streams API可以与AbortController配合使用来取消流式fetch操作。通过AbortSignal中断fetch请求，同时可通过break退出for await...of循环停止流的消费。}}

{{知识点36}}{{多个ReadableStream可以通过链式pipeTo()合并。使用reduce方法依次将多个响应流pipe到同一个writable流，除最后一个外都设置preventClose: true来保持writable流的开放。}}

{{知识点37}}{{Streams API的规范由WHATWG维护（streams.spec.whatwg.org），与W3C的其他Web API规范独立。所有主流浏览器（Chrome、Firefox、Safari、Edge）均已支持Streams API的核心功能。}}

{{知识点38}}{{ReadableStream和WritableStream的流锁定机制确保数据一致性。同一时间只能有一个reader或writer，需要先释放当前锁才能获取新的reader/writer。通过releaseLock()方法释放锁。}}

{{知识点39}}{{自定义ReadableStream的典型模式：在start回调中设置初始逻辑，在pull回调中按需生成数据并通过controller.enqueue()入队，在cancel回调中清理资源。start可以返回Promise以支持异步初始化。}}

{{知识点40}}{{Streams API在Node.js中也有对应的实现（node:stream模块），但Web Streams API和Node Streams API是不同的接口。Web Streams API更现代，Node.js 18+也支持Web Streams API的全局可用。}}
