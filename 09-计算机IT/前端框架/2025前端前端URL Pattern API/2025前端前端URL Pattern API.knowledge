# 2025前端前端URL Pattern API
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}{{URL Pattern API 概述：URL Pattern API 是一个 Web 标准 API，提供 URLPattern 接口用于将 URL 或 URL 的各部分与模式进行匹配。该 API 的模式语法基于 path-to-regexp 库，类似于 Ruby on Rails、Express.js 和 Next.js 中使用的语法。2025年9月起，该功能已成为 Baseline，在最新浏览器中广泛支持。}}

{{知识点2}}{{URLPattern 接口：URLPattern 是 URL Pattern API 的核心接口，表示一个可以匹配 URL 或 URL 各部分的模式。它支持捕获组（capturing groups）来提取匹配 URL 的部分。该接口在 Web Workers 中也可用，通过 [[Exposed]=(Window,Worker)] 标记。}}

{{知识点3}}{{URLPattern 构造函数 - 对象形式：new URLPattern(input, options) 接受一个 URLPatternInit 对象，其中可包含 protocol、username、password、hostname、port、pathname、search、hash 和 baseURL 属性。未指定的部分默认为通配符字符串 (*)，匹配任何对应的 URL 值。}}

{{知识点4}}{{URLPattern 构造函数 - 字符串形式：new URLPattern(url, baseURL, options) 接受一个 URL 字符串，可包含模式语法标记。如果是相对 URL，必须提供 baseURL 作为第二个参数。字符串中可能需要对特殊字符进行转义。}}

{{知识点5}}{{URLPattern 实例属性 - 只读属性：URLPattern 实例包含 protocol、username、password、hostname、port、pathname、search、hash 八个只读字符串属性，分别包含用于匹配 URL 各部分的模式。hasRegExpGroups 是一个布尔值属性，指示是否包含正则表达式捕获组。}}

{{知识点6}}{{URLPattern.test() 方法：test() 方法返回布尔值，判断给定 URL 是否匹配模式。支持三种参数形式：test(input) 传入对象、test(url) 传入绝对 URL 字符串、test(url, baseURL) 传入相对 URL 和基础 URL。所有组件都匹配时返回 true，否则返回 false。}}

{{知识点7}}{{URLPattern.exec() 方法：exec() 方法返回匹配结果对象或 null。返回对象包含 inputs 数组（传入的参数）以及 protocol、username、password、hostname、port、pathname、search、hash 八个属性，每个属性包含 input（匹配的输入）和 groups（捕获组结果）。}}

{{知识点8}}{{exec() 返回结果结构：URLPatternResult 对象中，每个 URL 部分对应一个 URLPatternComponentResult，包含 input 字符串和 groups 记录。groups 中的属性以数字索引（0, 1...）表示未命名组，以名称表示命名组。}}

{{知识点9}}{{模式语法基础 - 固定文本：模式可以包含固定文本，这些文本会被精确匹配。例如 new URLPattern({ pathname: "/books" }) 只匹配路径恰好为 /books 的 URL。}}

{{知识点10}}{{模式语法 - 命名捕获组：命名组以冒号前缀定义，如 :id。在 /books/:id 模式中，id 是组名，匹配 /books/123 时 groups 结果为 { id: '123' }。命名组在 pathname 中默认匹配除 / 之外的所有字符。}}

{{知识点11}}{{模式语法 - 未命名捕获组：未命名组使用正则表达式但不带冒号前缀，如 /books/(\\d+)。匹配结果以数字索引存储，groups 为 { '0': '123' }。}}

{{知识点12}}{{模式语法 - 段通配符：默认情况下，pathname 中的组匹配除 / 外的所有字符，hostname 中的组匹配除 . 外的所有字符，其他部分匹配所有字符。段通配符是非贪婪的，匹配最短可能字符串。}}

{{知识点13}}{{模式语法 - 正则匹配器：可以在组中指定正则表达式来约束匹配规则，语法为 :name(regex)。例如 :id(\\d+) 只匹配数字。正则表达式写在括号中，括号本身不是正则的一部分。}}

{{知识点14}}{{模式语法 - 通配符 *：星号 (*) 是通配符，匹配任意字符。例如 /posts/* 匹配 /posts/ 下的任何路径。在对象形式构造中，未指定的属性默认为通配符。}}

{{知识点15}}{{模式语法 - 组修饰符 ?：问号 (?) 修饰符使组变为可选。例如 /books/:id? 同时匹配 /books/123 和 /books，但不匹配 /books/ 或 /books/123/456。}}

{{知识点16}}{{模式语法 - 组修饰符 +：加号 (+) 修饰符使组重复一次或多次。例如 /books/:id+ 匹配 /books/123 和 /books/123/456/789，但不匹配 /books。}}

{{知识点17}}{{模式语法 - 组修饰符 *：星号修饰符使组重复零次或多次。例如 /books/:id* 匹配 /books、/books/123 和 /books/123/456，但不匹配 /books/。}}

{{知识点18}}{{模式语法 - 组分隔符：花括号 ({}) 包围的内容是组分隔符，不是捕获组但可以应用修饰符。例如 /book{s}? 匹配 /book 和 /books。组分隔符不能嵌套，但可以包含捕获组、正则、通配符或固定文本。}}

{{知识点19}}{{非捕获组使部分可选：/books{/old}? 使 /old 部分变为可选。组分隔符中还可以包含捕获组，如 /blog/:id(\\d+){-:title}? 可匹配 /blog/123-my-blog 和 /blog/123。}}

{{知识点20}}{{pathname 中的自动组前缀：在 pathname 模式中，如果组定义前有斜杠 (/)，会自动添加斜杠前缀。这对带修饰符的重复组很有用，使得 /books/:id* 能正确匹配 /books/123/456。}}

{{知识点21}}{{BaseURL 继承规则：URL 中比已定义最具体部分更不具体的部分可以从 baseURL 继承。直觉上，如果指定了 pathname，则左侧的 protocol、hostname 和 port 可以继承，右侧的 search 和 hash 不能继承。username 和 password 永远不会从 baseURL 继承。}}

{{知识点22}}{{hostname 影响默认端口：如果在 url 或 baseURL 中指定了 hostname 但未指定 port，端口会被隐式设置为空字符串 (""), 匹配默认端口 443。如果未指定 hostname，端口默认为通配符 (*)。}}

{{知识点23}}{{URLPattern 构造函数特殊字符转义：在字符串形式构造中，? 同时表示模式中的可选标记和 URL 的查询部分，: 同时表示命名组开始和 username/password 分隔符。需要使用 \\ 进行转义，或改用对象形式构造来避免歧义。}}

{{知识点24}}{{ignoreCase 选项：URLPatternOptions 对象支持 ignoreCase 布尔属性，默认为 false（区分大小写）。设置为 true 时启用不区分大小写的匹配。}}

{{知识点25}}{{相对 URL 匹配：test() 和 exec() 都支持相对 URL 匹配。传入相对 URL 字符串时必须提供 baseURL，两者组合解析为绝对 URL 后进行匹配。如果没有提供 baseURL，返回 false 或 null。}}

{{知识点26}}{{URL 对象作为输入：test() 和 exec() 都接受结构化对象作为输入，对象属性可以是 protocol、hostname、pathname 等 URL 各部分。对象中也可以包含 baseURL 属性来补充缺失部分。}}

{{知识点27}}{{正则表达式中的锚点：^（行首锚点）和 $（行尾锚点）在 URLPattern 中是冗余的，因为所有 URL 部分都隐式以 ^ 开头和 $ 结尾。}}

{{知识点28}}{{前瞻和后行断言：支持正则表达式的 (?=...)、(?!...)、(?<=...)、(?<!...) 断言。但需注意前瞻断言不消耗字符，因此 /a(?=b) 不会匹配 /ab，需要改为 /a(?=b).* 来消耗后续字符。}}

{{知识点29}}{{正则表达式限制：某些正则模式在 URLPattern 中行为与标准 RegExp 不同。例如括号在字符类范围内需要转义，即使在普通 RegExp 中不需要。new URLPattern({ pathname: "([()])" }) 会抛出错误，需要写成 ([\\(\\)])。}}

{{知识点30}}{{pathname 正则匹配的注意事项：pathname 始终以 / 开头，在正则匹配中不能省略 /(b.*) 不匹配，需要写成 (/b.*)。}}

{{知识点31}}{{与 path-to-regexp 的关系：URLPattern 的模式语法基于 path-to-regexp 库，该库被 Express.js、Next.js 等框架广泛使用。这意味着熟悉这些框架路由语法的开发者可以快速上手 URLPattern。}}

{{知识点32}}{{polyfill 支持：GitHub 上有 urlpattern-polyfill 项目为不支持 URLPattern 的浏览器提供 polyfill，使得开发者可以在所有环境中使用该 API。}}

{{知识点33}}{{Service Worker 中的使用：URL Pattern API 特别适用于 Service Worker 中的路由拦截。可以在 fetch 事件中使用 URLPattern 来匹配请求 URL，实现精细的缓存策略和请求处理逻辑。}}

{{知识点34}}{{hasRegExpGroups 属性的应用：hasRegExpGroups 属性指示模式中是否包含正则表达式捕获组。某些 API（如 Service Worker 路由）可能禁止使用正则表达式组，该属性可用于检测。}}

{{知识点35}}{{URLPattern 与 URL API 的区别：URL API 用于解析和构造 URL 字符串，而 URLPattern API 用于匹配和提取 URL 的各部分。URLPattern 不修改 URL，只做模式匹配和信息提取。}}

{{知识点36}}{{多组件匹配：URLPattern 可以同时匹配 URL 的多个组件。例如可以同时指定 protocol、hostname 和 pathname 的模式，只有所有组件都匹配时整体才匹配。}}

{{知识点37}}{{通配符子域名匹配：使用 * 匹配子域名，如 *.example.com 匹配 store.example.com 但不匹配 example.com 本身。结合 :subdomain 命名组可以提取子域名值。}}

{{知识点38}}{{协议匹配技巧：使用 http{s}? 模式可以同时匹配 http 和 https 协议。花括号使 s 可选，问号修饰符使其变为零次或一次。}}

{{知识点39}}{{端口匹配：可以使用 :port(80|443) 来限制匹配特定端口。不指定端口时，如果指定了 hostname，默认匹配空字符串（即默认端口）。}}

{{知识点40}}{{实际应用场景：URL Pattern API 的典型应用场景包括：Service Worker 路由拦截、客户端路由框架中的路径匹配、API 请求过滤、URL 验证和规范化、以及 Web 应用中的权限控制。}}

{{知识点41}}{{浏览器兼容性：截至2025年9月，URL Pattern API 已成为 Baseline 特性，在 Chrome、Edge、Firefox、Safari 最新版本中均获得支持。这是该 API 走向成熟的重要标志。}}

{{知识点42}}{{URLPatternInit 字典：URLPatternInit 是构造 URLPattern 时使用的字典类型，包含 protocol、username、password、hostname、port、pathname、search、hash 和 baseURL 九个可选的 USVString 属性。}}

{{知识点43}}{{URLPatternResult 结构：exec() 返回的 URLPatternResult 包含 inputs 序列（记录传入参数）和八个 URLPatternComponentResult 属性（对应 URL 各部分），每个 ComponentResult 有 input 和 groups 两个字段。}}

{{知识点44}}{{错误处理：URLPattern 构造函数在输入无效或语法不正确时抛出 TypeError。当相对 URL 没有提供 baseURL、或在使用 input 对象时提供了 baseURL 参数，也会抛出 TypeError。}}

{{知识点45}}{{默认 URLPattern 实例：new URLPattern() 或 new URLPattern({}) 创建的实例，所有部分都默认为通配符 *，hasRegExpGroups 为 false，可匹配任何 URL。}}
