# 2025前端Credential Management
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}{{Credential Management API 是什么：Credential Management API 是一个 Web 标准 API，允许网站创建、存储和检索用户凭据（credentials）。凭据是使系统能够做出身份验证决策的项目，例如决定是否将用户登录到账户。它本质上是用户向网站展示的、证明其身份的一条证据。}}

{{知识点2}}{{核心接口 CredentialsContainer：CredentialsContainer 是 Credential Management API 的核心接口，通过 navigator.credentials 属性访问。它暴露了三个主要方法：create() 创建新凭据、store() 本地存储新凭据、get() 检索凭据用于登录用户。该接口自2019年9月起在各浏览器中广泛可用。}}

{{知识点3}}{{navigator.credentials 访问方式：navigator.credentials 是 Navigator 接口的扩展属性，返回 CredentialsContainer 实例。这是前端开发者与 Credential Management API 交互的唯一入口点，所有凭据操作都通过这个属性发起。}}

{{知识点4}}{{凭据类型总览：API 支持四种凭据类型，均为 Credential 基类的子类：PasswordCredential（密码凭据）、IdentityCredential/FederatedCredential（联合身份凭据）、OTPCredential（一次性密码凭据）、PublicKeyCredential（公钥/WebAuthn凭据）。}}

{{知识点5}}{{PasswordCredential 接口：PasswordCredential 表示用户名/密码对的凭据信息。它继承自 Credential，提供只读属性 iconURL（图标URL）、name（显示名称）和 password（密码）。注意：该接口受限于顶层上下文，不能在 iframe 中使用。}}

{{知识点6}}{{PasswordCredential 构造函数：通过 new PasswordCredential({ id, password, name, iconURL }) 创建实例。创建后可通过 navigator.credentials.store(cred) 存储到浏览器凭据管理器中。在支持的浏览器中，PasswordCredential 实例可直接传递给 fetch() 的 credential 成员。}}

{{知识点7}}{{PasswordCredential 使用示例：const cred = new PasswordCredential({ id, password, name, iconURL }); navigator.credentials.store(cred).then(() => { /* 存储成功 */ }); 这种方式可以在用户注册或登录成功后自动保存凭据到浏览器。}}

{{知识点8}}{{Credential 基类：Credential 是所有凭据类型的基类接口，提供关于实体的信息作为信任决策的先决条件。它定义了 id（凭据标识符）和 type（凭据类型）两个基本属性，所有子类都继承这些属性。}}

{{知识点9}}{{CredentialsContainer.create() 方法：create() 返回一个 Promise，解析为基于提供的选项创建的新 Credential 实例，如果无法创建凭据则返回 null。在 WebAuthn 场景中，传入 publicKey 选项时会通过认证器创建新的公私钥对。}}

{{知识点10}}{{CredentialsContainer.get() 方法：get() 返回一个 Promise，解析为与提供的参数匹配的 Credential 实例。它是检索已存储凭据的核心方法，支持不同类型的凭据查询选项，如 password、federated、publicKey、otp、identity 等。}}

{{知识点11}}{{CredentialsContainer.store() 方法：store() 存储一组用户凭据，接收一个 Credential 实例作为参数，在 Promise 中返回该实例。通常在用户成功注册或登录后调用，将凭据保存到浏览器的密码管理器中。}}

{{知识点12}}{{CredentialsContainer.preventSilentAccess() 方法：preventSilentAccess() 设置一个标志，指定是否允许未来访问当前源时自动登录，返回一个空 Promise。通常在用户登出网站后调用，确保下次访问时不会自动登录。早期版本的规范称此方法为 requireUserMediation()。}}

{{知识点13}}{{Web Authentication API（WebAuthn）概述：WebAuthn 使用非对称（公钥）密码学替代密码或短信进行网站注册、身份验证和多因素认证。它是 Credential Management API 的扩展，抽象了用户代理和认证器之间的通信。}}

{{知识点14}}{{WebAuthn 防钓鱼保护：WebAuthn 提供防钓鱼保护，因为创建假登录网站的攻击者无法以用户身份登录，因为签名会随网站的源（origin）而变化。这是基于密码的认证方案无法提供的安全优势。}}

{{知识点15}}{{WebAuthn 减少数据泄露影响：使用 WebAuthn 时，开发者不需要对公钥进行哈希处理。即使攻击者获得了用于验证身份的公钥，也无法进行身份验证，因为它需要私钥。私钥始终保留在用户的认证器设备上。}}

{{知识点16}}{{WebAuthn 免受密码攻击：WebAuthn 免受密码攻击，因为用户可能在不同网站重复使用密码，攻击者可能通过数据泄露获得其他网站的密码。而数字签名比文本密码更难暴力破解。}}

{{知识点17}}{{PublicKeyCredential 接口：PublicKeyCredential 提供关于公钥/私钥对的信息，是使用不可钓鱼且抗数据泄露的非对称密钥对替代密码登录服务的凭据。它继承自 Credential，自2021年9月起在各浏览器中广泛可用。}}

{{知识点18}}{{PublicKeyCredential 关键属性：authenticatorAttachment（认证器附加方式）、id（base64url编码的凭据ID）、rawId（ArrayBuffer格式的全局唯一标识符）、response（AuthenticatorResponse 实例，包含认证响应数据）、type（始终为 "public-key"）。}}

{{知识点19}}{{PublicKeyCredential 静态方法：getClientCapabilities() 检查 WebAuthn 能力支持、isConditionalMediationAvailable() 检查条件调解是否可用、isUserVerifyingPlatformAuthenticatorAvailable() 检查平台认证器是否支持用户验证、parseCreationOptionsFromJSON() 和 parseRequestOptionsFromJSON() 用于反序列化服务器数据。}}

{{知识点20}}{{WebAuthn 注册流程：1）服务器发送用户信息、依赖方信息和 challenge；2）前端调用 navigator.credentials.create({ publicKey: {...} })；3）认证器获取用户同意后生成密钥对并返回 PublicKeyCredential；4）前端将凭据转发到服务器；5）服务器验证 challenge、来源和签名。}}

{{知识点21}}{{WebAuthn 认证流程：1）服务器生成 challenge 发送给前端；2）前端调用 navigator.credentials.get({ publicKey: { challenge, ... } })；3）认证器使用之前生成的私钥对 challenge 签名；4）前端将签名结果发送到服务器；5）服务器使用存储的公钥验证签名。}}

{{知识点22}}{{WebAuthn 认证器类型：认证器可以嵌入用户代理中、嵌入操作系统中（如 Windows Hello），也可以是物理令牌（如 USB 或蓝牙安全密钥）。authenticatorAttachment 属性区分 "platform"（平台认证器）和 "cross-platform"（跨平台认证器）。}}

{{知识点23}}{{challenge 安全要求：WebAuthn 中的 challenge 必须是至少16字节大小的随机信息缓冲区。challenge 由服务器生成，用于防止重放攻击，确保每次认证请求的唯一性。}}

{{知识点24}}{{FedCM API（联合凭据管理）概述：FedCM 解决了传统身份联合依赖 iframe、重定向和第三方 cookie 的问题，这些也是第三方跟踪所使用的技术。FedCM 提供了一个专门的机制用于 Web 上的联合身份流程，使支持的浏览器能够在依赖方上提供特殊的 UI 元素。}}

{{知识点25}}{{FedCM 核心概念：身份联合是将网站的用户认证委托给受信任的第三方身份提供商（IdP）的过程。依赖方（RP）如电商或社交网站，与 IdP（如 Google、Facebook、GitHub）集成，允许用户使用已注册的 IdP 账户登录。}}

{{知识点26}}{{FedCM 解决的问题：传统联合身份依赖 iframe、重定向和第三方 cookie，浏览器正在限制这些功能以保护用户隐私。FedCM 提供了专门的联合身份机制，支持个性化按钮、社交小部件、OIDC 前端通道注销和会话刷新等用例。}}

{{知识点27}}{{IdentityCredential 接口：IdentityCredential 表示来自成功联合身份验证的用户身份凭据。成功的 navigator.credentials.get() 调用（包含 identity 选项）会解析为 IdentityCredential 实例。关键属性包括 configURL（IdP 配置文件URL）、isAutoSelected（是否自动重认证）和 token（验证令牌）。}}

{{知识点28}}{{FedCM 身份提供商集成：IdP 需要提供配置文件和端点，RP 通过 navigator.credentials.get({ identity: { providers: [{ configURL, clientId, params }] } }) 发起联合登录请求。Google Sign In 是已支持 FedCM 的 IdP 示例。}}

{{知识点29}}{{FedCM 权限策略控制：identity-credentials-get Permissions-Policy 可用于控制 FedCM 的使用权限。开发者可以通过 iframe 的 allow 属性显式授予 FedCM 使用权限：<iframe src="3rd-party.example" allow="identity-credentials-get"></iframe>。}}

{{知识点30}}{{WebOTP API 概述：WebOTP API 允许 Web 应用从短信中自动提取一次性密码（OTP），无需用户手动复制粘贴。它通过 navigator.credentials.get({ otp: { transport: ["sms"] } }) 发起请求，当接收到包含应用域名的短信时，浏览器会提示用户同意检索 OTP。}}

{{知识点31}}{{WebOTP SMS 消息格式：短信最后一行必须包含域名和OTP，格式为 @域名 #OTP。例如：@www.example.com #123456。对于嵌入 iframe 的第三方网站，格式为 @顶级域名 #OTP @嵌入域名。OTP 与发送域名绑定，可缓解钓鱼攻击风险。}}

{{知识点32}}{{WebOTP 安全注意事项：短信 OTP 本身不太安全，攻击者可以伪造短信并劫持电话号码，运营商可能在账户关闭后将电话号码回收给新用户。建议尽可能使用更强的认证方式，如基于 WebAuthn 的密码+安全密钥或通行密钥（passkeys）。}}

{{知识点33}}{{FederatedCredential 接口（已弃用）：FederatedCredential 提供来自联合身份提供商的凭据信息，即网站信任的、能正确验证用户身份的实体。OpenID Connect 是此类框架的一个示例。该接口已被 FedCM API 中的 IdentityCredential 取代并标记为弃用。}}

{{知识点34}}{{Credential Management API 安全上下文要求：Credential Management API 仅在安全上下文（HTTPS）中可用。这意味着在 HTTP 页面上无法使用任何凭据管理功能，这是出于安全考虑的必要限制。}}

{{知识点35}}{{Credential Management Level 1 规范：Credential Management API 的正式规范为 W3C 的 "Credential Management Level 1"，定义了 CredentialsContainer、PasswordCredential、FederatedCredential 等接口。Level 2 规范正在发展中，扩展了更多凭据类型。}}

{{知识点36}}{{WebAuthn JSON 序列化便利方法：PublicKeyCredential 提供了 toJSON() 实例方法用于将凭据序列化为 JSON 字符串，以及 parseCreationOptionsFromJSON() 和 parseRequestOptionsFromJSON() 静态方法用于反序列化服务器数据，方便前后端之间的凭据数据传输。}}

{{知识点37}}{{WebAuthn 凭据信号方法：PublicKeyCredential 提供三个静态信号方法：signalAllAcceptedCredentials() 向认证器信号服务器仍持有的有效凭据ID、signalCurrentUserDetails() 信号用户更新了用户名或显示名、signalUnknownCredential() 信号凭据ID未被服务器识别（如已删除）。}}

{{知识点38}}{{条件调解（Conditional Mediation）：isConditionalMediationAvailable() 静态方法检查浏览器是否支持条件调解。条件调解允许在用户与登录表单交互时自动显示通行密钥（passkeys）选项，提供更流畅的认证体验，无需用户主动触发。}}

{{知识点39}}{{平台认证器可用性检测：isUserVerifyingPlatformAuthenticatorAvailable() 静态方法返回一个 Promise，解析为布尔值，表示绑定到平台的认证器是否能够验证用户。这可以用于决定是否向用户展示 WebAuthn 注册选项。}}

{{知识点40}}{{Credential Management API 实际应用场景：1）自动保存和填充登录凭据；2）无密码认证（WebAuthn/Passkeys）；3）联合登录（Google/GitHub 等 IdP 登录）；4）短信 OTP 自动填充；5）多因素认证（MFA）；6）支付验证等敏感操作的二次确认。}}
