# 2025前端Progress与Spinner
# 领域: 计算机IT
# 学科: 前端框架
# 来源: 2025前端Progress与Spinner
# 描述: 前端Progress与Spinner核心知识点

{{ARIA progressbar角色定义}}{{ARIA progressbar是一个WAI-ARIA范围小部件，表示请求已收到，应用程序正在朝着完成所请求操作的方向取得进展。它属于WAI-ARIA规范中的range类型控件，用于向辅助技术传达任务的完成进度。}}

{{ARIA progressbar核心属性}}{{aria-valuenow：只读属性，表示当前进度值，不确定时不包含此属性；aria-valuemin：最小值，默认为0；aria-valuemax：最大值，默认为100；aria-valuetext：当百分比不准确时，提供可读的文本描述；aria-label或aria-labelledby：提供可访问名称，必需属性。}}

{{ARIA progressbar后代元素的展示性角色}}{{progressbar的所有后代元素会被浏览器自动应用role="presentation"，因为辅助技术API无法表示progressbar内的语义元素。这意味着即使在progressbar内放置h3等语义标签，对辅助技术用户来说这些标签不存在。}}

{{原生progress元素优先原则}}{{MDN和W3C推荐优先使用原生HTML <progress>元素或<input type="range">而非自定义div+role="progressbar"。原生元素自带样式和语义，无需手动重建ARIA属性、JavaScript和CSS。}}

{{progress元素基本用法}}{{<progress id="load" max="100" value="23"></progress>，配合<label for="load">Loading:</label>提供可访问名称。原生progress元素无需role属性，浏览器自动识别。}}

{{Determinate与Indeterminate模式}}{{进度条分为确定型（determinate）和不确定型（indeterminate）两种模式。确定型显示具体进度百分比，通过aria-valuenow或value属性设置；不确定型不设置aria-valuenow，用于无法预知完成时间的场景，如文件上传中。}}

{{Radix UI Progress组件}}{{
@radix-ui/react-progress提供Root和Indicator两个部分。Root包含所有进度部分，Indicator用于视觉展示进度并使进度对辅助技术可访问。data-state属性有三个值：complete、indeterminate、loading。支持asChild、value、max、getValueLabel等属性。遵循progressbar角色要求。}}

{{shadcn/ui Progress组件}}{{基于Radix UI Progress封装，安装命令：pnpm dlx shadcn@latest add progress。使用方式：<Progress value={33} />。支持Field组件添加标签、受控模式、RTL支持。API参考Radix UI Progress文档。}}

{{MUI LinearProgress组件}}{{Material UI的线性进度条组件，支持variant属性：determinate（确定型，配合value属性）、indeterminate（不确定型）、buffer（缓冲型，配合value和valueBuffer属性）、query（反向动画）。支持颜色自定义、标签显示。默认使用CSS动画，在JavaScript加载前即可工作。}}

{{MUI CircularProgress组件}}{{Material UI的圆形进度条/旋转器，默认渲染indeterminate版本。支持determinate和indeterminate变体，可自定义size、颜色、min/max范围。支持enableTrackSlot始终显示轨道。可与Button和FAB组件集成实现加载状态。支持标签显示百分比。}}

{{MUI延迟出现策略}}{{基于Nielsen Norman Group的响应时间3个重要限制（0.1秒即时感、1秒保持流畅感、10秒保持注意力），MUI建议延迟显示进度条，避免快速操作时出现闪烁的进度条反而降低用户体验。}}

{{NProgress页面加载进度条}}{{NProgress是一个轻量级的页面加载进度条库，模仿YouTube和Medium的顶部进度条效果。通常与路由切换配合使用，在页面加载/导航时在顶部显示细长的进度条动画。API包括NProgress.start()、NProgress.done()、NProgress.set()、NProgress.inc()等方法。}}

{{NProgress配置选项}}{{可通过NProgress.configure()设置选项：minimum（最小百分比，默认0.08）、speed（动画速度，默认200ms）、easing（缓动函数，默认ease）、trickle（是否自动递增）、trickleSpeed（自动递增间隔，默认200ms）、showSpinner（是否显示旋转器，默认true）、parent（父容器选择器）。}}

{{CSS @keyframes旋转动画}}{{使用@keyframes定义spin动画：@keyframes spin { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }。应用于元素：animation: spin 1s linear infinite; 这是最基础的CSS旋转加载动画实现方式。}}

{{CSS圆环Spinner实现}}{{使用border和border-radius实现圆环spinner：设置元素width和height、border: 4px solid #f3f3f3、border-top: 4px solid #3498db、border-radius: 50%，配合spin动画。border-top颜色与背景色不同形成旋转视觉效果。}}

{{Three Dots弹跳动画}}{{三个圆点依次弹跳的加载动画。使用display: flex配合gap间距，每个dot设置border-radius: 50%，通过animation-delay实现错开效果：.dot:nth-child(1) { animation-delay: 0s; } .dot:nth-child(2) { animation-delay: 0.16s; } .dot:nth-child(3) { animation-delay: 0.32s; }。}}

{{Pulse脉冲动画}}{{使用scale变换实现脉冲效果：@keyframes pulse { 0%, 100% { transform: scale(1); opacity: 1; } 50% { transform: scale(1.5); opacity: 0.5; } }。适合用于按钮加载状态或小型元素的加载指示。}}

{{骨架屏Skeleton Screen概念}}{{骨架屏是在内容加载前显示的页面内容轮廓占位符，使用灰色矩形、圆形等形状模拟真实内容的布局结构。比传统spinner更能减少用户感知等待时间，因为用户能预见到即将出现的内容结构。Facebook、LinkedIn、YouTube等广泛使用。}}

{{骨架屏实现方式-CSS}}{{使用background: linear-gradient(90deg, #f0f0f0 25%, #e0e0e0 50%, #f0f0f0 75%)配合background-size: 200% 100%和shimmer动画实现闪光效果。@keyframes shimmer { 0% { background-position: -200% 0; } 100% { background-position: 200% 0; } }。}}

{{骨架屏实现方式-组件库}}{{Ant Design的Skeleton组件支持paragraph、title、avatar等子组件的骨架屏。MUI的Skeleton组件支持variant="text|circular|rectangular|rounded"，支持动画wave|pulse|false。shadcn/ui社区有多种skeleton实现方案。}}

{{骨架屏与LCP优化}}{{骨架屏有助于改善LCP（Largest Contentful Paint）指标。通过在首屏内容加载前显示骨架屏，减少CLS（Cumulative Layout Shift），因为骨架屏已预留了正确的空间，内容替换时不会引起布局偏移。}}

{{React Suspense与加载状态}}{{React 18+的Suspense组件配合fallback属性实现组件级加载状态：<Suspense fallback={<Spinner />}><LazyComponent /></Suspense>。结合React.lazy()实现代码分割时的加载指示。2025年React Server Components进一步扩展了Suspense的使用场景。}}

{{Next.js加载UI}}{{Next.js 13+ App Router使用loading.js文件自动创建加载UI。loading.js导出的组件会在路由段内容加载时自动显示，底层基于React Suspense。无需手动管理加载状态，自动处理流式渲染。}}

{{Vue异步组件加载状态}}{{Vue 3的defineAsyncComponent支持loadingComponent和errorComponent选项。在动态import的异步组件加载期间显示加载状态，在出错时显示错误状态。配合Suspense组件使用更灵活。}}

{{CSS only进度条实现}}{{纯CSS进度条使用width百分比配合transition动画：<div style="width: 70%; transition: width 0.3s ease;"></div>。background可使用渐变色增强视觉效果。linear-gradient可实现多色渐变进度条。}}

{{SVG环形进度条}}{{使用SVG的circle元素配合stroke-dasharray和stroke-dashoffset实现环形进度条。stroke-dasharray设置为圆周长（2πr），stroke-dashoffset从圆周长递减到0实现从空到满的动画效果。比CSS border方案更精确可控。}}

{{View Transitions API与加载动画}}{{2025年View Transitions API已被主流浏览器支持，可实现页面间平滑过渡动画。配合document.startViewTransition()在内容切换时自动创建快照过渡，减少传统spinner的需求，提供更流畅的加载体验。}}

{{可访问性最佳实践-进度条}}{{当进度条描述页面特定区域的加载进度时，应包含aria-describedby引用进度条状态，并在区域加载完成前设置aria-busy="true"。使用语义化的<progress>元素可自动获得正确的ARIA角色。}}

{{可访问性最佳实践-Spinner}}{{Spinner应使用role="status"或aria-live="polite"通知屏幕阅读器加载状态。提供视觉隐藏的文本如"正在加载..."供辅助技术读取。使用aria-hidden="true"隐藏纯装饰性的动画元素。}}

{{prefers-reduced-motion媒体查询}}{{使用@media (prefers-reduced-motion: reduce)为减少动画偏好的用户禁用或简化动画。将animation替换为简化的状态指示或静态图标。这是WCAG 2.1 Level AAA建议但Level A也推荐关注的方向。}}

{{Lottie动画与加载指示器}}{{Lottie是Airbnb开源的动画库，支持从After Effects导出的JSON动画文件在Web端播放。适合实现复杂的品牌化加载动画，比CSS动画更丰富。通过lottie-web库使用，支持控制播放速度、循环、方向等。}}

{{Rive动画引擎}}{{Rive是2025年流行的交互式矢量动画工具和运行时，比Lottie更轻量且支持状态机。适合创建可交互的加载指示器，可根据加载进度动态改变动画状态。@rive-app/canvas或@rive-app/react-canvas使用。}}

{{React Spinners组件库}}{{react-spinners提供多种CSS spinner组件：BeatLoader、BounceLoader、CircleLoader、ClimbingBoxLoader、ClipLoader、DotLoader、FadeLoader、GridLoader、HashLoader、MoonLoader、PacmanLoader、PropagateLoader、PulseLoader、RingLoader、RiseLoader、RotateLoader、ScaleLoader、SkewLoader、SquareLoader等。}}

{{TanStack Query加载状态}}{{TanStack Query（原React Query）提供isLoading、isFetching、isPending等状态管理。配合Placeholder Data实现数据切换时的无闪烁体验，减少对spinner的依赖。backgroundRefetch可在后台刷新数据同时保持旧数据显示。}}

{{骨架屏性能优化}}{{骨架屏应使用CSS transform而非width/height变化实现动画，利用GPU加速减少重排。content-visibility: auto可优化骨架屏渲染性能，只渲染视口内内容。使用will-change: transform提示浏览器优化动画层。}}

{{进度条与CLS优化}}{{进度条组件应预先设定固定高度和宽度容器，避免加载完成替换内容时引起CLS。使用aspect-ratio保持容器比例。动态高度的内容区域使用min-height预留空间。}}

{{2025年Progress组件设计趋势}}{{2025年前端加载指示器设计趋势：微交互化（加载状态中加入品牌微动画）、渐进式加载（先加载骨架再填充内容）、乐观更新（先显示结果再同步）、流式UI（利用HTTP Streaming逐步渲染）。传统spinner的使用正在减少，取而代之的是更智能的内容加载策略。}}
