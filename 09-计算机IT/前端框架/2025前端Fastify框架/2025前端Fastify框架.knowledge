# 2025前端Fastify框架
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}
Fastify 是一个高性能的 Node.js Web 框架，由 Matteo Collina 创建，目前最新版本为 v5.8.x（2025年）。它专注于提供最佳的开发者体验和最低的开销，是 OpenJS 基金会旗下项目。Fastify 的设计理念是"快"——通过高度优化的路由匹配、JSON Schema 验证和序列化，实现远超 Express 的性能表现。

{{知识点2}}
Fastify 的核心特性包括：高性能路由（基于 find-my-way 路由引擎）、基于 JSON Schema 的请求验证和响应序列化、插件化架构、封装上下文（Encapsulation Context）、丰富的钩子系统（Hooks）、装饰器 API（Decorators）、内置日志系统（基于 Pino）、以及原生的 async/await 支持。

{{知识点3}}
Fastify 使用 find-my-way 作为其路由引擎，这是一个基于 Radix Tree（基数树）的高性能 HTTP 路由器。相比 Express 使用的线性路由匹配，find-my-way 通过树形结构实现 O(log n) 的路由查找速度，支持参数化路由（`:param`）、通配符路由（`*`）和正则表达式路由。

{{知识点4}}
安装 Fastify 非常简单，通过 npm 或 yarn 即可：`npm i fastify`。创建服务器只需几行代码：导入 Fastify、创建实例、声明路由、启动监听。Fastify 同时支持 ESM（ES Modules）和 CommonJS 两种模块系统，ESM 需要在 package.json 中设置 `"type": "module"`。

{{知识点5}}
Fastify 的基本路由声明支持所有 HTTP 方法：GET、POST、PUT、DELETE、PATCH、HEAD、OPTIONS 等。路由处理函数可以使用回调风格（接收 request 和 reply 参数）或 async/await 风格（直接返回响应对象）。async 风格更加简洁，是推荐的写法。

{{知识点6}}
Fastify 的插件系统是其架构的核心。在 Fastify 中，"一切都是插件"（Everything is a plugin）。通过 `fastify.register()` API 注册插件，可以是一组路由、一个装饰器或其他功能。register 会按照声明顺序依次加载插件，确保异步引导的可预测性。

{{知识点7}}
插件封装（Plugin Encapsulation）是 Fastify 的关键特性。默认情况下，`register` 会创建一个新的作用域（scope），这意味着在插件内部通过 `decorate` 添加的属性只对该插件的子级可见，不会影响父级上下文。这形成了一个有向无环图（DAG）结构，避免了跨依赖问题。

{{知识点8}}
使用 `fastify-plugin` 包可以打破封装，使插件内部的装饰器和钩子暴露到父作用域。这在需要共享全局功能（如数据库连接）时非常有用。`fastify-plugin` 还支持声明 Fastify 版本兼容范围，确保向后兼容性。

{{知识点9}}
插件选项（Plugin Options）支持预定义的 Fastify 选项，包括 `logLevel`（自定义日志级别）、`logSerializers`（自定义日志序列化器）和 `prefix`（路由前缀）。插件还可以接受任意自定义选项，建议使用命名空间避免冲突。

{{知识点10}}
路由前缀（Route Prefixing）是通过 `prefix` 选项实现的，可以为一组路由统一添加 URL 前缀。例如 `fastify.register(routes, { prefix: '/api/v1' })` 会将所有路由自动加上 `/api/v1` 前缀。注意：使用 `fastify-plugin` 包装的插件不支持此选项。

{{知识点11}}
Fastify 的钩子（Hooks）系统允许在应用或请求/响应生命周期的特定事件上执行代码。通过 `fastify.addHook()` 方法注册钩子。钩子分为两大类：请求/回复钩子（Request/Reply Hooks）和应用钩子（Application Hooks）。

{{知识点12}}
请求/回复钩子按执行顺序为：`onRequest`（请求到达时）→ `preParsing`（解析前，可转换请求流）→ `preValidation`（验证前，可修改请求体）→ `preHandler`（处理器执行前）→ `preSerialization`（序列化前）→ `onSend`（发送前，可修改响应体）→ `onResponse`（响应发送后）→ `onTimeout`（超时时）→ `onRequestAbort`（客户端中止时）。

{{知识点13}}
应用钩子包括：`onReady`（服务器就绪时）、`onListen`（开始监听时）、`onClose`（服务器关闭时）、`preClose`（关闭前）、`onRoute`（路由注册时）、`onRegister`（插件注册时）。这些钩子用于监控和管理应用生命周期。

{{知识点14}}
生命周期（Lifecycle）定义了请求处理的完整流程：Incoming Request → Routing → Instance Logger → onRequest → preParsing → Parsing → preValidation → Validation → preHandler → User Handler → Reply → preSerialization → onSend → Outgoing Response → onResponse。每个阶段的错误都会自动生成相应的 HTTP 错误码。

{{知识点15}}
Fastify 使用 JSON Schema 进行请求验证。支持验证 body（POST/PUT/PATCH 请求体）、querystring/query（查询字符串）、params（路由参数）和 headers（请求头）。验证引擎为 Ajv v8，是一个高性能的 JSON Schema 验证器。

{{知识点16}}
响应序列化使用 fast-json-stringify 库，它根据 JSON Schema 编译出高度优化的序列化函数，比原生 `JSON.stringify` 快得多。可以在路由 schema 中定义 `response` 来指定不同 HTTP 状态码的响应格式。

{{知识点17}}
共享 Schema 通过 `fastify.addSchema()` API 添加，支持通过 `$ref` 关键字在多个路由间复用。共享 Schema 也受封装上下文限制，子作用域可以访问父作用域的 Schema，反之不行。使用 `fastify.getSchemas()` 或 `fastify.getSchema('id')` 检索已注册的 Schema。

{{知识点18}}
Body 内容类型验证（Body Content-Type Validation）允许为不同的 Content-Type 定义不同的验证 Schema。通过在 body schema 中使用 `content` 属性，可以分别对 `application/json`、`text/plain` 等不同格式进行验证。

{{知识点19}}
装饰器（Decorators）API 用于自定义 Fastify 核心对象。有三个主要方法：`decorate(name, value)` 装饰 Fastify 实例、`decorateRequest(name, value)` 装饰请求对象、`decorateReply(name, value)` 装饰回复对象。装饰器 API 是同步的，异步装饰应使用 `register` + `fastify-plugin`。

{{知识点20}}
装饰器的一个重要用途是避免在请求生命周期中动态修改对象形状（shape）。JavaScript 引擎会根据对象的初始形状进行优化，如果在运行时添加新属性会导致去优化（deoptimization）。使用 `decorateRequest` 预先定义属性可以保持对象形状一致，获得最佳性能。

{{知识点21}}
装饰器的 `dependencies` 参数用于声明装饰器之间的依赖关系。如果依赖的装饰器未被注册，Fastify 会在启动时抛出异常，而不是在运行时才失败。这对于构建可靠的插件生态系统非常重要。

{{知识点22}}
Fastify 内置了基于 Pino 的高性能日志系统。创建实例时通过 `{ logger: true }` 启用，或传入 Pino 配置对象进行自定义。Pino 是 Node.js 生态中最快的 JSON 日志库之一，Fastify 还支持为每个路由设置自定义日志级别和序列化器。

{{知识点23}}
Fastify 支持请求超时管理。通过 `connectionTimeout` 设置 socket 级别的超时，通过 `handlerTimeout` 设置应用级别的每路由超时。`handlerTimeout` 启动后，如果在指定时间内未发送响应，`request.signal` 会被中止并返回 503 错误。

{{知识点24}}
错误处理在 Fastify 中有多层机制：钩子中的错误通过 `done(error)` 传递；路由处理器中 throw Error 或 return Error 会被自动捕获；`setErrorHandler` 允许注册全局自定义错误处理器；`onError` 钩子用于日志记录而非修改错误。

{{知识点25}}
`reply.hijack()` 方法可以阻止 Fastify 自动发送响应，允许开发者完全接管响应流程。调用 hijack 后，后续的钩子和自动序列化将被跳过，但 `onResponse` 钩子仍会执行。适用于需要使用 `reply.raw` 直接操作底层 HTTP 响应的场景。

{{知识点26}}
Fastify 的优雅关闭（Graceful Shutdown）通过 `fastify.close()` 触发，依次执行：preClose 钩子 → 连接排空（connection draining）→ onClose 钩子。这确保了在服务器关闭前，所有正在处理的请求都能完成。

{{知识点27}}
Fastify 的异步引导（Async Bootstrapping）是其重要优势。插件加载在调用 `fastify.listen()`、`fastify.inject()` 或 `fastify.ready()` 时开始。使用 `await` 注册插件会加载该插件及其依赖，"最终化"封装过程。这保证了数据库连接等异步资源在服务器接受连接前就绪。

{{知识点28}}
Fastify 生态系统丰富，官方维护了大量插件：`@fastify/cors`（跨域）、`@fastify/jwt`（JWT 认证）、`@fastify/cookie`（Cookie 管理）、`@fastify/session`（会话管理）、`@fastify/multipart`（文件上传）、`@fastify/static`（静态文件服务）、`@fastify/websocket`（WebSocket 支持）、`@fastify/mongodb`/`@fastify/postgres`（数据库连接）等。

{{知识点29}}
Fastify 与 Express 的核心区别：1) 路由引擎不同（Radix Tree vs 线性匹配），Fastify 更快；2) Fastify 原生支持 JSON Schema 验证和序列化；3) Fastify 的插件封装模型更严格，避免全局污染；4) Fastify 原生支持 async/await；5) Fastify 的基准测试吞吐量通常是 Express 的 2-3 倍。

{{知识点30}}
Fastify 的 TypeScript 支持非常完善，提供完整的类型定义。包括 `FastifyInstance`、`FastifyRequest`、`FastifyReply`、`FastifyPluginAsync` 等核心类型。插件选项、装饰器、Schema 都可以享受完整的类型推断和自动补全。

{{知识点31}}
Fastify 的路由声明支持链式调用（chaining）和对象风格两种方式。链式调用：`fastify.get('/path', handler)`；对象风格：`fastify.route({ method: 'GET', url: '/path', schema: {...}, handler })`。对象风格更适合需要同时指定 Schema 和选项的场景。

{{知识点32}}
Fastify 的 `inject()` 方法允许在不启动 HTTP 服务器的情况下模拟请求，非常适合单元测试和集成测试。它使用 light-my-request 库实现，支持所有 HTTP 方法、请求头、请求体等，返回标准的响应对象。

{{知识点33}}
Fastify 的验证系统支持自定义验证器。虽然默认使用 Ajv，但可以通过 `schemaController` 选项替换为其他 JSON Schema 验证库。自定义异步验证器必须返回 `{error}` 对象而非抛出错误，否则会导致未处理的 Promise rejection。

{{知识点34}}
Fastify 支持 Content-Type 感知的序列化。通过 `reply.serializer()` 可以为特定路由设置自定义序列化器，`setReplySerializer` 设置全局回复序列化器，`setSerializerCompiler` 设置序列化编译器。这使得不同类型的内容可以有不同的序列化策略。

{{知识点35}}
Fastify 的推荐插件加载顺序为：1) Fastify 生态插件 → 2) 自定义插件 → 3) 装饰器 → 4) 钩子 → 5) 业务服务（路由）。这个顺序确保在当前作用域中可以访问所有已声明的属性。子服务可以复制相同的结构实现局部封装。

{{知识点36}}
Fastify 支持 ESM（ES Modules）从 Node.js v13.3.0 起。使用 ESM 时，插件可以使用 `export default` 导出，通过 `fastify.register(import('./plugin.mjs'))` 动态导入。这使得 Fastify 能够充分利用现代 JavaScript 的模块系统。

{{知识点37}}
Diagnostics Channel Hooks 是 Fastify 提供的诊断通道钩子，允许使用 Node.js 内置的 `diagnostics_channel` 模块来监控 Fastify 的内部事件。这对于性能监控、调试和集成 APM（应用性能管理）工具非常有用。

{{知识点38}}
Fastify 的 `addHook` 支持在路由级别注册钩子，而非全局级别。通过在路由定义中添加 `onRequest`、`preHandler` 等属性，可以仅为特定路由添加钩子处理。这比全局钩子更加灵活和高效。

{{知识点39}}
Fastify 使用封装上下文形成有向无环图（DAG）结构管理插件依赖。根上下文中的插件对所有子上下文可见；子上下文中的插件只对其自身和孙上下文可见，对父上下文不可见。这种严格的可见性规则是 Fastify 模块化架构的基础。

{{知识点40}}
Fastify 的 LTS（长期支持）策略遵循语义化版本。主版本线会获得一定时期的维护支持，包括安全补丁和关键 bug 修复。生产环境建议使用 LTS 版本线以获得稳定性和安全性保障。当前活跃的主版本为 v5.x。

{{知识点41}}
`fastify.ready()` 方法返回一个 Promise，在所有插件加载完成后 resolve。它常用于测试场景或需要确保所有异步初始化完成后再执行操作的情况。`fastify.listen()` 内部会先调用 `ready()` 再启动 HTTP 服务器。

{{知识点42}}
Fastify 的 reply 对象支持链式调用，例如 `reply.code(201).header('X-Custom', 'value').send(data)`。常用的 reply 方法包括：`send()`（发送响应）、`code()`（设置状态码）、`header()`（设置响应头）、`redirect()`（重定向）、`type()`（设置 Content-Type）。
