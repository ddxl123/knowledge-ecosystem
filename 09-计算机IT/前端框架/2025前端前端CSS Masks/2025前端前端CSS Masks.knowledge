# 2025前端前端CSS Masks
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}{{CSS Masking概述：CSS Masking模块定义了遮罩（masking）和裁剪（clipping）两种图形操作，用于部分或完全隐藏可视元素的某些区域。裁剪通过定义封闭矢量路径来实现，路径外的内容被隐藏；遮罩则通过图像的透明度或亮度来控制元素的可见性。规范来自CSS Masking Module Level 1。}}

{{知识点2}}{{mask属性（简写）：mask是一个简写属性，可以同时设置mask-image、mask-mode、mask-position、mask-size、mask-repeat、mask-origin、mask-clip和mask-composite等子属性。未显式声明的子属性会使用各自的初始值。使用mask简写可以覆盖之前级联中设置的任何遮罩。}}

{{知识点3}}{{mask-image属性：指定用作遮罩的图像源。值可以是none、URL引用（指向SVG <mask>元素或图片）、CSS渐变函数（如linear-gradient）、image()函数等。支持逗号分隔的多个遮罩图像层。只有通过HTTP/HTTPS协议提供的图像源才会被接受。}}

{{知识点4}}{{mask-mode属性：决定遮罩使用alpha通道还是亮度值来控制可见性。有三个关键字值：alpha（使用透明度值）、luminance（使用亮度值）、match-source（默认值，根据遮罩源类型自动决定）。当源是SVG <mask>元素时默认为luminance，其他图像源默认为alpha。}}

{{知识点5}}{{Alpha遮罩原理：在alpha遮罩模式下，只有遮罩图像的alpha透明度通道起作用。遮罩不透明的区域会显示元素对应部分，遮罩透明的区域会隐藏元素。颜色的色相、亮度等属性不影响遮罩效果。这是最常用的遮罩模式。}}

{{知识点6}}{{Luminance遮罩原理：在luminance遮罩模式下，遮罩的可见性由颜色的亮度和透明度共同决定。白色不透明区域（100%亮度）完全可见，黑色透明区域完全隐藏。亮度计算公式为：((0.2125 * R) + (0.7154 * G) + (0.0721 * B)) * A。例如纯绿色rgb(0, 128, 0)在luminance模式下约35.77%不透明。}}

{{知识点7}}{{mask-position属性：设置遮罩图像的位置，语法与background-position类似。默认值为0% 0%（左上角）。可以使用关键字（center、top、left等）、长度值或百分比值来定位。支持使用offset-x offset-y的双值语法。}}

{{知识点8}}{{mask-size属性：设置遮罩图像的尺寸，语法与background-size类似。默认值为auto。可以使用contain、cover关键字，或具体的长度/百分比值。在mask简写中，mask-size必须写在mask-position之后，用斜杠(/)分隔。}}

{{知识点9}}{{mask-repeat属性：控制遮罩图像的重复方式，语法与background-repeat类似。默认值为repeat。可选值包括repeat-x、repeat-y、no-repeat、space、round等。支持双值语法指定水平和垂直方向的重复方式。}}

{{知识点10}}{{mask-origin属性：指定遮罩图像的定位原点（参考框）。可选值包括content-box、padding-box（默认）、border-box、fill-box、stroke-box、view-box。决定mask-position的参考坐标系。}}

{{知识点11}}{{mask-clip属性：确定遮罩图像的绘制区域（裁剪区域）。可选值包括content-box、padding-box、border-box（默认）、fill-box、stroke-box、view-box，以及特殊值no-clip（不裁剪）。控制遮罩效果的生效范围。}}

{{知识点12}}{{mask-composite属性：定义多个遮罩层之间的合成方式，使用Porter-Duff合成算子。有四个值：add（叠加，默认）、subtract（减去下层）、intersect（取交集）、exclude（取非重叠区域）。支持逗号分隔的多个值，分别对应每个遮罩层。}}

{{知识点13}}{{多层遮罩：mask-image支持逗号分隔的多个遮罩图像值，形成多个遮罩层。每个层可以有独立的position、size、repeat、composite等属性值。多个层按从后往前的顺序合成，最终形成复合遮罩效果。none值在多层中作为透明黑色层处理。}}

{{知识点14}}{{mask-border简写属性：用于在元素边框区域应用遮罩效果，类似于border-image的工作方式。包含mask-border-source、mask-border-slice、mask-border-width、mask-border-outset、mask-border-repeat和mask-border-mode六个子属性。}}

{{知识点15}}{{mask-border-source属性：指定边框遮罩的图像源。可以是URL引用的图像、CSS渐变或none（默认值）。与mask-image类似但作用于元素的边框区域。}}

{{知识点16}}{{mask-border-slice属性：定义边框遮罩图像的切片方式，语法与border-image-slice相同。使用1-4个长度或百分比值将图像分割为9个区域（角和边）。可选inset关键字改变切片方向。}}

{{知识点17}}{{mask-border-width属性：定义边框遮罩的宽度，语法与border-image-width相同。默认值为auto。可以使用长度、百分比或number值。使用1-4个值分别指定上、右、下、左的宽度。}}

{{知识点18}}{{mask-border-outset属性：定义边框遮罩向外扩展的距离，语法与border-image-outset相同。默认值为0。使用1-4个length或number值，正值使遮罩向外延伸超出边框区域。}}

{{知识点19}}{{mask-border-repeat属性：定义边框遮罩边缘区域的重复方式，语法与border-image-repeat相同。可选值包括stretch（默认）、repeat、round、space。支持双值语法分别指定水平和垂直方向。}}

{{知识点20}}{{mask-border-mode属性：定义边框遮罩的遮罩模式，可选alpha或luminance，默认为alpha。与mask-mode类似，决定边框遮罩使用透明度还是亮度来控制可见性。}}

{{知识点21}}{{mask-type属性：用于SVG <mask>元素，定义默认遮罩类型。可选alpha或luminance。当mask-mode设为match-source且遮罩源为SVG <mask>元素时，mask-type的值决定使用哪种遮罩模式。也可以通过SVG mask-type属性设置。}}

{{知识点22}}{{遮罩与裁剪的区别：裁剪（clipping）使用矢量路径，路径外完全透明，路径内完全不透明，是二元的。遮罩（masking）使用图像，支持半透明渐变效果，能实现平滑过渡。裁剪性能更好且更容易插值动画；遮罩提供更多控制和选项。}}

{{知识点23}}{{clip-path属性：推荐的裁剪方式，替代已废弃的clip属性。支持inset()、circle()、ellipse()、polygon()、path()等基本形状函数。可引用SVG中定义的<clipPath>元素。可以对基本形状进行CSS过渡和动画。}}

{{知识点24}}{{clip-path动画：当clip-path使用基本形状（如polygon）定义时，可以进行CSS transition和animation。例如从三角形裁剪过渡到不同形状的三角形。要求起始和结束形状具有相同数量的点。这是创建酷炫视觉效果的强大工具。}}

{{知识点25}}{{渐变作为遮罩：CSS渐变函数可以用作mask-image的值。linear-gradient可以创建线性渐变遮罩实现淡入淡出效果，radial-gradient创建径向渐变遮罩。例如mask-image: linear-gradient(black, transparent)可使元素从上到下逐渐透明。}}

{{知识点26}}{{SVG遮罩引用：mask-image可以通过url()引用SVG中的<mask>元素，如mask-image: url("masks.svg#star")。SVG <mask>元素默认使用luminance模式，可通过mask-type属性改为alpha。SVG遮罩支持更复杂的矢量图形遮罩效果。}}

{{知识点27}}{{遮罩的背景相似性：CSS遮罩属性的设计与background属性高度相似。mask-position、mask-size、mask-repeat、mask-origin、mask-clip分别对应background-position、background-size、background-repeat、background-origin、background-clip。熟悉背景属性的开发者可以快速上手遮罩。}}

{{知识点28}}{{处理顺序：CSS中元素的渲染按以下顺序应用效果——先正常绘制（不含滤镜、遮罩、裁剪和透明度），然后依次应用filter effects、clipping、masking、opacity。理解这个顺序对调试遮罩效果很重要。}}

{{知识点29}}{{遮罩的CORS限制：mask-image的图像源受CORS策略限制，只接受通过HTTP/HTTPS协议提供的图像。本地文件（file://协议）的图像不会被接受，会渲染为透明黑色。本地测试需要搭建本地服务器。}}

{{知识点30}}{{遮罩失败处理：当遮罩图像为空（零宽或零高）、下载失败、浏览器不支持格式、不存在或值不指向遮罩图像时，遮罩会被计为透明黑色图像层，不显示任何内容。这是安全的降级行为。}}

{{知识点31}}{{Webkit旧版遮罩语法：早期WebKit支持的遮罩语法使用-webkit-mask-image前缀，现在标准语法已不需要前缀。旧语法如-webkit-gradient()已废弃。现代浏览器应使用标准的mask-image属性配合标准渐变函数。}}

{{知识点32}}{{几何框值（Geometry Box）：mask-origin和mask-clip都接受<geometry-box>值，包括content-box、padding-box、border-box，以及SVG相关的fill-box、stroke-box、view-box。在mask简写中，一个geometry-box值同时设置origin和clip，两个值则分别设置。}}

{{知识点33}}{{mask简写中的特殊规则：mask简写会重置所有mask-border-*子属性为初始值。因此如果需要设置边框遮罩，应在mask声明之后声明mask-border或其子属性。这是级联中的重要注意事项。}}

{{知识点34}}{{no-clip关键字：mask-clip属性支持特殊值no-clip，表示不裁剪遮罩图像的绘制区域。在mask简写中，一个geometry-box值和no-clip关键字一起使用时，geometry-box设置mask-origin，no-clip设置mask-clip，且两者顺序可互换。}}

{{知识点35}}{{遮罩在SVG中的应用：在SVG中，遮罩属性适用于容器元素（排除<defs>元素）和所有图形元素。SVG的<mask>元素配合maskContentUnits和maskUnits属性可定义更精确的遮罩区域。CSS遮罩可与SVG遮罩互操作。}}

{{知识点36}}{{遮罩的计算值：mask-image的计算值与指定值相同，但URL值会被转换为绝对路径。mask-image的动画类型为discrete（离散），意味着不能平滑过渡——遮罩图像的切换是瞬间的而非渐变的。}}

{{知识点37}}{{遮罩的实际应用场景：CSS Masks常用于创建图片裁切效果、文字填充效果、渐变淡出边缘、不规则形状头像、装饰性叠加层、SVG图标主题化、视差滚动效果、卡片翻转过渡等。是现代CSS视觉设计的重要工具。}}

{{知识点38}}{{遮罩性能考虑：CSS遮罩会创建新的合成层，过多的遮罩层可能影响渲染性能。对于简单的形状裁剪，clip-path比mask性能更好，因为更容易硬件加速和插值。在移动端尤其需要注意遮罩层的数量和复杂度。}}

{{知识点39}}{{mask属性的全局值：mask及其所有子属性都支持CSS全局关键字值：inherit（继承父元素）、initial（初始值）、revert（回退到用户代理样式）、revert-layer（回退到上一层级联层）、unset（未设置，有继承则继承否则为初始值）。}}

{{知识点40}}{{遮罩与PWA图标：CSS遮罩技术也用于PWA（渐进式Web应用）的图标定义。通过maskable图标，浏览器可以根据设备形状自适应裁剪应用图标，确保在不同设备上显示最佳效果。这是遮罩在实际产品中的重要应用。}}
