# 2025前端前端Page Visibility API
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}{{Page Visibility API 是什么：Page Visibility API 是一个 Web API，允许开发者检测当前页面是否对用户可见。当用户最小化窗口、切换到其他标签页、或将浏览器完全被其他窗口遮挡时，该 API 会发送 visibilitychange 事件通知页面可见性状态的变化。开发者可以据此调整页面行为，如暂停视频播放、减少网络请求等。}}

{{知识点2}}{{核心目的：Page Visibility API 解决的核心问题是让 Web 应用能够感知自身是否处于前台可见状态。这对于优化性能、节省资源、提升用户体验至关重要。在没有该 API 之前，开发者只能通过 blur/focus 事件来间接推测，但这些事件并不能准确反映页面是否真正隐藏。}}

{{知识点3}}{{Document.visibilityState 属性：Document.visibilityState 是一个只读属性，返回一个字符串表示文档当前的可见性状态。它有两个可能的值："visible" 表示页面至少部分可见（即页面是未最小化窗口的前台标签页），"hidden" 表示页面内容对用户不可见（标签页在后台、窗口最小化或设备屏幕关闭）。}}

{{知识点4}}{{Document.hidden 属性：Document.hidden 是一个只读的 Boolean 属性，返回 true 表示页面被隐藏，false 表示页面可见。它是 visibilityState 的简化替代方案，适用于只需要判断是否隐藏的场景。该属性自 2015 年 7 月起在主流浏览器中广泛可用。}}

{{知识点5}}{{visibilitychange 事件：当文档的可见性状态发生变化时，浏览器会触发 visibilitychange 事件。该事件不可取消（not cancelable），事件类型为通用的 Event 对象。开发者可以通过 addEventListener 或 onvisibilitychange 属性来监听此事件。}}

{{知识点6}}{{事件触发时机：visibilitychange 事件在以下场景触发：用户切换浏览器标签页、导航到新页面、最小化或关闭浏览器、在移动端切换到其他应用、设备屏幕锁定。transition to hidden 是页面可可靠观察到最后一个事件，开发者应将其视为用户会话的可能结束点。}}

{{知识点7}}{{与 blur/focus 事件的区别：onblur 和 onfocus 事件只能告诉你用户是否切换了窗口焦点，但并不一定意味着页面被隐藏。页面只有在用户切换标签页或最小化包含该标签的浏览器窗口时才会变为隐藏状态。Page Visibility API 能准确检测页面是否真正对用户不可见，比 blur/focus 更可靠。}}

{{知识点8}}{{与 beforeunload/unload 的对比：在移动端，pagehide、beforeunload 和 unload 事件可能永远不会触发。因为移动操作系统可以在没有任何通知的情况下终止后台应用进程。Page Visibility API 是唯一在所有平台上可靠触发的事件，是移动端和桌面端通用的最佳选择。}}

{{知识点9}}{{iframe 的可见性：iframe 的可见性状态与父文档相同。使用 CSS 属性（如 display: none）隐藏 iframe 不会触发可见性事件，也不会改变 iframe 内文档的可见性状态。iframe 的可见性完全跟随其父文档的可见性。}}

{{知识点10}}{{基本使用方法：监听 visibilitychange 事件是最常见的用法。通过 document.addEventListener("visibilitychange", callback) 注册监听器，然后在回调中通过 document.visibilityState 或 document.hidden 判断当前状态，执行相应的业务逻辑。}}

{{知识点11}}{{暂停和恢复音频播放：典型应用场景之一是音频控制。当页面隐藏时暂停播放，页面重新可见时恢复播放。需要注意记录隐藏前的播放状态，避免页面可见时错误地自动播放原本已暂停的音频。使用 playingOnHide 标志变量来追踪状态。}}

{{知识点12}}{{发送会话分析数据：transition to hidden 是发送分析数据的理想时机。推荐使用 navigator.sendBeacon() API 而非 XMLHttpRequest，因为 sendBeacon 更可靠，即使页面正在卸载也能确保数据发送成功。这可以作为用户会话的结束信号。}}

{{知识点13}}{{暂停轮询和网络请求：当页面不可见时，暂停对服务器的轮询请求可以显著减少不必要的网络流量和服务器负载。dashboard 类应用尤其适合此策略——只在页面可见时请求数据更新，隐藏时停止轮询，可见时恢复并立即刷新数据。}}

{{知识点14}}{{控制图片轮播：图片轮播组件应该在页面不可见时停止自动切换。这不仅节省资源，还能确保用户不会错过他们想看的内容。通过在 visibilitychange 回调中暂停/恢复定时器来实现。}}

{{知识点15}}{{节省电池和性能：浏览器对后台标签页有多种节流策略。大多数浏览器会停止向后台标签页发送 requestAnimationFrame 回调。setTimeout 等定时器在后台标签页中会被节流。Firefox 和 Chrome 都实现了基于预算的后台超时节流机制。}}

{{知识点16}}{{浏览器后台节流机制详解：Firefox 中后台窗口有独立的时间预算（最大 +50ms，最小 -150ms）。Chrome 类似但预算以秒为单位。窗口在后台 30 秒后（Chrome 为 10 秒）开始受到节流。定时器任务仅在预算非负时才允许执行。预算以每秒 10ms 的速率恢复。}}

{{知识点17}}{{节流豁免条件：播放音频的标签页被视为前台标签页，不受节流。使用实时网络连接（WebSocket、WebRTC）的标签页不会被节流，以避免连接超时意外关闭。IndexedDB 进程也不受节流，以避免超时问题。}}

{{知识点18}}{{推荐的会话管理策略：将每次 transition to visible 视为新会话——恢复之前的状态、重置分析计数器等。当应用过渡到 hidden 时结束会话——保存用户和应用状态、发送分析数据。如需聚合，可以在服务器端将多个可见性会话关联为更大的用户流程。}}

{{知识点19}}{{规范来源：Page Visibility API 定义在 WHATWG HTML 规范中。visibilityState 的规范引用为 HTML#dom-document-visibilitystate，hidden 为 HTML#dom-document-hidden，visibilitychange 事件为 HTML#event-visibilitychange。}}

{{知识点20}}{{浏览器兼容性：Page Visibility API 自 2015 年 7 月起在所有主流浏览器中广泛可用（Baseline: Widely available）。包括 Chrome、Firefox、Safari、Edge 等。visibilitychange 事件自 2021 年 4 月起也达到了广泛可用状态。}}

{{知识点21}}{{移动端的特殊性：在移动操作系统中，应用可以通过多种方式进入后台状态：用户点击通知切换应用、使用任务切换器、按 Home 键、操作系统自动切换（如来电）。进入后台后，进程可能被随时终止，不会触发 unload 事件。}}

{{知识点22}}{{设备屏幕关闭时的行为：当设备屏幕关闭时，visibilityState 会变为 "hidden"。这意味着即使浏览器进程仍在运行，页面也会被视为不可见。开发者可以利用这一点在屏幕关闭时暂停不必要的计算和动画。}}

{{知识点23}}{{navigator.sendBeacon 配合使用：在 visibilitychange 事件中使用 sendBeacon 是发送分析数据的最佳实践。sendBeacon 方法即使在页面卸载过程中也能可靠地将少量数据发送到服务器，比 XMLHttpRequest 或 fetch 更适合这种场景。}}

{{知识点24}}{{与 Page Lifecycle API 的关系：Page Lifecycle API 提供了更细粒度的页面生命周期管理最佳实践。它将页面生命周期分为 active、passive、hidden、frozen、terminated 等状态。Page Visibility API 的 visible/hidden 状态是 Page Lifecycle 的子集。}}

{{知识点25}}{{避免使用 unload 事件：Ilya Grigorik（Google 工程师）明确建议：使用 Page Visibility API，忘记 unload 等其他事件的存在。unload 事件在移动端不可靠，Safari 桌面版也不触发。visibilitychange 是唯一可靠的跨平台方案。}}

{{知识点26}}{{JavaScript 代码示例——基本监听：document.addEventListener("visibilitychange", () => { if (document.hidden) { // 页面隐藏逻辑 } else { // 页面可见逻辑 } }); 这是最基本的用法模式。}}

{{知识点27}}{{JavaScript 代码示例——音频暂停：let playingOnHide = false; document.addEventListener("visibilitychange", () => { if (document.hidden) { playingOnHide = !audio.paused; audio.pause(); } else if (playingOnHide) { audio.play(); } }); 该模式确保只恢复之前正在播放的音频。}}

{{知识点28}}{{JavaScript 代码示例——发送分析：document.onvisibilitychange = () => { if (document.visibilityState === "hidden") { navigator.sendBeacon("/log", analyticsData); } }; 使用 onvisibilitychange 属性绑定（注意是单数，非复数）。}}

{{知识点29}}{{性能优化最佳实践：在页面隐藏时应停止 requestAnimationFrame 动画循环、暂停视频/音频播放、减少或停止网络轮询、暂停 WebSocket 心跳但保持连接、保存当前状态到 localStorage 或 sessionStorage。}}

{{知识点30}}{{与 Intersection Observer API 的区别：Intersection Observer API 用于检测元素是否在视口中可见，适用于懒加载、无限滚动等场景。Page Visibility API 检测的是整个页面/标签页是否可见，适用于全局的资源管理。两者层级不同，用途不同。}}

{{知识点31}}{{visibilityState 的 "prerender" 值：在早期规范中，visibilityState 还有 "prerender" 值，表示页面正在预渲染但用户尚不可见。此值已在现代浏览器中被弃用，当前规范只保留了 "visible" 和 "hidden" 两个值。}}

{{知识点32}}{{React/Vue 中的使用：在 React 中可以使用 useEffect 钩子配合 visibilitychange 事件监听，在组件卸载时记得移除监听器。Vue 中可以在 onMounted/onUnmounted 生命周期中处理。关键是确保清理事件监听器以避免内存泄漏。}}

{{知识点33}}{{游戏和动画应用：对于 Web 游戏和复杂动画应用，Page Visibility API 至关重要。页面隐藏时应暂停游戏循环、停止渲染、释放 GPU 资源。这不仅节省电量，还能防止后台积累大量未显示的帧导致内存问题。}}

{{知识点34}}{{实时通信应用中的策略：聊天、协作等实时通信应用在页面隐藏时应保持 WebSocket 连接（因为重连成本高），但应停止发送心跳、暂停接收消息通知的 UI 更新、降低轮询频率。页面恢复可见时立即同步最新状态。}}

{{知识点35}}{{测试和调试技巧：在浏览器开发者工具中，可以通过 Application 面板模拟页面可见性状态变化。Chrome DevTools 提供了 "Emulate Page Visibility" 功能，可以快速测试 hidden/visible 状态切换而无需实际切换标签页。}}

{{知识点36}}{{安全性考虑：Page Visibility API 不会泄露敏感信息，它只告诉页面自身的可见性状态，无法获知其他标签页的信息。但在某些场景下，恶意网站可能利用可见性变化来推断用户行为模式（如用户切换到哪个应用），需要注意隐私保护。}}

{{知识点37}}{{Service Worker 中的限制：Service Worker 运行在独立的上下文中，无法直接访问 Page Visibility API。但主线程可以通过 postMessage 将可见性状态变化通知 Service Worker，让 Service Worker 根据页面状态调整后台同步策略。}}

{{知识点38}}{{与 Notification API 的协同：当页面处于隐藏状态时，可以通过 Notification API 显示系统通知来提醒用户。这是 Page Visibility API 与通知机制的典型协同使用——页面隐藏时不再更新 UI，而是通过通知告知用户重要信息。}}

{{知识点39}}{{内存管理策略：长时间处于 hidden 状态的页面应该考虑释放不必要的资源，如清空大型数组、断开不需要的 WebSocket 连接、停止 Web Worker 等。当页面恢复 visible 时再重新初始化这些资源。这对于内存敏感的移动端尤为重要。}}

{{知识点40}}{{2025 年的发展趋势：随着 Web 应用越来越复杂，Page Visibility API 的重要性持续增长。结合 Page Lifecycle API、Background Fetch API、Background Sync API 等新特性，开发者能够更精细地控制应用在不同生命周期阶段的行为，实现更智能的资源管理。}}

{{知识点41}}{{TypeScript 类型定义：在 TypeScript 中，document.visibilityState 的类型为 DocumentVisibilityState，即 "visible" | "hidden"。document.hidden 的类型为 boolean。这些类型定义已包含在 TypeScript 的内置 lib.dom.d.ts 中，无需额外安装类型包。}}

{{知识点42}}{{常见错误——忘记清理监听器：在单页应用（SPA）中，如果在组件内添加了 visibilitychange 事件监听器但忘记在组件卸载时移除，会导致内存泄漏和重复执行。务必在组件销毁时调用 removeEventListener 清理。}}

{{知识点43}}{{常见错误——错误使用 unload 事件：许多开发者习惯使用 unload 事件来保存状态或发送数据，但这在移动端完全不可靠。应将所有基于 unload 的逻辑迁移到 visibilitychange（当 visibilityState === "hidden" 时）。}}

{{知识点44}}{{与 document.hasFocus() 的区别：document.hasFocus() 检查文档或其子元素是否获得焦点，但它与页面可见性不完全相同。一个可见的页面可能没有焦点（如用户在另一个窗口上），而一个有焦点的页面在某些边缘情况下可能被遮挡。Page Visibility API 更准确地反映用户是否能看到页面内容。}}

{{知识点45}}{{跨浏览器一致性：所有主流浏览器对 Page Visibility API 的实现基本一致。visibilityState 的 "visible" 和 "hidden" 值在各浏览器中含义相同。事件触发时机也高度一致。这使得开发者可以放心使用而无需编写浏览器特定的兼容代码。}}
