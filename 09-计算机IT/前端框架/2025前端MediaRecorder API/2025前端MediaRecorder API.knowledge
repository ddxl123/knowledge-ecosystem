# 2025前端MediaRecorder API
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}{{MediaRecorder API 概述：MediaRecorder 是 MediaStream Recording API 的核心接口，用于录制来自 MediaStream 的音频和视频数据。它能将媒体流录制为 Blob 对象，支持 WebM、MP4 等容器格式。该 API 自 2021 年起已被主流浏览器广泛支持（Baseline Widely available），规范由 W3C MediaStream Recording 标准定义。}}

{{知识点2}}{{MediaRecorder 构造函数：使用 new MediaRecorder(stream, options) 创建实例。stream 参数是必填的 MediaStream 对象，可来自 getUserMedia()、<audio>/<video>/<canvas> 元素。options 可选参数包括 mimeType、audioBitsPerSecond、videoBitsPerSecond、bitsPerSecond、audioBitrateMode 等。如果指定的 mimeType 不受支持，将抛出 NotSupportedError DOMException。}}

{{知识点3}}{{mimeType 选项：指定录制媒体的容器格式和编解码器，如 "video/webm"、"video/mp4"、"audio/webm;codecs=opus" 等。可仅指定容器格式（浏览器自动选择编解码器），也可通过 codecs 参数精确指定编解码器和配置。默认为空字符串。创建前应使用 isTypeSupported() 检查支持情况。}}

{{知识点4}}{{audioBitsPerSecond 选项：指定音频编码比特率（单位：bps）。例如 128000 表示 128 Kbps。若未指定 audioBitsPerSecond 和 videoBitsPerSecond，音频默认采用自适应比特率（取决于采样率和声道数），视频默认为 2.5 Mbps。}}

{{知识点5}}{{videoBitsPerSecond 选项：指定视频编码比特率（单位：bps）。例如 2500000 表示 2.5 Mbps。可与 audioBitsPerSecond 分别设定，或使用 bitsPerSecond 统一设定。实际使用的比特率可能与指定值有差异。}}

{{知识点6}}{{bitsPerSecond 选项：同时设定音频和视频的总比特率。可替代 audioBitsPerSecond 和 videoBitsPerSecond 单独使用。若同时指定了 bitsPerSecond 和其中一个分项，则未指定的分项使用 bitsPerSecond 的值。}}

{{知识点7}}{{audioBitrateMode 选项：设定音频编码比特率模式，可选 "constant"（恒定比特率 CBR）或 "variable"（可变比特率 VBR，默认值）。VBR 模式下复杂信号使用更多空间，简单信号使用更少空间，通常能获得更好的音质/文件大小比。}}

{{知识点8}}{{videoKeyFrameIntervalDuration 选项：指定视频编码中关键帧之间的标称时间间隔。与 videoKeyFrameIntervalCount 配合使用，浏览器根据这两个参数控制关键帧生成策略。注意这两个参数不能同时在构造函数中指定，否则会抛出 NotSupportedError。}}

{{知识点9}}{{videoKeyFrameIntervalCount 选项：指定视频编码中关键帧之间的帧数间隔。与 videoKeyFrameIntervalDuration 共同影响关键帧生成。关键帧间隔影响视频的随机访问能力和压缩效率——间隔越短，文件越大但 seek 更快。}}

{{知识点10}}{{MediaRecorder.mimeType 只读属性：返回创建 MediaRecorder 时选定的录制容器 MIME 类型。如果构造时未指定 mimeType，则返回浏览器选择的默认类型。该属性在录制过程中保持不变。}}

{{知识点11}}{{MediaRecorder.state 只读属性：返回 MediaRecorder 的当前状态，值为 "inactive"（未激活）、"recording"（录制中）或 "paused"（已暂停）。状态转换遵循严格规则：start() 从 inactive 转为 recording，pause() 从 recording 转为 paused，resume() 从 paused 转为 recording，stop() 从任何活跃状态转为 inactive。}}

{{知识点12}}{{MediaRecorder.stream 只读属性：返回构造函数中传入的 MediaStream 对象。通过该属性可访问原始媒体流，获取其轨道（tracks）信息，或在录制过程中动态操作流。}}

{{知识点13}}{{MediaRecorder.videoBitsPerSecond 只读属性：返回视频编码实际使用的比特率。可能与构造函数中指定的值不同，因为浏览器可能根据硬件能力和编解码器限制进行调整。}}

{{知识点14}}{{MediaRecorder.audioBitsPerSecond 只读属性：返回音频编码实际使用的比特率。可能与构造函数中指定的值不同，反映了浏览器的实际编码能力。}}

{{知识点15}}{{MediaRecorder.isTypeSupported() 静态方法：传入一个 MIME 类型字符串，返回布尔值指示当前浏览器是否支持该格式的录制。返回 true 表示支持，false 表示不支持。注意：即使返回 true，录制仍可能因资源不足而失败。常用检查类型包括 "video/webm"、"video/mp4"、"video/webm;codecs=vp8"、"audio/webm;codecs=opus"、"video/mp4;codecs=avc1.64003E,mp4a.40.2" 等。}}

{{知识点16}}{{MediaRecorder.start() 方法：开始录制媒体，将数据收集到一个或多个 Blob 对象中。可选 timeslice 参数（毫秒数），若指定则每隔该时间段触发一次 dataavailable 事件并创建新 Blob；若不指定则录制到单个 Blob 直到 stop() 调用或流结束。调用时 state 必须为 inactive，否则抛出 InvalidStateError。}}

{{知识点17}}{{start() 方法的 timeslice 参数详解：start(10000) 表示每 10 秒产生一个 Blob 分片。timeslice 不是精确值，实际间隔可能因浏览器任务调度、设备特性（如 Safari 暂停摄像头、Android Chrome 锁屏）而偏长。不应依赖 timeslice 和分片数来计算已录制时间，建议使用独立计时器。}}

{{知识点18}}{{start() 方法的异常处理：InvalidStateError——state 不为 inactive 时调用；NotSupportedError——流不活跃、轨道格式无法录制、或同时指定了 videoKeyFrameIntervalDuration 和 videoKeyFrameIntervalCount；SecurityError——MediaStream 被配置为禁止录制（如用户拒绝了设备权限）。}}

{{知识点19}}{{MediaRecorder.stop() 方法：停止录制，触发最终的 dataavailable 事件（包含最后的 Blob 数据），然后触发 stop 事件，state 转为 inactive。录制也可能因媒体流自然结束而停止。stop() 后不再产生新的录制数据。}}

{{知识点20}}{{MediaRecorder.pause() 方法：暂停录制，state 从 recording 转为 paused。暂停期间停止向当前 Blob 收集数据，但保持 MediaRecorder 活跃状态以便恢复。如果 state 已经是 inactive 则抛出 InvalidStateError；如果已经是 paused 则静默不做操作。}}

{{知识点21}}{{MediaRecorder.resume() 方法：从暂停状态恢复录制，state 从 paused 转为 recording。录制数据继续收集到当前或新的 Blob 中。如果 state 为 inactive 则抛出 InvalidStateError。}}

{{知识点22}}{{MediaRecorder.requestData() 方法：手动触发 dataavailable 事件，获取当前已录制数据的 Blob 快照，然后创建新 Blob 继续录制。适用于需要按需获取中间数据的场景。如果 state 为 inactive 则抛出 InvalidStateError。与 timeslice 的区别在于 requestData() 是手动触发而非定时触发。}}

{{知识点23}}{{dataavailable 事件：MediaRecorder 最核心的事件，类型为 BlobEvent。在四种情况下触发：(1) 媒体流自然结束；(2) 调用 stop()；(3) 调用 requestData()；(4) start(timeslice) 达到时间片。事件对象的 data 属性包含录制的 Blob 数据，timecode 属性为 DOMHighResTimeStamp 时间戳。}}

{{知识点24}}{{BlobEvent 接口：dataavailable 事件的事件对象类型。继承自 Event，包含 data 只读属性（Blob 类型，包含录制数据）和 timecode 只读属性（DOMHighResTimeStamp，表示当前分片第一个数据块与录制器首个 BlobEvent 的时间差）。注意第一个 BlobEvent 的 timecode 不一定为零。}}

{{知识点25}}{{error 事件：当发生导致录制停止的致命错误时触发。事件基于 MediaRecorderErrorEvent 接口，其 error 属性包含描述实际错误的 DOMException 对象。常见错误包括资源不足、编解码器故障、流轨道异常等。}}

{{知识点26}}{{start 事件：录制开始时触发，对应 start() 方法的成功调用。可用于更新 UI 状态（如按钮变红、显示录制指示器）。}}

{{知识点27}}{{stop 事件：录制结束时触发，无论是因 stop() 调用还是媒体流自然结束。通常在此事件中处理最终的录制数据（合并所有 chunks 为最终 Blob）。}}

{{知识点28}}{{pause 和 resume 事件：分别在暂停和恢复录制时触发。可用于更新 UI 中的暂停/恢复状态指示。pause 事件在 state 转为 paused 后触发，resume 事件在 state 转为 recording 后触发。}}

{{知识点29}}{{Blob 数据收集模式：常用模式是维护一个 chunks 数组，在 ondataavailable 中 push(e.data)，在 onstop 中用 new Blob(chunks, { type: mediaRecorder.mimeType }) 合并为最终 Blob。然后使用 URL.createObjectURL(blob) 创建可播放的 URL，赋给 <audio> 或 <video> 元素的 src。}}

{{知识点30}}{{MediaRecorder 与 getUserMedia 配合使用：典型流程为 getUserMedia({ audio: true, video: true }) 获取 MediaStream → new MediaRecorder(stream) 创建录制器 → start()/stop() 控制录制 → ondataavailable 收集数据 → 合并为 Blob → 上传或播放。视频分辨率和帧率在 getUserMedia 的 constraints 中设定，不在 MediaRecorder 中配置。}}

{{知识点31}}{{MediaRecorder 与 Canvas 元素配合：可通过 canvas.captureStream(fps) 获取 Canvas 的 MediaStream，然后用 MediaRecorder 录制 Canvas 动画。适用于录制 Canvas 游戏画面、动画、可视化效果等场景。fps 参数指定捕获帧率。}}

{{知识点32}}{{MediaRecorder 与 AudioContext 配合：可通过 MediaStreamDestination 将 Web Audio API 的音频处理结果输出为 MediaStream，再用 MediaRecorder 录制。适用于录制经过音频处理（混响、均衡器等）的音频输出。}}

{{知识点33}}{{常见 MIME 类型与编解码器支持：WebM 容器支持 VP8/VP9 视频编解码器和 Opus/Vorbis 音频编解码器；MP4 容器支持 H.264(AVC)/H.265(HEVC)/AV1 视频编解码器和 AAC/Opus 音频编解码器。不同浏览器支持的组合不同，应使用 isTypeSupported() 动态检测。}}

{{知识点34}}{{录制结果的上传与存储：录制完成后可将 Blob 通过 FormData 上传到服务器，或使用 IndexedDB 在客户端存储。大文件分片上传时可结合 timeslice 或 requestData() 实现流式上传，避免内存中积累过大数据。}}

{{知识点35}}{{MediaRecorder 的安全限制：录制需要用户授权（如 getUserMedia 的设备权限提示）。某些浏览器在用户拒绝权限时抛出 SecurityError。使用屏幕捕获（getDisplayMedia）录制时也有额外的安全限制。跨域媒体流可能无法录制。}}

{{知识点36}}{{浏览器兼容性与 Polyfill：MediaRecorder 自 2021 年 4 月起被所有主流浏览器支持（Chrome、Firefox、Safari、Edge）。Safari 14.5+ 支持 WebM 容器，Safari 对 MP4 容器支持更好。如需旧版浏览器支持可使用 RecordRTC 等 polyfill 库，但功能和性能会有差异。}}

{{知识点37}}{{内存管理最佳实践：长时间录制时应使用 timeslice 定期释放内存；不再需要的 Blob 应调用 URL.revokeObjectURL() 释放引用；大文件应及时上传或存储而非全部保留在内存中。录制过程中注意监控内存使用量，避免页面崩溃。}}

{{知识点38}}{{MediaRecorder 错误处理策略：应同时监听 error 事件和处理 start()/stop() 的同步异常。error 事件后浏览器会触发 dataavailable（携带已录制数据）再触发 stop。建议在 error 处理中保存已录制数据并优雅降级。}}

{{知识点39}}{{MediaRecorder 状态机：完整的状态转换为 inactive →(start)→ recording →(pause)→ paused →(resume)→ recording →(stop)→ inactive。在 inactive 状态调用 pause/resume/stop/requestData 会抛出 InvalidStateError。在 recording 状态再次调用 start() 也会抛出 InvalidStateError。}}

{{知识点40}}{{流式录制与实时传输：结合 timeslice 参数和 WebSocket/WebRTC，可实现音视频的实时流式传输。每收到 dataavailable 事件就将 Blob 分片发送到服务器，实现准直播效果。注意网络延迟和分片大小的平衡。}}
