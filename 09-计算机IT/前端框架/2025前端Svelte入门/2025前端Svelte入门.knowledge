# 2025前端Svelte入门
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}{{Svelte 是什么：Svelte 是一个用于构建 Web 用户界面的框架。它使用编译器将声明式的 HTML/CSS/JavaScript 组件转换为精简、高度优化的原生 JavaScript 代码。与 React、Vue 等框架不同，Svelte 在构建时（而非运行时）完成大部分工作，因此产出的代码更小、更快。}}

{{知识点2}}{{Svelte 5 与 Runes 系统：Svelte 5 引入了 Runes（符文）系统，这是一种全新的响应式原语。Runes 使用 $ 前缀的特殊语法（如 $state、$derived、$effect）来声明响应式状态，取代了 Svelte 4 中的 let 赋值自动响应式和 $: 标签语法。Runes 使响应式行为更加显式和可预测。}}

{{知识点3}}{{$state 基础用法：$state rune 用于创建响应式状态。语法为 let count = $state(0)，之后 count 就是一个普通的数字变量，可以直接赋值 count++ 来更新，UI 会自动响应变化。不需要调用任何特殊 API，变量就是变量本身。}}

{{知识点4}}{{$state 深度响应式代理：当 $state 用于数组或简单对象时，结果是深度响应式状态代理（Deep Reactive Proxy）。Svelte 使用 JavaScript Proxy 来拦截属性的读写操作，包括数组的 push 等方法，从而触发细粒度的 UI 更新。例如 todos[0].done = true 会自动更新相关 UI。}}

{{知识点5}}{{$state.raw 用法：当你不希望对象或数组被深度代理时，可以使用 $state.raw。声明为 raw 的状态不能被修改（mutate），只能整体替换（reassign）。这对于大型数组和对象可以提升性能，因为避免了代理的开销。}}

{{知识点6}}{{$state.snapshot 快照：使用 $state.snapshot 可以获取深度响应式代理的静态快照。当需要将状态传递给不接受 Proxy 的外部库或 API（如 structuredClone）时非常有用。}}

{{知识点7}}{{$state.eager 立即更新：当状态变化后遇到 await 表达式时，UI 更新可能被延迟。使用 $state.eager(value) 可以让 UI 在状态变化时立即更新，适合需要即时视觉反馈的场景（如导航栏高亮）。}}

{{知识点8}}{{$derived 派生状态：$derived rune 用于声明从其他状态派生的值。语法为 let doubled = $derived(count * 2)，当 count 变化时 doubled 自动重新计算。表达式内不能有副作用，Svelte 会禁止在派生表达式中修改状态。}}

{{知识点9}}{{$derived.by 复杂派生：当派生逻辑较复杂不适合短表达式时，使用 $derived.by 接受一个函数。例如 let total = $derived.by(() => { let t = 0; for (const n of numbers) { t += n; } return t; })。$derived(expression) 等价于 $derived.by(() => expression)。}}

{{知识点10}}{{$derived 解构支持：对 $derived 使用解构赋值时，所有变量都具有响应性。let { a, b, c } = $derived(stuff()) 等价于分别用 $derived 获取 stuff().a、stuff().b、stuff().c。}}

{{知识点11}}{{$effect 副作用：$effect rune 用于在状态更新时运行副作用函数，如调用第三方库、操作 canvas、发起网络请求等。它只在浏览器端运行，不在服务端渲染时执行。Svelte 自动追踪 effect 内部同步读取的状态作为依赖。}}

{{知识点12}}{{$effect 生命周期：effect 在组件挂载到 DOM 后运行，状态变化后在微任务中重新执行。多次状态变化会批量处理（batch），且在 DOM 更新之后执行。effect 可以返回一个清理函数（teardown），在重新运行或组件销毁前执行。}}

{{知识点13}}{{$effect.pre 前置副作用：$effect.pre 与 $effect 类似，但在 DOM 更新之前运行。适用于需要在 DOM 更新前执行的逻辑，如自动滚动到底部的聊天消息列表。}}

{{知识点14}}{{$props 组件属性：组件的输入称为 props（属性）。使用 $props rune 接收父组件传入的属性，通常配合解构使用：let { adjective } = $props()。支持默认值、重命名、剩余属性等解构语法。}}

{{知识点15}}{{$props 类型安全：可以通过 TypeScript 类型注解为 props 添加类型安全。例如 let { adjective }: { adjective: string } = $props()，或使用 interface 定义 Props 类型。推荐添加类型以方便使用者发现组件接受哪些 props。}}

{{知识点16}}{{$props.id() 唯一标识：$props.id() 生成当前组件实例的唯一 ID，在服务端渲染和客户端水合时保持一致。用于通过 for 和 aria-labelledby 等属性关联表单元素。}}

{{知识点17}}{{$bindable 双向绑定：默认情况下 props 是单向的（父到子）。使用 $bindable rune 标记 prop 为可绑定，允许父组件通过 bind: 指令实现双向数据流。例如 let { value = $bindable() } = $props()，父组件用 bind:value={message} 绑定。}}

{{知识点18}}{{$inspect 调试工具：$inspect rune 类似 console.log，但会在参数变化时重新执行。仅在开发环境有效，生产构建中会被移除。支持 .with((type, value) => {}) 回调自定义处理。$inspect.trace() 可以追踪函数因哪些响应式状态变化而重新执行。}}

{{知识点19}}{{Svelte 组件文件结构：Svelte 组件是 .svelte 文件，包含三个可选部分：<script>（逻辑）、<style>（样式）和标记（模板）。样式默认是组件级作用域的（scoped），不会影响其他组件。}}

{{知识点20}}{{模板中的文本表达式：在模板中使用花括号 {expression} 嵌入 JavaScript 表达式。值为 null 或 undefined 时会被忽略，其他值会被转为字符串并自动转义以防止 XSS 攻击。需要渲染原始 HTML 时使用 {@html} 标签。}}

{{知识点21}}{{元素属性与布尔属性：属性值可以包含 JavaScript 表达式，如 <a href="page/{p}">。布尔属性在值为 truthy 时包含，falsy 时排除。其他属性在值为 nullish（null 或 undefined）时排除。属性名和值相同时可简写：{name} 等价于 name={name}。}}

{{知识点22}}{{事件监听：通过 on 前缀的属性监听 DOM 事件，如 onclick={() => console.log('clicked')}。事件属性区分大小写：onclick 监听 click 事件，onClick 监听 Click 事件。Svelte 使用事件委托（event delegation）技术优化性能。}}

{{知识点23}}{{事件委托机制：Svelte 对 click、input、keydown、mousedown 等常见事件使用事件委托，在应用根节点设置单一事件监听器来处理所有子元素的事件。手动派发委托事件时需要设置 bubbles: true。建议使用 svelte/events 的 on 函数而非原生 addEventListener。}}

{{知识点24}}{#{#if} 条件渲染：使用 {#if condition}...{:else if}...{:else}...{/if} 进行条件渲染。当条件为真时渲染对应区块，支持多个 else if 分支和可选的 else 兜底。}}

{{知识点25}}{#{#each} 列表渲染：使用 {#each items as item, index}...{/each} 遍历数组。支持解构、索引、key 表达式（如 {#each items as item (item.id)}）用于高效的 DOM 复用。}}

{{知识点26}}{#{#await} 异步渲染：使用 {#await promise}...{:then value}...{:catch error}...{/await} 处理 Promise。在等待时显示 loading 状态，解析后显示结果，出错时显示错误信息。}}

{{知识点27}}{#{#key} 键控区块：{#key expression}...{/key} 在表达式值变化时销毁并重新创建内部内容，常用于触发过渡动画。}}

{{知识点28}}{{SvelteKit 应用框架：SvelteKit 是 Svelte 的配套全栈应用框架，类似于 Next.js 之于 React。提供文件系统路由、服务端渲染（SSR）、静态站点生成（SSG）、API 路由、数据加载等功能。}}

{{知识点29}}{{Svelte 与 React/Vue 的区别：Svelte 是编译时框架，无需虚拟 DOM，编译后产出的代码体积小、运行时开销低。React 使用虚拟 DOM 和 JSX，Vue 使用响应式代理和模板。Svelte 的代码更接近原生 JavaScript，学习曲线更平缓。}}

{{知识点30}}{{Svelte 4 到 Svelte 5 的迁移：Svelte 5 引入 Runes 替代了 Svelte 4 的隐式响应式。Svelte 4 中 let count = 0 自动响应式，Svelte 5 中需要显式 let count = $state(0)。$: doubled = count * 2 变为 let doubled = $derived(count * 2)。on:click 变为 onclick。}}

{{知识点31}}{{Svelte 响应式模型——Push-Pull 机制：Svelte 使用 push-pull（推-拉）响应式模型。状态更新时，所有依赖项被立即通知（push），但派生值只在实际被读取时才重新计算（pull）。如果新值与旧值引用相同，下游更新会被跳过。}}

{{知识点32}}{{Svelte 中的类与状态：Svelte 5 支持在类字段中使用 $state。编译器会将字段转换为原型上的 getter/setter，引用私有字段。在类方法中需要注意 this 的绑定问题，建议使用箭头函数定义方法。}}

{{知识点33}}{{Svelte 内置响应式类：Svelte 提供了 Set、Map、Date、URL 等内置类的响应式实现，可从 svelte/reactivity 导入使用。这些类在被读写时会触发响应式更新。}}

{{知识点34}}{{Svelte 组件间通信方式：Svelte 支持多种组件通信方式：通过 props 向下传递数据、通过回调函数向上传递事件、通过 $bindable 实现双向绑定、通过 Context API（setContext/getContext）跨层级传递数据。}}

{{知识点35}}{{Svelte 的编译优势：Svelte 在编译时将组件转换为高效的命令式 DOM 操作代码。不需要虚拟 DOM diffing，没有运行时框架开销。编译后的代码直接操作真实 DOM，因此性能更优、包体积更小。}}

{{知识点36}}{{Svelte 模板中的组件使用：在模板中使用组件时，大写标签表示组件（如 <Widget />），小写标签表示 HTML 元素。使用点表示法支持命名空间组件（如 <my.stuff />）。组件通过 import 导入后直接使用。}}

{{知识点37}}{{Svelte 的样式作用域：<style> 标签内的 CSS 默认是组件级作用域的，Svelte 编译器会自动添加唯一的选择器来限定样式范围。使用 :global() 修饰符可以将样式提升为全局作用域。}}

{{知识点38}}{{Svelte 的 slot 插槽机制：Svelte 5 使用 snippet（片段）替代了传统的 slot 机制。通过 {#snippet} 定义可复用的模板片段，通过 {children} 等方式传递给子组件。这是 Svelte 5 组件组合的新范式。}}

{{知识点39}}{{Svelte 项目创建与开发：使用 npm create svelte@latest 创建新项目（基于 SvelteKit）。开发服务器使用 npm run dev，构建使用 npm run build。也支持 Vite 作为构建工具，提供快速的热模块替换（HMR）。}}

{{知识点40}}{{Svelte 的 untrack 函数：在 $derived 或 $effect 中，使用 untrack(fn) 可以读取状态而不将其注册为依赖。这在你不希望某个状态变化触发重新计算时非常有用。}}
