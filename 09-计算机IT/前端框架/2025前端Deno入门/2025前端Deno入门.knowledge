# 2025前端Deno入门
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}
Deno 是一个开源的 JavaScript、TypeScript 和 WebAssembly 运行时，由 Ryan Dahl（Node.js 的创始人）创建。它基于 V8 引擎、Rust 和 Tokio 构建，提供安全默认设置和出色的开发体验。名称发音为 /ˈdiːnoʊ/（dee-no）。

{{知识点2}}
Deno 的核心优势包括：原生支持 TypeScript（零配置）、默认安全的沙箱权限模型、内置完整工具链（linter/formatter/test runner）、完全兼容 Node.js 和 npm、基于 Web 标准 API。

{{知识点3}}
安装 Deno 的方式多样：macOS/Linux 使用 `curl -fsSL https://deno.land/install.sh | sh`，Windows 使用 `irm https://deno.land/install.ps1 | iex`。也支持 Homebrew（`brew install deno`）、npm（`npm install -g deno`）、Scoop、Chocolatey 等包管理器。Deno 是单个二进制可执行文件，无外部依赖。

{{知识点4}}
验证安装：运行 `deno --version` 会输出 Deno 版本号。运行 `deno help` 可查看所有命令和用法说明。

{{知识点5}}
运行 TypeScript 文件：直接使用 `deno run script.ts` 即可运行，无需预编译或额外配置。Deno 内置 TypeScript 编译器会自动将 TS 编译为 JS。

{{知识点6}}
TypeScript 类型检查：使用 `deno check module.ts` 进行类型检查而不执行代码。`deno run --check module.ts` 在运行前先类型检查。`deno run --check=all module.ts` 会同时检查远程模块和 npm 包。

{{知识点7}}
Deno 默认不进行类型检查以提升执行速度。`deno test` 命令默认启用类型检查，可用 `--no-check` 跳过。

{{知识点8}}
Deno 的安全模型：默认情况下，程序无文件读写、网络访问、环境变量访问或子进程权限。必须通过命令行标志（如 `--allow-read`、`--allow-write`、`--allow-net`、`--allow-env`、`--allow-run`）显式授权。

{{知识点9}}
权限标志详解：`--allow-read` 允许文件读取，`--allow-write` 允许文件写入，`--allow-net` 允许网络访问，`--allow-env` 允许环境变量访问，`--allow-run` 允许运行子进程。可限制为特定路径/域名，如 `--allow-read=/tmp`。

{{知识点10}}
`--allow-all`（或 `-A`）授予所有权限，禁用安全沙箱，等同于 Node.js 的权限模型，应谨慎使用。`--deny-read`、`--deny-net` 等拒绝标志优先级高于允许标志。

{{知识点11}}
Deno 使用 ECMAScript 模块（ESM）作为默认模块系统。本地导入必须包含完整文件扩展名，如 `import { add } from "./calc.ts"`，不能省略 `.ts`。

{{知识点12}}
导入第三方模块：使用 `jsr:` 前缀导入 JSR 包（如 `import { camelCase } from "jsr:@luca/cases@1.0.0"`），使用 `npm:` 前缀导入 npm 包（如 `import { say } from "npm:cowsay@1.6.0"`），也可使用 HTTPS URL 导入。

{{知识点13}}
JSR（JavaScript Registry）是 Deno 推荐的包注册表，支持 TypeScript 优先的包发布。使用 `deno publish` 命令可将模块发布到 JSR。

{{知识点14}}
npm 兼容性：Deno 完全兼容 Node.js 和 npm。使用 `npm:` 说明符直接导入 npm 包，无需 `npm install`，不会创建 `node_modules` 目录。Deno 支持 `package.json`，可声明依赖、使用 scripts。

{{知识点15}}
Node.js 内置模块兼容：使用 `node:` 前缀导入 Node 内置模块，如 `import path from "node:path"`、`import * as os from "node:os"`。Deno 提供完整的 Node.js API 兼容层。

{{知识点16}}
Import Attributes：Deno 支持 `with { type: "json" }` 语法导入 JSON 文件。从 Deno 2.4 起实验性支持 `with { type: "text" }` 和 `with { type: "bytes" }` 导入文本和字节模块。

{{知识点17}}
WebAssembly 支持：Deno 可直接导入 `.wasm` 模块，如 `import { add } from "./add.wasm"`。同时支持 WebAssembly 的完整 API。

{{知识点18}}
数据 URL 导入：Deno 支持 `data:` URL 方案导入模块，适用于测试、原型设计或动态生成模块。格式为 `data:[<media type>][;base64],<data>`。

{{知识点19}}
配置文件 `deno.json`（或 `deno.jsonc`）用于配置 TypeScript 编译器、linter、formatter 等工具。Deno 会自动检测当前目录或父目录中的配置文件。

{{知识点20}}
Import Map：`deno.json` 中的 `imports` 字段可定义依赖映射，如 `"@std/assert": "jsr:@std/assert@^1.0.0"`。支持自定义路径映射，如 `"@/": "./"` 实现简洁导入。

{{知识点21}}
Tasks 系统：`deno.json` 中的 `tasks` 字段定义自定义命令，通过 `deno task <name>` 执行，类似 `package.json` 的 `scripts`。示例：`"start": "deno run --allow-net --watch=static/,routes/,data/ dev.ts"`。

{{知识点22}}
内置测试运行器：使用 `Deno.test()` 函数定义测试，`deno test` 命令运行测试。自动发现匹配 `{*_,*.,}test.{ts,tsx,mts,js,mjs,jsx}` 模式的文件。支持 `--parallel` 并行运行测试。

{{知识点23}}
测试步骤（Test Steps）：使用 `t.step()` 将测试拆分为更小的部分，适合设置和清理操作。测试钩子包括 `Deno.test.beforeAll()`、`Deno.test.beforeEach()`、`Deno.test.afterEach()`、`Deno.test.afterEach()`。

{{知识点24}}
断言库：标准库提供 `jsr:@std/assert`（如 `assertEquals`）和 `jsr:@std/expect`（Jest 风格的 `expect().toBe()`）两种断言方式。

{{知识点25}}
内置 Linter：`deno lint` 命令分析代码中的潜在错误和风格问题，支持 ESLint 推荐规则集。`deno lint src/` 可指定目录。可在 `deno.json` 中配置自定义规则。

{{知识点26}}
内置 Formatter：`deno fmt` 命令基于 dprint 引擎自动格式化代码。`deno fmt --check` 检查格式但不修改文件，适合 CI 流水线集成。支持配置选项如 `bracePosition`、`lineWidth` 等。

{{知识点27}}
编译为独立可执行文件：`deno compile --allow-net server.ts` 将 Deno 程序编译为单个可执行文件，支持交叉编译到其他平台。无需安装 Deno 运行时即可运行。

{{知识点28}}
REPL 交互环境：`deno repl` 启动读取-求值-打印循环，支持 JavaScript 和 TypeScript 交互式编程。`deno eval "console.log('hello')"` 可执行单行脚本。

{{知识点29}}
依赖管理命令：`deno add` 添加依赖，`deno remove` 移除依赖，`deno install` 安装依赖或脚本，`deno outdated` 查看过时依赖，`deno audit` 审计依赖安全性。

{{知识点30}}
Web 标准 API：Deno 优先实现 Web 标准 API（如 Fetch API、WebSocket、Streams API、URL、FormData 等），最大化浏览器和服务器之间的代码复用。Deno 积极参与 TC39 和 WinterCG 标准制定。

{{知识点31}}
HTTP 服务器：使用 `Deno.serve()` 创建 HTTP 服务器，如 `Deno.serve(req => new Response("Hello!"))`。支持 HTTPS、WebSocket、HTTP/2 和自动响应体压缩。

{{知识点32}}
Deno Deploy：Deno 官方云部署平台，提供闪电般的边缘部署能力。Deno 也可部署到 AWS Lambda、Google Cloud Run、Cloudflare Workers 等主流云平台。

{{知识点33}}
`deno compile` 交叉编译：支持编译到不同平台（如从 macOS 编译 Linux 可执行文件），使用 `--target` 指定目标平台。生成的二进制文件体积小、启动快。

{{知识点34}}
Watch 模式：`deno run --watch script.ts` 在文件变更时自动重新运行，适合开发调试。`deno test --watch` 在测试开发中自动重跑测试。

{{知识点35}}
`deno doc` 命令为模块生成 API 文档，支持 JSDoc 注释。`deno info` 检查 ES 模块及其所有依赖的详细信息。

{{知识点36}}
`deno jupyter` 命令支持在 Jupyter Notebook 中使用 Deno，适合数据科学和交互式编程场景。

{{知识点37}}
`deno create` 命令从模板快速创建新项目，类似 `create-react-app`。`deno init` 创建新的 Deno 项目基础结构。

{{知识点38}}
`deno upgrade` 命令将 Deno 升级到最新版本。`deno x` 命令运行 npm 或 JSR 包，类似 npx。

{{知识点39}}
VS Code 集成：通过 Deno 扩展获得 LSP 支持，包括智能提示、类型检查、自动修复等。在 `.vscode/settings.json` 中配置 `"deno.enablePaths": ["./deno.json"]`。

{{知识点40}}
`links` 字段：`deno.json` 中的 `links` 字段允许用本地包覆盖依赖，类似 npm link。适合依赖调试、私有库开发和兼容性问题解决。

{{知识点41}}
CommonJS 支持：使用 `.cjs` 扩展名告知 Deno 该模块使用 CommonJS 系统。`deno.json` 中的 `package.json` 的 `type` 字段也会影响模块解析方式。

{{知识点42}}
Coverage 测试覆盖率：`deno coverage` 命令生成测试覆盖率报告，帮助了解代码测试覆盖情况。

{{知识点43}}
Benchmark 基准测试：`deno bench` 命令运行基准测试，用于性能分析和优化。

{{知识点44}}
Docker 支持：Deno 提供官方 Docker 镜像，支持容器化部署。项目地址：github.com/denoland/deno_docker。

{{知识点45}}
LSP（Language Server Protocol）：`deno lsp` 命令提供语言服务器协议集成，支持编辑器中的智能代码补全、错误提示、代码重构等功能。
