# 2025前端前端CSS Grid详解
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}
CSS Grid 是一种二维网格布局系统，专为解决网页布局问题而设计。与 Flexbox 的一维布局不同，Grid 可以同时控制行和列，是第一个专门为页面布局创建的 CSS 模块。它彻底改变了过去依赖表格、浮动、定位等 hack 方式进行布局的历史。
{{知识点2}}
Grid Container（网格容器）是应用了 display: grid 或 display: inline-grid 的元素，它是所有 Grid Item 的直接父级。display: grid 生成块级网格，display: inline-grid 生成行内级网格。只有直接子元素才会成为网格项，更深层的后代不受影响。
{{知识点3}}
Grid Item（网格项）是 Grid Container 的直接子元素。注意：只有直接子元素才是网格项，孙元素不是。如果需要让非直接子元素参与网格布局，需要使用 Subgrid 或将其父元素也设为 Grid Container。
{{知识点4}}
Grid Line（网格线）是构成网格结构的分隔线，分为垂直的列网格线和水平的行网格线。N 列的网格有 N+1 条列线，N 行的网格有 N+1 条行线。网格线可以使用正数从左到右/从上到下编号，也可以使用负数（-1 表示最后一条线）。
{{知识点5}}
Grid Track（网格轨道）是两条相邻网格线之间的空间，即网格的行或列。轨道大小由 grid-template-columns 和 grid-template-rows 定义，可以使用固定长度、百分比或 fr 单位。
{{知识点6}}
Grid Cell（网格单元格）是两条相邻行线和两条相邻列线之间的空间，是网格的最小单元。一个网格单元格由四条网格线围成。
{{知识点7}}
Grid Area（网格区域）是由四条网格线围成的矩形空间，可以包含任意数量的网格单元格。使用 grid-template-areas 命名区域后，可以通过 grid-area 属性将元素放置到指定区域。
{{知识点8}}
grid-template-columns 和 grid-template-rows 用于定义网格的列和行轨道。值可以是长度、百分比或 fr 单位。例如 grid-template-columns: 1fr 2fr 1fr 将容器分为三列，中间列宽度是两侧的两倍。fr 单位表示剩余空间的份额，在非弹性项（如 50px）计算后再分配。
{{知识点9}}
repeat() 函数用于简化重复的轨道定义。例如 repeat(3, 1fr) 等同于 1fr 1fr 1fr。repeat() 支持 auto-fill 和 auto-fit 关键字，用于响应式布局：repeat(auto-fill, minmax(200px, 1fr)) 可以根据容器宽度自动创建合适数量的列。
{{知识点10}}
minmax() 函数定义轨道的最小和最大尺寸范围。例如 minmax(100px, 1fr) 表示轨道最小 100px，最大占剩余空间的 1 份。常与 repeat(auto-fill/auto-fit) 配合实现响应式网格。
{{知识点11}}
auto-fill 与 auto-fit 的区别：auto-fill 会尽可能多地创建轨道（即使空轨道也会占位），auto-fit 会折叠空轨道并将剩余空间分配给已有项目。在响应式布局中，auto-fit 通常更实用，因为它会让项目自动填满容器宽度。
{{知识点12}}
grid-template-areas 通过命名区域定义网格模板。每个字符串代表一行，相同名称的区域会合并为一个网格区域。句点(.)表示空单元格。例如：grid-template-areas: "header header" "main sidebar" "footer footer"。每行的单元格数量必须相同。
{{知识点13}}
命名网格线使用方括号语法定义。例如 grid-template-columns: [start] 1fr [center] 1fr [end]。一条线可以有多个名称，如 [row1-end row2-start]。命名线可以通过名称引用，比数字编号更语义化。
{{知识点14}}
grid 是所有网格属性的简写形式，可同时设置 grid-template-rows、grid-template-columns、grid-template-areas、grid-auto-rows、grid-auto-columns 和 grid-auto-flow。注意：grid-template 不会重置隐式网格属性，而 grid 会。
{{知识点15}}
grid-auto-flow 控制自动放置算法的方向和模式。值为 row（默认）时按行填充，column 时按列填充。添加 dense 关键字（如 grid-auto-flow: row dense）会启用"密集"打包算法，尝试回填之前跳过的空位，可能导致视觉顺序与 DOM 顺序不一致。
{{知识点16}}
隐式网格（Implicit Grid）：当网格项超出显式定义的轨道数量时，浏览器会自动创建额外的轨道。grid-auto-rows 和 grid-auto-columns 控制这些隐式轨道的大小，默认值为 auto（根据内容自适应）。
{{知识点17}}
grid-column-start、grid-column-end、grid-row-start、grid-row-end 控制网格项在网格中的位置。可以使用行号、行名或 span 关键字。例如 grid-column: 1 / 3 表示从第 1 条列线到第 3 条列线（跨 2 列）。
{{知识点18}}
grid-column 和 grid-row 是简写属性。grid-column: 1 / 3 等同于 grid-column-start: 1; grid-column-end: 3。使用 span 关键字可以指定跨越的轨道数，如 grid-column: 2 / span 3 表示从第 2 列开始跨越 3 列。
{{知识点19}}
grid-area 是 grid-row-start、grid-column-start、grid-row-end、grid-column-end 的简写，顺序为 row-start / column-start / row-end / column-end。当与 grid-template-areas 配合时，可以只写区域名称，如 grid-area: header。
{{知识点20}}
justify-items 沿行轴（水平方向）对齐所有网格项的内容。可选值：stretch（默认，填满单元格宽度）、start、end、center。这是容器级属性，影响所有子项。
{{知识点21}}
align-items 沿列轴（垂直方向）对齐所有网格项的内容。可选值：stretch（默认）、start、end、center、baseline。与 justify-items 一起控制网格项在单元格内的对齐方式。
{{知识点22}}
place-items 是 align-items 和 justify-items 的简写。place-items: center 可以快速实现元素在单元格内完全居中。这是最简洁的 CSS 居中方式之一。
{{知识点23}}
justify-content 和 align-content 控制整个网格在容器内的对齐方式（当网格总尺寸小于容器时生效）。justify-content 控制行轴，align-content 控制列轴。可选值包括 start、end、center、stretch、space-around、space-between、space-evenly。
{{知识点24}}
place-content 是 align-content 和 justify-content 的简写。例如 place-content: center 将整个网格在容器内居中。
{{知识点25}}
justify-self 和 align-self 是项级属性，用于覆盖单个网格项的对齐方式，优先级高于容器的 justify-items 和 align-items。适合需要个别项特殊对齐的场景。
{{知识点26}}
gap（行间沟槽）属性设置网格轨道之间的间距。row-gap 设置行间距，column-gap 设置列间距。gap: 10px 20px 表示行间距 10px，列间距 20px。gap 不会影响网格项与容器边缘的距离。
{{知识点27}}
Subgrid（子网格）是 CSS Grid Level 2 的特性，允许嵌套网格继承父网格的轨道定义。使用 grid-template-columns: subgrid 或 grid-template-rows: subgrid，嵌套网格会使用父网格的轨道尺寸和线名称，解决了嵌套网格对齐困难的问题。
{{知识点28}}
Subgrid 的间隔继承：子网格默认继承父网格的 gap 值，但可以通过在子网格上设置 gap 属性来覆盖。将子网格的 gap 设为 0 会消除间距，类似于应用负 margin。
{{知识点29}}
Subgrid 的命名线继承：父网格的命名网格线会传递给子网格。子网格也可以定义自己的命名线，两者都会生效。例如 grid-template-columns: subgrid [line1] [line2] 会保留父网格的命名线并添加子网格自己的线名。
{{知识点30}}
Subgrid 的隐式网格限制：在使用 subgrid 的维度上不会创建隐式网格轨道。如果子网格中的项目数量超过父网格分配的轨道数，多余的项目会堆积在最后一个轨道中。可以通过只在一个维度使用 subgrid、另一个维度使用 grid-auto-rows 来解决。
{{知识点31}}
CSS Grid vs Flexbox 的选择：Grid 是二维布局（行+列），适合整体页面布局；Flexbox 是一维布局（行或列），适合组件内部的一维排列。两者可以且应该结合使用——Grid 用于宏观布局，Flexbox 用于微观组件。
{{知识点32}}
命名网格区域实现经典布局：使用 grid-template-areas 可以直观地定义页面结构。经典的圣杯布局示例：grid-template-areas: "header header header" "nav main aside" "footer footer footer"，代码即文档，一目了然。
{{知识点33}}
响应式网格布局：使用 repeat(auto-fit, minmax(250px, 1fr)) 可以创建无需媒体查询的响应式网格。当容器宽度变化时，列数会自动调整，每列最小 250px，多余空间平均分配。
{{知识点34}}
网格项的层叠与重叠：网格项可以占据相同区域从而重叠，类似 CSS 定位。使用 z-index 控制层叠顺序。这使得 Grid 可以实现复杂的视觉效果，如卡片叠加、仪表盘布局等。
{{知识点35}}
grid 简写中的 auto-flow 语法：grid: auto-flow dense 100px / 1fr 2fr 等同于分别设置 grid-auto-flow: row dense、grid-auto-rows: 100px、grid-template-columns: 1fr 2fr。斜杠左侧控制行，右侧控制列。
{{知识点36}}
fit-content() 函数用于限制轨道的最大尺寸。例如 fit-content(300px) 表示轨道大小由内容决定，但不超过 300px。适合内容驱动但需要上限的场景。
{{知识点37}}
min-content 和 max-content 是 Grid 的关键字值。min-content 是内容不溢出的最小宽度，max-content 是内容自然展开的最大宽度。用于 grid-template-columns/rows 可以实现内容自适应的轨道大小。
{{知识点38}}
Grid 布局的可访问性注意事项：使用 Grid 的视觉重排功能（如 order、grid-column/grid-row 重排）时，DOM 顺序和视觉顺序可能不一致，这会对屏幕阅读器和键盘导航造成影响。应确保逻辑顺序在 DOM 中正确，仅用 Grid 做视觉增强。
{{知识点39}}
Firefox DevTools 的 Grid Inspector 是最强大的 Grid 调试工具，可以可视化网格线、轨道、区域，显示行号和线名称。Chrome DevTools 也逐步增加了 Grid 调试功能。按 F12 选择容器元素即可查看网格覆盖。
{{知识点40}}
Grid Garden（cssgridgarden.com）是学习 CSS Grid 的交互式游戏，通过种植花园的方式练习网格属性。Grid by Example（gridbyexample.com）提供大量实例和视频教程。这些是掌握 Grid 的最佳学习资源。
{{知识点41}}
显式网格与隐式网格：显式网格由 grid-template-columns/rows 定义，隐式网格由 grid-auto-columns/rows 定义。当项目被放置到显式网格之外时，浏览器会创建隐式轨道。理解两者的关系是掌握 Grid 的关键。
{{知识点42}}
Grid 的 order 属性可以改变网格项的视觉顺序（默认值为 0）。order 值越小越靠前。注意：这只影响视觉呈现，不影响 DOM 顺序和可访问性工具的阅读顺序。
{{知识点43}}
嵌套网格（Nested Grid）：一个网格项可以同时是另一个网格的容器，通过在其上设置 display: grid 创建。嵌套网格的轨道与父网格完全独立，不会自动对齐。如果需要对齐，应使用 Subgrid。
{{知识点44}}
Grid 与绝对定位：当网格项设置为 position: absolute 时，它会脱离网格流。如果其父元素（网格容器）也是定位上下文（position: relative），则绝对定位的项目不会影响网格布局。
{{知识点45}}
Grid 布局中的 auto 值：在 grid-template 中，auto 表示轨道大小由内容决定。在隐式网格中，grid-auto-rows: auto 意味着轨道高度会适应最高的内容。结合 minmax() 使用（如 minmax(auto, 300px)）可以创建更灵活的布局。
{{知识点46}}
CSS Grid Module Level 2 引入了 Subgrid，这是 2023 年底各主流浏览器全面支持的特性。Level 2 还在讨论 Masonry Layout（瀑布流布局），通过 grid-template-rows: masonry 实现，目前仅 Firefox 实验性支持。
{{知识点47}}
Grid 的密集填充算法（dense packing）通过 grid-auto-flow: dense 启用，会回填之前的空位以减少空白。代价是视觉顺序可能与 DOM 顺序不一致，在需要保持顺序的场景（如表单、列表）中应避免使用。
{{知识点48}}
使用 Grid 实现 12 列栅格系统：grid-template-columns: repeat(12, 1fr) 创建 12 等分列，然后通过 grid-column: span N 控制每项占据的列数。这是传统 Bootstrap 栅格的现代 CSS 替代方案，无需额外框架。
{{知识点49}}
Grid 布局性能优化：避免在大型网格中使用过于复杂的 repeat() 嵌套。使用 will-change: transform 提示浏览器进行 GPU 加速。对于动态添加/删除项目的场景，使用 grid-auto-flow: dense 时要注意回流开销。
{{知识点50}}
Grid 的实用模式——等高列：在传统布局中实现等高列需要 hack，而 Grid 默认所有网格项在同一行内高度一致。只需设置 display: grid 和 grid-template-columns，各列自动等高，无需额外代码。
