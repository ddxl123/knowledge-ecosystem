# 2025前端WebXR入门
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}{{WebXR是一组标准的集合，用于支持在网页中渲染3D场景到VR/AR硬件设备上。WebXR Device API是其核心实现，负责管理输出设备选择、以适当帧率渲染3D场景，以及处理输入控制器产生的运动向量。WebXR同时支持虚拟现实（VR）和增强现实（AR），是已废弃的WebVR API的继任者。}}

{{知识点2}}{{WebXR Device API的核心能力包括：1）查找兼容的VR或AR输出设备；2）以适当帧率渲染3D场景到设备；3）可选地将输出镜像到2D显示器；4）创建表示输入控制器运动的向量。WebXR本身不是渲染技术，不提供3D数据管理或渲染功能，这些需要配合WebGL等图形API使用。}}

{{知识点3}}{{XRSystem是WebXR的入口接口，通过navigator.xr访问。它提供两个关键方法：isSessionSupported(mode)用于检测浏览器是否支持指定会话模式，requestSession(mode, options)用于请求创建新的XR会话。XRSystem还提供devicechange事件，当可用XR设备集合发生变化时触发。该接口早期版本中名为XR。}}

{{知识点4}}{{WebXR会话有三种模式：1）immersive-vr（沉浸式VR）：完全由数字创建的虚拟环境，需要独占VR设备；2）immersive-ar（沉浸式AR）：将虚拟内容叠加到真实世界视图上；3）inline（内联）：在普通浏览器窗口中显示，不要求专用设备。创建会话前应使用isSessionSupported()检测支持情况。}}

{{知识点5}}{{XRSession代表一个正在进行的XR会话，是WebXR交互的核心对象。其主要属性包括：inputSources（当前输入源列表）、renderState（渲染状态配置）、visibilityState（可见性状态）、enabledFeatures（已启用特性列表）。主要方法包括：requestAnimationFrame()、requestReferenceSpace()、updateRenderState()、end()。}}

{{知识点6}}{{WebXR要求安全上下文（HTTPS），这是因为在XR环境中，恶意网站可能通过伪造场景来误导用户做出危险动作。所有WebXR API调用都必须在安全上下文中进行，本地开发时localhost被视为安全上下文。}}

{{知识点7}}{{XRSession.requestAnimationFrame(callback)是WebXR的核心渲染调度方法，类似于window.requestAnimationFrame()但专为XR设计。浏览器在准备好绘制XR帧时调用回调函数，回调函数负责渲染场景并再次调用requestAnimationFrame()以继续动画循环。回调接收一个时间戳和XRFrame对象。}}

{{知识点8}}{{XRFrame对象在requestAnimationFrame回调中提供，包含渲染单帧所需的全部信息。其关键方法：getViewerPose(referenceSpace)返回XRViewerPose描述观看者位姿；getPose(sourceSpace, destinationSpace)计算两个空间之间的相对位姿；createAnchor()创建空间锚点；getHitTestResults()获取命中测试结果。}}

{{知识点9}}{{XRReferenceSpace定义了WebXR中的坐标参考系。通过session.requestReferenceSpace(type)创建。有五种类型：viewer（跟随观看者）、local（以观看者初始位置为原点）、local-floor（以地面为y=0）、bounded-floor（有边界限制的地面）、unbounded（无限制自由移动）。每种适用于不同的交互场景。}}

{{知识点10}}{{XRReferenceSpace是不可变的，要移动用户视点需要使用getOffsetReferenceSpace(transform)创建新的偏移参考空间。XRRigidTransform描述位置和旋转，可组合实现用户在虚拟空间中的移动。当设备检测到跟踪原点与环境不连续时会触发reset事件。}}

{{知识点11}}{{XRViewerPose描述观看者在空间中的位置和方向，包含views属性（一个XRView数组）表示左右眼的视图。每个XRView包含projectionMatrix（投影矩阵）和transform（视图变换），用于分别渲染左右眼的画面。单眼设备只有一个视图。}}

{{知识点12}}{{WebXR中的自由度（Degrees of Freedom, DoF）表示设备能检测的运动维度。3DoF支持三个旋转自由度：俯仰（Pitch）、偏航（Yaw）、翻滚（Roll），只能感知头部转动。6DoF在此基础上增加了三个平移自由度：前后、左右、上下移动，通常需要外部传感器支持。}}

{{知识点13}}{{视野（Field of View, FOV）是WebXR中的关键概念。人眼单眼FOV约135°，双眼总FOV约200-220°。VR头显通常从90°起步，高端设备可达约150°。FOV越大沉浸感越强，但也意味着更重、更贵的头显。WebXR中FOV由XRView的投影矩阵自动处理。}}

{{知识点14}}{{WebXR渲染流程：1）获取XR参考空间；2）请求动画帧；3）在回调中获取XRViewerPose；4）遍历views为每个眼睛渲染；5）使用WebGL绘制场景到帧缓冲；6）浏览器将帧缓冲传送给XR设备。左右眼图像分别渲染到同一帧缓冲的左右半部分。}}

{{知识点15}}{{刷新率和帧率是WebXR性能的关键。传统显示器60Hz，现代VR头显可达90Hz、120Hz甚至更高。60Hz时每帧可用时间约16.67ms，120Hz时仅约8.33ms。超出渲染时间会导致丢帧，引起画面卡顿。应以60FPS为基线目标进行优化。}}

{{知识点16}}{{XRRenderState控制渲染参数，通过session.updateRenderState()更新。可配置属性包括：baseLayer（XRWebGLLayer，定义渲染目标）、depthNear/depthFar（近远裁剪平面）、inlineVerticalFieldOfView（内联模式垂直FOV）。baseLayer连接WebGL上下文与XR会话。}}

{{知识点17}}{{XRWebGLLayer是WebXR与WebGL之间的桥梁，将WebGL渲染内容输出到XR设备。创建时传入XRSession和可选配置（如antialias抗锯齿、framebufferScaleFactor帧缓冲缩放因子）。通过layer.framebuffer获取专用的WebGL帧缓冲对象，所有WebXR渲染必须使用此帧缓冲。}}

{{知识点18}}{{XRInputSource代表一个XR输入源（如控制器、手部追踪）。关键属性：handedness（左手/右手/无）、targetRayMode（gaze/tracked-pointer/screen）、targetRaySpace（瞄准射线的空间）、gripSpace（握持空间，用于渲染控制器模型）、gamepad（扩展按钮和轴）。}}

{{知识点19}}{{WebXR输入事件体系：select事件在用户完成主操作（如扣扳机）时触发；selectstart在操作开始时触发；selectend在操作结束时触发；squeeze事件对应握紧操作；squeezestart/squeezeend对应握紧的开始和结束。这些事件提供XRInputSourceEvent对象，包含inputSource和frame属性。}}

{{知识点20}}{{瞄准射线模式（targetRayMode）有三种：gaze（视线追踪，射线从双眼之间向前延伸，无需控制器）、tracked-pointer（手柄控制器，射线从控制器前端沿指向方向延伸）、screen（触摸屏，基于用户在屏幕上的触摸位置确定射线）。}}

{{知识点21}}{{XRPose描述两个XRSpace之间的空间关系，包含transform（XRRigidTransform，位置和旋转）和linearVelocity/angularVelocity（线速度和角速度）。XRViewerPose继承自XRPose，额外提供views数组用于多视图渲染。}}

{{知识点22}}{{XRRigidTransform表示一个刚体变换，包含position（DOMPointReadOnly，3D位置）和orientation（DOMPointReadOnly，四元数表示的旋转）。还提供matrix（4x4变换矩阵）和inverse（逆变换）。用于在参考空间之间进行坐标变换。}}

{{知识点23}}{{WebXR的Hit Test模块（XRHitTestSource）支持射线投射命中测试，用于检测虚拟射线与真实环境表面的交点。通过session.requestHitTestSource()创建，每帧调用frame.getHitTestResults()获取结果。这对AR应用中将虚拟物体放置在真实表面上至关重要。}}

{{知识点24}}{{WebXR Anchors模块（XRAnchor）允许在真实世界中创建固定锚点，虚拟物体可以附着其上。锚点相对于环境保持稳定，即使用户移动或设备暂时丢失追踪。通过frame.createAnchor()创建锚点，frame.trackedAnchors获取当前追踪的锚点集合。}}

{{知识点25}}{{WebXR Hand Input模块支持手部追踪，通过XRHand接口提供26个关节的位置和旋转数据。每个关节对应一个XRJointSpace，可以通过frame.getJointPose()获取位姿。手部追踪使用户无需控制器即可与虚拟环境交互，是自然交互的重要方向。}}

{{知识点26}}{{WebXR Depth Sensing模块提供深度感知能力，返回场景中每个像素的距离信息。支持两种使用模式：cpu-usage（CPU端深度数据，XRCPUDepthInformation）和gpu-usage（GPU端深度纹理）。深度数据可用于遮挡、碰撞检测和环境理解等AR场景。}}

{{知识点27}}{{WebXR Layers API提供比基础XRWebGLLayer更高级的渲染层功能，支持多种层类型：projection（投影层）、quad（四边形层）、equirect（等距柱状投影层）、cube（立方体贴图层）。Layers API可以实现更好的性能和更灵活的内容布局，适用于复杂XR应用。}}

{{知识点28}}{{WebXR Lighting Estimation API（XRLightProbe）估算用户环境中的光照信息，包括环境光颜色、强度和方向。通过session.requestLightProbe()创建，frame.getLightEstimate()获取估计值。在AR场景中，正确的光照估计能让虚拟物体看起来更自然地融入真实环境。}}

{{知识点29}}{{WebXR DOM Overlays模块允许在沉浸式AR会话中叠加显示HTML DOM元素。通过设置"dom-overlay"特性并指定overlay元素，可以在AR视图上方显示按钮、文本等UI元素。这对AR应用的用户界面设计非常有用，避免了在3D空间中构建复杂UI的需要。}}

{{知识点30}}{{WebXR Gamepads模块将WebXR输入源与标准Gamepad API关联。每个XRInputSource的gamepad属性返回一个Gamepad对象，提供buttons数组和axes数组。这允许开发者访问控制器上的所有按钮、摇杆、触摸板等，实现丰富的交互功能。}}

{{知识点31}}{{session.visibilityState属性指示XR会话的可见性状态：visible（内容对用户可见且可交互）、visible-blurred（可见但失去焦点，应用应暂停高开销操作）、hidden（不可见，应用应停止渲染）。这有助于优化性能和电量消耗。}}

{{知识点32}}{{session.environmentBlendMode属性返回环境混合模式，描述XR设备如何将虚拟内容与现实环境混合：opaque（完全不透明，纯VR场景）、additive（叠加模式，仅添加光线）、alpha-blend（Alpha混合，虚拟内容带透明度叠加在现实上）。}}

{{知识点33}}{{WebXR设备选择和检测的最佳实践：始终先用isSessionSupported()检查能力支持；监听navigator.xr的devicechange事件处理设备连接/断开；在请求会话时通过requiredFeatures和optionalFeatures指定所需特性，确保优雅降级。}}

{{知识点34}}{{WebXR与Three.js等框架集成：Three.js通过WebXRManager原生支持WebXR。renderer.xr.enabled = true启用XR；使用renderer.setAnimationLoop()替代requestAnimationFrame；通过renderer.xr.getController()获取控制器。A-Frame、Babylon.js等框架也提供封装良好的WebXR集成。}}

{{知识点35}}{{WebXR空间追踪（Spatial Tracking）原理：设备使用传感器（陀螺仪、加速度计、摄像头等）持续估算用户在物理空间中的位置和朝向。XRReferenceSpace将这些追踪数据映射到一致的坐标系中。"追踪丢失"可能发生，此时设备位置不准确，reset事件会被触发通知应用。}}

{{知识点36}}{{WebXR浏览器兼容性（2025）：Chrome/Edge在Android和桌面端支持immersive-vr和immersive-ar；Firefox支持部分immersive-vr；Safari在visionOS上支持WebXR。移动端Chrome Android是AR体验的主要目标平台。始终检查isSessionSupported()而非假设浏览器支持。}}

{{知识点37}}{{WebXR安全与隐私考量：WebXR仅在安全上下文（HTTPS）下工作；设备传感器数据可能泄露用户位置信息；浏览器可能限制同时活跃的XR会话数量；某些功能（如手部追踪、深度感知）需要用户明确授权；应始终提供退出XR会话的明确方式。}}

{{知识点38}}{{WebXR性能优化策略：使用requestAnimationFrame而非setTimeout/setInterval；减少draw call数量；使用实例化渲染（instanced rendering）；合理设置帧缓冲缩放因子（framebufferScaleFactor）；在visibilityState为hidden时暂停渲染；使用Web Workers处理复杂计算；善用LOD（细节层次）。}}

{{知识点39}}{{WebXR开发调试工具：Chrome DevTools提供WebXR模拟扩展（WebXR API Emulator），可在桌面浏览器中模拟XR设备；Mozilla的WebXR模拟器Firefox插件也提供类似功能。使用immersive-web/webxr-samples中的官方示例学习和测试。}}

{{知识点40}}{{WebXR开发入门步骤：1）创建WebGL上下文；2）检查navigator.xr是否存在；3）调用isSessionSupported()确认支持；4）用户交互后调用requestSession()创建会话；5）创建XRWebGLLayer并关联到会话；6）获取参考空间；7）启动requestAnimationFrame渲染循环；8）在回调中获取视图位姿并渲染。}}

{{知识点41}}{{WebXR模块化规范设计：WebXR Device API是核心规范，各功能通过独立模块扩展：Anchors（锚点）、Depth Sensing（深度感知）、DOM Overlays（DOM叠加）、Gamepads（手柄）、Hand Input（手部追踪）、Hit Test（命中测试）、Layers（图层）、Lighting Estimation（光照估计）。这种模块化设计支持渐进增强。}}

{{知识点42}}{{WebXR的session.requestReferenceSpace()在不同设备上的行为差异：local类型在6DoF设备上会尽量保持原点稳定，在3DoF设备上原点跟随初始位置；local-floor在知道地面高度时使用精确值，否则浏览器会估算（为防指纹识别会四舍五入）；bounded-floor提供boundsGeometry多边形描述安全边界。}}

{{知识点43}}{{WebXR中glTF模型的使用：WebXR示例广泛使用glTF 2.0格式加载3D模型，因为它是Web 3D内容的行业标准格式。Three.js的GLTFLoader、Babylon.js的SceneLoader等都原生支持glTF加载。模型应优化多边形数量以满足XR高帧率渲染要求。}}

{{知识点44}}{{WebXR会话生命周期：requestSession()创建会话 → sessionstart/事件 → 运行渲染循环 → 用户或应用调用session.end() → end事件触发 → 清理资源。在end事件处理中应释放WebGL资源、取消动画帧、重置状态。会话结束后可再次创建新会话。}}
