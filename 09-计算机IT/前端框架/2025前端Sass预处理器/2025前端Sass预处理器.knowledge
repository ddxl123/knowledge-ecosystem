# 2025前端Sass预处理器
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}{{Sass是什么：Sass（Syntactically Awesome Style Sheets）是最成熟、最稳定的CSS预处理器，它扩展了CSS的功能，增加了变量、嵌套、混入、继承、函数等特性，使CSS更易维护和扩展。Sass文件编译后生成标准CSS文件供浏览器使用。}}

{{知识点2}}{{Sass的两种语法：Sass提供两种语法格式。SCSS语法（.scss文件）是CSS的超集，所有合法CSS也是合法SCSS，使用花括号和分号。缩进语法（.sass文件）使用缩进代替花括号、换行代替分号，更加简洁。SCSS是目前最常用的语法。}}

{{知识点3}}{{Sass的安装方式：可以通过npm安装Dart Sass（npm install sass），也可以通过Homebrew、Chocolatey等包管理器安装。Dart Sass是官方推荐的实现，取代了已废弃的Ruby Sass和LibSass。2025年应使用Dart Sass。}}

{{知识点4}}{{Sass编译命令：使用sass命令编译，如sass input.scss output.css。--watch标志可以监听文件变化自动编译，如sass --watch app/sass:public/stylesheets。支持监听整个目录。}}

{{知识点5}}{{变量（Variables）：使用$符号定义变量，如$primary-color: #333;。变量可以在整个样式表中复用，便于统一管理颜色、字体、间距等设计值。变量支持作用域，在块级作用域内定义的变量只在该作用域内可见。}}

{{知识点6}}{{变量默认值（!default）：使用!default标志定义变量默认值，如$color: red !default;。如果变量已经被赋值，则!default不会覆盖已有值。这在编写可配置的库时非常有用，允许用户自定义变量值。}}

{{知识点7}}{{嵌套规则（Nesting）：Sass允许CSS选择器按照HTML的层级结构嵌套书写。如nav { ul { margin: 0; } li { display: inline-block; } }，编译后会生成nav ul和nav li选择器，使代码结构更清晰。}}

{{知识点8}}{{父选择器（Parent Selector &）：&符号引用外层选择器，用于组合伪类、伪元素或BEM命名。如.alert { &:hover { font-weight: bold; } }编译为.alert:hover。还可以添加后缀，如.accordion { &__copy { } }生成.accordion__copy。}}

{{知识点9}}{{选择器嵌套中的&后缀：&后缀特别适合BEM命名法。如.block { &__element { } &--modifier { } }会生成.block__element和.block--modifier，大大简化了BEM类名的书写。}}

{{知识点10}}{{属性嵌套：Sass支持CSS属性的嵌套，如margin: { top: 10px; left: 20px; }，编译为margin-top: 10px; margin-left: 20px;。适用于font、border、background等有连字符的属性。}}

{{知识点11}}{{局部文件（Partials）：以_开头的Sass文件称为局部文件，如_variables.scss。下划线告诉Sass该文件不需要独立编译为CSS文件，只作为模块被其他文件导入使用。局部文件通过@use规则引入。}}

{{知识点12}}{{@use规则（模块系统）：@use是Sass现代模块系统的核心规则，用于加载其他Sass文件作为模块。如@use 'base';加载后通过命名空间访问成员：base.$primary-color。@use在编译中只会加载一次，即使被多次引用。}}

{{知识点13}}{{@use的命名空间：默认命名空间是文件名（不含下划线前缀和扩展名）。可通过as关键字自定义：@use 'corners' as c;。使用@use 'corners' as *可取消命名空间，但仅建议对自编写的样式表使用。}}

{{知识点14}}{{@use的私有成员：以-或_开头的变量、混入和函数是私有的，只在定义它们的文件内可见。如$-radius: 3px;在外部模块中无法访问。这实现了模块的封装性。}}

{{知识点15}}{{@use的配置（with）：通过with关键字在加载模块时配置变量。如@use 'library' with ($color: blue);。被配置的变量必须使用!default标志定义。这使得模块可以被灵活定制。}}

{{知识点16}}{{@forward规则：@forward加载一个Sass文件并将其公共成员（混入、函数、变量）暴露给使用@use加载当前文件的用户。用于构建库的入口文件，如bootstrap.scss可以@forward多个子模块。@forward不使成员在当前文件可用。}}

{{知识点17}}{{@forward的前缀：@forward "list" as list-*;会为所有转发的成员添加list-前缀。如模块中的reset混入会变为list-reset。这有助于避免命名冲突并提高可读性。}}

{{知识点18}}{{@forward的可见性控制：使用hide和show关键字控制转发哪些成员。@forward "list" hide reset, $gap;不转发指定成员。@forward "list" show reset;只转发指定成员。用于隐藏内部实现细节。}}

{{知识点19}}{{@import规则（已废弃）：@import是旧的导入规则，如@import 'base';。它会导致全局命名空间污染、变量冲突、无法控制可见性等问题。Sass官方已废弃@import，应使用@use和@forward替代。}}

{{知识点20}}{{混入（Mixins）：使用@mixin定义可复用的样式块，通过@include使用。如@mixin reset-list { margin: 0; padding: 0; list-style: none; }，使用时@include reset-list;。混入可以包含任何CSS声明和规则。}}

{{知识点21}}{{混入的参数：混入可以接受参数实现灵活定制。如@mixin rtl($property, $ltr-value, $rtl-value) { ... }。支持可选参数（带默认值）、关键字参数。参数列表支持尾逗号便于重构。}}

{{知识点22}}{{混入的可变参数：使用$args...语法接收不定数量参数。如@mixin shadow($shadows...) { box-shadow: $shadows; }。调用时可以传入任意数量的值，混入会将其作为列表处理。}}

{{知识点23}}{{@content指令：混入中的@content允许在混入中插入调用者传入的样式块。如@mixin media($query) { @media #{$query} { @content; } }，使用时@include media(min-width: 768px) { font-size: 16px; }。常用于媒体查询封装。}}

{{知识点24}}{{函数（Functions）：使用@function定义可复用的计算逻辑，通过@return返回值。如@function fibonacci($n) { ... @return $result; }。函数使用CSS函数语法调用：fibonacci(4)。函数应只用于计算值，副作用操作应使用混入。}}

{{知识点25}}{{函数的参数：与混入类似，函数支持必需参数、可选参数（带默认值）和关键字参数。如@function invert($color, $amount: 100%) { ... }。函数名中连字符和下划线等价。}}

{{知识点26}}{{继承（@extend）：@extend让一个选择器继承另一个选择器的样式。如.error--serious { @extend .error; border-width: 3px; }。Sass会智能合并选择器，生成.error, .error--serious { ... }，而非复制样式。}}

{{知识点27}}{{占位符选择器（%placeholder）：以%开头的选择器不会被编译输出到CSS中，只能通过@extend使用。如%message-shared { padding: 10px; }，.success { @extend %message-shared; }。用于定义只通过继承使用的基类。}}

{{知识点28}}{{@extend与@mixin的区别：@extend合并选择器，输出CSS更小，但不能传参数。@mixin复制样式代码到使用处，更灵活可传参但可能产生重复CSS。2025年推荐优先使用@mixin，@extend适合语义化继承场景。}}

{{知识点29}}{{Sass内置模块：Sass提供sass:math、sass:string、sass:color、sass:list、sass:map、sass:selector、sass:meta等内置模块。通过@use "sass:color";加载，使用color.scale()等函数。取代了旧的全局函数。}}

{{知识点30}}{{sass:color模块：提供颜色操作函数。color.adjust($color, $red: 20)调整通道值；color.scale($color, $lightness: 20%)按比例缩放；color.mix($color1, $color2)混合颜色。支持现代CSS颜色空间如display-p3。}}

{{知识点31}}{{sass:math模块：提供数学函数。math.ceil()向上取整、math.floor()向下取整、math.round()四舍五入、math.max()和math.min()求最值、math.random()随机数。还支持math.$pi和math.$e等常量。}}

{{知识点32}}{{sass:map模块：操作键值对数据结构。map.get($map, key)获取值；map.set($map, key, value)设置值；map.merge()合并map；map.keys()获取所有键；map.values()获取所有值。用于管理设计系统配置。}}

{{知识点33}}{{sass:list模块：操作列表数据。list.append($list, $val)追加元素；list.index($list, $val)查找索引；list.nth($list, $n)获取第n个元素；list.length($list)获取长度。列表索引从1开始。}}

{{知识点34}}{{sass:selector模块：操作选择器。selector.is-superselector()判断超集关系；selector.extend()扩展选择器；selector.unify()合并选择器；selector.parse()解析选择器为嵌套列表。用于高级选择器操作。}}

{{知识点35}}{{sass:string模块：字符串操作。string.index()查找子串位置；string.insert()插入字符串；string.slice()截取；string.to-upper-case()转大写；string.unquote()去除引号；string.quote()添加引号。}}

{{知识点36}}{{sass:meta模块：暴露Sass内部机制。meta.type-of()获取值类型；meta.global-variable-exists()检查全局变量是否存在；meta.mixin-exists()检查混入是否存在；meta.call()动态调用混入或函数。}}

{{知识点37}}{{控制指令@if/@else：条件判断。如@if $theme == 'dark' { color: white; } @else { color: black; }。结合@else if实现多分支。常用于响应式混入和主题切换。}}

{{知识点38}}{{控制指令@for循环：@for $i from 1 through 5 { .col-#{$i} { width: $i * 20%; } }。through包含终值，to不包含终值。用于生成网格系统、间距工具类等重复样式。}}

{{知识点39}}{{控制指令@each循环：遍历列表或map。@each $color in $colors { .bg-#{$color} { background: $color; } }。支持解构：@each $key, $value in $map { }。用于批量生成工具类。}}

{{知识点40}}{{控制指令@while循环：@while $i > 0 { .item-#{$i} { width: 10em * $i; } $i: $i - 2; }。条件为真时持续循环。使用较少，通常@for和@each已足够。}}

{{知识点41}}{{插值（Interpolation）：#{$variable}将变量值插入到选择器名、属性名或字符串中。如.class-#{$n} { }、font-#{$prop}: $value;。使Sass代码更加动态灵活。}}

{{知识点42}}{{数据类型：Sass支持数字（10px, 3em）、字符串（"hello", hello）、颜色（#fff, rgb(255,0,0)）、布尔值（true/false）、空值（null）、列表（1px 2px 3px）、映射（(key: value)）等数据类型。}}

{{知识点43}}{{数字运算：Sass支持+、-、*、/、%运算。除法/在新版Sass中需要用math.div()函数替代。支持单位运算和转换，如10px + 5px = 15px。比较运算符用于条件判断。}}

{{知识点44}}{{字符串操作：引号字符串和无引号字符串两种形式。支持+拼接、#{}插值。string.unquote()去除引号，string.quote()添加引号。颜色名如red是无引号字符串。}}

{{知识点45}}{{颜色函数：除内置模块外，Sass支持直接在CSS颜色函数中使用var()。如rgb(0 51 102 / var(--opacity))会被保留原样输出，而非编译时解析。这增强了与CSS自定义属性的兼容性。}}

{{知识点46}}{{@debug和@warn：@debug输出调试信息到标准错误，如@debug $value;。@warn输出警告信息，可被--quiet标志抑制。@error抛出错误终止编译。用于开发时调试Sass代码。}}

{{知识点47}}{{注释：Sass支持/* */多行注释（会输出到编译后的CSS）和//单行注释（不会输出）。使用--style compressed压缩模式时，/* */注释也会被移除（除非以/*!开头的保留注释）。}}

{{知识点48}}{{Sass与现代CSS的关系：2025年CSS原生已支持变量（var()）、嵌套、@layer等特性。Sass的不可替代之处在于：混入、函数、循环、条件判断、模块系统、颜色操作等编程能力。合理评估是否需要Sass。}}

{{知识点49}}{{Sass在构建工具中的集成：Vite内置支持Sass（需安装sass包）。Webpack通过sass-loader集成。PostCSS的postcss-sass插件也可处理。2025年推荐使用Dart Sass配合Vite或Webpack。}}

{{知识点50}}{{BEM与Sass的最佳实践：使用&父选择器配合BEM命名法。.block { &__element { &--modifier { } } }。结合@extend %placeholder实现基础样式继承，@mixin实现带参数的组件混入。保持嵌套不超过3层。}}
