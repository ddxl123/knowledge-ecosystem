# 2025前端Web Worker入门
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}{{Web Worker 是什么}}
Web Worker 是 HTML5 提供的 JavaScript 多线程解决方案。它允许在独立于主线程的后台线程中运行脚本，执行耗时的计算任务而不阻塞 UI 线程。Worker 通过构造函数创建，运行一个独立的 JavaScript 文件，拥有自己的事件循环和全局上下文。数据通过 postMessage/onmessage 消息机制在主线程与 Worker 之间传递，数据是复制而非共享的。

{{知识点2}}{{Web Worker 解决的核心问题}}
JavaScript 是单线程语言，长时间运行的计算（如图像处理、大数据排序、加密解密）会阻塞主线程，导致页面卡顿、无法响应用户交互。Web Worker 将这些任务放到后台线程执行，主线程可以继续处理用户事件和 UI 渲染，从而提升应用的响应性和用户体验。

{{知识点3}}{{Worker 构造函数}}
创建 Worker 使用 new Worker(url) 构造函数，参数 url 是要在 Worker 线程中执行的 JavaScript 文件路径。也可以使用 Blob URL 来创建 Worker。示例：const myWorker = new Worker("worker.js")。创建后 Worker 立即开始执行脚本。

{{知识点4}}{{Dedicated Worker（专用 Worker）}}
专用 Worker 是最常用的 Worker 类型，只能被创建它的脚本使用。其全局上下文由 DedicatedWorkerGlobalScope 对象表示。一个专用 Worker 仅能被首次生成它的脚本所使用，通信通过隐式的 postMessage 进行。

{{知识点5}}{{SharedWorker（共享 Worker）}}
SharedWorker 可以被多个脚本共享使用，即使这些脚本在不同的窗口、标签页或 iframe 中运行（必须同源）。与专用 Worker 不同，SharedWorker 通过 port 对象进行通信，需要显式打开端口连接。其全局上下文由 SharedWorkerGlobalScope 对象表示。

{{知识点6}}{{ServiceWorker 的区别}}
ServiceWorker 是一种特殊类型的 Worker，充当 Web 应用、浏览器和网络之间的代理服务器。它主要用于创建离线体验、拦截网络请求、管理缓存、处理推送通知和后台同步。与 Dedicated Worker 和 SharedWorker 不同，ServiceWorker 不直接与特定页面关联，而是作用于整个域。

{{知识点7}}{{Worker 中不能做的事}}
Worker 运行在独立的全局上下文中，不能直接操作 DOM 元素，不能使用 document 对象，不能访问 window 对象。Worker 中通过 self 关键字访问全局作用域（而非 window）。这些限制确保了线程安全。

{{知识点8}}{{Worker 中可以使用的 Web API}}
Worker 虽然不能操作 DOM，但可以使用大量 Web API，包括：Fetch API、WebSocket、IndexedDB、Web Crypto API、Cache API、Broadcast Channel API、Console API、TextEncoder/TextDecoder、FormData、XMLHttpRequest、Performance API、Notifications API 等。

{{知识点9}}{{postMessage 发送消息}}
主线程和 Worker 之间通过 postMessage() 方法发送消息。主线程中调用 worker.postMessage(data)，Worker 中直接调用 postMessage(data)。消息可以包含任何可序列化的 JavaScript 对象。在主线程中 postMessage 必须挂在 Worker 对象上，在 Worker 内部则直接调用。

{{知识点10}}{{onmessage 接收消息}}
主线程通过 worker.onmessage = (e) => {} 接收 Worker 发来的消息，Worker 通过 onmessage = (e) => {} 接收主线程的消息。消息数据存储在事件的 data 属性中（e.data）。也可以使用 addEventListener('message', handler) 方式监听。

{{知识点11}}{{数据传递机制：复制而非共享}}
主线程与 Worker 之间传递的数据是被复制（或转移）的，而不是共享的。这意味着修改主线程中的数据不会影响 Worker 中的数据，反之亦然。这种设计避免了多线程共享内存带来的竞态条件问题。

{{知识点12}}{{terminate 终止 Worker}}
从主线程调用 worker.terminate() 可以立即终止正在运行的 Worker。Worker 线程会被立即停止，不会有机会完成正在进行的操作。Worker 内部也可以通过 self.close() 来终止自身。

{{知识点13}}{{Worker 错误处理}}
Worker 运行出错时会触发 onerror 事件。错误事件对象包含三个关键属性：message（错误信息）、filename（出错脚本文件名）、lineno（出错行号）。错误事件不会冒泡，可以调用 preventDefault() 阻止默认行为。

{{知识点14}}{{importScripts 引入外部脚本}}
Worker 内部可以使用全局函数 importScripts() 来加载外部脚本。支持传入多个 URI：importScripts("foo.js", "bar.js")。脚本下载顺序不确定，但执行顺序与参数顺序一致。这是一个同步操作，所有脚本加载完毕后才返回。

{{知识点15}}{{Worker 特性检测}}
使用 Worker 前应进行特性检测：if (window.Worker) { ... }。这可以确保在不支持 Worker 的浏览器中优雅降级，避免运行时错误。所有现代浏览器（Chrome、Firefox、Safari、Edge）都支持 Worker。

{{知识点16}}{{Blob URL 创建 Worker}}
除了引用外部 JS 文件，还可以通过 Blob URL 动态创建 Worker。将 Worker 代码写成字符串，创建 Blob 对象，再通过 URL.createObjectURL(blob) 生成 URL 传给 Worker 构造函数。这种方式适合需要动态生成 Worker 代码的场景。

{{知识点17}}{{Subworker（子 Worker）}}
Worker 可以创建新的 Worker，称为 Subworker。Subworker 必须与父页面同源，其 URI 解析相对于父 Worker 的地址而非页面地址。这使得 Worker 之间可以建立依赖关系，但需要注意嵌套层次过深可能影响性能。

{{知识点18}}{{Worker 中的网络请求}}
Worker 可以使用 XMLHttpRequest 和 Fetch API 进行网络请求。但 XMLHttpRequest 的 responseXML 和 channel 属性始终返回 null。Fetch API 在 Worker 中没有此类限制，是推荐的网络请求方式。

{{知识点19}}{{SharedWorker 的 port 通信}}
SharedWorker 必须通过 port 对象进行通信。在主线程中使用 myWorker.port.postMessage() 发送消息，Worker 内部通过 onconnect 事件获取端口。如果使用 addEventListener 监听 message 事件，需要调用 port.start() 显式打开端口连接。

{{知识点20}}{{Worker 的事件循环}}
Worker 拥有独立的事件循环，与主线程的事件循环完全分离。Worker 内部可以使用 setTimeout、setInterval、requestAnimationFrame 等定时器 API。每个 Worker 都有自己的任务队列，不会与主线程的事件循环相互干扰。

{{知识点21}}{{Worker 的全局上下文}}
Worker 的全局上下文不是 window，而是 WorkerGlobalScope 的子类。Dedicated Worker 使用 DedicatedWorkerGlobalScope，SharedWorker 使用 SharedWorkerGlobalScope，ServiceWorker 使用 ServiceWorkerGlobalScope。通过 self 关键字访问全局对象。

{{知识点22}}{{WorkerGlobalScope 共享方法}}
Worker 和主线程共享 WindowOrWorkerGlobalScope 混入中的方法，包括：setTimeout、setInterval、clearTimeout、clearInterval、fetch、atob、btoa、queueMicrotask、structuredClone、createImageBitmap 等。

{{知识点23}}{{OffscreenCanvas 与 Worker 配合}}
OffscreenCanvas API 允许在 Worker 中进行 Canvas 渲染。通过 canvas.transferControlToOffscreen() 将 Canvas 控制权转移到 Worker，Worker 中可以使用 WebGL 或 2D 上下文进行绘制，渲染完成后通过 gl.commit() 将帧推回主线程。这是高性能图形渲染的关键技术。

{{知识点24}}{{Worker 中使用 IndexedDB}}
Worker 可以访问 IndexedDB API 进行客户端数据存储。这使得 Worker 可以独立进行数据的读写操作，无需将数据传回主线程。适合需要大量数据处理的场景，如离线数据同步、本地缓存管理等。

{{知识点25}}{{Worker 中使用 WebSocket}}
Worker 内部可以使用 WebSocket API 进行实时通信。这使得 WebSocket 的消息处理可以在后台线程完成，不会阻塞主线程。适合需要持续接收服务器推送数据的应用，如实时聊天、股票行情等。

{{知识点26}}{{Transferable Objects（可转移对象）}}
除了复制数据，还可以使用 Transferable Objects 将数据的所有权从主线程转移到 Worker，反之亦然。转移操作不会产生复制开销，原始线程会失去对该数据的访问权。典型用法：postMessage(buffer, [buffer])，第二个参数是可转移对象数组。ArrayBuffer、MessagePort、ImageBitmap 等支持转移。

{{知识点27}}{{Broadcast Channel API 跨 Worker 通信}}
Broadcast Channel API 允许同源的不同 Worker、窗口、iframe 之间进行简单通信。创建同名的 BroadcastChannel 实例即可互相发送消息，无需直接持有对方引用。适合在多个 Worker 或页面间同步状态。

{{知识点28}}{{Worker 的应用场景}}
Web Worker 的典型应用场景包括：图像处理（滤镜、压缩、人脸识别）、大数据排序和过滤、加密解密计算、WebAssembly 执行、物理引擎计算、语法高亮和代码格式化、数据压缩解压、后台数据同步等。

{{知识点29}}{{Worker 的限制与注意事项}}
Worker 的限制包括：不能直接操作 DOM、不能访问 window/document 对象、数据传递需要序列化/反序列化（有性能开销）、每个 Worker 有独立的内存空间、创建和销毁 Worker 有开销、同源策略限制。应避免频繁创建销毁 Worker，推荐使用 Worker 池。

{{知识点30}}{{Worker 线程数量建议}}
创建过多 Worker 会消耗大量系统资源。一般建议 Worker 数量不超过 CPU 核心数（navigator.hardwareConcurrency 可获取）。对于计算密集型任务，使用 Worker 池（Worker Pool）模式复用 Worker，避免频繁创建销毁的开销。

{{知识点31}}{{Worker 中的 structuredClone}}
Worker 内部可以使用 structuredClone() 方法进行深拷贝。该方法支持复杂对象的克隆，包括循环引用、Map、Set、Date、RegExp 等。它是 postMessage 底层序列化算法的独立版本，可以用于 Worker 内部的数据复制。

{{知识点32}}{{Worker 中使用 WebAssembly}}
WebAssembly 模块可以在 Worker 中实例化和执行。将 CPU 密集型的 WebAssembly 计算放到 Worker 中运行，可以避免阻塞主线程。通过 postMessage 传递 WASM 实例的内存（ArrayBuffer），实现主线程与 Worker 间的数据共享。

{{知识点33}}{{Worker 的生命周期管理}}
Worker 的生命周期：创建（new Worker）→ 运行（执行脚本）→ 通信（postMessage/onmessage）→ 终止（terminate 或 close）。Worker 在创建后立即开始执行，除非显式终止否则一直存在。主线程关闭时 Worker 也会被终止。

{{知识点34}}{{Worker 的安全限制}}
Worker 遵循同源策略，Worker 脚本必须与创建它的页面同源。跨域加载 Worker 脚本会触发安全错误。Worker 内部的网络请求也受 CORS 策略限制。本地文件（file://）协议下创建 Worker 在某些浏览器中可能受限。

{{知识点35}}{{现代框架中的 Worker 集成}}
现代前端框架提供了 Worker 集成方案：Webpack 的 worker-loader 和 Web Worker Plugin、Vite 的 ?worker 后缀导入、React 的 comlink 库简化 Worker 通信、Angular 的 @angular/platform-webworker 等。Comlink 库通过 Proxy 模式让 Worker 调用看起来像普通函数调用。

{{知识点36}}{{Worker 的调试方法}}
Worker 可以在浏览器开发者工具中调试。Chrome DevTools 的 Sources 面板会列出所有活跃的 Worker，可以设置断点、查看变量。Worker 内部的 console.log 输出会在主线程的控制台中显示。也可以在 Application 面板中查看 ServiceWorker 状态。

{{知识点37}}{{性能考量：消息传递开销}}
Worker 的 postMessage 涉及数据序列化和反序列化，对于大数据量可能成为性能瓶颈。优化策略：使用 Transferable Objects 转移而非复制大数据、使用 SharedArrayBuffer 实现真正的内存共享、减少消息传递频率、批量传递数据。

{{知识点38}}{{SharedArrayBuffer 与 Worker}}
SharedArrayBuffer 允许主线程和 Worker 之间共享内存，避免序列化开销。需要配合 Atomics API 使用来处理竞态条件。出于安全考虑，使用 SharedArrayBuffer 需要页面设置 COOP/COEP 响应头（Cross-Origin-Opener-Policy 和 Cross-Origin-Embedder-Policy）。
