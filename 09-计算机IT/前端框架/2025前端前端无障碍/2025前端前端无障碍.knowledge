# 2025前端前端无障碍
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}{{无障碍（Accessibility，缩写 a11y）是指让尽可能多的人能够使用网站，包括那些能力受限的用户。a11y 中的 11 代表 Accessibility 单词首字母 a 和末字母 y 之间的 11 个字母。无障碍设计的核心理念是：Web 从根本上就是为所有人设计的，无论其硬件、软件、语言、地理位置或能力如何。}}

{{知识点2}}{{WCAG（Web Content Accessibility Guidelines，Web 内容无障碍指南）是 W3C 发布的 Web 内容无障碍共享标准，由 WAI（Web Accessibility Initiative）负责维护。WCAG 2.2 是当前最新版本（2023年10月发布），包含 A、AA、AAA 三个合规级别，其中 A 为基本要求，AA 为标准目标，AAA 为专业支持级别。}}

{{知识点3}}{{WCAG 四大原则（POUR）：1) 可感知（Perceivable）——信息和 UI 组件必须以用户能感知的方式呈现；2) 可操作（Operable）——UI 组件和导航必须可操作；3) 可理解（Understandable）——信息和 UI 操作必须可理解；4) 健壮性（Robust）——内容必须足够健壮，能被各种用户代理（包括辅助技术）可靠地解释。}}

{{知识点4}}{{语义化 HTML 是无障碍的基础。使用正确的 HTML 元素（如 `<button>`、`<a>`、`<input>`、`<nav>`、`<main>`、`<header>`、`<footer>`）而非用 `<div>` 和 `<span>` 模拟，可以自动获得内置的键盘可访问性、角色和状态信息。MDN 明确建议：如果存在具有所需语义和行为的原生 HTML 元素，应优先使用而非 ARIA。}}

{{知识点5}}{{WAI-ARIA（Accessible Rich Internet Applications）是一套通过角色、状态和属性来增强 HTML 语义的技术规范。ARIA 第一法则：如果你可以使用原生 HTML 元素获得所需的语义和行为，就不要用 ARIA。WebAIM 的调查发现，使用了 ARIA 的首页平均比未使用 ARIA 的页面多出 41% 的检测错误，因此"没有 ARIA 比糟糕的 ARIA 更好"。}}

{{知识点6}}{{ARIA 角色（Roles）用于描述 HTML 中不存在的元素或尚无广泛浏览器支持的元素。常用角色包括：`role="progressbar"`（进度条）、`role="alert"`（警告）、`role="dialog"`（对话框）、`role="navigation"`（导航）、`role="tablist"`/`role="tab"`（标签页）、`role="menu"`/`role="menuitem"`（菜单）等。HTML 地标元素（如 `<main>`、`<header>`、`<nav>`）已有内置隐式 ARIA 角色，无需重复添加。}}

{{知识点7}}{{ARIA 状态和属性用于动态修改元素在无障碍树中的状态和属性。常用属性包括：`aria-label`（为元素提供无障碍名称）、`aria-labelledby`（引用其他元素作为标签）、`aria-describedby`（引用描述元素）、`aria-hidden`（从无障碍树中隐藏元素）、`aria-required`（标记必填字段）、`aria-expanded`（标记展开/折叠状态）等。}}

{{知识点8}}{{ARIA 实时区域（Live Regions）用于通知屏幕阅读器动态内容的变化。`aria-live` 属性设置更新优先级：`polite`（用户空闲时播报，最常用）、`assertive`（立即播报，仅用于时间敏感的关键通知）、`off`（仅在焦点位于该元素内时播报）。具有隐式实时区域的角色包括：`log`（日志）、`status`（状态）、`alert`（警告）、`progressbar`（进度条）、`marquee`（跑马灯）、`timer`（计时器）。}}

{{知识点9}}{{`aria-atomic` 属性控制屏幕阅读器是否始终播报实时区域的全部内容。默认值为 `false`（仅播报变化部分），设为 `true` 时每次更新都会完整播报。例如时钟更新时，若只播报变化的分钟数则信息不完整，设置 `aria-atomic="true"` 可确保播报完整时间。`aria-relevant` 属性指定哪些类型的变化应被播报，可选值包括 `additions`、`removals`、`text`、`all`。}}

{{知识点10}}{{键盘可访问性是无障碍的最低要求。所有交互元素必须可通过键盘操作：Tab 键在可聚焦元素间移动，Enter/Space 键激活按钮和链接，Escape 键关闭对话框，箭头键在组件内导航。确保键盘操作和鼠标操作产生相同的用户体验，鼠标事件处理器应同时绑定到键盘事件。}}

{{知识点11}}{{`tabindex` 属性控制元素的键盘可聚焦性：`tabindex="0"` 使元素可通过 Tab 键聚焦（按文档顺序），`tabindex="-1"` 使元素可通过 JavaScript 的 `focus()` 方法聚焦但不参与 Tab 顺序，正数值（如 `tabindex="3"`）应避免使用——它们会被置于所有默认交互元素之前，导致维护困难。}}

{{知识点12}}{{焦点管理是复杂组件的关键技术。Roving tabindex 技巧：在分组组件（如菜单、标签页、树）中，容器设为 `tabindex="0"`，子元素设为 `tabindex="-1"`，用户通过箭头键在子元素间导航时，动态将当前聚焦项设为 `tabindex="0"`，前一个聚焦项恢复为 `tabindex="-1"`。`aria-activedescendant` 技巧：容器使用单一事件处理器，通过 `aria-activedescendant` 属性跟踪"虚拟"焦点。}}

{{知识点13}}{{焦点指示器（Focus Indicator）对键盘用户至关重要。WCAG 2.4.7 要求交互元素必须有可见的焦点样式。CSS 的 `outline` 属性不应无条件设为 `none`，可使用 `:focus-visible` 伪类仅在键盘导航时显示焦点环。WCAG 2.2 引入了新的焦点外观要求（2.4.11），要求焦点指示器有足够的对比度和大小。}}

{{知识点14}}{{Skip Links（跳过链接）是页面顶部的隐藏链接，获得焦点后显示，允许键盘用户跳过重复的导航内容直接到达主内容区域。实现方式：在 `<body>` 开头添加 `<a href="#main-content" class="skip-link">跳到主内容</a>`，主内容区域使用 `<main id="main-content">`。这是 WCAG 2.4.1"绕过区块"要求的标准实现方式。}}

{{知识点15}}{{所有图片必须有 `alt` 属性。信息性图片的 `alt` 应描述图片内容和传达的信息；装饰性图片使用空 `alt=""` 告知屏幕阅读器跳过；包含文字的图片，`alt` 应包含图片中的文字；复杂图片（如图表、地图）需提供详细的文本替代方案，描述所有可见信息。缺少 `alt` 属性时，屏幕阅读器会播报文件名和路径。}}

{{知识点16}}{{标题层级（Headings）对辅助技术用户理解页面结构至关重要。每个页面应只有一个 `<h1>`，标题层级应按逻辑顺序递增（不应从 h2 跳到 h4），标题用于介绍内容而非纯视觉样式设计。屏幕阅读器用户可以通过标题列表快速导航页面内容。}}

{{知识点17}}{{表单无障碍要求：每个表单控件必须有关联的 `<label>` 元素（使用 `for` 属性或嵌套方式）；必填字段用 `aria-required="true"` 或 HTML5 `required` 属性标记；错误信息用 `aria-describedby` 关联到对应字段；使用 `<fieldset>` 和 `<legend>` 对相关控件分组（如单选按钮组）；`autocomplete` 属性帮助用户自动填充表单。}}

{{知识点18}}{{颜色对比度是可感知性的关键要求。WCAG 2.1 AA 级要求正常文本的对比度至少为 4.5:1，大文本（18px 以上加粗或 24px 以上普通）至少为 3:1。不应仅靠颜色传达信息（WCAG 1.4.1），如错误状态不能只用红色表示，还需添加图标或文字说明。色盲用户约占男性人口的 8%，设计时必须考虑。}}

{{知识点19}}{{链接无障碍：链接必须使用 `<a>` 元素并包含 `href` 属性（即使在 SPA 中）；链接文本应具有描述性，避免使用"点击这里"或"了解更多"等无上下文的文案；链接必须可通过下划线等方式与普通文本区分，不能仅靠颜色；屏幕阅读器用户可以通过链接列表导航，因此链接文本需要独立有意义。}}

{{知识点20}}{{按钮无障碍：操作按钮应使用 `<button>` 元素而非 `<div>` 或 `<span>`；添加 `type="button"` 防止浏览器在表单中默认提交；按钮文本应清楚描述其功能；图标按钮必须通过 `aria-label` 提供文本描述；确保按钮可通过键盘 Enter/Space 键激活。}}

{{知识点21}}{{landmark 地标元素帮助辅助技术用户理解页面布局并快速导航到关键区域。HTML5 语义元素对应隐式 ARIA 角色：`<header>` → `banner`（页头），`<nav>` → `navigation`（导航），`<main>` → `main`（主内容），`<aside>` → `complementary`（辅助内容），`<footer>` → `contentinfo`（页脚），`<section>` 需要 `aria-label` 才成为地标。}}

{{知识点22}}{{多媒体无障碍：视频需要提供字幕（captions）和文字描述（transcripts）；音频需要文字记录；自动播放的媒体应提供暂停/停止控制；避免闪烁内容（每秒闪烁超过 3 次可能诱发癫痫）；动画应提供暂停控制，使用 `prefers-reduced-motion` 媒体查询检测用户是否减少动画偏好。}}

{{知识点23}}{{认知无障碍（Cognitive Accessibility）关注有认知和学习障碍的用户。包括：使用简单语言、避免习语和复杂隐喻；提供一致的页面布局和导航；将复杂流程分成逻辑步骤并提供进度指示；减少干扰和不必要的内容；提供清晰的错误消息和恢复机制；使认证过程尽可能简单。WCAG 3.1.5 要求内容按 8 年级阅读水平编写。}}

{{知识点24}}{{移动设备无障碍：确保触摸目标足够大（推荐至少 44×44 像素）；支持横屏和竖屏两种方向；确保视口缩放未被禁用（不要设置 `user-scalable=no` 或 `maximum-scale=1`）；考虑移动辅助技术（iOS 的 VoiceOver、Android 的 TalkBack）；注意手势操作需要替代方案。}}

{{知识点25}}{{`prefers-reduced-motion` CSS 媒体查询检测用户是否在系统层面设置了减少动画的偏好。使用方式：`@media (prefers-reduced-motion: reduce) { * { animation: none !important; transition: none !important; } }`。这对有前庭障碍（vestibular disorders）的用户至关重要，某些动画可能引起恶心、头晕甚至癫痫发作。}}

{{知识点26}}{{CSS 无障碍最佳实践：不要仅用 CSS 视觉效果传达语义信息；使用 `::before` 和 `::after` 伪元素添加的内容对屏幕阅读器不完全可见；`visibility: hidden` 和 `display: none` 都会从无障碍树中隐藏元素；`clip-path` 或视觉隐藏类（如 `.sr-only`）可用于仅对屏幕阅读器可见的内容；确保响应式设计在不同缩放级别下正常工作。}}

{{知识点27}}{{无障碍测试方法：1) 自动化工具检查——axe-core、Lighthouse、WAVE 等可检测约 30-50% 的无障碍问题；2) 键盘测试——仅使用键盘完成所有操作；3) 屏幕阅读器测试——使用 NVDA（Windows）、VoiceOver（macOS/iOS）、TalkBack（Android）进行实际测试；4) 对比度检查——使用 Colour Contrast Analyser 等工具；5) HTML 验证——使用 W3C Validator 检查 HTML 合规性。}}

{{知识点28}}{{React 框架中的无障碍实践：使用语义化 JSX 元素而非 `<div>`；利用 `aria-*` 属性（React 中使用驼峰命名如 `ariaLabel`、`ariaDescribedBy`）；使用 `React.Fragment` 避免不必要的 DOM 节点；React 18+ 的 `useId` hook 用于生成稳定的无障碍 ID；使用 Reach UI、Radix UI 等无障碍优先的组件库。}}

{{知识点29}}{{Vue 框架中的无障碍实践：使用语义化 HTML 模板；`v-if` 和 `v-show` 对无障碍树的影响不同（`v-if` 完全移除元素，`v-show` 仅隐藏）；动态 ARIA 属性绑定如 `:aria-expanded="isOpen"`；Vue A11y 社区提供了 vue-axe、eslint-plugin-vuejs-accessibility 等工具；Nuxt.js 框架内置了部分无障碍支持。}}

{{知识点30}}{{Web 组件（Web Components）的无障碍挑战：Shadow DOM 中的 ARIA 属性可能无法被外部无障碍树正确关联；需要使用 `delegatesFocus` 选项管理焦点；自定义元素必须模拟原生元素的键盘行为；`aria-labelledby` 和 `aria-describedby` 无法跨 Shadow DOM 边界引用 ID。}}

{{知识点31}}{{SPA（单页应用）的无障碍挑战：路由切换时需要手动管理焦点（将焦点移到新页面的标题或主内容区）；页面标题需要随路由更新（`document.title`）；需要使用 ARIA 实时区域通知用户页面变化；历史记录管理需确保浏览器的前进/后退按钮正常工作。}}

{{知识点32}}{{无障碍设计系统和组件库推荐：Reach UI（React，由 React Router 团队维护）、Radix UI（React/Vue，无样式原语组件）、Headless UI（React/Vue，Tailwind CSS 团队）、Adobe 的 React Spectrum、Google 的 Material UI。这些库内置了 ARIA 角色、键盘交互和焦点管理等无障碍功能。}}

{{知识点33}}{{无障碍法律和标准：美国有 ADA（Americans with Disabilities Act）和 Section 508；欧盟有 European Accessibility Act（EAA，2025年6月生效）；中国有《信息技术 互联网内容无障碍可访问性技术要求与测试方法》（GB/T 37668-2019）。不合规可能导致法律诉讼，2023 年美国 Web 无障碍诉讼超过 4,000 起。}}

{{知识点34}}{{无障碍树（Accessibility Tree）是浏览器从 DOM 树生成的结构，供辅助技术（如屏幕阅读器）使用。它包含元素的角色、名称、描述、状态和值。`aria-hidden="true"` 会将元素及其子元素从无障碍树中移除；CSS 的 `display: none` 和 `visibility: hidden` 也会从无障碍树中移除元素。开发者工具（Chrome DevTools 的 Accessibility 面板）可以查看无障碍树。}}

{{知识点35}}{{`aria-activedescendant` 属性用于在不移动 DOM 焦点的情况下管理"虚拟"焦点。容器元素保持焦点，通过 `aria-activedescendant` 属性引用当前活跃后代元素的 ID。这在组合框（combobox）、列表框（listbox）、树形视图（tree）等组件中特别有用，简化了焦点管理逻辑。}}

{{知识点36}}{{无障碍的表单验证最佳实践：在用户提交后而非输入时显示错误（避免干扰）；错误消息使用 `role="alert"` 或 `aria-live="assertive"` 确保屏幕阅读器播报；使用 `aria-invalid="true"` 标记无效字段；通过 `aria-describedby` 将错误消息关联到字段；提供清晰的错误修复指导；在页面顶部汇总所有错误。}}

{{知识点37}}{{暗色模式（Dark Mode）的无障碍考虑：使用 `prefers-color-scheme` 媒体查询自动适应用户偏好；确保暗色模式下文本与背景的对比度仍满足 WCAG 要求（4.5:1）；避免纯黑色背景配纯白色文字（可能导致"光晕效应"，对阅读障碍用户不友好）；测试所有颜色在两种模式下的表现。}}

{{知识点38}}{{国际化（i18n）与无障碍：使用 `lang` 属性标识页面和内容的语言（`<html lang="zh-CN">`）；多语言页面中切换语言时更新 `lang` 属性；RTL（从右到左）语言使用 `dir="rtl"` 属性；确保翻译后的文本仍保持良好的对比度和可读性；图标和符号在不同文化中可能有不同含义。}}

{{知识点39}}{{无障碍与性能的关系：大量 DOM 节点会增加无障碍树的处理时间；虚拟滚动列表需要特别处理无障碍（确保屏幕阅读器能正确播报列表项数量和当前位置）；懒加载内容需要在加载完成时通知辅助技术；骨架屏（Skeleton Screen）对屏幕阅读器可能造成混乱，需要正确标记。}}

{{知识点40}}{{ARIA 组合模式（Composite Widgets）：标签页（Tabs）使用 `role="tablist"`、`role="tab"`、`role="tabpanel"` 组合，通过箭头键在标签间导航；手风琴（Accordion）使用 `aria-expanded` 和 `aria-controls`；模态对话框（Modal Dialog）使用 `role="dialog"` 和 `aria-modal="true"`，需将焦点锁定在对话框内；菜单（Menu）使用 `role="menu"` 和 `role="menuitem"`，支持方向键导航。这些模式的完整实现指南可参考 WAI-ARIA Authoring Practices（APG）。}}
