# 2025前端前端CSS Nesting
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}{{CSS Nesting 是什么：CSS Nesting（CSS 嵌套）是 W3C CSS Nesting Module Level 1 规范定义的原生 CSS 特性，允许在一条样式规则内部嵌套另一条样式规则，子规则的选择器相对于父规则选择器进行解析。它由浏览器原生解析，无需预处理器编译。}}

{{知识点2}}{{CSS Nesting 与预处理器嵌套的区别：CSS Nesting 由浏览器原生解析，而 Sass/Less 的嵌套需要预编译。CSS Nesting 不支持选择器拼接（如 &__child），Sass 可以。CSS Nesting 的 & 是选择器而非字符串拼接操作符。}}

{{知识点3}}{{& 嵌套选择器（Nesting Selector）：& 是 CSS Nesting 的核心符号，代表父规则的选择器。在嵌套规则中，& 被替换为父选择器。例如 .parent { & .child {} } 等价于 .parent .child {}。}}

{{知识点4}}{{省略 & 的嵌套：在大多数情况下可以省略 & 嵌套选择器。浏览器会自动在父选择器和子选择器之间添加空格（后代组合符）。.parent { .child {} } 等价于 .parent .child {}。}}

{{知识点5}}{{何时必须使用 &：当需要创建复合选择器（compound selector）时必须使用 &。例如 .a { &.b {} } 匹配 class="a b" 的元素，而 .a { .b {} } 匹配 .a 的后代 .b 元素。}}

{{知识点6}}{{& 与伪类的配合：使用 & 紧跟伪类可避免浏览器添加空格。.parent { &:hover {} } 等价于 .parent:hover {}，而 .parent { :hover {} } 等价于 .parent *:hover {}（注意星号）。}}

{{知识点7}}{{& 后置（Appending Nesting Selector）：& 可以放在嵌套选择器的后面，实现上下文反转。.foo { .bar & {} } 等价于 .bar .foo {}。这在需要基于父级状态改变子元素样式时非常有用。}}

{{知识点8}}{{& 多次使用：& 可以在同一条嵌套规则中出现多次。.card { .featured & & & {} } 等价于 .featured .card .card .card {}。}}

{{知识点9}}{{& 的特异性计算：& 嵌套选择器的特异性等同于 :is() 伪类，取关联选择器列表中最大的特异性。如果父选择器列表包含 #id（1-0-0）和 b（0-0-1），则 & 的特异性为 1-0-0。}}

{{知识点10}}{{嵌套中的后代组合符：不使用 & 时，浏览器在父子选择器间自动添加空格（后代组合符）。这是最常用的嵌套形式，如 .nav { .item {} } 等价于 .nav .item {}。}}

{{知识点11}}{{嵌套中的子组合符（>）：可以在嵌套中直接使用 > 子组合符。.parent { > .child {} } 等价于 .parent > .child {}。也可以写成 .parent { & > .child {} }。}}

{{知识点12}}{{嵌套中的兄弟组合符：相邻兄弟 (+) 和通用兄弟 (~) 组合符可在嵌套中使用。h2 { + p { color: tomato; } } 选中紧跟 h2 之后的 p 元素。}}

{{知识点13}}{{嵌套 @media 规则：可以在选择器规则内嵌套 @media 媒体查询。.foo { display: grid; @media (orientation: landscape) { grid-auto-flow: column; } }，浏览器会将其转换为外部 @media 包裹的等价形式。}}

{{知识点14}}{{嵌套多个 @media：@media 规则可以多层嵌套。内部 @media 的条件会与外部合并（AND 逻辑）。@media (orientation: landscape) 内嵌 @media (width >= 1024px) 等价于 @media (orientation: landscape) and (width >= 1024px)。}}

{{知识点15}}{{嵌套 @supports 规则：可以在选择器内嵌套 @supports 特性检测。例如 .box { display: flex; @supports (display: grid) { display: grid; } }，实现渐进增强。}}

{{知识点16}}{{嵌套 @layer 规则：Cascade Layers (@layer) 可以嵌套以创建子层。@layer base { @layer support {} } 创建 base.support 子层，用点号连接。}}

{{知识点17}}{{嵌套 @container 规则：容器查询 (@container) 可以嵌套在选择器规则内，根据容器尺寸应用不同样式，实现组件级响应式设计。}}

{{知识点18}}{{嵌套 @scope 规则：@scope 规则可以在嵌套中使用，限定样式的作用范围，实现更精确的样式封装。}}

{{知识点19}}{{嵌套 @starting-style 规则：@starting-style 可以嵌套使用，定义元素首次渲染时的初始样式，配合 transition 实现入场动画效果。}}

{{知识点20}}{{嵌套声明规则（Nested Declarations Rule）：CSS 规则按照文档中书写的顺序解析。嵌套中的声明和非嵌套声明按照出现顺序处理，而非先解析所有属性再处理嵌套规则。}}

{{知识点21}}{{CSSNestedDeclarations 在 CSSOM 中的表现：在 CSSOM 解析中，嵌套前的顶级规则作为 CSSRules 处理，嵌套后的顶级规则包装为 CSSNestedDeclarations，以保持解析顺序。}}

{{知识点22}}{{不支持选择器拼接（Concatenation）：CSS Nesting 不支持像 Sass 那样通过 & 拼接字符串创建新选择器。.component { &__child {} } 不会生成 .component__child {}，而是被解析为 .component __child {}（带空格）。}}

{{知识点23}}{{浏览器支持情况（2025）：Chrome 120+、Edge 120+、Firefox 117+、Safari 17.2+ 完全支持 CSS Nesting。Chrome 112-119、Safari 16.5-17.1 为部分支持。IE 11 不支持。}}

{{知识点24}}{{部分支持 vs 完全支持的差异：部分支持（Chrome 112-119、Safari 16.5-17.1）要求嵌套规则以符号（&、.、#、[、:）开头，不能直接以标签名开头。完全支持移除了此限制。}}

{{知识点25}}{{& 在嵌套规则外的含义：如果 & 用在嵌套上下文之外（顶层），它代表 :scope，即文档根。& { color: blue; } 会让文档根元素文字变蓝。}}

{{知识点26}}{{& 不能代表伪元素：& 选择器等价于 :is()，无法代表伪元素。.foo::before { .important & { color: red; } } 不会生效，因为 :is(.foo::before) 无法匹配任何内容。}}

{{知识点27}}{{嵌套规则中的伪元素限制：嵌套的 @media 等 at-rule 中的属性隐式包裹在 & 中，因此对伪元素的选择器也同样受限。.foo::before { @media (width < 600px) { color: red; } } 不会生效。}}

{{知识点28}}{{CSS Nesting 提升可读性：嵌套将相关的样式规则组织在一起，反映 HTML 的层级结构，使样式表更容易理解和维护。对比扁平的选择器写法，嵌套能直观看出组件的父子关系。}}

{{知识点29}}{{CSS Nesting 减少文件大小：嵌套消除了重复的选择器前缀，减少了 CSS 文件体积，从而降低用户下载数据量。在大型项目中效果尤为明显。}}

{{知识点30}}{{CSS Nesting 与 BEM 方法论：由于 CSS Nesting 不支持选择器拼接，BEM 中的 &__element 和 &--modifier 模式无法直接使用。需要改用完整的类名或结合 CSS Modules 等工具。}}

{{知识点31}}{{渐进增强策略：可以使用 @supports 规则检测浏览器是否支持 CSS Nesting，为不支持的浏览器提供回退样式。@supports (selector(&)) { /* 嵌套样式 */ }。}}

{{知识点32}}{{CSS Nesting 的级联顺序：嵌套规则的级联（cascade）行为与展开后的等价非嵌套规则完全一致。样式优先级由来源、特异性、顺序共同决定，嵌套不改变这些规则。}}

{{知识点33}}{{嵌套中的注释：在嵌套规则中可以正常使用 CSS 注释。注释可以放在父规则和子规则之间，不影响解析。}}

{{知识点34}}{{深层嵌套：CSS Nesting 支持多层嵌套。.a { .b { .c { .d {} } } } 等价于 .a .b .c .d {}。但应避免过深嵌套以保持可维护性，建议不超过 3 层。}}

{{知识点35}}{{CSS Nesting 的规范状态：CSS Nesting Module Level 1 是 W3C 工作草案，已在所有主流浏览器中实现。规范地址为 https://drafts.csswg.org/css-nesting-1/。}}

{{知识点36}}{{与 CSS Modules/CSS-in-JS 的关系：CSS Nesting 是原生 CSS 特性，可与 CSS Modules、CSS-in-JS 等工具配合使用。一些 CSS-in-JS 库（如 styled-components）早已支持嵌套语法，现在原生 CSS 也具备了同等能力。}}

{{知识点37}}{{嵌套中的属性声明位置：在嵌套规则中，直接属性声明（非嵌套子规则）按照书写顺序应用。可以混合属性声明和嵌套规则，如 .box { color: red; .child { font-size: 14px; } background: white; }。}}

{{知识点38}}{{性能影响：CSS Nesting 对渲染性能几乎没有负面影响。浏览器解析嵌套 CSS 的过程与解析展开后的非嵌套 CSS 基本相同，选择器匹配性能取决于最终的展开结果。}}

{{知识点39}}{{PostCSS 插件支持：postcss-nesting 和 postcss-preset-env 等工具可以在构建阶段将 CSS Nesting 语法转换为非嵌套等价形式，为旧浏览器提供兼容。}}

{{知识点40}}{{实际项目迁移建议：从 Sass/Less 迁移到原生 CSS Nesting 时，需注意：移除选择器拼接用法、替换变量语法（改用 CSS 自定义属性）、测试浏览器兼容性、逐步迁移而非一次性切换。}}
