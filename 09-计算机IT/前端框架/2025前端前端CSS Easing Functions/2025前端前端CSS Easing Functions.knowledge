# 2025前端前端CSS Easing Functions
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}{{CSS Easing Functions 是什么}}
CSS Easing Functions（缓动函数）是 CSS 中用于控制动画或过渡中值变化速率的函数。它们定义了时间进度（输入）与动画进度（输出）之间的映射关系，使动画不再是简单的线性变化，而是可以加速、减速或跳跃式变化。规范来源：CSS Easing Functions Level 1（W3C）。

{{知识点2}}{{三大类缓动函数}}
CSS 缓动函数分为三大类：① linear 缓动函数（线性），② cubic-bezier 缓动函数（三次贝塞尔曲线），③ steps 缓动函数（阶梯函数）。每类都有关键字简写和函数形式两种写法。

{{知识点3}}{{linear 关键字}}
`linear` 关键字等价于 `linear(0, 1)`，也等价于 `cubic-bezier(0, 0, 1, 1)`。表示动画以恒定速率进行，没有加速或减速。

{{知识点4}}{{linear() 函数语法}}
`linear()` 函数语法为 `linear(<number> [<percentage>{0,2}], ...)`，至少需要两个值。通过指定多个进度点及其可选的百分比位置，可以在点之间进行线性插值来近似复杂曲线。例如：`linear(0, 0.25, 1)` 或 `linear(0, 0.5 25% 75%, 1)`。

{{知识点5}}{{linear() 百分比停驻点}}
在 `linear()` 中，百分比值用于控制每个进度点在动画时间线上的位置。如 `linear(0, 0.25 75%, 1)` 表示在 75% 的时间内完成 25% 的进度，剩余 75% 的进度在最后 25% 的时间内完成。未指定百分比时，各点均匀分布。

{{知识点6}}{{linear() 双百分比停驻}}
`linear()` 支持双百分比语法定义停驻区间。例如 `linear(0, 0.5 25% 75%, 1)` 表示在 25% 时达到 0.5，然后保持 0.5 直到 75%，等价于 `linear(0, 0.5 25%, 0.5 75%, 1)`。

{{知识点7}}{{cubic-bezier() 函数语法}}
`cubic-bezier()` 接受四个参数：`cubic-bezier(x1, y1, x2, y2)`，分别定义两个控制点 P1(x1,y1) 和 P2(x2,y2) 的坐标。起点 P0 固定在 (0,0)，终点 P3 固定在 (1,1)。x 坐标必须在 [0,1] 范围内，y 坐标可以超出此范围。

{{知识点8}}{{ease 关键字}}
`ease` 是默认的缓动函数，等价于 `cubic-bezier(0.25, 0.1, 0.25, 1)`。动画开始时缓慢，中间加速，结束时逐渐减速。

{{知识点9}}{{ease-in 关键字}}
`ease-in` 等价于 `cubic-bezier(0.42, 0, 1, 1)`。动画开始时缓慢，然后逐渐加速，到终点时突然停止。适合模拟物体开始运动的场景。

{{知识点10}}{{ease-out 关键字}}
`ease-out` 等价于 `cubic-bezier(0, 0, 0.58, 1)`。动画开始时突然启动，然后逐渐减速到终点。适合模拟物体减速停止的场景。

{{知识点11}}{{ease-in-out 关键字}}
`ease-in-out` 等价于 `cubic-bezier(0.42, 0, 0.58, 1)`。动画开始时缓慢加速，中间最快，结束时缓慢减速。行为类似于 `ease`，但对称性更好。

{{知识点12}}{{cubic-bezier 控制点 y 值超出范围的效果}}
当 cubic-bezier 的 y1 或 y2 值超出 [0,1] 范围时，会产生"弹跳"效果——动画值会超过最终状态再返回。例如 `cubic-bezier(0.3, 0.8, 0.3, 2.3)` 会让元素先超过目标位置再弹回。但某些属性（如颜色值）会裁剪超出范围的输出。

{{知识点13}}{{cubic-bezier 无效值判断}}
cubic-bezier 无效的情况包括：① x 坐标超出 [0,1] 范围，② 参数不是数字，③ 参数数量不是 4 个。无效的 cubic-bezier 会导致整个 CSS 属性被忽略。

{{知识点14}}{{steps() 函数语法}}
`steps()` 语法为 `steps(<integer>, <step-position>?)`。第一个参数指定等距间隔的数量（正整数），第二个参数可选，指定跳跃发生的时间点。默认值为 `end`。

{{知识点15}}{{step-start 关键字}}
`step-start` 等价于 `steps(1, jump-start)`。动画立即跳到最终状态并保持到结束。适合需要瞬间切换效果的场景。

{{知识点16}}{{step-end 关键字}}
`step-end` 等价于 `steps(1, jump-end)`。动画保持初始状态直到结束时才跳到最终状态。

{{知识点17}}{{steps 的 jump-start 位置}}
`jump-start`（或 `start`）表示第一个阶梯在动画开始时发生跳跃。动画一开始就立即进入第一个步骤。

{{知识点18}}{{steps 的 jump-end 位置}}
`jump-end`（或 `end`）表示最后一个阶梯在动画结束时发生跳跃。这是默认行为，值在每个间隔结束时改变。

{{知识点19}}{{steps 的 jump-none 位置}}
`jump-none` 表示既不提前也不延迟跳跃。使用此选项时，整数参数必须大于 1。动画在开始和结束时都不会跳跃，而是在整个持续时间内均匀分布步骤。

{{知识点20}}{{steps 的 jump-both 位置}}
`jump-both` 表示同时在开始和结束时都发生跳跃。动画开始时立即跳跃一次，结束时也跳跃一次。

{{知识点21}}{{animation-timing-function 属性}}
`animation-timing-function` 属性用于设置 CSS 动画的缓动函数。接受所有 `<easing-function>` 值。可以应用于 `@keyframes` 规则中的每个关键帧，控制从该关键帧到下一个关键帧之间的变化速率。

{{知识点22}}{{transition-timing-function 属性}}
`transition-timing-function` 属性用于设置 CSS 过渡的缓动函数。语法与 `animation-timing-function` 相同，接受所有 `<easing-function>` 值。

{{知识点23}}{{steps 多段动画中的行为}}
如果动画包含多个关键帧段（segments），`steps()` 中指定的步数会应用于每个段。例如动画有 3 段且使用 `steps(2)`，总共会产生 6 个步骤，每段 2 个。

{{知识点24}}{{cubic-bezier 曲线的数学原理}}
三次贝塞尔曲线由四个点定义：P0（起点）、P1（控制点1）、P2（控制点2）、P3（终点）。在 CSS 中 P0=(0,0)，P3=(1,1)。曲线方程通过参数 t（0到1）计算 x 和 y 值，x 轴表示输入进度，y 轴表示输出进度。

{{知识点25}}{{linear 关键字与 linear() 函数的区别}}
`linear` 关键字总是被解释为 `linear(0, 1)`，而 `linear(0, 1)` 函数形式被解释为 `linear(0 0%, 1 100%)`。两者效果相同，但语义解析路径不同。

{{知识点26}}{{linear() 超出 0-1 范围的值}}
`linear()` 的进度值允许超出 0 到 1 的范围。例如 `linear(0, 1.5, 1)` 中的 1.5 表示动画会"过冲"到 150% 的位置再回来。

{{知识点27}}{{缓动函数在 Web 性能中的作用}}
使用 CSS 缓动函数（而非 JavaScript 手动计算）可以让浏览器将动画交给合成线程处理，特别是在使用 `transform` 和 `opacity` 时可以触发 GPU 加速，避免主线程重排重绘，提升动画性能。

{{知识点28}}{{steps() 的常见应用场景}}
steps() 函数常用于：① 精灵图动画（逐帧动画），② 打字机效果，③ 数字滚动计数器，④ 机械式旋转效果，⑤ 任何需要离散跳跃而非平滑过渡的动画。

{{知识点29}}{{cubic-bezier.com 可视化工具}}
Lea Verou 创建的 cubic-bezier.com 是一个在线可视化工具，允许开发者通过拖拽控制点来设计自定义贝塞尔曲线，并实时预览动画效果。MDN 官方文档推荐使用此工具。

{{知识点30}}{{linear() easing generator 工具}}
Jake Archibald 创建的 linear-easing-generator.netlify.app 是一个在线工具，帮助开发者生成 `linear()` 函数的点序列，用于近似任意缓动曲线。

{{知识点31}}{{CSS Easing Functions Level 2 草案}}
CSS Easing Functions Level 2 规范正在 W3C 草案阶段（css-easing-2），可能引入更多增强功能。Level 1 已被所有主流浏览器完整支持。

{{知识点32}}{{缓动函数的浏览器兼容性}}
CSS Easing Functions Level 1 中的所有关键字和函数（linear、cubic-bezier、steps 及其变体）在所有主流现代浏览器中得到完整支持，包括 Chrome、Firefox、Safari、Edge。`linear()` 函数在较新版本的浏览器中支持。

{{知识点33}}{{缓动函数与 @keyframes 的交互}}
在 `@keyframes` 规则中，可以在每个关键帧选择器上单独指定 `animation-timing-function`。这意味着同一个动画中不同阶段可以使用不同的缓动函数，实现更复杂的运动效果。

{{知识点34}}{{steps() 默认参数}}
`steps()` 的第二个参数（step-position）默认为 `end`。因此 `steps(4)` 等价于 `steps(4, end)`。在需要默认行为时可以省略。

{{知识点35}}{{cubic-bezier 的常用自定义曲线}}
常见的自定义 cubic-bezier 曲线包括：① `cubic-bezier(0.68, -0.55, 0.27, 1.55)` 弹性效果，② `cubic-bezier(0.25, 0.46, 0.45, 0.94)` 柔和减速，③ `cubic-bezier(0.55, 0.085, 0.68, 0.53)` 快速加速减速。这些曲线可以通过 cubic-bezier.com 工具验证。

{{知识点36}}{{will-change 与缓动函数的配合}}
在使用复杂缓动函数的动画元素上，可以配合 `will-change` 属性提示浏览器提前做好优化准备。例如 `will-change: transform` 可以让浏览器为 transform 动画创建独立的合成层。

{{知识点37}}{{缓动函数的可访问性考量}}
对于有前庭功能障碍（vestibular disorders）的用户，剧烈的缓动效果（如弹跳、快速加速）可能引起不适。建议通过 `prefers-reduced-motion` 媒体查询为这些用户提供更温和的缓动函数或直接禁用动画。
