# 2025前端Notification API
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}{{Notification API 概述：Notification API 允许网页在系统级别向用户显示通知，即使用户不在当前标签页也能收到提醒。它由 Notifications API 规范定义（WHATWG），是 Web 应用实现消息推送、待办提醒等功能的核心 API。}}

{{知识点2}}{{Notification 权限模型：使用通知前必须获得用户授权。Notification.permission 返回三种状态：'default'（未询问）、'granted'（已允许）、'denied'（已拒绝）。权限一旦设置通常在当前会话中持久保存。}}

{{知识点3}}{{requestPermission 方法：Notification.requestPermission() 是请求通知权限的静态方法，返回 Promise。应在用户手势（如点击按钮）的事件处理中调用，浏览器会弹出权限对话框让用户选择允许或拒绝。}}

{{知识点4}}{{用户手势要求：从 Firefox 72 和 Safari 开始，浏览器明确禁止非用户手势触发的通知权限请求。这意味着不能在页面加载时自动弹出权限对话框，必须由用户主动操作触发。}}

{{知识点5}}{{安全上下文要求：Notification API 仅在安全上下文（HTTPS）中可用。Chrome 和 Firefox 都要求页面通过 HTTPS 提供服务。localhost 在开发环境中通常被视为安全上下文。}}

{{知识点6}}{{Notification 构造函数：new Notification(title, options) 创建非持久通知实例。title 是必填参数，options 可选，包含 body、icon、tag 等配置。构造函数仅在页面上下文中可用，在 ServiceWorkerGlobalScope 中调用会抛出 TypeError。}}

{{知识点7}}{{非持久通知与持久通知：非持久通知通过 new Notification() 创建，生命周期与页面绑定，页面关闭后通知不可交互。持久通知通过 ServiceWorkerRegistration.showNotification() 创建，可在页面关闭后继续存在和响应交互。}}

{{知识点8}}{{Notification 实例属性 - title：notification.title 是只读属性，返回通知标题，即构造函数的第一个参数。标题显示在通知窗口顶部，是用户最先看到的内容。}}

{{知识点9}}{{Notification 实例属性 - body：notification.body 是只读字符串，显示在标题下方的通知正文。默认为空字符串。用于传递通知的详细信息，如消息内容、任务描述等。}}

{{知识点10}}{{Notification 实例属性 - icon：notification.icon 是只读字符串，包含通知图标的 URL。图标显示在通知中帮助用户快速识别通知来源，通常使用应用的品牌图标。}}

{{知识点11}}{{Notification 实例属性 - image：notification.image 是只读字符串，包含要作为通知一部分显示的图片 URL。用于在通知中展示富媒体内容，如消息预览图、文章缩略图等。}}

{{知识点12}}{{Notification 实例属性 - tag：notification.tag 是只读字符串，作为通知的标识符。具有相同 tag 的通知会被替换：如果同 tag 通知尚未显示，新的替换旧的；如果已显示，旧的被关闭，新的显示。}}

{{知识点13}}{{Notification 实例属性 - data：notification.data 是只读属性，返回通知关联的任意数据，通过结构化克隆算法存储。可用于在通知事件处理中获取关联数据，如消息 ID、跳转 URL 等。}}

{{知识点14}}{{Notification 实例属性 - navigate：notification.navigate 是只读属性，指定激活通知时要导航到的 URL。设置后，用户激活通知时会自动导航到该 URL，而不是触发 click 事件。这简化了常见的"点击通知打开页面"场景。}}

{{知识点15}}{{Notification 实例属性 - requireInteraction：notification.requireInteraction 是只读布尔值，设为 true 时通知会保持显示直到用户点击或关闭，而非自动消失。默认为 false，适用于需要用户立即处理的重要通知。}}

{{知识点16}}{{Notification 实例属性 - silent：notification.silent 是只读布尔值，设为 true 时通知静音，不产生声音或振动。默认为 null（遵循设备设置）。设置为 true 时不能同时指定 vibrate 属性。}}

{{知识点17}}{{Notification 实例属性 - renotify：notification.renotify 是只读布尔值，设为 true 时，新通知替换旧通知后仍会再次通知用户。默认为 false。使用此选项时必须同时设置 tag 属性。}}

{{知识点18}}{{Notification 实例属性 - vibrate：notification.vibrate 是只读属性，指定通知触发时设备的振动模式。值为振动模式数组（如 [200, 100, 200]）。设置 silent 为 true 时不能使用此属性。}}

{{知识点19}}{{Notification 实例属性 - dir 和 lang：notification.dir 指定通知文本方向（auto/ltr/rtl），notification.lang 指定语言代码（BCP 47 格式）。dir 默认为 auto（跟随浏览器语言设置），lang 默认为空字符串。}}

{{知识点20}}{{Notification 实例属性 - badge 和 timestamp：badge 是在空间不足时显示的小图标 URL（Android 上约 96x96px）。timestamp 是 Unix 时间戳（毫秒），表示通知关联的时间，可以是过去（离线消息）或未来（会议提醒）。}}

{{知识点21}}{{Notification 实例属性 - actions：notification.actions 是只读数组，定义通知的操作按钮。注意：非持久通知（new Notification）的 actions 必须为空数组，操作按钮仅在持久通知（ServiceWorkerRegistration.showNotification）中可用。}}

{{知识点22}}{{Notification 实例方法 - close：notification.close() 用于程序化关闭通知。现代浏览器通常在约 4 秒后自动关闭通知，但 close() 可用于提前关闭已不再相关（如用户已在网页中查看）的通知。注意：close() 也会从通知托盘中移除通知。}}

{{知识点23}}{{Notification 实例事件 - click：当用户点击通知时触发 click 事件。可通过 addEventListener('click', handler) 或 onclick 属性监听。如果设置了 navigate 属性，则不触发 click 事件而是直接导航。}}

{{知识点24}}{{Notification 实例事件 - close/error/show：close 事件在通知关闭时触发（不保证是用户主动关闭）；error 事件在通知出错时触发；show 事件在通知显示给用户时触发。这些事件可用于追踪通知的生命周期。}}

{{知识点25}}{{通知替换机制 - tag 的使用：通过给通知设置相同的 tag，可以实现通知替换。快速发送多个同 tag 通知时，浏览器会合并为一条，避免用户被大量通知淹没。这在消息应用中非常实用，如批量消息只显示最新一条。}}

{{知识点26}}{{ServiceWorkerRegistration.showNotification：持久通知的创建方法，从 Service Worker 注册对象调用。语法：registration.showNotification(title, options)。options 支持完整的通知选项包括 actions 操作按钮数组。返回 Promise。}}

{{知识点27}}{{持久通知的操作按钮：通过 ServiceWorkerRegistration.showNotification 的 options.actions 数组定义操作按钮。每个 action 对象包含 action（标识字符串）、title（显示文本）和可选的 icon（图标 URL）和 navigate（导航 URL）。}}

{{知识点28}}{{notificationclick 事件：Service Worker 全局作用域的事件，当用户点击持久通知时触发。事件对象为 NotificationEvent 类型，继承自 ExtendableEvent。通过 event.notification 获取通知对象，event.action 获取用户点击的操作按钮 ID。}}

{{知识点29}}{{notificationclose 事件：Service Worker 全局作用域的事件，当持久通知被关闭时触发。可用于追踪通知关闭行为，执行清理操作。注意：关闭不一定是用户主动操作，也可能是系统自动清理。}}

{{知识点30}}{{NotificationEvent 接口：表示在 ServiceWorkerGlobalScope 上分发的通知事件。继承自 ExtendableEvent。提供 notification 属性（返回对应的 Notification 对象）和 action 属性（返回用户点击的操作按钮 ID 字符串）。}}

{{知识点31}}{{ServiceWorkerRegistration.getNotifications：返回当前 Service Worker 注册的所有通知列表，按创建顺序排列。可传入 filter 参数（如 { tag: 'messages' }）过滤特定标签的通知。用于检查和管理已有通知。}}

{{知识点32}}{{Push API 与 Notification API 的关系：Push API 负责从服务器接收推送消息（即使应用未运行），Notification API 负责将消息展示为系统通知。两者通常配合使用：Push API 的 push 事件处理器中调用 showNotification 显示通知。}}

{{知识点33}}{{PushManager.subscribe：通过 PushManager.subscribe() 订阅推送通知，返回 PushSubscription 对象，包含端点 URL 和加密密钥。将订阅信息发送到服务器后，服务器即可向该端点推送消息。每个订阅对 Service Worker 是唯一的。}}

{{知识点34}}{{移动端通知限制：在大多数移动浏览器上，直接使用 new Notification() 构造函数会抛出 TypeError。移动端应注册 Service Worker 并使用 ServiceWorkerRegistration.showNotification()。这是因为移动页面通常不在后台运行。}}

{{知识点35}}{{跨域 iframe 限制：Chrome 和 Firefox 已禁止从跨域 iframe 中请求通知权限。这意味着嵌入的第三方内容无法请求通知权限，增强了用户安全性，防止恶意网站通过 iframe 滥用通知。}}

{{知识点36}}{{通知最佳实践 - 渐进式请求：不要在页面加载时立即请求通知权限。应在用户了解通知用途后，通过明确的"启用通知"按钮触发请求。提供上下文说明通知将发送什么内容，提高用户授权率。}}

{{知识点37}}{{通知最佳实践 - 价值驱动：只发送对用户有价值的通知。每个通知应有明确目的：提醒、新消息、状态更新等。过度通知会导致用户关闭通知权限甚至弃用应用。}}

{{知识点38}}{{notificationclick 中的窗口管理：在 notificationclick 事件处理器中，通常需要检查是否已有打开的应用窗口，如果有则聚焦该窗口，没有则打开新窗口。使用 clients.matchAll({ type: 'window' }) 查找现有客户端，clients.openWindow() 打开新窗口。}}

{{知识点39}}{{waitUntil 在通知事件中的使用：notificationclick 和 notificationclose 事件继承自 ExtendableEvent，支持 event.waitUntil(promise) 方法。用于延长事件生命周期直到异步操作完成，如打开窗口、发送分析数据等。}}

{{知识点40}}{{Notification.maxActions 静态属性：Notification.maxActions 是只读静态属性，返回设备和浏览器支持的最大操作按钮数量。开发时应检查此值以确保操作按钮不超过平台限制，避免部分按钮被截断。}}

{{知识点41}}{{通知的结构化克隆：Notification 的 data 属性使用结构化克隆算法存储，支持大多数 JavaScript 数据类型（对象、数组、Date、Map、Set 等）。不支持函数和 DOM 节点。这保证了数据在不同上下文间传递的可靠性。}}

{{知识点42}}{{VAPID 与 Web Push：VAPID（Voluntary Application Server Identification）是 Web Push 的认证机制。服务器生成公私钥对，订阅时将公钥传给客户端，推送时用私钥签名。这确保只有合法的服务器能向订阅者发送推送消息。}}

{{知识点43}}{{通知权限的不可逆性：一旦用户做出允许或拒绝的选择，权限通常不能通过代码自动更改。被拒绝后，用户需要手动到浏览器设置中修改权限。因此首次请求权限的时机和方式至关重要。}}

{{知识点44}}{{通知与页面可见性：使用 document.visibilityState 配合通知可以优化用户体验。当页面不可见时发送系统通知；当页面变为可见时关闭通知。使用 visibilitychange 事件监听页面可见性变化。}}

{{知识点45}}{{浏览器兼容性注意事项：Notification API 在所有主流桌面浏览器中得到支持，但移动浏览器支持有限。Safari 从 OS X 10.8+ 支持。Chrome、Firefox、Edge 均支持完整 API。移动端建议使用 Service Worker 方案。}}

{{知识点46}}{{Firefox 的推送配额限制：Firefox 对推送消息有配额限制，但生成通知的推送消息不受此限制。配额在用户每次访问站点时刷新。Chrome 没有推送配额限制。开发者应注意这些平台差异。}}

{{知识点47}}{{通知规范来源：Notification API 的规范由 WHATWG 维护，地址为 https://notifications.spec.whatwg.org/。Push API 由 W3C 维护。两个规范协同定义了完整的 Web 通知推送体系。}}

{{知识点48}}{{Notification 继承自 EventTarget：Notification 接口继承自 EventTarget，因此支持 addEventListener、removeEventListener 和 dispatchEvent 等标准事件方法，遵循 Web 标准事件模型。}}

{{知识点49}}{{通知在 Web Workers 中可用：Notification 构造函数在 Web Workers 中可用（不仅限于主线程），但在 ServiceWorkerGlobalScope 中不可用。Service Worker 中必须使用 ServiceWorkerRegistration.showNotification()。}}

{{知识点50}}{{通知的 structured clone 与序列化：当 data 属性序列化失败时会抛出 DataCloneError DOMException。确保传入 data 的数据类型是结构化克隆算法支持的，避免传入函数、Symbol 等不可克隆的值。}}
