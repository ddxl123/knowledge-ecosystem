# 2025前端Ember.js入门
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}
{{Ember.js 是什么？}}
Ember.js 是一个生产力高、经过实战检验的 JavaScript 框架，用于构建现代 Web 应用程序。它提供了构建丰富 UI 所需的一切，支持在任何设备上运行。Ember 通过提供管理现代 Web 应用复杂性的核心功能，以及一个支持快速迭代的集成开发工具包，帮助开发者高效工作。Ember.js 的核心理念是"约定优于配置"（Convention over Configuration），通过共享约定让开发者能够构建可复用的生态系统。

{{知识点2}}
{{Ember CLI 命令行工具}}
Ember CLI 是 Ember 的官方命令行工具，用于创建、开发和构建 Ember 应用。全局安装命令为 `npm install -g ember-cli`。核心命令包括：`ember new <app-name>` 创建新应用，`ember serve` 启动开发服务器，`ember generate` 生成代码脚手架，`ember build` 构建生产版本，`ember test` 运行测试。2025 版 Ember 已集成 Vite 作为构建工具，开发服务器启动速度更快。

{{知识点3}}
{{创建第一个 Ember 应用}}
使用 `ember new ember-quickstart --lang en --strict` 命令创建新应用。`--lang en` 设置应用主语言以提升无障碍性，`--strict` 启用 TypeScript 严格模式。创建完成后进入目录执行 `npm start`，默认在 http://localhost:4200 运行。新应用开箱即包含：开发服务器、模板编译、JS/CSS 压缩、通过 Babel 支持的现代语法特性。

{{知识点4}}
{{Glimmer 渲染引擎}}
Glimmer 是 Ember 底层使用的渲染引擎，以其极高的渲染性能著称。开发者无需直接学习 Glimmer API，它在框架内部运作。Glimmer 引擎使得 Ember 的模板编译和 DOM 更新非常高效，是 Ember Octane 版本的核心技术基础。Glimmer.js 也可以作为独立的轻量级 UI 框架使用。

{{知识点5}}
{{应用模板（Application Template）}}
应用模板位于 `app/templates/application.gjs`，是 Ember 应用的根模板，始终在屏幕上显示。`.gjs` 扩展名代表 "Glimmer JavaScript"，允许在同一文件中编写 JavaScript 和模板。模板使用 `<template>` 标签包裹 HTML 内容。`{{outlet}}` 是一个特殊占位符，用于渲染子路由的内容。

{{知识点6}}
{{路由（Routing）}}
路由是 Ember 应用的核心部分，用于驱动应用状态与 URL 的映射关系。URL 可通过用户首次加载、手动修改地址栏、点击链接或应用内事件来改变。Ember 路由器将当前 URL 映射到一个或多个路由处理器（Route Handler）。路由处理器可以：渲染模板、加载模型数据、重定向到新路由、处理涉及模型变更或路由转换的操作。使用 `ember generate route <name>` 生成路由。

{{知识点7}}
{{路由的模型钩子（Model Hook）}}
路由的 `model()` 方法是获取数据的核心位置。当用户首次访问路由时，model 钩子自动运行。model 钩子返回的数据会自动在模板中以 `@model` 可用，在控制器中以 `this.model` 可用。它可以返回任何类型的数据（字符串、对象、数组），最常见的模式是返回 Promise。如果返回 Promise，路由会等待 Promise 解析后再渲染模板。

{{知识点8}}
{{动态路由段（Dynamic Segments）}}
在路由定义中使用 `:param_name` 语法定义动态段，例如 `this.route('post', { path: '/post/:post_id' })`。动态段的值在 URL 中会变化，并在路由的 model 钩子中通过 `params` 对象获取，如 `params.post_id`。这是实现详情页面等需要根据 URL 参数加载不同数据的常用模式。

{{知识点9}}
{{组件（Components）}}
组件是 Ember 应用的基本构建块，用于创建可复用的 UI 元素并定义应用的布局和功能。Ember Octane 之后推荐使用 Glimmer 组件（`@glimmer/component`），它是轻量级的、无状态的。组件使用 `.gjs` 文件格式，将 JavaScript 逻辑和模板放在同一文件中。组件可以通过 `ember generate component <name>` 生成。

{{知识点10}}
{{组件参数（Arguments）}}
组件通过 `@` 前缀接收外部传入的参数，如 `<Hello @name="Jen Weber" />`。在组件内部通过 `@name` 访问参数。参数是自动追踪的（automatically tracked），当参数值变化时，组件会自动更新重新渲染。这是 Ember 数据向下流动的核心机制。

{{知识点11}}
{{追踪属性（@tracked）}}
`@tracked` 是 Ember Octane 的核心响应式机制，来自 `@glimmer/tracking` 模块。用 `@tracked` 装饰器标记的属性值发生变化时，Ember 会自动检测并重新渲染使用了该属性的模板部分。追踪还支持通过 getter、方法和函数进行链式追踪——如果一个 tracked 属性被某个 getter 使用，该 getter 也会被追踪。参数（`@arg`）默认是自动追踪的。

{{知识点12}}
{{自动追踪（Autotracking）}}
Autotracking 是 Ember 的响应式模型，决定了何时重新渲染以及渲染什么。首次渲染时，Ember 记录所有被使用的追踪值。默认假设渲染值不会变化，使用 `@tracked` 标记可能变化的值。当 tracked 值变化时，Ember 只重新渲染受影响的部分，而非整个组件，实现精准高效的更新。

{{知识点13}}
{{模板语法基础}}
Ember 模板基于标准 HTML，支持所有有效的 HTML 特性：Web Components、SVG、HTML 注释、空白字符规则等。额外语法包括：`{{expression}}` 插值表达式、`{{#each}}...{{/each}}` 列表循环、`{{#if}}...{{else}}...{{/if}}` 条件渲染。Ember 还支持自闭合语法 `<div />` 作为 `<div></div>` 的简写。

{{知识点14}}
{{each 循环助手}}
`{{#each}}` 是 Ember 内置的块级助手，用于遍历数组并渲染列表。语法为 `{{#each @model as |item|}}...{{/each}}`，其中 `|item|` 定义每次迭代的变量名。在 `.gjs` 模板中使用时，通常配合 `@model` 访问路由模型数据。循环体内的 HTML 会对数组中每个元素渲染一次。

{{知识点15}}
{{助手函数（Helper Functions）}}
助手函数是从模板中调用的 JavaScript 函数。Ember 的模板语法限制了可表达的内容以保持结构清晰，当需要使用 JavaScript 计算时使用助手。助手可以是本地定义的普通函数，也可以从其他文件导入。支持位置参数和命名参数，如 `{{substring @username start=0 end=1}}`。助手可嵌套使用，用括号包裹：`{{sum (multiply 2 4) 2}}`。

{{知识点16}}
{{服务（Services）}}
Service 是在应用整个生命周期中存在的 Ember 对象，可在应用的不同部分注入使用。服务适用于需要共享状态或持久连接的功能，如：用户认证、地理位置、WebSocket、服务器推送通知、第三方 API、日志记录等。使用 `ember generate service <name>` 创建，通过 `@service` 装饰器注入到组件中。服务是懒加载的，一旦加载则持续到应用退出。

{{知识点17}}
{{服务注入}}
在组件中使用 `@service` 装饰器注入服务。无参数时按属性名匹配服务：`@service shoppingCart;`。也可指定服务名：`@service('shopping-cart') cart;`。注入的服务可在模板中直接使用。对于可能不存在的服务，使用 `getOwner(this).lookup('service:name')` 查找，避免注入时抛出错误。

{{知识点18}}
{{EmberData 概述}}
EmberData 是 Ember 内置的强大数据管理库（可选但默认包含），提供格式化请求、规范化响应和高效本地数据缓存的工具。Ember 本身可与任何后端配合：REST、JSON:API、GraphQL 等。EmberData 默认与 JSON:API 规范配合工作，也可通过适配器配置支持其他后端格式。它支持流式服务器（如 WebSocket）实现实时 UI。

{{知识点19}}
{{Store 和单一数据源}}
EmberData 的 Store 是应用中模型的中央存储库，是"单一数据源"（Single Source of Truth）。路由和控制器向 Store 请求模型，Store 负责知道如何获取数据。当多个组件请求同一模型时，Store 能检测到并只发送一次请求，避免重复获取和数据不同步问题。这遵循 SOLID 原则中的单一职责原则。

{{知识点20}}
{{EmberData 模型}}
EmberData 模型是表示应用底层数据的对象，与路由的 model 钩子是不同概念。模型通常是持久化的，数据变更需要存储在不会丢失的地方。典型的模型数据从服务器加载并保存到服务器。EmberData 通过适配器模式支持多种后端，包括 JSON:API、REST 等。使用 `this.store.findAll('model')` 或 `this.store.findRecord('model', id)` 查询数据。

{{知识点21}}
{{本地类（Native Classes）}}
Ember 全面采用 ES2015+ 的原生 JavaScript 类语法。类使用 `class` 关键字定义，通过 `new` 关键字创建实例。类的四大元素：构造函数（constructor）、方法（methods）、字段（fields）、访问器（getters/setters）。两种修饰符：`static` 和装饰器（decorators）。Ember 推荐始终为类命名，便于调试和工具支持。

{{知识点22}}
{{类字段（Class Fields）}}
类字段允许在类定义中直接声明实例属性，语法为 `name = 'value'`。每个实例获得字段的独立副本——这对原始值不重要，但对对象和数组很关键（每个实例的数组是不同引用）。字段初始化时可使用 `this`，但应避免在字段初始化器中依赖其他字段的状态，因为字段初始化顺序可能变化。

{{知识点23}}
{{装饰器（Decorators）}}
装饰器是 Ember 中广泛使用的语法特性，用于修改类、方法或属性的行为。常见装饰器包括：`@tracked` 标记响应式属性、`@service` 注入服务、`@action` 绑定方法上下文。装饰器使用 `@` 前缀，放在被装饰的元素之前。它们是 TC39 提案的一部分，Ember 已在生产中广泛使用。

{{知识点24}}
{{路由生成器}}
使用 `ember generate route <name>` 命令生成路由，会自动创建：路由模板文件（`.gjs`）、路由对象文件（`.js`）、在 `app/router.js` 中添加路由条目、路由的单元测试文件。这是 Ember CLI 脚手架功能的典型体现，减少了样板代码的编写。

{{知识点25}}
{{嵌套路由与 {{outlet}}}}
Ember 支持嵌套路由，子路由的内容渲染在父路由模板的 `{{outlet}}` 位置。例如 application 模板中的 `{{outlet}}` 会渲染当前路由的模板。这使得应用可以有层级化的页面结构，共享布局和导航元素。路由嵌套关系在 `app/router.js` 中定义。

{{知识点26}}
{{LinkTo 组件}}
`<LinkTo>` 是 Ember 内置的组件，用于创建指向应用内路由的链接。它替代了标准的 `<a>` 标签用于内部导航，自动处理路由转换而不触发页面刷新。使用方式如 `<LinkTo @route="posts">Posts</LinkTo>`，支持传递动态段参数和查询参数。

{{知识点27}}
{{查询参数（Query Parameters）}}
查询参数是 URL 中 `?key=value` 形式的参数，在 Ember 中通过控制器定义和管理。与动态段不同，查询参数是可选的，常用于过滤、排序、分页等场景。控制器是放置 URL 查询参数以及从路由层传递给组件的操作和属性的正确位置。

{{知识点28}}
{{Promise 与异步模型}}
Ember 的 model 钩子原生支持 Promise，这是处理异步数据加载的核心模式。可以使用 `async/await` 语法或直接返回 Promise。当 model 返回 Promise 时，路由自动等待解析后才渲染模板。Ember 还提供 `RSVP.hash` 来并行处理多个 Promise，返回包含所有结果的对象。

{{知识点29}}
{{multiple models 加载}}
当一个路由需要返回多个 API 请求的结果时，使用 `RSVP.hash`。例如：`model() { return RSVP.hash({ songs: this.store.findAll('song'), albums: this.store.findAll('album') }); }`。在模板中通过 `@model.songs` 和 `@model.albums` 分别访问。所有 Promise 都解析后路由才会渲染。

{{知识点30}}
{{测试（Testing）}}
Ember 将测试视为一等公民。运行测试：`npm test` 执行一次完整测试套件，开发服务器运行时访问 `/tests` 可看到 QUnit 测试界面并自动更新。支持过滤测试（Filter 输入框）、单个测试重跑。调试技巧：在代码中添加 `debugger` 语句，或在测试中使用 `await pauseTest()` 暂停执行，完成后在控制台调用 `resumeTest()` 继续。

{{知识点31}}
{{Ember Inspector}}
Ember Inspector 是一个浏览器扩展（也可作为书签工具），用于实时检查和调试 Ember 应用。它可以检查组件树、查看路由、检查 EmberData 记录、监控性能等。也是识别网页中 Ember 应用的实用工具。安装后在浏览器开发者工具中会出现 Ember 标签页。

{{知识点32}}
{{Ember Octane}}
Ember Octane 是 Ember 的现代化编程模型，引入了核心改进：Glimmer 组件（轻量无状态）、`@tracked` 追踪属性替代 computed properties、原生类语法替代 Ember Object、模板标签（`<template>` in `.gjs`/`.gjs` 文件）、角度括号组件调用语法（`<Component />`）。Octane 是当前 Ember 的默认编程范式。

{{知识点33}}
{{.gjs 文件格式}}
`.gjs`（Glimmer JavaScript）是 Ember 的单文件组件格式，允许在同一文件中编写 JavaScript 逻辑和 `<template>` 模板。这类似于 Vue 的 Svelte 组件或 Vue 单文件组件。使用 `.gjs` 时，助手函数和组件可以直接在文件中定义或导入，无需全局注册。这是 Ember 现代开发的推荐方式。

{{知识点34}}
{{CSS 样式管理}}
Ember 应用的全局样式存放在 `app/styles/app.css` 中。支持标准 CSS 编写，也可以集成 Sass、Less 等预处理器。组件级样式可通过 CSS Modules 或其他插件实现隔离。样式自动被 Ember CLI 构建流程处理和压缩。

{{知识点35}}
{{Ember FastBoot（SSR）}}
FastBoot 是 Ember 的服务端渲染（SSR）库，允许在服务器上预渲染 Ember 应用的 HTML。这提升了首屏加载性能和 SEO 友好性。FastBoot 可与 Ember 应用无缝集成，提供快速启动指南和详细用户文档。

{{知识点36}}
{{Ember Engines}}
Ember Engines 是一个插件，允许多个逻辑应用组合成用户视角下的单一应用。适用于大型团队协作和微前端架构。每个 Engine 可以有自己的路由、模板和服务，通过挂载点集成到主应用中。支持懒加载以优化大型应用的性能。

{{知识点37}}
{{Ember Animated}}
Ember Animated 是 Ember 的核心动画原语库，提供声明式和命令式动画能力。基于物理的动画模型、可中断动画、列表排序动画等高级功能。用于在 Ember 应用中创建流畅的过渡效果和交互动画。

{{知识点38}}
{{RFC 流程与社区治理}}
Ember.js 遵循社区驱动的开发流程，所有重大变更通过 RFC（Request for Comments）公开提案。RFC 在 GitHub 的 emberjs/rfcs 仓库中进行，社区成员可以审查和评论。这种机制确保了 API 变更的透明性和可预测性。重大变更通常附带 codemod 工具辅助迁移。

{{知识点39}}
{{弃用指南（Deprecation Guides）}}
Ember 使用谨慎的弃用流程来推出 API 变更。弃用指南文档记录了所有过去和即将到来的弃用项，按版本分类（v1.x 到 v3.x）。这帮助开发者了解哪些 API 即将移除以及如何迁移。Ember Data 和 Ember CLI 也有各自的弃用指南。

{{知识点40}}
{{Ember Observer 与生态}}
Ember Observer 为已发布的 Ember 插件提供分类和评分，还支持在整个插件目录中进行代码搜索。Ember 生态系统包括：Ember CLI（开发工具）、Ember Inspector（调试工具）、Ember Animated（动画）、Ember Twiddle（在线代码编辑器）、FastBoot（SSR）、Ember Engines（微前端）等。共享约定使得开发者能基于彼此的工作构建和改进生态。

{{知识点41}}
{{开发服务器与热重载}}
Ember 使用 Vite 作为开发服务器（2025 版），启动速度快（约 1 秒）。运行 `npm start` 启动后，文件修改会自动触发浏览器热重载。开发服务器默认监听 http://localhost:4200。支持模块热替换（HMR）以保持应用状态。

{{知识点42}}
{{生产构建与部署}}
使用 `ember build --environment=production` 构建生产版本。构建过程包括：JavaScript 和 CSS 压缩、Tree Shaking 未使用代码、模板预编译、资源指纹化（fingerprinting）用于缓存破坏。构建产物可部署到 Netlify、Vercel、GitHub Pages 等静态托管平台，或配合 FastBoot 进行 SSR 部署。

{{知识点43}}
{{控制器（Controllers）}}
控制器在 Ember 中仍然有效且推荐使用。它们是放置 URL 查询参数、从路由层传递给组件的操作和属性，以及生命周期钩子代码的正确位置。控制器与路由一一对应，处理特定路由的状态管理。虽然很多逻辑已转移到组件中，但控制器在查询参数管理方面仍有独特作用。

{{知识点44}}
{{应用初始化器（Initializers）}}
初始化器用于在应用启动时执行设置代码。分为应用初始化器（Application Initializers）和实例初始化器（Instance Initializers）。应用初始化器在应用注册表创建后运行，用于注册服务、组件等。实例初始化器在应用实例创建后运行，用于注入依赖或设置初始状态。

{{知识点45}}
{{Ember 的版本发布策略}}
Ember 采用六周发布周期。发布分为三个通道：Release（稳定版）、Beta（测试版）、Canary（开发版）。每个 LTS（长期支持）版本提供 36 周的安全补丁支持。版本号遵循语义化版本（SemVer），确保向后兼容性。这种可预测的发布策略是 Ember 企业级应用的重要保障。
