# 2025React入门
# 领域: 计算机IT
# 学科: 前端框架
# 来源: 2025React入门
# 描述: React核心知识点

{{什么是React}}{{React是一个用于构建用户界面的JavaScript库，由Meta（原Facebook）维护。它采用声明式编程范式，通过组件化的方式构建可复用的UI模块。React 19（2025年正式版本）引入了Actions、新Hooks（如useActionState、useFormStatus）、Server Components等特性，进一步提升了开发体验和性能。}}

{{JSX语法基础}}{{JSX是JavaScript XML的缩写，是React中描述UI的语法扩展。它允许在JavaScript中直接编写类似HTML的标记。JSX最终会被Babel等工具编译为React.createElement()函数调用。例如：const element = <h1 className="title">Hello</h1>。注意：JSX中class要写成className，for要写成htmlFor。}}

{{JSX表达式嵌入}}{{在JSX中使用花括号{}可以嵌入任何有效的JavaScript表达式，包括变量、函数调用、三元运算符等。例如：<h1>{user.name}</h1>、<p>{isLoggedIn ? '欢迎' : '请登录'}</p>。注意只能嵌入表达式，不能嵌入语句（如if、for）。}}

{{JSX条件渲染}}{{React中常用的条件渲染方式有三种：1）三元运算符：{condition ? <A /> : <B />}；2）逻辑与运算符：{condition && <A />}；3）提前return：在函数组件中用if语句提前返回不同的JSX。2025年推荐优先使用三元运算符和提前return的方式。}}

{{JSX列表渲染}}{{使用数组的map方法渲染列表：{items.map(item => <li key={item.id}>{item.name}</li>)}。key属性是必须的，用于帮助React识别哪些元素发生了变化。key应该是稳定且唯一的标识符，不建议使用数组索引作为key（除非列表是静态的）。}}

{{函数组件}}{{函数组件是React中最基本的组件形式，本质上是一个返回JSX的JavaScript函数。2025年函数组件是唯一推荐的组件写法（类组件已不推荐使用）。函数组件接收props作为参数，可以使用Hooks管理状态和副作用。示例：function Welcome(props) { return <h1>Hello, {props.name}</h1>; }}}

{{组件Props}}{{Props（properties）是父组件向子组件传递数据的方式，类似于函数参数。Props是只读的，子组件不能修改props。支持传递任意类型的数据，包括字符串、数字、对象、数组、函数甚至JSX。可以通过解构赋值简化：function Button({ label, onClick }) { return <button onClick={onClick}>{label}</button>; }。}}

{{组件Children}}{{props.children是一个特殊的prop，表示组件标签之间的内容。这使得组件可以作为"容器"使用，如：<Card><h2>标题</h2><p>内容</p></Card>。Card组件内部通过{props.children}渲染嵌套内容。这是React组合模式的基础。}}

{{useState Hook}}{{useState是React最基础的Hook，用于在函数组件中添加状态。语法：const [state, setState] = useState(initialValue)。调用setState会触发组件重新渲染。注意：状态更新是异步的，多次setState可能会被合并。不能在条件语句或循环中调用useState（Hook规则）。}}

{{useState函数式更新}}{{当新状态依赖于旧状态时，应使用函数式更新：setState(prev => prev + 1)。这确保了在并发模式下状态更新的正确性。直接使用setState(count + 1)在批量更新场景下可能读到过时的值。这是React 18+并发特性下推荐的最佳实践。}}

{{useEffect Hook}}{{useEffect用于处理副作用（数据获取、订阅、手动DOM操作等）。语法：useEffect(() => { /* 副作用代码 */ return () => { /* 清理函数 */ }; }, [依赖数组])。依赖数组为空[]表示只在挂载和卸载时执行；省略依赖数组表示每次渲染都执行；传入特定依赖表示只在依赖变化时执行。}}

{{useEffect清理函数}}{{useEffect返回的函数会在组件卸载前和下次effect执行前被调用，用于清理副作用（如取消订阅、清除定时器、断开连接）。示例：useEffect(() => { const timer = setInterval(...); return () => clearInterval(timer); }, [])。正确清理可以避免内存泄漏。}}

{{useRef Hook}}{{useRef返回一个可变的ref对象，其.current属性在组件整个生命周期中保持不变。用途：1）访问DOM元素：<div ref={myRef}>；2）保存不触发重新渲染的可变值（如定时器ID、前一个状态）。与useState不同，修改ref.current不会触发重新渲染。}}

{{useContext Hook}}{{useContext用于在组件树中共享全局数据（主题、语言、用户信息等），避免props逐层传递（prop drilling）。步骤：1）用createContext创建上下文；2）在顶层用Context.Provider提供值；3）在任意子组件中用useContext(Context)消费值。示例：const theme = useContext(ThemeContext)。}}

{{自定义Hook}}{{自定义Hook是以"use"开头的函数，用于提取和复用组件逻辑。它可以调用其他Hook，返回任意值。示例：function useLocalStorage(key, initialValue) { const [value, setValue] = useState(() => localStorage.getItem(key) || initialValue); useEffect(() => { localStorage.setItem(key, value); }, [key, value]); return [value, setValue]; }。自定义Hook是React逻辑复用的核心机制。}}

{{useReducer Hook}}{{useReducer是useState的替代方案，适用于复杂状态逻辑。语法：const [state, dispatch] = useReducer(reducer, initialState)。reducer是一个纯函数：(state, action) => newState。通过dispatch({type: 'ACTION'})触发状态更新。适合管理多个相关状态或状态转换逻辑复杂的场景。}}

{{useMemo Hook}}{{useMemo用于缓存计算结果，避免每次渲染都执行昂贵的计算。语法：const memoizedValue = useMemo(() => computeExpensiveValue(a, b), [a, b])。只有当依赖变化时才会重新计算。适用于大数据过滤/排序、复杂对象创建等场景。注意：不要过度使用，简单计算的缓存开销可能大于重新计算。}}

{{useCallback Hook}}{{useCallback用于缓存函数引用，避免子组件因父组件重新渲染而不必要的重渲染。语法：const memoizedFn = useCallback(() => { doSomething(a, b); }, [a, b])。通常与React.memo配合使用。在2025年React Compiler推出后，许多手动useCallback优化可能不再需要。}}

{{useTransition Hook}}{{useTransition（React 18+）用于标记非紧急的状态更新为"过渡"，允许React优先处理用户交互（如输入），延迟渲染非关键UI。语法：const [isPending, startTransition] = useTransition()。示例：startTransition(() => { setSearchResults(filterData(query)); })。在2025年这是优化大列表渲染和搜索体验的重要工具。}}

{{useDeferredValue Hook}}{{useDeferredValue（React 18+）用于延迟更新某个值，与useTransition类似但更灵活。语法：const deferredValue = useDeferredValue(value)。常用于搜索框：输入时立即更新输入框，延迟更新搜索结果列表。React会在空闲时间处理延迟值的渲染，避免阻塞用户输入。}}

{{useId Hook}}{{useId（React 18+）用于生成唯一ID，解决服务端渲染（SSR）时的ID不匹配问题。语法：const id = useId()。生成的ID格式类似":r1:"。适用于需要关联label和input的无障碍属性（如aria-labelledby）。不要在循环中对每个item调用useId，应该用useId生成前缀再拼接。}}

{{useActionState Hook}}{{useActionState（React 19新增）用于管理表单提交等异步Action的状态。语法：const [state, formAction, isPending] = useActionState(actionFn, initialState)。自动跟踪pending状态，提供服务端错误处理。与<form action={formAction}>配合使用，是React 19中处理表单的推荐方式。}}

{{useFormStatus Hook}}{{useFormStatus（React 19新增，来自react-dom）用于在表单组件内部获取表单提交状态。返回{ pending, data, method, action }。必须在<form>的子组件中使用。示例：const { pending } = useFormStatus(); return <button disabled={pending}>{pending ? '提交中...' : '提交'}</button>。}}

{{useOptimistic Hook}}{{useOptimistic（React 19新增）用于实现乐观更新（Optimistic UI）。语法：const [optimisticState, addOptimistic] = useOptimistic(state, updateFn)。在异步操作执行期间立即显示预期结果，操作完成后用实际结果替换。提升用户体验，让界面感觉更"即时"。}}

{{React事件处理}}{{React使用合成事件（SyntheticEvent）系统，事件处理器使用驼峰命名（onClick、onChange、onSubmit）。传递函数引用：<button onClick={handleClick}>。传递带参数的函数：<button onClick={() => handleClick(id)}>。事件处理器接收合成事件对象e，调用e.preventDefault()阻止默认行为。}}

{{条件与列表中的key}}{{key是React用于识别元素身份的标识符。在列表渲染中必须提供稳定唯一的key，帮助React高效地进行DOM diff。在条件渲染中，可以用key强制React重新创建组件（而非复用状态），如：<Child key={userId} userId={userId}>。}}

{{组件组合模式}}{{React推荐使用组合而非继承来复用组件逻辑。常见模式包括：1）包含关系（children prop）；2）特例关系（具名props如header、footer）；3）render props模式（传递函数返回JSX）；4）高阶组件HOC（较少用）。2025年优先使用Hooks和组合模式。}}

{{受控组件与非受控组件}}{{受控组件：表单元素的值由React状态控制，通过onChange事件更新状态。示例：<input value={name} onChange={e => setName(e.target.value)} />。非受控组件：通过ref直接访问DOM元素的值。2025年推荐使用受控组件，因为数据流更清晰可预测。对于文件上传等场景，非受控组件更合适。}}

{{React状态管理方案}}{{React内置状态管理（useState、useReducer、Context）适合中小型应用。大型应用推荐：1）Zustand——轻量级、API简洁，2025年最流行；2）Jotai——原子化状态管理；3）TanStack Query——服务端状态管理；4）Redux Toolkit——传统但成熟的选择。选择标准：应用复杂度、团队熟悉度、性能需求。}}

{{React Router路由}}{{React Router是React最常用的路由库（2025年为v7版本）。基本配置：BrowserRouter包裹应用，Routes内定义Route。示例：<Routes><Route path="/" element={<Home />} /><Route path="/user/:id" element={<User />} /></Routes>。常用Hooks：useParams获取路由参数，useNavigate编程式导航，useSearchParams获取查询参数。}}

{{React数据获取}}{{2025年推荐的数据获取方式：1）React 19 Server Components——在服务端直接async/await获取数据；2）TanStack Query（React Query）——客户端数据获取的标准方案，提供缓存、重新获取、乐观更新等；3）useEffect + fetch——简单场景可用但不推荐生产使用（无缓存、竞态处理）。}}

{{Server Components（RSC）}}{{React Server Components允许组件在服务端渲染并发送到客户端，不增加客户端bundle大小。Server Components可以直接访问数据库、文件系统等服务端资源。客户端组件使用'use client'指令标记。Next.js App Router默认所有组件都是Server Components，通过这个架构实现更好的性能和SEO。}}

{{Server Actions}}{{Server Actions（React 19）是可以在客户端组件中直接调用的服务端函数，使用'use server'指令标记。它们自动处理序列化、请求发送和响应解析。配合useActionState和useFormStatus使用，简化表单处理流程。示例：async function createPost(formData) { 'use server'; await db.insert(formData.get('title')); }}}

{{React性能优化基础}}{{核心优化策略：1）React.memo——避免不必要的重渲染，仅在props变化时重新渲染；2）useMemo——缓存计算结果；3）useCallback——缓存函数引用；4）虚拟列表——使用react-window或@tanstack/react-virtual处理大列表；5）代码分割——React.lazy + Suspense实现按需加载；6）图片懒加载。}}

{{React.lazy与Suspense}}{{React.lazy用于代码分割，实现组件的懒加载。语法：const LazyComponent = lazy(() => import('./HeavyComponent'))。配合Suspense使用：<Suspense fallback={<Loading />}><LazyComponent /></Suspense>。这可以显著减少初始包大小，提升首屏加载速度。2025年推荐对路由级组件和重型组件使用懒加载。}}

{{错误边界（Error Boundaries）}}{{错误边界是React中捕获子组件渲染错误的机制，防止整个应用崩溃。注意：错误边界只能用类组件实现（目前），但可以使用react-error-boundary库的函数式方案。用法：<ErrorBoundary fallback={<ErrorPage />}><App /></ErrorBoundary>。可以捕获渲染错误、生命周期错误，但不能捕获事件处理器中的错误。}}

{{React 19新特性概览}}{{React 19（2025年正式发布）核心新特性：1）Actions——统一的异步操作模型；2）useActionState、useFormStatus、useOptimistic——表单和乐观更新Hooks；3）ref作为prop——不再需要forwardRef；4）<Context>作为Provider的简写；5）改进的SSR支持；6）React Compiler（自动优化，减少手动memo/useCallback）。}}

{{React Compiler}}{{React Compiler（React 19配套）是一个自动优化编译器，能够自动进行记忆化（memoization），无需手动编写useMemo、useCallback和React.memo。安装后作为Babel插件使用。它通过分析组件代码，自动确定哪些值需要缓存，大幅提升开发者体验并减少优化遗漏。}}

{{React开发工具链}}{{2025年推荐的React开发工具链：1）构建工具——Vite（快速开发服务器和HMR）；2）包管理——pnpm；3）类型检查——TypeScript；4）代码规范——ESLint + Prettier；5）测试——Vitest + React Testing Library；6）调试——React DevTools浏览器扩展；7）脚手架——create-vite或Next.js。}}

{{React与TypeScript}}{{2025年TypeScript已成为React开发的标准配置。关键类型：1）组件Props类型——interface Props { name: string; age?: number; }；2）事件类型——React.MouseEvent<HTMLButtonElement>；3）useState泛型——useState<string>('')；4）children类型——React.ReactNode；5）组件类型——React.FC<Props>（但更推荐直接用函数声明）。}}

{{React测试基础}}{{React测试三大工具：1）Vitest——测试运行器（替代Jest，更快）；2）React Testing Library——组件测试工具，以用户行为为中心；3）MSW（Mock Service Worker）——API mock。测试原则：测试用户能感知的行为，不测试实现细节。示例：render(<Counter />)、fireEvent.click(screen.getByText('增加'))、expect(screen.getByText('1')).toBeInTheDocument()。}}

{{React样式方案}}{{2025年主流样式方案：1）CSS Modules——作用域隔离，原生支持；2）Tailwind CSS——实用优先的CSS框架，2025年最受欢迎；3）CSS-in-JS——styled-components、emotion（注意：部分方案对RSC不兼容）；4）vanilla-extract——零运行时CSS-in-JS。推荐：新项目优先选择Tailwind CSS或CSS Modules。}}
