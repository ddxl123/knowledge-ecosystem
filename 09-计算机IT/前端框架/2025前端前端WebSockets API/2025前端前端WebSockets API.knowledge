# 2025前端前端WebSockets API
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}
{{WebSockets API 是什么}}
WebSockets API 使得在用户的浏览器和服务器之间打开一个双向交互式通信会话成为可能。使用该 API，可以向服务器发送消息并接收响应，而无需轮询服务器等待回复。WebSocket 连接建立后，服务器可以主动向客户端推送消息，实现了真正的实时通信。该 API 自 2015 年 7 月起在所有主流浏览器中广泛可用。

{{知识点2}}
{{WebSocket 接口与 WebSocketStream 接口}}
WebSockets API 提供两种机制：WebSocket 接口和 WebSocketStream 接口。WebSocket 接口稳定且浏览器和服务器支持良好，但不支持背压（backpressure）。WebSocketStream 接口是基于 Promise 的替代方案，利用 Streams API 自动处理背压，但它是非标准的，目前仅在一个渲染引擎中受支持。

{{知识点3}}
{{WebSocket 构造函数}}
使用 new WebSocket(url) 或 new WebSocket(url, protocols) 创建 WebSocket 对象。url 参数必须使用 ws、wss、http 或 https 协议方案，且不能包含 URL 片段（fragment）。如果提供相对 URL，则相对于调用脚本的基础 URL 进行解析。protocols 参数是可选的，可以是单个字符串或字符串数组，表示客户端希望使用的子协议。

{{知识点4}}
{{WebSocket 连接状态 readyState}}
WebSocket.readyState 只读属性返回连接的当前状态，共有四个值：CONNECTING（0）表示连接尚未打开；OPEN（1）表示连接已打开且可以通信；CLOSING（2）表示连接正在关闭；CLOSED（3）表示连接已关闭或无法打开。

{{知识点5}}
{{WebSocket 事件：open}}
当 WebSocket 连接成功建立时，会触发 open 事件。在 open 事件触发后，socket 才能传输数据。可以通过 addEventListener('open', handler) 或 onopen 属性来监听此事件。这是开始发送消息的正确时机。

{{知识点6}}
{{WebSocket 事件：message}}
当通过 WebSocket 接收到数据时，会触发 message 事件。服务器发送的数据通过 event.data 属性获取，可以是文本字符串或二进制数据（Blob 或 ArrayBuffer，取决于 binaryType 设置）。可以通过 JSON.parse(e.data) 解析 JSON 格式的消息。

{{知识点7}}
{{WebSocket 事件：error}}
当连接建立过程中或连接建立后发生错误时，会触发 error 事件。发生错误时，连接会被关闭，随后会触发 close 事件。error 事件通常用于记录错误日志或通知用户连接出现问题。

{{知识点8}}
{{WebSocket 事件：close}}
当 WebSocket 连接关闭时（无论是客户端还是服务器关闭，或因错误导致），会触发 close 事件。这是清理资源（如清除定时器）的最佳时机。可以通过 addEventListener('close', handler) 或 onclose 属性来监听。

{{知识点9}}
{{WebSocket.send() 方法}}
send() 方法将指定数据排队传输到服务器，会增加 bufferedAmount 的值。数据类型可以是 String（以 UTF-8 编码）、ArrayBuffer、Blob、TypedArray 或 DataView。该方法是异步的，不会等待数据实际传输完成就返回。如果在 CONNECTING 状态调用会抛出异常，在 CLOSING 或 CLOSED 状态调用会静默丢弃数据。

{{知识点10}}
{{WebSocket.close() 方法}}
close() 方法关闭 WebSocket 连接或连接尝试。可选参数 code 是关闭代码（1000 表示正常关闭，3000-4999 为自定义代码），reason 是不超过 123 字节的关闭原因字符串。关闭过程会先完成已发送消息的传输，再开始关闭握手。

{{知识点11}}
{{WebSocket.binaryType 属性}}
binaryType 属性设置或返回连接使用的二进制数据类型。可选值为 'blob'（默认）或 'arraybuffer'。设置为 'arraybuffer' 时，接收到的二进制数据将以 ArrayBuffer 形式提供，适合需要直接操作二进制数据的场景。

{{知识点12}}
{{WebSocket.bufferedAmount 属性}}
bufferedAmount 是只读属性，表示已排队但尚未发送到服务器的字节数。每次调用 send() 方法时，该值会增加相应数据的字节数。当数据实际发送后，该值会减少。可用于实现流量控制和发送速率监控。

{{知识点13}}
{{WebSocket.url 属性}}
url 是只读属性，返回 WebSocket 连接的绝对 URL。该值在构造函数中设置，即使后续 URL 所指向的资源发生变更也不会改变。可用于调试和日志记录。

{{知识点14}}
{{WebSocket.protocol 属性}}
protocol 是只读属性，返回服务器选择的子协议。在连接建立过程中，客户端通过 Sec-WebSocket-Protocol 头发送支持的子协议列表，服务器从中选择一个。如果无法建立连接，该值为空字符串。

{{知识点15}}
{{WebSocket.extensions 属性}}
extensions 是只读属性，返回服务器选择的扩展。常见的扩展是 permessage-deflate（用于压缩消息）。扩展在 WebSocket 握手过程中协商确定。

{{知识点16}}
{{WebSocket 协议方案 ws 和 wss}}
WebSocket 使用两种 URI 方案：ws:// 表示非加密连接（对应 HTTP），wss:// 表示加密连接（对应 HTTPS）。在实际应用中，网页通过 HTTPS 提供服务时，WebSocket 连接应使用 wss 协议。大多数浏览器现在要求安全的 WebSocket 连接。

{{知识点17}}
{{WebSocket 握手过程}}
WebSocket 连接通过 HTTP 升级机制建立。客户端发送包含 Upgrade: websocket 和 Connection: Upgrade 头的 HTTP 请求，服务器返回 101 Switching Protocols 响应表示同意升级。握手过程中使用 Sec-WebSocket-Key、Sec-WebSocket-Accept、Sec-WebSocket-Version 等头部进行验证。

{{知识点18}}
{{Sec-WebSocket-Key 和 Sec-WebSocket-Accept 头}}
Sec-WebSocket-Key 是请求头，包含客户端生成的随机 nonce，用于验证客户端确实打算打开 WebSocket 连接（由浏览器自动添加）。Sec-WebSocket-Accept 是响应头，表示服务器同意升级连接，其值由 Sec-WebSocket-Key 计算得出。

{{知识点19}}
{{Sec-WebSocket-Version 头}}
在请求中，该头表示客户端理解的 WebSocket 协议版本。在响应中，仅当服务器不支持请求的协议版本时才发送，列出服务器支持的版本。

{{知识点20}}
{{Sec-WebSocket-Protocol 头}}
在请求中，该头按优先顺序列出客户端支持的子协议。在响应中，表示服务器从客户端的偏好中选择的子协议。常见子协议包括 soap、wamp、ship 等，也可以是客户端和服务器共同理解的自定义名称。

{{知识点21}}
{{Sec-WebSocket-Extensions 头}}
在请求中，该头按优先顺序列出客户端支持的 WebSocket 扩展。在响应中，表示服务器选择的扩展。最常用的扩展是 permessage-deflate，用于在 WebSocket 层面压缩消息。

{{知识点22}}
{{WebSocket 消息格式}}
WebSocket 消息以帧（frame）的形式传输。文本消息使用 UTF-8 编码，二进制消息直接传输原始数据。每条消息可以由一个或多个帧组成。WebSocket 协议定义了文本帧、二进制帧、关闭帧、Ping 帧和 Pong 帧等类型。

{{知识点23}}
{{WebSocket 与 HTTP 轮询对比}}
HTTP 轮询（Polling）需要客户端定期发送请求检查新数据，存在延迟和带宽浪费。长轮询（Long Polling）减少请求次数但仍有开销。WebSocket 建立持久连接后，服务器可实时推送数据，延迟最低、效率最高，适合实时聊天、游戏、股票行情等场景。

{{知识点24}}
{{WebSocket 与 Server-Sent Events (SSE) 对比}}
SSE 是单向的（服务器到客户端），基于 HTTP 协议，自动支持重连和事件 ID 追踪，实现简单。WebSocket 是双向的，支持更复杂的消息交换，但实现相对复杂。如果只需要服务器推送，SSE 可能是更好的选择；如果需要双向通信，则应使用 WebSocket。

{{知识点25}}
{{WebSocket 与 WebTransport 对比}}
WebTransport API 被期望在许多应用中替代 WebSocket。WebTransport 是多功能的低级 API，支持背压、单向流、乱序传输和不可靠的数据报传输。WebTransport 比 WebSocket 更强大但更复杂，跨浏览器支持也不如 WebSocket 广泛。对于标准连接和广泛兼容性需求，WebSocket 仍是首选。

{{知识点26}}
{{WebSocket 在 Web Workers 中使用}}
WebSocket 接口可在 Web Workers 中使用，这允许在后台线程中处理 WebSocket 通信，避免阻塞主线程的 UI 渲染。这对于需要处理大量实时数据的应用特别有用。

{{知识点27}}
{{WebSocket 与 bfcache（往返缓存）}}
如果页面有打开的 WebSocket 连接，浏览器可能不会将其添加到 bfcache 中。最佳实践是在用户离开页面时关闭连接，可监听 pagehide 事件进行关闭，并在 pageshow 事件（检查 event.persisted）中重新建立连接。

{{知识点28}}
{{WebSocket 安全考虑}}
不应在混合内容环境中使用 WebSocket（即不从 HTTPS 页面打开非安全 WebSocket 连接，反之亦然）。大多数浏览器现在只允许安全的 WebSocket 连接。应使用 wss:// 协议确保数据传输加密。还需注意防止跨站 WebSocket 劫持（CSWSH）攻击。

{{知识点29}}
{{WebSocket 子协议（Sub-protocols）}}
子协议允许单个服务器实现多种 WebSocket 子协议，根据指定值处理不同类型的交互。子协议在连接建立过程中通过 Sec-WebSocket-Protocol 头协商，每次连接只能选择一个子协议。可以在 IANA WebSocket Subprotocol Name Registry 中注册标准子协议名称。

{{知识点30}}
{{JSON over WebSocket}}
常见的消息传递模式是使用 JSON 发送序列化的 JavaScript 对象。发送时使用 JSON.stringify() 将对象序列化为文本字符串，接收时使用 JSON.parse() 将文本反序列化为对象。这是 WebSocket 通信中最常用的数据交换格式。

{{知识点31}}
{{WebSocket 连接重连策略}}
WebSocket 连接可能因网络问题、服务器重启等原因断开。实现健壮的重连策略通常包括：在 close 事件中触发重连、使用指数退避（exponential backoff）避免频繁重连、设置最大重连次数、在页面恢复时检查连接状态。

{{知识点32}}
{{WebSocket 心跳机制}}
为保持 WebSocket 连接活跃并检测断开，通常实现心跳机制：客户端定期发送 Ping 消息，服务器回复 Pong 消息。WebSocket 协议内置了 Ping/Pong 帧支持，但浏览器 API 不直接暴露这些帧，因此通常使用应用层的文本消息实现心跳。

{{知识点33}}
{{CloseEvent 接口}}
CloseEvent 是 WebSocket 对象在连接关闭时发送的事件接口。包含三个属性：code（关闭状态码）、reason（关闭原因字符串）和 wasClean（布尔值，表示连接是否干净关闭）。常见的关闭码包括 1000（正常关闭）、1001（离开）、1006（异常关闭）等。

{{知识点34}}
{{MessageEvent 接口}}
MessageEvent 是 WebSocket 在接收到服务器消息时发送的事件接口。主要属性包括 data（消息数据）、origin（消息来源的 Origin）、lastEventId（事件 ID）和 source（消息来源的引用）。data 属性包含实际的消息内容。

{{知识点35}}
{{WebSocket 常用库和框架}}
Node.js 生态中常用的 WebSocket 库包括：ws（流行且轻量的 WebSocket 客户端和服务器库）、Socket.IO（基于长轮询/WebSocket 的第三方传输协议）、SocketCluster（面向可扩展性的 pub/sub 框架）。其他框架如 SignalR（.NET）、Django Channels（Python）、Flask-SocketIO（Python）也广泛使用。

{{知识点36}}
{{WebSocket 与帧压缩（permessage-deflate）}}
permessage-deflate 是 WebSocket 协议的扩展，用于在传输层面压缩消息。通过在 WebSocket 握手时协商 Sec-WebSocket-Extensions: permessage-deflate 头来启用。可以显著减少网络传输数据量，但会增加 CPU 开销。

{{知识点37}}
{{WebSocket 连接的性能优化}}
优化 WebSocket 性能的关键策略：使用二进制格式（如 Protocol Buffers）替代 JSON 减少数据大小；启用 permessage-deflate 压缩；合理控制消息发送频率；使用 bufferedAmount 监控发送队列避免内存溢出；在不需要时及时关闭连接释放资源。

{{知识点38}}
{{RFC 6455 WebSocket 协议规范}}
RFC 6455 是 WebSocket 协议的官方规范，定义了握手过程、帧格式、关闭握手、错误处理等细节。该规范由 IETF 发布，是所有 WebSocket 实现的基础。规范定义了操作码（opcode）来区分不同类型的帧：0x1 为文本帧、0x2 为二进制帧、0x8 为关闭帧、0x9 为 Ping 帧、0xA 为 Pong 帧。

{{知识点39}}
{{WebSocket 适用场景}}
WebSocket 适用于需要实时双向通信的场景：实时聊天应用、多人在线游戏、实时协作编辑、股票行情推送、实时通知系统、在线教育互动课堂、物联网设备通信、实时数据仪表盘。对于只需要服务器单向推送的场景，SSE 可能是更简单的选择。

{{知识点40}}
{{WebSocket 不支持背压的问题}}
传统 WebSocket API 没有背压机制，当消息到达速度超过应用处理速度时，会导致内存占用持续增长或 CPU 占用 100%。WebSocketStream 通过 Streams API 提供自动背压支持来解决此问题。开发者也可以通过监控 bufferedAmount 和控制消息处理速率来手动实现背压。
