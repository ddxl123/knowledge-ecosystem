# 2025前端前端File System Access
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}{{File System Access API概述：File System Access API是Web平台提供的一组接口，允许Web应用直接与用户本地文件系统或网络文件系统进行交互。核心功能包括读取文件、写入/保存文件以及访问目录结构。该API通过文件句柄（Handle）机制实现对文件和目录的操作，是Web应用走向"原生应用"能力的关键一步。}}

{{知识点2}}{{规范来源与标准化状态：File System Access API由WHATWG维护，规范地址为https://fs.spec.whatwg.org。它属于File System API的一部分，核心接口（如FileSystemHandle、FileSystemFileHandle、FileSystemDirectoryHandle）已被纳入Baseline，自2023年3月起在主流浏览器中广泛可用。}}

{{知识点3}}{{FileSystemHandle基类：FileSystemHandle是所有文件系统句柄的基类接口，代表文件系统中的一个文件或目录条目。它有两个只读属性：kind（返回'file'或'directory'）和name（返回条目名称）。通常不直接使用，而是通过其子类FileSystemFileHandle和FileSystemDirectoryHandle操作。}}

{{知识点4}}{{FileSystemHandle实例方法：FileSystemHandle提供四个核心方法：isSameEntry()用于比较两个句柄是否指向同一文件/目录；queryPermission()查询当前权限状态；requestPermission()请求读取或读写权限；remove()请求从文件系统中删除该条目。}}

{{知识点5}}{{FileSystemFileHandle接口：FileSystemFileHandle继承自FileSystemHandle，代表文件系统中的一个文件条目。提供getFile()方法返回File对象（Promise），createWritable()方法返回FileSystemWritableFileStream对象用于写入，createSyncAccessHandle()方法返回同步访问句柄。}}

{{知识点6}}{{FileSystemDirectoryHandle接口：FileSystemDirectoryHandle继承自FileSystemHandle，代表文件系统中的一个目录条目。提供getDirectoryHandle()获取子目录句柄、getFileHandle()获取文件句柄、removeEntry()删除子条目、resolve()解析相对路径等方法。同时支持异步迭代器协议。}}

{{知识点7}}{{窗口选择器方法showOpenFilePicker()：window.showOpenFilePicker()方法显示一个文件选择器，允许用户选择一个或多个文件。返回Promise，解析为FileSystemFileHandle数组。可通过pickerOpts参数配置文件类型过滤、是否允许多选等选项。}}

{{知识点8}}{{窗口选择器方法showSaveFilePicker()：window.showSaveFilePicker()方法显示保存文件选择器，允许用户选择保存位置和文件名。返回Promise，解析为一个FileSystemFileHandle。配合createWritable()可将数据写入选定的文件。}}

{{知识点9}}{{窗口选择器方法showDirectoryPicker()：window.showDirectoryPicker()方法显示目录选择器，允许用户选择一个目录。返回Promise，解析为FileSystemDirectoryHandle对象。选择后可遍历目录内容、创建子目录和文件等操作。}}

{{知识点10}}{{文件类型过滤配置：showOpenFilePicker和showSaveFilePicker支持types参数进行文件类型过滤。每个type包含description（描述文字）和accept（MIME类型到扩展名数组的映射）。通过excludeAllOption: true可禁止"所有文件"选项，multiple: false限制单选。}}

{{知识点11}}{{FileSystemWritableFileStream接口：FileSystemWritableFileStream继承自WritableStream，用于向文件写入数据。提供write()写入内容、seek()移动文件游标、truncate()调整文件大小三个核心方法。write()支持直接传数据或传入{type, position, data}格式的选项对象。}}

{{知识点12}}{{文件写入流程：标准写入流程为：1.通过选择器获取FileSystemFileHandle；2.调用createWritable()获取FileSystemWritableFileStream；3.调用write()写入Blob/String/ArrayBuffer等数据；4.调用close()关闭流并持久化到磁盘。写入过程中数据先写入临时文件，通过安全检查后才覆盖原文件。}}

{{知识点13}}{{Origin Private File System（OPFS）概述：OPFS是File System API提供的源私有文件存储端点，对页面源私有且对用户不可见。它提供高度优化的原地写入访问能力，不受用户可见文件系统的安全检查限制，因此性能更好。适用于持久上传、离线缓存、大文件存储等场景。}}

{{知识点14}}{{OPFS访问入口：通过navigator.storage.getDirectory()方法获取OPFS根目录的FileSystemDirectoryHandle。该方法返回Promise，解析为OPFS根目录句柄。从该根目录可创建/访问文件和子目录。OPFS受浏览器存储配额限制，可用navigator.storage.estimate()查询使用量。}}

{{知识点15}}{{OPFS与用户可见文件系统的区别：OPFS不需要权限提示和安全检查，性能更高；受浏览器存储配额限制（类似IndexedDB）；清除站点存储数据会删除OPFS；浏览器将OPFS持久化到磁盘但文件位置不可预期；不适合用户直接浏览的文件存储。}}

{{知识点16}}{{OPFS主线程操作：在主线程中操作OPFS使用异步Promise-based API。通过getFileHandle()和getDirectoryHandle()获取文件/目录句柄，传入{create: true}可自动创建不存在的条目。读取用getFile()获取File对象再用text()等方法；写入用createWritable()获取写入流。}}

{{知识点17}}{{OPFS Web Worker同步操作：OPFS在Web Worker中支持FileSystemSyncAccessHandle进行同步读写，性能更高（避免Promise开销）。通过createSyncAccessHandle()获取同步句柄，提供read()、write()、getSize()、truncate()、flush()、close()等同步方法。这是OPFS的独特优势。}}

{{知识点18}}{{FileSystemSyncAccessHandle详解：该接口仅在专用Web Worker中可用，仅用于OPFS文件。它对文件持有独占锁，提供原地字节级读写。read(buffer, {at: offset})从指定偏移读取；write(buffer, {at: offset})从指定偏移写入；flush()刷盘；close()关闭并释放锁。}}

{{知识点19}}{{同步句柄早期规范变更：在规范早期版本中，close()、flush()、getSize()和truncate()被错误地指定为异步方法，部分旧版浏览器实现了异步版本。当前规范和所有现代浏览器已修正为同步实现，但开发者仍需注意旧浏览器的兼容性。}}

{{知识点20}}{{目录遍历与异步迭代器：FileSystemDirectoryHandle实现了异步迭代器协议，支持for await...of循环和entries()、values()、keys()方法。遍历目录中的所有条目：for await (const [name, handle] of dirHandle) {}。还可使用递归生成器函数深度遍历子目录。}}

{{知识点21}}{{文件删除操作：两种删除方式：1.在父目录上调用removeEntry(name)删除指定名称的子条目；2.直接在句柄上调用remove()方法。删除目录时传入{recursive: true}可递归删除所有内容。快速清空OPFS：await (await navigator.storage.getDirectory()).remove({recursive: true})。}}

{{知识点22}}{{路径解析resolve()方法：FileSystemDirectoryHandle.resolve(handle)方法接受一个FileSystemHandle参数，返回一个字符串数组表示从当前目录到目标条目的相对路径。如果目标不在当前目录下则返回null。可用于判断文件是否属于某个目录。}}

{{知识点23}}{{拖放获取文件句柄：通过DataTransferItem.getAsFileSystemHandle()方法可从HTML拖放事件中获取FileSystemHandle。该方法返回Promise，如果拖放的是文件则解析为FileSystemFileHandle，如果是目录则解析为FileSystemDirectoryHandle。实现了拖放与文件系统API的桥接。}}

{{知识点24}}{{安全上下文要求：File System Access API仅在安全上下文（HTTPS）中可用。所有涉及用户可见文件系统操作的接口都需要安全上下文。这是浏览器的安全策略，防止恶意网页未经授权访问用户文件。localhost在开发时被视为安全上下文。}}

{{知识点25}}{{权限模型与用户授权：访问用户文件系统需要用户显式授权。每次通过选择器选择文件/目录时自动获得该条目的权限。对于已有句柄，可通过queryPermission({mode: 'readwrite'})检查权限状态，通过requestPermission({mode: 'readwrite'})请求权限。权限状态为'granted'、'denied'或'prompt'。}}

{{知识点26}}{{安全检查与临时文件机制：写入用户可见文件系统时，数据先写入临时文件，经过安全检查（如Chrome的Safe Browsing检查）后才覆盖原文件。这确保了安全但降低了性能。OPFS不经过这些检查，因此写入性能显著优于用户可见文件系统。}}

{{知识点27}}{{OPFS典型使用场景：1.持久化上传器：将上传文件复制到本地沙盒分块上传，支持断点续传；2.游戏/媒体应用：本地解压大型tarball、后台预取资源；3.音视频/图片编辑器：原地修改文件元数据（如ID3/EXIF标签）；4.离线视频查看器：下载大文件离线观看；5.离线邮件客户端：本地缓存附件。}}

{{知识点28}}{{与IndexedDB的对比：OPFS和IndexedDB都是源私有存储，但OPFS提供文件系统语义（目录/文件层次结构）和字节级访问，适合大文件和结构化数据；IndexedDB是键值存储，适合结构化对象。OPFS在Web Worker中支持同步操作，而IndexedDB始终异步。OPFS更适合SQLite等需要随机访问的场景。}}

{{知识点29}}{{浏览器兼容性状态：截至2025年，File System Access API核心接口（FileSystemHandle、FileSystemFileHandle、FileSystemDirectoryHandle）已在Chrome、Edge、Opera等Chromium浏览器中全面支持。Firefox和Safari对用户可见文件系统选择器API的支持有限，但OPFS在主流浏览器中支持较好。}}

{{知识点30}}{{FileSystemObserver接口（实验性）：FileSystemObserver是实验性接口，提供文件系统变更观察机制，避免轮询开销。通过new FileSystemObserver(callback)创建观察者，调用observe(handle)开始观察文件或目录变化。回调接收FileSystemChangeRecord数组，包含changedHandle和type等信息。调用disconnect()停止观察。}}

{{知识点31}}{{FileSystemChangeRecord对象：FileSystemChangeRecord包含文件系统变更的详细信息，由FileSystemObserver回调接收。主要属性包括changedHandle（变更的文件/目录句柄）和type（变更类型）。可用于实现实时文件同步、自动保存等功能。}}

{{知识点32}}{{与WebAssembly的协同：FileSystemSyncAccessHandle的同步特性使其非常适合WebAssembly场景。SQLite WASM等项目使用OPFS同步句柄实现高性能数据库操作。同步读写避免了异步Promise在WASM调用中的复杂性和性能开销，是Web端数据库的重要存储后端。}}

{{知识点33}}{{存储配额与驱逐策略：OPFS受浏览器存储API配额限制，与IndexedDB、Cache Storage等共享源的存储空间。浏览器可能在存储压力下驱逐OPFS数据。开发者应通过navigator.storage.estimate()监控使用量，必要时实现数据清理策略或提示用户。}}

{{知识点34}}{{文件句柄持久化与恢复：FileSystemHandle可以通过IndexedDB进行序列化和持久化存储。应用重启后可以从IndexedDB中恢复之前获得的文件句柄，无需用户再次选择。但恢复后仍需检查和请求权限。这使得应用可以"记住"用户选择的文件/目录。}}

{{知识点35}}{{write()方法的多种写入模式：FileSystemWritableFileStream.write()支持四种模式：1.直接传入数据（字符串/Blob/ArrayBuffer）；2.{type: "write", position, data}从指定位置写入；3.{type: "seek", position}移动文件游标；4.{type: "truncate", size}调整文件大小。灵活的写入模式支持复杂的文件编辑场景。}}

{{知识点36}}{{最佳实践与注意事项：1.始终检查权限后再操作文件；2.写入操作使用try-catch处理可能的异常（如磁盘满、权限撤销）；3.OPFS中使用同步API在Worker中操作以获得最佳性能；4.大型文件操作考虑分块处理；5.定期使用flush()确保数据持久化；6.用完后调用close()释放资源和锁。}}
