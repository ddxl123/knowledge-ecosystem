# 2025前端前端Server Components
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}
{{Server Components（服务端组件）是React 19中正式稳定的一种新组件类型。它们在打包之前、在与客户端应用或SSR服务器分离的环境中提前渲染。这个分离的环境就是React Server Components中的"server"。服务端组件可以在构建时在CI服务器上运行一次，也可以在每次请求时使用web服务器运行。}}

{{知识点2}}
{{React Server Components在React 19中已稳定，不会在次要版本之间破坏兼容性。但用于实现RSC打包器或框架的底层API不遵循semver，可能在React 19.x的次要版本之间发生变化。建议固定到特定的React版本或使用Canary发布。}}

{{知识点3}}
{{Server Components无需服务器即可运行。服务端组件可以在构建时运行以读取文件系统或获取静态内容，因此不需要web服务器。例如，可以从内容管理系统读取静态数据。渲染输出可以通过SSR生成HTML并上传到CDN。客户端不会看到原始组件或昂贵的渲染库，只会看到渲染后的输出。}}

{{知识点4}}
{{Server Components支持异步组件（Async Components）。这是服务端组件的新特性，允许在render中使用await。当在异步组件中await时，React会暂停（suspend）并等待promise解析后再恢复渲染。这在服务器/客户端边界上通过Suspense的流式传输支持工作。}}

{{知识点5}}
{{Server Components没有专用指令标记。一个常见误解是用"use server"标记服务端组件，但实际上没有用于标记服务端组件的指令。"use server"指令用于Server Functions（服务端函数）。在Next.js的App Router中，默认所有组件都是Server Components，除非用"use client"标记。}}

{{知识点6}}
{{'use client'指令用于声明服务端和客户端模块图（模块依赖树）之间的边界。将'use client'添加到文件顶部，可将该模块及其传递依赖标记为客户端代码。标记后，该文件的所有导入和它直接渲染的组件都会包含在客户端bundle中。}}

{{知识点7}}
{{'use client'必须位于文件的最顶部，在任何导入或其他代码之前（注释可以放在前面）。必须使用单引号或双引号书写，不能使用反引号。当从另一个客户端渲染模块导入时，该指令不产生效果。}}

{{知识点8}}
{{组件是服务端还是客户端取决于组件的使用位置（component usage），而非组件定义。如果组件定义在带有'use client'指令的模块中，或者在客户端组件中被导入和调用，则该组件的使用是客户端组件。否则就是服务端组件。同一个组件定义可以在不同地方同时作为服务端组件和客户端组件使用。}}

{{知识点9}}
{{'use server'指令用于标记可以从客户端代码调用的服务端函数（Server Functions）。在异步函数体的顶部添加'use server'即可将函数标记为客户端可调用。也可以将指令添加到文件顶部，将文件中所有导出都标记为Server Functions。Server Functions设计用于突变（mutations）更新服务端状态，不推荐用于数据获取。}}

{{知识点10}}
{{Server Functions的参数必须是可序列化的。支持的类型包括：基本类型（string、number、bigint、boolean、undefined、null、全局注册的Symbol）、可迭代对象（String、Array、Map、Set、TypedArray和ArrayBuffer）、Date、FormData实例、纯对象、Server Functions本身以及Promises。不支持React元素/JSX、非Server Function的函数、类、类实例等。}}

{{知识点11}}
{{Server Components可以直接访问数据层，无需构建API层。在服务端组件中可以直接导入ORM或数据库客户端进行查询，因为凭证和查询逻辑不会包含在客户端bundle中。这消除了客户端-服务端瀑布式数据获取的问题。}}

{{知识点12}}
{{Server Components可以将数据和JSX作为props传递给Client Components。当服务端组件渲染后，bundler会将数据、渲染的服务端组件和动态客户端组件组合成一个bundle。客户端组件通过props接收服务端组件的渲染结果。}}

{{知识点13}}
{{RSC Payload（React Server Component Payload）是渲染后的React Server Components树的紧凑二进制表示。它包含：服务端组件的渲染结果、客户端组件应渲染位置的占位符及其JavaScript文件引用、从服务端组件传递给客户端组件的任何props。}}

{{知识点14}}
{{在Next.js App Router中的渲染流程：服务端 —— 服务端组件被渲染为RSC Payload，客户端组件与RSC Payload一起用于预渲染HTML。客户端首次加载 —— HTML用于立即显示快速非交互预览，RSC Payload用于协调客户端和服务端组件树，JavaScript用于hydrate客户端组件使其可交互。}}

{{知识点15}}
{{Server Components不能使用交互式API如useState、useEffect等。要为服务端组件添加交互性，需要与Client Components组合使用。通过在客户端组件文件中添加'use client'指令，然后在服务端组件中导入该客户端组件来实现组合。}}

{{知识点16}}
{{服务端和客户端组件的交错使用（Interleaving）模式。可以将Server Components作为props传递给Client Components，常见的模式是使用children创建"插槽"。例如在Page（服务端组件）中，将Cart（服务端组件）作为Modal（客户端组件）的children传入，实现服务端渲染的内容嵌套在客户端交互组件中。}}

{{知识点17}}
{{React Context在Server Components中不受支持。要使用context，需要创建一个接受children的Client Component作为Provider，然后在Server Component（如layout）中导入该Provider。建议将providers渲染在树中尽可能深的位置，以便Next.js优化服务端组件的静态部分。}}

{{知识点18}}
{{第三方组件集成策略。当使用依赖客户端特性的第三方组件时，可以在Client Component中包装它。例如一个使用useState但没有'use client'指令的Carousel组件，只需在客户端组件中导入使用即可正常工作，无需修改第三方库。}}

{{知识点19}}
{{Server Components的bundle大小优化。由于服务端组件不在浏览器中运行，其依赖的大型库（如marked 35.9K、sanitize-html 206K gzipped）不会包含在客户端bundle中。只有渲染后的输出发送到客户端，显著减少JavaScript bundle大小，改善FCP（First Contentful Paint）。}}

{{知识点20}}
{{Server Components与Suspense的协同工作。在服务端组件中创建promise但不await它，可以将promise传递给客户端组件。客户端组件使用React的use() API来恢复（resume）服务端发起的promise。这允许高优先级内容先渲染，低优先级内容稍后流式加载。}}

{{知识点21}}
{{数据获取策略：Server Components中可以使用fetch API或ORM/数据库客户端直接获取数据。fetch请求默认不缓存，会阻塞页面渲染直到请求完成。使用use cache指令可缓存结果，或将获取组件包裹在<Suspense>中以流式传输新鲜数据。}}

{{知识点22}}
{{Streaming（流式传输）是提升初始加载时间和用户体验的关键技术。有两种方式使用流式传输：1）使用loading.js文件流式传输整个路由段；2）使用<Suspense>组件更精细地控制页面哪些部分进行流式传输。loading.js会被嵌套在layout.js中，自动将page.js包裹在<Suspense>边界中。}}

{{知识点23}}
{{loading.js文件的工作原理。在同一文件夹中创建loading.js文件，用户导航时会立即看到布局和加载状态，新内容渲染完成后自动替换。但如果layout访问未缓存或运行时数据（如cookies()、headers()或未缓存的fetch），则不会回退到同路由段的loading.js，而是阻塞导航直到layout完成渲染。}}

{{知识点24}}
{{Server Functions与HTML表单的集成。将Server Function传递给<form action>，React会自动进行渐进增强（progressive enhancement），表单可以在JavaScript bundle加载之前提交。React会将表单的FormData作为Server Function的第一个参数传入。使用useActionState可以在支持渐进增强的同时根据Server Function的结果更新UI。}}

{{知识点25}}
{{Server Functions的安全考虑。Server Function的参数完全由客户端控制，应始终将参数视为不可信输入，验证和转义参数。在任何Server Function中，都要验证登录用户是否有权限执行该操作。实验性API taintUniqueValue和taintObjectReference可用于防止敏感数据从Server Function泄露。}}

{{知识点26}}
{{Server Functions应使用Transition调用。传递给<form action>或formAction的Server Functions会自动在transition中调用。Server Functions是暴露的服务端端点，可以在客户端代码的任何地方调用，不仅限于表单中。}}

{{知识点27}}
{{Server Components的两种运行模式：构建时（build time）和请求时（request time）。构建时模式适合静态内容，渲染输出可上传到CDN。请求时模式适合动态内容，每次请求都可以访问最新数据和服务端资源。这种架构结合了MPA的简单请求/响应心智模型和SPA的无缝交互体验。}}

{{知识点28}}
{{减少JS bundle大小的最佳实践。将'use client'添加到具体的交互组件而非将大部分UI标记为Client Components。例如Layout组件主要包含静态元素（logo、导航链接），只需将交互式搜索栏标记为Client Component，其余部分保持为Server Component。}}

{{知识点29}}
{{Server Components与后续导航的行为。后续导航时，RSC Payload会被预取和缓存以实现即时导航。客户端组件完全在客户端渲染，不需要服务端渲染的HTML。这种分层策略使得首次加载和后续导航都有优化的性能表现。}}

{{知识点30}}
{{Server Components中的模块评估规则。一个模块可以在被服务端代码导入时在服务端评估，也可以在被客户端代码导入时在客户端评估。标记为客户端评估的代码不仅限于组件，客户端模块子树中的所有代码都会被发送到客户端并在客户端运行。}}

{{知识点31}}
{{Server Components在Next.js中的默认行为。在App Router中，layouts和pages默认是Server Components，可以在服务端获取数据、渲染UI部分，可选地缓存结果，并流式传输到客户端。当需要交互或浏览器API时，可以使用Client Components来添加功能层。}}

{{知识点32}}
{{Server Components通过服务端渲染可以直接访问数据库等后端资源，凭证不会暴露给客户端。但仍然需要确保请求经过适当的认证和授权。最佳实践是在数据安全指南中遵循服务端数据访问的安全规范。}}

{{知识点33}}
{{Server Components的Suspense回退策略。使用<Suspense>包裹动态内容，非边界内的内容会立即发送到客户端。loading.js提供路由段级别的流式传输，而<Suspense>提供更细粒度的组件级别流式传输。推荐在运行时或未缓存数据访问附近使用<Suspense>而非仅依赖loading.js。}}

{{知识点34}}
{{Server Components与水合（Hydration）的关系。HTML用于首次显示快速但非交互的路由预览，RSC Payload用于协调客户端和服务端组件树，JavaScript用于hydrate客户端组件使应用可交互。只有Client Components需要hydration，Server Components的渲染结果直接作为静态HTML呈现。}}

{{知识点35}}
{{Server Components生态中的框架支持。Next.js（App Router）是最成熟的支持RSC的框架，在React 19稳定后其他框架也在跟进。底层RSC API需要与bundler（如Webpack、Turbopack）集成，框架开发者需要关注React版本兼容性。}}
