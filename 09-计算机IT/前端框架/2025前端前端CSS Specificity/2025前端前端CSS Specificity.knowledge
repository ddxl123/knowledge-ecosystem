# 2025前端前端CSS Specificity
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}{{CSS特异性（Specificity）的定义：特异性是应用到CSS声明上的权重。当两个或更多规则为同一个元素设置不同属性值时，特异性算法根据匹配选择器中各类选择器的权重来决定最终生效的样式。特异性值本质上是一个三列值：ID - CLASS - TYPE。}}

{{知识点2}}{{特异性的三列权重体系：特异性用三个列的值表示——ID列、CLASS列、TYPE列。每列的值来自匹配选择器中对应类型选择器组件的数量，比较时从左到右逐列对比。}}

{{知识点3}}{{ID列（最高权重）：仅包含ID选择器，如 #example。每个匹配选择器中的ID向权重值增加 1-0-0。ID选择器是特异性最高的常规选择器类型。}}

{{知识点4}}{{CLASS列（中等权重）：包含类选择器（如 .myClass）、属性选择器（如 [type="radio"]、[lang|="fr"]）和伪类（如 :hover、:nth-of-type(3n)、:required）。每个类、属性选择器或伪类向权重值增加 0-1-0。}}

{{知识点5}}{{TYPE列（最低权重）：包含类型/元素选择器（如 p、h1、td）和伪元素（如 ::before、::placeholder 等双冒号选择器）。每个类型或伪元素向权重值增加 0-0-1。}}

{{知识点6}}{{特异性为零的选择器：通配选择器 (*)、:where() 伪类及其参数在计算特异性时权重为 0-0-0。它们会匹配元素但不影响特异性权重值。}}

{{知识点7}}{{组合符不影响特异性：+（相邻兄弟）、>（子选择器）、~（后续兄弟）、空格（后代）、||（列组合符）等组合符虽然让选择器更精确地选择元素，但不会增加特异性权重值。}}

{{知识点8}}{{三列比较规则：确定特异性值后，从左到右逐列比较。ID列值大的选择器总是胜出，不管其他列的值如何。例如 #myElement (1-0-0) 胜过 .bodyClass .sectionClass .parentClass [id="myElement"] (0-4-0)。}}

{{知识点9}}{{ID列相同时比较CLASS列：如果两个竞争选择器的ID列值相同，则比较CLASS列。CLASS列值大的胜出，不管TYPE列的值如何。}}

{{知识点10}}{{CLASS列相同时比较TYPE列：如果ID列和CLASS列都相同，则比较TYPE列。TYPE列值大的选择器胜出。}}

{{知识点11}}{{所有列相同时的来源顺序规则：如果竞争选择器在三列的值完全相同，则后声明的样式优先（proximity rule/就近原则）。例如 input.myClass (0-1-1) 和 :root input (0-1-1) 中，后声明的胜出。}}

{{知识点12}}{{:is()、:not()、:has() 伪类的特异性规则：这三个伪类本身不增加特异性权重，但其参数中的选择器会贡献权重。特异性取参数列表中最高特异性的那个选择器的值。例如 :is(p) 的特异性为 0-0-1，与 p 相同。}}

{{知识点13}}{{:is() 接受复杂选择器列表的特性：:is() 可以接受逗号分隔的复杂选择器列表。通过在参数中包含高特异性选择器（如 #fakeId），可以人为提升特异性。例如 :is(p, #fakeId) 的特异性为 1-0-0。}}

{{知识点14}}{{:not() 的特异性计算：:not() 同样取参数中最高特异性的值。例如 div:not(.inner) p 的特异性为 0-1-2（.inner 贡献 0-1-0，div 贡献 0-0-1，p 贡献 0-0-1）。}}

{{知识点15}}{{:has() 的特异性计算：:has() 也遵循同样的规则。例如 h2:has(~ h2) 的特异性为 0-0-2（两个 h2 类型选择器各贡献 0-0-1）。}}

{{知识点16}}{{利用 :not(#fakeId) 提升特异性的技巧：通过在 :not() 参数中重复使用不存在的ID，可以人为提升特异性而不改变选择目标。例如 a:not(#fakeId#fakeId#fakeID) 的特异性为 3-0-1，这是一种hack技巧，使用时应添加注释说明原因。}}

{{知识点17}}{{内联样式的特异性：元素上的内联样式（如 style="font-weight: bold;"）总是覆盖作者样式表中的常规样式，可视为具有最高特异性权重 1-0-0-0。唯一能覆盖内联样式的方法是使用 !important。}}

{{知识点18}}{{!important 的作用与影响：标记为 !important 的CSS声明会覆盖同一级联层和来源中的所有其他声明。虽然技术上 !important 不属于特异性，但它直接与特异性及级联交互，反转了样式表的级联顺序。}}

{{知识点19}}{{!important 之间的竞争规则：当同一来源和级联层中冲突的声明都带有 !important 标记时，特异性更高的声明会被应用。使用 !important 覆盖特异性被认为是不良实践。}}

{{知识点20}}{{:where() 的特异性归零特性：:where() 伪类的特异性始终被替换为 0-0-0。它允许创建非常精确的选择器而不增加特异性。这在创建第三方CSS供他人覆盖时非常有用。}}

{{知识点21}}{{:where() 的实际应用场景：当你编写一个主题或组件库时，使用 :where(#defaultTheme) a (0-0-1) 而非 #defaultTheme a (1-0-1)，可以让使用者仅用类型选择器就能轻松覆盖样式。}}

{{知识点22}}{{@scope 块与特异性的关系：将规则集放在 @scope 块内不会影响其选择器的特异性，无论在范围根和范围限制中使用什么选择器。但如果显式添加 :scope 伪类，则需要将 :scope 的特异性 (0-1-0) 计入。}}

{{知识点23}}{{CSS嵌套与特异性：CSS嵌套中的 & 组合符不增加特异性权重，但嵌套规则本身会增加特异性。嵌套在特异性和功能上与 :is() 伪类非常相似，取嵌套选择器列表中最高特异性的值。}}

{{知识点24}}{{匹配选择器决定特异性：特异性来自匹配的选择器，而非整个选择器列表。例如选择器列表 [type="password"], input:focus, :root #myApp input:required 中，密码输入框在不同状态下匹配不同的选择器，特异性也不同。}}

{{知识点25}}{{特异性的实际计算示例——基础：p { } 的特异性为 0-0-1；.intro { } 为 0-1-0；#sidebar { } 为 1-0-0。这三个选择器从左到右特异性依次增高。}}

{{知识点26}}{{特异性的实际计算示例——组合：div.wrapper p.intro { } 的特异性为 0-1-2（一个类选择器 .intro 贡献 0-1-0，div 和 p 两个类型选择器各贡献 0-0-1）。}}

{{知识点27}}{{特异性的实际计算示例——ID+类：#nav .item a:hover { } 的特异性为 1-2-1（#nav 贡献 1-0-0，.item 贡献 0-1-0，:hover 贡献 0-1-0，a 贡献 0-0-1）。}}

{{知识点28}}{{特异性与级联层（Cascade Layers）的关系：级联层的优先级独立于特异性。在 @layer 中声明的样式即使特异性很高，也会被不在任何层中的样式覆盖。层的声明顺序决定层间优先级，后声明的层优先级更高。}}

{{知识点29}}{{避免 !important 的替代方案：使用级联层（@layer）组织CSS、降低选择器特异性、利用 :where() 归零特异性、合理组织样式表的加载顺序。理解特性和级联可以消除对 !important 的需求。}}

{{知识点30}}{{JavaScript框架与内联样式：许多JavaScript框架和库会添加内联样式。要覆盖这些样式，可以使用 !important 搭配精确的选择器，例如 p[style*="purple"] { color: rebeccapurple !important; }，同时务必添加注释说明原因。}}

{{知识点31}}{{伪元素的特异性：所有双冒号伪元素（如 ::before、::after、::placeholder、::first-line）在特异性计算中属于TYPE列，每个贡献 0-0-1。注意 ::before 等同于旧语法 :before 时的特异性计算。}}

{{知识点32}}{{属性选择器与类选择器的特异性相同：属性选择器（如 [type="text"]、[data-role="nav"]）在特异性上与类选择器等价，都属于CLASS列，每个贡献 0-1-0。这意味着 .nav 和 [data-role="nav"] 的特异性相同。}}

{{知识点33}}{{:nth-child() 和 :nth-of-type() 的特异性：这些结构性伪类属于CLASS列，特异性为 0-1-0。例如 ul li:nth-child(2) 的特异性为 0-1-1（:nth-child(2) 贡献 0-1-0，li 和 ul 各贡献 0-0-1）。}}

{{知识点34}}{{:scope 伪类的特异性：:scope 作为常规伪类，特异性为 0-1-0。在 @scope 块中显式使用 :scope 时需要将其计入特异性计算。}}

{{知识点35}}{{级联特异性的四层优先级：从低到高依次为——1) 浏览器默认样式；2) 用户样式表；3) 作者样式表（常规）；4) 作者样式表中的 !important。在每个层级内，特异性决定哪个声明生效。}}

{{知识点36}}{{特异性调试技巧：使用浏览器开发者工具的"Computed"面板可以查看最终应用的样式及其来源。被覆盖的样式会被划掉显示，帮助识别特异性冲突。也可以使用在线工具如 specificity.keegan.st 计算特异性值。}}

{{知识点37}}{{降低特异性的最佳实践：优先使用类选择器而非ID选择器；避免过度嵌套选择链；使用BEM等命名方法论控制特异性；利用 :where() 为第三方样式提供低特异性入口。保持特异性扁平和低值是可维护CSS的关键。}}

{{知识点38}}{{!important 在用户样式表中的角色：!important 在用户样式表中具有最高优先级，可以覆盖作者样式表中的 !important 声明。这是无障碍访问的重要机制，允许用户强制应用辅助功能相关的样式。}}
