# 2025前端IndexedDB入门
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}
IndexedDB是浏览器内置的低级API，用于在客户端存储大量结构化数据（包括文件和Blob）。它使用索引实现对数据的高性能搜索，是Web Storage（localStorage/sessionStorage）的有力补充，适合需要存储大量数据的场景。

{{知识点2}}
IndexedDB是一个事务型数据库系统，类似于SQL关系型数据库，但它不是基于SQL的关系数据库，而是一个基于JavaScript的面向对象数据库。数据以键值对形式存储，值可以是复杂的结构化对象。

{{知识点3}}
IndexedDB遵循同源策略（Same-Origin Policy）。每个源（域名+协议+端口）都有自己独立的数据库集合，不同源之间不能互相访问数据。例如http://www.example.com和https://www.example.com的数据库是隔离的。

{{知识点4}}
IndexedDB的操作是异步的，不会阻塞应用程序。API不会通过返回值直接给你数据，而是通过请求-回调模式：你发起一个"请求"，然后通过DOM事件（success/error）获取结果。

{{知识点5}}
打开数据库使用indexedDB.open()方法，第一个参数是数据库名称，第二个参数是版本号（整数）。如果数据库不存在则创建，存在但版本号更高则触发升级。版本号会被四舍五入为整数，如2.1和2.4都会变成2。

{{知识点6}}
indexedDB.open()返回一个IDBOpenDBRequest对象，通过onsuccess和onerror事件处理结果。成功时event.target.result是IDBDatabase实例，即数据库连接对象。

{{知识点7}}
onupgradeneeded事件是唯一可以修改数据库结构的地方。在这个事件处理器中，你可以创建/删除对象存储（Object Store）和索引（Index）。当首次创建数据库或版本号升级时触发。

{{知识点8}}
对象存储（Object Store）是IndexedDB中存储数据的机制，类似于关系数据库中的"表"。每个对象存储必须有唯一的名称，可以有一个键生成器和/或一个键路径。记录按键的升序排列。

{{知识点9}}
创建对象存储使用db.createObjectStore(name, options)方法。options参数可包含keyPath（键路径，指定对象中作为主键的属性名）和autoIncrement（是否自动生成递增键）。

{{知识点10}}
键路径（keyPath）定义了对象中哪个属性作为主键。例如{keyPath: "ssn"}表示使用对象的ssn属性作为键。使用键路径时只能存储JavaScript对象，且对象必须包含对应属性。

{{知识点11}}
键生成器（autoIncrement: true）自动为每条记录生成递增的数字键，从1开始。使用键生成器时可以存储任意类型的值（包括原始值），也可以同时使用keyPath和autoIncrement。

{{知识点12}}
索引（Index）是在对象存储上创建的辅助数据结构，允许通过对象的非主键属性来查找记录。索引中的记录会在引用的对象存储插入、更新或删除时自动维护。多个索引可以引用同一个对象存储。

{{知识点13}}
创建索引使用objectStore.createIndex(name, keyPath, options)。options中的unique标志可以强制唯一性约束，确保没有两条记录在该索引键路径上有相同的值。

{{知识点14}}
事务（Transaction）是IndexedDB的核心概念。所有读写操作都必须在事务中进行。事务提供了数据隔离，确保多标签页同时操作不会互相干扰。事务有明确的生命周期，完成后不能再使用。

{{知识点15}}
创建事务使用db.transaction(storeNames, mode)。storeNames是要访问的对象存储名称（可以是数组），mode是访问模式："readonly"（只读，默认）或"readwrite"（读写）。事务在没有新请求时会自动提交。

{{知识点16}}
IDBTransaction接口的三个关键事件：complete（事务成功完成）、error（请求出错冒泡到事务）、abort（事务被中止）。可以使用transaction.abort()手动回滚事务中的所有更改。

{{知识点17}}
添加数据使用objectStore.add(value)（键已存在会报错）或objectStore.put(value)（键存在则更新）。这两个方法都返回IDBRequest对象，通过onsuccess/onerror监听结果。

{{知识点18}}
获取数据可以通过主键直接获取：objectStore.get(key)；也可以通过索引获取：index.get(key)；还可以使用getAll()获取所有记录。获取结果在request.result中。

{{知识点19}}
更新数据使用objectStore.put(value)，如果键存在则更新原记录，不存在则插入新记录。也可以在游标遍历时使用cursor.update(value)更新当前记录。

{{知识点20}}
删除数据使用objectStore.delete(key)删除指定键的记录，或objectStore.clear()清除整个对象存储。也可以在游标遍历时使用cursor.delete()删除当前记录。

{{知识点21}}
游标（IDBCursor）用于遍历或迭代对象存储/索引中的多条记录。游标有源（source，指向对象存储或索引）、位置（position）和方向（direction）。可以同时创建无限数量的游标。

{{知识点22}}
游标的四种遍历方向："next"（正序，含重复键）、"nextunique"（正序，去重）、"prev"（倒序，含重复键）、"prevunique"（倒序，去重）。通过openCursor(range, direction)指定。

{{知识点23}}
游标的常用方法：advance(count)跳过指定数量的记录、continue(key)移动到下一个位置、continuePrimaryKey(key, primaryKey)移动到指定索引键和主键位置、delete()删除当前记录、update(value)更新当前记录。

{{知识点24}}
IDBKeyRange定义键范围，用于限定查询条件。可以使用IDBKeyRange.lowerBound(lower)（大于等于）、upperBound(upper)（小于等于）、bound(lower, upper)（范围）、only(key)（精确匹配）来创建范围。

{{知识点25}}
错误事件在IndexedDB中会冒泡：从请求（IDBRequest）冒泡到事务（IDBTransaction），再冒泡到数据库（IDBDatabase）。可以在数据库对象上添加一个统一的错误处理器，避免为每个请求单独处理错误。

{{知识点26}}
VERSION_ERR是打开数据库时常见的错误，表示磁盘上存储的数据库版本号大于你试图打开的版本号。这种情况必须在错误处理器中处理。

{{知识点27}}
IndexedDB支持Web Workers，可以在Worker线程中使用IndexedDB API，不影响主线程性能。这对于需要大量数据处理的场景非常有用。

{{知识点28}}
IndexedDB存储的数据受浏览器配额限制。浏览器会根据磁盘空间和使用情况分配存储空间。隐私模式下的IndexedDB数据仅在内存中保留，关闭无痕/隐私浏览会话后数据会被清除。

{{知识点29}}
IndexedDB支持存储通过结构化克隆算法（Structured Clone Algorithm）处理的任何JavaScript对象，包括Array、Date、RegExp、Map、Set、Blob、File、ImageData等复杂类型，但不能存储函数和DOM节点。

{{知识点30}}
IndexedDB的基本使用模式五步走：①打开数据库 ②创建对象存储 ③开启事务并发起请求 ④监听DOM事件等待操作完成 ⑤在请求对象上获取结果进行处理。

{{知识点31}}
事务在事件循环任务之间会在active和inactive状态之间切换。在创建事务的任务和请求的success/error事件处理器任务中是active的，其他任务中是inactive的。在inactive状态发起请求会失败。

{{知识点32}}
IndexedDB不支持：国际化排序（不同语言排序方式不同，需要自行处理）、与服务器端数据库同步（需要自己编写同步代码）、全文搜索（没有类似SQL的LIKE操作符）。

{{知识点33}}
数据库版本升级时，如果需要修改现有对象存储的结构（如更改keyPath），必须先删除旧的对象存储再重新创建，这会清除该存储中的所有数据。升级前应先读取并备份数据。

{{知识点34}}
IDBRequest是IndexedDB中所有异步操作的通用接口。它有readyState（请求状态）、result（操作结果）、error（错误信息）属性，以及onsuccess和onerror事件处理器。

{{知识点35}}
在实际项目中，IndexedDB的原始API使用较为繁琐，推荐使用封装库如idb（Google出品的轻量级Promise封装）或Dexie.js，它们提供了更友好的Promise API和更好的错误处理。

{{知识点36}}
IndexedDB 3.0是当前规范版本（W3C标准），所有主流浏览器自2015年7月起均已支持IndexedDB。可以在Web Workers中使用，是构建离线应用（如PWA）的核心存储技术。

{{知识点37}}
事务失败的常见原因包括：违反约束（如重复添加相同主键、unique索引冲突）、脚本显式调用abort()、请求success/error处理器中有未捕获的异常、I/O错误、配额超限、浏览器崩溃等。

{{知识点38}}
使用objectStore.openCursor()打开游标，返回的onsuccess事件中event.target.result是IDBCursorWithValue实例（包含key和value）。当遍历结束时result为null，用cursor.continue()继续下一条。

{{知识点39}}
事务自动提交机制：当事务处于active状态且没有新的请求被发起，同时没有其他未完成的请求时，事务会自动提交。也可以使用transaction.commit()显式提前提交，无需等待所有事件派发。

{{知识点40}}
IndexedDB与Service Worker结合使用是PWA离线存储的标准方案。Service Worker拦截网络请求，配合IndexedDB缓存数据，可以实现完全离线运行的Web应用。
