# 2025前端Service Worker详解
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}{{Service Worker 是什么？Service Worker 是一种事件驱动的 Worker，注册在特定的源（origin）和路径下，以 JavaScript 文件的形式存在。它可以控制关联的网页/站点，拦截和修改导航及资源请求，并以极细粒度的方式缓存资源，从而让开发者完全控制应用在各种场景下的行为，最典型的是网络不可用时的离线体验。（来源：MDN Web API - Service Worker API）}}

{{知识点2}}{{Service Worker 的运行上下文。Service Worker 运行在 Worker 上下文中，因此没有 DOM 访问权限，运行在与主线程不同的线程上。它是非阻塞的，设计为完全异步。因此，同步 XHR 和 Web Storage（localStorage）等同步 API 无法在 Service Worker 中使用。IndexedDB 可以在 Service Worker 中使用来进行数据存储。（来源：MDN Web API - Service Worker API）}}

{{知识点3}}{{Service Worker 的安全要求。Service Worker 仅在安全上下文（Secure Context）中可用，即文档必须通过 HTTPS 提供服务。浏览器也将 http://localhost 视为安全上下文以方便本地开发。HTTP 连接容易受到中间人攻击（MitM），如果允许访问这些强大的 API 将带来更大风险。（来源：MDN Web API - Service Worker API）}}

{{知识点4}}{{Service Worker 的生命周期三阶段。Service Worker 遵循下载（Download）→安装（Install）→激活（Activate）的生命周期。首次访问受控站点时立即下载；之后在导航到范围内页面或事件触发且超过24小时未下载时更新。安装在文件与现有版本不同时触发（字节级比较）。首次安装成功后立即激活，后续新版本会在后台安装但不立即激活，成为"等待中的 Worker"。（来源：MDN Web API - Service Worker API）}}

{{知识点5}}{{Service Worker 的注册机制。使用 navigator.serviceWorker.register() 方法注册 Service Worker。注册成功后，Service Worker 文件会被下载到客户端并尝试安装和激活。注册时可通过 scope 参数指定 Service Worker 控制的 URL 子集，默认为 SW 文件所在目录。scope 可通过 Service-Worker-Allowed 响应头扩大。（来源：MDN Web API - Using Service Workers）}}

{{知识点6}}{{Service Worker 注册失败的常见原因。包括：未在安全上下文下运行（非 HTTPS）；SW 文件路径不正确（必须相对于源而非应用根目录）；路径指向不同源的 SW；scope 范围超过 SW 路径允许的范围；浏览器设置阻止（如隐私模式、阻止 Cookies 等）。（来源：MDN Web API - Using Service Workers）}}

{{知识点7}}{{install 事件的用途。install 事件是 Service Worker 安装或更新时触发的第一个事件，仅触发一次。通常用于预缓存应用离线运行所需的资源。通过 event.waitUntil() 确保在缓存操作完成前 SW 不会被标记为安装完成。如果 waitUntil 中的 Promise 被 reject，安装将失败。（来源：MDN Web API - Using Service Workers）}}

{{知识点8}}{{activate 事件的用途。activate 事件在 Service Worker 激活时触发。首次安装时用途不大，但在 SW 更新时非常重要——主要用于清理旧版本 SW 使用的缓存和其他资源。在 activate 事件中可以安全地删除旧缓存、迁移数据库等。（来源：MDN Web API - Using Service Workers）}}

{{知识点9}}{{skipWaiting() 方法。默认情况下，新版 Service Worker 要等所有使用旧版 SW 的页面关闭后才会激活。调用 self.skipWaiting() 可以让新 SW 立即激活，无需等待旧页面关闭。skipWaiting() 意味着新 SW 将接管控制已打开的页面。（来源：MDN Web API - Using Service Workers）}}

{{知识点10}}{{Clients.claim() 方法。Service Worker 激活后默认只控制在 register() 成功后打开的页面，文档需要重新加载才能被控制。在 activate 事件中调用 clients.claim() 可以让 SW 立即接管所有已打开的受控页面，无需用户刷新。常与 skipWaiting() 配合使用以实现即时更新。（来源：MDN Web API - Using Service Workers）}}

{{知识点11}}{{fetch 事件的拦截机制。每当受 Service Worker 控制的资源被请求时，都会触发 fetch 事件。通过 event.respondWith() 方法可以拦截请求并返回自定义响应，实现缓存优先、网络优先等各种策略。fetch 事件会拦截范围内的所有请求，包括跨域嵌入资源的请求。（来源：MDN Web API - Using Service Workers）}}

{{知识点12}}{{Cache Storage API 概述。Cache 接口提供了一个持久化的存储机制，用于缓存 Request/Response 对象对。Cache 对象的生命周期由浏览器决定，但单个源的脚本通常可以依赖之前填充的 Cache。一个源可以有多个命名的 Cache 对象。Cache API 不遵守 HTTP 缓存头，开发者需要自行管理更新和清理。（来源：MDN Web API - Cache）}}

{{知识点13}}{{Cache API 核心方法。cache.match() 查找匹配请求返回第一个响应；cache.matchAll() 返回所有匹配响应；cache.add()/cache.addAll() 获取 URL 并缓存响应；cache.put() 直接存储请求/响应对；cache.delete() 删除匹配条目；cache.keys() 返回所有缓存键。（来源：MDN Web API - Cache）}}

{{知识点14}}{{缓存优先策略（Cache First）。先从缓存中查找资源，如果命中则直接返回缓存响应；未命中则从网络请求，适用于不常变化的静态资源（如字体、图片、CSS）。这种策略速度快，能提供良好的离线体验。（来源：MDN Web API - Using Service Workers）}}

{{知识点15}}{{网络优先策略（Network First）。先尝试从网络获取资源，失败时回退到缓存。适用于需要最新数据但也要保证离线可用的场景，如新闻页面、用户动态等。可以结合超时设置实现"快照-然后更新"模式。（来源：MDN Web API - Using Service Workers）}}

{{知识点16}}{{仅网络策略（Network Only）。所有请求直接走网络，不使用缓存。适用于实时性要求极高且不需要离线支持的场景。（来源：MDN Caching Strategies）}}

{{知识点17}}{{仅缓存策略（Cache Only）。所有请求直接从缓存返回，不走网络。仅适用于在 SW 安装阶段已经预缓存好的资源。（来源：MDN Caching Strategies）}}

{{知识点18}}{{Stale-While-Revalidate 策略。先返回缓存中的旧版本（stale），同时在后台发起网络请求更新缓存（revalidate）。适合需要快速响应但允许短暂过时的资源，如 API 响应、头像等。（来源：MDN Caching Strategies）}}

{{知识点19}}{{缓存版本管理最佳实践。使用版本号命名缓存（如 font-cache-v1），在 activate 事件中通过 caches.keys() 遍历所有缓存名，删除不在期望列表中的旧缓存。确保每次部署新版本时更新缓存版本号。旧缓存需在 activate 阶段清理而非 install 阶段，避免影响正在使用的旧版本。（来源：MDN Web API - Cache）}}

{{知识点20}}{{静态路由（Static Routing）。InstallEvent.addRoutes() 方法允许开发者在安装阶段预先声明某些资源应从何处获取（网络或缓存），从而绕过 Service Worker 的运行时处理。这可以避免 SW 启动带来的性能开销——当页面长时间未加载时，浏览器需要等待 SW 启动才能决定如何加载内容。（来源：MDN Web API - Service Worker API）}}

{{知识点21}}{{Navigation Preload API。NavigationPreloadManager 提供了导航预加载功能，允许浏览器在 Service Worker 启动的同时并行发起导航请求的预加载。这解决了 SW 启动延迟导致的首次加载性能问题，通过让网络请求与 SW 初始化并行来缩短加载时间。（来源：MDN Web API - NavigationPreloadManager）}}

{{知识点22}}{{Push API 与 Service Worker 的配合。Push API 使 Web 应用能够接收从服务器推送的消息，无论应用是否在前台甚至是否已加载。应用需要先有活跃的 Service Worker，然后通过 PushManager.subscribe() 订阅推送通知。每次订阅会生成唯一的 PushSubscription，包含端点 URL 和加密密钥。（来源：MDN Web API - Push API）}}

{{知识点23}}{{Push 事件处理。当服务器推送消息到达时，Service Worker 的 push 事件被触发。SW 通过 ServiceWorkerRegistration.showNotification() 展示通知。每个订阅对 SW 是唯一的，端点 URL 是能力 URL（capability URL），知道它就能向应用发送消息，因此需要保密。（来源：MDN Web API - Push API）}}

{{知识点24}}{{Background Synchronization API。允许 Web 应用将服务器同步工作延迟到 Service Worker 中处理，适用于设备离线时的请求排队。例如邮件客户端让用户在离线时撰写邮件，应用前端注册 sync 请求，SW 在网络恢复时自动处理发送。通过 registration.sync.register("tag") 注册同步任务，在 SW 中监听 sync 事件处理。（来源：MDN Web API - Background Synchronization API）}}

{{知识点25}}{{Service Worker 中的通信机制。Service Worker 与客户端页面之间通过 postMessage() 和 MessageChannel 进行双向通信。SW 监听 message 事件接收消息，客户端通过 navigator.serviceWorker.controller.postMessage() 发送消息。也可以使用 BroadcastChannel API 实现一对多广播通信。（来源：MDN Web API - Using Service Workers）}}

{{知识点26}}{{Service Worker 的更新机制。SW 更新在以下情况自动触发：导航到范围内页面时；SW 触发事件且24小时内未更新时。浏览器获取 SW 文件后进行字节级比较，不同则触发安装流程。开发者也可通过 registration.update() 手动触发更新检查。新版本在后台安装，不影响当前活跃版本。（来源：MDN Web API - Service Worker API）}}

{{知识点27}}{{ExtendableEvent 与 waitUntil()。install 和 activate 事件是 ExtendableEvent 类型，其 waitUntil() 方法接受一个 Promise 参数，延长事件的生命周期直到 Promise 解决。这确保在异步操作（如缓存填充、旧缓存清理）完成前，SW 不会进入下一阶段，功能性事件（如 fetch）不会被分发。（来源：MDN Web API - Service Worker API）}}

{{知识点28}}{{FetchEvent 的 respondWith() 方法。FetchEvent 对象传递给 fetch 事件处理器，包含请求信息和响应能力。respondWith() 方法允许开发者用自定义的 Response 对象替代默认的网络响应。可以传入一个 Promise，该 Promise 解析为 Response 对象。这是实现缓存策略的核心方法。（来源：MDN Web API - Service Worker API）}}

{{知识点29}}{{Service Worker 中的模块导入限制。Service Worker 不能动态导入 JavaScript 模块，import() 在 SW 全局作用域中会抛出错误。但允许使用 import 语句进行静态导入。这意味着 SW 中的依赖必须在编写时确定，不能按需动态加载。（来源：MDN Web API - Service Worker API）}}

{{知识点30}}{{Service Worker 的作用域控制。一个 Service Worker 可以控制多个页面。每次范围内页面加载时，SW 都会安装并操作该页面。需要注意 SW 中全局变量的管理——每个页面不会获得独立的 Worker 实例，所有受控页面共享同一个 SW。scope 参数决定 SW 控制的 URL 范围。（来源：MDN Web API - Using Service Workers）}}

{{知识点31}}{{Service Worker 与 HTTP 缓存的关系。Cache API 不遵守 HTTP 缓存头（如 Cache-Control、Expires），它是一个独立的缓存层。Service Worker 的 fetch 拦截发生在 HTTP 缓存之前，因此可以在 SW 中决定是否使用 HTTP 缓存。最佳实践是让 SW 缓存和 HTTP 缓存协同工作，而非互相替代。（来源：MDN Web API - Cache）}}

{{知识点32}}{{Workbox 工具库概述。Workbox 是 Google 推出的 Service Worker 工具库，封装了常用的缓存策略、路由匹配、预缓存等功能。它提供了 workbox-strategies（缓存策略）、workbox-routing（路由）、workbox-precaching（预缓存）等模块，大幅简化 SW 开发。支持 webpack、Rollup 等构建工具集成。（来源：Google Chrome Developers - Workbox）}}

{{知识点33}}{{Service Worker 的单例特性。同一个 Service Worker 注册在同一个 scope 下是单例的。浏览器确保同一时间只有一个版本的 SW 在运行。新版本安装后成为"等待中的 Worker"（installing/waiting），直到旧版本的所有客户端关闭才激活。这保证了不会有两个版本的 SW 同时运行。（来源：MDN Web API - Using Service Workers）}}

{{知识点34}}{{Service Worker 中使用 IndexedDB。由于 Web Storage API（localStorage）是同步的，不能在 Service Worker 中使用。IndexedDB 是异步 API，可以在 SW 中正常工作，常用于存储需要持久化的结构化数据，如用户设置、待同步队列、离线表单数据等。（来源：MDN Web API - Using Service Workers）}}

{{知识点35}}{{Service Worker 中的错误处理。install 阶段缓存失败会导致安装失败，SW 进入 redundant 状态。fetch 事件中需要捕获网络请求异常（如断网），提供回退响应。对跨域 opaque 响应无法读取状态码和头部（response.type === 'opaque'），需注意缓存这类响应可能占用配额但无法验证内容。（来源：MDN Web API - Using Service Workers）}}

{{知识点36}}{{Service Worker 的存储配额。浏览器对每个源的缓存存储有硬性限制，可通过 StorageManager.estimate() 查询使用情况。浏览器会尽力管理磁盘空间，但可能删除整个源的 Cache 存储。建议定期清理过期缓存，监控存储使用量，避免超过配额导致数据丢失。（来源：MDN Web API - Cache）}}

{{知识点37}}{{Web App Manifest 与 Service Worker。Service Worker 常与 Web App Manifest 配合实现 PWA（渐进式 Web 应用）。Manifest 定义应用的名称、图标、主题色、启动 URL 等元数据，SW 提供离线缓存和推送通知能力。两者结合使 Web 应用可以被"安装"到设备主屏幕，提供类原生应用体验。（来源：MDN Web API - Service Worker API）}}

{{知识点38}}{{Service Worker 的调试方法。Chrome DevTools 的 Application 面板提供 Service Worker 调试功能，包括查看注册状态、强制更新、模拟离线等。可以通过 chrome://inspect/#service-workers 查看已注册的 SW。Firefox DevTools 也提供类似功能，并支持在 HTTP 下调试（需手动开启选项）。（来源：MDN Web API - Using Service Workers）}}

{{知识点39}}{{Service Worker 在 SSR/SSG 中的应用。在 Next.js、Nuxt.js 等框架中，Service Worker 可以缓存服务端渲染的 HTML 和静态资源，实现混合缓存策略。对于静态生成（SSG）的页面可采用缓存优先，动态页面采用网络优先+离线回退。需注意 HTML 页面的缓存更新策略，避免展示过时内容。（来源：MDN Web API - Service Worker API）}}

{{知识点40}}{{Service Worker 的未来发展方向。Service Worker 生态持续扩展，相关 API 包括：Background Fetch API（后台大文件下载）、Content Index API（内容索引）、Cookie Store API（异步 Cookie 操作）、Web Periodic Background Synchronization API（定期后台同步）、Web Payment Handler API（支付处理）等。这些 API 共同推动 Web 平台向原生应用能力靠拢。（来源：MDN Web API - Service Worker API）}}
