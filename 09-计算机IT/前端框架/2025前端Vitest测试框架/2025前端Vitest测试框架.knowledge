# 2025前端Vitest测试框架
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}
{{Vitest 是什么？}}
Vitest（发音为"veetest"）是由 Vite 驱动的下一代前端测试框架。它利用 Vite 的配置、转换器、解析器和插件系统，让你用与应用相同的配置来运行测试。Vitest 要求 Vite >=v6.0.0 和 Node >=v20.0.0，采用 ESM 优先设计，开箱即支持 TypeScript、JSX 和 PostCSS。

{{知识点2}}
{{Vitest 的核心特性}}
Vitest 的核心特性包括：Vite 配置/转换器/解析器/插件复用；智能即时的 Watch 模式（类似 HMR）；开箱即用的 TypeScript/JSX/ESM 支持；组件测试（Vue/React/Svelte/Lit/Marko 等）；Jest 兼容的 Mock API；浏览器模式（Browser Mode）；测试分片（Sharding）支持；未捕获错误报告。

{{知识点3}}
{{安装 Vitest}}
安装方式为 npm install -D vitest（也支持 yarn/pnpm/bun）。在 package.json 中添加 "test": "vitest" 脚本后即可运行 npm run test。也可以使用 npx vitest 直接运行而无需本地安装。Vitest 会自动提示安装缺失的依赖包。

{{知识点4}}
{{基本测试写法}}
测试文件需包含 .test. 或 .spec. 在文件名中。使用 import { expect, test } from 'vitest' 导入，test（别名 it）接收测试名称和测试函数。示例：test('adds 1 + 2 to equal 3', () => { expect(sum(1, 2)).toBe(3) })。

{{知识点5}}
{{配置 Vitest}}
Vitest 默认读取 vite.config.* 文件。如需测试专用配置，可创建 vitest.config.ts（优先级更高）。可使用 defineConfig 从 'vitest/config' 导入，或在 vite.config 中添加 /// <reference types="vitest/config" /> 获取类型支持。使用 mergeConfig 可合并多个配置。

{{知识点6}}
{{describe 套件与测试组织}}
describe 创建测试套件，可嵌套使用。支持 describe.skip、describe.only、describe.todo 等修饰符。套件内可包含多个 test/it 用例，形成层级化的测试结构。describe.concurrent 可让套件内所有测试并行执行。

{{知识点7}}
{{test 修饰符}}
test 支持多种链式修饰符：test.skip（跳过）、test.only（仅运行此项）、test.todo（标记为待办）、test.fails（预期失败）、test.concurrent（并行执行）、test.sequential（顺序执行，已弃用，用 concurrent: false 替代）。也可以用对象语法传参：test('name', { skip: true, concurrent: true }, () => {})。

{{知识点8}}
{{测试超时配置}}
默认测试超时为 5000ms。可全局配置 testTimeout，也可为单个测试设置：test('name', () => {}, 10_000) 或 test('name', { timeout: 10_000 }, () => {})。

{{知识点9}}
{{测试重试 retry}}
test 支持 retry 选项。简单用法：test('name', { retry: 3 }, () => {})。Vitest 4.1+ 支持对象配置：retry: { count: 3, delay: 1000, condition: /timeout/ }，其中 condition 可为 RegExp 或函数，用于根据错误决定是否重试。

{{知识点10}}
{{测试重复 repeats}}
repeats 选项指定测试重复执行次数，默认为 0（只运行一次）。用于调试不稳定的测试（flaky tests）。示例：test('flaky', { repeats: 10 }, () => {})。

{{知识点11}}
{{Watch 模式}}
Vitest 开发环境下默认进入 Watch 模式，利用类似 Vite HMR 的模块图智能分析，仅重新运行受影响的测试。CI 环境下自动切换为 Run 模式。使用 vitest run 强制单次运行，vitest watch 强制 Watch 模式。--standalone 标志可让 Vitest 后台运行，仅在测试变更时执行。

{{知识点12}}
{{CLI 命令}}
主要命令：vitest（启动，默认 Watch）、vitest run（单次运行）、vitest watch（Watch 模式）、vitest bench（运行基准测试）、vitest related <files>（仅运行覆盖指定源文件的测试）、vitest list（列出匹配的测试）、vitest init browser（初始化浏览器模式）。自 Vitest 3 起支持按文件名和行号过滤：vitest foo.test.ts:10。

{{知识点13}}
{{CLI 选项}}
常用选项：-r/--root（根路径）、-c/--config（配置文件）、-u/--update（更新快照）、-w/--watch、-t/--testNamePattern（按名称过滤）、--reporter（报告器）、--coverage（覆盖率）、--silent（静默输出）、--pool（线程池类型）。支持 camelCase 和 kebab-case，布尔值可用 no- 前缀取反。

{{知识点14}}
{{并行执行与隔离}}
Vitest 默认使用 node:child_process 在多进程中并行运行测试。可设置 --pool=threads 使用 node:worker_threads 进一步提速。每个文件的环境默认隔离，防止状态污染。可使用 --no-isolate 禁用隔离以换取性能。fileParallelism 选项控制文件级并行度。

{{知识点15}}
{{环境 Environment}}
支持多种测试环境：node（默认）、happy-dom、jsdom。happy-dom 和 jsdom 需单独安装。在配置中设置 test.environment: 'happy-dom' 即可。每个测试文件可通过文件顶部注释 // @vitest-environment jsdom 覆盖全局环境。

{{知识点16}}
{{vi.fn() Mock 函数}}
vi.fn() 创建一个 Mock 函数，可追踪调用情况。支持 mockImplementation、mockReturnValue、mockResolvedValue 等方法。fn.mock.calls 记录所有调用参数，fn.mock.results 记录返回值。vi.isMockFunction(fn) 判断是否为 Mock 函数。

{{知识点17}}
{{vi.spyOn() 监视}}
vi.spyOn(object, 'method') 创建对象方法的监视器，可拦截调用而不改变原始实现。支持链式 .mockReturnValue()、.mockImplementation() 等。注意：在浏览器模式下对模块导出的 spyOn 不可用，需用 vi.mock 替代。

{{知识点18}}
{{vi.mock() 模块 Mock}}
vi.mock('./module') 会在文件顶部提升（hoisted），在所有 import 之前执行。可传入工厂函数返回自定义 Mock：vi.mock('./module', () => ({ fn: vi.fn() }))。使用 importOriginal 参数可部分 Mock：vi.mock(import('./module'), async (importOriginal) => { const mod = await importOriginal(); return { ...mod, mocked: vi.fn() } })。

{{知识点19}}
{{Mock 定时器}}
vi.useFakeTimers() 启用假定时器，vi.useRealTimers() 恢复。vi.setSystemTime(date) Mock 系统时间。vi.advanceTimersByTime(ms) 推进定时器。适用于测试 setTimeout、setInterval、Date 等时间相关逻辑。

{{知识点20}}
{{Mock 全局变量}}
vi.stubGlobal('name', value) 设置全局变量。vi.stubEnv('KEY', 'value') Mock 环境变量（配合 unstubEnvs: true 可自动恢复）。vi.unstubAllGlobals() 和 vi.unstubAllEnvs() 手动恢复所有 Mock。

{{知识点21}}
{{快照测试 Snapshot}}
expect(value).toMatchSnapshot() 将值与存储的快照文件比较。首次运行创建 .snap 文件，后续运行比较差异。expect(value).toMatchInlineSnapshot() 将快照内联存储在测试文件中。使用 vitest -u 或 Watch 模式按 u 键更新快照。CI 环境默认不写入快照。

{{知识点22}}
{{文件快照与视觉快照}}
expect(result).toMatchFileSnapshot('./output.html') 可指定文件扩展名的快照，更易读。Vitest 4+ 浏览器模式支持 toMatchScreenshot() 进行视觉回归测试，截图比较 UI 组件。4.1.4+ 支持 ARIA 快照，捕获 DOM 元素的无障碍树进行语义断言。

{{知识点23}}
{{测试覆盖率 Coverage}}
支持两种覆盖率提供者：v8（默认，推荐，速度快、内存低）和 istanbul（兼容性好，支持任何 JS 运行时）。安装 @vitest/coverage-v8 或 @vitest/coverage-istanbul。配置 coverage.enabled: true 或 CLI --coverage 标志。coverage.include/exclude 控制覆盖范围。支持自定义报告器。

{{知识点24}}
{{V8 覆盖率 vs Istanbul 覆盖率}}
V8 覆盖率：无需预编译，速度快，内存低，自 Vitest v3.2.0 起精度等同 Istanbul（基于 AST 的覆盖率重映射）。限制：不支持 Firefox/Bun。Istanbul 覆盖率：通过代码插桩工作，任何 JS 运行时可用，但需预编译步骤，速度较慢，文件体积增大。

{{知识点25}}
{{测试上下文 Test Context}}
测试回调的第一个参数为 test context。内置属性：task（测试元数据）、expect（绑定当前测试的 expect）、skip（跳过测试）、annotate（添加注解）、signal（AbortSignal，超时或取消时中断）、onTestFailed/onTestFinished（钩子）。并发测试中必须使用上下文中的 expect 以确保正确的测试关联。

{{知识点26}}
{{test.extend 扩展测试上下文}}
受 Playwright Fixtures 启发，test.extend 允许定义可复用的 fixture。Builder 模式（4.1+ 推荐）：test.extend('name', value) 或 test.extend('name', async (deps, { onCleanup }) => { ... })。支持作用域：test（默认）、file、worker。onCleanup 注册清理逻辑。支持 auto: true 自动执行和 injected: true 可注入覆盖。

{{知识点27}}
{{测试标签 Tags 4.1+}}
test('name', { tags: ['db', 'flaky'] }, () => {}) 为测试添加自定义标签。标签可在配置中定义过滤规则，用于选择性运行测试。strictTags 配置控制未定义标签的行为。meta 选项（4.1+）可附加自定义元数据供报告器使用。

{{知识点28}}
{{测试过滤 Filtering}}
多种过滤方式：CLI 文件名过滤（vitest foobar）、-t/--testNamePattern 正则匹配、--testPathPattern 路径匹配、文件名:行号（vitest foo.test.ts:10）、test.only 仅运行特定测试、标签过滤、--project 按项目过滤。

{{知识点29}}
{{In-Source Testing 源码内测试}}
Vitest 支持在源代码文件内编写测试，类似 Rust 的模块测试。使用 import.meta.vitest 条件判断：if (import.meta.vitest) { const { it, expect } = import.meta.vitest; ... }。测试与实现共享闭包，可测试私有状态，无需导出。需在配置中启用 includeSource。

{{知识点30}}
{{基准测试 Benchmarking}}
使用 bench 函数进行性能基准测试，底层基于 Tinybench。示例：bench('normal', () => { ... })。vitest bench 命令运行基准测试。可比较不同实现的性能差异，输出 ops/sec 等指标。

{{知识点31}}
{{类型测试 Type Testing}}
实验性功能，可用于捕获类型回归。使用 expectTypeOf 和 assertType 进行类型断言。vitest typecheck 命令运行类型检查测试。示例：expectTypeOf(add(1, 2)).toBeNumber()。

{{知识点32}}
{{浏览器模式 Browser Mode}}
Vitest 浏览器模式在真实浏览器中运行测试，提供 window、document 等浏览器全局对象。支持 Playwright、WebdriverIO 和 Preview 提供者。配置 test.browser.enabled: true 并指定 provider 和 instances。支持 Vue/React/Svelte/Solid/Marko/Qwik 等框架。支持视觉回归测试和 ARIA 快照。

{{知识点33}}
{{浏览器模式安装}}
运行 npx vitest init browser 自动安装和配置。手动安装需选择提供者：@vitest/browser-playwright（推荐，支持并行）、@vitest/browser-webdriverio 或 @vitest/browser-preview（仅预览）。配置 instances 指定浏览器：{ browser: 'chromium' }。

{{知识点34}}
{{多项目配置 Projects}}
Vitest 支持在单进程中定义多个项目配置，适用于 monorepo。在根配置中设置 test.projects: ['packages/*']。支持 glob 模式、内联配置和配置文件引用。每个项目必须有唯一名称。--project CLI 选项可过滤运行特定项目。支持 extends: true 继承根配置。

{{知识点35}}
{{项目配置 defineProject}}
项目配置文件中推荐使用 defineProject（而非 defineConfig）以获得更好的类型安全。项目不支持所有配置选项（如 coverage、reporters 仅根级可用）。使用 mergeConfig 合并共享配置：mergeConfig(sharedConfig, defineProject({ test: { ... } }))。

{{知识点36}}
{{Mock 类 Classes}}
可通过 vi.mock 工厂函数 Mock 类：vi.mock('./module', () => { const SomeClass = vi.fn(class { someMethod = vi.fn() }); return { SomeClass } })。也可用 vi.spyOn：vi.spyOn(mod, 'SomeClass').mockImplementation(class { someMethod = vi.fn() })。

{{知识点37}}
{{Mock 文件系统}}
Vitest 支持 Mock 文件系统操作。通过 vi.mock 模块或使用 memfs 等库模拟文件系统。结合 vi.mock('fs') 可拦截文件读写操作，测试涉及文件 I/O 的逻辑而不影响真实文件系统。

{{知识点38}}
{{Mock 请求 Requests}}
可通过 vi.mock 拦截 HTTP 请求，或使用 MSW（Mock Service Worker）等库。vi.stubGlobal 可 Mock fetch 等全局请求函数。在浏览器模式下可使用更真实的网络拦截。

{{知识点39}}
{{全局配置 globals}}
在 vitest.config.ts 中设置 test.globals: true，可在测试文件中直接使用 describe/it/expect/vi 等全局 API 而无需 import。这提供更好的 Jest 兼容性。需配合 TypeScript 的类型声明：在 tsconfig 中添加 "types": ["vitest/globals"]。

{{知识点40}}
{{Setup 文件}}
通过 test.setupFiles 配置全局设置文件，在每个测试文件运行前执行。通过 test.globalSetup 配置全局 setup，仅执行一次（适合数据库初始化等）。示例：test.setupFiles: ['./setup.ts']。可用于全局注册 Mock、配置环境等。

{{知识点41}}
{{报告器 Reporters}}
Vitest 内置多种报告器：default（默认）、basic、verbose、dot、json、junit、html 等。可组合使用：reporters: ['default', 'json']。支持自定义报告器。reporter 配置可在根级和项目级设置。

{{知识点42}}
{{测试注解 Annotations 3.2+}}
annotate 函数为测试添加注解，显示在报告器中。用法：await annotate('message', 'type')。可用于关联 Issue 链接、添加调试信息等。注解支持附件（attachment）。

{{知识点43}}
{{AbortSignal 与取消}}
测试上下文中的 signal 是 AbortSignal，可在以下情况中断：测试超时、用户 Ctrl+C、programmatic cancelCurrentRun、并行测试中 bail 标志触发。用于取消 fetch 等异步操作：await fetch('/resource', { signal })。

{{知识点44}}
{{Shell 自动补全}}
Vitest 提供 shell 自动补全，基于 @bomb.sh/tab。在 ~/.zshrc 中添加 source <(vitest complete zsh) 即可启用。支持命令、选项和选项值的补全。与包管理器集成：npm vitest <Tab>、pnpm vitest <Tab> 等。

{{知识点45}}
{{expect 断言 API}}
Vitest 内置 Chai 断言库，同时兼容 Jest expect API。常用断言：toBe（严格相等）、toEqual（深度相等）、toBeTruthy/toBeFalsy、toContain（包含）、toThrow（抛出异常）、toHaveBeenCalled（Mock 调用）、toHaveBeenCalledWith（参数匹配）。支持自定义匹配器 expect.extend。

{{知识点46}}
{{生命周期钩子 Hooks}}
beforeAll/afterAll：套件开始前/结束后执行一次。beforeEach/afterEach：每个测试前/后执行。支持异步。作用域遵循 describe 层级。onTestFailed/onTestFinished 可绑定到特定测试。

{{知识点47}}
{{异步测试}}
Vitest 原生支持异步测试。测试函数返回 Promise 时，Runner 等待其 resolve。支持 async/await 语法。也支持 done 回调风格（从 Jest 迁移而来，推荐用 async 替代）。rejects 断言验证 Promise 拒绝：await expect(promise).rejects.toThrow()。

{{知识点48}}
{{vi.restoreAllMocks}}
vi.restoreAllMocks() 恢复所有被 spyOn 的原始实现。vi.clearAllMocks() 清除所有 Mock 的调用记录。vi.resetAllMocks() 重置所有 Mock（清除实现和记录）。建议在 beforeEach 中使用以确保测试隔离。

{{知识点49}}
{{覆盖率阈值 Thresholds}}
可配置覆盖率最低阈值，低于阈值测试失败。示例：coverage.thresholds: { lines: 80, functions: 80, branches: 80, statements: 80 }。支持按文件/目录设置不同阈值。CI 中用于强制代码质量门禁。

{{知识点50}}
{{从 Jest 迁移}}
Vitest 设计为 Jest 兼容，迁移成本低。主要差异：使用 vi 替代 jest（vi.fn/vi.mock/vi.spyOn）、配置文件不同（vitest.config.ts）、无需 babel（原生 ESM/TS 支持）。vitest 提供迁移指南，大部分 Jest 测试只需少量修改即可运行。done 回调需改为 async 函数。
