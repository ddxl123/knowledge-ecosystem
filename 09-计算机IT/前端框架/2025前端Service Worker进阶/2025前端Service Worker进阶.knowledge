# 2025前端Service Worker进阶
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}{{Service Worker 核心概念 — Service Worker 是一种事件驱动的 Worker，注册在特定的源和路径下，以 JavaScript 文件形式存在，可以控制关联的网页/站点，拦截和修改导航及资源请求，并以极细粒度缓存资源。它运行在 Worker 上下文中，没有 DOM 访问权限，运行在与主线程不同的线程上，设计为非阻塞且完全异步的。}}

{{知识点2}}{{Service Worker 安全要求 — Service Worker 仅在安全上下文（HTTPS）中可用，浏览器将 http://localhost 也视为安全上下文以方便本地开发。HTTP 连接容易受到中间人攻击，因此不允许在非安全连接上使用 Service Worker 这类强大 API。}}

{{知识点3}}{{Service Worker 生命周期 — 生命周期包含三个阶段：下载（Download）、安装（Install）和激活（Activate）。首次访问时立即下载，之后在导航到范围内页面或事件触发且超过 24 小时时更新。安装在检测到新版本时尝试，激活则在旧版本控制的所有页面关闭后才执行。}}

{{知识点4}}{{install 事件与预缓存 — install 事件是 Service Worker 接收的第一个事件，标准做法是在此事件中使用 Cache API 预缓存应用所需的静态资源。通过 event.waitUntil() 传入 Promise，确保在安装完成前所有资源缓存完毕，安装失败则 Service Worker 不会被激活。}}

{{知识点5}}{{activate 事件与旧缓存清理 — activate 事件在新版本 Service Worker 被激活时触发，主要用于清理与旧版本关联的缓存和其他资源。在此事件中删除不再需要的旧缓存名称，是保持缓存整洁的最佳实践。}}

{{知识点6}}{{skipWaiting() 强制激活 — 默认情况下，新版本 Service Worker 必须等待所有使用旧版本的页面关闭后才能激活。调用 self.skipWaiting() 可以让新 Service Worker 立即激活，跳过等待阶段，适用于需要紧急更新的场景。}}

{{知识点7}}{{Clients.claim() 立即控制 — 新激活的 Service Worker 默认只能控制在 register() 成功后打开的页面。在 activate 事件中调用 self.clients.claim() 可以让 Service Worker 立即接管所有已打开的同源页面，无需用户刷新。}}

{{知识点8}}{{FetchEvent 拦截请求 — fetch 事件是 Service Worker 最核心的功能事件。每当页面发起网络请求时，Service Worker 会收到 FetchEvent，通过 event.respondWith() 方法可以完全控制响应——从缓存返回、修改请求、添加自定义头等。}}

{{知识点9}}{{Cache API 基础操作 — Cache 接口提供持久化的 Request/Response 对存储。核心方法包括：cache.match() 匹配缓存、cache.add()/addAll() 获取并缓存、cache.put() 直接写入、cache.delete() 删除、cache.keys() 列出所有键。缓存不会自动过期或更新，需要手动管理。}}

{{知识点10}}{{CacheStorage 多缓存管理 — CacheStorage 接口管理多个命名的 Cache 对象。使用 caches.open('cache-name') 打开特定缓存，caches.has() 检查存在，caches.delete() 删除缓存，caches.keys() 列出所有缓存名称。建议使用版本化命名策略。}}

{{知识点11}}{{缓存配额与存储估算 — 每个浏览器对单个源的缓存存储有硬性限制。使用 StorageManager.estimate() API 可以获取当前存储使用量和配额信息。浏览器可能在磁盘空间不足时删除整个源的缓存数据，因此不能将缓存作为唯一数据存储。}}

{{知识点12}}{{VARY 头与缓存匹配 — Cache API 的键匹配算法依赖 HTTP 响应的 VARY 头。匹配新键时需要同时检查键和缓存条目的 VARY 值。注意 Cache API 不遵守 HTTP 缓存头（如 Cache-Control），缓存行为完全由代码控制。}}

{{知识点13}}{{Cache-First 策略 — 缓存优先策略：先检查缓存，命中则直接返回缓存响应，未命中则发起网络请求并将响应存入缓存后返回。适用于不经常变化的静态资源（CSS、JS、图片、字体等），能提供最快的响应速度。}}

{{知识点14}}{{Network-First 策略 — 网络优先策略：先尝试网络请求，成功则返回网络响应并更新缓存，失败则回退到缓存。适用于需要最新数据但又需要离线可用的场景，如 API 数据、新闻内容等。}}

{{知识点15}}{{Stale-While-Revalidate 策略 — 过期同时验证策略：立即从缓存返回响应（即使可能过期），同时在后台发起网络请求更新缓存。下次请求时就能获得最新数据。适用于对实时性要求不高但需要快速响应的资源。}}

{{知识点16}}{{Network-Only 策略 — 纯网络策略：所有请求都走网络，不使用缓存。适用于不支持离线的场景或需要实时数据的操作，如支付请求、实时通信等敏感操作。}}

{{知识点17}}{{Cache-Only 策略 — 纯缓存策略：所有请求都从缓存返回，不发起网络请求。适用于在 install 事件中已完全预缓存的资源，确保即使无网络也能正常显示。}}

{{知识点18}}{{Runtime Caching 运行时缓存 — 在 fetch 事件中根据请求类型动态决定缓存策略的模式。可以根据 URL 路径、文件扩展名、请求方法等条件，对不同资源应用不同的缓存策略，实现精细化的缓存控制。}}

{{知识点19}}{{Navigation Preload 导航预加载 — NavigationPreloadManager API 允许浏览器在 Service Worker 启动的同时并行发起导航请求的预加载，解决 Service Worker 启动延迟导致的首次加载性能问题。在 activate 事件中调用 registration.navigationPreload.enable() 启用。}}

{{知识点20}}{{Service-Worker-Navigation-Preload 头 — 导航预加载请求会携带 Service-Worker-Navigation-Preload HTTP 头，服务端可以据此区分普通请求和预加载请求，返回定制化内容（如仅返回页面骨架或根据登录状态返回不同内容）。}}

{{知识点21}}{{Static Routing 静态路由 — InstallEvent.addRoutes() 方法允许在安装阶段预先声明资源的获取方式，当页面请求匹配已声明的路由时，浏览器可以绕过 Service Worker 直接获取资源，消除 Service Worker 启动带来的性能开销。这是 2025 年的重要新特性。}}

{{知识点22}}{{Push API 推送通知 — Push API 赋予 Web 应用接收服务器推送消息的能力，无论应用是否在前台运行甚至未加载。需要通过 PushManager.subscribe() 订阅，获得包含端点和加密密钥的 PushSubscription，然后通过 Service Worker 的 push 事件处理接收。}}

{{知识点23}}{{PushSubscription 与安全 — 每个订阅对 Service Worker 是唯一的，端点 URL 是能力 URL（capability URL），知道端点即可发送消息，因此需要保密。不同浏览器对推送消息的资源限制策略不同，Firefox 有配额限制但通知消息豁免，Chrome 无限制。}}

{{知识点24}}{{Notification API 通知展示 — 收到推送消息后，Service Worker 通过 ServiceWorkerRegistration.showNotification() 展示系统通知。可以自定义标题、内容、图标、操作按钮、振动模式等。通知点击事件通过 notificationclick 事件监听处理。}}

{{知识点25}}{{Background Sync 后台同步 — Background Sync API 允许 Service Worker 在网络恢复时自动同步数据。通过 registration.sync.register('tag-name') 注册同步标签，当网络恢复时浏览器会触发 sync 事件，Service Worker 在其中执行数据同步操作。}}

{{知识点26}}{{Periodic Background Sync 周期同步 — PeriodicSyncManager API 允许应用以最小间隔注册周期性后台同步任务。通过 registration.periodicSync.register('tag', { minInterval }) 注册，浏览器会在条件允许时（网络、电量等）触发 periodicsync 事件。需要 PWA 安装和用户授权。}}

{{知识点27}}{{Service Worker 消息通信 — Service Worker 与页面之间通过 postMessage() API 进行双向通信。页面通过 navigator.serviceWorker.controller.postMessage() 发送，Service Worker 通过 self.clients.sendMessage() 回复。使用 message 事件监听接收。}}

{{知识点28}}{{Service Worker 更新检测 — 浏览器在以下情况检查更新：每次导航到范围内页面时、Service Worker 触发事件且距上次检查超过 24 小时时。也可通过 registration.update() 手动触发。更新时浏览器重新下载脚本并与当前版本逐字节比较。}}

{{知识点29}}{{Service Worker 范围控制 — scope 参数定义 Service Worker 控制的 URL 范围，默认为脚本所在目录。scope 不能超出脚本所在路径。通过 register('/sw.js', { scope: '/app/' }) 可以指定控制范围，但需要在 HTTP 响应头中设置 Service-Worker-Allowed。}}

{{知识点30}}{{Workbox 工具库 — Google 推出的 Service Worker 开发工具库，提供预构建缓存策略、路由规则、预缓存清单生成、过期插件等。核心模块包括 workbox-routing（路由）、workbox-strategies（策略）、workbox-precaching（预缓存）、workbox-expiration（过期管理）。}}

{{知识点31}}{{Workbox Precaching 预缓存 — workbox-precaching 模块在构建时生成资源清单（manifest），Service Worker 安装时自动缓存清单中的所有资源。支持增量更新，仅下载和缓存变更的文件。通过 self.__WB_MANIFEST 注入清单变量。}}

{{知识点32}}{{Workbox Expiration 过期策略 — workbox-expiration 插件为缓存条目提供基于数量和时间的过期策略。maxEntries 限制缓存最大条目数，maxAgeSeconds 设置最大存活时间。LRU（最近最少使用）算法自动淘汰最旧条目。}}

{{知识点33}}{{Workbox Background Sync Plugin — workbox-background-sync 插件在网络离线时将失败的请求存入 IndexedDB 队列，网络恢复后自动重发。可配置重试次数、延迟策略和请求过滤器，适用于表单提交、数据同步等场景。}}

{{知识点34}}{{Service Worker 离线页面设计 — 在 Service Worker 的 fetch 事件中，当网络请求失败且缓存也无命中时，可以返回一个预定义的离线页面（offline fallback）。通过在 install 事件中预缓存离线页面，确保用户在无网络时仍能看到友好的提示。}}

{{知识点35}}{{Service Worker 调试工具 — Chrome DevTools 的 Application 面板提供 Service Worker 调试功能：查看注册状态、强制更新、模拟离线、查看缓存内容、测试推送通知。Network 面板可以查看请求是从网络还是 Service Worker 缓存返回的。}}

{{知识点36}}{{Service Worker 与 ESM 模块 — 2025 年主流浏览器已支持在 Service Worker 中使用 ES 模块（import/export），通过 register('sw.js', { type: 'module' }) 注册模块类型 Service Worker。注意动态 import() 仍不被允许，只能使用静态 import 语句。}}

{{知识点37}}{{Service Worker 更新流程最佳实践 — 推荐流程：在 install 中预缓存核心资源 → 在 activate 中清理旧缓存 → 在 fetch 中应用缓存策略 → 使用 skipWaiting() + clients.claim() 实现无缝更新 → 通过 message 通知用户有新版本可用，提供"立即更新"按钮。}}

{{知识点38}}{{Service Worker 缓存版本管理 — 使用包含版本号或哈希值的缓存名称（如 'app-cache-v2' 或 'static-abc123'），在 activate 事件中遍历 caches.keys() 删除不匹配当前版本的旧缓存。这确保了缓存一致性并避免提供过期资源。}}

{{知识点39}}{{Service Worker 错误处理 — 在 fetch 事件中必须妥善处理异常：网络请求可能超时或失败，缓存操作可能因配额不足而失败。建议使用 try-catch 包裹 respondWith()，并在所有策略失败时返回降级响应（如离线页面或错误提示）。}}

{{知识点40}}{{Service Worker 测试策略 — 测试 Service Worker 需要考虑多种场景：首次安装、更新激活、离线回退、缓存命中/未命中、网络慢速模拟等。推荐使用 Puppeteer 配合 Chrome DevTools Protocol 模拟各种网络和 Service Worker 状态进行端到端测试。}}
