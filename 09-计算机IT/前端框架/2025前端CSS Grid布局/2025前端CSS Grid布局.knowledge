# 2025前端CSS Grid布局
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}{{CSS Grid布局是一种二维网格布局系统，是CSS中第一个专门为解决页面布局问题而创建的模块。与表格布局、浮动、定位等"hack"方式不同，Grid能真正实现行列对齐、垂直居中、元素重叠等功能。}}

{{知识点2}}{{Grid容器（Grid Container）是通过设置display: grid或display: inline-grid的元素。它是所有网格项的直接父元素。display: grid生成块级网格，display: inline-grid生成行内级网格。}}

{{知识点3}}{{Grid项（Grid Item）是Grid容器的直接子元素。只有直接子元素才是网格项，孙元素不属于网格项。如果需要孙元素参与网格布局，需要使用嵌套Grid或subgrid。}}

{{知识点4}}{{Grid线（Grid Line）是构成网格结构的分隔线，分为垂直的列网格线和水平的行网格线。Grid线在定义网格时自动编号，从1开始，也可以使用-1表示最后一条线。}}

{{知识点5}}{{Grid轨道（Grid Track）是两条相邻Grid线之间的空间，可以理解为网格的列或行。轨道的大小由grid-template-columns和grid-template-rows定义。}}

{{知识点6}}{{Grid单元格（Grid Cell）是两条相邻行Grid线和两条相邻列Grid线之间的空间，是网格的最小单位。}}

{{知识点7}}{{Grid区域（Grid Area）是由四条Grid线包围的总空间，可以包含任意数量的Grid单元格。通过grid-template-areas可以为区域命名。}}

{{知识点8}}{{grid-template-columns属性定义网格的列轨道。可以使用长度值、百分比、fr单位或auto。例如grid-template-columns: 1fr 1fr 1fr将容器分为等宽的三列。}}

{{知识点9}}{{grid-template-rows属性定义网格的行轨道。例如grid-template-rows: 100px 1fr auto定义三行：第一行固定100px，第二行占据剩余空间，第三行根据内容自适应。}}

{{知识点10}}{{fr单位（fraction unit）允许将轨道大小设置为网格容器可用空间的比例。fr在非弹性项目计算之后再分配剩余空间。例如1fr 50px 1fr 1fr中，50px固定，剩余空间三等分。}}

{{知识点11}}{{repeat()函数用于简化重复的轨道定义。例如repeat(3, 1fr)等同于1fr 1fr 1fr。支持auto-fill和auto-fit关键字实现响应式：repeat(auto-fill, minmax(200px, 1fr))。}}

{{知识点12}}{{minmax()函数定义轨道的最小和最大尺寸范围。例如minmax(100px, 1fr)表示轨道最小100px，最大为可用空间的1份。常用于响应式网格布局。}}

{{知识点13}}{{grid-template-areas属性通过引用网格区域名称来定义网格模板。重复区域名使内容跨越多个单元格，句号(.)表示空单元格。每行声明必须有相同数量的单元格。}}

{{知识点14}}{{grid-template是grid-template-rows、grid-template-columns和grid-template-areas的简写属性。语法为grid-template: <rows> / <columns>，也支持复杂语法同时定义areas。}}

{{知识点15}}{{grid属性是所有grid-template-*、grid-auto-*和grid-auto-flow的简写。推荐使用grid而非grid-template，因为grid会重置隐式网格属性。}}

{{知识点16}}{{grid-auto-rows和grid-auto-columns定义隐式创建的行/列轨道的大小。当网格项放置在显式网格之外时，会自动创建隐式轨道。默认值为auto。}}

{{知识点17}}{{grid-auto-flow控制自动放置算法的工作方式。值row（默认）按行填充，column按列填充。添加dense关键字启用紧凑填充算法，可能改变项的显示顺序。}}

{{知识点18}}{{grid-column-start、grid-column-end、grid-row-start、grid-row-end属性通过Grid线编号来放置网格项。grid-column和grid-row是对应的简写属性，用斜杠分隔起止线。}}

{{知识点19}}{{grid-area是grid-row-start、grid-column-start、grid-row-end、grid-column-end的简写。语法为grid-area: <row-start> / <column-start> / <row-end> / <column-end>。也可用于命名区域。}}

{{知识点20}}{{命名网格线使用方括号语法定义，例如grid-template-columns: [start] 1fr [middle] 1fr [end]。一条线可以有多个名称，如[row1-end row2-start]。}}

{{知识点21}}{{justify-items属性沿行轴（内联轴）对齐所有网格项的值。可选值：stretch（默认，填满单元格宽度）、start、end、center。可在单个项上用justify-self覆盖。}}

{{知识点22}}{{align-items属性沿列轴（块轴）对齐所有网格项。可选值：stretch（默认）、start、end、center、baseline。可在单个项上用align-self覆盖。还支持safe和unsafe修饰符。}}

{{知识点23}}{{place-items是align-items和justify-items的简写属性。语法为place-items: <align> / <justify>。如果只写一个值，两个方向都应用。place-items: center可快速实现多方向居中。}}

{{知识点24}}{{justify-content属性在网格总大小小于容器时，沿行轴对齐整个网格。可选值：start、end、center、stretch、space-around、space-between、space-evenly。}}

{{知识点25}}{{align-content属性在网格总大小小于容器时，沿列轴对齐整个网格。可选值与justify-content相同。place-content是两者的简写。}}

{{知识点26}}{{justify-self和align-self属性用于控制单个网格项在其单元格内的对齐方式，覆盖容器级别的justify-items和align-items设置。}}

{{知识点27}}{{gap属性（原grid-gap）定义网格线之间的间距。row-gap定义行间距，column-gap定义列间距。gap是两者的简写，语法为gap: <row-gap> <column-gap>。}}

{{知识点28}}{{Subgrid（子网格）是CSS Grid Level 2的特性。在grid-template-columns或grid-template-rows上设置subgrid值，使嵌套网格使用父网格的轨道定义，实现跨层级对齐。}}

{{知识点29}}{{Subgrid的间距会继承父网格的gap值，但可以在子网格上通过gap属性覆盖。子网格中的线编号从1重新开始，但可以使用父网格的命名线。}}

{{知识点30}}{{auto-fill与auto-fit的区别：auto-fill在没有足够项时仍创建空轨道，auto-fit则折叠空轨道使项填满可用空间。在repeat()中使用如repeat(auto-fit, minmax(250px, 1fr))实现响应式布局。}}

{{知识点31}}{{Grid布局与Flexbox的关系：Grid是二维布局（行和列），Flexbox是一维布局（行或列）。两者可以协同使用——Grid用于页面整体布局，Flexbox用于组件内部布局。}}

{{知识点32}}{{Grid项的层叠与重叠：Grid项可以互相重叠，类似定位元素。通过grid-area或grid-column/grid-row使多个项占据相同区域，再用z-index控制层叠顺序。}}

{{知识点33}}{{fit-content()函数限制轨道大小不超过指定值，同时允许内容在最小内容大小和该值之间伸缩。例如fit-content(300px)表示轨道最大不超过300px。}}

{{知识点34}}{{min-content和max-content关键字：min-content使轨道收缩到最小内容宽度，max-content使轨道扩展到最大内容宽度。常用于自适应文本布局。}}

{{知识点35}}{{Grid布局的可访问性注意事项：Grid可以改变视觉顺序与DOM顺序不一致，使用order属性或grid-placement属性时需确保屏幕阅读器仍能正确解读内容。}}

{{知识点36}}{{响应式Grid布局模式：无需媒体查询即可实现响应式。经典模式grid-template-columns: repeat(auto-fit, minmax(min(250px, 100%), 1fr))可自动适应不同屏幕宽度。}}

{{知识点37}}{{命名网格区域的响应式设计：通过媒体查询在不同断点重新定义grid-template-areas，可以优雅地重排页面布局，例如在移动端从三列变为单列。}}

{{知识点38}}{{Grid与容器查询的结合（2025新特性）：配合CSS容器查询，组件可以根据自身容器大小而非视口大小来调整Grid布局，实现更精细的响应式设计。}}

{{知识点39}}{{grid-template的简写注意事项：grid-template不重置隐式网格属性（grid-auto-rows、grid-auto-columns、grid-auto-flow），而grid简写会重置所有子属性。通常推荐使用grid。}}

{{知识点40}}{{Masonry布局（瀑布流）：CSS Grid Level 3规范草案中的特性，通过grid-template-rows: masonry实现瀑布流效果。目前仅Firefox实验性支持，是未来Grid的重要扩展方向。}}
