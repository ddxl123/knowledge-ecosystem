# 2025前端前端Gamepad API
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}{{Gamepad API 是什么}}
Gamepad API 是一组 Web API，允许前端开发者通过 JavaScript 访问和响应游戏手柄/控制器的输入信号。它提供了连接检测、按钮状态读取、摇杆轴值获取、振动反馈等功能，无需任何浏览器插件。该 API 自 2017 年起已被所有主流浏览器广泛支持（Baseline Widely available）。

{{知识点2}}{{Gamepad API 的核心接口}}
Gamepad API 包含三个核心接口：Gamepad（表示一个已连接的手柄）、GamepadButton（表示手柄上的一个按钮）、GamepadEvent（手柄相关事件对象）。此外还有实验性的 GamepadHapticActuator（振动反馈硬件）和 GamepadPose（VR 控制器姿态信息）接口。

{{知识点3}}{{Navigator.getGamepads() 方法}}
Navigator.getGamepads() 是获取已连接手柄的主要方法，返回一个 Gamepad 对象数组，每个元素对应一个已连接的手柄。如果某个手柄在会话中断开，其对应位置会变为 null 以保持索引不变。如果没有手柄连接，数组第一个元素为 null。

{{知识点4}}{{gamepadconnected 事件}}
当新手柄连接到计算机时，页面会收到 gamepadconnected 事件。如果页面加载时已有手柄连接，该事件会在用户按下按钮或移动摇杆时才触发。出于安全原因（防指纹追踪），手柄只有在页面可见且用户交互后才会暴露给页面。

{{知识点5}}{{gamepaddisconnected 事件}}
当手柄断开连接（物理拔出或超时）时，页面会收到 gamepaddisconnected 事件。开发者应在此事件中清理对手柄的引用并停止相关的轮询循环，避免无效操作。

{{知识点6}}{{Gamepad 接口的 id 属性}}
Gamepad.id 是一个只读字符串，包含控制器的标识信息。在 Firefox 中通常包含三部分用连字符分隔：两个 4 位十六进制数（USB 厂商 ID 和产品 ID）以及驱动提供的控制器名称。该信息可用于查找按钮映射方案和向用户展示反馈。

{{知识点7}}{{Gamepad 接口的 index 属性}}
Gamepad.index 是一个只读整数，对当前连接的每个手柄唯一自增。它用于区分多个同时连接的控制器，也作为 Navigator.getGamepads() 返回数组的索引。注意：断开后再连接新设备可能会复用之前的 index。

{{知识点8}}{{Gamepad 接口的 connected 属性}}
Gamepad.connected 是一个只读布尔值，表示手柄是否仍连接到系统。已连接为 true，断开为 false。在使用手柄对象前应检查此属性。

{{知识点9}}{{Gamepad 接口的 mapping 属性}}
Gamepad.mapping 是一个只读字符串，指示浏览器是否已将设备控件重映射到已知布局。当前支持两种布局：""standard"" 表示标准游戏手柄布局，""xr-standard"" 表示标准 XR 游戏手柄布局。如果 mapping 为 ""standard""，则按钮和轴的索引遵循 W3C 规范定义的固定映射。

{{知识点10}}{{Gamepad 接口的 timestamp 属性}}
Gamepad.timestamp 是一个 DOMHighResTimeStamp 只读值，表示该手柄数据最后更新的时间。该值相对于 PerformanceTiming.navigationStart，且单调递增——新值总是大于等于旧值，可用于判断硬件数据是否已更新。

{{知识点11}}{{Gamepad 接口的 axes 属性}}
Gamepad.axes 是一个只读浮点数数组，表示设备上的控制轴（如模拟摇杆）。每个条目的值范围为 -1.0 到 1.0，-1.0 代表最低值，1.0 代表最高值。标准布局下通常有 4 个轴：左摇杆水平(0)、左摇杆垂直(1)、右摇杆水平(2)、右摇杆垂直(3)。

{{知识点12}}{{Gamepad 接口的 buttons 属性}}
Gamepad.buttons 是一个只读的 GamepadButton 对象数组，表示手柄上的所有按钮。标准布局下通常有 17 个按钮（0-16），包括 A/B/X/Y、方向键、肩键、扳机键等。每个按钮对象包含 pressed、touched 和 value 属性。

{{知识点13}}{{Gamepad 接口的 hapticActuators 属性}}
Gamepad.hapticActuators 是一个只读数组，包含 GamepadHapticActuator 对象，每个对象代表控制器上可用的触觉反馈硬件（通常是振动马达）。这是一个实验性属性，浏览器支持有限。

{{知识点14}}{{Gamepad 接口的 vibrationActuator 属性}}
Gamepad.vibrationActuator 是一个只读的 GamepadHapticActuator 对象，代表控制器上可用的触觉反馈硬件。与 hapticActuators 数组不同，这是一个单一对象属性，Chrome 等浏览器通过此属性支持振动功能。

{{知识点15}}{{GamepadButton 接口}}
GamepadButton 接口定义了手柄上的单个按钮，提供对按钮当前状态的访问。通过 Gamepad.buttons 数组获取每个按钮的 GamepadButton 对象。

{{知识点16}}{{GamepadButton.pressed 属性}}
GamepadButton.pressed 是一个只读布尔值，表示按钮当前是否被按下。按下为 true，未按下为 false。这是最常用的按钮检测方式，适用于数字按钮（如 A/B/X/Y 键）。

{{知识点17}}{{GamepadButton.touched 属性}}
GamepadButton.touched 是一个只读布尔值，表示按钮当前是否被触摸。某些手柄的按钮支持触摸检测，即使未完全按下也能检测到手指接触。注意：并非所有手柄和平台都支持此功能。

{{知识点18}}{{GamepadButton.value 属性}}
GamepadButton.value 是一个只读浮点数，表示模拟按钮的当前状态。值范围为 0.0 到 1.0，0.0 表示未按下，1.0 表示完全按下。这对扳机键（trigger）特别有用，因为扳机键通常支持模拟输入（半按/全按）。

{{知识点19}}{{GamepadEvent 接口}}
GamepadEvent 接口是 Gamepad API 的事件对象，用于 gamepadconnected 和 gamepaddisconnected 事件。它扩展了标准 Event 接口，添加了 gamepad 属性，返回触发事件的 Gamepad 对象。

{{知识点20}}{{GamepadEvent() 构造函数}}
GamepadEvent() 构造函数用于创建新的 GamepadEvent 对象。语法为 new GamepadEvent(type, options)，其中 type 是事件类型，options 中可指定 gamepad 属性。一般不需要手动构造，浏览器会自动创建。

{{知识点21}}{{标准游戏手柄布局（Standard Gamepad Layout）}}
标准游戏手柄布局定义了按钮和轴的固定映射：按钮 0 是底部面部按钮（A/Cross），按钮 1 是右侧面部按钮（B/Circle），按钮 2 是左侧面部按钮（X/Square），按钮 3 是顶部面部按钮（Y/Triangle）。轴 0-1 控制左摇杆，轴 2-3 控制右摇杆。mapping 属性为 ""standard"" 时遵循此布局。

{{知识点22}}{{GamepadHapticActuator 接口}}
GamepadHapticActuator 接口代表控制器中的触觉反馈硬件，主要用于振动功能。提供 playEffect()、pulse() 和 reset() 方法，以及 effects 和 type 属性。该接口属于实验性功能，浏览器支持程度不一。

{{知识点23}}{{GamepadHapticActuator.playEffect() 方法}}
playEffect() 方法使硬件播放特定的振动效果。支持的效果类型包括 ""dual-rumble""（双马达隆隆声）和 ""trigger-rumble""（扳机振动）。参数包括 startDelay（延迟启动）、duration（持续时间）、weakMagnitude（弱马达强度）和 strongMagnitude（强马达强度），强度范围为 0.0 到 1.0。

{{知识点24}}{{GamepadHapticActuator.pulse() 方法}}
pulse() 方法使硬件以指定强度脉冲振动指定时长。参数为 intensity（强度，0.0-1.0）和 duration（持续时间，毫秒）。返回一个 Promise。这是旧式的振动 API，推荐使用 playEffect() 替代。

{{知识点25}}{{GamepadHapticActuator.reset() 方法}}
reset() 方法停止硬件正在播放的振动效果。返回一个 Promise。在需要立即停止振动时使用，比等待效果自然结束更及时。

{{知识点26}}{{GamepadHapticActuator.effects 属性}}
effects 是一个只读数组，返回该执行器支持的触觉效果枚举值。可能的值包括 ""dual-rumble"" 和 ""trigger-rumble""。可用于检测手柄支持哪些振动效果。

{{知识点27}}{{手柄连接的安全限制}}
出于安全考虑（防止指纹追踪），Gamepad API 在 Firefox 中仅在用户与可见页面上的手柄进行交互后才暴露手柄信息。在 Chrome 中也类似，需要用户在页面聚焦时按下按钮才会触发 gamepadconnected 事件。这意味着开发者不能在页面加载时静默检测用户连接的手柄。

{{知识点28}}{{轮询式读取模式}}
Gamepad API 采用轮询（polling）而非事件驱动的模式来读取按钮和轴的实时状态。开发者应在 requestAnimationFrame 循环中调用 Navigator.getGamepads() 获取当前帧的最新状态，而不是依赖事件中的 gamepad 对象（该对象可能已过时）。

{{知识点29}}{{requestAnimationFrame 结合使用}}
最佳实践是在 requestAnimationFrame 回调中轮询手柄状态。这样可以与浏览器的渲染帧同步，确保每帧读取最新的输入数据，实现流畅的游戏控制。示例：function gameLoop() { const gp = navigator.getGamepads()[0]; /* 处理输入 */ requestAnimationFrame(gameLoop); }

{{知识点30}}{{按钮缓存与单次按下检测}}
要区分按钮的""按下""和""按住""状态，需要实现按钮缓存机制：每帧将当前按钮状态保存到缓存数组，然后在下一帧对比当前状态和缓存。如果按钮在当前帧为按下状态但缓存中未按下，则为新按下（单次触发）。

{{知识点31}}{{死区（Deadzone）处理}}
模拟摇杆存在物理漂移问题，即使不触碰摇杆也可能返回非零值。解决方案是设置死区阈值（通常为 0.05-0.15），当轴值绝对值小于阈值时视为零。示例：const deadzone = 0.1; const x = Math.abs(axisValue) < deadzone ? 0 : axisValue;

{{知识点32}}{{多手柄支持}}
Gamepad API 支持同时连接多个手柄。通过 Gamepad.index 区分不同手柄，通过 Navigator.getGamepads() 获取所有手柄。典型的多人游戏需要为每个玩家分配不同的 index 并独立处理各自的输入。

{{知识点33}}{{Permissions Policy 限制}}
Navigator.getGamepads() 受 Permissions Policy（权限策略）控制。如果页面的 HTTP 响应头中设置了 ""gamepad"" 策略为不允许，调用 getGamepads() 会抛出 SecurityError DOMException。这允许网站控制哪些页面可以访问手柄。

{{知识点34}}{{GamepadPose 接口（实验性）}}
GamepadPose 接口表示 WebVR 控制器的姿态信息，包括 3D 空间中的位置和方向。该接口用于旧版 WebVR API，已被更新的 WebXR Device API 取代。通过 Gamepad.pose 属性访问。

{{知识点35}}{{Gamepad.displayId 属性（实验性）}}
Gamepad.displayId 是一个只读属性，返回关联的 VRDisplay 的 displayId。用于 WebVR 场景中标识手柄控制的是哪个 VR 显示设备。该属性属于旧版 WebVR API，在 WebXR 中不再使用。

{{知识点36}}{{Gamepad.hand 属性（实验性）}}
Gamepad.hand 是一个只读枚举值，定义控制器被握持的手或最可能被握持的手。可能的值包括 ""left""（左手）、""right""（右手）和 """"（空字符串，未知或不适用）。主要用于 VR 控制器。

{{知识点37}}{{手柄按钮名称映射（Xbox 布局示例）}}
不同手柄的按钮编号可能不同。Xbox 360 手柄的标准映射：0=DPad-Up, 1=DPad-Down, 2=DPad-Left, 3=DPad-Right, 4=LB, 5=RB, 6=LT, 7=RT, 8=Back, 9=Start, 10=Left Stick, 11=Right Stick, 12=A, 13=B, 14=X, 15=Y。开发者应检查 mapping 属性确认是否为标准布局。

{{知识点38}}{{WebXR 中的 Gamepad 扩展}}
在 WebXR 环境中，XRInputSource.gamepad 属性返回一个 Gamepad 对象，其 mapping 为 ""xr-standard""。这允许 WebXR 应用程序读取 VR 控制器的按钮和轴输入，与普通 Gamepad API 的用法类似但布局不同。

{{知识点39}}{{手柄连接的检测时机}}
如果手柄在页面加载前就已连接，gamepadconnected 事件会在用户首次操作（按按钮或移动摇杆）时才触发。这确保了用户必须主动交互，而非页面被动扫描。开发者应准备好在任意时刻接收此事件。

{{知识点40}}{{Gamepad API 的浏览器兼容性}}
Gamepad API 的核心功能（Gamepad、GamepadButton、gamepadconnected/disconnected 事件、getGamepads()）自 2017 年 3 月起被所有主流浏览器广泛支持，包括 Chrome、Firefox、Safari 和 Edge。实验性扩展（hapticActuators、pose、hand 等）的兼容性各不相同。

{{知识点41}}{{振动反馈的跨浏览器差异}}
振动反馈（GamepadHapticActuator）的支持因浏览器而异。Chrome 通过 Gamepad.vibrationActuator.playEffect() 支持 ""dual-rumble"" 效果。Firefox 的支持更为有限。Safari 对振动 API 的支持较晚。开发者应使用 feature detection 并提供降级方案。

{{知识点42}}{{Gamepad API 与 Web Game Development}}
Gamepad API 是 Web 游戏开发的重要组成部分，与 Canvas/WebGL、Web Audio、Fullscreen API 等配合使用。MDN 提供了专门的指南 ""Implementing controls using the Gamepad API""，展示了如何在游戏循环中集成手柄输入处理。

{{知识点43}}{{事件驱动 vs 轮询的混合模式}}
实际开发中通常采用混合模式：用 gamepadconnected/disconnected 事件管理手柄的连接和断开状态，用 requestAnimationFrame + getGamepads() 轮询实时输入状态。事件用于生命周期管理，轮询用于每帧输入读取。

{{知识点44}}{{手柄 ID 的指纹风险}}
Gamepad.id 字符串包含厂商 ID、产品 ID 和设备名称，可用于唯一标识用户的硬件设备。这是浏览器要求用户交互才能暴露手柄信息的主要原因之一。开发者应避免存储或传输此信息，除非有明确的用户授权。

{{知识点45}}{{Gamepad API 的使用示例——移动球体}}
经典示例：监听 gamepadconnected 事件，在事件回调中启动 requestAnimationFrame 循环，每帧读取按钮 0-3 的 pressed 状态，更新 DOM 元素的 left/top CSS 属性来移动球体。断开时取消动画帧。这是理解 Gamepad API 工作流程的最小完整示例。

{{知识点46}}{{Gamepad API 的使用示例——完整状态显示}}
进阶示例：动态创建 DOM 元素显示所有已连接手柄的按钮和轴状态，每帧更新按钮的按下/释放视觉效果和轴的进度条。该示例展示了如何处理多个手柄、遍历所有按钮和轴、以及将数值映射到 UI 渲染。

{{知识点47}}{{Gamepad 接口的 Gamepad.index 复用机制}}
当一个手柄断开后，新连接的手柄可能会复用之前的 index。这意味着开发者不能仅依赖 index 来长期标识一个特定的手柄。如果需要持久标识，应结合 Gamepad.id（设备类型信息）和 index 一起使用。

{{知识点48}}{{GamepadButton 的模拟值检测技巧}}
检测模拟按钮（如扳机键）时，建议同时检查 pressed 和 value：if (button.value > 0 || button.pressed)。这是因为某些浏览器/手柄组合中，pressed 布尔值可能不准确，而 value 值更可靠。两者结合可确保兼容性。

{{知识点49}}{{Gamepad API 的性能注意事项}}
在 requestAnimationFrame 中调用 getGamepads() 的开销很小，但应避免在循环中进行不必要的 DOM 操作。最佳实践是先读取输入、更新游戏状态，最后统一渲染。仅在按钮状态实际变化时更新 UI，而非每帧都重绘。

{{知识点50}}{{Gamepad API 的 W3C 规范状态}}
Gamepad API 由 W3C Gamepad Community Group 维护，规范地址为 https://w3c.github.io/gamepad/。核心接口已稳定，扩展接口（Gamepad Extensions）仍在发展中。规范定义了 ""standard"" 映射的标准按钮和轴布局。
