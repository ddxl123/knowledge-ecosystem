# 2025前端Three.js 3D
# 领域: 计算机IT
# 学科: 前端框架
# 来源: 2025前端Three.js 3D
# 描述: Three.js 3D核心知识点

{{Three.js概述}}
Three.js是一个基于WebGL的JavaScript 3D图形库，由Ricardo Cabello（Mr.doob）创建并维护。它封装了底层WebGL API，提供了更简洁易用的接口来创建和显示3D图形。Three.js支持场景、相机、渲染器、材质、光照、阴影、动画、粒子系统、物理模拟等完整的3D开发能力，是目前最流行的前端3D渲染库，广泛应用于数据可视化、产品展示、游戏开发、建筑可视化等领域。

{{Scene场景对象}}
Scene是Three.js中所有3D对象的容器，相当于一个"微型宇宙"。通过new THREE.Scene()创建。Scene定义了世界空间坐标系（World Space），原点为(0,0,0)。所有添加到场景中的对象形成一个场景图（Scene Graph），这是一种树状结构，Scene位于树的顶端。可以通过scene.add()添加对象，scene.remove()移除对象。scene.background可以设置背景颜色或纹理，scene.fog可以添加雾化效果。

{{PerspectiveCamera透视相机}}
PerspectiveCamera是最常用的相机类型，模拟人眼的透视效果。构造函数参数包括：fov（视野角度，通常35-75度）、aspect（宽高比，通常为容器宽/高）、near（近裁剪面，如0.1）、far（远裁剪面，如100）。相机默认位于原点(0,0,0)，需要通过camera.position.set()移动才能看到场景。camera.lookAt()可以让相机看向指定位置。远近裁剪面之间的区域称为视锥体（Viewing Frustum），只有在此范围内的物体才会被渲染。

{{OrthographicCamera正交相机}}
OrthographicCamera使用正交投影，物体不会因距离远近而产生近大远小的透视效果。构造函数参数为left、right、top、bottom、near、far，定义了立方体形状的可视区域。适用于2D场景、UI覆盖层、工程图纸、建筑平面图等不需要透视效果的场景。正交相机在等距视角游戏和CAD应用中非常有用。

{{WebGLRenderer渲染器}}
WebGLRenderer是Three.js的核心渲染器，负责将Scene和Camera的内容渲染到Canvas元素上。通过new THREE.WebGLRenderer()创建，可通过{antialias: true}参数启用抗锯齿。关键方法：renderer.setSize()设置渲染尺寸，renderer.setPixelRatio()设置像素比以适配HiDPI屏幕，renderer.render(scene, camera)执行渲染。renderer.domElement是自动创建的Canvas元素，需要手动添加到DOM中。renderer.toneMapping可设置色调映射，renderer.outputColorSpace控制输出色彩空间。

{{BufferGeometry缓冲几何体}}
BufferGeometry是Three.js中所有几何体的基类，基于GPU友好的缓冲区存储顶点数据。内置几何体包括：BoxGeometry（立方体）、SphereGeometry（球体）、PlaneGeometry（平面）、CylinderGeometry（圆柱体）、ConeGeometry（圆锥体）、TorusGeometry（环面）、TorusKnotGeometry（环面纽结）、CapsuleGeometry（胶囊体）、CircleGeometry（圆形）、RingGeometry（环形）、DodecahedronGeometry（十二面体）、IcosahedronGeometry（二十面体）等。自定义几何体通过设置position、normal、uv等BufferAttribute来定义顶点数据。

{{MeshBasicMaterial基础材质}}
MeshBasicMaterial是最简单的材质，不受光照影响，始终显示自身颜色。适用于不需要光照的场景，如线框渲染、UI元素、调试用途。支持color、wireframe、map（纹理贴图）、opacity、transparent等属性。当场景中看不到物体时，可以用MeshBasicMaterial临时替换来排查是否是光照问题。

{{MeshStandardMaterial标准PBR材质}}
MeshStandardMaterial基于物理的渲染（PBR）材质，是Three.js中最常用的材质类型。核心属性包括：color（基础颜色）、roughness（粗糙度，0-1）、metalness（金属度，0-1）、map（颜色贴图）、normalMap（法线贴图）、roughnessMap（粗糙度贴图）、metalnessMap（金属度贴图）、aoMap（环境光遮蔽贴图）、emissive（自发光颜色）、emissiveMap（自发光贴图）、envMap（环境贴图）、displacementMap（置换贴图）。PBR材质能产生逼真的物理渲染效果，适合产品展示和建筑可视化。

{{MeshPhysicalMaterial物理材质}}
MeshPhysicalMaterial继承自MeshStandardMaterial，扩展了更多物理属性。额外支持：clearcoat（清漆层，模拟汽车漆面效果）、clearcoatRoughness（清漆粗糙度）、sheen（光泽层，模拟布料效果）、sheenRoughness和sheenColor、transmission（透射率，模拟玻璃透明效果）、thickness（厚度，配合透射使用）、ior（折射率，默认1.5）、iridescence（虹彩效果）、anisotropy（各向异性）。适合渲染汽车、玻璃、宝石等需要高级视觉效果的物体。

{{MeshPhongMaterial和MeshLambertMaterial}}
MeshPhongMaterial支持镜面高光，适合塑料、金属等有光泽表面。属性包括shininess（光泽度）、specular（高光颜色）。MeshLambertMaterial基于Lambertian反射模型，只计算漫反射，不支持高光，性能更好，适合粗糙表面如木头、布料。这两种材质相比PBR材质性能开销更小，适合对性能要求较高的场景。

{{AmbientLight环境光}}
AmbientLight在整个场景中均匀照射所有物体，模拟环境间接光照。构造函数：new THREE.AmbientLight(color, intensity)。环境光不会产生阴影，通常作为场景的基础照明与其他光源配合使用。单独使用会导致场景缺乏立体感，因为没有阴影和方向性。

{{DirectionalLight方向光}}
DirectionalLight模拟太阳光等无限远处的平行光源，光线沿同一方向平行照射。构造函数：new THREE.DirectionalLight(color, intensity)。通过position属性设置方向，target属性指向目标点。支持阴影投射：castShadow设为true，shadow.mapSize设置阴影贴图分辨率。适合模拟日光、月光等自然光源。

{{PointLight点光源}}
PointLight从一个点向四面八方发射光线，模拟灯泡等点光源。构造函数参数：color、intensity、distance（光照范围，默认0表示无限）、decay（衰减，默认2）。支持阴影投射。在物理正确的光照模式下（renderer.physicallyCorrectLights = true），光照衰减遵循物理规律。

{{SpotLight聚光灯}}
SpotLight从一个点向特定方向发射锥形光束，模拟手电筒、舞台灯等。额外属性：angle（光束角度，弧度制）、penumbra（边缘柔化程度，0-1）、target（照射目标）。支持阴影投射。通过设置penumbra可以产生柔和的光照边缘效果。

{{HemisphereLight半球光}}
HemisphereLight模拟来自天空和地面的环境光照。构造函数：new HemisphereLight(skyColor, groundColor, intensity)。上半球为天空颜色，下半球为地面颜色，物体表面会根据法线方向接收不同颜色的光照。适合户外场景，可以快速营造自然的环境光照氛围，无需额外的环境光。

{{阴影系统}}
Three.js支持阴影渲染，需要设置：1）渲染器开启阴影：renderer.shadowMap.enabled = true；2）光源投射阴影：light.castShadow = true；3）物体投射阴影：mesh.castShadow = true；4）物体接收阴影：mesh.receiveShadow = true。阴影贴图类型：BasicShadowMap（基础）、PCFShadowMap（PCF软阴影，默认）、PCFSoftShadowMap（更柔和的PCF阴影）、VSMShadowMap（方差阴影）。shadow.mapSize控制阴影质量。

{{纹理Texture}}
Texture类用于加载和管理纹理贴图。通过TextureLoader加载图片纹理：new THREE.TextureLoader().load('texture.jpg')。纹理的wrapS/wrapT控制平铺方式（RepeatWrapping、ClampToEdgeWrapping、MirroredRepeatWrapping），magFilter/minFilter控制过滤方式（NearestFilter、LinearFilter等）。纹理坐标（UV）定义了纹理如何映射到几何体表面。支持的纹理类型包括颜色贴图、法线贴图、高光贴图、环境贴图、位移贴图等。

{{CubeTexture和环境贴图}}
CubeTexture用于立方体纹理，通常作为环境贴图（envMap）使用，模拟环境反射。通过CubeTextureLoader加载6张图片（对应立方体6个面）。也可以使用PMREMGenerator从HDR环境贴图生成预过滤的环境贴图。环境贴图可以让物体表面反射周围环境，是实现逼真金属和玻璃效果的关键。

{{GLTF模型加载}}
GLTF（GL Transmission Format）是3D模型的标准传输格式，被称为"3D界的JPEG"。通过GLTFLoader加载.glb或.gltf文件：new GLTFLoader().load('model.glb', (gltf) => { scene.add(gltf.scene); })。GLTF支持PBR材质、骨骼动画、变形目标、多网格等特性。gltf.animations包含动画剪辑，gltf.scene包含场景树。DRACOLoader可以加载Draco压缩的GLTF模型以减小文件大小。

{{OBJ和FBX模型加载}}
OBJLoader加载.obj格式模型，这是一种简单的纯文本3D模型格式，不支持动画和PBR材质。MTLLoader配合OBJLoader可以加载材质文件。FBXLoader加载.fbx格式模型，支持骨骼动画和更复杂的场景。FBX是Autodesk的专有格式，在游戏行业广泛使用。注意：OBJ和FBX格式相对老旧，推荐优先使用GLTF格式。

{{模型动画AnimationMixer}}
AnimationMixer是Three.js动画系统的核心，用于播放模型中的动画。创建方式：const mixer = new THREE.AnimationMixer(model)。AnimationAction代表一个动画动作，通过mixer.clipAction(clip)创建。action.play()播放，action.stop()停止，action.paused暂停。mixer.update(deltaTime)在每帧更新动画。支持动画权重混合、时间缩放、循环模式等。多个AnimationAction可以同时播放实现动画融合。

{{骨骼动画和变形目标}}
骨骼动画（Skeletal Animation）通过骨骼层级驱动网格变形，适合角色动画。SkinnedMesh是支持骨骼绑定的网格，Bone是骨骼对象。变形目标（Morph Targets）通过在不同形状之间插值实现动画，适合面部表情等。morphTargetInfluences数组控制每个变形目标的影响程度。

{{射线投射Raycaster}}
Raycaster用于鼠标拾取和碰撞检测。创建：const raycaster = new THREE.Raycaster()。设置射线：raycaster.setFromCamera(mouse, camera)。检测交叉：const intersects = raycaster.intersectObjects(objects)。intersects数组按距离排序，第一个元素为最近的交点。可以设置参数：near（最近检测距离）、far（最远检测距离）来控制检测范围。配合鼠标事件实现3D场景中的交互功能。

{{OrbitControls轨道控制器}}
OrbitControls是最常用的相机控制器，允许用户通过鼠标旋转、缩放和平移相机。通过import { OrbitControls } from 'three/examples/jsm/controls/OrbitControls'引入。创建：const controls = new OrbitControls(camera, renderer.domElement)。关键属性：enableDamping（惯性阻尼）、dampingFactor（阻尼系数）、autoRotate（自动旋转）、minDistance/maxDistance（距离限制）、minPolarAngle/maxPolarAngle（角度限制）。每帧需要调用controls.update()。

{{其他相机控制器}}
Three.js提供多种相机控制器：FlyControls（飞行控制器，类似飞行模拟）、TrackballControls（轨迹球控制器，无上下方向限制）、PointerLockControls（指针锁定控制器，第一人称视角）、FirstPersonControls（第一人称控制器）、DragControls（拖拽控制器，用于拖拽物体）。根据应用场景选择合适的控制器可以显著提升用户体验。

{{性能优化-几何体和材质}}
性能优化关键点：1）使用BufferGeometry替代旧版Geometry（已被移除）；2）合并静态几何体使用BufferGeometryUtils.mergeGeometries()减少draw call；3）使用InstancedMesh进行实例化渲染，适合大量相同物体；4）复用材质和纹理对象，避免重复创建；5）使用LOD（Level of Detail）根据距离显示不同精度的模型；6）合理设置相机的near/far裁剪面，缩小视锥体范围。

{{性能优化-渲染和内存}}
渲染优化：1）使用renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2))限制最大像素比；2）使用renderer.setSize的updateStyle参数控制CSS尺寸；3）启用frustumCustum剔除视锥体外物体（默认开启）；4）使用WebGLRenderTarget进行离屏渲染和后处理；5）使用renderer.info查看渲染统计信息（draw calls、三角形数、纹理内存等）。内存优化：及时调用.dispose()释放几何体、材质、纹理的GPU内存。

{{InstancedMesh实例化渲染}}
InstancedMesh用于高效渲染大量相同几何体和材质的物体，只需一次draw call。创建：new THREE.InstancedMesh(geometry, material, count)。通过setMatrixAt(index, matrix)设置每个实例的变换矩阵，setColorAt(index, color)设置实例颜色。适用于粒子系统、草地、森林、人群等需要渲染大量重复物体的场景。比单独创建Mesh性能提升巨大。

{{后处理效果PostProcessing}}
Three.js通过EffectComposer实现后处理效果链。常用效果包括：RenderPass（基础渲染通道）、UnrealBloomPass（泛光效果）、SSAOPass（屏幕空间环境光遮蔽）、SMAAPass/FXAApass（抗锯齿）、BokehPass（景深效果）、FilmPass（胶片颗粒效果）。后处理需要使用WebGLRenderTarget进行离屏渲染，然后通过多个Pass依次处理。每个Pass会增加GPU开销，需要在效果和性能之间权衡。

{{粒子系统Points}}
Points类用于创建粒子系统，可以高效渲染大量点。创建方式：定义BufferGeometry设置顶点位置，使用PointsMaterial材质控制粒子大小、颜色、纹理。PointsMaterial支持size（粒子大小）、sizeAttenuation（距离衰减）、map（粒子纹理）、color、opacity等属性。粒子系统适合创建星星、火焰、烟雾、雪花、雨滴等效果。也可以使用ShaderMaterial自定义粒子着色器实现更复杂的效果。

{{着色器ShaderMaterial}}
ShaderMaterial允许编写自定义GLSL着色器。顶点着色器（Vertex Shader）处理每个顶点的位置变换，片元着色器（Fragment Shader）计算每个像素的最终颜色。关键属性：vertexShader（顶点着色器代码）、fragmentShader（片元着色器代码）、uniforms（着色器变量）、side（渲染面）、transparent、depthWrite等。RawShaderMaterial使用更底层的着色器，不包含Three.js内置的uniform和attribute。自定义着色器可以实现无限的视觉效果，如水波、火焰、卡通渲染等。

{{Water水体效果}}
Three.js examples提供了Water类，实现逼真的水面反射和折射效果。基于自定义着色器实现，支持波纹动画、反射、折射、菲涅尔效果。创建时需要传入水体几何体和参数如textureWidth、textureHeight（反射纹理分辨率）、distortionScale（扭曲程度）、waterColor等。通常配合环境贴图使用，适合海洋、湖泊、河流等水体场景。

{{Sky天空盒}}
Three.js examples提供了Sky类，可以创建程序化生成的天空。基于大气散射模型，支持太阳位置、大气参数调节。通过sun属性控制太阳方向，turbidity（浊度）、rayleigh（瑞利散射）、mieCoefficient（米氏散射系数）、mieDirectionalG（米氏散射方向性）等参数控制天空外观。配合DirectionalLight和HemisphereLight可以创建逼真的户外光照环境。

{{物理引擎集成}}
Three.js本身不包含物理引擎，但可以与多种物理引擎集成：Ammo.js（Bullet物理引擎的JavaScript版本）、Cannon.js/Cannon-es（轻量级物理引擎）、Rapier（Rust编写的高性能物理引擎）、PhysX（NVIDIA的物理引擎）。物理引擎提供刚体模拟、碰撞检测、关节约束、布料模拟等功能。通过在渲染循环中同步物理引擎的物体位置来实现物理效果。

{{WebGPU渲染器}}
Three.js在2024-2025年逐步完善了WebGPU渲染器支持。WebGPU是下一代Web图形API，相比WebGL提供更好的性能和更现代的API设计。WebGPURenderer支持计算着色器（Compute Shader），可以用于粒子模拟、光线追踪等GPU计算任务。WebGPU还支持更高效的多线程渲染和更好的GPU资源管理。目前WebGPU仍在发展中，兼容性不如WebGL完善，但在支持的浏览器中性能优势明显。

{{ToneMapping色调映射}}
色调映射将HDR（高动态范围）颜色映射到LDR（低动态范围）显示。Three.js支持多种色调映射模式：NoToneMapping（无映射）、LinearToneMapping、ReinhardToneMapping、CineonToneMapping、ACESFilmicToneMapping（电影级映射，推荐）。通过renderer.toneMapping设置，renderer.toneMappingExposure控制曝光度。ACESFilmicToneMapping能产生最接近电影画面的色彩效果，是当前最常用的色调映射方式。

{{色彩空间管理}}
Three.js r152+引入了新的色彩空间管理系统。renderer.outputColorSpace = THREE.SRGBColorSpace设置输出色彩空间。纹理需要正确设置colorSpace：颜色贴图和发射贴图使用SRGBColorSpace，其他贴图（法线、粗糙度等）使用LinearSRGBColorSpace。颜色管理通过THREE.ColorManagement.enabled = true启用（默认开启）。正确的色彩空间设置对于PBR材质的准确渲染至关重要。

{{场景图和Object3D}}
Object3D是Three.js中所有可显示对象的基类，包括Mesh、Light、Camera、Group等。Object3D具有position（位置）、rotation（旋转）、quaternion（四元数旋转）、scale（缩放）、visible（可见性）等属性。通过add()和remove()管理父子层级关系，子对象的变换相对于父对象。Group是Object3D的子类，用于将多个对象组织在一起进行统一变换。Matrix4表示4x4变换矩阵，compose()和decompose()用于矩阵和位置/旋转/缩放之间的转换。

{{调试和开发工具}}
Three.js提供多种调试工具：1）AxesHelper显示XYZ坐标轴；2）GridHelper显示网格地面；3）ArrowHelper显示方向箭头；4）Box3Helper显示包围盒；5）CameraHelper显示相机视锥体；6）DirectionalLightHelper、PointLightHelper、SpotLightHelper显示光源位置和方向。dat.GUI/lil-gui库常用于创建参数控制面板，实时调试材质、光照、相机参数。renderer.info可以查看渲染统计信息帮助性能分析。

{{Three.js与React集成（React Three Fiber）}}
React Three Fiber（R3F）是Three.js的React渲染器，允许使用声明式的JSX语法编写3D场景。核心组件：<Canvas>（渲染画布）、<mesh>（网格）、<boxGeometry>（几何体）、<meshStandardMaterial>（材质）。支持所有React特性：hooks、context、suspense等。@react-three/drei提供了大量实用组件如OrbitControls、Environment、Text等。@react-three/postprocessing提供后处理效果。R3F是2025年React生态中最流行的3D开发方案。

{{Three.js与Vue集成}}
TresJS是Three.js的Vue 3集成库，基于Composition API。使用自定义渲染器将Vue组件映射到Three.js对象。核心组件：<TresCanvas>（画布）、<TresMesh>（网格）、<TresPerspectiveCamera>（相机）。支持Vue的响应式系统，数据变化自动更新3D场景。Cientos是TresJS的辅助库，提供常用组件封装。Vue 3 + TresJS是Vue生态中3D开发的主流方案。

{{Three.js r170+新特性}}
Three.js持续更新，2025年的主要改进包括：1）WebGPU渲染器进一步稳定，支持更多特性；2）Node Material系统提供更灵活的材质节点图；3）改进的阴影系统和光照模型；4）更好的TypeScript类型支持；5）性能优化和内存管理改进；6）新的几何体和材质类型。Three.js采用语义化版本，每月发布新版本，关注GitHub仓库的Releases页面了解最新变化。
