# 2025前端API设计模式
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}{{RESTful API 是什么？REST（Representational State Transfer）是一种由 Roy Fielding 在 2000 年博士论文中提出的架构风格，用于分布式超媒体系统。它不是协议或标准，而是一组架构约束原则。RESTful API 是符合 REST 架构风格的 Web API，利用标准 HTTP 方法（GET、POST、PUT、DELETE）和 URI 来标识和操作资源。}}

{{知识点2}}{{REST 六大核心约束包括：统一接口（Uniform Interface）、客户端-服务器分离（Client-Server）、无状态（Stateless）、可缓存（Cacheable）、分层系统（Layered System）、按需代码（Code on Demand，可选）。这些约束共同保证了 REST 架构的简洁性、可扩展性和可演进性。}}

{{知识点3}}{{REST 统一接口的四个子约束：资源标识（通过 URI 唯一标识每个资源）、通过表示操作资源（服务器返回的表示可用于修改资源状态）、自描述消息（每个响应包含足够处理信息）、HATEOAS（超媒体作为应用状态引擎，客户端仅需初始 URI，其余通过超链接驱动）。}}

{{知识点4}}{{REST 无状态原则要求每个客户端请求必须包含服务器完成该请求所需的全部信息，服务器不存储客户端上下文。客户端应用完全负责维护会话状态。这一约束简化了服务器设计，提升了可扩展性，使得任何服务器实例都能处理任何请求。}}

{{知识点5}}{{RESTful API 中 HTTP 方法语义：GET 用于获取资源（安全且幂等）、POST 用于创建新资源（非幂等）、PUT 用于完整替换资源（幂等）、PATCH 用于部分更新资源（非幂等）、DELETE 用于删除资源（幂等）。正确使用 HTTP 方法语义是 RESTful 设计的基础。}}

{{知识点6}}{{GraphQL 是什么？GraphQL 是 Facebook（现 Meta）于 2012 年内部开发、2015 年开源的 API 查询语言和运行时。它允许客户端精确指定所需数据的结构和字段，解决了 REST 中常见的过度获取（over-fetching）和不足获取（under-fetching）问题。GraphQL 通过强类型 Schema 定义数据结构，支持查询（Query）、变更（Mutation）和订阅（Subscription）三种操作类型。}}

{{知识点7}}{{GraphQL Schema 与类型系统是 GraphQL 的核心。Schema 使用 SDL（Schema Definition Language）定义，包含标量类型（String、Int、Float、Boolean、ID）、对象类型、接口类型、联合类型、枚举类型和输入类型。类型系统在编译时验证查询正确性，提供强大的自动文档和内省（Introspection）能力。}}

{{知识点8}}{{GraphQL 查询的精确获取特性：客户端在查询中指定所需字段，服务器仅返回请求的字段。例如查询用户时可以只要 name 和 email，不要 address 和 phone。这避免了 REST 中获取整个资源表示导致的带宽浪费，特别适合移动端和弱网络环境。}}

{{知识点9}}{{GraphQL Resolver 是服务器端处理每个字段查询的函数。Resolver 构成执行树，每个字段对应一个 resolver。Resolver 可以从数据库、REST API、微服务等多种数据源获取数据。GraphQL 执行引擎按照查询的字段树深度优先遍历执行 resolver，组装最终响应。}}

{{知识点10}}{{GraphQL N+1 查询问题是 resolver 执行中的经典性能陷阱。当查询列表中每个元素的嵌套字段时，可能为每个元素触发一次数据库查询。解决方案包括 DataLoader 模式（批量加载和缓存）、JOIN 查询优化、以及预加载策略。DataLoader 在同一事件循环 tick 内合并多个请求为批量调用。}}

{{知识点11}}{{GraphQL 分页策略包括：偏移分页（offset/limit，简单但大数据集性能差）、游标分页（cursor-based，使用 Relay 规范的 Connection 模式：edges/node/cursor/pageInfo）。Relay Connection 规范定义了 PageInfo 类型包含 hasNextPage/hasPreviousPage/startCursor/endCursor，是 GraphQL 分页的事实标准。}}

{{知识点12}}{{GraphQL Federation（联合）是 Apollo 提出的微服务架构模式，允许多个团队独立开发和部署各自的 GraphQL 子图（Subgraph），通过网关（Gateway/Supergraph）组合成统一的 API。每个子图使用 @key 指令定义实体，通过 @extends 和 @external 实现跨服务引用，支持大型组织的 API 规模化治理。}}

{{知识点13}}{{gRPC 是什么？gRPC 是 Google 开发的高性能远程过程调用（RPC）框架，使用 Protocol Buffers（protobuf）作为默认的接口定义语言（IDL）和消息序列化格式。gRPC 基于 HTTP/2 协议，支持双向流式传输、头部压缩和多路复用。客户端可以像调用本地方法一样调用远程服务器上的方法。}}

{{知识点14}}{{Protocol Buffers（protobuf）是 Google 的结构化数据序列化机制。开发者在 .proto 文件中定义消息结构和服务接口，protoc 编译器生成各语言的数据访问类。相比 JSON，protobuf 体积更小（二进制格式）、序列化/反序列化速度更快、具有强类型和向后兼容的字段编号机制。}}

{{知识点15}}{{gRPC 四种通信模式：一元 RPC（Unary，请求-响应）、服务器流式 RPC（Server Streaming，客户端发一个请求，服务器返回流）、客户端流式 RPC（Client Streaming，客户端发送流，服务器返回一个响应）、双向流式 RPC（Bidirectional Streaming，双方同时发送消息流）。流式模式适合实时数据推送、大文件传输等场景。}}

{{知识点16}}{{gRPC-Web 是 gRPC 的浏览器客户端实现，解决浏览器不直接支持 HTTP/2 帧控制的问题。gRPC-Web 通过代理层（如 Envoy）将浏览器的 HTTP/1.1 请求转换为后端的 gRPC 调用。支持 Unary 和 Server Streaming 两种模式。2024 年 Connect 协议（由 Buf 团队推出）提供了更原生的浏览器 gRPC 体验。}}

{{知识点17}}{{tRPC 是什么？tRPC 是一个端到端类型安全的 TypeScript API 框架，无需 Schema 定义或代码生成。它直接利用 TypeScript 类型推断，在客户端获得完整的类型安全和自动补全。tRPC 集合了 REST 和 GraphQL 的概念优势，构建时发现类型错误，运行时零额外开销。}}

{{知识点18}}{{tRPC 核心特性包括：全静态类型安全（输入、输出和错误均有类型推断）、零代码生成（纯 TypeScript 类型推断）、轻量级（零运行时依赖）、框架无关（支持多种后端框架适配器）、请求批处理（同时间的多个请求自动合并为一个 HTTP 请求）、订阅支持（类型安全的实时更新）。}}

{{知识点19}}{{tRPC 与 TanStack Query 集成是其最强大的前端使用模式。tRPC 提供 @trpc/react-query 包，将 tRPC 的过程调用直接映射为 TanStack Query 的 useQuery/useMutation，获得自动缓存、后台刷新、乐观更新、分页等能力，同时保持完整的类型安全。}}

{{知识点20}}{{tRPC 的 Router 和 Procedure 概念：Router 是 tRPC 服务端的核心组织单元，包含多个 Procedure（过程）。Procedure 分为 query（查询）、mutation（变更）和 subscription（订阅）类型。每个 Procedure 可定义 input 验证（使用 Zod 等库）、output 验证和中间件，形成类型安全的 API 管道。}}

{{知识点21}}{{TanStack Query（原 React Query）是前端服务器状态管理库，专注于获取、缓存、同步和更新服务器状态。它解决了传统状态管理库处理异步/服务器状态的不足，提供自动缓存、请求去重、后台数据刷新、垃圾回收、结构共享等能力。核心 API 包括 useQuery、useMutation、useInfiniteQuery 和 QueryClient。}}

{{知识点22}}{{OpenAPI（前身 Swagger）是 RESTful API 的描述规范。OpenAPI 3.x 使用 YAML/JSON 描述 API 的路径、操作、参数、请求体、响应、安全方案等。基于 OpenAPI Spec 可自动生成客户端 SDK（如 openapi-generator）、API 文档（如 Swagger UI）、以及类型定义（如 openapi-typescript），实现 REST API 的类型安全。}}

{{知识点23}}{{BFF（Backend For Frontend）模式是为每种前端客户端（Web、Mobile、IoT）提供专门的后端服务层。BFF 聚合多个下游微服务的数据，按前端需求裁剪响应。2025 年 BFF 常用 Node.js/TypeScript 实现，与 GraphQL 或 tRPC 结合，由前端团队自主维护，解决 API 粒度不匹配和多端差异化需求问题。}}

{{知识点24}}{{API 版本控制策略：URL 路径版本（/v1/users，最直观）、请求头版本（Accept: application/vnd.api.v1+json，URL 纯净）、查询参数版本（?version=1，灵活但不优雅）。GraphQL 通常通过 Schema 演进（弃用字段 @deprecated）和增量添加实现无版本破坏性变更。gRPC 通过 Protobuf 的字段编号机制天然支持向后兼容。}}

{{知识点25}}{{API 限流（Rate Limiting）是保护 API 不被滥用的关键机制。常见算法包括：固定窗口计数器（简单但有突发问题）、滑动窗口（更平滑）、令牌桶（Token Bucket，允许一定突发）、漏桶（Leaky Bucket，恒定速率输出）。前端应对限流需实现指数退避重试（Exponential Backoff）和 Retry-After 头解析。}}

{{知识点26}}{{前端 API 缓存策略分层：HTTP 缓存层（Cache-Control、ETag、Last-Modified）、客户端状态管理缓存（TanStack Query、SWR、Apollo Cache）、Service Worker 缓存（Cache API、IndexedDB 持久化）。合理分层缓存可显著减少网络请求、提升用户体验。GraphQL 的规范化缓存（按 __id 拼接实体）是其独特优势。}}

{{知识点27}}{{SWR（Stale-While-Revalidate）是由 Vercel 提出的数据获取策略，先返回缓存数据（stale），同时在后台重新验证（revalidate），验证完成后更新。SWR 库实现了这一策略，提供 useSWR Hook。TanStack Query 也采用类似策略。这种模式让用户感知更快的响应速度，同时保持数据最终一致性。}}

{{知识点28}}{{Server-Sent Events（SSE）是 HTML5 标准的服务器向客户端单向推送技术，基于 HTTP 协议的长连接。相比 WebSocket，SSE 更简单、自动重连、支持 HTTP/2 多路复用。2025 年 SSE 在 AI 流式响应（如 ChatGPT API 的 stream 模式）中广泛应用。EventSource API 是浏览器原生的 SSE 客户端。}}

{{知识点29}}{{WebSocket 是全双工通信协议，允许客户端和服务器之间建立持久连接并双向实时通信。通过 HTTP 升级握手建立连接后，使用 ws:// 或 wss:// 协议。适用于聊天、实时协作、游戏、金融行情等场景。Socket.IO 是最流行的 WebSocket 库，提供自动重连、房间、广播等高级功能。}}

{{知识点30}}{{API 错误处理最佳实践：使用标准 HTTP 状态码（2xx 成功、4xx 客户端错误、5xx 服务器错误）、返回结构化错误响应（包含 code/message/details/requestId）、前端统一错误拦截（Axios 拦截器/Fetch 封装）、区分可重试错误（5xx/429）和不可重试错误（4xx）、用户友好的错误消息映射。}}

{{知识点31}}{{前端 API 层抽象模式：Repository 模式（将 API 调用封装为领域方法）、Service 层模式（集中管理请求逻辑）、Hook 模式（React 中使用自定义 Hook 封装数据获取）。推荐将 API 调用与 UI 组件解耦，通过类型安全的抽象层隔离网络细节，便于测试和切换实现。}}

{{知识点32}}{{GraphQL 客户端选型：Apollo Client（功能最全，规范化缓存、乐观更新、开发工具）、urql（轻量可扩展）、Relay（Facebook 出品，编译时优化最强）、TanStack Query + graphql-request（灵活组合）。选择标准：项目规模、缓存需求、框架生态、团队经验。2025 年 Apollo Client 和 TanStack Query 组合是主流。}}

{{知识点33}}{{API 安全最佳实践：使用 HTTPS 加密传输、JWT/OAuth2 认证授权、CORS 精确配置（避免通配符 *）、输入验证与参数化查询防注入、API 密钥管理（环境变量/密钥管理服务）、请求签名防篡改、Content-Security-Policy 头部。GraphQL 需特别注意查询深度限制和复杂度分析防 DDoS。}}

{{知识点34}}{{API Mock 与测试策略：MSW（Mock Service Worker）通过拦截网络请求实现 API Mock，不影响业务代码、支持 REST 和 GraphQL、可在浏览器和 Node.js 中运行。相比传统 Mock 方式（修改 fetch/axios），MSW 在网络层拦截更真实。结合 OpenAPI 自动生成 Mock 数据是 2025 年的主流实践。}}

{{知识点35}}{{前端 API 请求库对比：Fetch API（原生，无依赖但 API 较底层）、Axios（功能丰富，拦截器/取消请求/自动 JSON 转换）、ky（基于 Fetch 的轻量封装）、ofetch（unjs 出品，Nuxt 默认）。2025 年趋势：基于 Fetch 的轻量封装更受欢迎，Axios 仍是企业项目首选，原生 Fetch 在新项目中使用率上升。}}

{{知识点36}}{{tRPC 迁移策略：tRPC 可渐进式采用，不需要一次性替换所有 API。推荐策略：先用 tRPC 实现新端点，再逐步迁移旧端点。tRPC 支持挂载为现有 REST API 的端点，可与其他 API 共存。对于非 TypeScript 项目，可使用 OpenAPI 插件（trpc-openapi）将 tRPC 过程暴露为 REST 端点。}}

{{知识点37}}{{API 设计模式选型决策框架：简单 CRUD 且团队熟悉 → RESTful；需要灵活数据获取和移动端优化 → GraphQL；高性能微服务间通信 → gRPC；全栈 TypeScript 项目追求类型安全 → tRPC。实际项目常混合使用：对外 REST/GraphQL，对内 gRPC，全栈 TS 用 tRPC 做 BFF。}}

{{知识点38}}{{GraphQL 性能优化策略：持久化查询（Persisted Queries，只允许预注册的查询执行）、响应缓存（CDN 级别缓存 GET 请求）、DataLoader 批量加载、查询复杂度分析与限制（防止过深/过宽查询）、延迟加载（@defer 指令）、流式加载（@stream 指令，逐条返回列表数据）。}}

{{知识点39}}{{gRPC 在前端的适用场景与局限：适用场景包括内部微服务通信、需要高性能低延迟的实时应用、跨语言多平台服务。前端局限：浏览器原生不支持（需 gRPC-Web 或 Connect 代理）、调试不如 REST/GraphQL 直观（二进制协议）、学习曲线较陡（需学 Protobuf）。Connect 协议正在改善浏览器体验。}}

{{知识点40}}{{API 文档自动化趋势：2025 年主流方案包括基于代码注释自动生成（Swagger JSDoc/tsdoc）、API-first 设计（先写 OpenAPI Spec 再实现）、类型推断自动文档（tRPC 的 playground）、GraphQL 的 GraphiQL/Altair 内省文档。API 文档自动化是前后端协作效率的关键因素，减少沟通成本和接口不一致。}}
