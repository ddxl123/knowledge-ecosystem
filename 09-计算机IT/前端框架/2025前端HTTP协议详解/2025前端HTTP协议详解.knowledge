# 2025前端HTTP协议详解
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}{{HTTP协议概述}}
HTTP（HyperText Transfer Protocol）是应用层协议，采用客户端-服务器模型。客户端（通常是浏览器）发送请求，服务器返回响应。HTTP位于TCP/IP协议栈的应用层，默认端口80（HTTPS为443）。HTTP是无状态的，但通过Cookie机制可以实现有状态的会话。HTTP/1.1默认使用持久连接（Keep-Alive），避免为每个请求都建立新TCP连接。HTTP协议的设计原则包括：简单性（人类可读）、可扩展性（通过HTTP头部）、无状态但非无会话。

{{知识点2}}{{HTTP请求方法（HTTP Verbs）}}
HTTP定义了一组请求方法来指示请求的目的：GET（获取资源，安全且幂等，可缓存）、HEAD（获取响应头，无响应体）、POST（提交数据，可能产生副作用）、PUT（替换目标资源的所有当前表示）、DELETE（删除指定资源）、CONNECT（建立到服务器的隧道）、OPTIONS（描述通信选项，用于CORS预检请求）、TRACE（执行消息回环测试）、PATCH（对资源应用部分修改）。GET/HEAD/OPTIONS/TRACE是安全的（不修改服务器状态），GET/HEAD/PUT/DELETE是幂等的（多次执行结果相同）。

{{知识点3}}{{HTTP响应状态码 - 1xx信息性}}
1xx状态码表示临时响应，需要请求者继续执行操作。100 Continue：客户端应继续发送请求体。101 Switching Protocols：服务器同意切换协议（如升级到WebSocket）。102 Processing：WebDAV专用，表示服务器已收到请求但尚无状态可用。103 Early Hints：允许服务器在最终响应前发送Link头部，让浏览器提前预加载资源，提升页面加载性能。103是前端性能优化中值得关注的新特性。

{{知识点4}}{{HTTP响应状态码 - 2xx成功}}
2xx表示请求成功。200 OK：请求成功，GET返回资源内容，POST返回操作结果。201 Created：请求成功并创建了新资源（POST/PUT）。202 Accepted：请求已接收但尚未处理完成，适用于异步场景。204 No Content：成功但无响应体，常用于DELETE操作。205 Reset Content：要求用户端重置文档视图。206 Partial Content：范围请求成功，用于断点续传和视频流播放。207 Multi-Status：WebDAV专用，返回多个资源的状态信息。

{{知识点5}}{{HTTP响应状态码 - 3xx重定向}}
3xx表示需要进一步操作才能完成请求。300 Multiple Choices：有多个可能的响应。301 Moved Permanently：资源永久移动到新URL，浏览器会缓存重定向。302 Found：资源临时移动（语义上允许方法改变，但实践中浏览器保持原方法）。303 See Other：要求用GET方法访问另一个URI。304 Not Modified：资源未修改，使用缓存版本（条件请求）。307 Temporary Redirect：临时重定向，严格保持原请求方法不变。308 Permanent Redirect：永久重定向，严格保持原请求方法不变。307/308比302/301更严格。

{{知识点6}}{{HTTP响应状态码 - 4xx客户端错误}}
4xx表示客户端请求有误。400 Bad Request：请求语法错误或参数无效。401 Unauthorized：需要身份认证（配合WWW-Authenticate头部）。403 Forbidden：服务器拒绝执行请求（认证后权限不足）。404 Not Found：资源不存在。405 Method Not Allowed：请求方法不被允许。408 Request Timeout：服务器等待请求超时。409 Conflict：请求与服务器当前状态冲突。413 Payload Too Large：请求体过大。415 Unsupported Media Type：不支持的媒体类型。429 Too Many Requests：请求频率超限，配合Retry-After头部使用。431 Request Header Fields Too Large：请求头部过大。

{{知识点7}}{{HTTP响应状态码 - 5xx服务器错误}}
5xx表示服务器处理请求时出错。500 Internal Server Error：服务器内部错误，通用错误响应。501 Not Implemented：服务器不支持请求所需的功能。502 Bad Gateway：网关或代理服务器从上游收到无效响应。503 Service Unavailable：服务器暂时无法处理请求（过载或维护），常配合Retry-After使用。504 Gateway Timeout：网关或代理服务器等待上游服务器响应超时。505 HTTP Version Not Supported：不支持的HTTP版本。在前端开发中，理解5xx错误有助于正确处理服务端异常和实现重试机制。

{{知识点8}}{{HTTP请求头部（Request Headers）}}
HTTP请求头部提供请求的附加信息。Host（必需，HTTP/1.1）：指定服务器域名和端口。User-Agent：客户端标识。Accept：客户端可处理的MIME类型。Accept-Language/Content-Language：语言偏好。Content-Type：请求体的MIME类型。Authorization：认证凭证。Cookie：发送存储的Cookie。Referer：请求来源页面URL。Origin：CORS请求的源。Cache-Control：缓存策略。If-Modified-Since/If-None-Match：条件请求头部，用于缓存验证。Range：范围请求，用于断点续传。

{{知识点9}}{{HTTP响应头部（Response Headers）}}
HTTP响应头部提供响应的附加信息。Content-Type：响应体的MIME类型和字符编码。Content-Length：响应体大小。Content-Disposition：控制响应是内联显示还是作为附件下载。Set-Cookie：设置Cookie。Location：重定向目标URL（配合3xx使用）。Access-Control-Allow-Origin：CORS允许的源。Cache-Control：缓存策略。ETag：资源的唯一标识，用于条件请求。Last-Modified：资源最后修改时间。Vary：影响缓存键的请求头部。Server：服务器软件信息。Strict-Transport-Security：HSTS安全头部。

{{知识点10}}{{HTTP消息体格式}}
HTTP消息由头部和可选的空行+消息体组成。请求格式：请求行（方法+URI+版本）、请求头部、空行、请求体。响应格式：状态行（版本+状态码+原因短语）、响应头部、空行、响应体。Content-Type头部决定消息体的解析方式：application/json（JSON数据）、application/x-www-form-urlencoded（表单数据）、multipart/form-data（文件上传）、text/html（HTML）、text/plain（纯文本）。在前端fetch/axios中，需要正确设置Content-Type以匹配发送的数据格式。

{{知识点11}}{{CORS跨域资源共享}}
CORS（Cross-Origin Resource Sharing）是浏览器的安全机制，用于控制跨域HTTP请求。同源策略限制不同源（协议+域名+端口）之间的请求。CORS通过添加新的HTTP头部让服务器声明哪些源可以访问资源。简单请求（GET/HEAD/POST且Content-Type为text/plain/multipart/form-data/application/x-www-form-urlencoded）直接发送。非简单请求会先发送OPTIONS预检请求（Preflight），服务器通过Access-Control-Allow-Origin、Access-Control-Allow-Methods、Access-Control-Allow-Headers头部响应。带凭证的请求需要设置credentials: 'include'且服务器返回Access-Control-Allow-Credentials: true。

{{知识点12}}{{CORS预检请求（Preflight）}}
当请求满足以下任一条件时，浏览器会先发送OPTIONS预检请求：使用了GET/HEAD/POST以外的方法；POST请求的Content-Type不是application/x-www-form-urlencoded、multipart/form-data或text/plain；设置了自定义请求头部。预检请求包含Origin、Access-Control-Request-Method和Access-Control-Request-Headers。服务器响应Access-Control-Allow-Origin、Access-Control-Allow-Methods、Access-Control-Allow-Headers，以及可选的Access-Control-Max-Age（预检缓存时间）。预检请求会增加延迟，可通过设置合理的Max-Age减少。

{{知识点13}}{{HTTP缓存机制概述}}
HTTP缓存分为私有缓存（浏览器缓存）和共享缓存（代理缓存、CDN）。缓存策略通过Cache-Control头部控制：no-store完全禁止缓存；no-cache每次使用前需向服务器验证；max-age指定缓存有效期（秒）；private仅允许私有缓存存储；public允许任何缓存存储；must-revalidate过期后必须向服务器验证。前端优化中，静态资源通常使用长期缓存（max-age=31536000）配合文件名哈希实现缓存更新。HTML文件通常使用no-cache确保每次获取最新版本。

{{知识点14}}{{条件请求与ETag}}
条件请求是HTTP缓存验证的核心机制。ETag（Entity Tag）是资源的唯一标识符（如"abc123"）。强ETag要求字节完全匹配，弱ETag（以W/开头）允许语义等价。客户端通过If-None-Match头部携带ETag值发送条件请求：如果资源未变化，服务器返回304 Not Modified（不传输响应体）；如果已变化，返回200和新资源。类似地，Last-Modified配合If-Modified-Since使用时间戳进行验证。ETag比Last-Modified更精确，但Last-Modified在分布式系统中可能更高效。条件请求广泛用于缓存验证和乐观并发控制。

{{知识点15}}{{HTTP Cookie机制}}
Cookie是服务器通过Set-Cookie头部设置、浏览器自动携带在后续请求中的小型数据。Cookie用于会话管理（登录状态）、个性化（用户偏好）和追踪。关键属性：name=value（键值对）、Domain（适用域名）、Path（适用路径）、Expires/Max-Age（过期时间，无则为会话Cookie）、Secure（仅HTTPS传输）、HttpOnly（禁止JavaScript访问，防XSS）、SameSite（Strict/Lax/None，防CSRF）。SameSite=Lax是现代浏览器默认值，允许顶级导航的GET请求携带Cookie。前端应避免将敏感信息存储在Cookie中，推荐使用HttpOnly+Secure+SameSite=Strict。

{{知识点16}}{{HTTP/1.1协议特性}}
HTTP/1.1是使用最广泛的版本之一。关键特性：持久连接（默认启用Keep-Alive，减少TCP握手开销）；管线化（Pipelining，允许不等待响应就发送下一个请求，但实际实现困难且已被浏览器禁用）；Host头部（必需，支持虚拟主机，同一IP托管多个域名）；分块传输编码（Transfer-Encoding: chunked，支持流式传输）；缓存控制（Cache-Control头部）。HTTP/1.1的主要限制：每个连接同时只能处理一个请求（队头阻塞）、头部未压缩、无法设置请求优先级。

{{知识点17}}{{HTTP/2协议核心特性}}
HTTP/2是HTTP协议的重大修订，主要目标是减少延迟和队头阻塞。核心特性：多路复用（Multiplexing）：在单个TCP连接上并行交错传输多个请求/响应，消除了HTTP层的队头阻塞；头部压缩（HPACK）：使用静态/动态表和霍夫曼编码压缩HTTP头部，减少冗余传输；二进制分帧：将HTTP消息分割为更小的帧进行传输，提高解析效率；流优先级：支持请求优先级排序。HTTP/2不改变HTTP语义（方法、状态码、URI、头部字段保持不变），只改变数据的格式化和传输方式。Server Push已被大多数浏览器移除，替代方案为preload和103 Early Hints。

{{知识点18}}{{HTTP/3协议与QUIC}}
HTTP/3是HTTP的最新主要版本，最大变化是使用QUIC协议替代TCP。QUIC基于UDP构建，提供可靠传输。核心优势：消除TCP层队头阻塞（QUIC的流之间相互独立，单个流的丢包不影响其他流）；更快的连接建立（QUIC将传输层握手和TLS握手合并，0-RTT或1-RTT建立连接）；连接迁移（基于连接ID而非IP:端口，网络切换时连接不断）；内置TLS 1.3加密。HTTP/3使用QPACK头部压缩替代HPACK。浏览器通过Alt-Svc头部发现HTTP/3支持。截至2025年，HTTP/3已被所有主流浏览器和主要CDN支持。

{{知识点19}}{{HTTPS与TLS}}
HTTPS是HTTP over TLS（Transport Layer Security），在HTTP和TCP之间加入TLS加密层。TLS提供：加密（保护数据不被窃听）、身份验证（通过证书验证服务器身份）、完整性（防止数据被篡改）。TLS握手过程：客户端发送ClientHello（支持的TLS版本、密码套件、随机数）→服务器返回ServerHello+证书→客户端验证证书并生成预主密钥→双方生成会话密钥。TLS 1.3简化了握手为1-RTT，支持0-RTT恢复。前端需要理解：混合内容（HTTPS页面加载HTTP资源会被阻止）、HSTS头部（强制HTTPS）、证书透明度（CT）。

{{知识点20}}{{HTTP认证机制}}
HTTP认证通过Authorization头部传递凭据。基本认证（Basic）：Base64编码的用户名:密码，需配合HTTPS使用。摘要认证（Digest）：使用哈希算法避免明文传输密码。Bearer Token：OAuth 2.0常用的认证方式，Authorization: Bearer <token>。JWT（JSON Web Token）：自包含的令牌，包含用户信息和签名，常用于前后端分离架构。现代前端更多使用基于Token的认证：登录后获取Token，存储在Cookie（HttpOnly）或localStorage中，通过Authorization头部或Cookie自动携带。OAuth 2.0支持授权码流程、隐式流程等多种模式。

{{知识点21}}{{HTTP内容协商}}
内容协商是客户端和服务器协商最佳响应表示的机制。服务器驱动协商：服务器根据请求头部（Accept、Accept-Language、Accept-Encoding、Accept-Charset）选择最佳响应。例如Accept-Encoding: gzip, br表示客户端支持gzip和Brotli压缩。代理驱动协商：服务器返回300 Multiple Choices让客户端选择。前端常见场景：Accept头部协商响应格式（application/json vs text/html）；Accept-Encoding协商压缩算法（gzip/deflate/br）；Accept-Language协商语言版本。Content-Encoding头部指示响应体使用的压缩方式。

{{知识点22}}{{HTTP范围请求（Range Requests）}}
范围请求允许客户端只请求资源的一部分，通过Range头部指定字节范围（如bytes=0-499）。服务器支持范围请求时返回206 Partial Content和Content-Range头部。应用场景：大文件下载的断点续传（配合If-Range头部）；视频流播放（按需加载片段）；PDF分页加载。服务器通过Accept-Ranges: bytes声明支持范围请求。如果范围无效返回416 Range Not Satisfiable。配合ETag和If-Range头部可以确保续传时资源未发生变化。前端的大文件上传/下载场景中，范围请求是实现断点续传的基础。

{{知识点23}}{{WebSocket协议}}
WebSocket是基于HTTP的全双工通信协议，通过HTTP Upgrade机制建立连接。握手过程：客户端发送Upgrade: websocket和Connection: Upgrade头部的HTTP请求→服务器返回101 Switching Protocols→连接升级为WebSocket。WebSocket特点：持久连接、双向通信、低延迟、减少HTTP头部开销。适用于实时应用（聊天、游戏、实时数据推送、协同编辑）。前端使用new WebSocket('ws://...')或new WebSocket('wss://...')建立连接。注意：WebSocket不受同源策略限制，但仍需服务器验证Origin头部。wss://使用TLS加密。

{{知识点24}}{{Server-Sent Events（SSE）}}
SSE是服务器向客户端推送事件的单向通信机制，基于HTTP协议。服务端设置Content-Type: text/event-stream，通过流式传输发送事件数据。数据格式：每条消息以data:字段开头，两条换行符分隔。支持的字段：event（事件类型）、data（数据内容）、id（事件ID，用于断线重连）、retry（重连间隔）。前端使用EventSource API：const source = new EventSource('/events')，监听message事件。SSE相比WebSocket的优势：自动重连、基于HTTP（无需新协议）、更好的兼容性。适用于通知推送、实时数据流、进度更新等单向数据场景。

{{知识点25}}{{Fetch API}}
Fetch API是现代浏览器提供的HTTP请求接口，替代XMLHttpRequest。基本用法：fetch(url, options)返回Promise<Response>。常用选项：method（请求方法）、headers（请求头）、body（请求体）、mode（cors/no-cors/same-origin）、credentials（omit/same-origin/include）、cache（default/no-store/no-cache等）、signal（AbortController信号，用于取消请求）。Response对象提供：ok（状态码是否200-299）、status、headers、json()/text()/blob()/formData()等方法。Fetch不自动reject HTTP错误状态码（4xx/5xx），需手动检查response.ok。配合async/await使用更优雅。

{{知识点26}}{{Axios与HTTP客户端库}}
Axios是最流行的前端HTTP客户端库，基于Promise。相比Fetch的优势：自动转换JSON数据、自动reject非2xx状态码、支持请求/响应拦截器、支持取消请求（CancelToken或AbortController）、支持上传进度监控、自动设置CSRF Token、更简洁的API。核心功能：axios.get/post/put/delete()、axios.create()创建实例（可配置baseURL、timeout、headers等默认值）、拦截器（请求拦截：添加Token、日志；响应拦截：错误处理、数据转换）。其他HTTP库：ky（基于Fetch的轻量库）、ofetch（Nuxt团队出品）、superagent。选择建议：新项目优先考虑Fetch API+轻量封装。

{{知识点27}}{{HTTP安全头部}}
安全头部是防御Web攻击的重要手段。Content-Security-Policy（CSP）：限制资源加载源，防XSS和数据注入。Strict-Transport-Security（HSTS）：强制浏览器使用HTTPS。X-Content-Type-Options: nosniff：阻止MIME类型嗅探。X-Frame-Options：控制页面能否被iframe嵌入（防点击劫持）。Referrer-Policy：控制Referer头部信息泄露。Permissions-Policy：控制浏览器功能权限（摄像头、麦克风等）。Cross-Origin-Opener-Policy（COOP）：隔离浏览上下文。Cross-Origin-Embedder-Policy（COEP）：控制跨域资源加载。这些头部是现代Web安全的基础。

{{知识点28}}{{HTTP/2 Server Push与103 Early Hints}}
HTTP/2 Server Push允许服务器在客户端请求前主动推送资源，但因实现复杂和缓存效率问题，已被Chrome、Firefox等主要浏览器移除。替代方案：103 Early Hints状态码允许服务器在最终响应前发送Link头部，浏览器可提前预加载资源（如CSS、JS、字体）。使用方式：服务器先返回103 Early Hints + Link: <style.css>; rel=preload头部，再返回最终的200响应。前端优化：使用<link rel="preload">声明关键资源，使用<link rel="preconnect">提前建立连接。这些机制显著提升首屏加载性能。

{{知识点29}}{{HTTP Keep-Alive与连接管理}}
Keep-Alive是HTTP/1.1默认启用的持久连接机制，允许在同一个TCP连接上发送多个请求/响应，避免频繁建立TCP连接的开销。Connection: keep-alive头部表示启用，Connection: close表示关闭。超时和最大请求数通过Keep-Alive: timeout=5, max=100控制。HTTP/2进一步优化：单一连接多路复用，完全消除了建立多个连接的需求。HTTP/3使用QUIC的连接迁移特性，网络切换（如WiFi到4G）时连接不中断。前端开发中，浏览器自动管理连接池，但理解连接机制有助于优化网络性能和调试连接问题。

{{知识点30}}{{HTTP请求生命周期}}
一个完整的HTTP请求生命周期：DNS解析（域名→IP地址）→TCP三次握手（SYN→SYN-ACK→ACK）→TLS握手（HTTPS场景）→发送HTTP请求→服务器处理→返回HTTP响应→浏览器解析（HTML→DOM树、CSS→CSSOM、合并→渲染树→布局→绘制）。前端优化的关键环节：DNS预解析（dns-prefetch）、TCP预连接（preconnect）、减少请求次数（合并文件、Sprite图）、减少请求大小（压缩、Tree Shaking）、使用CDN（就近访问）、HTTP/2多路复用（无需合并文件）。理解请求生命周期是前端性能优化的基础。

{{知识点31}}{{HTTP请求体编码格式}}
前端向服务器发送数据时，Content-Type决定数据编码格式。application/x-www-form-urlencoded：键值对编码（key=value&key2=value2），传统表单默认格式。multipart/form-data：多部分编码，支持文件上传，每个字段有独立的边界分隔。application/json：JSON格式，前后端分离架构中最常用。text/plain：纯文本。FormData API：JavaScript构造表单数据，支持文件上传。fetch发送JSON时需手动设置Content-Type: application/json；发送FormData时浏览器自动设置Content-Type和boundary。大文件上传推荐使用分片上传+FormData。

{{知识点32}}{{HTTP压缩与传输编码}}
HTTP压缩减少传输数据量，提升加载速度。常见压缩算法：gzip（最广泛支持）、deflate、br（Brotli，压缩率更高，现代浏览器均支持）。请求时客户端通过Accept-Encoding: gzip, br声明支持的压缩算法，服务器通过Content-Encoding: gzip/br告知使用的压缩。Transfer-Encoding: chunked允许分块传输，不需要预先知道Content-Length。前端优化：静态资源预压缩（Brotli压缩率优于gzip约15-20%）、动态内容实时压缩、避免对已压缩资源（图片、视频、字体）重复压缩。CDN通常自动处理压缩协商。

{{知识点33}}{{HTTP与前端安全（CSRF/XSS）}}
CSRF（跨站请求伪造）：利用用户已登录状态，诱导用户访问恶意链接发起请求。防御：SameSite Cookie属性（Lax/Strict）、CSRF Token（服务端生成随机Token，请求时验证）、检查Origin/Referer头部、关键操作要求二次确认。XSS（跨站脚本攻击）：注入恶意脚本到页面。防御：CSP头部限制脚本源、HttpOnly Cookie防止JS读取、输出编码/转义、使用textContent而非innerHTML。混合内容：HTTPS页面加载HTTP资源会被浏览器阻止或警告。前端应确保所有资源使用HTTPS，使用protocol-relative URL或严格HTTPS。

{{知识点34}}{{HTTP/2多路复用详解}}
HTTP/2的多路复用是其最核心的改进。在HTTP/1.1中，浏览器对同一域名限制6-8个TCP连接，每个连接一次只能处理一个请求（队头阻塞）。HTTP/2在单个TCP连接上将请求/响应拆分为更小的帧（Frame），通过流（Stream）标识符交错传输。每个流有独立的优先级和流量控制。请求的HEADERS帧和DATA帧可以交叉到达，浏览器按流标识符重组。这消除了HTTP层的队头阻塞，但TCP层的队头阻塞仍然存在（一个TCP包丢失会阻塞所有流），这是HTTP/3改用QUIC的动机。前端不再需要域名分片、文件合并等HTTP/1.1时代的优化手段。

{{知识点35}}{{HTTP Cookie安全最佳实践}}
Cookie安全配置对于Web应用至关重要。HttpOnly：禁止JavaScript通过document.cookie访问，有效防御XSS窃取Cookie。Secure：Cookie仅通过HTTPS加密连接传输。SameSite=Strict：Cookie不随任何跨站请求发送，最强CSRF防护但可能影响用户体验（外部链接不会携带Cookie）。SameSite=Lax：顶级导航的GET请求允许携带Cookie，是现代浏览器默认值，平衡安全和可用性。SameSite=None; Secure：允许跨站发送（需配合Secure），适用于第三方Cookie场景。前端最佳实践：敏感Cookie设置HttpOnly+Secure+SameSite=Lax；非敏感数据使用localStorage/sessionStorage；定期审查Cookie设置。

{{知识点36}}{{HTTP请求重试与错误处理}}
健壮的前端应用需要完善的HTTP错误处理策略。重试策略：可重试的错误（5xx、429、网络超时）应使用指数退避（Exponential Backoff）重试，如1s→2s→4s→8s，添加随机抖动（Jitter）避免雷群效应。不可重试的错误（4xx除429外）不应重试。AbortController：用于取消进行中的请求（组件卸载、用户取消、竞态条件处理）。竞态条件：快速切换页面时，旧请求可能覆盖新数据，使用请求标识或取消旧请求解决。错误分类：网络错误（离线、DNS失败）、超时错误、HTTP错误（状态码）、解析错误（JSON格式错误）。实现统一的错误处理中间件。

{{知识点37}}{{HTTP与前端性能监控}}
Web Performance API提供了HTTP请求性能数据。Navigation Timing API：页面加载各阶段耗时（DNS→TCP→TTFB→内容下载→DOM解析）。Resource Timing API：每个资源请求的详细时间数据（startTime、duration、transferSize、encodedBodySize、decodedBodySize）。PerformanceObserver：监听性能条目（LCP、FID、CLS等Core Web Vitals）。自定义性能监控：记录API请求的响应时间、成功率、错误类型。关键指标：TTFB（Time To First Byte，首字节时间）、FCP（First Contentful Paint）、LCP（Largest Contentful Paint）。使用navigator.sendBeacon()在页面卸载时发送性能数据。

{{知识点38}}{{HTTP/3的0-RTT连接}}
HTTP/3基于QUIC协议支持0-RTT（Zero Round Trip Time）连接建立，极大减少了连接延迟。首次连接需要1-RTT（QUIC合并了传输和TLS握手）；后续连接可使用之前保存的会话票据（Session Ticket），实现0-RTT恢复——客户端在握手完成前就可以发送应用数据。安全考虑：0-RTT数据可能被重放攻击，因此服务器需要实现重放保护机制。0-RTT仅适用于幂等请求（GET），不应用于非幂等操作。前端优化建议：使用Connection: keep-alive和预连接（preconnect）充分利用0-RTT优势，减少冷启动延迟。

{{知识点39}}{{HTTP与Service Worker缓存}}
Service Worker是前端控制HTTP请求的终极方案。通过拦截fetch事件，可以实现：自定义缓存策略（Cache First、Network First、Stale While Revalidate等）、离线支持、请求修改、响应合成。缓存API：caches.open()打开缓存空间、cache.put/add/addAll存储响应、cache.match匹配缓存。与HTTP缓存的区别：Service Worker缓存完全由JavaScript控制，优先级高于HTTP缓存。最佳实践：使用Workbox库简化Service Worker开发；预缓存关键资源（Precache）；运行时缓存动态资源（Runtime Caching）；使用Background Sync处理离线请求。Service Worker + HTTP缓存形成多层缓存架构。

{{知识点40}}{{HTTP请求合并与批处理}}
为减少HTTP请求数量，前端采用多种请求合并策略。GraphQL：将多个查询合并为一个请求，避免REST API的Over-fetching和Under-fetching。批处理API（Batch API）：将多个操作合并为一个HTTP请求，如Google的Batch Request。BFF（Backend for Frontend）层：聚合多个后端服务的数据为一个响应。HTTP/2多路复用的影响：HTTP/2下请求数量不再是主要瓶颈，但仍需考虑服务器处理开销。前端优化策略：关键数据服务端渲染（SSR）减少客户端请求；非关键数据延迟加载（Lazy Load）；使用Intersection Observer实现按需加载；预取可能需要的资源（prefetch/preload）。

{{知识点41}}{{HTTP与CDN加速}}
CDN（Content Delivery Network）通过全球分布式节点加速HTTP资源分发。工作原理：用户请求→DNS解析到最近的CDN节点→节点有缓存则直接返回→无缓存则回源获取并缓存。CDN相关HTTP头部：Cache-Control（控制CDN缓存行为）、Vary（影响缓存键）、CDN-Cache-Control（RFC 9213，CDN专用缓存控制）、Surrogate-Control（特定CDN的缓存控制）、Age（资源在CDN缓存中的年龄）。前端优化：静态资源使用长期缓存+文件名哈希；HTML使用短缓存或no-cache；使用CDN的HTTP/2和HTTP/3支持；利用CDN的边缘计算能力（Edge Computing）。

{{知识点42}}{{HTTP与前端请求拦截器模式}}
请求拦截器是现代前端架构的重要模式。Axios拦截器：请求拦截器（添加Token、日志、修改headers）、响应拦截器（错误处理、数据转换、Token刷新）。Fetch API实现拦截器：通过包装fetch函数或使用中间件模式。常见功能：自动附加Authorization头部、Token过期自动刷新（避免并发请求重复刷新）、请求/响应日志记录、统一错误处理、请求重试、请求取消（AbortController）、请求去重（相同请求复用）、Loading状态管理。Token刷新策略：使用请求队列，刷新期间新请求入队，刷新完成后依次重发。

{{知识点43}}{{HTTP状态码429与限流策略}}
429 Too Many Requests是HTTP标准的限流响应码，表示客户端发送请求过于频繁。服务器通过Retry-After头部告知客户端应等待的秒数。前端限流策略：客户端节流（Throttle）——限制请求发送频率，如每秒最多N个请求；客户端防抖（Debounce）——延迟发送请求直到操作停止。实现方案：令牌桶算法、滑动窗口。配合429处理：解析Retry-After头部，等待指定时间后重试；实现指数退避重试；在UI层显示限流提示。常见场景：API Rate Limiting、登录尝试限制、文件上传频率限制。前端应在请求层统一处理429响应。

{{知识点44}}{{HTTP与GraphQL over HTTP}}
GraphQL通常通过HTTP POST请求传输。请求格式：POST /graphql，Content-Type: application/json，body包含query（查询字符串）、variables（变量）和operationName。响应格式：标准JSON，包含data（查询结果）和errors（错误信息）字段。GraphQL的优势：单次请求获取多资源、精确获取所需字段、强类型Schema。HTTP层面的考虑：可使用GET请求进行查询（查询字符串放在URL中，利于CDN缓存）；Batching——多个查询合并为一个HTTP请求；文件上传使用multipart/form-data规范（graphql-multipart-request-spec）；Subscription使用WebSocket传输。2025年，GraphQL已在大规模应用中证明其价值。

{{知识点45}}{{HTTP协议版本演进总结}}
HTTP协议经历了重大演进：HTTP/0.9（1991年，仅支持GET，无头部，纯文本）→HTTP/1.0（1996年，引入头部、状态码、Content-Type）→HTTP/1.1（1997年，持久连接、Host头部、分块传输、管线化）→HTTP/2（2015年，多路复用、头部压缩HPACK、二进制分帧、服务器推送）→HTTP/3（2022年标准化，基于QUIC/UDP、消除TCP队头阻塞、0-RTT连接、内置TLS 1.3）。每个版本的核心改进：1.1解决连接复用、2解决队头阻塞和头部开销、3解决TCP层队头阻塞和连接延迟。截至2025年，HTTP/2是最广泛使用的版本，HTTP/3快速普及中。

{{知识点46}}{{HTTP与Web安全策略（CORS/CSP/SRI）}}
现代Web安全依赖多个HTTP层面的策略。CORS：控制跨域资源访问，通过Origin和Access-Control-Allow-*头部协商。CSP（Content Security Policy）：通过Content-Security-Policy头部限制页面可加载的资源类型和来源，有效防御XSS。常用指令：default-src、script-src、style-src、img-src、connect-src。SRI（Subresource Integrity）：通过integrity属性验证外部资源（CDN上的JS/CSS）未被篡改，格式为integrity="sha384-..."。COOP/COEP：Cross-Origin-Opener-Policy和Cross-Origin-Embedder-Policy实现跨域隔离，启用SharedArrayBuffer等高级API。这些机制共同构建了纵深防御体系。

{{知识点47}}{{HTTP与RESTful API设计}}
REST（Representational State Transfer）是基于HTTP的API设计风格。核心原则：资源通过URI标识（/users/123）、使用HTTP方法表示操作（GET读取、POST创建、PUT更新、DELETE删除）、无状态通信、统一接口。HTTP状态码映射：GET成功→200、POST创建→201、PUT/PATCH更新→200、DELETE成功→204。最佳实践：使用名词复数表示资源集合（/users）、嵌套资源表示关系（/users/123/orders）、版本控制（/api/v1/users或Accept头部）、分页（?page=1&limit=20）、过滤（?status=active）、排序（?sort=created_at:desc）。HATEOAS：响应中包含相关资源链接。

{{知识点48}}{{HTTP与前端文件上传}}
前端文件上传是常见的HTTP交互场景。基础方案：使用<input type="file">选择文件，通过FormData构造请求，fetch/axios发送POST请求，Content-Type为multipart/form-data。大文件上传：分片上传（将文件切分为多个小块分别上传）+断点续传（记录已上传分片）+并发控制（限制同时上传的分片数）。前端实现：File.slice()切割文件、Web Worker后台计算哈希（用于文件标识和秒传判断）、进度监控（XMLHttpRequest.upload.onprogress或Fetch API+ReadableStream）。上传协议选择：简单场景用multipart/form-data，大文件用分片上传，超大文件可考虑TUS协议（可恢复上传协议）。

{{知识点49}}{{HTTP与前端状态管理}}
HTTP请求与前端状态管理密切相关。TanStack Query（React Query）：自动管理服务端状态的获取、缓存、同步和更新。核心概念：Query（数据获取，useQuery）、Mutation（数据修改，useMutation）、staleTime（数据过期时间）、cacheTime（缓存保留时间）、refetchOnWindowFocus（窗口聚焦时重新获取）。SWR（Stale-While-Revalidate）：Vercel团队的数据获取策略，先返回缓存数据，同时在后台重新验证。与传统状态管理（Redux、Zustand）的区别：TanStack Query/SWR专门管理服务端状态，Redux管理客户端状态。2025年趋势：服务端状态和客户端状态分离管理已成为主流架构模式。

{{知识点50}}{{HTTP与前端监控（APM）}}
前端APM（Application Performance Monitoring）依赖HTTP请求监控。关键指标：请求成功率、平均响应时间、P95/P99延迟、错误率、吞吐量。监控实现：拦截器记录每个请求的开始/结束时间、状态码、响应大小；使用Performance API获取精确的网络时间数据；错误分类统计（网络错误、超时、4xx、5xx）。告警策略：5xx错误率超过阈值、P95延迟超过SLA、429限流频率异常。工具链：Sentry（错误监控）、DataDog（全栈监控）、自建监控（Prometheus+Grafana）。前端应上报：请求URL、方法、状态码、耗时、错误信息、用户标识，用于服务端分析和告警。
