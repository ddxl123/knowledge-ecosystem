# 2025前端微信小程序
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}
微信小程序是一种不需要下载安装即可使用的应用，由微信团队推出。小程序框架提供了简单高效的开发框架和丰富的组件及API，帮助开发者在微信中开发具有原生APP体验的服务。小程序运行在微信客户端内，拥有独立的运行环境和生命周期管理机制。2025年，微信小程序已覆盖社交、电商、工具、游戏等多个领域，日活跃用户超6亿。

{{知识点2}}
小程序的项目结构由主体部分和页面部分组成。主体部分包含三个必需文件：app.js（小程序逻辑）、app.json（小程序公共配置）、app.wxss（公共样式表，可选）。每个页面由四个文件组成：.js（页面逻辑，必需）、.wxml（页面结构，必需）、.json（页面配置，可选）、.wxss（页面样式，可选）。描述页面的四个文件必须具有相同的路径与文件名。

{{知识点3}}
app.json是小程序的全局配置文件，用于设置页面路径、窗口表现、网络超时时间、底部tab等。核心配置项包括：pages（页面路径列表）、window（全局默认窗口表现）、tabBar（底部tab栏）、networkTimeout（各类网络请求的超时时间）、permission（权限声明）等。页面级的.json配置会覆盖app.json中相同字段的全局配置。

{{知识点4}}
小程序采用双线程架构：渲染层（WebView线程）和逻辑层（AppService线程）。渲染层负责页面渲染，逻辑层负责数据处理和业务逻辑。两个线程之间通过JSBridge进行数据通信，数据通过setData方法从逻辑层传递到渲染层。这种架构保证了安全性（逻辑层无法直接操作DOM）但也带来了通信开销。

{{知识点5}}
WXML（WeiXin Markup Language）是框架设计的一套标签语言，结合基础组件和事件系统构建页面结构。WXML的核心能力包括：数据绑定（使用Mustache语法{{}}绑定数据）、列表渲染（wx:for指令循环渲染数组）、条件渲染（wx:if/wx:elif/wx:else）、模板（template定义和使用）、事件绑定（bind和catch前缀绑定事件处理函数）。

{{知识点6}}
WXSS（WeiXin Style Sheets）是小程序的样式语言，具有CSS大部分特性并进行了扩展。主要扩展包括：rpx（responsive pixel）尺寸单位，规定屏幕宽为750rpx，可自适应不同屏幕尺寸（iPhone6上1rpx=0.5px）；样式导入（@import语句导入外联样式表）。支持的选择器包括：.class、#id、element、element,element、::after、::before等。定义在app.wxss中的为全局样式，页面级wxss中定义的为局部样式。

{{知识点7}}
小程序的生命周期分为应用生命周期和页面生命周期。应用生命周期：onLaunch（初始化，全局只触发一次）、onShow（小程序启动或从后台进入前台）、onHide（小程序从前台进入后台）。页面生命周期：onLoad（页面加载）、onShow（页面显示）、onReady（页面初次渲染完成）、onHide（页面隐藏）、onUnload（页面卸载）。理解生命周期对管理数据请求和资源释放至关重要。

{{知识点8}}
小程序的数据绑定和视图更新通过setData方法实现。setData将数据从逻辑层发送到视图层，同时改变对应的this.data的值。调用格式为this.setData({key: value})。注意事项：仅支持可JSON化的数据；单次设置的数据不应超过1024KB；尽量避免过于频繁地调用setData，应合并多次更新为一次调用以减少通信开销。

{{知识点9}}
小程序网络请求必须使用HTTPS协议。核心API包括：wx.request（发起HTTP请求）、wx.uploadFile（上传文件）、wx.downloadFile（下载文件）、wx.connectSocket（WebSocket通信）、wx.createUDPSocket（UDP通信，基础库2.7.0+）、wx.createTCPSocket（TCP连接，基础库2.18.0+）。默认超时时间为60秒，可在app.json中通过networkTimeout配置。wx.request、wx.uploadFile、wx.downloadFile的最大并发限制为10个。

{{知识点10}}
小程序的服务器域名配置要求：域名必须在小程序后台"开发-开发设置-服务器域名"中配置；只支持https和wss协议；不能使用IP地址或localhost；域名必须经过ICP备案；不支持配置父域名。DNS预解析域名最多可添加5个，用于在小程序启动时提前解析业务域名，优化请求速度。

{{知识点11}}
小程序组件系统提供了丰富的内置组件，分为视图容器（view、scroll-view、swiper）、基础内容（text、rich-text、icon）、表单组件（button、input、checkbox、radio、picker、slider、switch）、导航（navigator）、媒体组件（image、video、camera、live-player）、地图（map）、画布（canvas）、开放能力（web-view、ad）等。每个组件都有独立的属性、事件和样式定义。

{{知识点12}}
小程序的事件系统支持冒泡事件和非冒泡事件。使用bind前缀绑定的事件会冒泡，使用catch前缀绑定的事件会阻止冒泡。mut-bind前缀用于互斥事件绑定（基础库2.8.2+）。事件对象包含type、timeStamp、target、currentTarget、detail等属性。dataset用于在组件上自定义数据并在事件处理函数中获取。

{{知识点13}}
小程序路由管理通过wx.navigateTo、wx.redirectTo、wx.switchTab、wx.navigateBack、wx.reLaunch等API实现。navigateTo保留当前页面跳转新页面（可返回）；redirectTo关闭当前页面跳转新页面（不可返回）；switchTab跳转到tabBar页面并关闭其他非tabBar页面；navigateBack返回上一页面；reLaunch关闭所有页面打开新页面。页面栈最多10层。

{{知识点14}}
分包加载是小程序优化包体积的重要机制。通过app.json中的subPackages字段声明分包结构，每个分包包含root（分包根目录）、name（分包别名）、pages（分包页面路径）、independent（是否独立分包）等配置。打包原则：tabBar页面必须在主包内；subPackages配置路径外的目录被打包到主包中；分包之间不能互相require JS文件（使用分包异步化时不受此限制）。

{{知识点15}}
分包预下载允许在进入某个页面时预下载可能需要的分包。通过app.json中的preloadRule字段配置，可指定触发预下载的页面和预下载的分包。独立分包（independent: true）可以独立于主包和其他分包运行，用户进入独立分包页面时无需下载主包。分包大小限制：单个分包/主包大小不能超过2MB，整个小程序所有分包大小不超过20MB。

{{知识点16}}
小程序的存储API提供本地数据缓存能力。wx.setStorageSync/wx.setStorage用于同步/异步存储数据；wx.getStorageSync/wx.getStorage用于获取数据；wx.removeStorageSync/wx.removeStorage用于删除数据；wx.clearStorageSync/wx.clearStorage用于清空所有缓存。单个key允许存储的最大数据大小为1MB，所有数据存储上限为10MB。小程序卸载后数据不会自动清除。

{{知识点17}}
小程序提供了丰富的媒体API。wx.chooseImage选择图片、wx.previewImage预览图片、wx.getImageInfo获取图片信息、wx.chooseVideo选择视频、wx.createCameraContext操作相机、wx.getRecorderManager录音管理、wx.createInnerAudioContext音频播放。wx.saveImageToPhotosAlbum保存图片到相册需要用户授权scope.writePhotosAlbum。

{{知识点18}}
小程序的用户信息获取经历了多次变更。2022年起，getUserProfile和getUserInfo接口已不再支持获取用户头像和昵称信息。当前推荐方案：使用头像昵称填写能力（button组件的open-type="chooseAvatar"获取头像，input组件配合type="nickname"获取昵称），或通过wx.login获取code后由服务端换取openid/unionid进行用户标识。

{{知识点19}}
小程序云开发（CloudBase）提供了Serverless开发模式，无需搭建服务器。核心能力包括：云函数（在云端运行的Node.js函数，可免access_token调用微信开放API）、云数据库（JSON数据库，支持实时推送）、云存储（文件上传下载，自带CDN加速）。云函数通过cloud.callFunction调用，云数据库通过wx.cloud.database()获取引用。

{{知识点20}}
Skyline是微信小程序的新渲染引擎，旨在提供更接近原生的渲染性能。相比WebView架构，Skyline创建独立渲染线程负责Layout、Composite和Paint任务，AppService中划出独立上下文运行JS逻辑。优势包括：界面更不容易被逻辑阻塞、无需为每个页面新建JS引擎实例、页面间可共享资源、无需通过JSBridge进行数据交换。Skyline同时推出了Worklet机制替代WXS用于高性能动画。

{{知识点21}}
小程序的权限管理通过授权机制实现。wx.authorize发起授权请求，wx.getSetting获取用户授权设置，wx.openSetting打开小程序设置页面。常用权限scope包括：scope.userLocation（地理位置）、scope.userLocationBackground（后台定位）、scope.record（录音）、scope.camera（摄像头）、scope.writePhotosAlbum（相册写入）、scope.userInfo（用户信息）等。

{{知识点22}}
小程序的地图组件map支持展示地理位置、路线规划和标注。属性包括：latitude/longitude（中心经纬度）、scale（缩放级别，3-20）、markers（标记点数组）、polyline（路线）、circles（圆）、include-points（缩放视野以包含所有给定坐标点）、show-location（显示带有方向的当前定位点）。wx.getLocation获取当前地理位置，wx.openLocation打开地图选择位置。

{{知识点23}}
小程序的自定义组件机制允许将页面内的功能模块抽象为可复用的自定义组件。每个自定义组件由4个文件组成：.js（组件逻辑，Component构造器）、.json（配置，设置component:true）、.wxml（组件模板）、.wxss（组件样式）。组件支持properties（外部属性）、data（内部数据）、methods（方法）、lifetimes（组件生命周期）、observers（数据监听器）等配置。

{{知识点24}}
小程序支持使用npm安装第三方包。使用流程：在项目根目录执行npm init初始化package.json，然后npm install安装依赖包，最后在微信开发者工具中点击"工具-构建npm"即可使用。注意：小程序不支持直接运行Node.js原生模块，npm包需要支持小程序运行环境。常用的npm包包括：miniprogram-computed（计算属性）、wx-promise-pro（Promise化API）等。

{{知识点25}}
小程序的分享功能通过onShareAppMessage和onShareTimeline实现。onShareAppMessage允许用户将页面分享给好友或群聊，返回对象包含title、path、imageUrl等字段。onShareTimeline（基础库2.11.3+）允许用户将页面分享到朋友圈。页面的.json文件中设置enableShareAppMessage和enableShareTimeline可批量开启分享能力。

{{知识点26}}
小程序的登录流程：前端调用wx.login获取临时code，将code发送到开发者服务器，服务器使用code+appid+appsecret向微信接口换取openid和session_key。为了安全，session_key不应下发到小程序端。推荐使用自定义登录态（如自定义token）在小程序端与服务器之间维持会话。

{{知识点27}}
小程序的支付功能通过wx.requestPayment实现。调用前需要在服务端生成预付单（调用统一下单API），获取支付参数（timeStamp、nonceStr、package、signType、paySign）后传入wx.requestPayment。支付结果以success/fail回调返回，但最终支付结果应以后端异步通知（回调URL）为准。

{{知识点28}}
小程序支持使用WXS（WeiXin Script）在视图层执行脚本。WXS与JavaScript是不同的语言，有自己的语法，运行在渲染层。主要用途：在wxml中进行数据处理和格式化（如日期格式化、金额计算），避免频繁调用setData。WXS文件可以内嵌在wxml中使用<wxs>标签，也可以单独定义.wxs文件通过require引入。

{{知识点29}}
小程序的性能优化策略包括：合理使用分包加载减小主包体积；避免频繁调用setData，合并多次更新；使用WXS替代逻辑层计算减少通信；图片使用WebP格式并合理压缩；使用骨架屏优化加载体验；合理使用缓存减少网络请求；避免在onLoad/onShow中执行过多耗时操作；使用IntersectionObserver实现图片懒加载。

{{知识点30}}
小程序的调试工具：微信开发者工具提供模拟器、调试器（Console/Network/Sources/AppData等面板）、性能分析器、代码质量扫描等能力。真机调试支持在真实设备上运行并查看日志。远程调试允许开发者在开发者工具上远程调试真机上的小程序。实时日志（RealtimeLogManager）可在小程序管理后台查看线上运行时日志。

{{知识点31}}
小程序的更新机制：小程序启动时会自动检查更新。开发者可通过wx.getUpdateManager获取UpdateManager实例，监听onCheckForUpdate（检查更新结果）、onUpdateReady（有新版本可用）、onUpdateFailed（更新失败）事件。当有新版本时，调用applyUpdate强制重启并使用新版本。建议在onUpdateReady时弹窗提示用户重启以应用更新。

{{知识点32}}
小程序支持web-view组件嵌入网页。web-view会自动铺满整个页面并覆盖其他组件，页面路由、下拉刷新等能力受限。嵌入的网页必须在小程序管理后台配置业务域名（需验证域名所有权）。网页通过JSSDK 1.3.0+与小程序进行通信，可调用小程序API（如wx.miniProgram.navigateTo）。每个页面只能有一个web-view组件。

{{知识点33}}
小程序的开放能力包括：客服消息（button的open-type="contact"）、获取手机号（button的open-type="getPhoneNumber"，需企业认证）、打开APP（button的open-type="launchApp"）、打开生活号/公众号（open-type="openPublicProfile"）、打开设置页（open-type="openSetting"）、意见反馈（open-type="feedback"）等。不同open-type需要对应的小程序资质和权限。

{{知识点34}}
小程序的状态管理方案：官方推荐使用globalData在app.js中存储全局数据。社区流行的方案包括：MobX-miniprogram（响应式状态管理）、Westore（腾讯开源，基于diff更新）、Taro Redux（跨端框架）。对于简单场景，可以通过页面间通信（EventChannel、页面栈getCurrentPages）或本地存储实现状态共享。

{{知识点35}}
小程序的跨端开发框架：Taro（京东开源，支持React/Vue语法编译到多端）、uni-app（DCloud开源，支持Vue语法，覆盖小程序/H5/App）、mpvue（美团开源，基于Vue）、Chameleon（滴滴开源，类Vue语法）。这些框架允许开发者使用Web技术栈编写一套代码，编译到微信、支付宝、百度等多个小程序平台。

{{知识点36}}
小程序的无障碍访问（Accessibility）支持：基础库2.9.0+开始支持aria-*属性，为视障用户提供语音朗读能力。常用属性包括aria-label（元素描述）、aria-role（元素角色）、aria-hidden（隐藏元素）、aria-checked（选中状态）等。开发者应在图片、图标等非文本元素上添加aria-label描述，确保小程序对辅助技术的友好性。

{{知识点37}}
小程序的隐私保护机制（2023年起强化）：使用用户隐私数据前需弹出隐私弹窗获取用户同意。通过wx.getPrivacySetting检查用户隐私授权状态，在页面json中配置__usePrivacyCheck__:true开启隐私保护。涉及的隐私API包括：获取位置、相机、相册、麦克风、剪贴板、手机号等。隐私协议需在小程序管理后台配置。

{{知识点38}}
小程序的微信云托管（WX Cloud Run）是一种容器化的Serverless服务。无需管理服务器，支持自动扩缩容。小程序内通过callContainer（HTTPS调用）和connectContainer（WebSocket通信）直接访问云托管服务，无需配置服务器域名。支持多种编程语言（Node.js、Python、Go、Java等），适合复杂后端业务场景。

{{知识点39}}
小程序的骨架屏（Skeleton Screen）是一种优化加载体验的技术。在页面数据加载完成前先展示页面的大致结构（灰色占位块），给用户即时反馈的感知。微信开发者工具提供了自动生成骨架屏代码的能力（详情-生成骨架屏），会基于当前页面的WXML和WXSS自动生成骨架屏模板。

{{知识点40}}
小程序的插件机制允许开发者将功能封装为插件供其他小程序使用。插件拥有独立的AppService和渲染环境，不能独立运行。使用插件需要在app.json中声明plugins字段，指定插件的appid和版本号。插件可以提供自定义组件、页面和JS接口供宿主小程序调用。插件需要在小程序管理后台提交审核后方可被其他小程序使用。
