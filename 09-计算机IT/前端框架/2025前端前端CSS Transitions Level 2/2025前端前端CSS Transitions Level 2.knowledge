# 2025前端前端CSS Transitions Level 2
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}{{CSS Transitions Level 2 是什么？}}
CSS Transitions Level 2 是 W3C CSS 工作组制定的增量规范（delta specification），基于 Level 1 扩展而来。它定义了如何在 CSS 属性值之间创建渐进式过渡效果，并新增了对离散属性动画、起始样式定义等高级功能的支持。当前状态为编辑草案（Editor's Draft），规范地址为 drafts.csswg.org/css-transitions-2/。

{{知识点2}}{{CSS Transitions Level 2 与 Level 1 的核心区别}}
Level 2 是增量规范，仅包含与 Level 1 的差异部分。核心新增包括：transition-behavior 属性（允许离散属性过渡）、@starting-style 规则（定义元素首次渲染的起始样式）、与 Web Animations API 的深度集成、以及对 overlay 属性过渡的支持。

{{知识点3}}{{transition-behavior 属性}}
transition-behavior 是 Level 2 新增的属性，用于控制是否对离散动画属性启动过渡。取值为 normal（默认，不启动离散过渡）或 allow-discrete（允许离散属性也参与过渡）。这使得 display、visibility 等离散属性也能实现过渡效果。

{{知识点4}}{{transition-behavior 的 allow-discrete 值}}
当设置 transition-behavior: allow-discrete 时，浏览器会对离散动画属性也启动过渡。离散属性值通常在过渡进度 50% 时进行翻转切换。但 display 属性有特殊处理：从 none 到可见值时在 0% 翻转（立即可见），从可见值到 none 时在 100% 翻转（保持可见到动画结束）。

{{知识点5}}{{transition 简写中的 transition-behavior}}
transition-behavior 可以包含在 transition 简写属性中。例如 transition: all 0.7s allow-discrete 等价于分别设置 transition-property、transition-duration 和 transition-behavior。在简写中 allow-discrete 对普通可动画属性没有影响，仅作用于离散属性。

{{知识点6}}{{@starting-style 规则概述}}
@starting-style 是 Level 2 引入的 at 规则，用于定义元素首次渲染时的起始样式。默认情况下 CSS 过渡不会在元素初始样式更新或 display 从 none 变化时触发，@starting-style 允许开发者覆盖这一默认行为，为没有先前状态的元素提供过渡起点。

{{知识点7}}{{@starting-style 的两种使用方式}}
@starting-style 可以作为独立块使用，包含完整的选择器和规则集；也可以嵌套在现有规则集中，仅包含属性声明。独立方式：@starting-style { [popover]:popover-open { opacity: 0; } }。嵌套方式：在选择器内使用 @starting-style { opacity: 0; }。

{{知识点8}}{{@starting-style 与 CSS Nesting}}
@starting-style 支持 CSS 嵌套语法，可以在选择器内部直接嵌套使用。这使得起始样式的定义更加简洁直观，无需重复书写选择器。但注意伪元素不能使用嵌套选择器（&），需要使用独立的 @starting-style 块。

{{知识点9}}{{@starting-style 的层叠规则}}
@starting-style 内的规则与原始规则具有相同的特异性（specificity）。为确保起始样式生效，应将 @starting-style 放在原始规则之后。如果放在原始规则之前，原始样式会覆盖起始样式。@starting-style 规则不会应用到 after-change style 中。

{{知识点10}}{{@starting-style 的三个样式状态}}
使用 @starting-style 时存在三个样式状态：起始样式状态（starting-style）、过渡后的状态（transitioned）、默认状态（default）。元素首次渲染时从起始样式过渡到过渡状态；之后移除类时，从过渡状态过渡回默认状态（而非起始样式）。这意味着进入和退出动画可以不同。

{{知识点11}}{{@starting-style 与 Popover 动画}}
@starting-style 特别适用于 Popover API 元素的进入/退出动画。Popover 在显示时被提升到 top layer 并从 display:none 变为可见，@starting-style 为这种场景提供了起始样式定义，配合 transition-behavior: allow-discrete 可实现完整的进入退出动画。

{{知识点12}}{{overlay 属性}}
overlay 是 Level 2 相关的 CSS 属性，控制出现在 top layer 中的元素是否实际渲染在 top layer。取值为 auto（在 top layer 渲染）或 none（不在 top layer 渲染）。overlay 只能由浏览器设置，开发者样式不能直接改变其值，但可以将其加入过渡属性列表。

{{知识点13}}{{overlay 在过渡中的作用}}
将 overlay 加入 transition-property 列表并设置 transition-behavior: allow-discrete，可以延迟元素从 top layer 的移除，使其在过渡动画完成后再移除。这避免了元素过早从 top layer 消失导致动画不流畅的问题。

{{知识点14}}{{display 属性的过渡}}
Level 2 允许 display 属性参与过渡。通过设置 transition-property: display 和 transition-behavior: allow-discrete，可以实现 display 从 none 到 block 的渐进切换。从 none 到可见值时，display 在动画 0% 时翻转确保内容全程可见；从可见值到 none 时在 100% 翻转。

{{知识点15}}{{离散属性动画的翻转行为}}
离散动画属性在过渡中通常在 50% 进度处进行值翻转。但 display 和 content-visibility 有特殊例外：过渡到 none/hidden 时在 100% 翻转，过渡自 none/hidden 时在 0% 翻转。这种设计确保过渡内容在整个动画持续期间都是可见的。

{{知识点16}}{{transitionable 的定义扩展}}
Level 2 扩展了 transitionable 的定义。属性值在以下情况下可过渡：动画类型既不是 not animatable 也不是 discrete，或者 transition-behavior 为 allow-discrete 且动画类型为 discrete。这大大扩展了可过渡属性的范围。

{{知识点17}}{{CSSTransition 接口}}
CSS Transitions Level 2 定义了 CSSTransition Web API 接口，继承自 Animation 接口。它包含 transitionProperty 属性用于获取过渡的属性名。开发者可以通过 JavaScript 监听和控制 CSS 过渡的生命周期。

{{知识点18}}{{TransitionEvent 事件对象}}
TransitionEvent 接口提供三个关键属性：propertyName（过渡的 CSS 属性名）、elapsedTime（过渡已过去的时间，秒）、pseudoElement（触发过渡的伪元素名称，如 ::before）。这些属性在事件处理中用于获取过渡的详细信息。

{{知识点19}}{{过渡事件类型}}
Level 2 定义了四种过渡事件：transitionrun（过渡首次创建时触发）、transitionstart（过渡延迟结束后开始实际动画时触发）、transitionend（过渡完成时触发）、transitioncancel（过渡被取消时触发）。这提供了完整的过渡生命周期控制。

{{知识点20}}{{transitioncancel 事件}}
当过渡被取消时（例如属性值在过渡完成前再次改变），会触发 transitioncancel 事件。在 Level 2 中，当元素不再连接到 DOM 或不再被渲染时，浏览器必须取消该元素上所有正在运行的过渡。

{{知识点21}}{{过渡的所有权元素（Owning Element）}}
过渡的所有权元素是指应用了 transition-property 属性并生成该过渡的元素或伪元素。过渡可以从其所有权元素解除关联，此时该过渡不再拥有所有权元素。正在运行的过渡集合仅包含具有所有权元素的过渡。

{{知识点22}}{{过渡的取消与解除关联}}
当浏览器需要取消过渡时，必须先解除过渡与所有权元素的关联，然后对过渡运行动画取消程序。但通过 Web Animations API 调用 cancel() 方法不会导致过渡解除与所有权元素的关联。

{{知识点23}}{{中断过渡的快速反转（Reversing Shortening Factor）}}
Level 2 明确了反转缩短因子和反转调整起始值与过渡本身关联，而非与其动画效果关联。这意味着即使通过 Web Animations API 更新或替换了过渡的动画效果，过渡在产生反转效果时仍使用相同的反转值。

{{知识点24}}{{当前过渡生成（Current Transition Generation）}}
每个顶层浏览上下文关联一个当前过渡生成计数器，在每次样式更改事件时递增。每次生成新过渡时，当前值（已递增）作为该过渡的过渡生成值存储。这用于跟踪和管理过渡的时序。

{{知识点25}}{{interpolate-size 属性}}
interpolate-size 是与过渡相关的实验性属性，允许在 length-percentage 值和固有尺寸值（如 auto、min-content、max-content、fit-content）之间进行插值。设置为 allow-keywords 可启用此功能，是实现 height: auto 过渡的关键。

{{知识点26}}{{interpolate-size 的继承性}}
interpolate-size 是可继承属性，初始值为 numeric-only。在 :root 上设置 interpolate-size: allow-keywords 可为整个文档启用固有尺寸动画。也可以限制在特定容器元素上设置，仅对该容器及其后代生效。

{{知识点27}}{{calc-size() 函数}}
calc-size() 是 CSS Values and Units Level 5 中的函数，其返回值可以进行插值。在属性值中包含 calc-size() 会自动对该选择器应用 interpolate-size: allow-keywords。但 interpolate-size 因其继承性通常是更优的方案。

{{知识点28}}{{transition-timing-function 的应用层级}}
与 CSS Animations 不同，CSS Transitions 的 timing function 应用于动画效果（animation effect）而非单个关键帧。这允许它在计算反转缩短因子时使用的 transformed progress 中得到反映，确保过渡反转时的平滑性。

{{知识点29}}{{transition-delay 与 start delay}}
transition-delay 属性指定过渡关联动画效果的 start delay（启动延迟）。在过渡被创建后，会等待指定的延迟时间后才开始实际的属性值变化动画。transitionrun 事件在延迟开始时触发，transitionstart 在延迟结束后触发。

{{知识点30}}{{before-change style 与 @starting-style 的关系}}
在 Level 1 中，过渡只能在有已定义 before-change style 的元素上启动。Level 2 通过 @starting-style 定义 starting style，当元素没有 before-change style 时（如新插入 DOM 或从 display:none 变化），使用 starting style 代替 before-change style 来比较和启动过渡。

{{知识点31}}{{starting style 的继承规则}}
starting style 从父元素的 after-change style 继承，与 after-change style 的继承方式相同。这确保了在 @starting-style 中未显式声明的属性能够正确继承父元素的样式，维持样式计算的一致性。

{{知识点32}}{{不渲染元素的过渡取消}}
Level 2 明确要求：当元素不再连接到 DOM 或不再被渲染（being rendered）时，浏览器必须取消该元素上所有正在运行的过渡，并从已完成过渡集合中移除相关过渡。这是对 Level 1 行为的补充说明。

{{知识点33}}{{transition-property 展开与 shorthand 属性}}
transition-property 可以指定简写属性和 all 关键字，但过渡是为每个可过渡的长手子属性单独生成的。过渡的展开过渡属性名称是为其生成过渡的长手子属性名称（如 border-left-width 而非 border）。

{{知识点34}}{{Popover 进入/退出动画的完整模式}}
实现 Popover 完整动画需要四层配合：① transition-behavior: allow-discrete 允许离散属性过渡；② @starting-style 定义进入动画的起始值；③ overlay 过渡延迟 top layer 移除；④ display 过渡确保退出动画可见。这四者协同工作实现平滑的进入退出效果。

{{知识点35}}{{Dialog 元素的过渡}}
与 Popover 类似，HTML dialog 元素也可以使用 CSS Transitions Level 2 的特性实现进入/退出动画。使用 :open 伪类选择器匹配打开状态的 dialog，配合 @starting-style 和 transition-behavior: allow-discrete 实现动画效果。

{{知识点36}}{{::backdrop 伪元素的过渡}}
top layer 元素的 ::backdrop 伪元素也可以参与过渡。例如 Popover 的背景遮罩可以使用 background-color 过渡实现淡入淡出效果。注意 ::backdrop 不能使用 CSS 嵌套选择器（&），需要使用独立的 @starting-style 块。

{{知识点37}}{{transition 与 animation 的选择}}
@starting-style 仅适用于 CSS transitions，使用 CSS animations 实现类似效果时不需要 @starting-style。Transitions 适合简单的状态变化动画（A→B），Animations 适合复杂的多关键帧动画。Level 2 大大增强了 transitions 的能力，缩小了两者的差距。

{{知识点38}}{{浏览器兼容性与渐进增强}}
transition-behavior 的推荐渐进增强写法是先写不含 allow-discrete 的过渡声明，再写包含 allow-discrete 的声明。这样不支持的浏览器会忽略第二条，第一条作为回退方案确保普通属性仍能过渡。@starting-style 和 interpolate-size 需要检查浏览器兼容性。

{{知识点39}}{{CSS Transitions Level 2 与 Web Animations API 的关系}}
Level 2 明确了 CSS Transitions 与 Web Animations API 的集成细节。过渡的动画效果属性（duration、delay、timing function）映射到 Web Animations 的 iteration duration、start delay 和 timing function。CSSTransition 接口继承自 Animation 接口。

{{知识点40}}{{content-visibility: hidden 的过渡特殊行为}}
与 display: none 类似，content-visibility: hidden 在离散动画中也有特殊处理：过渡到 hidden 时在 100% 翻转，过渡自 hidden 时在 0% 翻转。这确保使用 content-visibility 的元素在过渡期间保持内容可见，与 display 的行为一致。
