# 2025前端Micro Frontends
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}{{微前端定义}}
微前端(Micro Frontends)是一种架构风格，将独立交付的前端应用组合成一个更大的整体。该概念最早由ThoughtWorks在2016年底的技术雷达中提出，将微服务的理念延伸到前端领域。核心思想是将网站或Web应用视为由独立团队拥有的功能组合，每个团队负责一个特定的业务领域，端到端地开发其功能（从数据库到用户界面）。

{{知识点2}}{{微前端解决的核心问题}}
微前端主要解决前端单体(Frontend Monolith)问题：随着应用规模增长和团队人数增加，代码库变得越来越难以维护；技术栈被锁定无法渐进升级；多个团队在同一代码库上工作时互相冲突；部署需要整体发布。微前端通过拆分使各部分可独立开发、测试和部署。

{{知识点3}}{{微前端的核心原则}}
微前端架构遵循五大核心原则：(1)技术无关——每个团队可以选择和升级自己的技术栈；(2)隔离团队代码——不共享运行时，构建独立自包含的应用；(3)建立团队前缀——命名约定隔离CSS、事件、LocalStorage等；(4)偏好原生浏览器特性——使用浏览器事件而非自定义PubSub系统；(5)构建弹性站点——即使JavaScript失败，功能仍然可用。

{{知识点4}}{{微前端与微服务的关系}}
微前端是微服务理念在前端的延伸。微服务将后端拆分为独立服务，微前端则将前端拆分为独立子应用。两者共享相同的核心价值观：独立开发部署、技术多样性、团队自治、增量升级。区别在于微前端运行在浏览器端，需要处理DOM隔离、样式冲突、路由协调等浏览器特有问题。

{{知识点5}}{{微前端的集成方式}}
微前端有三种主要集成方式：(1)构建时集成——通过NPM包发布子应用，主应用作为依赖安装（缺点是需要重新构建部署）；(2)服务端集成——如SSR中通过SSI/ESI在服务端组装页面片段；(3)运行时集成——通过JavaScript在浏览器端动态加载和挂载子应用（最灵活，是主流方案）。

{{知识点6}}{{single-spa框架概述}}
single-spa是最流行的微前端框架之一，是一个用于将多个JavaScript微前端组合在一起的框架。它允许在同一页面上使用多个框架（React、Angular、Vue等）且无需页面刷新，支持独立部署、懒加载以改善初始加载时间。核心概念包括root config（根配置）、applications（应用）和parcels（包裹）。

{{知识点7}}{{single-spa应用生命周期}}
single-spa中每个应用必须实现三个生命周期函数：bootstrap（引导——应用首次加载时的初始化）、mount（挂载——将DOM元素渲染到页面上）、unmount（卸载——从DOM中移除元素并清理事件监听器等）。应用通过registerApplication注册，需提供名称、加载函数和激活条件函数。

{{知识点8}}{{single-spa Root Config}}
Root Config是single-spa的入口，负责渲染HTML页面和注册应用。每个应用注册时需要三个要素：(1)应用名称；(2)加载应用代码的函数（通常使用SystemJS或import maps动态加载）；(3)确定应用何时激活/失活的函数（通常是路由匹配规则）。推荐使用create-single-spa脚手架快速创建。

{{知识点9}}{{single-spa Parcels}}
Parcel是single-spa中独立于路由的应用组件，可以在任何框架中挂载和使用。与Application不同，Parcel不由路由激活，而是由代码手动控制挂载/卸载。Parcel可以实现跨框架的组件共享，例如用React写的组件可以在Vue应用中使用。

{{知识点10}}{{qiankun框架概述}}
qiankun是基于single-spa的微前端实现库，由蚂蚁金服孵化。它旨在更轻松地构建生产级微前端架构系统。qiankun在蚂蚁内部已服务超过2000个线上应用。核心设计哲学是"简单"和"解耦"——对用户来说像jQuery一样简单，同时确保子应用真正具备独立开发和运行的能力。

{{知识点11}}{{qiankun核心特性}}
qiankun提供以下核心特性：(1)基于single-spa，提供更开箱即用的API；(2)技术无关——任何JS框架均可接入；(3)HTML Entry接入模式——像使用iframe一样简单地接入子应用；(4)样式隔离——确保样式互不干扰（Shadow DOM或scoped CSS）；(5)JS沙箱——确保全局变量/事件不冲突；(6)预加载——在浏览器空闲时预加载未打开的子应用资源。

{{知识点12}}{{qiankun的JS沙箱机制}}
qiankun提供三种JS沙箱模式：(1)SnapshotSandbox——快照沙箱，不支持Proxy的降级方案，切换应用时保存/恢复window快照（性能较差）；(2)ProxySandbox——代理沙箱，基于Proxy实现，多个子应用可以同时存在但互不影响；(3)LegacySandbox——单例沙箱，基于Proxy但同一时间只能激活一个子应用。沙箱解决了全局变量污染和事件监听冲突问题。

{{知识点13}}{{qiankun的样式隔离}}
qiankun提供两种样式隔离方案：(1)严格模式(StrictStyleIsolation)——基于Shadow DOM实现，子应用样式完全隔离但可能导致弹窗等逃逸Shadow DOM的元素样式丢失；(2)宽松模式(ExperimentalStyleIsolation)——通过CSS选择器加前缀实现作用域隔离，不使用Shadow DOM，兼容性更好但隔离性稍弱。

{{知识点14}}{{为什么不用iframe}}
iframe虽然天然提供隔离，但作为微前端方案有严重缺陷：(1)URL状态不同步——浏览器刷新时iframe状态丢失；(2)UI不同步——iframe内的弹窗、下拉框等无法覆盖父页面；(3)通信困难——只能通过postMessage，效率低且繁琐；(4)性能开销——每个iframe有独立的文档上下文，内存和加载开销大；(5)SEO不友好。

{{知识点15}}{{Webpack Module Federation概述}}
Module Federation是Webpack 5引入的革命性特性，是一种JavaScript应用去中心化的架构模式。它允许多个独立构建的应用在运行时共享代码和依赖。核心概念包括：Container（容器，每个独立构建就是一个容器）、Local Module（本地模块）、Remote Module（远程模块，从其他容器异步加载）、Shared Module（共享模块，如React等公共库）。

{{知识点16}}{{Module Federation 2.0}}
Module Federation 2.0在Webpack 5内置版本基础上增加了：(1)动态类型提示——TypeScript类型支持；(2)Manifest——清单文件描述远程模块信息；(3)Federation Runtime——独立运行时，不依赖Webpack；(4)运行时插件系统——可扩展运行时行为；(5)Chrome DevTools支持；(6)同时支持Rspack和Webpack构建工具。

{{知识点17}}{{Module Federation配置详解}}
Module Federation通过ModuleFederationPlugin配置，主要属性包括：name（容器名称）、filename（远程入口文件名如remoteEntry.js）、exposes（暴露给其他容器的模块）、remotes（引用的远程容器）、shared（共享依赖，可配置singleton、requiredVersion等）。多个构建可以互相暴露和消费模块，形成统一应用。

{{知识点18}}{{Module Federation动态远程容器}}
Module Federation支持在运行时动态连接远程容器。通过container.init()和container.get()两个方法实现：先调用__webpack_init_sharing__初始化共享作用域，再调用目标容器的init方法注入共享模块，最后通过get方法获取具体模块。这允许动态加载A/B测试、运行时决定使用哪个版本的联邦模块等场景。

{{知识点19}}{{Web Components与微前端}}
Web Components（Custom Elements、Shadow DOM、HTML Templates）是微前端的天然集成方案。每个团队将组件封装为Custom Element（如<order-minicart></order-minicart>），DOM规范即组件的公共API。其他团队只需与DOM交互，无需了解实现细节。Shadow DOM提供样式和DOM的天然隔离。但Web Components在服务端渲染、路由等方面需要额外支持。

{{知识点20}}{{Module Federation vs single-spa}}
两者的核心区别：(1)single-spa是微前端编排框架，关注应用的注册、加载和生命周期管理；Module Federation是构建工具特性，关注模块级别的代码共享和远程加载。(2)single-spa需要额外的模块加载器（如SystemJS）；Module Federation由Webpack/Rspack原生支持。(3)Module Federation更适合共享组件库和依赖；single-spa更适合整应用级别的编排。两者可以结合使用。

{{知识点21}}{{微前端的路由管理}}
微前端路由分两层：(1)主应用路由——决定加载和激活哪个子应用（由single-spa等框架管理，基于URL前缀匹配）；(2)子应用路由——子应用内部的页面路由（由各子应用自己的路由库管理，如react-router、vue-router）。关键挑战是路由同步——浏览器URL、主应用路由状态、子应用路由状态三者需要保持一致。

{{知识点22}}{{微前端的应用间通信}}
微前端中子应用间通信的常见方案：(1)URL参数/查询字符串——最简单，适合传递简单状态；(2)CustomEvent浏览器事件——使用原生事件系统，各子应用监听/触发自定义事件；(3)全局状态管理——如Redux Store共享，但违反隔离原则；(4)props传递——主应用通过渲染函数向子应用传入回调和数据；(5)BroadcastChannel API——同源标签页间通信；(6)自定义事件总线（EventEmitter）。

{{知识点23}}{{微前端的CSS样式隔离方案}}
样式隔离是微前端的重要挑战，解决方案包括：(1)CSS Modules——编译时生成唯一类名；(2)Shadow DOM——浏览器原生隔离但兼容性有限；(3)BEM命名约定——人工约定命名空间；(4)CSS-in-JS——如styled-components，运行时隔离；(5)PostCSS插件自动加前缀；(6)iframe天然隔离但限制多。qiankun等框架内置了样式隔离方案。

{{知识点24}}{{微前端的共享依赖策略}}
公共依赖（如React、Vue、lodash）的处理策略：(1)外部化+CDN——将公共库标记为external，通过CDN加载；(2)import maps——浏览器原生的模块映射机制；(3)Module Federation shared——Webpack原生支持依赖共享，可配置singleton确保只加载一个版本；(4)NPM包+去重——通过包管理器的版本提升机制去重。关键是平衡包体积和版本兼容性。

{{知识点25}}{{微前端的性能优化}}
微前端性能优化策略：(1)预加载/预取——在浏览器空闲时预加载其他子应用资源（qiankun支持prefetch）；(2)懒加载——按需加载子应用，初始只加载当前路由需要的；(3)共享依赖——避免重复加载React等大型库；(4)沙箱按需创建——只在子应用激活时创建沙箱；(5)资源缓存策略——合理配置缓存头和Service Worker；(6)按功能拆包——每个子应用只打包自己需要的代码。

{{知识点26}}{{微前端的部署策略}}
微前端的独立部署特性：(1)每个子应用独立构建和部署，更新后主应用自动获取最新版本；(2)需要版本管理机制——通过remoteEntry.js等入口文件的缓存策略控制更新；(3)灰度发布——可对单个子应用进行灰度；(4)回滚简单——只需回滚特定子应用而非整个前端；(5)CI/CD流程——每个子应用有独立的构建流水线；(6)CDN部署——静态资源部署到CDN，入口文件需注意缓存策略。

{{知识点27}}{{微前端的监控与错误处理}}
微前端架构下的监控要点：(1)全局错误边界——主应用需捕获子应用渲染错误，提供降级UI；(2)子应用加载失败处理——网络超时、资源404等情况的兜底方案；(3)性能监控——分别监控各子应用的加载时间、渲染性能；(4)日志聚合——跨子应用的日志需要统一收集和关联；(5)健康检查——子应用可用性探测。

{{知识点28}}{{微前端与SSR服务端渲染}}
微前端的服务端渲染挑战：(1)各子应用可能使用不同框架，SSR方案各异；(2)页面组装需要在服务端完成HTML拼接；(3)数据获取需要协调多个子应用的异步数据；(4)streaming SSR可逐步输出各子应用HTML以提升首屏性能。single-spa提供了Layout Engine支持SSR场景，qiankun主要面向CSR。

{{知识点29}}{{微前端与Monorepo}}
微前端项目常见的代码组织方式：(1)多仓库——每个子应用独立仓库，完全自治但管理成本高；(2)Monorepo——所有子应用在一个仓库中，使用Turborepo/Nx/Lerna等工具管理，便于共享代码和统一配置，但需要CI/CD支持部分构建。推荐小型团队使用Monorepo，大型组织使用多仓库或混合模式。

{{知识点30}}{{微前端的安全考量}}
微前端安全注意事项：(1)跨子应用的XSS风险——子应用间通信的数据需要验证和消毒；(2)沙箱逃逸——JS沙箱不是安全边界，恶意代码可能突破沙箱；(3)CORS策略——跨域加载子应用资源需要正确配置CORS；(4)CSP策略——Content-Security-Policy需要允许动态加载脚本；(5)依赖安全——共享依赖的漏洞影响所有子应用。

{{知识点31}}{{微前端的测试策略}}
微前端测试分为多层：(1)单元测试——各子应用内部的组件/函数测试，与传统方式相同；(2)集成测试——测试子应用在主应用中的挂载、卸载、通信；(3)端到端测试——使用Cypress/Playwright测试完整的用户流程；(4)契约测试——验证子应用暴露的API/事件是否符合约定；(5)视觉回归测试——确保样式隔离后各子应用UI不受影响。

{{知识点32}}{{微前端的增量迁移策略}}
将现有单体应用迁移到微前端的渐进策略：(1)绞杀者模式(Strangler Pattern)——逐步用新微前端替换旧应用的功能，新旧并存过渡；(2)先拆分新功能——新功能用微前端开发，旧功能保持原样；(3)识别自然边界——按业务模块而非技术模块拆分；(4)逐步建立共享基础设施——先搭建主应用框架、通信机制、部署流程，再逐步迁移子应用。

{{知识点33}}{{2025年微前端新趋势}}
2025年微前端领域的发展趋势：(1)Module Federation 2.0成为主流——原生TypeScript支持、运行时独立、跨构建工具；(2)Rspack/Buildpack等新一代构建工具原生支持Module Federation；(3)Import Maps浏览器原生支持成熟——减少对SystemJS等polyfill的依赖；(4)Server Components与微前端的融合——React Server Components带来的服务端组件化思维影响微前端架构；(5)WASM微前端——WebAssembly用于非JS子应用的集成。

{{知识点34}}{{微前端适用场景与不适用场景}}
适用场景：(1)大型企业应用——多团队协作、技术栈多样；(2)遗留系统渐进重构——从jQuery/AngularJS逐步迁移到React/Vue；(3)平台型产品——需要支持插件化/可扩展架构；(4)多品牌/白标产品——同一平台服务不同品牌定制。不适用场景：(1)小型应用——增加架构复杂度得不偿失；(2)单一团队维护——独立部署的优势无法体现；(3)对性能极度敏感——额外的加载和沙箱开销不可避免。

{{知识点35}}{{Garfish微前端框架}}
Garfish是字节跳动开源的微前端框架，提供应用编排、JS沙箱、样式隔离、预加载等能力。特点包括：(1)支持多应用同时渲染；(2)基于Proxy的沙箱实现；(3)支持CJS和ESM两种子应用接入方式；(4)内置路由驱动能力；(5)完善的插件系统。与qiankun相比，Garfish在多应用共存和ESM支持上更有优势。

{{知识点36}}{{micro-app框架}}
micro-app是京东开源的微前端框架，采用Web Component + iframe沙箱的方案。核心特点：(1)接入像iframe一样简单——只需一个HTML标签<micro-app url="xxx">即可嵌入子应用；(2)基于CustomElement实现，对主应用无框架要求；(3)使用iframe作为JS沙箱，隔离性强且无Proxy兼容性问题；(4)支持vite等ESM构建工具的子应用。

{{知识点37}}{{Import Maps与微前端}}
Import Maps是浏览器原生的模块映射机制（<script type="importmap">），允许在HTML中声明模块标识符到URL的映射。在微前端中，Import Maps可用于：(1)共享依赖——声明react、vue等公共库的CDN地址；(2)替代SystemJS——减少额外的模块加载器依赖；(3)环境无关的模块解析——统一开发和生产环境的模块路径。目前主流浏览器均已支持。

{{知识点38}}{{微前端的状态管理}}
微前端架构下状态管理的层次：(1)局部状态——各子应用内部使用自己的状态管理（Redux、Vuex、Zustand等）；(2)共享状态——跨子应用需要共享的用户信息、权限等全局状态；(3)状态同步方案——通过CustomEvent广播变化、主应用持有全局Store向下传递、或使用BroadcastChannel等API。原则是尽量减少共享状态，保持子应用独立性。

{{知识点39}}{{微前端与Vite}}
Vite作为新一代构建工具，其ESM原生特性对微前端的影响：(1)Module Federation在Vite中通过@originjs/vite-plugin-federation插件支持；(2)Vite的快速HMR提升微前端开发体验；(3)Vite构建的子应用接入qiankun等框架时需要处理ESM格式的兼容；(4)Vite Dev Server的模块代理能力可用于本地开发时的跨应用调试。Vite生态的微前端支持在持续完善中。

{{知识点40}}{{微前端架构的团队组织}}
微前端不仅是技术架构，更是组织架构的映射。Conway定律指出系统设计受组织沟通结构影响。微前端实践中：(1)每个子应用对应一个跨职能团队（前端+后端+测试+产品）；(2)团队对子应用拥有完全所有权——技术选型、开发节奏、发布周期；(3)需要平台团队维护主应用框架、CI/CD基础设施、共享组件库；(4)团队间通过API契约和事件协议协作，减少直接代码依赖。
