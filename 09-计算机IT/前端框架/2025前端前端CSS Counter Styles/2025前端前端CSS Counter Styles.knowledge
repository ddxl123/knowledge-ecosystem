# 2025前端前端CSS Counter Styles
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}{{CSS Counter Styles 是什么：CSS Counter Styles Level 3 模块允许开发者通过 @counter-style at-rule 自定义计数器样式，用于控制列表标记（marker）和计数器生成内容的外观。它解决了 CSS 1/2.1 中内置列表样式无法满足全球排版需求的问题，提供了开放式的自定义机制。}}

{{知识点2}}{{@counter-style 规则语法：@counter-style <counter-style-name> { <declaration-list> }。其中 counter-style-name 是一个大小写敏感的 custom-ident，不能是 none，也不能是 decimal、disc、square、circle、disclosure-open、disclosure-closed 这六个不可覆盖的内置名称。}}

{{知识点3}}{{system 描述符——计数算法核心：system 描述符指定将计数器整数值转换为字符串的算法。可选值包括 cyclic（循环）、numeric（数字）、alphabetic（字母）、symbolic（符号）、additive（加法）、fixed（固定）以及 extends（继承）。默认值为 symbolic。}}

{{知识点4}}{{cyclic 系统：循环遍历 symbols 描述符中提供的符号列表，到达末尾后回到开头重新开始。适用于基本的项目符号样式，如 ● ○ ● ○。至少需要一个符号。}}

{{知识点5}}{{numeric 系统：将符号解释为位值计数法中的数字。第一个符号解释为 0，第二个为 1，依此类推。类似 alphabetic，但区别在于 numeric 从 0 开始计数。至少需要两个符号。}}

{{知识点6}}{{alphabetic 系统：将符号解释为字母编号系统的数字。第一个符号解释为 1，第二个为 2。当符号用尽后会产生 aa、ab、ac 这样的组合。至少需要两个符号，仅支持正计数器值。}}

{{知识点7}}{{symbolic 系统：循环遍历符号列表，每次循环时将符号重复次数加倍。第一次循环用 1 个符号，第二次用 2 个，第三次用 3 个。例如 a b → aa bb → aaa bbb。至少需要一个符号，仅支持正值。}}

{{知识点8}}{{additive 系统：用于表示"符号值"编号系统，如罗马数字。不通过位置复用数字，而是定义额外的数字代表更大的值。需要配合 additive-symbols 描述符使用，至少需要一个加法元组。}}

{{知识点9}}{{fixed 系统：定义一个有限的符号集，只遍历一次符号列表。可选指定一个整数参数表示起始位置（默认为 1）。符号用尽后使用 fallback 样式。}}

{{知识点10}}{{extends 系统：继承另一个已定义计数器样式的算法，允许修改部分方面。不能包含 symbols 或 additive-symbols 描述符，否则规则无效。如果引用的样式不存在，默认继承 decimal。}}

{{知识点11}}{{symbols 描述符：指定计数器使用的符号。符号可以是字符串（string）、图像（image）或自定义标识符（custom-ident）。当 system 为 cyclic、numeric、alphabetic、symbolic 或 fixed 时必须指定。}}

{{知识点12}}{{additive-symbols 描述符：定义加法元组列表，每个元组包含一个非负整数权重和一个计数器符号。元组必须按权重降序排列，否则整个描述符无效。仅在 system: additive 时使用。}}

{{知识点13}}{{negative 描述符：指定当计数器值为负数时，在表示前后添加的符号。接受一个或两个 symbol 值：一个值时添加在前面，两个值时分别添加在前后。默认值为 "-"。仅对 symbolic、alphabetic、numeric 和 additive 系统有效。}}

{{知识点14}}{{prefix 描述符：指定在标记表示前添加的符号。前缀在最终阶段添加，在 negative 描述符添加的字符之前。默认值为空字符串。可用于添加如括号、方括号等前缀。}}

{{知识点15}}{{suffix 描述符：指定在标记表示后添加的符号。后缀在标记表示之后添加，包括 negative 描述符添加的字符之后。默认值为 ". "（句号加空格）。常见用法如 ". "、": " 等。}}

{{知识点16}}{{range 描述符：定义计数器样式适用的值范围。值可以是 auto 或逗号分隔的范围列表（每对包含下界和上界，可用 infinite 表示无穷）。超出范围时使用 fallback 样式。支持多个不连续范围。}}

{{知识点17}}{{range 的 auto 值：cyclic/numeric/fixed 系统默认范围为负无穷到正无穷；alphabetic/symbolic 系统默认为 1 到正无穷；additive 系统默认为 0 到正无穷。使用 extends 时，auto 继承被扩展系统的默认范围。}}

{{知识点18}}{{pad 描述符：用于确保标记表示达到最小长度。接受一个非负整数（最小长度）和一个符号（填充字符）。例如 pad: 3 "0" 可使计数显示为 001、002、003。超出最小长度的表示正常显示。}}

{{知识点19}}{{fallback 描述符：指定当计数器样式无法表示某个值时使用的备选样式。接受一个 counter-style-name。如果备选样式也无法表示，会继续沿 fallback 链查找，最终回退到 decimal。默认值为 decimal。}}

{{知识点20}}{{speak-as 描述符：描述语音合成器（如屏幕阅读器）如何朗读计数器样式。可选值包括 auto、bullets、numbers、words、spell-out 或引用另一个计数器样式名称。auto 根据 system 值自动决定朗读方式。}}

{{知识点21}}{{speak-as 的 auto 行为：当 system 为 alphabetic 时等效于 spell-out；cyclic 时等效于 bullets；extends 时继承被扩展样式的 speak-as 设置；其他情况等效于 numbers。}}

{{知识点22}}{{counter() 和 counters() 函数：counter() 函数用于在 content 属性中插入计数器值，counters() 用于嵌套计数器。它们与 @counter-style 配合使用，通过 list-style-type 属性引用自定义样式名来应用。}}

{{知识点23}}{{symbols() 函数——内联匿名计数器：symbols() 函数允许直接在 list-style 等属性中内联定义匿名计数器样式，无需 @counter-style 规则。语法为 symbols(<type>? <string>|<image>+)。功能较 @counter-style 更简单，不支持 identifier 类型符号。}}

{{知识点24}}{{符号类型支持：symbols 描述符支持三种符号类型——字符串（如 "A"、"Ⅰ"）、图像（如 url("icon.svg")）和自定义标识符（如 A、●）。标识符中 ASCII 非字母字符（如 *）需要转义或加引号。}}

{{知识点25}}{{内置不可覆盖的计数器样式：decimal、disc、square、circle、disclosure-open 和 disclosure-closed 这六个内置样式名称不能用作自定义 @counter-style 的名称，但可以在 extends 系统中引用它们。}}

{{知识点26}}{{CSSCounterStyleRule 接口：JavaScript 中通过 CSSCounterStyleRule 接口操作 @counter-style 规则，继承自 CSSRule。提供 name、system、symbols、additiveSymbols、negative、prefix、suffix、range、pad、speakAs、fallback 等属性。自 2023 年 9 月起在所有主流浏览器中可用（Baseline）。}}

{{知识点27}}{{级联与作用域规则：@counter-style 定义的样式在整个文档中可用。同名的多个 @counter-style 规则按标准级联规则决定胜负，且是"原子级"替换——替换整个规则而非单个描述符。预定义样式在 UA 样式表中，因此在级联中总是输。}}

{{知识点28}}{{Shadow DOM 中的行为：@counter-style 的 counter-style-name 是一个 tree-scoped name，意味着在 Shadow DOM 中定义的计数器样式有作用域隔离。可以通过 Shadow DOM 访问和覆盖内置计数器样式。}}

{{知识点29}}{{表示长度限制：symbolic 和 additive 系统以及 pad 描述符可能生成与输入成正比长度的表示。用户代理必须支持至少 60 个 Unicode 码点的表示，超过此长度的可能使用 fallback 样式代替，以防止恶意内存消耗。}}

{{知识点30}}{{表示生成算法：生成计数器表示的步骤为：1)检查样式是否已知；2)检查值是否在范围内；3)使用算法生成初始表示（负值取绝对值）；4)应用 pad 填充；5)应用 negative 包裹；6)返回结果。注意 prefix 和 suffix 不在此算法中，而是由 ::marker 伪元素的内容生成算法添加。}}

{{知识点31}}{{罗马数字示例——additive 系统实践：使用 additive 系统实现罗马数字：additive-symbols: 1000 "M", 900 "CM", 500 "D", 400 "CD", 100 "C", 90 "XC", 50 "L", 40 "XL", 10 "X", 9 "IX", 5 "V", 4 "IV", 1 "I"; 配合 range: 1 3999 可确保正确性。}}

{{知识点32}}{{圈数字示例——fixed 系统实践：使用 fixed 系统创建圈数字列表：system: fixed; symbols: Ⓐ Ⓑ Ⓒ Ⓓ Ⓔ Ⓕ Ⓖ Ⓗ Ⓘ Ⓙ Ⓚ Ⓛ Ⓜ Ⓝ Ⓞ Ⓟ Ⓠ Ⓡ Ⓢ Ⓣ Ⓤ Ⓥ Ⓦ Ⓧ Ⓨ Ⓩ; suffix: " "; 非常适合有限集合的编号场景。}}

{{知识点33}}{{零填充编号示例——pad 描述符实践：system: extends decimal; pad: 3 "0"; suffix: ": "; 可生成 001:、002:、003: 这样的编号效果，常用于需要对齐的有序列表场景。}}

{{知识点34}}{{emoji 列表符号：@counter-style thumbs { system: cyclic; symbols: "\1F44D"; suffix: " "; } 可以使用 emoji 作为列表标记符号，创造更生动的视觉效果。}}

{{知识点35}}{{W3C 预定义计数器样式库：W3C 提供了超过 100 个预定义的计数器样式代码片段（Ready-made Counter Styles 文档），覆盖世界各地语言和文化的需求，包括中文、日文、韩文、阿拉伯文、希伯来文、泰文、埃塞俄比亚文等。}}

{{知识点36}}{{与 list-style 属性的关系：@counter-style 定义的自定义样式通过 list-style-type 属性应用到列表元素。同时也可在 counter() 和 counters() 函数中使用，以及在 content 属性和 ::marker 伪元素中使用。}}

{{知识点37}}{{counter-increment 与 counter-reset 配合：CSS 计数器系统通过 counter-reset 初始化、counter-increment 递增、counter-set 设置值。这些属性与 @counter-style 定义的样式配合使用，控制计数器的实际数值。}}

{{知识点38}}{{::marker 伪元素的交互：@counter-style 的 prefix 和 suffix 不参与 counter() 函数返回值，而是由 ::marker 伪元素的内容构造算法添加。这意味着 counter() 只返回纯表示字符串，前缀后缀由 ::marker 独立处理。}}

{{知识点39}}{{浏览器兼容性：@counter-style 自 2023 年 9 月起成为 Baseline（广泛可用）特性。Chrome 91+、Firefox 33+、Safari 17+、Edge 91+ 均支持。CSSCounterStyleRule JavaScript 接口同样广泛可用。}}

{{知识点40}}{{向后兼容与渐进增强：@counter-style 规则遵循 CSS 的向前兼容解析要求——不理解该规则的浏览器会忽略它而不报错。未识别或未实现的描述符也会被忽略，不会使规则无效。这使得可以安全地在生产环境中使用。}}
