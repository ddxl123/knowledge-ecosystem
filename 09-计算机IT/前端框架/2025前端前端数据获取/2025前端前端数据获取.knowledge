# 2025前端前端数据获取
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}
{{Fetch API 是浏览器原生的网络请求接口，使用 Promise 处理请求和响应。fetch() 方法接受资源路径和可选的 init 配置对象，返回 Promise<Response>。与 XMLHttpRequest 不同，Fetch API 基于 Promise 设计，语法更简洁，支持 Stream API，是现代前端数据获取的基础。2025 年新增 fetchLater() 延迟请求 API，可在页面关闭或导航时发送请求。}}

{{知识点2}}
{{Fetch API 的基本用法：fetch(url, options) 接收两个参数。第一个参数是请求 URL，第二个是配置对象，包含 method、headers、body、mode、credentials、cache 等选项。默认 method 为 GET，不携带 credentials。响应对象需通过 .json()、.text()、.blob() 等方法解析 body。fetch 不会在 HTTP 错误状态码时 reject，需手动检查 response.ok。}}

{{知识点3}}
{{Fetch API 的错误处理：fetch() 只在网络故障时才会 reject（抛出 TypeError），HTTP 错误状态码（如 404、500）不会触发 reject。必须手动检查 response.ok（状态码 200-299）或 response.status。正确模式是先检查 ok，再调用 json()/text() 解析 body。结合 async/await 可以用 try-catch 统一处理网络错误和业务错误。}}

{{知识点4}}
{{Fetch API 与 AbortController：通过 AbortController 可以取消正在进行的 fetch 请求。创建 AbortController 实例，将 signal 传入 fetch 的配置对象，调用 controller.abort() 即可取消请求。取消后 fetch 会 reject 一个 AbortError。这在组件卸载、用户取消操作、竞态请求场景中非常重要，避免内存泄漏和状态更新问题。}}

{{知识点5}}
{{Axios 是基于 Promise 的 HTTP 客户端库，支持浏览器和 Node.js。相比原生 Fetch，Axios 自动转换 JSON 数据、自动处理 HTTP 错误状态码（非 2xx 会 reject）、支持请求和响应拦截器、支持请求取消（基于 AbortController）、支持并发请求（axios.all/axios.spread）。安装方式：npm install axios。}}

{{知识点6}}
{{Axios 拦截器机制：拦截器类似 Express 中间件，可在请求发出前和响应到达前进行处理。axios.interceptors.request.use() 添加请求拦截器，axios.interceptors.response.use() 添加响应拦截器。常用场景包括：自动附加 Authorization token、统一错误处理、请求/响应日志记录、数据格式转换。请求拦截器按 LIFO 顺序执行，响应拦截器按 FIFO 顺序执行。}}

{{知识点7}}
{{Axios 实例与配置：通过 axios.create(config) 创建自定义实例，可预设 baseURL、timeout、headers 等默认配置。适合为不同 API 服务创建独立实例。实例上的配置会与每次请求的配置合并。常用配置项：baseURL（基础路径）、timeout（超时毫秒数，默认 0 无超时）、headers（自定义请求头）、transformRequest/transformResponse（数据转换函数）。}}

{{知识点8}}
{{Axios 错误处理：Axios 在非 2xx 状态码时自动 reject Promise，错误对象 AxiosError 包含 response（服务器响应）、request（请求信息）、message（错误描述）、code（错误码如 ECONNABORTED）、config（请求配置）等属性。可通过 error.response.status 判断具体状态码，error.response.data 获取服务器返回的错误信息。建议设置 timeout 防止请求无限挂起。}}

{{知识点9}}
{{SWR（stale-while-revalidate）是 Vercel 团队开发的数据获取 React Hooks 库，名称来自 HTTP 缓存策略 RFC 5861。核心思想：先返回缓存中的旧数据（stale），同时在后台发起请求获取新数据（revalidate），拿到新数据后更新界面。这使得用户界面始终能快速响应，同时保证数据最终一致性。}}

{{知识点10}}
{{SWR 的核心用法：useSWR(key, fetcher) 接收两个参数。key 是唯一标识数据的字符串或数组（也可用函数做条件获取），fetcher 是接收 key 作为参数并返回数据的异步函数。返回值包含 data（数据）、error（错误）、isLoading（首次加载中）、isValidating（验证中，包括后台重新获取）。支持全局配置 SWRConfig 包裹组件树。}}

{{知识点11}}
{{SWR 的缓存策略：SWR 使用内置缓存存储已获取的数据。默认 staleTime 为 0（数据立即标记为过期），但只要缓存存在就先返回旧数据。关键配置项：dedupingInterval（去重间隔，默认 2000ms）、refreshInterval（自动轮询间隔）、revalidateOnFocus（窗口聚焦时重新验证，默认 true）、revalidateOnReconnect（网络重连时重新验证，默认 true）、revalidateIfStale（数据过期时是否自动重新验证，默认 true）。}}

{{知识点12}}
{{SWR 的高级功能：useSWR 支持条件获取（key 为 falsy 时不发起请求）、依赖获取（key 依赖其他 SWR 数据）、分页查询（useSWRInfinite）、乐观更新（optimisticData）、全局 mutate 方法、本地缓存覆盖（fallbackData）、错误重试（shouldRetryOnError、errorRetryCount）、聚焦时重新验证、网络恢复时重新验证等。}}

{{知识点13}}
{{TanStack Query（原 React Query）是功能强大的异步状态管理和数据获取库。支持 React、Vue、Solid、Svelte、Angular、Lit 等框架。核心定位是服务端状态（server state）管理——处理远程、异步、可能过期、可能被他人修改的数据。提供自动缓存、后台刷新、垃圾回收、分页、无限滚动、离线支持、SSR 支持等完整功能。}}

{{知识点14}}
{{TanStack Query 的 useQuery 基本用法：useQuery({ queryKey, queryFn, ...options })。queryKey 是数组，用于唯一标识和缓存查询；queryFn 是返回 Promise 的异步函数。返回值包含 data、error、isPending（首次加载）、isFetching（任何加载）、isSuccess、isError、status 等。必须在 QueryClientProvider 内使用，通过 QueryClient 管理全局缓存。}}

{{知识点15}}
{{TanStack Query 的 Query Key 设计：Query Key 必须是数组，可包含字符串、数字、对象等可序列化值。TanStack Query 使用确定性哈希（deterministic hashing）缓存数据，对象内键的顺序不影响缓存命中。最佳实践：将 queryFn 中依赖的所有变量都包含在 queryKey 中，这确保变量变化时自动重新获取数据。推荐使用 Query Key Factory 统一管理。}}

{{知识点16}}
{{TanStack Query 的 staleTime 与 gcTime：staleTime（默认 0）控制数据被标记为过期的时间，在此期间不会触发后台重新获取。gcTime（默认 5 分钟，原 cacheTime）控制未使用的缓存数据在内存中保留的时间，超过后被垃圾回收。合理设置 staleTime 可减少不必要的网络请求，如列表页设置 30-60 秒，用户详情页设置 5 分钟。}}

{{知识点17}}
{{TanStack Query 的 Mutations：useMutation 用于创建/更新/删除数据等写操作。useMutation({ mutationFn, onSuccess, onError, onSettled }) 中 mutationFn 接收变量并返回 Promise。返回 mutate 函数触发操作，以及 isPending、isError、isSuccess 等状态。成功后通常配合 queryClient.invalidateQueries() 使相关查询缓存失效以触发重新获取。}}

{{知识点18}}
{{TanStack Query 的乐观更新（Optimistic Updates）：在 mutation 发送请求前立即更新 UI，提供即时反馈。实现方式：在 onMutate 中通过 queryClient.setQueryData 保存当前数据并写入乐观数据，在 onError 中回滚，在 onSettled 中 invalidate 确保最终一致性。这大幅提升用户体验，但需妥善处理失败回滚。}}

{{知识点19}}
{{TanStack Query 的缓存生命周期：新查询挂载时若无缓存则显示加载态并发起请求；完成后数据缓存。第二个相同 key 的查询挂载时立即返回缓存数据，同时在后台重新获取。所有实例卸载后启动 gcTime 倒计时，到期后缓存被垃圾回收。在 gcTime 内重新挂载则立即返回缓存数据并在后台刷新。}}

{{知识点20}}
{{TanStack Query 的并行查询：多个 useQuery 在同一组件中调用时自动并行执行，无需等待前一个完成。对于动态数量的并行查询，可使用 useQueries（注意不是 useQuery 的复数形式）。组合查询中，可用 enabled: false 禁用自动获取，然后在前置查询成功后手动启用，实现依赖查询（Dependent Queries）。}}

{{知识点21}}
{{TanStack Query 的无限滚动（Infinite Queries）：useInfiniteQuery 支持分页和无限滚动场景。配置 getNextPageParam 和 getPreviousPageParam 函数控制分页逻辑。返回 data.pages 数组和 data.pageParams。fetchNextPage/fetchPreviousPage 触发加载更多。hasNextPage/hasPreviousPage 判断是否还有更多数据。与 IntersectionObserver 结合实现滚动加载。}}

{{知识点22}}
{{数据获取竞态条件（Race Condition）：在快速切换页面或频繁触发请求时，后发起的请求可能先返回，导致显示过期数据。解决方案：1) 使用 AbortController 取消旧请求；2) 使用请求标志（request flag）标记最新请求；3) 使用 TanStack Query/SWR 等库自动处理竞态；4) 使用 Redux-Saga 等中间件取消旧任务。}}

{{知识点23}}
{{React Suspense 与数据获取：React 18+ 的 Suspense 支持数据获取集成。TanStack Query 提供 suspense: true 选项，使查询在数据未就绪时触发 Suspense 边界。结合 useSuspenseQuery 可在组件内直接使用数据而无需处理加载态。SWR 同样支持 suspense: true 模式。这使得数据获取代码更简洁，配合 ErrorBoundary 统一处理错误。}}

{{知识点24}}
{{客户端数据获取 vs 服务端数据获取：客户端获取（CSR）在浏览器中发起请求，适合交互频繁的场景，但有 SEO 和首屏性能问题。服务端获取（SSR/SSG/ISR）在服务端预取数据，HTML 包含完整内容，对 SEO 友好。Next.js 的 Server Components 允许在服务端直接 await 数据获取。现代框架通常推荐混合策略：关键数据服务端获取，交互数据客户端获取。}}

{{知识点25}}
{{GraphQL 数据获取：GraphQL 是 API 查询语言，允许客户端精确请求所需字段。常用客户端库：Apollo Client（功能全面，内置缓存和状态管理）、urql（轻量级，可扩展）、Relay（Facebook 出品，性能优化极致）。Apollo Client 的 useQuery/useMutation hooks 与 TanStack Query 类似但针对 GraphQL 优化，支持规范化缓存（normalized cache）和乐观 UI。}}

{{知识点26}}
{{Server Actions（React 19）与数据获取：React 19 的 Server Actions 允许客户端直接调用服务端函数，无需手动创建 API 端点。通过 'use server' 指令标记服务端函数，客户端可直接调用。结合 useActionState、useFormStatus、useOptimistic 等 hooks 处理表单提交和乐观更新。这简化了传统的客户端-服务端数据流，但不完全替代传统 API 调用。}}

{{知识点27}}
{{请求重试策略：数据获取失败后的重试策略对用户体验至关重要。指数退避（Exponential Backoff）是常用策略：每次重试间隔翻倍（如 1s → 2s → 4s），避免服务器压力。TanStack Query 内置 retry 和 retryDelay 配置（默认重试 3 次）。SWR 通过 onErrorRetry 配置重试逻辑。Axios 可配合 axios-retry 插件实现自动重试。}}

{{知识点28}}
{{数据获取的缓存层级：现代前端应用通常有多个缓存层级：1) 浏览器 HTTP 缓存（Cache-Control、ETag）；2) Service Worker 缓存（Cache API）；3) 应用层缓存（TanStack Query、SWR、Apollo Client 内置缓存）；4) 状态管理缓存（Redux、Zustand 存储的服务器数据）。合理设计缓存层级可显著减少网络请求，提升应用性能。}}

{{知识点29}}
{{SWR 的全局配置与中间件：通过 SWRConfig 组件可为子组件树设置全局默认配置，包括 fetcher、缓存策略、错误重试、刷新间隔等。SWR 支持中间件（middleware）机制，可以在数据获取前后添加自志、认证、缓存转换等逻辑。中间件接收 useSWR 的 next 函数，可拦截和修改 key、fetcher 和返回的数据。}}

{{知识点30}}
{{TanStack Query 的 DevTools：TanStack Query 提供专用开发工具 @tanstack/react-query-devtools，可视化展示查询状态、缓存数据、活跃/非活跃查询、刷新时间等。支持手动触发重新获取、使缓存失效、重置查询等操作。对于调试缓存问题和理解数据流非常有价值。在生产环境中可通过 enabled={process.env.NODE_ENV === 'development'} 条件加载。}}

{{知识点31}}
{{API 状态管理模式：2025 年主流前端 API 状态管理模式包括：1) 服务端状态用 TanStack Query/SWR 管理，与客户端状态分离；2) 使用 Zustand/Jotai 等轻量状态库管理客户端状态；3) 避免将服务端数据存入 Redux 等全局状态库；4) 服务端组件直接获取数据，客户端组件通过 hooks 获取。这种分离使代码更清晰、缓存更高效。}}

{{知识点32}}
{{fetchLater() API（2025 新特性）：fetchLater() 是 Fetch API 的新增方法，允许延迟发送请求。可在指定时间后、页面关闭或导航时发送请求。适用于分析数据上报、表单草稿保存、使用日志提交等非关键但需要确保发送的场景。返回 FetchLaterResult 对象，支持 DeferredRequestInit 配置延迟时间。通过 Permissions-Policy 的 deferred-fetch 控制权限。}}

{{知识点33}}
{{SWR 与 TanStack Query 的对比选择：SWR 更轻量（~4KB gzipped），API 更简单，适合中小型项目和简单数据获取场景。TanStack Query 功能更全面（~13KB gzipped），提供 mutations、离线支持、SSR 支持、更精细的缓存控制，适合大型复杂应用。两者都支持 React Suspense、窗口聚焦重新验证、自动垃圾回收。选择依据：项目复杂度、团队熟悉度、生态需求。}}

{{知识点34}}
{{数据获取中的安全最佳实践：1) 始终验证和清理用户输入后再拼入请求参数；2) 使用 HTTPS 传输数据；3) 敏感数据（token）不要存在 URL 参数中；4) 实施 CORS 策略限制跨域请求；5) 设置合理的请求超时时间（建议 5-30 秒）；6) 对敏感 API 使用 CSRF token；7) 使用 Content-Security-Policy 限制加载源；8) 避免在客户端存储过多敏感数据。}}

{{知识点35}}
{{Axios 的并发请求处理：Axios 提供 axios.all() 和 axios.spread() 处理并发请求，类似 Promise.all。axios.all([req1, req2]) 等待所有请求完成，axios.spread((res1, res2) => {}) 展开结果。更推荐直接使用 Promise.all([axios.get(url1), axios.get(url2)])。每个请求可独立设置 AbortController signal 以支持单独取消。结合 async/await 语法更清晰。}}

{{知识点36}}
{{React Query 的 Prefetching（预获取）：通过 queryClient.prefetchQuery() 可在用户实际需要数据前预先获取，提升感知性能。常见模式：1) 在路由切换时预获取下一页数据；2) 鼠标悬停在链接上时预获取目标页面数据（onMouseEnter）；3) 无限滚动中预获取下一页。prefetchQuery 只在缓存为空或数据过期时发起请求，不会重复获取。}}

{{知识点37}}
{{数据获取的性能优化策略：1) 请求去重（deduping）：相同 key 的多个请求合并为一个；2) 分页和虚拟化：只获取可视区域数据；3) 按需加载（lazy loading）：组件可见时才获取数据；4) 压缩和缓存响应：启用 gzip/brotli，设置合适的 Cache-Control；5) 使用 CDN 加速 API 响应；6) 批量请求：多个小请求合并为一个批量请求；7) 使用 React.memo 和 useMemo 避免不必要的重新渲染。}}

{{知识点38}}
{{Service Worker 与离线数据获取：Service Worker 可拦截网络请求并实现离线缓存策略。常用策略包括：Cache First（优先使用缓存）、Network First（优先网络）、Stale While Revalidate（返回缓存同时后台更新）。Workbox 库简化了 Service Worker 缓存配置。TanStack Query 支持离线数据持久化（persistQueryClient），将缓存数据存储到 IndexedDB，在离线时仍可访问。}}

{{知识点39}}
{{Next.js App Router 中的数据获取：Next.js 13+ App Router 推荐 Server Components 中直接使用 async/await 获取数据，无需 useEffect 或客户端数据获取库。fetch 请求在服务端自动去重和缓存（可通过 cache: 'no-store' 禁用）。客户端组件仍需使用 TanStack Query 等库。Route Handlers（API Routes）提供 RESTful API 端点。Server Actions 处理表单提交和数据变更。}}

{{知识点40}}
{{WebSocket 与实时数据获取：对于需要实时更新的场景（聊天、股票行情、协作编辑），HTTP 轮询效率低下。WebSocket 提供全双工通信通道，支持服务端主动推送数据。Socket.IO 是流行的 WebSocket 库，提供自动重连、房间、广播等功能。TanStack Query 可配合 WebSocket 实现查询数据的实时更新，通过 queryClient.setQueryData 在收到消息时更新缓存。}}
