# 2025前端Prisma ORM
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}
{{Prisma ORM 是什么}}
Prisma ORM 是一个开源的下一代 Node.js/TypeScript ORM（对象关系映射）工具。它由三个核心部分组成：Prisma Client（自动生成的类型安全查询构建器）、Prisma Migrate（数据库迁移系统）和 Prisma Studio（可视化数据库管理 GUI）。Prisma Client 可以在任何 Node.js 或 TypeScript 后端应用中使用，包括 REST API、GraphQL API、gRPC 等场景。

{{知识点2}}
{{Prisma Schema 基本结构}}
Prisma Schema（.prisma 文件）是 Prisma 项目的核心配置文件，包含三部分：datasource（数据源，配置数据库连接）、generator（生成器，指定要生成的客户端代码）和 data model（数据模型，定义应用实体）。例如：datasource db { provider = "postgresql" } 配置 PostgreSQL 数据源，generator client { provider = "prisma-client" } 生成客户端代码。

{{知识点3}}
{{数据模型（Models）}}
Models 是 Prisma Schema 中定义应用实体的核心部分。每个 model 映射到关系数据库中的一张表或 MongoDB 中的一个集合。Model 定义了字段（包括标量字段和关系字段），并作为 Prisma Client API 查询的基础。例如 model User { id Int @id @default(autoincrement()) email String @unique name String? } 定义了一个用户模型。

{{知识点4}}
{{字段类型与属性}}
Prisma Schema 支持多种标量字段类型：Int、String、Boolean、Float、DateTime、Json、BigInt、Decimal、Bytes 等。字段属性包括：@id（主键）、@default()（默认值）、@unique（唯一约束）、@map()（映射数据库列名）、@db 类型映射等。可选字段在类型后加 ? 标记。

{{知识点5}}
{{自动生成主键}}
使用 @id @default(autoincrement()) 可以创建自增整数主键，@default(cuid()) 或 @default(uuid()) 可生成字符串主键。MongoDB 中使用 @default(auto()) @map("_id") @db.ObjectId 生成 ObjectId 类型主键。@updatedAt 修饰符可自动在记录更新时刷新时间戳。

{{知识点6}}
{{Prisma Client 安装与生成}}
安装命令为 npm install prisma --save-dev 和 npm install @prisma/client。运行 npx prisma generate 读取 Prisma Schema 并自动生成 Prisma Client 代码到指定的 output 目录。修改数据模型后需重新运行 prisma generate 来更新生成的代码。

{{知识点7}}
{{Prisma Client 基本使用}}
导入并实例化：import { PrismaClient } from "./generated/client"; const prisma = new PrismaClient();。Prisma Client 提供完全类型安全的 API，所有查询返回普通 JavaScript 对象（POJOs），支持 async/await 语法。在 TypeScript 项目中，所有查询都享有完整的类型推断。

{{知识点8}}
{{创建记录（Create）}}
使用 prisma.model.create() 创建单条记录：prisma.user.create({ data: { email: "elsa@prisma.io", name: "Elsa" } })。使用 prisma.model.createMany() 批量创建记录，支持 skipDuplicates 选项跳过重复记录。部分数据库支持 createManyAndReturn() 在创建后返回记录。

{{知识点9}}
{{查询记录（Read）}}
findUnique() 根据唯一字段或 ID 查询单条记录；findMany() 查询多条记录，支持 where 过滤、orderBy 排序、take 限制数量、skip 跳过数量；findFirst() 返回第一条匹配记录。例如 prisma.user.findMany({ where: { email: { endsWith: "prisma.io" } } })。

{{知识点10}}
{{更新记录（Update）}}
update() 更新单条记录，updateMany() 批量更新多条记录，upsert() 执行更新或创建操作。支持原子数字操作如 increment、decrement、multiply、divide、set。例如 prisma.post.update({ where: { id: 1 }, data: { title: "新标题" } })。

{{知识点11}}
{{删除记录（Delete）}}
delete() 删除单条记录，deleteMany() 删除多条记录，deleteMany({}) 删除所有记录。删除有关联记录的实体时可能因外键约束报错，需通过 referential actions（如 Cascade）配置级联删除行为。

{{知识点12}}
{{关系（Relations）概述}}
Prisma 支持三种关系类型：一对一（1-1，如 User ↔ Profile）、一对多（1-n，如 User ↔ Post）、多对多（m-n，如 Post ↔ Category）。关系由关系字段（Prisma 层级，不存于数据库）和关系标量字段（数据库中的外键）组成。关系通过 @relation 属性定义。

{{知识点13}}
{{一对一关系}}
一对一关系通过在一方添加可选关系字段（Profile?）和唯一外键实现。例如：model Profile { user User @relation(fields: [userId], references: [id]) userId Int @unique }。userId 字段上的 @unique 确保一个 User 只能有一个 Profile。

{{知识点14}}
{{一对多关系}}
一对多关系在"一"方使用数组类型（Post[]），在"多"方使用可选或必填关系字段和外键。例如：User 模型有 posts Post[] 字段，Post 模型有 author User @relation(fields: [authorId], references: [id]) 和 authorId Int 外键。

{{知识点15}}
{{多对多关系（隐式与显式）}}
隐式多对多关系：Prisma 自动管理中间关系表，Schema 中双方都用数组字段（posts Post[] 和 categories Category[]），无需手动定义关系表。显式多对多关系：需要手动定义中间模型（如 CategoriesOnPosts），可添加额外字段如 assignedAt。隐式多对多要求双方使用单一 @id。

{{知识点16}}
{{嵌套写入（Nested Writes）}}
嵌套写入允许在单个事务中操作关联记录。支持嵌套 create、connect、disconnect、set、update、upsert、delete 等操作。例如创建用户时同时创建帖子：prisma.user.create({ data: { email: "...", posts: { create: [{ title: "Post 1" }] } } })。

{{知识点17}}
{{过滤与排序（Filtering and Sorting）}}
where 过滤支持丰富操作符：equals、contains、startsWith、endsWith、gt、gte、lt、lte、in、not、notIn 等。支持 OR、AND、NOT 逻辑组合。orderBy 支持按字段升序（asc）或降序（desc）排序，支持多字段排序和关联排序。

{{知识点18}}
{{关联过滤}}
可在 where 中通过关联字段过滤：prisma.user.findMany({ where: { posts: { some: { published: true } } } })。支持 some（至少一个匹配）、every（全部匹配）、none（无匹配）三种关联过滤模式。

{{知识点19}}
{{字段选择（Select）与包含（Include）}}
select 指定返回哪些字段，减少数据传输：prisma.user.findUnique({ where: { id: 1 }, select: { email: true, name: true } })。include 指定包含哪些关联数据：prisma.user.findMany({ include: { posts: true } })。select 和 include 不能同时使用在同一层级。

{{知识点20}}
{{分页（Pagination）}}
支持偏移分页（offset pagination）：使用 skip 和 take 参数。支持游标分页（cursor-based pagination）：使用 cursor 和 take 参数，性能更好。例如 prisma.post.findMany({ cursor: { id: lastId }, take: 10, skip: 1 })。

{{知识点21}}
{{聚合与分组（Aggregation and GroupBy）}}
aggregate() 支持 _avg、_sum、_min、_max、_count 等聚合操作。groupBy() 按字段分组并支持聚合，类似 SQL 的 GROUP BY。支持 where 分组前过滤和 having 分组后过滤。例如 prisma.user.groupBy({ by: ['country'], _sum: { profileViews: true } })。

{{知识点22}}
{{事务（Transactions）}}
Prisma 支持三种事务方式：嵌套写入（自动事务）、顺序操作（$transaction([...]) 传入查询数组）、交互式事务（$transaction(async (tx) => { ... })）。交互式事务支持 maxWait（获取事务最大等待时间）、timeout（事务最大运行时间）和 isolationLevel（隔离级别）选项。

{{知识点23}}
{{事务隔离级别}}
支持 ReadUncommitted、ReadCommitted、RepeatableRead、Snapshot、Serializable 五种隔离级别。PostgreSQL 默认 ReadCommitted，MySQL 默认 RepeatableRead，SQLite 和 CockroachDB 仅支持 Serializable。MongoDB 不支持隔离级别设置。

{{知识点24}}
{{Prisma Migrate}}
Prisma Migrate 是基于迁移文件的数据库迁移系统。运行 prisma migrate dev --name init 创建初始迁移，自动生成 SQL 文件。迁移文件存储在 prisma/migrations/ 目录下，可提交到版本控制。支持开发环境（migrate dev）和生产环境（migrate deploy）两种模式。

{{知识点25}}
{{内省（Introspection）}}
运行 prisma db pull 可从现有数据库反向生成 Prisma Schema。适用于已有数据库的项目集成 Prisma，或当数据库 Schema 是唯一事实来源时。内省会自动检测表、字段、类型、关系和索引。

{{知识点26}}
{{Prisma Studio}}
Prisma Studio 是 Prisma ORM 的可视化数据库管理工具，提供图形界面查看和编辑数据库中的数据。它是 Prisma ORM 中唯一不开源的组件，只能在本地运行。通过 npx prisma studio 命令启动。

{{知识点27}}
{{Client Extensions（客户端扩展）}}
Prisma Client Extensions 允许扩展 Prisma Client 的功能。支持四种组件类型：model（为模型添加自定义方法或字段）、client（添加客户端级别方法）、query（创建自定义查询逻辑）、result（为查询结果添加自定义字段）。通过 $extends() 方法或 Prisma.defineExtension() 创建扩展。

{{知识点28}}
{{原始数据库查询（Raw Queries）}}
Prisma Client 支持通过 $queryRaw 和 $executeRaw 执行原始 SQL 查询。$queryRaw 用于读取操作，$executeRaw 用于写入操作。支持模板字符串语法防止 SQL 注入：prisma.$queryRaw`SELECT * FROM User WHERE id = ${id}`。

{{知识点29}}
{{中间件（Middleware / Extensions with $use）}}
Prisma Client 支持通过扩展中的 query 组件拦截和修改查询行为。可实现日志记录、性能监控、软删除、数据验证等横切关注点。扩展组件在查询执行前后运行，可修改查询参数和结果。

{{知识点30}}
{{错误处理}}
Prisma 提供结构化错误类型，通过 PrismaClientKnownRequestError 捕获已知错误。错误包含 code（错误代码，如 P2025 表示记录未找到）、meta（附加信息）和 message（错误描述）。常见的错误代码包括 P2002（唯一约束违反）、P2003（外键约束违反）、P2025（记录不存在）。

{{知识点31}}
{{连接管理与生命周期}}
Prisma Client 使用连接池管理数据库连接。建议创建单例实例避免连接池耗尽。在应用关闭时调用 prisma.$disconnect() 优雅关闭连接。Next.js 等框架中建议使用全局变量缓存 Prisma Client 实例，防止开发环境热重载创建过多连接。

{{知识点32}}
{{支持的数据库}}
Prisma ORM 支持多种数据库：PostgreSQL、MySQL、MariaDB、SQLite、Microsoft SQL Server、MongoDB、CockroachDB。还支持 PlanetScale、Neon、Turso 等无服务器数据库。不同数据库对某些功能的支持程度不同。

{{知识点33}}
{{类型安全与代码生成}}
Prisma Client 是类型安全的：TypeScript 项目中所有查询参数和返回值都有完整的类型推断。select 选择不同字段时返回类型会自动变化（部分类型）。修改 Schema 后需运行 prisma generate 更新类型定义。支持 Prisma Validator 辅助构建类型安全的查询参数。

{{知识点34}}
{{环境变量与配置}}
数据库连接 URL 通过 prisma.config.ts 配置文件管理，使用 env() 函数引用环境变量。Prisma CLI 命令不会自动加载 .env 文件，需配合 dotenv 等包使用。DATABASE_URL 格式因数据库而异，如 postgresql://user:password@host:port/database。

{{知识点35}}
{{Prisma 与 Next.js 集成}}
在 Next.js 项目中，通常在 lib/prisma.ts 中创建 Prisma Client 单例。App Router 中可直接在 Server Components 和 Server Actions 中使用。API Routes 中同样可用。开发环境热重载时需缓存实例防止连接泄漏。

{{知识点36}}
{{种子数据（Seeding）}}
Prisma 支持通过 prisma/seed.ts 文件定义种子数据。在 prisma.config.ts 中配置 seed 命令（如 seed: "tsx ./prisma/seed.ts"），运行 prisma db seed 执行。种子脚本中可使用 Prisma Client 创建初始数据。

{{知识点37}}
{{Prisma Accelerate}}
Prisma Accelerate 是 Prisma 的连接池和全球缓存服务，适用于无服务器和边缘环境。通过连接字符串代理数据库连接，提供连接池管理、查询缓存和全球分布加速功能。可显著减少无服务器函数的冷启动时间和数据库连接开销。

{{知识点38}}
{{Prisma Pulse}}
Prisma Pulse 是数据库变更实时流服务，允许应用订阅数据库中的创建、更新和删除事件。适用于实时通知、事件驱动架构、数据同步等场景。通过 Prisma Client 扩展使用，支持过滤特定模型和操作类型的事件。

{{知识点39}}
{{全文搜索（Full-Text Search）}}
Prisma Client 支持利用数据库原生全文搜索功能。PostgreSQL 使用 @@fulltext 属性，MySQL 使用 @db.Text。搜索 API 支持搜索、排序和过滤。PostgreSQL 和 MySQL 支持按相关性排序（_relevance）。

{{知识点40}}
{{排除字段（Omit）}}
Prisma Client 支持 omit 选项排除查询结果中的特定字段，与 select 互补。omit: { password: true } 可排除密码字段。适用于敏感数据保护场景，在查询级别而非 Schema 级别控制字段可见性。
