# 2025前端WebAssembly入门
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}
WebAssembly（简称 Wasm）是一种低级字节码格式，最初为 Web 设计。它不是为了让人手写，而是作为 C、C++、Rust 等源语言的有效编译目标。Wasm 以紧凑的二进制格式运行，提供接近原生的性能。来源：MDN WebAssembly 文档。

{{知识点2}}
WebAssembly 不是 JavaScript 的替代品，而是互补。JavaScript 是高级语言，灵活表达力强；WebAssembly 是低级汇编级语言，性能接近原生。两者可以在同一个应用中协作——Wasm 模块可被 JS 导入调用，Wasm 代码也可以导入并同步调用 JS 函数。

{{知识点3}}
WebAssembly 的核心设计目标：(1) 快速、高效、可移植——利用通用硬件能力在不同平台接近原生速度运行；(2) 可读可调试——有人类可读的文本格式；(3) 安全——在内存安全的沙箱执行环境中运行，强制执行同源策略和权限策略；(4) 不破坏 Web——保持向后兼容。

{{知识点4}}
WebAssembly 由 W3C WebAssembly 社区组和工作组开发为开放标准，所有主流浏览器厂商均积极参与。截至 2025 年，所有主流浏览器（Chrome、Firefox、Safari、Edge）都已支持 WebAssembly。

{{知识点5}}
WebAssembly 在浏览器中的运行模型：Web 平台由虚拟机（VM）和 Web API 两部分组成。VM 以前只能运行 JavaScript，现在可以同时运行 JavaScript 和 WebAssembly 两种代码。两种代码可以互相调用。

{{知识点6}}
WebAssembly 的关键概念包括：Module（模块）、Memory（内存）、Table（表）、Instance（实例）。Module 是已编译的无状态 Wasm 代码，可被高效共享和多次实例化。Instance 是 Module 的有状态可执行实例。

{{知识点7}}
WebAssembly.Module 对象包含已编译的无状态 WebAssembly 代码。它可以通过 postMessage 在 Worker 之间高效共享，并可被多次实例化。Module 是 Wasm 的编译产物。

{{知识点8}}
WebAssembly.Instance 是 Module 的有状态可执行实例。Instance 对象包含所有导出的 WebAssembly 函数，允许从 JavaScript 调用 Wasm 代码。通过 instance.exports 访问导出的功能。

{{知识点9}}
WebAssembly.Memory 是一个可调整大小的 ArrayBuffer，存储 Instance 访问的原始字节。Wasm 代码使用线性内存模型进行内存管理，Memory 对象是 JS 和 Wasm 之间共享内存的桥梁。

{{知识点10}}
WebAssembly.Table 是一个可调整大小的类型化数组，存储不透明值（如函数引用）。Table 用于实现间接函数调用，支持动态链接等高级特性。

{{知识点11}}
WebAssembly 使用线性内存（Linear Memory）模型。内存是一个连续的、可调整大小的字节数组。Wasm 代码通过 load/store 指令直接读写这块内存。JS 可以通过 Memory 对象的 buffer 属性访问同一块内存。

{{知识点12}}
加载和运行 WebAssembly 代码的推荐方式是使用 WebAssembly.instantiateStreaming()，它可以直接从网络流获取字节码并一步完成编译和实例化，效率最高。示例：WebAssembly.instantiateStreaming(fetch("module.wasm"), importObject)。

{{知识点13}}
WebAssembly.instantiate() 有两种重载形式：第一种接受字节码数组，返回 Promise（解析为包含 module 和 instance 的对象）；第二种接受已编译的 Module 对象，直接返回 instance。通常只需要 instance，但 module 可用于缓存或共享。

{{知识点14}}
旧版加载方式需要先 fetch 字节码转为 ArrayBuffer，再调用 WebAssembly.instantiate(bytes)。新版 instantiateStreaming 直接处理字节流，省去了 ArrayBuffer 转换步骤，更高效。

{{知识点15}}
WebAssembly 有一个人类可读的文本格式（.wat 文件），用于调试、测试、学习和手写。浏览器开发者工具中查看 Wasm 模块源码时显示的就是这种文本格式。文本格式可以转换为二进制格式（.wasm）。

{{知识点16}}
Emscripten 是将 C/C++ 代码编译为 WebAssembly 的主要工具链。使用 emcc 命令可以将 C/C++ 源文件编译为 .wasm 模块，同时生成 JavaScript 胶水代码和可选的 HTML 文件。命令示例：emcc hello.c -o hello.html。

{{知识点17}}
将 Rust 编译为 WebAssembly 推荐使用 wasm-pack 工具。wasm-pack 帮助编译代码并生成适合在浏览器中使用的包。安装方式：cargo install wasm-pack。创建项目：cargo new --lib hello-wasm。

{{知识点18}}
wasm-bindgen 是 Rust 生态中用于 WebAssembly 的核心库，提供 Rust 和 JavaScript 之间的通信桥梁。通过 #[wasm_bindgen] 属性宏，可以标记 Rust 函数导出给 JS 调用，或声明外部 JS 函数供 Rust 调用。

{{知识点19}}
Rust + WebAssembly 有两种主要用例：(1) 构建整个应用——完全基于 Rust 的 Web 应用（如 yew、leptos 框架）；(2) 构建应用的一部分——在现有 JS 前端中使用 Rust 处理性能关键部分。目前社区更侧重后者。

{{知识点20}}
WebAssembly.Global 对象表示一个全局变量实例，可从 JavaScript 和 WebAssembly 两端访问，并可在多个 Module 实例之间导入/导出。这支持了动态链接多个模块的能力。

{{知识点21}}
WebAssembly.Tag 定义了一种 WebAssembly 异常类型，可以被抛出到/从 WebAssembly 代码。WebAssembly.Exception 表示从 Wasm 抛出到 JS 或从 JS 抛出到 Wasm 的运行时异常。这是 Wasm 异常处理提案的一部分。

{{知识点22}}
WebAssembly.validate() 函数用于验证给定的类型化数组是否为合法的 WebAssembly 二进制代码。返回布尔值，可用于在实例化前安全检查 Wasm 模块的有效性。

{{知识点23}}
WebAssembly 的安全模型基于沙箱执行环境。Wasm 代码在受控的线性内存中运行，不能直接访问主机内存或执行系统调用。在 Web 环境中，Wasm 还强制执行浏览器的同源策略和权限策略。

{{知识点24}}
WebAssembly 的编译流程：源代码（C/C++/Rust 等）→ 编译器（Emscripten/wasm-pack 等）→ .wasm 二进制模块 → 浏览器下载 → 编译为机器码 → 实例化执行。浏览器在下载后会将 Wasm 快速编译为原生机器码。

{{知识点25}}
WebAssembly 不仅限于浏览器使用。它支持非 Web 嵌入（Non-web embeddings），可以在服务器端（如 Node.js、Deno、Wasmer）、边缘计算、IoT 设备等环境中运行。WASI（WebAssembly System Interface）为非 Web 环境提供标准化系统接口。

{{知识点26}}
WebAssembly 的导入/导出机制：Wasm 模块可以声明导入项（需要外部提供）和导出项（供外部使用）。在浏览器中，导入对象通常是 JS 函数和对象，导出的函数可以被 JS 调用。这实现了 Wasm 和宿主环境的双向通信。

{{知识点27}}
WebAssembly 指令集是基于栈的虚拟机指令集。指令操作数从栈上弹出，结果压回栈上。支持的数据类型包括：i32、i64、f32、f64（以及 SIMD 扩展的 v128）。指令集设计为硬件无关，可在不同平台上高效实现。

{{知识点28}}
WebAssembly SIMD（单指令多数据）提案已经标准化，引入了 128 位向量类型和相关指令，允许并行处理多个数据元素。这对图像处理、音频处理、加密、科学计算等场景可带来显著性能提升。

{{知识点29}}
WebAssembly 的异常处理提案引入了 try-catch-finally 结构，允许 Wasm 代码捕获和抛出异常。通过 WebAssembly.Tag 定义异常类型，实现了 Wasm 和 JS 之间的异常互操作。

{{知识点30}}
WebAssembly 垃圾回收提案（WasmGC）允许 Wasm 使用宿主环境的垃圾回收器，使得 Java、Kotlin、Dart 等带 GC 的语言可以高效编译为 Wasm。这是 2024-2025 年的重要进展，已获得主流浏览器支持。

{{知识点31}}
WebAssembly 组件模型（Component Model）是一个高级抽象，定义了 Wasm 模块之间的标准化接口和组合方式。它引入了 WIT（WebAssembly Interface Type）接口定义语言，支持跨语言、跨运行时的模块互操作。

{{知识点32}}
WebAssembly 的模块链接方式：可以通过 import/export 在多个 Wasm 模块之间建立依赖关系。动态链接允许在运行时连接模块，静态链接在编译时合并。WebAssembly.Module 和 WebAssembly.Instance 的分离设计支持灵活的链接策略。

{{知识点33}}
WebAssembly 在前端的实际应用场景：(1) 图像/视频编辑（如 Figma、Photoshop Web）；(2) 游戏引擎（如 Unity WebGL、Unreal Engine）；(3) 加密和压缩库；(4) 代码编辑器（如 VS Code Web 使用 Wasm 版 ripgrep）；(5) 音视频处理；(6) CAD/3D 可视化。

{{知识点34}}
WebAssembly Worker 线程使用：Wasm 模块可以在 Web Worker 中运行，实现多线程计算。通过 postMessage 在主线程和 Worker 之间传递数据和模块。Module 对象可以高效地通过 postMessage 共享。

{{知识点35}}
WebAssembly 的性能特点：(1) 下载体积小——紧凑的二进制格式比等效 JS 更小；(2) 解析快——二进制格式解析速度远快于 JS 文本解析；(3) 编译优化——浏览器可快速将 Wasm 编译为优化的机器码；(4) 执行稳定——没有 JIT 的去优化问题，性能可预测。

{{知识点36}}
wasm-pack 工作流：(1) cargo new --lib 创建库项目；(2) 在 Cargo.toml 添加 wasm-bindgen 依赖；(3) 编写 Rust 代码并用 #[wasm_bindgen] 标记导出；(4) 运行 wasm-pack build 生成 npm 包；(5) 在 JS 项目中 import 使用。

{{知识点37}}
WebAssembly 的 Text Format（.wat）语法使用 S 表达式。一个最小的 Wasm 模块可以表示为 (module)。函数定义示例：(func (export "add") (param $a i32) (param $b i32) (result i32) (i32.add (local.get $a) (local.get $b)))。

{{知识点38}}
WebAssembly 的版本控制策略：Wasm 采用无版本（versionless）设计，通过特性检测而非版本号来处理新功能。模块头部包含魔数（\0asm）和版本号（目前为 1），但向后兼容性是核心承诺。

{{知识点39}}
AssemblyScript 是一种专门面向 WebAssembly 的 TypeScript 变体。它允许前端开发者使用类似 TypeScript 的语法编写代码并编译为 Wasm，无需学习 Rust 或 C++。适合想要利用 Wasm 性能但不想切换语言的前端团队。

{{知识点40}}
WebAssembly 的调试方法：(1) 浏览器 DevTools 支持查看 Wasm 模块的文本格式和反汇编；(2) Chrome DevTools 支持 Wasm 断点调试；(3) Emscripten 生成的 DWARF 调试信息可实现源码级调试；(4) console.log 通过 JS 胶水代码间接输出。

{{知识点41}}
WebAssembly System Interface（WASI）为 Wasm 提供标准化的系统级 API，包括文件系统访问、网络、环境变量等。WASI 使得 Wasm 模块可以在不同运行时（Wasmer、Wasmtime、WasmEdge）之间可移植运行，被称为"Web 的容器"。

{{知识点42}}
WebAssembly 2.0 规范于 2025 年进入推荐标准阶段，引入了多值返回、引用类型、批量内存操作、尾调用优化等特性。这些扩展在保持向后兼容的同时大幅增强了 Wasm 的表达能力。
