# 2025前端SSR与SSG
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}{{SSR（Server-Side Rendering）即服务端渲染，指在服务器端将前端框架（如React、Vue、Svelte）的组件渲染为完整的HTML，然后发送给浏览器。用户可以立即看到页面内容，无需等待JavaScript下载和执行。SSR的核心优势是首屏加载速度快和SEO友好，因为搜索引擎爬虫可以直接获取完整的HTML内容。}}

{{知识点2}}{{SSG（Static Site Generation）即静态站点生成，指在构建时（build time）预先将页面渲染为静态HTML文件。这些文件可以直接部署到CDN，用户请求时无需服务器实时渲染，响应速度极快。SSG适合内容不频繁变化的网站，如博客、文档站和营销页面。}}

{{知识点3}}{{Hydration（水合/激活）是SSR的关键步骤。服务器返回的HTML是静态的，需要在客户端下载JavaScript后，将事件监听器和交互逻辑绑定到已有的DOM元素上，使页面变得可交互。这个过程称为水合。Hydration期间页面虽然可见但不可交互，可能出现Hydration Mismatch（水合不匹配）错误。}}

{{知识点4}}{{Streaming SSR（流式SSR）是一种优化的SSR方式，服务器可以分块发送HTML而不是等待整个页面渲染完成。React 18引入了renderToPipeableStream API支持流式SSR，允许渐进式页面展示——用户可以先看到已完成的部分，剩余部分逐步加载。这显著改善了TTFB（Time to First Byte）指标。}}

{{知识点5}}{{RSC（React Server Components）是React 18+引入的革命性概念。服务器组件在服务端执行，不会向客户端发送任何JavaScript。它们可以直接访问数据库、文件系统等后端资源。客户端组件通过'use client'指令声明，负责交互逻辑。RSC Payload是一种紧凑的二进制格式，用于在客户端协调服务端组件和客户端组件树。}}

{{知识点6}}{{Next.js是React生态中最主流的SSR/SSG框架（2025年最新版本为16.x）。其App Router架构默认将layouts和pages作为Server Components，支持流式渲染、并行路由和拦截路由等高级特性。Next.js同时支持SSR、SSG、ISR和PPR（Partial Prerendering）多种渲染策略。}}

{{知识点7}}{{ISR（Incremental Static Regeneration，增量静态再生）是Next.js独有的特性，结合了SSG和SSR的优点。页面在构建时静态生成，但可以在运行时按需或定时重新生成。通过设置revalidate参数（秒数），Next.js会在缓存过期后的首次请求时返回旧页面，同时在后台生成新版本。这避免了全量重建的开销。}}

{{知识点8}}{{Next.js的generateStaticParams函数用于SSG场景，在构建时为动态路由生成所有可能的参数组合。例如博客的[id]路由，可以在构建时预先生成所有已知博客文章的页面。配合revalidate参数，这些页面可以在运行时按需更新。}}

{{知识点9}}{{Next.js中Server Components和Client Components的组合方式：Server Component可以导入并渲染Client Component，但Client Component不能导入Server Component。不过Client Component可以通过children或props接收Server Component的渲染结果。这种设计确保了服务端代码不会泄露到客户端。}}

{{知识点10}}{{Nuxt 3/4是Vue生态的主流SSR/SSG框架。默认使用Universal Rendering（通用渲染），即在服务端渲染Vue组件为HTML，客户端进行Hydration。Nuxt支持通过一行配置（ssr: false）切换为纯客户端渲染，也支持Hybrid Rendering（混合渲染）——不同路由使用不同渲染策略。}}

{{知识点11}}{{Nuxt的Hybrid Rendering允许在同一应用中混合使用SSR和CSR。某些路由可以设置为服务端渲染（如首页、商品详情页），另一些路由可以设置为客户端渲染（如管理后台）。通过routeRules配置可以精确控制每个路由的渲染策略，包括缓存头、重定向等。}}

{{知识点12}}{{Nuxt支持Edge-Side Rendering（边缘渲染），将SSR逻辑部署到CDN边缘节点。这大大降低了服务端渲染的延迟，因为边缘节点离用户更近。Cloudflare Workers、Vercel Edge Functions等平台都支持边缘SSR运行时。}}

{{知识点13}}{{Astro是2025年内容驱动网站的热门框架，其核心理念是"零JavaScript默认"——服务端生成的HTML不包含任何客户端JavaScript，除非显式声明需要交互的"岛屿"（Islands）。Astro 5引入了Content Layer和Server Islands两大特性。}}

{{知识点14}}{{Astro的Islands Architecture（岛屿架构）是一种部分Hydration策略。页面大部分是静态HTML，只有标注为交互的"岛屿"组件才会被Hydration。不同岛屿可以独立Hydration，支持设置优先级（client:visible、client:idle、client:load等指令），大幅减少客户端JavaScript体积。}}

{{知识点15}}{{Astro 5的Server Islands将岛屿架构扩展到服务端。页面可以包含静态HTML和动态服务端渲染的岛屿。静态部分可以被CDN缓存，动态部分（如用户头像、购物车）按需从服务器获取。每个岛屿独立加载，一个慢的岛屿不会阻塞其他内容的展示。}}

{{知识点16}}{{Remix（已合并入React Router v7）采用全栈路由模型。每个路由模块可以导出loader（服务端数据加载）和action（服务端表单处理）函数。构建时Remix会自动将loader和action从客户端bundle中移除，确保服务端代码不会泄露。这种模型天然支持SSR。}}

{{知识点17}}{{Remix的loader/action模型：loader在GET请求时执行，返回组件所需数据；action在POST/PUT/DELETE请求时执行，处理表单提交。两者都在服务端运行，客户端通过useLoaderData和useActionData获取结果。这种设计使得SSR和CSR之间的数据流保持一致。}}

{{知识点18}}{{Vite内置了SSR支持，提供了底层API（createViteServer的middlewareMode）用于构建自定义SSR应用。典型的Vite SSR应用包含entry-client.js（客户端入口，负责mount）和entry-server.js（服务端入口，负责render）。Vite的SSR模式主要用于框架开发者和需要完全控制SSR流程的场景。}}

{{知识点19}}{{PPR（Partial Prerendering）是Next.js 14+引入的实验性渲染策略。页面的静态外壳在构建时预渲染，动态部分在请求时服务端渲染。静态壳可以立即从CDN返回，动态部分通过流式传输填充。这实现了接近SSG的速度同时保持SSR的动态能力。}}

{{知识点20}}{{CSR（Client-Side Rendering）即客户端渲染，是传统SPA的渲染方式。浏览器下载一个空HTML壳和大量JavaScript，然后在客户端生成页面内容。CSR的优势是开发简单、可以离线工作，劣势是首屏慢、SEO差。2025年纯CSR已较少用于面向用户的应用，主要用于管理后台等场景。}}

{{知识点21}}{{TTFB（Time to First Byte）是衡量SSR性能的关键指标，表示从发出请求到收到第一个字节的时间。SSR通常比SSG的TTFB更高，因为需要实时渲染。通过Streaming SSR、边缘渲染、缓存策略等手段可以优化SSR的TTFB。}}

{{知识点22}}{{FCP（First Contentful Paint）是衡量用户感知加载速度的指标。SSR和SSG都能提供快速的FCP，因为浏览器直接收到渲染好的HTML。CSR的FCP较慢，因为需要先下载、解析并执行JavaScript才能渲染页面。}}

{{知识点23}}{{Hydration Mismatch（水合不匹配）是SSR常见的问题。当服务端渲染的HTML与客户端Hydration时生成的DOM不一致时，React/Vue会报错。常见原因包括：使用Date.now()等时间相关代码、访问window/document等浏览器API、随机数不一致。解决方案包括使用useEffect将浏览器专用代码延迟到客户端执行。}}

{{知识点24}}{{数据获取策略是SSR的核心考量。服务端组件可以直接调用数据库或内部API（无需暴露给客户端），客户端组件需要通过API端点获取数据。Next.js的fetch在Server Components中会自动缓存和去重。Nuxt的useAsyncData和useFetch组合式API支持在服务端和客户端统一获取数据。}}

{{知识点25}}{{缓存策略对SSR性能至关重要。常见策略包括：HTTP缓存（Cache-Control头）、CDN缓存、应用层缓存（如Next.js的Data Cache）、ISR的定时重新验证。Next.js支持revalidatePath和revalidateTag进行按需缓存失效。合理的缓存策略可以将SSR的性能提升到接近SSG的水平。}}

{{知识点26}}{{SSR的SEO优势：搜索引擎爬虫可以直接获取完整的HTML内容，无需执行JavaScript。这对内容网站、电商网站至关重要。SSG的SEO效果同样优秀。CSR需要依赖爬虫的JavaScript执行能力，且可能延迟索引。结构化数据、meta标签、Open Graph等在SSR/SSG中更容易正确设置。}}

{{知识点27}}{{Edge Runtime vs Node.js Runtime：2025年的SSR框架普遍支持两种服务端运行时。Edge Runtime（如Cloudflare Workers、Vercel Edge Functions）启动快、延迟低，但API受限（无完整Node.js API）。Node.js Runtime功能完整但冷启动较慢。Next.js和Nuxt都允许按路由选择运行时。}}

{{知识点28}}{{Serverless SSR是2025年的主流部署模式。每个请求触发一个无服务器函数执行SSR，无需管理服务器。Vercel、Netlify、AWS Lambda等平台提供Serverless SSR支持。优势是自动扩缩容、按需付费，劣势是冷启动延迟和函数执行时间限制。}}

{{知识点29}}{{Next.js的'use client'指令用于声明客户端组件边界。标记后，该文件及其所有导入都包含在客户端bundle中。通过将'use client'添加到特定交互组件而非大型UI部分，可以有效减少客户端JavaScript体积。Server Components中不需要'use client'，它们默认在服务端运行。}}

{{知识点30}}{{Next.js的RSC Payload是React Server Components的序列化格式，是一种紧凑的二进制表示。它包含：Server Components的渲染结果、Client Components的位置引用和JavaScript文件路径、从Server Component传递给Client Component的props。客户端使用RSC Payload来协调服务端和客户端组件树。}}

{{知识点31}}{{Content Layer是Astro 5引入的内容管理抽象层。它提供统一的类型安全API来定义、加载和访问内容，无论来源是什么。内置的glob loader可以从磁盘加载Markdown/MDX文件，自定义loader可以从任何API获取数据，社区loader支持Storyblok、Cloudinary、Notion等第三方CMS。}}

{{知识点32}}{{代码分割与SSR：SSR框架通常在构建时将应用代码分割为多个chunk。服务端按需加载渲染所需的chunk，客户端通过动态import()延迟加载非关键代码。Next.js自动进行基于路由的代码分割。这减少了初始JavaScript传输量，改善了FCP和TTI（Time to Interactive）。}}

{{知识点33}}{{.server和.client文件约定是Remix/React Router中隔离服务端和客户端代码的机制。.server文件或目录中的代码永远不会被打包到客户端bundle中。.client文件中的代码不会包含在服务端bundle中。构建时如果检测到.server文件的代码被客户端引用，构建会失败。}}

{{知识点34}}{{SSR中的CSS处理：SSR需要在服务端也能正确处理CSS。CSS-in-JS方案（如styled-components）需要特殊的SSR支持来提取服务端渲染时的CSS。CSS Modules和Tailwind CSS在SSR中通常更简单，因为它们在构建时生成静态CSS文件。Astro默认将CSS提取为外部样式表。}}

{{知识点35}}{{SSR的错误处理和降级策略：服务端渲染失败时，常见的降级策略包括：回退到客户端渲染（CSR Fallback）、显示预渲染的静态页面、使用stale-while-revalidate返回缓存内容。Next.js的error.tsx和not-found.tsx文件约定用于处理路由级别的错误和404页面。}}

{{知识点36}}{{Nuxt的universal rendering流程：浏览器请求URL → 服务端执行Vue组件渲染为完整HTML → 返回给浏览器（用户立即看到内容） → 浏览器下载JavaScript → Vue接管DOM进行Hydration → 页面可交互。后续客户端导航由Vue Router处理，不再经过服务端。}}

{{知识点37}}{{ISR的工作机制详解：(1)构建时静态生成页面；(2)用户请求时返回缓存的静态页面；(3)revalidate秒数过去后，下一个请求仍返回旧页面，但触发后台重新生成；(4)新页面生成成功后替换旧缓存；(5)后续请求返回新页面。如果重新生成失败，旧页面继续服务。这种stale-while-revalidate模式保证了高可用性。}}

{{知识点38}}{{2025年SSR框架对比：Next.js（React）适合全栈Web应用，生态最大；Nuxt（Vue）适合Vue项目，开发体验优秀；Astro适合内容网站，零JS默认；SvelteKit（Svelte）编译时优化，bundle最小；Remix/React Router v7适合表单密集型应用，Web标准优先。选择取决于技术栈、项目类型和团队偏好。}}

{{知识点39}}{{SSR性能优化实践：(1)使用Streaming SSR渐进式输出HTML；(2)将页面外壳设为静态，动态部分延迟加载；(3)合理使用缓存（CDN、ISR、应用层缓存）；(4)减少服务端数据获取时间（数据库索引、连接池）；(5)使用Edge Runtime降低网络延迟；(6)代码分割减少JavaScript体积。}}

{{知识点40}}{{React 19与SSR的演进：React 19进一步完善了RSC模型，Server Actions成为标准的数据变更方式。Server Components可以async/await直接获取数据，无需useEffect或第三方数据获取库。Actions支持从Server Component或Client Component直接调用服务端逻辑，简化了全栈开发流程。}}
