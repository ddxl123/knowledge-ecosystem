# 2025前端tRPC框架
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}{{tRPC（TypeScript Remote Procedure Call）是一个用于构建和消费全栈类型安全API的框架，无需Schema定义或代码生成。它利用TypeScript的类型推断能力，直接在客户端和服务器之间共享类型，实现编译时的类型检查。tRPC的口号是"Move Fast and Break Nothing"，由Alex "KATT" Johansson创建。}}

{{知识点2}}{{tRPC的核心理念是端到端类型安全（End-to-end Type Safety）。在全栈TypeScript项目中，客户端调用API时能获得完整的类型推断和自动补全，包括输入参数、返回值和错误类型的类型安全。当修改服务端过程的输入或输出类型时，客户端代码会在编译时报错，无需运行时才发现问题。}}

{{知识点3}}{{tRPC结合了REST和GraphQL的概念优势：像REST一样简单直接，不需要额外的查询语言；像GraphQL一样提供精确的数据获取和类型安全。但与两者不同的是，tRPC不需要Schema定义文件、不需要代码生成步骤、不需要特殊的查询语法，完全依靠TypeScript类型系统自动推断。}}

{{知识点4}}{{tRPC的初始化通过initTRPC.create()完成，整个应用应只初始化一次。初始化时可通过链式调用配置Context类型（.context<T>()）、元数据类型（.meta<T>()）以及运行时选项（如数据转换器、错误格式化器等）。典型的初始化代码为：const t = initTRPC.context<Context>().meta<Meta>().create({...})。}}

{{知识点5}}{{Router（路由器）是tRPC中组织API的过程集合。通过t.router()创建，传入一个对象，其中每个键对应一个过程或子路由。路由器的类型（AppRouter）会被导出并在客户端导入使用，但只导入类型声明而非实际代码，确保服务端代码不会泄露到客户端。}}

{{知识点6}}{{Procedure（过程）是tRPC中暴露给客户端的函数，分为三种类型：Query用于获取数据（通常不修改数据），Mutation用于发送数据（创建/更新/删除操作），Subscription用于建立实时事件流。过程使用不可变的Builder模式构建，支持链式调用添加输入验证、中间件等功能。}}

{{知识点7}}{{publicProcedure是tRPC中最基础的过程类型，通过导出t.procedure重命名而来。它是所有其他命名过程的基础，代表无需认证即可访问的公共端点。实际项目中通常会基于publicProcedure创建authedProcedure等自定义基础过程，以实现代码和行为复用。}}

{{知识点8}}{{tRPC的输入验证通常使用Zod库（也支持任何符合标准的验证库）。通过在过程定义上链式调用.input(schema)来指定输入验证规则。Zod schema会同时用于运行时验证和TypeScript类型推断，确保输入数据的类型安全。输入验证失败时会自动返回BAD_REQUEST错误。}}

{{知识点9}}{{Middleware（中间件）通过t.procedure.use()添加到过程中，在过程执行前后执行。中间件必须调用opts.next()并返回其结果。典型用例包括：身份验证检查、请求计时日志、权限校验等。中间件支持"Context Extension"，可以在类型安全的前提下动态修改和扩展上下文对象。}}

{{知识点10}}{{Context（上下文）是所有tRPC过程共享的请求级别数据，通常存放认证信息、数据库连接等。通过initTRPC.context<T>()定义上下文类型，通过createContext()函数为每个请求创建运行时上下文。createContext在每次请求时调用一次，同一请求内所有过程（包括批处理中的多个过程）共享同一上下文。}}

{{知识点11}}{{TRPCError是tRPC提供的专用错误类，用于在过程中抛出错误。创建方式为throw new TRPCError({ code: 'ERROR_CODE', message: '...', cause: originalError })。cause参数可选，用于保留原始错误的堆栈信息。所有过程中的错误都会经过onError处理方法后才发送给客户端。}}

{{知识点12}}{{tRPC定义了完整的错误代码体系，每种错误对应不同的HTTP状态码。常用错误码包括：PARSE_ERROR(400)解析错误、BAD_REQUEST(400)客户端错误、UNAUTHORIZED(401)未认证、FORBIDDEN(403)无权限、NOT_FOUND(404)未找到、TIMEOUT(408)超时、TOO_MANY_REQUESTS(429)频率限制、INTERNAL_SERVER_ERROR(500)服务器内部错误等。}}

{{知识点13}}{{tRPC支持请求批处理（Request Batching），同一时间发出的多个请求会自动合并为一个HTTP请求。这通过httpBatchLink实现，可以显著减少网络请求数量。批处理中所有过程共享同一个Context，由createContext()在请求开始时调用一次创建。}}

{{知识点14}}{{Links（链接）是tRPC客户端中自定义数据流的机制。链接以数组形式配置在客户端，形成链接链（Link Chain）。请求时按数组顺序从左到右执行，响应时反向从右到左执行。每个链接可以修改操作、添加副作用（如日志记录）或处理结果。链接是tRPC客户端的核心扩展机制。}}

{{知识点15}}{{httpBatchLink是tRPC最常用的终端链接（Terminating Link），用于通过HTTP发送请求并支持自动批处理。配置时需指定API的URL，可选配置headers（支持异步函数，用于动态添加认证头等）。同时支持配置maxURLLength来控制批处理请求的最大URL长度。}}

{{知识点16}}{{httpLink是不带批处理功能的HTTP终端链接。每个请求单独发送一个HTTP请求。适用于不需要批处理或需要更细粒度控制每个请求的场景。与httpBatchLink类似，支持配置url、headers和maxURLLength等选项。}}

{{知识点17}}{{loggerLink是一个内置的调试链接，用于在控制台记录tRPC请求和响应的日志信息。开发环境下默认启用。通过LoggerLinkOptions可以配置日志级别、自定义日志函数等。是开发调试tRPC API的有力工具。}}

{{知识点18}}{{tRPC支持创建自定义Link。自定义链接是一个三层嵌套函数：最外层是初始化阶段（整个应用只执行一次，适合存储缓存等状态），中间层接收op（操作信息）和next（调用下一个链接的函数），最内层返回一个Observable用于传播结果。自定义链接可用于实现认证刷新、请求重试、指标收集等功能。}}

{{知识点19}}{{tRPC与TanStack React Query的集成是前端最常用的集成方式。通过@trpc/tanstack-react-query包提供createTRPCContext()创建TRPCProvider和useTRPC hook。集成后可以使用标准的useQuery/useMutation hooks，同时享受tRPC的端到端类型安全和React Query的缓存、重试、乐观更新等功能。}}

{{知识点20}}{{tRPC客户端通过createTRPCClient<AppRouter>()创建，底层使用JavaScript Proxy实现。客户端对象的方法调用会自动映射到服务端过程路径，如client.user.get.query()对应服务端的user.get过程。客户端必须传入links数组，至少包含一个终端链接（如httpBatchLink）。}}

{{知识点21}}{{Vanilla客户端（@trpc/client）是tRPC的框架无关客户端，可在任何JavaScript运行环境中使用。不需要React或任何UI框架依赖。通过import type只导入AppRouter类型声明，确保不会将服务端代码引入客户端。适合Node.js脚本、移动端或非React前端框架使用。}}

{{知识点22}}{{Superjson是tRPC推荐的数据转换器（Data Transformer），允许在客户端和服务端之间透明传输Date、Map、Set等JavaScript特殊类型。通过initTRPC.create({ transformer: superjson })配置在服务端，同时在客户端的httpLink/httpBatchLink中也需配置相同的transformer。}}

{{知识点23}}{{Devalue是另一种数据转换器选项，相比superjson更注重性能和紧凑的负载体积，但牺牲了部分人类可读性。使用parse和stringify方法（而非JSON.parse/stringify），具有XSS防护能力。配置方式与superjson类似，需同时在服务端和客户端设置。支持为上传和下载方向使用不同的转换器。}}

{{知识点24}}{{tRPC通过适配器（Adapters）与不同的后端框架集成。tRPC本身不是一个独立的服务器，需要借助宿主系统运行。适配器作为宿主系统和tRPC API之间的粘合层，统一提供createContext和onError等配置能力。常见的适配器包括Standalone、Express、Fastify、Next.js、AWS Lambda和Fetch等。}}

{{知识点25}}{{Standalone适配器（@trpc/server/adapters/standalone）是最简单的后端适配器，用于创建标准的Node.js HTTP服务器。适合本地开发或服务器化基础设施，无需现有HTTP服务器即可快速启动。通过createHTTPServer()或createHTTPHandler()创建服务器实例，配置router和createContext即可。}}

{{知识点26}}{{Express适配器允许将tRPC挂载到现有的Express应用中，通常作为/api/trpc路径下的端点。通过createExpressMiddleware()创建中间件，传入router和createContext。适合已有Express后端的项目渐进式引入tRPC，不需要一次性迁移所有API端点。}}

{{知识点27}}{{Next.js适配器是tRPC最流行的全栈集成方式，支持Pages Router和App Router。在Next.js中，tRPC过程通常定义在pages/api/trpc/[trpc].ts或app/api/trpc/[trpc]/route.ts中。结合next.js的SSR能力，可以使用createServerSideHelpers进行服务端预取，实现更好的首屏加载性能。}}

{{知识点28}}{{Fetch适配器基于标准的Fetch API，适用于边缘运行时（Edge Runtime）和多种现代全栈框架。可与Next.js App Router、Astro、Remix、SolidStart等框架配合使用。通过createFetchHandler()创建处理器，是最通用的服务端适配器选择。}}

{{知识点29}}{{tRPC支持实时订阅（Subscriptions），用于在客户端和服务端之间建立持久的实时连接。支持两种传输协议：WebSocket（需要单独的WebSocket服务器）和Server-Sent Events/SSE（推荐，更易设置）。订阅过程使用t.procedure.subscription(async function*() {...})定义，使用async generator yield事件数据。}}

{{知识点30}}{{tracked()是tRPC提供的用于订阅重连的辅助函数。当订阅断开时，客户端会自动尝试重连并发送最后接收到的事件ID（lastEventId）。服务端可通过opts.input中的lastEventId来获取客户端最后收到的事件，从而实现断点续传。对于SSE传输，lastEventId通过HTTP头传播。}}

{{知识点31}}{{tRPC的错误格式化（Error Formatting）允许自定义发送给客户端的错误响应格式。通过initTRPC.create({ errorFormatter: ... })配置。默认情况下，开发环境（isDev=true）会包含错误堆栈信息，生产环境则隐藏堆栈。可通过getHTTPStatusCodeFromError()辅助函数从TRPCError中提取HTTP状态码。}}

{{知识点32}}{{tRPC支持元数据（Metadata），通过.initTRPC.meta<T>()定义元数据类型，可在过程定义时通过.meta({...})附加元数据。元数据常与中间件配合使用，例如标记过程是否需要认证、设置速率限制等。中间件可以通过opts.meta读取过程的元数据信息。}}

{{知识点33}}{{tRPC支持内联子路由（Inline Sub-Router），允许在路由器定义中直接嵌套对象结构，无需显式调用router()。例如：router({ nested: { proc: publicProcedure.query(...) } })等同于router({ nested: router({ proc: publicProcedure.query(...) }) })。这使路由定义更加简洁。}}

{{知识点34}}{{tRPC支持服务端调用（Server-side Calls），通过router.createCaller(context)创建一个可以内部调用过程的调用者实例，无需通过HTTP请求。这在服务端渲染、脚本、测试等场景中非常有用。调用者的方法与客户端API一致，同样享受完整的类型安全。}}

{{知识点35}}{{tRPC的可复用基础过程（Base Procedures）模式是推荐的代码复用方式。通过在publicProcedure上链式调用.use()和.input()创建命名的基础过程，如authedProcedure（需要登录）、adminProcedure（需要管理员权限）等。所有应用过程都基于这些基础过程构建，实现认证、授权等通用逻辑的复用。}}

{{知识点36}}{{tRPC使用不可变的Builder模式构建过程。每次链式调用（.input()、.use()、.query()等）都会创建一个新的过程实例，不会修改原始对象。这意味着可以从同一个基础过程派生出多个不同的最终过程，每个都有自己的输入验证、中间件和处理逻辑。}}

{{知识点37}}{{tRPC的.query()和.mutation()方法接收一个解析器函数（resolver function），该函数接收一个包含ctx（上下文）、input（已验证的输入）和signal（AbortSignal）等属性的参数对象。解析器函数的返回值类型会自动推断并传递给客户端，实现完整的类型安全链路。}}

{{知识点38}}{{tRPC内置支持AbortSignal，允许客户端在请求未完成时取消操作。在过程中通过opts.signal访问AbortSignal，可以将其传递给异步操作（如数据库查询或外部API调用），实现请求取消的优雅传播。这在用户快速切换页面等场景中避免不必要的资源消耗。}}

{{知识点39}}{{tRPC的客户端-服务端类型共享通过import type实现。客户端只导入AppRouter的类型声明而非实际代码，TypeScript编译器会在编译时移除类型导入。这确保了：1）服务端代码不会泄露到客户端bundle；2）客户端获得完整的类型推断；3）修改服务端类型会立即在客户端产生编译错误。}}

{{知识点40}}{{tRPC适用于新建项目和已有项目（Brownfield Projects）。对于已有项目，推荐的渐进式迁移策略是：先对新功能使用tRPC，后续逐步迁移现有端点。tRPC可以作为端点挂载在现有REST API上，不需要一次性重写整个后端。社区提供了大量开源项目作为参考，如T3 Stack等。}}
