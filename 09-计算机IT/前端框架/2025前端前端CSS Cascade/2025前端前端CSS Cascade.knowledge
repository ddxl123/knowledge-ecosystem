# 2025前端前端CSS Cascade
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}{{CSS Cascade（级联）是CSS算法的核心机制，用于当多个CSS声明作用于同一元素的同一属性时，决定哪个声明的值生效。它通过来源（origin）、重要性（importance）、特异性（specificity）、作用域接近度（scoping proximity）和出现顺序（order of appearance）五个步骤依次筛选。}}

{{知识点2}}{{CSS声明的三种来源类型：User-agent样式表（浏览器默认样式）、Author样式表（开发者编写的样式）、User样式表（用户自定义样式）。Author样式在normal情况下优先于User和User-agent样式。}}

{{知识点3}}{{Cascade的完整优先级顺序（从低到高）：1.user-agent normal → 2.user normal → 3.author normal → 4.CSS关键帧动画 → 5.author !important → 6.user !important → 7.user-agent !important → 8.CSS过渡（transitions）。}}

{{知识点4}}{{!important声明在级联中的行为是反转的：normal声明中author优先于user优先于user-agent，但!important声明中user-agent !important优先于user !important优先于author !important。}}

{{知识点5}}{{CSS动画（@keyframes）的属性值优先于所有normal声明，但低于!important声明。CSS过渡（transition）的属性值优先于一切，包括!important声明。}}

{{知识点6}}{{Cascade Layers（级联层）是CSS Cascade Level 5引入的特性，通过@layer at-rule声明，允许开发者显式控制样式层的优先级顺序。层内声明的normal样式优先级低于非层声明。}}

{{知识点7}}{{@layer的声明顺序决定优先级：后声明的层优先级更高。例如@layer A; @layer B; @layer C; 中，C层的normal样式优先于B层，B层优先于A层。}}

{{知识点8}}{{层外（unlayered）的normal样式优先于所有层内normal样式，无论特异性如何。这是层机制的关键设计：未分层的样式始终"赢"。}}

{{知识点9}}{{!important在层中的行为是反转的：!important声明在先声明的层中优先级更高。即A层的!important > B层的!important > C层的!important > 非层!important。}}

{{知识点10}}{{@layer的三种使用方式：1.块声明 @layer name { rules }；2.导入式 @import url("file.css") layer(name)；3.匿名层 @layer { rules }。未被分配到任何层的样式被视为最后声明的匿名层的一部分。}}

{{知识点11}}{{Cascade Layers可以嵌套，形成嵌套层命名空间。例如@layer framework { @layer utilities { ... } }，通过layer.utilities方式引用嵌套层。}}

{{知识点12}}{{@layer声明可以不带花括号仅创建层名而不添加样式：@layer base, utilities, components; 这在初始化层顺序时非常有用。}}

{{知识点13}}{{Specificity（特异性/权重）是CSS级联算法的第三步。当来源和重要性相同时，特异性更高的选择器胜出。特异性用三列值表示：ID - CLASS - TYPE。}}

{{知识点14}}{{ID列：每包含一个ID选择器（如#example），加1-0-0。ID选择器在特异性中权重最高，一个ID选择器即可超越任意数量的CLASS和TYPE选择器。}}

{{知识点15}}{{CLASS列：每包含一个类选择器（如.myClass）、属性选择器（如[type="radio"]）或伪类（如:hover、:nth-of-type），加0-1-0。}}

{{知识点16}}{{TYPE列：每包含一个类型选择器（如p、h1）或伪元素（如::before、::placeholder），加0-0-1。类型选择器特异性最低。}}

{{知识点17}}{{通用选择器(*)和:where()伪类不计入特异性，值为0-0-0。但组合符（+、>、~、空格、||）也不增加特异性权重。}}

{{知识点18}}{{:is()、:not()、:has()伪类本身不增加特异性，但其参数中的选择器会贡献特异性。取参数列表中特异性最高的选择器的值。例如:is(p, #fakeId)的特异性为1-0-0。}}

{{知识点19}}{{CSS嵌套（nesting）的行为类似:is()伪类。嵌套规则的特异性取决于逗号分隔选择器列表中特异性最高的选择器。例如 p, #fakeId { span { } } 中span的特异性为1-0-1。}}

{{知识点20}}{{内联样式（inline styles，即style属性）的特异性可视为1-0-0-0，高于任何选择器。normal内联样式优先于所有其他author normal样式，唯一能覆盖内联样式的方法是使用!important。}}

{{知识点21}}{{三列比较规则：从左到右逐列比较，ID列大的直接胜出；ID列相同比CLASS列；CLASS列也相同比TYPE列；三列都相同则比较出现顺序（后声明的胜出）。}}

{{知识点22}}{{特异性的实际应用：#myElement { color: green; } (1-0-0) 会胜过 .bodyClass .sectionClass .parentClass [id="myElement"] { color: yellow; } (0-4-0)，因为1 > 0在ID列比较中直接胜出。}}

{{知识点23}}{{@scope at-rule允许定义精确的作用域范围，包含作用域根（scope root）和可选的作用域限制（scope limit）。@scope (.article-body) to (figure) { } 定义了一个"甜甜圈"作用域。}}

{{知识点24}}{{@scope在级联中的特异性：@scope内的裸选择器和&嵌套选择器等效于:where(:scope)前置，:where()零特异性，因此不额外增加权重。但显式使用:pseudo-class会增加0-1-0的CLASS级特异性。}}

{{知识点25}}{{Scoping proximity（作用域接近度）是级联算法的第四步：当两个选择器在同一来源层中特异性相同时，距离作用域根DOM层级更近的声明胜出。这是CSS Cascade Level 6引入的概念。}}

{{知识点26}}{{Order of appearance（出现顺序）是级联的最后一步：当来源、重要性、特异性、作用域接近度都相同时，代码中后出现的声明胜出。这是最常见的"覆盖"机制。}}

{{知识点27}}{{CSS Reset（如normalize.css）的作用：通过在author样式中设置常见属性的已知状态，消除不同浏览器user-agent样式表之间的差异，为跨浏览器一致性打下基础。}}

{{知识点28}}{{层中的!important优先级反转是设计意图：这使得底层基础样式（如reset层）可以通过!important"锁定"关键属性，而上层组件样式无法轻易覆盖。这对设计系统和组件库非常有用。}}

{{知识点29}}{{@layer在实际项目中的最佳实践：定义层顺序（如@layer reset, base, components, utilities），utilities层最高优先级确保工具类可以覆盖组件样式，reset层最低优先级。}}

{{知识点30}}{{层与!important结合的设计系统模式：将基础设计token放入@layer base，将组件样式放入@layer components，base层的!important声明可以确保token不被组件覆盖，实现"不可破坏"的设计约束。}}

{{知识点31}}{{CSS级联与CSS继承的区别：级联决定多个声明中哪个生效，继承决定没有显式声明时属性值如何从父元素传递。继承的属性优先级最低，任何显式声明都会覆盖继承值。}}

{{知识点32}}{{@scope的inline用法：将@scope放在<style>标签内且省略prelude时，自动作用域到父元素。如<section class="article-body"><style>@scope { img { ... } }</style></section>。}}

{{知识点33}}{{@scope的作用域限制（scope limit）是排他的（exclusive），即limit元素本身不被选中。可以通过scope limit > *使下界变为包含的，或scope root > *使上界变为排他的。}}

{{知识点34}}{{:scope伪类在@scope块内提供直接选择作用域根的便捷方式。@scope (.feature) { :scope { background: purple; } } 直接为作用域根应用样式。}}

{{知识点35}}{{!important的使用原则：应尽量避免使用!important，因为它破坏了级联的自然流程。在需要覆盖第三方库样式或用户样式时可谨慎使用，每次使用都应添加注释说明原因。}}

{{知识点36}}{{CSS特异性陷阱：过度使用ID选择器会导致特异性过高，使样式难以覆盖。现代CSS最佳实践建议优先使用类选择器，保持特异性尽可能低和平坦。}}

{{知识点37}}{{@layer的命名空间隔离：层内样式对外部选择器不可见（不增加特异性），但层内选择器可以匹配外部元素。层只影响优先级计算，不影响选择器匹配。}}

{{知识点38}}{{CSS @import与@layer的结合：@import url("styles.css") layer(components)可以将外部样式表导入指定层，实现模块化的层管理。}}

{{知识点39}}{{特异性与!important的关系：!important在技术上与特异性无关，但它与特异性和级联直接交互。在同一层和来源中，!important声明覆盖任何其他声明，不论特异性。}}

{{知识点40}}{{层中!important的完整优先级（author样式，从低到高）：A层normal → B层normal → C层normal → 非层normal → 内联normal → 动画 → 非层!important → C层!important → B层!important → A层!important → 内联!important → 过渡。}}
