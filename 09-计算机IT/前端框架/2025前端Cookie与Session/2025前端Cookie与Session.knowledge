# 2025前端Cookie与Session
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}{{Cookie基本概念：Cookie是服务器发送到用户浏览器并保存在本地的一小段数据，浏览器会在后续向同一服务器发起请求时自动携带Cookie。Cookie主要用于会话管理（登录状态、购物车）、个性化（语言偏好、UI主题）和跟踪（用户行为分析）三大场景。}}

{{知识点2}}{{Cookie的工作机制：服务器通过Set-Cookie响应头向浏览器发送Cookie，浏览器保存后，在后续每次请求该服务器时通过Cookie请求头将数据发回。例如服务器返回"Set-Cookie: yummy_cookie=choco"，浏览器后续请求会自动携带"Cookie: yummy_cookie=choco"。}}

{{知识点3}}{{Set-Cookie响应头：服务器使用Set-Cookie响应头来设置Cookie，格式为"name=value"。可以在一个响应中设置多个Cookie，每个Set-Cookie头设置一个独立的Cookie。支持的属性包括Expires、Max-Age、Domain、Path、Secure、HttpOnly、SameSite等。}}

{{知识点4}}{{Cookie请求头：浏览器在向服务器发送请求时，会将匹配的Cookie通过Cookie请求头发送。多个Cookie之间用分号和空格分隔，格式为"Cookie: name1=value1; name2=value2"。只有满足Domain、Path、Secure等条件的Cookie才会被发送。}}

{{知识点5}}{{会话Cookie（Session Cookie）：没有设置Expires或Max-Age属性的Cookie称为会话Cookie，会在当前会话结束后被删除。浏览器定义"当前会话"结束的时机，某些浏览器使用会话恢复功能可能导致会话Cookie无限期存在。}}

{{知识点6}}{{持久性Cookie（Persistent Cookie）：通过Expires属性指定过期日期或通过Max-Age属性指定有效时长的Cookie。Expires设置的是绝对时间（GMT格式），Max-Age设置的是相对秒数。当两者同时设置时，Max-Age优先级更高。}}

{{知识点7}}{{Cookie的Expires属性：指定Cookie的过期日期，值为HTTP日期时间戳（GMT格式）。例如"Set-Cookie: id=a3fWa; Expires=Wed, 21 Oct 2025 07:28:00 GMT"。注意：日期和时间是相对于客户端的，如果服务器与客户端时间不同步可能导致误差。}}

{{知识点8}}{{Cookie的Max-Age属性：指定Cookie从设置开始的有效秒数。例如"Max-Age=2592000"表示Cookie在30天后过期。Max-Age比Expires更可靠，因为它不依赖客户端时钟。Max-Age为0或负数时立即删除Cookie。}}

{{知识点9}}{{Cookie的Domain属性：指定哪些主机可以接受该Cookie。如果不设置Domain，默认为当前host（不包含子域名）。如果设置了Domain，通常会包含子域名。例如设置Domain=mozilla.org，则developer.mozilla.org也会收到该Cookie。}}

{{知识点10}}{{Cookie的Path属性：指定URL路径前缀，只有请求URL匹配该路径时才会发送Cookie。路径分隔符为"/"，子路径也会被匹配。例如Path=/docs会匹配/docs、/docs/、/docs/Web/、/docs/Web/HTTP，但不匹配/或/docsets。}}

{{知识点11}}{{Cookie的Secure属性：标记为Secure的Cookie只通过HTTPS加密请求发送给服务器，不会通过不安全的HTTP发送（localhost除外）。Secure属性防止中间人攻击者窃取Cookie，但不能阻止有权访问客户端磁盘或JavaScript的人读取修改Cookie。}}

{{知识点12}}{{Cookie的HttpOnly属性：设置了HttpOnly的Cookie无法通过JavaScript的document.cookie API访问，只能由服务器操作。这对存储会话ID等敏感信息的Cookie非常重要，可以有效缓解XSS攻击窃取Cookie的风险。}}

{{知识点13}}{{Cookie的SameSite属性：控制Cookie在跨站请求中的发送行为，有三个值：Strict（仅在同站请求中发送）、Lax（在同站请求和部分跨站导航中发送）和None（在同站和跨站请求中都发送，但必须同时设置Secure）。未设置SameSite时默认为Lax。}}

{{知识点14}}{{SameSite=Strict：Cookie仅在请求来源站点与Cookie来源站点完全一致时发送。用户从外部站点通过链接点击进入时不会携带该Cookie。适用于需要最高安全级别的场景，但可能导致用户体验不佳（如从外部链接进入后需要重新登录）。}}

{{知识点15}}{{SameSite=Lax：与Strict类似，但在用户导航到Cookie源站点时会发送Cookie（如点击链接）。这是大多数浏览器的默认值。对于GET请求的顶级导航会发送Cookie，但POST请求不会。适用于大多数认证Cookie场景。}}

{{知识点16}}{{SameSite=None：允许Cookie在同站和跨站请求中都发送，但必须同时设置Secure属性（要求安全上下文）。适用于需要跨站发送Cookie的场景，如第三方嵌入内容。不设置Secure属性时浏览器会拒绝该Cookie。}}

{{知识点17}}{{Cookie前缀__Host-：使用此前缀的Cookie必须同时满足：设置Secure属性、从安全来源发送、不包含Domain属性、Path设为"/"。这使Cookie成为"域锁定"的，提供最强的安全保障。}}

{{知识点18}}{{Cookie前缀__Secure-：使用此前缀的Cookie必须设置Secure属性并从安全来源发送。限制比__Host-弱，允许设置Domain属性。两个前缀用于在安全来源上断言Cookie的特定事实，如果不符合条件浏览器会拒绝设置。}}

{{知识点19}}{{JavaScript操作Cookie：通过document.cookie属性可以读取和设置Cookie。设置Cookie使用"document.cookie = 'name=value'"语法，读取时返回当前域下所有非HttpOnly的Cookie字符串。不能通过fetch()或XMLHttpRequest直接修改Cookie。}}

{{知识点20}}{{Cookie的大小限制：浏览器通常限制每个Cookie最大约4KB，每个域名下的Cookie总数通常限制在几百个。由于Cookie会在每次请求中发送，过多或过大的Cookie会影响网络性能，特别是移动网络。现代Web存储API（localStorage、IndexedDB）更适合大数据存储。}}

{{知识点21}}{{第一方Cookie与第三方Cookie：第一方Cookie的域与当前页面域一致，由当前访问的网站设置。第三方Cookie的域与当前页面域不同，通常由嵌入的第三方资源（如广告、追踪器）设置。第三方Cookie主要用于跨站追踪和广告投放，多数浏览器已开始限制或阻止。}}

{{知识点22}}{{删除Cookie的方法：要立即删除Cookie，使用相同的name、path和domain再次设置Cookie，将Expires设为过去的时间或将Max-Age设为0或负数。也可使用Clear-Site-Data响应头清除指定域的所有Cookie。}}

{{知识点23}}{{Session（会话）基本概念：Session是服务器端存储的用户会信息。服务器为每个用户创建一个唯一的Session ID，通过Cookie传递给浏览器，后续请求通过Session ID识别用户。Session数据存储在服务器端（内存、数据库、Redis等），比Cookie更安全。}}

{{知识点24}}{{Session与Cookie的核心区别：Cookie存储在客户端浏览器中，每次请求自动携带，存在被篡改和窃取的风险；Session存储在服务器端，客户端只保存Session ID，安全性更高。Cookie有大小限制（约4KB），Session理论上不受此限。Cookie可以设置长期有效，Session通常有较短的超时时间。}}

{{知识点25}}{{Session的工作流程：1）用户首次访问时服务器创建Session并生成Session ID；2）Session ID通过Set-Cookie发送到浏览器；3）浏览器后续请求携带Session ID的Cookie；4）服务器根据Session ID查找对应的Session数据；5）Session超时或用户登出时销毁Session。}}

{{知识点26}}{{Session固定攻击（Session Fixation）：攻击者获取一个有效的Session ID，诱导用户使用该ID进行认证，认证成功后攻击者即可劫持用户会话。防护方法：用户认证成功后必须重新生成Session ID（会话再生），即使已有Session也要重新发送新的Session Cookie。}}

{{知识点27}}{{会话固定攻击的攻击方式：1）通过URL参数传递Session ID诱导点击；2）通过隐藏表单字段传递；3）通过XSS注入document.cookie代码设置Cookie；4）通过META标签注入Set-Cookie；5）通过拦截HTTP响应修改Set-Cookie头。}}

{{知识点28}}{{Session劫持（Session Hijacking）：攻击者窃取已认证用户的Session ID来冒充用户。常见手段包括XSS攻击获取Cookie、网络嗅探、预测Session ID等。防护措施：使用HTTPS传输、设置HttpOnly和Secure属性、定期轮换Session ID、绑定IP地址或User-Agent。}}

{{知识点29}}{{CSRF攻击与Cookie防护：跨站请求伪造利用浏览器自动携带Cookie的特性，诱导用户在已登录状态下执行非预期操作。SameSite属性是防御CSRF的有效手段（Strict或Lax模式）。传统的防护方式还包括CSRF Token、检查Referer/Origin头等。}}

{{知识点30}}{{XSS攻击与Cookie防护：跨站脚本攻击可注入恶意JavaScript读取Cookie。防护方法：敏感Cookie设置HttpOnly属性阻止JS访问；设置Content-Security-Policy限制脚本来源；对用户输入进行转义和验证；使用__Host-前缀加强Cookie安全。}}

{{知识点31}}{{Cookie的安全最佳实践：1）始终在生产环境使用HTTPS并设置Secure属性；2）敏感Cookie设置HttpOnly防止XSS窃取；3）使用SameSite=Lax或Strict防止CSRF；4）敏感Cookie设置较短的过期时间；5）使用__Host-或__Secure-前缀；6）避免在Cookie中存储敏感数据，使用不透明标识符。}}

{{知识点32}}{{第三方Cookie的隐私法规：欧盟GDPR、欧盟电子隐私指令、加州消费者隐私法等法规要求网站告知用户Cookie使用情况，允许用户选择退出，且在不接收Cookie的情况下仍可使用大部分服务。全球主要浏览器正在逐步淘汰第三方Cookie（Google Chrome的Privacy Sandbox计划）。}}

{{知识点33}}{{LocalStorage vs Cookie vs SessionStorage：LocalStorage持久存储（除非手动清除），同源页面共享，容量约5-10MB；SessionStorage仅在当前标签页会话有效，关闭标签页即清除；Cookie每次请求自动发送到服务器，容量约4KB。LocalStorage/SessionStorage不会自动发送到服务器。}}

{{知识点34}}{{Cookie Store API：现代浏览器提供的异步Cookie操作API，相比document.cookie更高效。支持get()、getAll()、set()、delete()方法，返回Promise。通过cookieStore事件监听Cookie变更。是Document.cookie的现代替代方案，但兼容性仍在完善中。}}

{{知识点35}}{{JWT（JSON Web Token）与传统Session：JWT是一种无状态认证方案，Token存储在客户端（通常在Cookie或localStorage），服务器不需要存储Session信息。优点是易于水平扩展，缺点是Token无法在服务端主动失效。传统Session需要服务器存储状态，但支持主动注销和更细粒度的控制。}}

{{知识点36}}{{Cookie与Session的性能优化：减少Cookie数量和大小以降低请求体积；静态资源使用不同域名避免携带Cookie；对非敏感数据考虑使用localStorage替代；Session数据使用Redis等内存数据库加速读取；合理设置Session过期时间避免服务器内存膨胀。}}

{{知识点37}}{{Cookie的Domain匹配规则：Domain值必须是当前域名或其父域名。例如在sub.example.com上可以设置Domain=example.com但不能设置Domain=other.com。设置Domain后Cookie对所有子域名可见，不设置Domain则仅对当前精确域名可见。}}

{{知识点38}}{{跨域场景下的Cookie处理：跨域请求（CORS）默认不携带Cookie，需要设置fetch的credentials: 'include'或XMLHttpRequest的withCredentials=true，同时服务器响应需设置Access-Control-Allow-Credentials: true，且不能使用通配符*作为Access-Control-Allow-Origin。}}

{{知识点39}}{{SameSite=None的兼容性问题：较旧的浏览器（Chrome 80之前、Safari 12之前）不支持SameSite=None，会将其视为SameSite=None（允许跨站）或忽略。需要同时设置Secure属性，且在不支持的浏览器上可能需要额外的降级方案（如在URL中传递Session ID）。}}

{{知识点40}}{{服务端Session存储方案：1）内存存储：重启丢失，不支持集群；2）文件存储：IO瓶颈；3）数据库（MySQL等）：持久化但IO较慢；4）Redis/Memcached：高速内存缓存，支持过期自动清理和集群，是生产环境的首选方案。Express.js中常用express-session配合connect-redis。}}
