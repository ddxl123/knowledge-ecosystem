# 2025前端Navigation API
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}
Navigation API 是一个现代浏览器 API，旨在替代 History API，为单页应用（SPA）提供更强大的导航控制能力。它通过 window.navigation 属性访问，返回一个全局 Navigation 对象，每个窗口对象都有对应的 navigation 实例。

{{知识点2}}
Navigation API 的核心设计理念是将所有页面导航集中到一个地方处理。与 History API 不同，Navigation API 可以检测和响应所有类型的导航，包括链接点击、表单提交、程序化导航等，非常适合 SPA 框架的路由功能。

{{知识点3}}
Navigation API 于 2026 年 1 月达到 Baseline 状态，即在最新的设备和浏览器版本上均可使用。这意味着它已成为现代 Web 开发的标准工具。

{{知识点4}}
Navigation 接口是 Navigation API 的核心，通过 Window.navigation 属性访问。它允许在当前窗口中控制所有导航操作，包括程序化发起导航、检查导航历史条目以及在导航发生时进行管理。

{{知识点5}}
Navigation.currentEntry 是一个只读属性，返回 NavigationHistoryEntry 对象，表示用户当前所在的历史条目。这是获取当前页面状态的首选方式。

{{知识点6}}
Navigation.entries() 方法返回一个 NavigationHistoryEntry 对象数组，表示所有现有的历史条目。Navigation API 只暴露当前浏览上下文中与当前页面同源的历史条目，不包括 iframe 内的导航或跨域导航。

{{知识点7}}
Navigation.canGoBack 是一个只读属性，返回布尔值，表示是否可以在导航历史中向后导航（即 currentEntry 不是列表中的第一个条目）。

{{知识点8}}
Navigation.canGoForward 是一个只读属性，返回布尔值，表示是否可以在导航历史中向前导航（即 currentEntry 不是列表中的最后一个条目）。

{{知识点9}}
Navigation.navigate(url, options) 方法用于导航到指定 URL，创建新的导航历史条目。options 参数可包含 state 属性来设置历史条目状态，以及 history 属性来指定是 "push" 还是 "replace"。

{{知识点10}}
Navigation.back() 方法用于向后导航一个历史条目，Navigation.forward() 方法用于向前导航一个历史条目。两者都返回包含 committed 和 finished 两个 Promise 的对象。

{{知识点11}}
Navigation.traverseTo(key) 方法用于导航到由 key 标识的特定历史条目。key 通过 NavigationHistoryEntry.key 属性获取，代表历史条目在列表中的槽位。

{{知识点12}}
Navigation.reload(options) 方法用于重新加载当前 URL，可以同时更新历史条目的状态。

{{知识点13}}
Navigation.updateCurrentEntry(options) 方法用于更新当前条目的状态，适用于状态变更独立于导航或重新加载的场景，例如页面中展开/折叠元素的状态。

{{知识点14}}
Navigation.transition 是一个只读属性，返回 NavigationTransition 对象，表示正在进行的导航状态。如果没有导航正在进行，则返回 null。

{{知识点15}}
Navigation.activation 是一个只读属性，返回 NavigationActivation 对象，包含关于最近一次跨文档导航的信息，即"激活"当前文档的导航。

{{知识点16}}
navigate 事件是 Navigation API 最重要的事件，当任何类型的导航被触发时都会触发。这是实现集中式路由控制的关键。

{{知识点17}}
NavigateEvent 是 navigate 事件的事件对象，包含导航的详细信息：destination（导航目标）、navigationType（导航类型）、canIntercept（是否可拦截）、formData（表单数据）、hashChange（是否为片段导航）、downloadRequest（下载请求）等。

{{知识点18}}
NavigateEvent.intercept() 方法是 Navigation API 的核心能力，用于拦截导航并定义自定义行为。它可以将导航转换为同文档导航，接受 handler 和 precommitHandler 回调函数，以及 focusReset 和 scroll 选项。

{{知识点19}}
intercept() 的 handler 回调在导航提交后执行，此时 currentEntry 已更新，新 URL 已显示在浏览器中。典型的用法是根据路径加载和渲染新内容。

{{知识点20}}
intercept() 的 precommitHandler 回调在导航提交前执行，此时 currentEntry 尚未更新。它接受 NavigationPrecommitController 对象作为参数，可以用于重定向或取消导航。

{{知识点21}}
NavigationPrecommitController.redirect(url, options) 方法用于在 precommitHandler 中实现重定向。这比取消原始导航再发起新导航更简洁，因为不会暴露中间状态。

{{知识点22}}
NavigationPrecommitController.addHandler(callback) 方法用于在 precommitHandler 中动态添加导航提交后执行的处理器，适用于提交后操作依赖于预提交阶段操作的场景。

{{知识点23}}
intercept() 的 focusReset 选项控制导航后的焦点行为。默认值 "after-transition" 会在 handler Promise 解决后聚焦带有 autofocus 属性的元素或 body 元素；"manual" 则禁用默认行为。

{{知识点24}}
intercept() 的 scroll 选项控制导航后的滚动行为。默认值 "after-transition" 允许浏览器处理滚动（如滚动到片段标识符或恢复滚动位置）；"manual" 则禁用默认行为。

{{知识点25}}
NavigateEvent.scroll() 方法用于手动触发浏览器驱动的滚动行为，适用于希望在导航处理完成之前就执行滚动的场景。

{{知识点26}}
NavigateEvent.canIntercept 是只读属性，返回布尔值，表示导航是否可以被拦截。例如跨域导航无法被拦截。

{{知识点27}}
NavigateEvent.navigationType 返回导航类型，有四种值："push"（推送新条目）、"reload"（重新加载）、"replace"（替换当前条目）、"traverse"（遍历历史条目）。

{{知识点28}}
NavigateEvent.signal 返回一个 AbortSignal，当导航被取消时（如用户按下停止按钮或另一个导航开始取消了当前导航），该信号会被中止。

{{知识点29}}
NavigateEvent.sourceElement 返回触发导航的元素（如用户点击的链接元素），仅当导航由元素触发时有值。

{{知识点30}}
NavigateEvent.userInitiated 返回布尔值，表示导航是否由用户发起（如点击链接、提交表单、点击浏览器的前进/后退按钮）。

{{知识点31}}
NavigateEvent.info 返回由发起导航操作传递的 info 数据值，可以用于在导航操作间传递自定义数据。

{{知识点32}}
NavigateEvent.hasUAVisualTransition 返回布尔值，表示用户代理在派发事件前是否为此次导航执行了视觉过渡。

{{知识点33}}
NavigationHistoryEntry 接口表示单个导航历史条目，包含 key（槽位标识）、id（条目唯一标识）、index（在列表中的索引）、url（绝对 URL）、sameDocument（是否同文档）等属性。

{{知识点34}}
NavigationHistoryEntry.getState() 方法返回历史条目关联状态的克隆。状态是开发者定义的信息，可以是任意数据，如 UI 状态、访问次数等。

{{知识点35}}
NavigationHistoryEntry.dispose 事件在条目不再属于历史条目列表时触发。例如用户向后导航三次后再向前导航到其他地方，那三个历史条目将被 dispose。

{{知识点36}}
NavigationHistoryEntry.key 和 id 的区别：key 代表历史条目在列表中的槽位，当条目被 replace 时 key 会被复用；id 则始终唯一标识特定的历史条目，适用于与外部资源（如缓存）关联。

{{知识点37}}
NavigationTransition 接口表示正在进行的导航，包含 committed（导航已提交的 Promise）、finished（导航完成的 Promise）、from（来源条目）、to（目标条目）、navigationType 等属性。

{{知识点38}}
Navigation API 的所有导航方法（navigate、back、forward、traverseTo 等）都返回包含 { committed, finished } 两个 Promise 的对象。committed 在 URL 变更和新条目创建时 fulfill；finished 在所有 intercept handler 的 Promise fulfill 时 fulfill。

{{知识点39}}
navigatesuccess 事件在导航成功完成时触发，navigateerror 事件在导航失败时触发。这两个事件与 navigation 方法返回的 finished Promise 的 fulfill/reject 同步。

{{知识点40}}
Navigation API 的一个限制是页面首次加载时不会触发 navigate 事件。对于使用 SSR 的站点这通常没问题，但纯客户端渲染的站点可能需要额外的初始化函数。

{{知识点41}}
Navigation API 只在单个框架内操作——顶层页面或单个特定的 iframe。这减少了旧 History API 中令人困惑的边界情况。

{{知识点42}}
Navigation API 不能用于以编程方式修改或重排历史列表。例如无法删除临时模态框的导航条目来阻止用户通过前进按钮再次打开它。

{{知识点43}}
Navigation API 取代 History API 的典型模式：以前需要监听所有链接的 click 事件、调用 preventDefault()、执行 History.pushState()、设置页面视图。现在只需监听 navigate 事件并调用 intercept() 即可处理所有类型的导航。

{{知识点44}}
Navigation API 与 SPA 框架集成时，可以在 navigate 事件处理器中根据 URL 路径匹配路由规则，然后在 intercept handler 中加载对应组件、获取数据并渲染页面内容。

{{知识点45}}
NavigationDestination 接口表示导航的目标，通过 NavigateEvent.destination 访问。包含 url（目标 URL）、sameDocument（是否同文档）、key/index/id（遍历类型时的目标条目信息）等属性，以及 getState() 方法获取目标条目的状态。
