# 2025前端前端Long Tasks API
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}{{Long Tasks API 是什么：Long Tasks API 是一个 Web API，用于检测主线程上执行时间超过 50ms 的长时间任务。它帮助开发者识别性能瓶颈，改善用户体验。该 API 通过 PerformanceObserver 来监听 longtask 类型的性能条目。}}

{{知识点2}}{{Long Task 的定义：一个 Long Task 是指主线程不间断地忙碌 50ms 或更长时间的任意时间段。50ms 的阈值来源于 RAIL 模型——为了保证用户交互在 100ms 内响应，浏览器需要每 50ms 就有机会处理用户输入。}}

{{知识点3}}{{Long Tasks 对用户体验的影响：长时间任务阻塞主线程会导致以下问题：1) 延迟 Time to Interactive (TTI)；2) 高/不稳定的输入延迟；3) 高/不稳定的事件处理延迟；4) 动画和滚动卡顿（Janky animations and scrolling）。}}

{{知识点4}}{{PerformanceLongTaskTiming 接口：PerformanceLongTaskTiming 是 Long Tasks API 的核心接口，继承自 PerformanceEntry。它表示一个长任务的性能计时条目，包含了任务的持续时间、开始时间、归因信息等。}}

{{知识点5}}{{PerformanceLongTaskTiming.duration 属性：继承自 PerformanceEntry，返回一个 DOMHighResTimeStamp，表示任务从开始到结束的经过时间，精度为 1ms。这是判断一个任务是否为长任务的关键指标。}}

{{知识点6}}{{PerformanceLongTaskTiming.entryType 属性：继承自 PerformanceEntry，对于长任务始终返回字符串 "longtask"。这是在 PerformanceObserver 回调中识别长任务条目的依据。}}

{{知识点7}}{{PerformanceLongTaskTiming.startTime 属性：继承自 PerformanceEntry，返回一个 DOMHighResTimeStamp，表示任务开始的时间点。结合 duration 可以计算出任务的结束时间。}}

{{知识点8}}{{PerformanceLongTaskTiming.name 属性的取值：PerformanceEntry.name 属性返回一个字符串，指明可归因于长任务的浏览上下文或框架。可能的值包括：cross-origin-ancestor、cross-origin-descendant、cross-origin-unreachable、multiple-contexts、same-origin-ancestor、same-origin-descendant、same-origin、self、unknown。}}

{{知识点9}}{{self 归因类型：当 name 属性值为 "self" 时，表示长任务发生在当前文档自身的上下文中，而非来自 iframe 或其他嵌入内容。这是最常见且最容易处理的归因类型。}}

{{知识点10}}{{same-origin-ancestor 归因类型：当 name 为 "same-origin-ancestor" 时，表示长任务可归因于与当前文档同源的祖先框架。这意味着是上层同源页面中的代码导致了长任务。}}

{{知识点11}}{{same-origin-descendant 归因类型：当 name 为 "same-origin-descendant" 时，表示长任务可归因于与当前文档同源的后代框架。即下层嵌入的同源 iframe 中的代码导致了长任务。}}

{{知识点12}}{{cross-origin-ancestor 归因类型：当 name 为 "cross-origin-ancestor" 时，表示长任务可归因于与当前文档跨源的祖先框架。由于安全限制，无法获取具体的归因细节。}}

{{知识点13}}{{cross-origin-descendant 归因类型：当 name 为 "cross-origin-descendant" 时，表示长任务可归因于与当前文档跨源的后代框架。同样受同源策略限制，归因信息有限。}}

{{知识点14}}{{cross-origin-unreachable 归因类型：当 name 为 "cross-origin-unreachable" 时，表示长任务可归因于一个跨源的框架，但由于安全限制无法确定具体是哪个框架。}}

{{知识点15}}{{multiple-contexts 归因类型：当 name 为 "multiple-contexts" 时，表示长任务涉及多个浏览上下文，无法精确归因到单个框架。常见于多个 iframe 同时执行大量工作的情况。}}

{{知识点16}}{{unknown 归因类型：当 name 为 "unknown" 时，表示浏览器无法确定长任务的归因。这是最不理想的归因结果，开发者需要结合其他工具来定位问题。}}

{{知识点17}}{{PerformanceLongTaskTiming.attribution 属性：attribution 是一个只读属性，返回一个 TaskAttributionTiming 实例的序列（数组）。它提供了关于长任务归因的更详细信息，特别是涉及 iframe、embed 或 object 等容器时。}}

{{知识点18}}{{PerformanceLongTaskTiming.toJSON() 方法：toJSON() 方法返回 PerformanceLongTaskTiming 对象的 JSON 表示。这在序列化性能数据、发送到分析服务器时非常有用。}}

{{知识点19}}{{TaskAttributionTiming 接口：TaskAttributionTiming 接口提供关于长任务所涉及的工作及其关联帧上下文（容器）的详细信息。容器是指被认定为长任务来源的 iframe、embed 或 object 元素。该接口继承自 PerformanceEntry。}}

{{知识点20}}{{TaskAttributionTiming.containerType 属性：返回帧容器的类型，可能的值为 "iframe"、"embed" 或 "object"。这帮助开发者识别导致长任务的具体嵌入元素类型。}}

{{知识点21}}{{TaskAttributionTiming.containerSrc 属性：返回容器的 src 属性值，即嵌入内容的 URL。这对于定位哪个外部资源导致了长任务非常有用。}}

{{知识点22}}{{TaskAttributionTiming.containerId 属性：返回容器的 id 属性值。帮助开发者通过 DOM id 快速定位导致长任务的嵌入元素。}}

{{知识点23}}{{TaskAttributionTiming.containerName 属性：返回容器的 name 属性值。与 containerId 类似，但使用 name 属性来标识容器元素。}}

{{知识点24}}{{TaskAttributionTiming 的特殊属性值：TaskAttributionTiming 的 duration 始终返回 0（不适用），entryType 始终返回 "taskattribution"，name 始终返回 "unknown"，startTime 始终返回 0。这些固定值使其与 PerformanceLongTaskTiming 明显区分。}}

{{知识点25}}{{使用 PerformanceObserver 监听长任务：要获取长任务信息，需要创建 PerformanceObserver 实例，调用 observe() 方法并传入 type: "longtask"。设置 buffered: true 可以获取浏览器在构建文档期间缓冲的长任务数据。}}

{{知识点26}}{{Long Tasks API 基本用法示例：const observer = new PerformanceObserver((list) => { list.getEntries().forEach((entry) => { console.log(entry); }); }); observer.observe({ type: "longtask", buffered: true }); 这段代码创建了一个观察者来监听并记录所有长任务。}}

{{知识点27}}{{Long Tasks API 的规范来源：Long Tasks API 由 W3C Web 性能工作组维护，规范地址为 https://w3c.github.io/longtasks/。它是一个 W3C 工作草案（Working Draft），目前仍在发展中。}}

{{知识点28}}{{Long Tasks API 的浏览器兼容性：Long Tasks API 在主流浏览器中获得较好支持。Chrome 58+、Edge 79+ 原生支持。Firefox 和 Safari 的支持情况有所不同，使用前建议检查兼容性表。PerformanceObserver 自 2020 年 1 月起已被广泛支持。}}

{{知识点29}}{{Long Tasks 与 Total Blocking Time (TBT) 的关系：Total Blocking Time (TBT) 是一个基于 Long Tasks API 的 Web 性能指标。TBT 计算的是 FCP（首次内容绘制）到 TTI（可交互时间）之间所有长任务（超过 50ms 的部分）阻塞时间的总和。TBT = Σ(task.duration - 50ms)。}}

{{知识点30}}{{Long Tasks 与 Time to Interactive (TTI) 的关系：TTI 衡量页面从开始加载到能够可靠地快速响应用户输入的时间。Long Tasks 是导致 TTI 延迟的主要原因——主线程被长任务阻塞时，用户交互无法被及时处理。}}

{{知识点31}}{{常见的长任务来源：常见的长任务包括：1) 长时间运行的事件处理器（如复杂的点击事件处理）；2) 昂贵的重排（Reflow）和重绘（Repaint）操作；3) 浏览器在事件循环不同轮次之间执行的超过 50ms 的工作；4) 大量 DOM 操作；5) 复杂的 JavaScript 计算。}}

{{知识点32}}{{Culprit Browsing Context Container 概念：长任务引用了"罪魁祸首浏览上下文容器"（culprit browsing context container）的概念，简称为"容器"。容器是指长任务发生的顶层页面、iframe、embed 或 object 元素。TaskAttributionTiming 提供的 containerId、containerName 和 containerSrc 属性有助于确定任务的来源。}}

{{知识点33}}{{Long Tasks API 与 Scheduler API 的协作：现代浏览器引入了 Scheduler API（scheduler.yield() 和 scheduler.postTask()），与 Long Tasks API 配合使用。开发者可以使用 Scheduler API 将长任务分解为多个小任务，从而避免产生 Long Task。Long Tasks API 用于检测问题，Scheduler API 用于解决问题。}}

{{知识点34}}{{Long Tasks API 在 React 中的应用：React 18 的并发特性（Concurrent Features）如 startTransition、useDeferredValue 等本质上是为了避免长任务。通过将更新标记为非紧急，React 可以在浏览器空闲时分片执行渲染工作，从而避免阻塞主线程超过 50ms。}}

{{知识点35}}{{Long Tasks API 在实际监控中的应用：在生产环境中，可以通过 Long Tasks API 收集真实用户监控（RUM）数据。将长任务的 entryType、duration、startTime、name 和 attribution 信息发送到分析服务器，帮助团队了解真实用户的性能体验。}}

{{知识点36}}{{避免长任务的策略：1) 将长任务分解为较小的异步任务（使用 setTimeout、requestIdleCallback 或 scheduler.yield()）；2) 使用 Web Workers 将计算密集型工作移出主线程；3) 优化关键渲染路径，减少强制同步布局；4) 使用虚拟滚动减少 DOM 操作；5) 延迟加载非关键资源。}}

{{知识点37}}{{requestIdleCallback 与长任务：requestIdleCallback 允许在浏览器空闲期间执行低优先级工作，是避免产生长任务的重要工具。它接收一个 deadline 参数，开发者可以检查剩余空闲时间来决定执行多少工作。}}

{{知识点38}}{{Long Tasks API v2（Long Animation Frames）：Chrome 团队提出了 Long Animation Frames API (LoAF) 作为 Long Tasks API 的演进版本。LoAF 提供了更细粒度的归因信息，能够更好地关联到具体的脚本、样式计算和渲染工作，解决原始 Long Tasks API 归因不够精确的问题。}}

{{知识点39}}{{DOMHighResTimeStamp 精度：Long Tasks API 使用 DOMHighResTimeStamp 来表示时间戳，精度为 1 毫秒（ms）。这比 Date.now() 的精度更高，且不受系统时钟调整的影响。时间戳表示自页面导航开始以来经过的时间。}}

{{知识点40}}{{buffered 选项的重要性：在调用 observe() 时设置 buffered: true 非常重要。这确保了 PerformanceObserver 能够接收到在观察者创建之前就已经记录的长任务。如果不设置 buffered，可能会错过页面加载期间的重要长任务数据。}}

{{知识点41}}{{PerformanceObserver.supportedEntryTypes 静态属性：在使用 Long Tasks API 前，可以通过 PerformanceObserver.supportedEntryTypes 检查浏览器是否支持 "longtask" 类型。这是一个静态只读属性，返回当前用户代理支持的所有 entryType 值的数组。}}

{{知识点42}}{{Long Tasks API 与 Core Web Vitals 的关系：Long Tasks API 是理解和优化 Core Web Vitals 的重要工具。特别是 INP（Interaction to Next Paint）指标，它关注用户交互到下一帧绘制的延迟。长任务是导致 INP 评分差的主要原因之一。}}

{{知识点43}}{{长任务的时间阈值选择理由：50ms 的阈值基于 Google 的 RAIL 性能模型。该模型建议应用在 100ms 内响应用户输入，而浏览器需要在每个帧（约 16.6ms for 60fps）之间有机会处理事件。50ms 的阈值给浏览器留出了足够的时间来处理用户交互。}}

{{知识点44}}{{Long Tasks API 的局限性：Long Tasks API 存在一些局限：1) 只能检测超过 50ms 的任务，无法检测接近阈值的"准长任务"；2) 归因信息在跨域场景下有限；3) 无法直接提供修复建议；4) 不同浏览器的实现可能存在差异。Long Animation Frames API 旨在解决部分问题。}}

{{知识点45}}{{PerformanceEntry 接口基础：PerformanceLongTaskTiming 和 TaskAttributionTiming 都继承自 PerformanceEntry。PerformanceEntry 是 Performance Timeline API 的基础接口，定义了 name、entryType、startTime 和 duration 四个基本属性，各种性能条目类型都基于此扩展。}}
