# 2025前端前端实时通信
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}{{前端实时通信概述
前端实时通信是指服务器能够主动向客户端推送数据，而不需要客户端反复轮询的技术总称。传统Web模型基于请求-响应模式，客户端必须主动请求才能获取新数据。实时通信技术打破了这一限制，允许服务器在数据可用时立即将其推送到浏览器。主要技术包括SSE（Server-Sent Events）、WebSocket、WebTransport、WebRTC和Push API等，各有不同的适用场景和能力特点。}}

{{知识点2}}{{SSE（Server-Sent Events）基本概念
SSE是一种允许服务器通过HTTP连接向客户端推送数据的Web API。与WebSocket不同，SSE是单向通信——数据只能从服务器流向客户端。SSE使用text/event-stream格式传输数据，基于标准HTTP协议，因此无需特殊协议支持。SSE连接通过EventSource接口管理，自2020年起在所有主流浏览器中可用（Baseline Widely Available）。SSE适合新闻推送、股票行情、社交动态流等只需服务器单向推送的场景。}}

{{知识点3}}{{EventSource接口详解
EventSource接口是Web内容与服务器发送事件的交互入口。创建EventSource实例后，会与HTTP服务器建立持久连接，接收text/event-stream格式的事件。连接保持打开状态直到调用EventSource.close()关闭。EventSource继承自EventTarget，主要属性包括readyState（连接状态：CONNECTING=0、OPEN=1、CLOSED=2）、url（源URL）、withCredentials（是否带凭据）。主要方法为close()用于关闭连接。主要事件包括open、message和error。}}

{{知识点4}}{{SSE的连接状态管理
EventSource.readyState属性返回连接的当前状态，有三个可能值：CONNECTING（0）表示连接尚未打开；OPEN（1）表示连接已打开且可以通信；CLOSED（2）表示连接已关闭或无法打开。当连接断开时，EventSource会自动尝试重连，这是SSE相对于WebSocket的一个重要优势——内置自动重连机制，无需开发者手动实现。}}

{{知识点5}}{{SSE事件格式与数据传输
SSE使用特定的文本格式传输事件。服务器发送的每个事件可以包含以下字段：event（事件类型，默认为message）、data（事件数据）、id（事件ID，用于断线重连时的Last-Event-ID）、retry（建议的重连间隔毫秒数）。多个data字段会用换行符连接。如果服务器发送了event字段，客户端将收到以该字段值命名的自定义事件；否则触发通用message事件。数据默认以UTF-8编码传输。}}

{{知识点6}}{{SSE的HTTP/2连接数限制
在非HTTP/2环境下，SSE存在最大打开连接数限制的问题，对浏览器来说限制非常低（通常为6个）。这个限制是按浏览器+域名计算的，即同一浏览器对同一域名最多只能打开6个SSE连接，跨标签页共享。Chrome和Firefox已将此问题标记为"Won't fix"。使用HTTP/2时，最大并发HTTP流数量由服务器和客户端协商（默认为100），大大缓解了此限制。因此在生产环境中推荐使用HTTP/2。}}

{{知识点7}}{{SSE与CORS跨域设置
EventSource构造函数接受可选的withCredentials参数，用于控制是否在跨域请求中发送凭据（如Cookie和认证头）。默认值为false，即不发送凭据。设置为true时，服务器响应头必须包含适当的CORS头（如Access-Control-Allow-Origin和Access-Control-Allow-Credentials）。跨域SSE请求需要服务器端配合CORS配置才能正常工作。}}

{{知识点8}}{{SSE典型应用场景
SSE最适合以下场景：实时新闻和信息流推送、股票/加密货币行情更新、社交媒体动态通知、日志实时流式输出、AI大模型流式回答（ChatGPT风格的逐字输出）、构建进度通知、监控仪表盘数据更新。这些场景的共同特点是只需要服务器向客户端单向推送数据，客户端无需通过同一连接向服务器发送消息。}}

{{知识点9}}{{WebSocket基本概念
WebSocket API提供了在用户浏览器和服务器之间建立双向交互通信会话的能力。与SSE不同，WebSocket支持全双工通信——客户端和服务器都可以随时向对方发送数据。WebSocket自2015年7月起在所有主流浏览器中广泛可用（Baseline Widely Available）。WebSocket连接通过HTTP升级握手建立，之后通信不再使用HTTP协议，而是使用ws://或wss://（加密）专用协议。}}

{{知识点10}}{{WebSocket连接建立过程
WebSocket连接通过HTTP升级握手（Upgrade Handshake）建立。客户端发送一个带有Upgrade: websocket和Connection: Upgrade头的HTTP请求，以及Sec-WebSocket-Key（客户端生成的nonce）和Sec-WebSocket-Version头。服务器如果同意升级，返回101 Switching Protocols状态码，并在响应头中包含基于Sec-WebSocket-Key计算出的Sec-WebSocket-Accept值。握手完成后，连接从HTTP切换到WebSocket协议进行通信。}}

{{知识点11}}{{WebSocket构造函数与协议
WebSocket构造函数接受一个必需参数——WebSocket服务器的URL（如ws://localhost:8080或wss://example.com/socket）。可选的第二个参数protocols允许单个服务器实现多个子协议，可以是单个字符串或字符串数组。在生产环境中应始终使用wss://（WebSocket Secure）协议。构造函数在目标不允许访问时会抛出SecurityError，大多数用户代理现在要求对所有WebSocket连接使用安全链接。}}

{{知识点12}}{{WebSocket readyState四种状态
WebSocket.readyState只读属性返回连接的当前状态，有四种可能值：CONNECTING（0）表示Socket已创建但连接尚未打开；OPEN（1）表示连接已打开且可以通信；CLOSING（2）表示连接正在关闭过程中；CLOSED（3）表示连接已关闭或无法打开。开发者应检查readyState再决定是否调用send()方法，否则可能抛出异常或数据被静默丢弃。}}

{{知识点13}}{{WebSocket send()方法详解
WebSocket.send()方法将指定数据排入队列通过WebSocket连接传输到服务器，同时增加bufferedAmount的值。支持发送的数据类型包括：String（以UTF-8编码）、ArrayBuffer（底层二进制数据）、Blob（以二进制帧传输，忽略Blob.type）、TypedArray或DataView。send()是异步的——它不等待数据实际传输完成就返回，只是将数据加入内部缓冲区。如果在CONNECTING状态调用会抛出InvalidStateError异常，在CLOSING或CLOSED状态调用数据会被静默丢弃。}}

{{知识点14}}{{WebSocket事件模型
WebSocket支持四个主要事件：open事件在连接建立时触发；message事件在从服务器接收到数据时触发，事件对象的data属性包含接收到的数据；error事件在连接因错误关闭时触发；close事件在连接关闭时触发。所有事件都可以通过addEventListener()监听，也可以通过onopen、onmessage、onerror、onclose属性直接赋值处理函数。接收到的数据可以是文本字符串或二进制数据（Blob或ArrayBuffer），取决于binaryType属性设置。}}

{{知识点15}}{{WebSocket背压问题与内存风险
传统WebSocket API的一个重要缺陷是不支持背压（backpressure）。当服务器发送消息的速度超过客户端处理速度时，消息会在内存中不断堆积，最终导致设备内存耗尽、CPU占用100%或两者同时发生。WebSocket API没有内置机制来通知服务器减慢发送速度。这是WebSocket在高吞吐量场景中的一个关键限制，也是WebSocketStream和WebTransport出现的重要原因。}}

{{知识点16}}{{WebSocketStream接口
WebSocketStream是WebSocket API的基于Promise的替代接口，使用Streams API发送和接收数据，因此可以自动利用流的背压机制来调节读写速度，避免应用瓶颈。WebSocketStream通过opened Promise获取ReadableStream和WritableStream，使用reader.read()接收数据、writer.write()发送数据。但WebSocketStream目前是非标准实验性技术，仅在一个渲染引擎中支持，不建议在生产环境使用。}}

{{知识点17}}{{WebSocket子协议与扩展
WebSocket支持通过子协议（sub-protocol）和扩展（extensions）增强功能。子协议在WebSocket握手时通过Sec-WebSocket-Protocol头协商，允许在同一连接上实现多种应用层协议（如STOMP、MQTT over WebSocket等）。扩展通过Sec-WebSocket-Extensions头协商，最常见的是permessage-deflate压缩扩展。WebSocket对象的protocol和extensions只读属性分别返回服务器选定的子协议和扩展。}}

{{知识点18}}{{WebSocket与bfcache兼容性
如果页面有打开的WebSocket连接，浏览器可能不会将其添加到bfcache（往返缓存）中。bfcache允许用户在离开页面后通过浏览器前进/后退按钮快速恢复页面状态。WebSocket连接的存在会阻止此优化。因此最佳实践是在用户离开页面时主动关闭WebSocket连接（例如在pagehide事件中调用close()），以确保页面能被正确缓存，提升用户体验。}}

{{知识点19}}{{WebSocket重连策略实现
WebSocket本身不提供自动重连机制（与SSE不同），需要开发者自行实现。常见的重连策略包括：固定间隔重连（简单但可能在服务器故障时造成大量请求）、指数退避（Exponential Backoff，每次重连间隔翻倍，通常加入随机抖动Jitter防止惊群效应）、断路器模式（Circuit Breaker，连续失败后停止重连一段时间）。重连时通常需要考虑状态恢复，如重新订阅频道、同步最新数据等。}}

{{知识点20}}{{WebTransport基本概念
WebTransport是W3C标准化的低级API，提供连接HTTP/3服务器、发起可靠和不可靠双向传输的能力。自2026年3月起成为Baseline（最新设备和浏览器版本可用）。WebTransport需要安全上下文（HTTPS），支持Web Workers。它是WebSocket API的预期替代方案，提供背压支持、单向流、乱序交付和不可靠数据报等WebSocket和WebSocketStream都不支持的高级功能。}}

{{知识点21}}{{WebTransport核心属性
WebTransport的主要只读属性包括：ready（连接就绪的Promise）、closed（连接关闭的Promise）、datagrams（WebTransportDatagramDuplexStream实例，用于发送和接收数据报）、congestionControl（拥塞控制偏好：高吞吐或低延迟）、incomingBidirectionalStreams（服务器打开的双向流，返回WebTransportBidirectionalStream的ReadableStream）、incomingUnidirectionalStreams（服务器打开的单向流）、reliability（是否支持不可靠传输如UDP）。}}

{{知识点22}}{{WebTransport流与数据报
WebTransport同时支持流（Streams）和数据报（Datagrams）两种传输方式。流分为双向流和单向流，通过createBidirectionalStream()和createUnidirectionalStream()创建。流提供可靠、有序的数据传输。数据报通过datagrams属性的WebTransportDatagramDuplexStream接口发送和接收，提供不可靠、无序的传输——类似于UDP，适合实时游戏、视频会议等对延迟敏感但可以容忍少量丢包的场景。数据报和流可以同时使用。}}

{{知识点23}}{{WebTransport连接管理
使用WebTransport时，先通过new WebTransport(url)创建实例（URL必须是HTTPS，且需要显式指定端口号），然后等待transport.ready Promise解析后即可使用。通过transport.closed Promise监控连接关闭，区分正常关闭和错误关闭。close()方法主动关闭连接。getStats()方法异步返回HTTP/3连接统计信息。WebTransportError类型提供比标准DOMException更详细的错误信息。}}

{{知识点24}}{{WebTransport vs WebSocket对比
WebTransport与WebSocket的主要区别：传输层——WebSocket基于TCP，WebTransport基于HTTP/3（QUIC/UDP）；可靠性——WebSocket始终可靠有序，WebTransport支持可靠流和不可靠数据报；背压——WebSocket不支持，WebTransport内置背压；多路复用——WebSocket单连接单通道，WebTransport支持多流复用；延迟——WebTransport基于QUIC通常延迟更低；浏览器支持——WebSocket广泛可用，WebTransport较新。WebTransport更适合需要灵活传输模式的复杂场景。}}

{{知识点25}}{{WebRTC基本概念
WebRTC（Web Real-Time Communication）是一套支持浏览器之间进行实时音视频通信和数据交换的API。WebRTC无需安装插件即可在浏览器中实现点对点（Peer-to-Peer）连接，支持音频和视频会议、文件交换、屏幕共享、身份管理等功能。核心接口包括RTCPeerConnection（表示两个对等端之间的连接）、RTCDataChannel（双向数据通道）和MediaStream（媒体流）。WebRTC连接通常需要信令服务器辅助建立，但媒体传输是点对点的。}}

{{知识点26}}{{RTCDataChannel数据通道
RTCDataChannel是WebRTC提供的双向数据通道接口，允许在已建立的RTCPeerConnection上交换任意二进制数据。可用于游戏状态同步、文件传输、聊天消息等场景。DataChannel支持可靠（TCP-like）和不可靠（UDP-like）两种传输模式，以及有序和无序两种交付方式。DataChannel通过RTCPeerConnection.createDataChannel()创建，对方通过datachannel事件接收。相比WebSocket，DataChannel支持更低延迟的P2P数据传输。}}

{{知识点27}}{{Push API推送通知
Push API使Web应用能够接收从服务器推送的消息，即使应用不在前台甚至未加载。这依赖于Service Worker——应用必须有活跃的Service Worker才能订阅推送通知，通过PushManager.subscribe()获取PushSubscription（包含端点URL和加密密钥）。收到推送消息时触发Service Worker的push事件，可调用showNotification()显示通知。每个订阅是唯一的capability URL，需要保密。Push API自2023年3月起在所有主流浏览器中可用。}}

{{知识点28}}{{轮询（Polling）与长轮询（Long Polling）
轮询是最简单的"伪实时"方案：客户端定时向服务器发送请求查询新数据。优点是实现简单、兼容性好，缺点是浪费带宽和服务器资源、实时性差。长轮询是其改进版：客户端发送请求后服务器保持连接打开直到有新数据才响应，客户端收到响应后立即发起新请求。长轮询在WebSocket和SSE不可用时仍是可靠降级方案，但相比真正的实时技术效率较低且实现复杂度更高。}}

{{知识点29}}{{Comet与实时通信演进历史
Comet是一类使用HTTP长连接实现服务器推送的Web应用架构的统称，早于WebSocket出现。包括长轮询（Long Polling）、HTTP流（HTTP Streaming，也称Forever Iframe）等技术。2011年WebSocket协议标准化（RFC 6455）后逐渐取代Comet成为主流双向通信方案。SSE在HTML5规范中定义为单向推送方案。2021年WebRTC数据通道成为P2P数据传输标准。2026年WebTransport正式进入Baseline，代表了实时通信技术的最新演进方向。}}

{{知识点30}}{{实时通信技术选型指南
选择实时通信技术的关键考量因素：方向性——单向推送用SSE，双向通信用WebSocket/WebTransport；可靠性——需要可靠传输用WebSocket，可容忍丢包用WebTransport数据报；延迟要求——低延迟场景优选WebTransport或WebRTC DataChannel；浏览器兼容性——WebSocket兼容最广，WebTransport较新；复杂度——SSE最简单，WebTransport最复杂；P2P需求——用WebRTC。常见组合：SSE+HTTP POST用于AI流式对话，WebSocket用于聊天室和协作编辑，WebTransport用于云游戏。}}

{{知识点31}}{{WebSocket常用第三方库
生产环境中常用的WebSocket客户端库：Socket.IO——最流行，自动降级到轮询，支持房间/命名空间/自动重连，但协议不标准需配合服务端；ws——Node.js最流行的WebSocket实现，轻量高性能；ReconnectingWebSocket——自动重连的WebSocket封装；SockJS——提供类似WebSocket的API，自动降级；µWebSockets——C++实现的高性能服务端。选择时需考虑是否需要自动降级、重连、二进制支持等特性。}}

{{知识点32}}{{SSE在React/Vue中的实践
在现代前端框架中使用SSE：React中通常在useEffect hook中创建EventSource实例，在清理函数中调用close()防止内存泄漏；Vue中在onMounted中创建、onUnmounted中关闭。常见模式是将SSE封装为自定义Hook（如useSSE）或Composable，统一管理连接状态、错误处理和数据解析。配合AbortController可以在组件卸载时优雅取消请求。对于AI流式回答场景，通常需要处理逐行到达的JSON数据并实时更新UI。}}

{{知识点33}}{{WebSocket心跳保活机制
WebSocket连接可能因网络问题或服务器超时而静默断开，需要心跳（Heartbeat/Ping-Pong）机制检测。实现方式：客户端定期发送ping消息，服务器收到后回复pong消息。如果在超时时间内未收到pong，认为连接已断开并触发重连。心跳间隔通常为30秒，超时时间10-15秒。注意浏览器WebSocket API不暴露原生ping/pong帧，需要在应用层实现。一些框架如Socket.IO内置了心跳机制。}}

{{知识点34}}{{WebTransport拥塞控制
WebTransport的congestionControl属性指示应用对高吞吐量或低延迟的偏好。可选值包括"low-latency"（优先低延迟，适合实时交互如游戏和视频会议）和"throughput"（优先高吞吐量，适合大文件传输和流媒体）。拥塞控制由底层QUIC协议实现，根据网络状况自动调节发送速率。开发者可以根据应用需求在创建WebTransport实例时指定拥塞控制策略，优化网络资源利用。}}

{{知识点35}}{{实时通信中的安全考量
实时通信的安全要点：始终使用加密连接（wss://、HTTPS）防止中间人攻击；WebSocket不受同源策略限制，需在服务端验证Origin头防止CSRF；SSE受CORS策略约束，需正确配置跨域头；WebTransport强制要求HTTPS安全上下文；实现消息认证和授权机制，验证连接身份；对传输数据进行输入验证和转义，防止注入攻击；Push API的订阅端点是capability URL需保密；合理设置连接速率限制防止DoS攻击。}}

{{知识点36}}{{Server-Sent Events的服务端实现
SSE服务端需要设置正确的HTTP响应头Content-Type: text/event-stream和Cache-Control: no-cache，以及Connection: keep-alive。服务端按事件格式逐行写入数据：每行以"data:"、"event:"、"id:"或"retry:"开头，事件之间用空行分隔。支持SSE的服务端框架丰富：Node.js的Express、Python的Flask/FastAPI、Go的net/http、Java的Spring WebFlux等。部分框架提供专门的SSE封装，如NestJS的@Sse()装饰器。}}

{{知识点37}}{{WebSocket协议规范与版本
WebSocket协议由IETF标准化为RFC 6455（2011年），W3C定义了浏览器API规范。协议使用ws://（端口80）或wss://（端口443）URL方案。握手阶段使用HTTP Upgrade机制，之后切换到WebSocket帧协议通信。帧类型包括文本帧、二进制帧、关闭帧、Ping帧和Pong帧。协议版本通过Sec-WebSocket-Version头协商。当前WHATWG维护的WebSockets规范是浏览器实现的主要参考标准。}}

{{知识点38}}{{WebTransport over HTTP/3技术细节
WebTransport基于HTTP/3（RFC 9114），而HTTP/3运行在QUIC传输协议（RFC 9000）之上，QUIC使用UDP。WebTransport通过draft-ietf-webtrans-http3规范定义。连接过程：客户端通过CONNECT方法建立HTTP/3连接，协商WebTransport扩展。QUIC提供内置的多路复用、0-RTT连接建立、连接迁移（网络切换不断连）等优势。相比WebSocket基于TCP，WebTransport避免了队头阻塞（Head-of-Line Blocking）问题。}}

{{知识点39}}{{实时通信中的消息格式设计
实时通信中常用的消息格式：JSON——最通用，易调试但体积较大且解析开销；Protocol Buffers（Protobuf）——二进制格式，紧凑高效，适合高频消息；MessagePack——二进制JSON替代，比JSON紧凑且保持自描述性；自定义二进制协议——最高效但开发维护成本高。选择考虑因素：消息频率、带宽限制、开发效率、跨语言兼容性。WebSocket支持文本和二进制帧，WebTransport的流和数据报都支持二进制数据。}}

{{知识点40}}{{SSE的EventSource polyfill与兼容性
虽然SSE自2020年起在所有主流浏览器中可用，但在某些场景仍需polyfill：旧版浏览器支持、需要POST请求（原生EventSource仅支持GET）、需要自定义请求头、需要请求体携带数据。常用polyfill包括：eventsource（Node.js兼容实现）、Yaffle/EventSource（支持自定义headers和请求方法）、@microsoft/fetch-event-source（基于fetch API实现，支持POST和自定义headers，被广泛用于AI流式请求场景）。}}

{{知识点41}}{{实时协作编辑中的通信架构
实时协作编辑（如Google Docs、Figma）的通信架构通常结合多种技术：WebSocket用于实时操作同步，支持双向低延迟通信；操作转换（OT）或CRDT算法处理并发冲突；SSE可用于通知和状态广播；WebRTC DataChannel用于P2P协作场景减少服务器中转。典型架构包括客户端操作队列、服务端操作转换和广播、版本向量或Lamport时间戳用于因果排序。WebSocket是此类场景的首选传输层。}}

{{知识点42}}{{云游戏与WebTransport
云游戏是WebTransport的典型应用场景。游戏需要极低延迟（<50ms）和高帧率视频流传输。WebTransport的数据报模式允许丢弃过时的视频帧而不等待重传，类似UDP的行为。拥塞控制的low-latency模式确保最小化传输延迟。多流复用允许游戏控制输入、视频流、音频流在独立通道传输互不干扰。相比WebSocket基于TCP的有序传输会导致旧帧阻塞新帧，WebTransport的不可靠传输模式更适合实时游戏场景。}}

{{知识点43}}{{Service Worker与Push API配合
Push API的工作流程：1）页面通过navigator.serviceWorker.register()注册Service Worker；2）通过PushManager.subscribe()请求推送订阅，浏览器会请求用户授权通知权限；3）订阅成功后获得PushSubscription对象，包含endpoint URL和加密密钥p256dh和auth；4）将订阅信息发送给后端服务器保存；5）服务器使用Web Push协议向endpoint发送推送消息；6）Service Worker的push事件被触发，调用showNotification()显示系统通知。整个过程不需要页面保持打开状态。}}

{{知识点44}}{{WebSocket连接池与负载均衡
大规模WebSocket应用需要考虑连接管理：连接池限制单客户端并发连接数；负载均衡器（如Nginx、HAProxy）需要配置为支持WebSocket升级（proxy_pass + Upgrade头转发）；有状态的WebSocket连接使得水平扩展复杂化，需要粘性会话（Sticky Session）或分布式消息总线（如Redis Pub/Sub）跨节点广播消息。Spring WebSocket、Socket.IO Cluster等框架提供了集群模式支持，通过适配器实现跨节点消息同步。}}

{{知识点45}}{{fetch() + ReadableStream实现SSE替代方案
除了EventSource API，也可以使用fetch API配合ReadableStream手动解析SSE流。这种方式的优势在于：支持POST请求和自定义请求头、支持AbortController取消、更灵活的错误处理、可携带请求体。实现要点：使用fetch发起请求，通过response.body.getReader()获取ReadableStreamReader，逐块读取并手动解析event-stream格式数据。这种方式在AI大模型流式接口中被广泛使用，如OpenAI的Chat Completions API流式响应。}}

{{知识点46}}{{实时通信中的错误处理最佳实践
各技术的错误处理策略：SSE——监听error事件，EventSource会自动重连，可通过readyState判断状态；WebSocket——监听error和close事件，检查CloseEvent的code和reason，实现指数退避重连；WebTransport——捕获WebTransport构造函数异常，监听closed Promise，区分正常关闭和异常关闭；通用——实现健康检查机制、记录连接日志、设置合理的超时时间、实现优雅降级（如WebSocket不可用时降级到SSE或长轮询）。}}

{{知识点47}}{{WebSocket vs SSE vs WebTransport性能对比
性能对比维度：延迟——WebSocket和WebTransport较低（持久连接），SSE略高（HTTP头部开销）；吞吐量——WebTransport数据报最高（无队头阻塞），WebSocket次之，SSE受限于单向性；并发连接——SSE在HTTP/1.1下限6个连接，HTTP/2下限100个流，WebSocket无此限制但每个标签页独立连接；内存占用——SSE最轻量，WebSocket中等，WebTransport因多流管理稍重；建立连接速度——WebTransport支持0-RTT最快，WebSocket需要一次完整握手，SSE等同HTTP。}}

{{知识点48}}{{实时通信在微服务架构中的应用
微服务架构中实时通信的典型模式：API Gateway统一管理WebSocket连接并路由到后端微服务；消息代理（如RabbitMQ、Kafka、Redis Streams）实现服务间事件驱动通信；WebSocket网关服务处理连接生命周期管理和消息分发；SSE用于服务到客户端的单向事件通知；事件溯源（Event Sourcing）模式结合实时推送实现状态同步。常用框架：Spring Cloud Stream、NestJS Microservices、Socket.IO Redis Adapter等。}}

{{知识点49}}{{WebTransport的浏览器兼容性现状
截至2026年3月，WebTransport已进入Baseline状态（最新设备和浏览器版本可用）。Chrome从97版本开始支持，Firefox从114版本开始支持，Safari从17.4版本开始支持。但WebTransport需要HTTP/3服务器支持，这是其部署的主要障碍——HTTP/3服务器基础设施相比HTTP/2还不那么普及。对于需要支持旧版浏览器的应用，建议实现WebSocket降级方案。WebTransport的安全上下文要求意味着只能在HTTPS环境下使用。}}

{{知识点50}}{{实时通信技术2025发展趋势
2025年前端实时通信的主要趋势：WebTransport标准化推进，逐步替代部分WebSocket使用场景；AI流式接口推动SSE使用量激增（如LLM流式输出）；WebSocketStream标准化进展（解决背压问题）；WebRTC数据通道在去中心化应用中更广泛使用；边缘计算推动实时通信节点下沉减少延迟；WebTransport与WebCodecs结合实现实时音视频处理；HTTP/3和QUIC基础设施逐步成熟为WebTransport提供更好支持。}}
