# 2025前端前端CSS At-rules
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}{{CSS At-rules概述：At-rules是CSS语句，用于指示CSS如何行为。它们以@符号（U+0040）开头，后跟一个标识符。At-rules从@关键字开始到下一个分号(;）或下一个CSS块（{}），以先出现者为准。At-rules用于分组和构建样式规则、声明与所选内容不直接关联的样式信息，以及管理导入和命名空间等语法构造。}}

{{知识点2}}{{At-rules的两种形式：At-rules一般分为两种形式——语句At-rules（Statement at-rules）和块At-rules（Block at-rules）。语句At-rules以分号结尾，如@charset "utf-8"；块At-rules以{}块结尾，可包含嵌套规则、其他At-rules或属性声明，如@media screen and (width >= 900px) { ... }。}}

{{知识点3}}{{@charset规则：@charset CSS规则指定样式表中使用的字符编码。语法为@charset "UTF-8"。它必须放在样式表的最开头，前面不能有其他字符（除了Unicode BOM）。虽然形式上是At-rule，但严格来说它是一个特定的字节序列，不是真正的At-rule。在实际开发中，通常推荐使用UTF-8编码，因此@charset较少使用。}}

{{知识点4}}{{@import规则：@import用于告诉CSS引擎包含外部样式表。语法为@import url; 或 @import url layer(layer-name) supports(condition) media-queries;。重要规则：@import必须位于所有其他类型规则之前（除了@charset和@layer语句）。@import不是嵌套语句，不能在条件组At-rules内部使用。支持通过layer()导入到级联层，通过supports()进行条件导入。}}

{{知识点5}}{{@namespace规则：@namespace定义样式表的默认命名空间或命名空间前缀。语法为@namespace url("XML-namespace-URL")（默认命名空间）或@namespace prefix url("XML-namespace-URL")（带前缀的命名空间）。它用于限制通用选择器、类型选择器和属性选择器仅匹配特定命名空间内的元素。主要在处理包含多个命名空间的文档时使用（如HTML内嵌SVG或MathML）。所有@namespace规则必须在@charset和@import之后，在所有其他At-rules之前。}}

{{知识点6}}{{@media规则概述：@media是最常用的条件组At-rules之一，用于根据设备是否满足媒体查询条件来应用样式。语法为@media screen and (width >= 900px) { ... }。可以放在代码顶层，也可以嵌套在其他条件组At-rules中。@media是实现响应式设计的核心工具。}}

{{知识点7}}{{媒体类型（Media Types）：@media支持三种主要媒体类型——all（适用于所有设备）、print（用于分页材料和打印预览模式）、screen（主要用于屏幕）。CSS2.1和Media Queries 3定义了额外的媒体类型（tty、tv、projection、handheld、braille、embossed、aural），但在Media Queries 4中已被弃用。}}

{{知识点8}}{{媒体特性（Media Features）：媒体特性描述用户代理、输出设备或环境的特定特征。常用特性包括：width/height（视口宽高）、orientation（方向）、aspect-ratio（宽高比）、hover（是否支持悬停）、pointer（指点设备精度）、prefers-color-scheme（亮/暗色偏好）、prefers-reduced-motion（减少动效偏好）、color-gamut（色彩范围）、display-mode（显示模式）、forced-colors（限制调色板检测）、device-posture（设备姿态，折叠屏检测）。}}

{{知识点9}}{{媒体查询逻辑运算符：@media支持三种逻辑运算符——and（逻辑与，两个条件都为真才生效）、not（逻辑非，否定整个媒体查询）、only（仅在浏览器支持媒体查询时应用）。可以使用逗号分隔多个媒体查询（相当于逻辑或）。现代CSS还支持范围语法如(width >= 900px)和(width >= 400px) and (width <= 800px)。}}

{{知识点10}}{{@supports规则：@supports是特性查询At-rule，用于检测浏览器是否支持指定的CSS特性。语法为@supports (display: flex) { ... }。支持and、or、not逻辑运算符和括号分组。声明语法检查属性:值声明，函数语法支持selector()（检查选择器支持）、font-tech()（检查字体技术）、font-format()（检查字体格式）。@supports使渐进增强变得简单直接。}}

{{知识点11}}{{@supports的selector()函数：通过@supports selector(h2 > p)可以检测浏览器是否支持特定的CSS选择器语法。这对于使用新的选择器特性（如:has()、:is()、:where()等）时进行渐进增强非常有用。如果浏览器不支持selector()函数本身，整个条件将被评估为false。}}

{{知识点12}}{{@supports的font-tech()和font-format()函数：font-tech()检查浏览器是否支持指定的字体渲染技术，如color-COLRv1（多色字形）、variations（字体变体）、palettes（字体调色板）等。font-format()检查是否支持指定字体格式，如woff2、opentype、truetype等。这在使用高级字体特性时进行兼容性检测非常有用。}}

{{知识点13}}{{@keyframes规则：@keyframes用于定义命名的CSS动画，描述动画序列中间步骤（关键帧）的样式。语法为@keyframes slide-in { from { ... } to { ... } } 或使用百分比如0%、30%、100%。from等价于0%，to等价于100%。通过animation-name属性将关键帧应用到元素。}}

{{知识点14}}{{@keyframes的细节规则：如果关键帧未指定起始（0%/from）或结束（100%/to）状态，浏览器将使用元素的现有样式。未在每个关键帧中指定的属性会被插值（如果可能）。如果同一时间偏移有多个关键帧定义，所有值都会被使用。关键帧中的!important声明会被忽略。多个同名@keyframes规则时，解析器遇到的最后一个生效。}}

{{知识点15}}{{@font-face规则：@font-face用于定义字体资源位置和样式特征。语法为@font-face { font-family: "Trickster"; src: local("Trickster"), url("trickster.woff2") format("woff2"); }。必须指定font-family和src描述符。推荐使用WOFF2格式，因为压缩效率最高且浏览器支持良好。}}

{{知识点16}}{{@font-face描述符详解：font-display控制字体加载行为（auto、block、swap、fallback、optional）；font-family指定字体名称；font-weight/font-style/font-stretch支持范围值如font-weight: 100 400；unicode-range指定Unicode码点范围；src支持local()和url()函数；font-feature-settings控制OpenType高级排版特性；font-variation-settings控制字体变体；size-adjust定义字形缩放乘数；ascent-override/descent-override/line-gap-override覆盖字体度量。}}

{{知识点17}}{{@font-face的字体加载策略：local()优先查找用户设备上已安装的字体，url()指定要下载的字体资源。浏览器按列表声明顺序尝试加载资源，通常local()应写在url()之前。font-display描述符控制字体的显示策略：swap先显示后备字体再切换，optional允许浏览器根据网络条件决定是否加载，fallback是折中方案。}}

{{知识点18}}{{@counter-style规则：@counter-style用于定义自定义计数器样式，扩展预定义列表样式。语法为@counter-style thumbs { system: cyclic; symbols: "\1F44D"; suffix: " "; }。主要描述符包括：system（算法类型，如cyclic、numeric、alphabetic、symbolic、fixed、additive）、symbols（符号定义）、additive-symbols（加法系统符号）、negative（负数符号）、prefix/suffix（前缀/后缀）、range（范围）、pad（填充）、speak-as（语音合成方式）、fallback（后备样式）。}}

{{知识点19}}{{@page规则：@page用于指定打印页面的方面，如尺寸、方向和边距。语法为@page { size: A4; margin: 2cm; }。支持伪类:left（偶数页）、:right（奇数页）和命名页面（如@page wide { size: a4 landscape; }）。支持margin at-rules如@top-right { content: "Page " counter(pageNumber); }用于在页边距区域添加内容。page-orientation描述符指定页面方向。}}

{{知识点20}}{{@layer规则概述：@layer用于创建级联层，是CSS级联机制的重要增强。@layer既是语句At-rules（定义层优先顺序），也是块At-rules（定义层内样式）。语句形式如@layer theme, layout, utilities;定义层的优先顺序，最后列出的层优先级最高。块形式如@layer utilities { .padding-sm { padding: 0.5rem; } }创建命名层并包含样式。}}

{{知识点21}}{{@layer的优先级和嵌套：在级联层中，后声明的层优先级更高。未在任何层中声明的样式始终覆盖层内声明的样式。!important声明的优先级顺序与正常声明相反——在层内的!important声明优先于层外的!important声明。层可以嵌套，如@layer framework { @layer layout { } }，使用点号引用嵌套层：@layer framework.layout { ... }。支持通过@import导入到层中：@import "theme.css" layer(utilities);。}}

{{知识点22}}{{@layer的匿名层：使用@layer块At-rules但不包含层名可创建匿名级联层，如@layer { p { margin-block: 1rem; } }。匿名层的功能与命名层相同，但后续无法向其添加规则。匿名层和命名层的优先级顺序都按声明顺序决定。}}

{{知识点23}}{{@property规则：@property是CSS Houdini API的一部分，允许开发者在样式表中直接注册CSS自定义属性。语法为@property --rotation { syntax: "<angle>"; inherits: false; initial-value: 45deg; }。三个描述符：syntax（允许值类型，如"<angle>"、"<length>"、"*"等）、inherits（是否继承）、initial-value（初始值）。必须同时指定syntax和inherits，否则整个规则无效。}}

{{知识点24}}{{@property的价值和应用场景：@property使得自定义属性可以进行类型检查和约束、设置默认值、定义是否继承。它解决了普通CSS自定义属性（--属性）无法进行动画过渡的问题，因为浏览器知道了属性的类型。例如注册一个<angle>类型的--rotation属性后，就可以在动画中平滑过渡其值。如果同时通过CSS @property和JavaScript registerProperty()注册同名属性，JavaScript注册优先。}}

{{知识点25}}{{@container规则（容器查询）：@container是容器查询的核心At-rule，允许根据父容器的尺寸、样式或滚动状态来应用样式，而非视口大小。语法为@container (width > 400px) { h2 { font-size: 1.5em; } }。支持命名容器（@container sidebar (width > 400px) { ... }），需在容器元素上设置container-name和container-type属性。这是响应式设计的重大进步，使组件级响应式成为可能。}}

{{知识点26}}{{@container的查询类型：支持三种容器查询类型——尺寸查询（size container queries）检查容器的width、height、inline-size、block-size、aspect-ratio、orientation等；样式查询（style queries）检查容器的CSS自定义属性值如style(--theme: dark)；滚动状态查询（scroll-state queries）检查容器的滚动状态如scroll-state(stuck: top)和scroll-state(scrollable: top)。支持and、or、not逻辑运算符。}}

{{知识点27}}{{@scope规则：@scope用于定义样式的作用范围，实现精确的样式隔离。语法为@scope (.article-body) to (figure) { img { border: 5px solid black; } }。scope root定义上界（包含），scope limit定义下界（排除），形成"甜甜圈"作用域。省略scope limit则选择整个子树。支持内联用法：在<style>标签内使用@scope { ... }自动作用于父元素。}}

{{知识点28}}{{@scope与传统作用域方案对比：@scope相比传统方案（如BEM命名约定、CSS Modules、Shadow DOM）有独特优势——它提供原生CSS级联内的作用域，不改变选择器特异性计算方式，支持上界和下界限制，且可内联使用。@scope不会完全隔离继承属性（如color、font-family仍会继承），这是与Shadow DOM的重要区别。}}

{{知识点29}}{{@starting-style规则：@starting-style用于定义元素首次接收样式更新时的起始属性值，解决CSS过渡在元素初始渲染或从display: none切换时不触发的问题。可以作为独立块使用：@starting-style { [popover]:popover-open { opacity: 0; } }，或嵌套在规则集中。特别适用于popover、dialog等top layer元素的入场/退场过渡动画。}}

{{知识点30}}{{@starting-style的三种状态：使用@starting-style时需管理三种样式状态——starting-style状态（起始样式）、transitioned状态（过渡后的样式）、default state（默认状态）。元素首次渲染或从display: none切换时从@starting-style过渡，返回时则过渡到默认状态而非starting-style，因此入场和退场过渡可以不同。@starting-style仅与CSS transitions相关，使用CSS animations时不需要。}}

{{知识点31}}{{@view-transition规则：@view-transition用于将当前文档和目标文档纳入视图过渡，实现跨文档导航的页面过渡效果。语法为@view-transition { navigation: auto; types: slide; }。navigation描述符控制过渡行为（auto启用同源导航过渡，none禁用）。types描述符指定过渡类型标识，可用于条件化过渡样式。跨文档视图过渡要求源和目标文档同源。}}

{{知识点32}}{{@view-transition与View Transition API：@view-transition是View Transition API的CSS部分。配合::view-transition-old()和::view-transition-new()伪元素以及@keyframes可以创建自定义的页面过渡动画。支持SPA（同文档）和MPA（跨文档）两种场景。SPA中通过document.startViewTransition()触发，MPA中通过@view-transition { navigation: auto; }声明启用。}}

{{知识点33}}{{@position-try规则：@position-try用于定义锚点定位（Anchor Positioning）的自定义回退位置选项。语法为@position-try --try-option-name { top: anchor(--tooltip bottom); left: anchor(--tooltip left); }。名称使用dashed-ident（--前缀）。描述符包括position-anchor、position-area和各种inset/margin属性。通过position-try-fallbacks属性引用，实现当首选位置空间不足时自动切换到备选位置。}}

{{知识点34}}{{@font-feature-values规则：@font-feature-values（及其子规则@swash、@ornaments、@annotation、@stylistic、@styleset、@character-variant）用于控制OpenType字体按字体族的显示方式。它为font-variant-alternates中的特性索引定义自定义名称，使开发者可以使用易记的名称而非数字索引来引用字体特性。需要在@font-face声明的字体族中使用。}}

{{知识点35}}{{CSS At-rules与嵌套：现代CSS支持At-rules的嵌套，包括@media、@supports、@layer、@scope等都可以嵌套在选择器规则中。例如：.card { @media (width > 400px) { font-size: 1.2rem; } }。这大大减少了CSS文件中重复选择器的需要，使样式代码更紧凑和可维护。嵌套的At-rules遵循与顶层相同的行为规则。}}

{{知识点36}}{{CSS At-rules的级联和特异性：不同At-rules对CSS级联有不同影响。@layer改变了级联优先级——层内样式低于未分层样式，层的声明顺序决定优先级，!important的优先级反转。@scope影响选择器匹配范围但不改变特异性计算。@supports和@media是条件规则，仅在条件满足时应用其中的样式。理解这些差异对掌握CSS级联至关重要。}}

{{知识点37}}{{CSS At-rules的浏览器支持现状（2025年）：@media、@keyframes、@font-face、@import、@charset、@supports、@page、@namespace等属于Baseline广泛可用特性。@layer、@container、@property、@scope、@starting-style、@view-transition、@position-try属于较新特性，现代浏览器（Chrome、Firefox、Safari最新版）均已支持，但需注意旧版浏览器兼容性。使用@supports进行渐进增强是推荐做法。}}

{{知识点38}}{{At-rules在实际项目中的最佳实践：1）使用@layer组织CSS架构（如reset层、base层、components层、utilities层）；2）使用@container替代部分@media实现组件级响应式；3）使用@property注册自定义属性以支持动画；4）使用@scope实现组件样式隔离；5）使用@starting-style实现平滑的入场过渡；6）始终通过@supports进行特性检测实现渐进增强；7）@import应放在文件开头，考虑使用layer()和supports()参数。}}

{{知识点39}}{{CSS At-rules的JavaScript操作：所有At-rules都可以通过CSS对象模型（CSSOM）在JavaScript中操作。常用接口包括CSSMediaRule（@media）、CSSKeyframesRule（@keyframes）、CSSSupportsRule（@supports）、CSSLayerBlockRule/CSSLayerStatementRule（@layer）、CSSContainerRule（@container）、CSSFontFaceRule（@font-face）、CSSPageRule（@page）等。可以通过document.styleSheets获取样式表并操作其cssRules集合。}}

{{知识点40}}{{CSS条件规则的统一框架：@media、@supports、@container等都属于CSS条件规则（Conditional Group Rules），它们共享相似的语法结构——一个条件后跟一个包含规则的块。条件为真时应用块内的样式。这些条件规则可以相互嵌套，形成复杂的条件逻辑。CSS Conditional Rules模块定义了这些规则的通用行为和接口。}}
