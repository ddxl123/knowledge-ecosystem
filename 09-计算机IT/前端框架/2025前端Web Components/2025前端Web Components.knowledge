# 2025前端Web Components
# 领域: 计算机IT
# 学科: 前端框架

{{知识点1}}{{Web Components 是什么？Web Components 是一组 Web 平台 API，允许开发者创建可复用的自定义 HTML 元素。它由三项核心技术组成：Custom Elements（自定义元素）、Shadow DOM（影子 DOM）和 HTML Templates（HTML 模板）。这些技术可以独立使用，也可以组合使用，目标是实现组件的封装与复用，无需依赖任何框架。}}

{{知识点2}}{{Custom Elements（自定义元素）：Custom Elements 是 Web Components 的核心 API 之一，允许开发者通过 JavaScript 类定义新的 HTML 元素。使用 customElements.define() 方法注册自定义元素，注册后可以像使用原生 HTML 标签一样使用。元素名称必须包含连字符（如 my-component），以避免与原生元素冲突。}}

{{知识点3}}{{自定义元素的两种类型：1）Autonomous custom elements（自主自定义元素）：继承自 HTMLElement，需要从零实现所有行为；2）Customized built-in elements（定制内置元素）：继承自原生 HTML 元素（如 HTMLParagraphElement），通过 is 属性使用，可扩展现有元素的行为。注意：Safari 不计划支持定制内置元素。}}

{{知识点4}}{{自定义元素的生命周期回调：Web Components 定义了多个生命周期回调函数：connectedCallback() 在元素插入 DOM 时调用；disconnectedCallback() 在元素从 DOM 移除时调用；adoptedCallback() 在元素被移动到新文档时调用；attributeChangedCallback() 在属性变化时调用。此外还有新的 connectedMoveCallback() 用于通过 moveBefore() 移动元素时的状态保持。}}

{{知识点5}}{{connectedCallback() 的最佳实践：规范建议尽量在 connectedCallback() 中实现元素的初始化逻辑，而非 constructor()。因为 constructor 中不能检查元素的属性或子元素，也不能添加新属性或子元素。connectedCallback() 可能被多次调用（元素在 DOM 中移动时），因此需要注意幂等性。}}

{{知识点6}}{{observedAttributes 与属性响应：通过定义静态属性 observedAttributes 数组，可以指定需要监听的属性名。当这些属性发生变化时，attributeChangedCallback(name, oldValue, newValue) 会被自动调用。这是实现属性驱动更新的核心机制。}}

{{知识点7}}{{connectedMoveCallback() 与状态保持移动：这是较新的 API，当通过 Element.moveBefore() 移动元素时，会调用此回调而非 disconnectedCallback/connectedCallback。这避免了移动元素时触发不必要的清理和初始化逻辑，有助于保持元素状态。可通过定义空的 connectedMoveCallback() 来阻止其他两个回调的触发。}}

{{知识点8}}{{Shadow DOM（影子 DOM）：Shadow DOM 允许将一个隐藏的 DOM 树附加到常规 DOM 元素上。这棵影子树从 shadow root 开始，其内部结构对外部完全隔离。Shadow DOM 是实现样式和行为封装的关键技术，浏览器早已用它来封装 <video>、<input> 等原生元素的内部结构。}}

{{知识点9}}{{Shadow DOM 的关键术语：Shadow host（影子宿主）：附加影子 DOM 的常规 DOM 节点；Shadow tree（影子树）：影子 DOM 内部的 DOM 树；Shadow boundary（影子边界）：影子 DOM 结束、常规 DOM 开始的分界线；Shadow root（影子根）：影子树的根节点。}}

{{知识点10}}{{attachShadow() 与 mode 选项：通过 element.attachShadow({ mode: 'open' }) 创建影子 DOM。mode 有两个值：'open' 允许外部 JavaScript 通过 element.shadowRoot 访问影子 DOM 内部；'closed' 则使 shadowRoot 返回 null，阻止外部访问。但 closed 模式并非安全机制，浏览器扩展等方式仍可绕过。}}

{{知识点11}}{{Shadow DOM 的声明式创建：可以使用 <template shadowrootmode="open"> 在 HTML 中声明式地创建影子 DOM，无需 JavaScript。浏览器解析后会将 template 内容替换为附加到父元素的影子根。还支持 shadowrootclonable 和 shadowrootdelegatesfocus 等属性。这对服务端渲染（SSR）场景特别有用。}}

{{知识点12}}{{Shadow DOM 的 CSS 封装：影子 DOM 内部的样式不会影响外部文档，外部样式也不会渗透到影子 DOM 内部。这是通过 CSS 作用域隔离实现的。在影子树内使用 <style> 标签定义的样式仅作用于该影子树。这解决了全局 CSS 冲突的问题。}}

{{知识点13}}{{Shadow DOM 的 JavaScript 封装：document.querySelectorAll() 等常规 DOM 查询方法无法穿透影子边界找到影子 DOM 内部的元素。影子 DOM 中的元素对外部 JavaScript 是不可见的，除非通过 open 模式的 shadowRoot 属性主动访问。}}

{{知识点14}}{{CSS 伪类选择器与 Web Components：:host 选择影子宿主元素；:host(selector) 仅在影子宿主匹配 selector 时选中；:host-context(selector) 当影子宿主的祖先匹配 selector 时选中；:defined 匹配所有已定义的元素（包括自定义元素）；:state(custom-state) 匹配处于特定自定义状态的元素。}}

{{知识点15}}{{CSS 伪元素与 Web Components：::part 选择影子树中具有匹配 part 属性的元素，允许外部有选择地样式化影子 DOM 内部元素；::slotted 选择插入到 slot 中的内容。这些机制提供了受控的样式穿透能力，在封装与灵活性之间取得平衡。}}

{{知识点16}}{{HTML <template> 元素：<template> 用于定义不会在页面加载时渲染的 HTML 片段模板。其内容可以通过 JavaScript 的 template.content 访问并克隆使用。它是 Web Components 中构建可复用结构的基础，配合 importNode() 或 cloneNode() 将模板内容插入影子 DOM。}}

{{知识点17}}{{<slot> 元素与内容分发：Slot 是影子 DOM 中的占位符，允许外部内容插入到组件内部。通过 name 属性定义具名插槽（named slot），外部使用 slot="name" 属性指定内容去向。未指定 slot 属性的内容会进入默认插槽（unnamed slot）。Slot 支持回退内容（fallback content）。}}

{{知识点18}}{{Slot 的 slotchange 事件：当 slot 中的内容发生变化时，会触发 slotchange 事件。这是监听组件内容变化的重要机制。需要注意的是，slotchange 只在直接子节点变化时触发，子节点的属性变化不会触发此事件。}}

{{知识点19}}{{Custom Element 注册规则：使用 customElements.define(name, constructor, options) 注册。name 必须以小写字母开头且包含连字符（如 my-button）。options 仅用于定制内置元素，包含 extends 属性指定要扩展的原生元素。注册是全局的，每个名称只能注册一次。}}

{{知识点20}}{{Scoped Custom Element Registries（作用域自定义元素注册表）：为了解决全局注册名称冲突的问题，可以创建独立的注册表，其定义仅适用于特定 DOM 子树。通过 new CustomElementRegistry() 构造函数创建，配合 CustomElementRegistry.initialize() 方法关联到 DOM 子树。这对于大型应用和第三方组件集成非常有用。}}

{{知识点21}}{{自定义元素的 constructor() 规范要求：constructor 中必须首先调用 super()。不能在 constructor 中检查或操作元素的属性、子元素，也不能添加新属性或子元素。constructor 适合设置初始状态、默认值、注册事件监听器和创建影子根。}}

{{知识点22}}{{Web Components 的事件传播：Shadow DOM 中的事件传播遵循 composed path 机制。Event.composed 属性表示事件是否会穿越影子边界传播到常规 DOM。Event.composedPath() 返回事件的完整传播路径，但不包含 closed 模式影子树中的节点。部分事件（如 focus、mousemove）默认 composed 为 true。}}

{{知识点23}}{{Web Components 与框架的互操作：Web Components 是 Web 标准，可以与任何框架配合使用。React 19+ 对 Web Components 的支持已有显著改善。Angular、Vue 原生支持 Web Components。框架组件可以使用 Web Components，Web Components 也可以在框架模板中使用，实现真正的跨框架复用。}}

{{知识点24}}{{Lit 框架简介：Lit 是 Google 维护的轻量级 Web Components 库（约 5KB），基于 Web Components 标准构建。它提供了响应式属性、声明式模板、高效的 DOM 更新和生命周期管理。Lit 组件就是标准的 Web Components，可以在任何 HTML 环境中使用。Lit 是 Polymer 的继任者。}}

{{知识点25}}{{Lit 的核心特性：1）响应式属性系统：属性变化自动触发重新渲染；2）Tagged template literals：使用 html 和 css 标签模板定义模板和样式；3）高效更新：仅更新变化的 DOM 部分（基于 lit-html 模板引擎）；4）装饰器支持：TypeScript 装饰器简化组件定义；5）标准兼容：生成标准 Web Components。}}

{{知识点26}}{{Stencil.js 框架：Stencil 是 Ionic 团队开发的 Web Components 编译器/工具链。它采用类 JSX 的语法编写组件，编译为标准 Web Components。支持 TypeScript、虚拟 DOM、异步渲染和自动懒加载。Stencil 的优势在于提供类 React 的开发体验，同时输出框架无关的标准组件。}}

{{知识点27}}{{Web Components 的无障碍访问（Accessibility）：Web Components 需要特别关注无障碍性。使用 ElementInternals API（attachInternals()）可以为自定义元素添加 ARIA 角色、标签和状态。支持 shadowrootdelegatesfocus 属性控制焦点委托。自定义元素应遵循 WAI-ARIA 最佳实践，确保键盘导航和屏幕阅读器兼容。}}

{{知识点28}}{{ElementInternals API：ElementInternals 允许自定义元素像原生表单元素一样参与表单提交。通过 attachInternals() 获取，可以设置表单值（setFormData）、验证状态（setValidity）和可访问性属性。这使得 Web Components 能够无缝集成到 HTML 表单中。}}

{{知识点29}}{{Web Components 的样式方案：1）Shadow DOM 封装样式：天然隔离；2）CSS Custom Properties（CSS 变量）：可穿透影子边界，是组件主题定制的主要方式；3）::part() 伪元素：允许外部选择性样式化；4）Constructable Stylesheets：通过 new CSSStyleSheet() 创建可共享的样式表，减少内存占用；5）adoptedStyleSheets：将样式表附加到影子根。}}

{{知识点30}}{{Constructable Stylesheets（可构造样式表）：通过 new CSSStyleSheet() 创建样式表实例，然后通过 shadowRoot.adoptedStyleSheets = [sheet] 应用。优势在于一个样式表实例可以被多个影子根共享，显著减少内存消耗。支持 replace() 和 replaceSync() 方法动态修改样式。}}

{{知识点31}}{{Web Components 的性能优化：1）使用惰性初始化：延迟创建影子 DOM 和复杂子元素；2）避免在 connectedCallback 中进行大量 DOM 操作；3）使用 Constructable Stylesheets 共享样式；4）合理使用 observedAttributes 减少不必要的回调；5）对于大型列表使用虚拟滚动等技术。}}

{{知识点32}}{{Web Components 与 SSR（服务端渲染）：声明式影子 DOM（Declarative Shadow DOM，DSD）通过 <template shadowrootmode="open"> 实现服务端渲染。服务端可以直接输出带有影子 DOM 的 HTML，客户端无需 JavaScript 即可渲染。这解决了传统 Web Components 依赖客户端 JS 的 SEO 和首屏性能问题。}}

{{知识点33}}{{CustomStateSet 与自定义状态：通过 ElementInternals.states 属性获取 CustomStateSet，可以为自定义元素定义自定义状态。使用 CSS 的 :state() 伪类选择器匹配这些状态。例如：element.internals.states.add('checked')，然后在 CSS 中使用 :state(checked) 选择器。这为样式化自定义状态提供了标准化方案。}}

{{知识点34}}{{Web Components 的设计系统实践：使用 Web Components 构建设计系统的优势在于框架无关性。组件可以在 React、Vue、Angular 等不同框架的项目中统一使用。推荐的设计模式包括：使用 CSS 变量实现主题化、通过 slot 实现内容组合、通过属性/事件实现 API 交互、提供良好的 TypeScript 类型定义。}}

{{知识点35}}{{Web Components 的测试策略：1）使用 @web/test-runner 进行浏览器环境测试；2）使用 @open-wc/testing 提供的测试工具；3）测试生命周期回调的正确触发；4）测试属性变化响应；5）测试 slot 内容分发；6）测试事件的触发和传播；7）使用 Storybook 进行可视化测试和文档。}}

{{知识点36}}{{Web Components 的浏览器支持现状（2025）：Custom Elements v1、Shadow DOM v1 和 HTML Templates 在所有现代浏览器中均已获得完整支持，包括 Chrome、Firefox、Safari 和 Edge。Scoped Custom Element Registries 和 Declarative Shadow DOM 等新特性也已在主流浏览器中实现或正在推进中。}}

{{知识点37}}{{Web Components 的事件通信模式：自定义元素通过 CustomEvent 进行向上传递信息（子组件到父组件），通过属性设置向下传递数据（父组件到子组件）。推荐使用 detail 属性携带数据，并设置 composed 和 bubbles 控制事件的传播范围。这是 Web Components 的单向数据流模式。}}

{{知识点38}}{{Web Components 与 ES Modules：Web Components 天然支持 ES Modules，组件定义可以作为模块导出。使用 <script type="module"> 加载，自动获得严格模式和作用域隔离。通过 import/export 可以实现组件的按需加载和代码分割。这与现代前端构建工具（Vite、Webpack）完美集成。}}

{{知识点39}}{{Web Components 的常见陷阱：1）忘记调用 super()；2）在 constructor 中操作 DOM/属性；3）Shadow DOM 的事件冒泡行为与常规 DOM 不同；4）CSS 继承在影子边界处被重置（除 CSS Custom Properties 等少数属性外）；5）全局注册表的名称冲突；6）connectedCallback 可能多次调用需注意幂等性。}}

{{知识点40}}{{Web Components 的未来发展方向：1）Scoped Custom Element Registries 解决命名冲突；2）CSS Anchor Positioning 与组件定位；3）Dialog/Popover API 与组件集成；4）CSS Container Queries 对组件响应式设计的支持；5）跨 Shadow DOM 的样式共享机制改进；6）更强的表单集成能力。}}
