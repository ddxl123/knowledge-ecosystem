# 2025网络爬虫入门
# 领域: 计算机IT
# 学科: 网络爬虫
# 来源: 2025网络爬虫入门
# 描述: 网络爬虫核心知识点

{{网络爬虫的定义与用途}}{{网络爬虫（Web Crawler/Spider）是一种自动抓取互联网信息的程序。它模拟浏览器向服务器发送请求，获取网页内容并提取所需数据。常见用途包括：搜索引擎索引、价格监控、舆情分析、学术数据采集、新闻聚合等。2025年的爬虫技术已高度成熟，Python是最主流的爬虫开发语言，配合丰富的生态系统（requests、Scrapy、Playwright等）可以高效完成各类数据采集任务。}}

{{HTTP协议基础}}{{HTTP（超文本传输协议）是爬虫与服务器通信的基础。它是无状态的请求-响应协议，基于TCP/IP，默认端口80（HTTPS为443）。一个完整的HTTP事务包括：客户端发送请求（Request）→ 服务器处理并返回响应（Response）。HTTP/1.1支持持久连接（Keep-Alive），HTTP/2支持多路复用，HTTP/3基于QUIC协议。爬虫开发者必须理解HTTP协议才能正确构造请求和解析响应。}}

{{HTTP请求方法}}{{常用的HTTP请求方法包括：GET（获取资源，参数在URL中）、POST（提交数据，参数在请求体中）、PUT（更新资源）、DELETE（删除资源）、HEAD（仅获取响应头）、OPTIONS（查询支持的方法）。爬虫最常用的是GET和POST。GET请求参数会暴露在URL中，有长度限制；POST请求参数在请求体中，适合提交表单、登录认证等场景。2025年RESTful API普遍遵循这些方法语义。}}

{{HTTP状态码}}{{HTTP状态码表示服务器对请求的处理结果，是爬虫判断请求是否成功的关键。常见状态码：200（成功）、301（永久重定向）、302（临时重定向）、304（未修改，使用缓存）、400（请求错误）、403（禁止访问）、404（未找到）、429（请求过多，被限流）、500（服务器内部错误）。爬虫应重点关注403和429，这通常意味着触发了反爬机制，需要调整策略。}}

{{HTTP请求头详解}}{{请求头（Request Headers）携带客户端信息，对爬虫至关重要。关键字段：User-Agent（浏览器标识，反爬必检）、Referer（来源页面，防盗链）、Cookie（会话信息）、Accept（期望的内容类型）、Accept-Language（语言偏好）、Accept-Encoding（支持的压缩格式）、Connection（连接管理）。爬虫必须合理设置请求头，尤其是User-Agent和Referer，否则极易被识别为机器程序。2025年主流网站普遍检测请求头完整性。}}

{{HTTP响应头与编码}}{{响应头（Response Headers）包含服务器返回的元信息。重要字段：Content-Type（内容类型，如text/html、application/json）、Content-Length（内容长度）、Set-Cookie（设置Cookie）、Location（重定向目标URL）、Content-Encoding（压缩方式，如gzip、br）。爬虫需要正确处理编码：先检查Content-Type中的charset，再用对应编码解码响应体。2025年多数网站使用UTF-8编码，部分旧站仍用GBK/GB2312。}}

{{URL结构解析}}{{URL（统一资源定位符）的标准结构：协议://域名:端口/路径?查询参数#片段标识。例如：https://www.example.com:443/search?q=python&page=1#results。爬虫需要掌握URL拼接（urljoin）、参数编码（urlencode）、路径解析等操作。Python的urllib.parse模块提供urlparse、urljoin、urlencode等工具函数。相对URL需要基于基准URL拼接为绝对URL才能正确请求。}}

{{Python requests库入门}}{{requests是Python最流行的HTTP库，语法简洁，功能强大。安装：pip install requests。基本用法：import requests; response = requests.get('https://example.com')。支持GET/POST/PUT/DELETE等所有HTTP方法，自动处理编码、重定向、Cookie等。返回的Response对象包含status_code（状态码）、headers（响应头）、text（文本内容）、json()（JSON解析）、content（二进制内容）等属性。2025年requests 2.32+版本原生支持HTTP/2。}}

{{GET请求与参数传递}}{{GET请求通过URL传递参数。使用requests时，通过params字典自动编码：requests.get(url, params={'q': 'python', 'page': 1})。params中的列表会自动展开为多个同名参数（如a=1&a=2）。也可以手动拼接URL字符串，但需注意特殊字符编码（如中文用quote函数）。GET请求适合查询、搜索等不修改服务器数据的操作。响应内容通过response.text获取字符串，response.json()解析JSON。}}

{{POST请求与表单提交}}{{POST请求将数据放在请求体中发送。requests支持多种数据格式：表单数据用data参数（自动设置Content-Type: application/x-www-form-urlencoded），JSON数据用json参数（自动设置Content-Type: application/json），文件上传用files参数。示例：requests.post(url, data={'username': 'test', 'password': '123'})。登录认证通常需要POST请求配合Cookie/Session维持会话状态。2025年API普遍采用JSON格式的POST请求。}}

{{请求头伪装与反检测}}{{反爬网站会检查请求头的完整性和真实性。伪装策略：1）设置常见浏览器的User-Agent（从真实浏览器复制或使用fake-useragent库随机生成）；2）添加Referer指向目标网站；3）设置Accept、Accept-Language等字段模拟真实浏览器；4）使用Session保持Cookie。高级策略：通过浏览器开发者工具抓取真实请求头，原样复制到爬虫中。2025年Cloudflare等WAF还会检测HTTP/2指纹和TLS指纹。}}

{{Cookie与Session管理}}{{Cookie是服务器保存在客户端的会话信息，爬虫需要正确处理Cookie才能维持登录状态。requests.Session()对象会自动管理Cookie：登录时Cookie自动保存，后续请求自动携带。手动操作：response.cookies获取响应Cookie，requests.get(url, cookies={'key': 'value'})设置Cookie。注意HttpOnly标记的Cookie不能被JavaScript访问但爬虫可以直接使用。Session还共享连接池，提升请求效率。}}

{{HTML文档结构基础}}{{HTML（超文本标记语言）是网页的基本结构。由标签（Tag）组成，常见标签：html（根元素）、head（头部信息）、body（页面主体）、div（块级容器）、span（行内容器）、a（超链接）、img（图片）、table（表格）、form（表单）、script（JavaScript）、style（CSS样式）。标签可以有属性（如class、id、href、src）。爬虫的核心任务就是从HTML结构中定位并提取所需数据。理解DOM树结构是HTML解析的基础。}}

{{BeautifulSoup解析HTML}}{{BeautifulSoup是Python最常用的HTML/XML解析库。安装：pip install beautifulsoup4。基本用法：from bs4 import BeautifulSoup; soup = BeautifulSoup(html, 'html.parser')。支持多种解析器：html.parser（内置）、lxml（速度快）、html5lib（容错性强）。常用方法：find()查找第一个匹配元素、find_all()查找所有匹配元素、select()使用CSS选择器、get_text()提取文本、获取属性用tag['href']。BeautifulSoup API直观易用，适合初学者。}}

{{lxml解析器与高性能HTML处理}}{{lxml是基于C语言libxml2/libxslt的高性能Python解析库。安装：pip install lxml。解析速度远超BeautifulSoup的html.parser。用法：from lxml import etree; tree = etree.HTML(html_text)。支持XPath和CSS选择器（需安装cssselect）。lxml.etree.HTML()自动修复不完整的HTML。适合大规模爬取场景。lxml还可以处理XML文档，支持XSLT转换。2025年lxml 5.x版本性能进一步优化，仍是高性能爬虫的首选解析器。}}

{{CSS选择器详解}}{{CSS选择器是定位HTML元素的强大工具，在爬虫中广泛使用。基本语法：标签名（div）、类名（.class）、ID（#id）、属性（[attr=value]）、后代（div p）、子元素（div>p）、相邻兄弟（div+p）、通用兄弟（div~p）、伪类（:nth-child(2)、:first-child）。BeautifulSoup的select()方法和lxml的cssselect()都支持CSS选择器。示例：soup.select('div.content > p.text')选择class为content的div下直接子元素p中class为text的元素。}}

{{XPath基础语法}}{{XPath是XML路径语言，用于在HTML/XML文档中定位节点。基本语法：/（绝对路径，从根节点）、//（相对路径，任意位置）、.（当前节点）、..（父节点）、@（属性）。常用表达式：//div[@class='title']（class为title的div）、//a/@href（所有a标签的href属性）、//p/text()（所有p标签的文本内容）、//div[contains(@class,'item')]（class包含item的div）。lxml的tree.xpath()方法返回匹配节点列表。XPath比CSS选择器功能更强大，支持文本匹配和数学运算。}}

{{XPath轴与高级函数}}{{XPath轴定义了节点之间的关系。常用轴：child（子节点）、parent（父节点）、ancestor（祖先）、descendant（后代）、following-sibling（后续兄弟）、preceding-sibling（前序兄弟）、self（自身）。高级函数：contains()（包含）、starts-with()（开头匹配）、text()（文本节点）、normalize-space()（去除空白）、position()（位置）、last()（最后一个）。示例：//div[contains(@class,'item')]/following-sibling::div[1]获取第一个后续兄弟div。这些高级功能在复杂页面解析中非常实用。}}

{{正则表达式在爬虫中的应用}}{{正则表达式（Regular Expression）用于文本匹配和提取，适合处理非结构化数据。Python的re模块提供支持。常用模式：.（任意字符）、*（零或多次）、+（一或多次）、?（零或一次）、\d（数字）、\w（字母数字下划线）、\s（空白字符）、[]（字符集）、()（捕获组）。爬虫常用场景：提取电话号码（r'1[3-9]\d{9}'）、邮箱（r'[\w.]+@[\w.]+')、URL链接、价格信息等。注意正则不应用于解析HTML结构，应配合BeautifulSoup/lxml使用。}}

{{JSON数据解析与处理}}{{现代网站大量使用JSON（JavaScript对象表示法）作为数据交换格式。Python的json模块：import json; data = json.loads(json_string)解析JSON字符串，json.dumps(data)序列化为JSON。requests库的response.json()直接解析响应JSON。常见场景：API接口返回JSON数据、Ajax请求的响应、嵌入在HTML中的JSON数据（如<script>标签内的变量）。JSONPath和jq工具可以方便地提取嵌套JSON中的特定字段。2025年JSON仍是API数据格式的主流标准。}}

{{Ajax与动态数据抓取}}{{许多现代网站使用Ajax技术动态加载数据，页面初始HTML不包含完整数据。识别方法：页面滚动加载更多、点击按钮刷新内容、URL不变但内容更新。抓取策略：1）在浏览器开发者工具的Network标签中找到实际数据请求的API接口；2）直接请求该API获取JSON数据，比解析HTML更高效；3）注意API可能有分页参数、时间戳、签名等。这是2025年最推荐的动态数据获取方式，比渲染整个页面效率高得多。}}

{{反爬机制概述与应对策略}}{{网站反爬的常见手段：1）User-Agent检测（过滤非浏览器请求）；2）IP频率限制（单IP请求过多被封）；3）Cookie验证（要求携带特定Cookie）；4）验证码（图形验证码、滑块验证码、行为验证码）；5）JavaScript渲染（页面内容由JS动态生成）；6）请求签名（参数加密、时间戳验证）；7）Honeypot陷阱（隐藏链接吸引爬虫进入黑名单）。应对策略：模拟真实浏览器行为、控制请求频率、使用代理IP、处理验证码、渲染JavaScript。}}

{{IP代理池的构建与使用}}{{当爬虫IP被目标网站封禁时，需要使用代理IP绕过限制。代理类型：透明代理（服务器知道你的真实IP）、匿名代理（服务器知道你在用代理但不知道真实IP）、高匿代理（服务器不知道你用代理）。构建代理池：1）购买付费代理服务（快代理、芝麻代理等）；2）维护免费代理列表并定期验证可用性；3）实现代理轮换和失效检测。requests使用代理：requests.get(url, proxies={'http': 'http://proxy:port'})。2025年住宅代理和移动代理成为主流。}}

{{User-Agent轮换与浏览器指纹}}{{User-Agent是识别客户端类型的关键标识。轮换策略：维护常见浏览器UA列表，每次请求随机选择。fake-useragent库提供便捷的UA随机生成：from fake_useragent import UserAgent; ua = UserAgent(); headers = {'User-Agent': ua.random}。2025年高级反爬还会检测浏览器指纹，包括：Canvas指纹、WebGL指纹、屏幕分辨率、时区、语言、已安装插件等。应对方案：使用Playwright/Puppeteer等无头浏览器，配合stealth插件隐藏自动化痕迹。}}

{{验证码识别技术}}{{验证码是常见的反爬手段。常见类型：1）图形验证码（扭曲文字/数字）；2）滑块验证码（拖动滑块到指定位置）；3）点选验证码（按顺序点击特定元素）；4）Google reCAPTCHA/hCaptcha（行为验证）。识别方案：OCR技术（Tesseract、ddddocr库）处理简单图形验证码；第三方打码平台（2Captcha、AntiCaptcha）处理复杂验证码；深度学习模型训练专用识别器。2025年AI验证码识别能力大幅提升，但网站验证方式也在不断升级。}}

{{Selenium浏览器自动化}}{{Selenium是经典的浏览器自动化工具，可以渲染JavaScript、模拟用户操作。安装：pip install selenium。基本用法：from selenium import webdriver; driver = webdriver.Chrome(); driver.get(url); element = driver.find_element('css selector', '.class')。支持Chrome、Firefox等主流浏览器。优点：能处理任何JavaScript渲染的页面；缺点：速度慢、资源消耗大。2025年Selenium 4支持WebDriver BiDi协议，性能有所提升，但仍建议优先使用API抓取方式。}}

{{Playwright——现代浏览器自动化}}{{Playwright是微软开发的现代化浏览器自动化工具，2025年已成为Selenium的主流替代品。安装：pip install playwright && playwright install。支持Chromium、Firefox、WebKit三大引擎。优势：1）自动等待元素加载；2）原生支持异步（async/await）；3）更强的反检测能力；4）更快的执行速度；5）原生支持移动端模拟。from playwright.sync_api import sync_playwright; with sync_playwright() as p: browser = p.chromium.launch(); page = browser.new_page(); page.goto(url)。}}

{{Scrapy框架概述}}{{Scrapy是Python最强大的爬虫框架，适合大规模数据采集。安装：pip install scrapy。核心组件：Spider（定义爬取逻辑）、Item（数据模型）、Pipeline（数据处理管道）、Middleware（中间件，处理请求/响应）、Engine（调度引擎）、Downloader（下载器）、Scheduler（请求调度器）。优势：内置请求去重、并发控制、数据导出、日志系统。支持异步IO（基于Twisted），性能优异。2025年Scrapy 2.11+版本支持async/await语法，进一步提升开发体验。}}

{{Scrapy项目结构与创建}}{{使用scrapy startproject project_name创建项目。标准目录结构：project_name/（项目根目录）、scrapy.cfg（部署配置）、project_name/（Python包）、__init__.py、items.py（数据模型定义）、middlewares.py（中间件）、pipelines.py（数据管道）、settings.py（全局配置）、spiders/（爬虫目录）。settings.py中重要配置：ROBOTSTXT_OBEY（遵守robots.txt）、CONCURRENT_REQUESTS（并发数）、DOWNLOAD_DELAY（下载延迟）、DEFAULT_REQUEST_HEADERS（默认请求头）。}}

{{Scrapy Spider编写}}{{Spider是Scrapy的核心，定义了爬取的URL、解析逻辑和数据提取规则。基本结构：import scrapy; class MySpider(scrapy.Spider): name='myspider'; start_urls=['https://example.com']; def parse(self, response): yield {'title': response.css('h1::text').get()}。Spider支持三种请求方式：1）start_urls自动生成初始请求；2）start_requests()方法手动构造；3）yield scrapy.Request(url, callback=self.parse_item)在回调中生成新请求。支持通过meta传递数据到回调函数。2025年推荐使用async def parse风格编写Spider。}}

{{Scrapy Item与数据管道}}{{Item定义爬取数据的结构化模型：import scrapy; class ProductItem(scrapy.Item): name = scrapy.Field(); price = scrapy.Field(); url = scrapy.Field()。Pipeline（数据管道）处理Spider提取的数据，常用操作：数据清洗（去除空白、格式化）、验证（检查必填字段）、去重（基于特定字段）、存储（写入文件/数据库）。Pipeline需要在settings.py中启用：ITEM_PIPELINE = {'myproject.pipelines.CleanPipeline': 100, 'myproject.pipelines.SavePipeline': 200}。数字越小优先级越高。}}

{{Scrapy中间件机制}}{{中间件分为Downloader Middleware（下载中间件）和Spider Middleware（爬虫中间件）。下载中间件最常用，可在请求发出前和响应返回后进行处理。典型用途：1）UserAgentMiddleware——随机切换User-Agent；2）ProxyMiddleware——设置代理IP；3）RetryMiddleware——失败重试；4）处理Cookie和重定向。自定义中间件需实现process_request()和process_response()方法，并在settings.py的DOWNLOADER_MIDDLEWARES中注册。中间件是实现反反爬策略的核心机制。}}

{{数据存储——CSV与JSON文件}}{{最简单的数据存储方式。CSV存储：使用Python csv模块或pandas。import csv; with open('data.csv', 'w', newline='', encoding='utf-8') as f: writer = csv.DictWriter(f, fieldnames=['name','price']); writer.writeheader(); writer.writerows(data_list)。JSON存储：import json; with open('data.json', 'w', encoding='utf-8') as f: json.dump(data_list, f, ensure_ascii=False, indent=2)。Scrapy内置Feed Export：scrapy crawl spider -o data.json。CSV适合表格数据，JSON适合嵌套结构数据。2025年JSON Lines（.jsonl）格式因流式写入优势而流行。}}

{{数据存储——SQLite与MySQL}}{{数据库适合结构化数据的持久化存储。SQLite：Python内置支持，无需服务器，适合小规模项目。import sqlite3; conn = sqlite3.connect('data.db'); cursor = conn.cursor(); cursor.execute('CREATE TABLE IF NOT EXISTS items (id INTEGER PRIMARY KEY, name TEXT, price REAL)')。MySQL：需安装pymysql，适合大规模项目。from pymysql import connect; conn = connect(host='localhost', user='root', password='pwd', db='crawler')。Scrapy可通过自定义Pipeline将数据写入数据库，实现去重和增量更新。}}

{{数据存储——MongoDB与Elasticsearch}}{{MongoDB是文档型数据库，适合存储非结构化/半结构化爬取数据。安装pymongo：from pymongo import MongoClient; client = MongoClient('localhost', 27017); db = client['crawler_db']; collection = db['items']; collection.insert_one({'name': 'test', 'price': 99.9})。优势：无需预定义Schema，支持嵌套文档，适合JSON格式数据。Elasticsearch是分布式搜索引擎，适合海量数据的全文检索和分析。2025年两者配合使用（MongoDB存储原始数据，ES提供搜索能力）是常见的数据存储架构。}}

{{robots.txt协议与爬虫道德}}{{robots.txt是网站告知爬虫哪些页面可以访问的协议。位于网站根目录（如https://example.com/robots.txt）。格式：User-agent: *（适用所有爬虫）、Disallow: /admin/（禁止爬取admin目录）、Allow: /public/（允许爬取）、Crawl-delay: 1（请求间隔秒数）。Python的urllib.robotparser模块可以解析robots.txt：from urllib.robotparser import RobotParser; rp = RobotParser(); rp.set_url('https://example.com/robots.txt'); rp.read(); rp.can_fetch('*', url)。遵守robots.txt是爬虫开发者的基本道德准则。}}

{{爬虫性能优化技巧}}{{提升爬虫效率的关键策略：1）并发请求——使用异步IO（aiohttp、Scrapy内置并发）替代同步阻塞；2）请求去重——使用布隆过滤器或集合避免重复请求；3）增量爬取——记录已爬URL，只抓取新数据；4）合理设置延迟——在不触发反爬的前提下最大化速度；5）连接复用——使用Session保持TCP连接；6）多线程/多进程——充分利用多核CPU；7）分布式部署——将任务分发到多台机器。Scrapy默认支持并发和去重，是高性能爬虫的首选框架。}}

{{分布式爬虫架构简介}}{{单机爬虫在大规模数据采集时存在性能瓶颈，分布式爬虫将任务分发到多台机器并行执行。Scrapy-Redis是最常用的分布式方案：基于Redis作为共享的请求队列和去重集合，多个Spider实例从同一队列取任务。安装：pip install scrapy-redis。配置：SCHEDULER = 'scrapy_redis.scheduler.Scheduler'；DUPEFILTER_CLASS = 'scrapy_redis.dupefilter.RBFingerprintFilter'；REDIS_URL = 'redis://localhost:6379'。2025年Scrapy-Redis仍是最成熟的分布式爬虫方案，配合Docker容器化部署更加便捷。}}

{{爬虫的法律与合规问题}}{{网络爬虫涉及多项法律法规，必须注意合规：1）遵守robots.txt协议和网站服务条款；2）不爬取个人隐私数据（姓名、电话、身份证等），受《个人信息保护法》保护；3）不进行破坏性爬取（导致服务器宕机），可能触犯《刑法》非法获取计算机信息系统数据罪；4）尊重版权，不大量复制受版权保护的内容；5）爬取公开数据一般合法，但需注意数据使用方式。2025年全球数据保护法规趋严，爬虫开发者务必了解当地法律。}}

{{爬虫实战——完整项目流程}}{{一个完整的爬虫项目流程：1）需求分析——明确目标数据和数据量；2）目标分析——用浏览器开发者工具分析页面结构和网络请求；3）技术选型——静态页面用requests+BeautifulSoup，动态页面用Playwright或找API接口；4）编写爬虫——实现请求、解析、存储逻辑；5）反爬应对——处理验证码、代理、限流等；6）数据清洗——去除无效数据、格式统一；7）测试与优化——小规模测试后全量运行；8）部署运行——使用cron定时执行或部署到云服务器。}}
