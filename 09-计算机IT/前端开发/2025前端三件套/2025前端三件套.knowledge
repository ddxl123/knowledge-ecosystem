# 2025前端三件套
# 领域: 计算机IT
# 学科: 前端开发
# 来源: 2025前端三件套
# 描述: HTML/CSS/JavaScript前端开发核心知识点

{{知识点1}}{{DOCTYPE 声明}}
<!DOCTYPE html> 是 HTML5 的文档类型声明，必须放在 HTML 文档的第一行。它告诉浏览器使用标准模式（standards mode）来渲染页面，而不是怪异模式（quirks mode）。HTML5 的 DOCTYPE 非常简洁，不需要引用 DTD。

{{知识点2}}{{HTML5 语义化标签}}
HTML5 引入了多个语义化标签：<header>（页头）、<nav>（导航）、<main>（主体内容）、<article>（独立内容块）、<section>（章节）、<aside>（侧边栏）、<footer>（页脚）。语义化标签有助于 SEO、可访问性和代码可读性。

{{知识点3}}{{HTML5 多媒体标签}}
<video> 和 <audio> 标签用于嵌入视频和音频。常用属性：controls（显示控件）、autoplay（自动播放）、muted（静音）、loop（循环）、preload（预加载策略）。示例：<video src="movie.mp4" controls width="640"></video>。

{{知识点4}}{{HTML5 Canvas}}
<canvas> 元素用于通过 JavaScript 绘制图形。获取绘图上下文：const ctx = canvas.getContext('2d')。常用方法：fillRect()、strokeRect()、arc()、beginPath()、moveTo()、lineTo()、fill()、stroke()。Canvas 是像素级绘制，适合游戏和数据可视化。

{{知识点5}}{{HTML 表单元素}}
HTML5 新增表单类型：email、url、tel、number、range、date、time、color、search。新增表单属性：placeholder、required、pattern、autofocus、autocomplete。新增元素：<datalist>（下拉建议）、<output>（计算结果）、<progress>（进度条）、<meter>（度量值）。

{{知识点6}}{{HTML 表格}}
<table> 用于展示表格数据。子元素：<thead>（表头）、<tbody>（表体）、<tfoot>（表尾）、<tr>（行）、<th>（表头单元格）、<td>（数据单元格）。属性：colspan（跨列）、rowspan（跨行）。注意：表格应用于展示数据，不应用于页面布局。

{{知识点7}}{{meta 标签}}
常用 meta 标签：<meta charset="UTF-8">（字符编码）、<meta name="viewport" content="width=device-width, initial-scale=1.0">（响应式视口）、<meta name="description" content="...">（SEO 描述）、<meta http-equiv="refresh" content="5;url=...">（页面跳转）。

{{知识点8}}{{HTML 实体}}
HTML 实体用于显示保留字符：&lt;（<）、&gt;（>）、&amp;（&）、&quot;（"）、&apos;（'）、&nbsp;（空格）、&copy;（©）。格式：&name; 或 &#数字;。

{{知识点9}}{{HTML5 Web Storage}}
localStorage 和 sessionStorage 是客户端存储方案。localStorage 永久存储（除非手动清除），sessionStorage 在会话结束时清除。API：setItem(key, value)、getItem(key)、removeItem(key)、clear()。容量约 5-10MB，只能存字符串。

{{知识点10}}{{HTML5 语义化链接标签}}
<a> 标签的 target 属性：_self（当前窗口）、_blank（新窗口，建议配合 rel="noopener noreferrer"）、_parent（父框架）、_top（顶层框架）。download 属性可提示下载而非导航。href="mailto:" 和 href="tel:" 可创建邮件和电话链接。

{{知识点11}}{{CSS 选择器优先级}}
CSS 优先级计算：!important > 内联样式 > ID 选择器(#) > 类选择器(.) / 属性选择器([]) / 伪类(:) > 元素选择器 / 伪元素(::) > 通配符(*)。优先级用四位表示：(0,0,0,0)。相同优先级后声明覆盖前声明。继承的样式优先级最低。

{{知识点12}}{{CSS 盒模型}}
每个元素都是一个盒子：content → padding → border → margin。box-sizing: content-box（默认，width 只含内容）vs border-box（width 含 content + padding + border）。开发中通常全局设置 box-sizing: border-box 便于计算。

{{知识点13}}{{Flexbox 弹性布局}}
display: flex 创建弹性容器。主轴方向：flex-direction（row/column/row-reverse/column-reverse）。主轴对齐：justify-content（flex-start/center/flex-end/space-between/space-around/space-evenly）。交叉轴对齐：align-items（stretch/center/flex-start/flex-end/baseline）。子项属性：flex-grow、flex-shrink、flex-basis、order、align-self。

{{知识点14}}{{Grid 网格布局}}
display: grid 创建网格容器。定义行列：grid-template-columns / grid-template-rows（如 repeat(3, 1fr)）。间距：gap / row-gap / column-gap。子项定位：grid-column / grid-row（如 1 / 3 跨两列）。区域模板：grid-template-areas 命名布局区域。

{{知识点15}}{{CSS 定位 position}}
position 取值：static（默认，无定位）、relative（相对自身偏移，保留原空间）、absolute（相对最近定位祖先，脱离文档流）、fixed（相对视口，脱离文档流）、sticky（粘性定位，滚动到阈值变 fixed）。偏移属性：top/right/bottom/left。z-index 控制层叠顺序（需定位元素才生效）。

{{知识点16}}{{CSS 浮动与清除}}
float: left/right 使元素浮动，常用于文字环绕图片。浮动元素脱离文档流，父容器高度塌陷。清除浮动方法：1) 父元素 overflow: hidden；2) 伪元素 clearfix：.clearfix::after { content: ''; display: block; clear: both; }；3) 父元素也设置 float。现代布局推荐用 Flexbox/Grid 替代浮动布局。

{{知识点17}}{{CSS BFC（块级格式化上下文）}}
BFC 是一个独立的渲染区域，内部布局不影响外部。触发条件：float 非 none、overflow 非 visible、display 为 inline-block/flex/grid/table、position 为 absolute/fixed。BFC 特性：包含浮动子元素、阻止 margin 合并、不与浮动元素重叠。

{{知识点18}}{{CSS 过渡 transition}}
transition: property duration timing-function delay。常用属性：transition-property（all/具体属性）、transition-duration（如 0.3s）、transition-timing-function（ease/linear/ease-in/ease-out/cubic-bezier()）、transition-delay。多个过渡用逗号分隔。

{{知识点19}}{{CSS 动画 animation}}
@keyframes 定义关键帧。animation 属性简写：animation: name duration timing-function delay iteration-count direction fill-mode play-state。animation-name 引用 @keyframes 名称。animation-iteration-count: infinite（无限循环）。animation-direction: alternate（往返播放）。

{{知识点20}}{{CSS 变量（自定义属性）}}
CSS 变量以 -- 开头，用 var() 引用。定义：--main-color: #3498db; 引用：color: var(--main-color, fallback)。作用域遵循 CSS 级联，:root 中定义全局变量。JavaScript 操作：getComputedStyle(el).getPropertyValue('--var')、el.style.setProperty('--var', 'value')。

{{知识点21}}{{CSS 响应式设计}}
媒体查询：@media (max-width: 768px) { ... }。常用断点：手机 < 768px、平板 768-1024px、桌面 > 1024px。相对单位：em（相对父元素字体）、rem（相对根元素字体）、vw/vh（视口宽高百分比）、%（相对父元素）。clamp() 函数：clamp(min, preferred, max) 实现流体尺寸。

{{知识点22}}{{CSS 伪类与伪元素}}
伪类（:）：:hover、:focus、:active、:nth-child(n)、:first-child、:last-child、:not()、:checked、:disabled、:empty。伪元素（::）：::before、::after（需 content 属性）、::first-line、::first-letter、::selection、::placeholder。伪元素是虚拟元素，伪类是状态选择。

{{知识点23}}{{CSS Transform 变换}}
transform: translate(x, y)（位移）、rotate(deg)（旋转）、scale(x, y)（缩放）、skew(deg)（倾斜）。变换不影响文档流。transform-origin 设置变换原点。3D 变换：translateZ()、rotateX()、rotateY()。配合 perspective 属性创建 3D 空间。

{{知识点24}}{{CSS 阴影与滤镜}}
box-shadow: h-offset v-offset blur spread color inset。text-shadow: h-offset v-offset blur color。backdrop-filter 对元素背后内容应用滤镜（如 blur()、brightness()）。filter 属性：blur()、brightness()、contrast()、grayscale()、saturate()、drop-shadow()。

{{知识点25}}{{CSS 文字与字体}}
字体相关：font-family（字体栈）、font-size、font-weight（100-900/normal/bold）、font-style（normal/italic/oblique）、line-height（行高）。text-overflow: ellipsis 配合 overflow: hidden 和 white-space: nowrap 实现单行省略。@font-face 自定义字体。font-display 控制字体加载行为。

{{知识点26}}{{JavaScript 数据类型}}
基本类型（7 种）：string、number、boolean、null、undefined、symbol（唯一标识符）、bigint（大整数）。引用类型：Object（Array、Function、Date、RegExp 等都是对象）。typeof 操作符检测类型，注意 typeof null === 'object' 是历史遗留 bug。

{{知识点27}}{{JavaScript 变量声明}}
var：函数作用域，有变量提升，可重复声明。let：块级作用域，无提升（暂时性死区 TDZ），不可重复声明。const：块级作用域，声明时必须初始化，不可重新赋值（但对象属性可修改）。推荐优先使用 const，需要重新赋值时用 let，避免 var。

{{知识点28}}{{JavaScript 类型转换}}
隐式转换：+ 运算符会将数字转为字符串拼接，-*/ 会将字符串转为数字。== 会进行类型转换（如 '' == false 为 true），=== 严格相等不转换。显式转换：Number()、String()、Boolean()、parseInt()、parseFloat()。Falsy 值：false、0、''、null、undefined、NaN。

{{知识点29}}{{JavaScript 作用域与闭包}}
作用域类型：全局作用域、函数作用域、块级作用域（let/const）。作用域链：内部函数可访问外部函数的变量。闭包：函数能够访问其定义时所在的词法作用域，即使该函数在其他地方执行。闭包用途：数据私有化、函数工厂、柯里化。注意闭包可能导致内存泄漏。

{{知识点30}}{{JavaScript this 指向}}
this 的值取决于函数调用方式：1) 全局上下文：window（严格模式 undefined）；2) 对象方法：调用对象；3) 构造函数：新创建的实例；4) 箭头函数：继承外层 this（不可改变）；5) call/apply/bind 手动指定。ES6 class 中 this 指向实例。

{{知识点31}}{{ES6 箭头函数}}
语法：const fn = (a, b) => a + b（单表达式自动返回）。多条语句需 {} 和 return。单参数可省略括号。特点：没有自己的 this（继承外层）、不能用作构造函数、没有 arguments 对象、没有 prototype 属性。适合回调函数场景。

{{知识点32}}{{ES6 解构赋值}}
数组解构：const [a, b, ...rest] = [1, 2, 3, 4]。对象解构：const { name, age, alias: nick } = obj（alias 是匹配键，nick 是变量名）。默认值：const { x = 10 } = {}。嵌套解构：const { a: { b } } = { a: { b: 2 } }。函数参数解构：function({ name = 'unknown' }) {}。

{{知识点33}}{{ES6 模板字符串}}
使用反引号 `` 定义。支持多行字符串（保留换行和缩进）。嵌入表达式：${expression}，可调用函数、计算。标签模板字符串：tag`text ${expr}`，tag 函数接收模板片段和表达式值数组。常用场景：SQL 拼接防护、HTML 模板、国际化。

{{知识点34}}{{ES6 Promise}}
Promise 代表异步操作的最终结果。三种状态：pending → fulfilled / rejected。创建：new Promise((resolve, reject) => { ... })。链式调用：.then()、.catch()、.finally()。静态方法：Promise.all()（全部成功才成功）、Promise.allSettled()（等所有完成）、Promise.race()（取最先完成）、Promise.any()（取最先成功）。

{{知识点35}}{{ES6 async/await}}
async 函数返回 Promise。await 暂停执行直到 Promise 完成（只能在 async 函数内使用）。错误处理：try/catch 包裹 await。并行执行：await Promise.all([fetch1(), fetch2()])。顶层 await 在 ES2022 模块中支持。避免在循环中串行 await，应使用 Promise.all 并行。

{{知识点36}}{{ES6 Class 类}}
class 声明：class Person { constructor(name) { this.name = name; } greet() { return `Hi, ${this.name}`; } }。继承：class Student extends Person { constructor(name, grade) { super(name); this.grade = grade; } }。静态方法：static method()。私有属性（ES2022）：#privateField。getter/setter：get fullName() {}。

{{知识点37}}{{ES6 模块系统}}
导出：export default xxx（默认导出）、export { a, b }（命名导出）、export const x = 1（声明时导出）。导入：import xxx from './module'（默认）、import { a, b } from './module'（命名）、import * as mod from './module'（全部）。模块是严格模式，文件级作用域。动态导入：import('./module') 返回 Promise。

{{知识点38}}{{ES6 Map 与 Set}}
Map：键值对集合，键可以是任何类型。API：set(key, value)、get(key)、has(key)、delete(key)、clear()、size。遍历：forEach、for...of。Set：唯一值集合。API：add(value)、has(value)、delete(value)、size。WeakMap/WeakSet：键为弱引用，可被垃圾回收。

{{知识点39}}{{ES6 迭代器与生成器}}
可迭代协议（Iterable）：对象实现 Symbol.iterator 方法。生成器函数：function* gen() { yield 1; yield 2; }。生成器返回迭代器，调用 next() 执行到下一个 yield。用途：惰性求值、异步流程控制、自定义迭代行为。for...of 遍历可迭代对象。

{{知识点40}}{{JavaScript 数组常用方法}}
变异方法：push/pop（尾部增删）、unshift/shift（头部增删）、splice（任意位置增删改）、sort、reverse。非变异方法：concat、slice、map、filter、reduce、find、findIndex、some、every、includes、flat、flatMap。ES2023 新增：toSorted()、toReversed()、toSpliced()（返回新数组不修改原数组）。

{{知识点41}}{{JavaScript 对象操作}}
Object.keys()、Object.values()、Object.entries() 获取键/值/键值对数组。Object.assign(target, ...sources) 浅拷贝合并。展开运算符：{ ...obj } 浅拷贝。Object.freeze() 冻结对象（浅冻结）。Object.fromEntries() 将键值对数组转为对象。可选链：obj?.a?.b。空值合并：a ?? b（仅 null/undefined 取 b）。

{{知识点42}}{{DOM 获取元素}}
document.getElementById('id')：通过 ID 获取单个元素。document.querySelector('.class')：CSS 选择器获取第一个匹配元素。document.querySelectorAll('div.item')：获取所有匹配元素（NodeList）。document.getElementsByClassName()：通过类名获取 HTMLCollection（动态集合）。document.getElementsByTagName()：通过标签名获取。

{{知识点43}}{{DOM 创建与操作}}
创建元素：document.createElement('div')。添加子元素：parent.appendChild(child)、parent.insertBefore(new, ref)、parent.append()、parent.prepend()。删除：parent.removeChild(child)、el.remove()。替换：parent.replaceChild(new, old)。克隆：el.cloneNode(deep)。innerHTML 设置 HTML 内容（注意 XSS 风险），textContent 设置纯文本。

{{知识点44}}{{DOM 属性与样式}}
标准属性：el.id、el.className、el.src、el.href。自定义数据属性：el.dataset.xxx（对应 data-xxx 属性）。样式操作：el.style.color = 'red'（行内样式）、getComputedStyle(el).color（计算后样式）。classList：add()、remove()、toggle()、contains()。setAttribute()、getAttribute()、removeAttribute() 操作属性。

{{知识点45}}{{DOM 事件机制}}
事件流三阶段：捕获（Capture）→ 目标（Target）→ 冒泡（Bubble）。addEventListener(type, handler, options) 注册事件。options：{ capture: true }（捕获阶段）、{ once: true }（执行一次）、{ passive: true }（不调用 preventDefault，优化滚动性能）。removeEventListener() 移除事件（需传同一函数引用）。

{{知识点46}}{{DOM 事件对象}}
事件回调参数 event 对象：event.target（触发元素）、event.currentTarget（绑定元素）、event.type（事件类型）、event.preventDefault()（阻止默认行为）、event.stopPropagation()（阻止冒泡）、event.stopImmediatePropagation()（阻止同一元素上后续监听器）。

{{知识点47}}{{DOM 事件委托}}
利用事件冒泡机制，在父元素上监听子元素的事件。优势：减少事件监听器数量、自动处理动态添加的子元素。实现：parent.addEventListener('click', e => { if (e.target.matches('.item')) { ... } })。注意：不适合不冒泡的事件（如 focus/blur，可用 focusin/focusout 替代）。

{{知识点48}}{{BOM 浏览器对象模型}}
window 对象：window.innerWidth/innerHeight（视口尺寸）、window.scrollTo()、window.open()、window.close()。navigator：navigator.userAgent（浏览器信息）、navigator.geolocation（地理位置）。location：location.href（URL）、location.reload()、location.assign()。history：history.back()、history.forward()、history.pushState()。

{{知识点49}}{{JavaScript 错误处理}}
try...catch...finally 捕获错误。Error 类型：Error、TypeError、ReferenceError、SyntaxError、RangeError。自定义错误：class MyError extends Error {}。错误属性：message、name、stack。throw 主动抛出错误。全局错误监听：window.onerror、window.addEventListener('unhandledrejection')。

{{知识点50}}{{JavaScript 正则表达式}}
字面量：/pattern/flags。构造函数：new RegExp('pattern', 'flags')。flags：g（全局）、i（忽略大小写）、m（多行）、s（dotAll）、u（Unicode）。常用方法：test()（返回布尔值）、match()（返回匹配数组）、replace()（替换）、split()。常用元字符：\d（数字）、\w（单词字符）、\s（空白）、.（任意字符）、* + ?（量词）。

{{知识点51}}{{JavaScript 防抖与节流}}
防抖（debounce）：事件触发后等待 n 毫秒执行，若期间再次触发则重新计时。适合搜索输入、窗口 resize。节流（throttle）：n 毫秒内只执行一次。适合滚动事件、按钮防重复点击。实现核心：防抖用 clearTimeout/setTimeout，节流用时间戳或定时器标记。

{{知识点52}}{{JavaScript 深拷贝}}
浅拷贝：展开运算符 {...obj}、Object.assign()、Array.from()、slice()。深拷贝：structuredClone(obj)（现代浏览器原生支持）、JSON.parse(JSON.stringify(obj))（不支持函数/Symbol/循环引用/Date/RegExp）。手写深拷贝需递归处理对象、数组、Date、RegExp，维护 visited Map 处理循环引用。

{{知识点53}}{{JavaScript 事件循环 Event Loop}}
JavaScript 是单线程，通过事件循环处理异步。执行栈同步代码优先。微任务（Microtask）：Promise.then/catch/finally、MutationObserver、queueMicrotask。宏任务（Macrotask）：setTimeout/setInterval、I/O、UI 渲染。每轮循环：执行栈清空 → 执行所有微任务 → 取一个宏任务执行 → 执行所有微任务 → 渲染。

{{知识点54}}{{JavaScript Proxy 与 Reflect}}
Proxy 创建对象的代理，拦截基本操作。handler 方法：get(target, prop)、set(target, prop, value)、has(target, prop)、deleteProperty(target, prop)、apply(target, thisArg, args)。Reflect 提供与 Proxy trap 对应的默认行为方法。用途：数据响应式（Vue 3）、表单校验、日志追踪。

{{知识点55}}{{Web 性能优化基础}}
关键优化策略：1) 减少 HTTP 请求（合并文件、雪碧图）；2) 资源压缩（gzip/brotli）；3) 懒加载（图片 loading="lazy"、动态 import()）；4) CDN 加速；5) 浏览器缓存（Cache-Control、ETag）；6) 减少重排重绘（批量 DOM 操作、使用 DocumentFragment）；7) 关键 CSS 内联、非关键 CSS 异步加载。

{{知识点56}}{{CSS 现代布局技巧 - Container Queries}}
容器查询（Container Queries）允许子元素根据父容器尺寸而非视口尺寸响应。语法：@container (min-width: 400px) { .card { ... } }。需先声明容器：container-type: inline-size。比媒体查询更灵活，适合组件化开发中的自适应布局。

{{知识点57}}{{CSS 嵌套规则}}
CSS 原生嵌套（CSS Nesting，2023 年主流浏览器支持）：.parent { color: red; & .child { color: blue; } &:hover { color: green; } }。可嵌套选择器减少重复书写。& 代表父选择器。注意浏览器兼容性，生产环境建议使用 PostCSS 降级处理。

{{知识点58}}{{JavaScript Symbol 类型}}
Symbol 是 ES6 引入的原始类型，每个 Symbol 值唯一。用途：对象唯一属性键（避免命名冲突）、定义类的私有属性。内置 Symbol：Symbol.iterator（定义迭代行为）、Symbol.toPrimitive（类型转换）、Symbol.hasInstance（instanceof 行为）。Symbol.for('key') 创建全局共享 Symbol。

{{知识点59}}{{JavaScript WeakRef 与 FinalizationRegistry}}
WeakRef（ES2021）创建对象的弱引用，不阻止垃圾回收。使用：const ref = new WeakRef(obj); ref.deref() 获取对象（可能返回 undefined）。FinalizationRegistry 在对象被回收后执行清理回调。适用于大型缓存和资源管理场景，避免内存泄漏。

{{知识点60}}{{HTML5 Web Workers}}
Web Worker 在后台线程运行 JS，不阻塞主线程。创建：const worker = new Worker('worker.js')。通信：worker.postMessage(data)（主线程发消息）、onmessage 接收。worker 内部：self.postMessage()、self.onmessage。SharedWorker 可被多个页面共享。Worker 不能访问 DOM。

{{知识点61}}{{CSS Container Style Queries}}
样式容器查询（Style Queries）根据容器的 CSS 属性值而非尺寸来应用样式。语法示例：@container style(--theme: dark) { .card { background: #333; } }。这使得组件可以根据父级的主题变量自适应，进一步提升组件化开发的灵活性。

{{知识点62}}{{CSS :has() 选择器}}
:has() 被称为「父选择器」，允许选择包含特定子元素的父元素。示例：div:has(> img)（包含直接子级 img 的 div）、form:has(:invalid)（包含无效输入的表单）。可用于实现以前纯 CSS 无法做到的选择逻辑，是 CSS 选择器的重大突破。

{{知识点63}}{{CSS Subgrid}}
Subgrid 允许嵌套网格继承父网格的行列定义。用法：子元素设置 display: grid; grid-template-columns: subgrid;。解决了嵌套网格对齐难题，特别适合卡片组件内部内容与外部网格对齐的场景。

{{知识点64}}{{JavaScript Temporal API}}
Temporal 是新的日期/时间 API（Stage 3 提案），解决 Date 对象的各种问题。核心类型：Temporal.Now、Temporal.PlainDate、Temporal.PlainTime、Temporal.PlainDateTime、Temporal.ZonedDateTime、Temporal.Duration。不可变设计，时区支持完善。

{{知识点65}}{{JavaScript 数组 at() 方法}}
ES2022 新增 Array.prototype.at()，支持负索引访问。arr.at(-1) 等价于 arr[arr.length - 1]。同样适用于字符串和 TypedArray。比传统 arr[arr.length - 1] 更简洁直观。

{{知识点66}}{{CSS Scroll-Driven Animations}}
CSS 滚动驱动动画（2023+）通过 animation-timeline 将动画与滚动关联。animation-timeline: scroll()（滚动容器进度）或 view()（元素进入视口进度）。无需 JavaScript 监听 scroll 事件，性能更好。示例：@keyframes fade-in { from { opacity: 0; } } .el { animation: fade-in linear; animation-timeline: view(); }。

{{知识点67}}{{JavaScript Array.fromAsync}}
ES2024 新增 Array.fromAsync()，从异步可迭代对象创建数组。用法：const arr = await Array.fromAsync(asyncIterable)。解决了需要手动 for-await-of 收集异步数据的问题。支持 map 函数参数。

{{知识点68}}{{HTML5 Popover API}}
HTML 原生弹出层（Popover API，2024 主流支持）。用法：在元素上添加 popover 属性，配合 popovertarget 属性指定触发按钮。CSS ::backdrop 伪元素控制背景。免 JS 实现 tooltip、菜单、模态框等弹出交互。

{{知识点69}}{{CSS @scope 规则}}
@scope 允许限定样式的作用范围。语法：@scope (.card) to (.card-content) { h2 { color: red; } }。.card 的直接后代中，在 .card-content 之外的 h2 才会生效。解决了 CSS 作用域问题，比 BEM 命名更灵活，比 Shadow DOM 更轻量。

{{知识点70}}{{JavaScript Set 方法扩展}}
ES2025 为 Set 新增集合运算方法：set.intersection(otherSet)（交集）、set.union(otherSet)（并集）、set.difference(otherSet)（差集）、set.symmetricDifference(otherSet)（对称差集）、set.isSubsetOf(otherSet)、set.isSupersetOf(otherSet)、set.isDisjointFrom(otherSet)。
