# 模板语法

## 插值与表达式
- 文本插值：{{ message }}，双向绑定响应式数据
- v-once：一次性插值，不随数据变化更新
- v-html：输出原始HTML（XSS风险，避免用于用户输入）
- JavaScript表达式：{{ number + 1 }}、{{ ok ? 'Yes' : 'No' }}
- 模板中只能使用单个表达式，不能使用语句

## 指令
- v-bind:attr → :attr，动态绑定属性
- v-on:event → @event，事件监听
- v-model：双向数据绑定，表单输入
- v-if/v-else-if/v-else：条件渲染（销毁/重建组件）
- v-show：条件显示（CSS display切换，频繁切换用v-show）
- v-for：列表渲染，:key绑定唯一标识

## Class与Style绑定
- 对象语法：:class="{ active: isActive, 'text-danger': hasError }"
- 数组语法：:class="[activeClass, errorClass]"
- 内联样式：:style="{ color: activeColor, fontSize: fontSize + 'px' }"
- 自动前缀：Vue自动添加浏览器CSS前缀
- 组件上使用class：会合并到组件根元素

## 事件处理
- 内联处理器：@click="count++"
- 方法处理器：@click="handleClick"
- 事件修饰符：.stop（阻止冒泡）、.prevent（阻止默认）、.once
- 按键修饰符：.enter、.tab、.delete、.esc、.space、.up/.down
- 系统修饰符：.ctrl、.alt、.shift、.meta

## 表单绑定
- v-model本质是语法糖：:value + @input
- 文本输入：<input v-model="text">
- 复选框：单个绑定bool，多个绑定数组
- 单选框：v-model绑定选中值
- 选择框：v-model绑定选中选项
- 修饰符：.lazy（change事件）、.number（转数字）、.trim（去空格）
