# Vue.js实战
# 领域: 计算机IT
# 学科: 前端开发
# 来源: Vue.js实战（2025版）
# 描述: Vue.js 3框架核心概念与实战应用，涵盖组合式API、响应式系统、组件化开发及生态工具链

{{Vue.js核心概念}}{{Vue.js是渐进式JavaScript框架，核心特性：响应式数据绑定、组件化系统、虚拟DOM(Diff算法)、模板语法。Vue 3基于Proxy实现响应式(取代Vue 2的Object.defineProperty)，支持Map/Set等数据结构。Composition API是Vue 3推荐的代码组织方式，将相关逻辑聚合在一起。}}

{{响应式系统}}{{ref()创建基本类型的响应式引用，通过.value访问。reactive()创建对象的响应式代理，直接访问属性。computed()创建计算属性(缓存依赖)。watch()监听响应式数据变化，watchEffect()自动追踪依赖。shallowRef/shallowReactive创建浅层响应式。toRef/toRefs解构reactive对象保持响应性。readonly创建只读代理。}}

{{模板语法与指令}}{{插值：{{ }}文本绑定、v-html(原始HTML)、v-bind(属性绑定，:简写)、v-on(事件监听，@简写)。条件渲染：v-if/v-else-if/v-else(销毁重建)、v-show(CSS显示隐藏)。列表渲染：v-for="(item, index) in list" :key="item.id"。表单：v-model双向绑定(支持text/checkbox/radio/select)。事件修饰符：.stop/.prevent/.once/.self。}}

{{组件系统}}{{Vue组件由<template>、<script setup>、<style scoped>组成。<script setup>是Composition API的语法糖，顶层变量/函数自动暴露给模板。defineProps定义组件入参，defineEmits定义事件，defineExpose暴露组件方法。组件注册：全局(app.component)和局部(import后直接使用)。动态组件：<component :is="compName">。}}

{{Props与Emits}}{{Props是父传子的数据流。defineProps({ prop: { type, required, default, validator } })。支持所有JavaScript类型作为prop类型。Emits是子传父的通信方式。defineEmits(['update', 'delete'])。emit('update', payload)触发事件。单向数据流：子组件不应直接修改props。v-model在组件上是语法糖，对应modelValue prop和update:modelValue事件。}}

{{插槽(Slots)}}{{默认插槽：<slot></slot>，父组件在标签内放置内容。具名插槽：<slot name="header">，父组件用<template v-slot:header>或<template #header>提供内容。作用域插槽：<slot :data="item">向父组件传递数据，父组件用#default="{ data }"接收。插槽实现组件内容分发，增强组件灵活性和复用性。}}

{{生命周期钩子}}{{Composition API生命周期：onBeforeMount、onMounted(DOM已挂载)、onBeforeUpdate、onUpdated、onBeforeUnmount、onUnmounted(清理副作用)。onActivated/onDeactivated(keep-alive组件)。onErrorCaptured(错误捕获)。setup()相当于beforeCreate和created。onMounted中进行DOM操作、数据获取、第三方库初始化。}}

{{Vue Router}}{{Vue Router是Vue官方路由管理器。createRouter创建路由实例，createWebHistory/createWebHashHistory选择模式。动态路由：/user/:id，useRoute()获取参数。嵌套路由：children配置。导航守卫：beforeEach(全局)、beforeEnter(路由级)、beforeRouteEnter(组件级)。路由懒加载：component: () => import('./Page.vue')。导航：useRouter().push/replace。}}

{{Pinia状态管理}}{{Pinia是Vue官方推荐的状态管理库(替代Vuex)。defineStore定义store，支持Composition API和Options API两种风格。State定义数据，Getters定义计算属性，Actions定义方法(同步和异步)。Store之间可互相引用。插件：持久化(pinia-plugin-persistedstate)、日志。SSR支持。DevTools集成调试。相比Vuex：无mutation、TypeScript支持更好、模块化更自然。}}

{{组合式函数(Composables)}}{{Composables是以use开头的函数，封装可复用的有状态逻辑。类似React的自定义Hook。示例：useMouse()追踪鼠标位置、useFetch()数据获取、useLocalStorage()本地存储持久化。composables文件夹存放可组合函数。逻辑复用、代码组织清晰、类型推断友好。与mixin相比：避免命名冲突、来源清晰、返回值类型明确。}}

{{Transition与动画}}{{<Transition>组件实现进入/离开动画。CSS过渡类名：v-enter-from/active/to、v-leave-from/active/to。支持自定义类名和第三方动画库(Animate.css)。<TransitionGroup>用于列表过渡。JS钩子：onBeforeEnter/onEnter/onAfterEnter。mode="out-in/in-out"控制过渡顺序。Vue Use Motion提供声明式动画。}}

{{Teleport与Suspense}}{{<Teleport to="body">将组件DOM渲染到指定目标位置，适用于模态框、通知等需要跳出组件DOM层级的场景。<Suspense>处理异步组件加载状态，v-slot:default显示异步组件，v-slot:fallback显示加载中。异步组件：defineAsyncComponent()或<script setup>中的顶层await。Suspense仍为实验性功能。}}

{{表单处理与验证}}{{v-model在表单元素上实现双向绑定。自定义组件的v-model：modelValue prop + update:modelValue事件。多个v-model：v-model:title。表单验证库：Vuelidate(轻量)、vee-validate(功能丰富，基于Yup/Zod)。表单状态管理：独立ref管理或reactive对象。服务端验证错误回显到表单。异步验证(如用户名可用性检查)。}}

{{服务端渲染(SSR)与Nuxt}}{{Nuxt是Vue全栈框架，提供SSR/SSG/ISR渲染策略。文件系统路由(pages/)、自动导入组件(composables/)、服务端API(server/api/)。useState跨请求共享状态。useFetch/useAsyncData在setup中获取数据。SEO优化：useSeoMeta/useHead。部署：Node.js服务器、静态托管、边缘渲染。Nuxt Modules生态扩展功能。}}

{{TypeScript与Vue}}{{Vue 3原生支持TypeScript。defineProps<{...}>()泛型定义prop类型。defineEmits<{...}>()定义事件类型。ref<T>()/reactive<T>()泛型指定响应式类型。组件实例类型：InstanceType<typeof Component>。模板中TypeScript支持有限，复杂逻辑放在<script setup>中。tsconfig.json配置strict模式。类型工具：PropType、ExtractPropTypes。}}

{{性能优化}}{{v-once渲染一次不再更新。v-memo记忆模板子树(条件缓存)。shallowRef避免深层响应式转换。虚拟滚动：vue-virtual-scroller处理大列表。组件懒加载：defineAsyncComponent。keep-alive缓存组件状态。Tree-shaking减小打包体积。SSR提升首屏性能。避免不必要的computed/watch。Web Workers处理CPU密集任务。}}

{{测试策略}}{{Vitest是Vue推荐的单元测试框架(Vite原生集成)。Vue Test Utils：mount/shallowMount渲染组件，find/findAll查询元素，trigger触发事件。组件测试：props传递、事件触发、插槽渲染、异步行为。E2E测试：Cypress、Playwright。Mock：vi.mock()模拟模块。覆盖率：@vitest/coverage-v8。快照测试和视觉回归测试。}}

{{构建工具Vite}}{{Vite是Vue推荐的构建工具。基于ESM的开发服务器，按需编译，冷启动极快。HMR(热模块替换)即时更新。插件系统：@vitejs/plugin-vue处理SFC。配置：vite.config.ts。构建使用Rollup，支持代码分割、CSS提取、资源处理。环境变量：.env文件，VITE_前缀暴露给客户端。开发代理：server.proxy配置API代理。}}

{{组件库与UI框架}}{{Element Plus：饿了么出品，Vue 3组件库，企业级后台管理系统首选。Ant Design Vue：蚂蚁金服设计语言。Naive UI：图森未来出品，Vue 3原生，TypeScript友好。Vuetify：Material Design风格。PrimeVue：丰富的组件集。Headless UI：无样式基础组件(配合Tailwind)。Shadcn Vue：可复制的UI组件。自定义设计系统方案。}}

{{国际化与主题}}{{vue-i18n：Vue官方国际化插件，支持复数、日期/数字格式化、组件翻译。createI18n创建实例。$t('key')翻译文本。语言切换和动态加载语言包。主题方案：CSS变量(自定义属性)动态切换。暗色模式：prefers-color-scheme或class切换。主题配置：颜色、间距、字体等设计令牌(Design Tokens)。多品牌主题切换。}}
