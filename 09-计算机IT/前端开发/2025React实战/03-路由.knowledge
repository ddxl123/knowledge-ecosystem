# React Router

## 基础路由
- React Router v6：声明式路由配置
- BrowserRouter/HashRouter：路由器容器
- Routes/Route：路由定义，path→element映射
- Link/NavLink：声明式导航，不刷新页面
- Navigate：编程式导航/重定向

## 嵌套路由与布局
- 嵌套路由：Route内嵌Route，children定义子路由
- Outlet：渲染子路由的占位组件
- 布局路由：共享导航栏/侧边栏的布局组件
- 索引路由：index Route，父路由默认显示的子路由
- 路径相对/绝对：相对路径不以/开头，绝对路径以/开头

## 动态路由与参数
- 路径参数：Route path="/user/:id"，useParams()获取
- 查询参数：useSearchParams()获取和设置URL查询参数
- 通配符：path="*" 匹配任意路径（404页面）
- 可选参数：path="/user/:id?" 参数可选
- 路由匹配：最长前缀匹配原则

## 导航与跳转
- useNavigate：编程式导航，navigate('/path')、navigate(-1)
- useLocation：获取当前location对象，监听路由变化
- useMatch：匹配特定路径模式
- NavLink：自动添加激活状态样式
- 搜索参数：navigate({ search: '?key=value' })

## 路由保护与懒加载
- 路由守卫：PrivateRoute组件检查认证状态，未登录重定向
- 懒加载：React.lazy(() => import('./Page'))，Suspense包裹
- 错误边界：ErrorBoundary捕获路由加载错误
- 数据加载：loader函数在路由渲染前获取数据
- 数据提交：action函数处理表单提交
