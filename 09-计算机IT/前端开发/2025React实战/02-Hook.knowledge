# Hook

## useState
- const [state, setState] = useState(initialValue)
- setState触发重新渲染，函数式更新：setState(prev => prev + 1)
- 惰性初始化：useState(() => expensiveComputation())
- 对象/数组更新：setState({ ...prev, key: newValue })，不可变更新
- 批量更新：React 18自动批量处理，多个setState合并为一次渲染

## useEffect
- useEffect(() => { ... }, [deps])：副作用钩子
- 依赖数组：[]空数组仅执行一次，省略每次渲染执行
- 清理函数：return () => { cleanup }，组件卸载或依赖变化前执行
- 常见用途：数据获取、订阅、手动DOM操作、定时器
- useEffect不能直接用async函数，需在内部定义异步函数

## useContext
- createContext：创建上下文对象
- Context.Provider：提供值
- useContext(ThemeContext)：消费上下文值
- 适用场景：主题、语言、认证状态等全局共享数据
- 性能注意：Context值变化时所有消费者重新渲染

## useReducer
- const [state, dispatch] = useReducer(reducer, initialState)
- reducer函数：(state, action) => newState
- 适合复杂状态逻辑：多个相关状态、状态转换规则复杂
- dispatch稳定引用，不需要useCallback包装
- 可配合Context实现全局状态管理

## 自定义Hook
- 以use开头的函数，封装可复用的状态逻辑
- useFetch、useLocalStorage、useDebounce、useMediaQuery
- 可组合其他Hook，但必须遵守Hook规则
- 规则：只在顶层调用，不在条件/循环中调用
- 只在React函数组件或自定义Hook中调用
