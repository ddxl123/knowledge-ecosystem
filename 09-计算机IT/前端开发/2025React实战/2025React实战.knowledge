# React实战
# 领域: 计算机IT
# 学科: 前端开发
# 来源: React实战（2025版）
# 描述: React框架核心概念与实战应用，涵盖组件化、Hooks、状态管理、路由及现代React开发模式

{{React核心概念}}{{React是Facebook开源的JavaScript UI库，核心理念是声明式UI和组件化。JSX是JavaScript的语法扩展，允许在JS中编写类似HTML的结构。虚拟DOM(Virtual DOM)是真实DOM的轻量副本，通过Diff算法最小化DOM操作。React元素是不可变的对象描述，组件是返回React元素的函数或类。}}

{{组件与Props}}{{函数组件是现代React推荐写法，接收props参数返回JSX。Props是从父组件传递到子组件的只读数据。children prop用于组件嵌套。PropTypes或TypeScript进行类型检查。默认参数(props默认值)通过解构赋值设置。组件组合(Composition)优于继承(Inheritance)。Fragment(<></>)避免多余的DOM节点。}}

{{State与事件处理}}{{useState Hook为函数组件添加状态：const [state, setState] = useState(initialValue)。setState触发重新渲染。事件处理采用驼峰命名(onClick、onChange)，传递函数引用。受控组件(Controlled)：表单值由React state控制。非受控组件(Uncontrolled)：使用ref直接访问DOM。批量更新：React 18自动批量处理多个setState。}}

{{Effect与生命周期}}{{useEffect Hook处理副作用(数据获取、订阅、DOM操作)。useEffect(() => { ... }, [deps])：依赖数组控制执行时机。空数组[]仅在挂载时执行，不传则每次渲染执行。返回清理函数处理订阅取消等。useLayoutEffect在DOM更新后同步执行。严格模式下Effect会执行两次(开发环境)以检测副作用问题。}}

{{Hooks详解}}{{核心Hooks：useState(状态)、useEffect(副作用)、useContext(上下文)、useReducer(复杂状态逻辑)、useMemo(记忆化计算)、useCallback(记忆化函数)、useRef(可变引用/DOM引用)、useImperativeHandle(自定义暴露方法)。自定义Hook：以use开头的函数，复用状态逻辑。规则：只在顶层调用Hook，不在循环/条件中调用。}}

{{Context与状态管理}}{{Context API解决props逐层传递(Prop Drilling)问题。createContext创建上下文，Provider提供值，useContext消费值。Redux：全局状态管理，单一数据源Store、纯函数Reducer、不可变更新。Redux Toolkit(RTK)简化Redux开发：createSlice、configureStore、RTK Query(API缓存)。Zustand：轻量状态管理，API简洁。Jotai：原子化状态管理。}}

{{React Router}}{{React Router是React标准路由库。v6+使用<BrowserRouter>、<Routes>、<Route>。嵌套路由：<Route>内嵌<Route>，<Outlet>渲染子路由。动态路由参数：useParams()获取URL参数。编程式导航：useNavigate()。数据加载：loader在路由渲染前获取数据。错误处理：errorElement。代码分割：React.lazy + Suspense按路由懒加载。}}

{{性能优化}}{{React.memo包装组件避免不必要渲染。useMemo缓存计算结果。useCallback缓存函数引用。React DevTools Profiler分析渲染性能。代码分割：React.lazy()和Suspense按需加载。虚拟列表：react-window/react-window处理大列表。避免在渲染中创建新对象/函数引用。并发特性(Concurrent Features)：startTransition标记低优先级更新。}}

{{表单处理}}{{受控表单：每个输入绑定state，onChange更新。非受控表单：ref获取值，适合简单场景。表单库：React Hook Form(高性能，基于非受控)、Formik(功能丰富)。Yup/Zod进行表单验证。React Hook Form的useForm/register/handleSubmit/formState核心API。Server Actions(React 19)简化服务端表单处理。}}

{{服务端渲染(SSR)}}{{SSR在服务端生成HTML，提升首屏性能和SEO。Next.js是React SSR主流框架。渲染策略：SSR(每次请求渲染)、SSG(构建时静态生成)、ISR(增量静态再生)、Streaming SSR(流式渲染)。RSC(React Server Components)：组件在服务端执行，减少客户端JS。Suspense边界控制加载状态。Hydration使服务端HTML具有交互能力。}}

{{Next.js框架}}{{Next.js是全栈React框架。App Router(新)基于文件系统的路由，支持嵌套布局。Server Components默认在服务端执行。数据获取：fetch在Server Component中直接使用。API Routes创建后端接口。Middleware处理请求拦截。图片优化：<Image>组件自动优化。部署：Vercel、自托管Node.js、静态导出。}}

{{TypeScript与React}}{{TypeScript为React添加类型安全。组件Props定义：interface或type。useState泛型指定状态类型。事件类型：React.MouseEvent、React.ChangeEvent。Context类型：createContext<Type>(default)。ref类型：useRef<HTMLInputElement>(null)。组件类型：React.FC<Props>(不推荐)或直接函数声明。泛型组件支持多种数据类型。}}

{{测试策略}}{{单元测试：Jest测试框架 + React Testing Library(RTL)。RTL原则：测试用户行为而非实现细节。render渲染组件，screen查询元素，fireEvent/userEvent模拟交互。测试异步：waitFor、findBy。Mock：jest.mock()模拟模块。E2E测试：Cypress、Playwright。快照测试：toMatchSnapshot()。MSW(Mock Service Worker)模拟API。}}

{{样式方案}}{{CSS Modules：局部作用域CSS，.module.css文件。Styled Components/CSS-in-JS：JavaScript中编写CSS。Tailwind CSS：原子化CSS类名，实用优先。Sass/SCSS：CSS预处理器。CSS变量(自定义属性)动态主题。CSS-in-JS运行时性能问题，零运行时方案：vanilla-extract、Linaria。暗色模式：prefers-color-scheme媒体查询或class切换。}}

{{数据获取}}{{fetch API：原生HTTP请求。axios：功能丰富的HTTP库。React Query/TanStack Query：服务端状态管理，提供缓存、后台刷新、乐观更新、分页、无限滚动。SWR(Vercel)：轻量数据获取库。RTK Query：Redux集成的API缓存。Suspense数据获取：结合use(promise)或框架loader。错误边界(Error Boundary)处理获取失败。}}

{{国际化(i18n)}}{{react-intl(FormatJS)：最流行的React国际化库，支持ICU消息格式。i18next：通用国际化框架，配合react-i18next使用。核心功能：文本翻译、日期/数字/货币格式化、复数处理、RTL布局支持。语言切换：URL路径(/en/about)、子域名(en.example.com)、用户设置。动态加载语言包减少初始加载量。}}

{{动画实现}}{{CSS Transition/Animation：简单过渡效果。Framer Motion：声明式React动画库，支持手势、布局动画、AnimatePresence。React Spring：基于物理的动画。React Transition Group：管理组件进入/退出动画。GSAP：高性能动画库。View Transitions API：浏览器原生页面过渡动画。Lottie：After Effects动画导出为JSON在Web播放。}}

{{安全实践}}{{XSS防护：React默认转义JSX内容，dangerouslySetInnerHTML需谨慎使用。CSRF防护：SameSite Cookie、CSRF Token。CSP(内容安全策略)：限制脚本来源。避免使用eval和innerHTML。输入验证(客户端+服务端)。HTTPS传输。依赖安全：npm audit检查漏洞。敏感数据不放在客户端代码中。环境变量通过NEXT_PUBLIC_前缀暴露。}}

{{React 19新特性}}{{React 19重要更新：Actions(异步状态转换的统一模型)、useActionState简化表单和Action处理、useFormStatus获取表单提交状态、useOptimistic乐观更新、use()函数读取Promise和Context。ref作为普通prop传递(无需forwardRef)。Document Metadata支持在组件中设置<title>/<meta>。Server Components和Server Actions成为一等公民。}}

{{微前端架构}}{{微前端将大型前端应用拆分为独立子应用。方案：Module Federation(Webpack 5，共享模块)、qiankun(基于single-spa，沙箱隔离)、Micro Apps(京东)、Wujie(无界)。子应用独立开发部署，主应用负责路由和集成。共享依赖(React等)避免重复加载。沙箱隔离：JS沙箱(Proxy/快照)、CSS沙箱(Shadow DOM/命名空间)。}}
