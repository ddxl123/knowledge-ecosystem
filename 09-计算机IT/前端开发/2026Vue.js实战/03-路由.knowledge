# Vue Router

## 路由配置
- createRouter + createWebHistory/createWebHashHistory
- 路由表：path、component、name、children、meta
- 路由懒加载：component: () => import('./Page.vue')
- 路由别名：alias: ['/u', '/user-list']
- 重定向：redirect: { name: 'home' }

## 动态路由
- 路径参数：path: '/user/:id'，useRoute().params.id
- 查询参数：useRoute().query
- 路由props解耦：props: true，params作为props传入
- 捕获所有：path: '/:pathMatch(.*)*'
- 敏感/严格模式：strict、sensitive配置

## 导航守卫
- 全局前置：router.beforeEach(async (to, from) => { })
- 全局解析：router.beforeResolve()
- 全局后置：router.afterEach()
- 路由独享：beforeEnter守卫
- 组件内：onBeforeRouteLeave、onBeforeRouteUpdate

## 高级功能
- 路由元信息：to.meta.requiresAuth
- 路由懒加载+骨架屏：Suspense配合loading组件
- 过渡动画：Transition包裹router-view
- 滚动行为：scrollBehavior自定义
- 路由中间件组合：组合多个守卫逻辑

## 数据加载（Vue Router数据加载器）
- loader函数：在路由进入前加载数据
- 组合式API：useLoaderData()
- 缓存策略：数据缓存和重新验证
- SSR支持：服务端和客户端统一数据加载
- 错误处理：errorCaptured和ErrorBoundary
