# 工程化与构建

## Vite 6+
- 基于ESM的开发服务器，HMR秒级更新
- 生产构建：Rollup 4，tree-shaking优化
- 环境变量：import.meta.env.VITE_*
- 插件生态：Vue、SVG、PWA、自动导入等
- 开发服务器代理：解决跨域问题

## 自动导入
- unplugin-auto-import：自动导入ref、computed等API
- unplugin-vue-components：自动注册组件，无需手动import
- 按需导入：减少打包体积
- TypeScript支持：自动生成类型声明文件
- ESLint配合：避免未定义变量警告

## TypeScript深度集成
- 泛型组件：defineProps<{}>()、defineEmits<{}>()
- 模板类型检查：Volar插件提供模板内类型检查
- ref类型推断：ref<number>(0)
- 组件类型：InstanceType<typeof Component>
- Pinia类型：完善的state/getters/actions类型

## 测试策略
- Vitest：Vite原生测试，兼容Jest API
- Vue Test Utils：组件挂载和交互测试
- Cypress/Playwright：E2E端到端测试
- MSW(Mock Service Worker)：API mock
- 测试金字塔：单元→集成→E2E

## 性能优化
- 组件懒加载：defineAsyncComponent + Suspense
- 虚拟列表：vue-virtual-scroller处理大数据列表
- 图片优化：懒加载、WebP、响应式图片
- Tree-shaking：确保按需引入，避免全量导入
- 预加载/预获取：<link rel="preload/prefetch">
