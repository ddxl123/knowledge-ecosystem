# 2025Java核心编程
# 领域: 计算机IT
# 学科: Java编程
# 来源: 2025Java核心编程
# 描述: Java编程核心知识点

{{知识点1}}{{Java语言特点：Java是一门面向对象的编程语言，具有平台无关性（Write Once, Run Anywhere）、自动垃圾回收、强类型检查、多线程支持等特点。Java程序编译为字节码（.class文件），由JVM解释执行。}}

{{知识点2}}{{Java基本数据类型：Java有8种基本数据类型——byte(1字节)、short(2字节)、int(4字节)、long(8字节)、float(4字节)、double(8字节)、char(2字节)、boolean(1位)。每种基本类型都有对应的包装类（Integer、Long等），支持自动装箱和拆箱。}}

{{知识点3}}{{Java变量类型：Java中变量分为局部变量（方法内声明，栈内存）、成员变量（类中声明，堆内存）和静态变量（类级别，方法区）。局部变量必须初始化后才能使用，成员变量有默认值。}}

{{知识点4}}{{Java运算符：算术运算符（+、-、*、/、%）、关系运算符（==、!=、>、<、>=、<=）、逻辑运算符（&&、||、!）、位运算符（&、|、^、~、<<、>>、>>>）、赋值运算符、三元运算符（?:）。注意==比较引用，equals()比较内容。}}

{{知识点5}}{{Java流程控制：if-else、switch-case（支持String和枚举）、for、while、do-while、for-each循环。break跳出当前循环，continue跳过本次迭代，return返回方法值。Java 14+引入switch表达式和箭头语法。}}

{{知识点6}}{{Java数组：数组是固定长度的同类型元素集合。声明方式：int[] arr = new int[10]或int[] arr = {1,2,3}。数组长度用arr.length获取。多维数组本质是数组的数组。Arrays工具类提供sort、binarySearch、copyOf等方法。}}

{{知识点7}}{{Java字符串：String是不可变对象（final class），每次修改创建新对象。StringBuilder是可变的，适合频繁拼接。StringBuffer是线程安全的StringBuilder。字符串常量池（String Pool）在堆中，字面量共享引用。String.intern()将字符串放入常量池。}}

{{知识点8}}{{Java封装：封装是将数据（属性）和操作数据的方法绑定在一起，通过访问修饰符控制访问权限。private隐藏实现细节，提供getter/setter方法。封装提高安全性、降低耦合、便于维护。}}

{{知识点9}}{{Java继承：Java使用extends关键字实现单继承。子类继承父类的非private属性和方法。super关键字引用父类，this引用当前对象。所有类默认继承Object类。方法重写（Override）要求方法签名相同、返回类型协变、访问权限不能更严格。}}

{{知识点10}}{{Java多态：多态指同一操作作用于不同对象产生不同行为。实现方式：方法重载（编译时多态）和方法重写（运行时多态）。父类引用指向子类对象时，调用的是子类重写的方法（动态绑定）。instanceof判断对象类型。}}

{{知识点11}}{{Java抽象类：用abstract修饰的类，不能实例化。可以包含抽象方法（无方法体）和具体方法。子类必须实现所有抽象方法，否则也是抽象类。抽象类用于定义模板和公共行为，是"is-a"关系。}}

{{知识点12}}{{Java接口：用interface定义，Java 8后可以有default和static方法，Java 9支持private方法。类用implements实现接口，可实现多个接口。接口中的变量默认是public static final。接口是"has-a"能力的契约，支持多继承。}}

{{知识点13}}{{Java内部类：分为成员内部类（依赖外部类实例）、静态内部类（不依赖外部类）、局部内部类（方法内定义）和匿名内部类（无名实现）。Lambda表达式是函数式接口匿名内部类的简写。}}

{{知识点14}}{{Java访问修饰符：public（所有类可见）、protected（同包+子类）、default/包级私有（同包可见）、private（仅本类可见）。类只能用public或default修饰。}}

{{知识点15}}{{Java static关键字：static修饰的变量和方法属于类而非实例，通过类名直接访问。静态方法不能访问非静态成员。static代码块在类加载时执行一次。静态内部类不持有外部类引用。}}

{{知识点16}}{{Java final关键字：final修饰变量（常量，不可重新赋值）、方法（不可重写）、类（不可继承）。final引用类型变量引用不可变，但对象内容可变。局部变量可以不立即初始化。}}

{{知识点17}}{{Java异常体系：Throwable是顶层类，分为Error（不可恢复，如OOM）和Exception。Exception分为受检异常（必须处理，如IOException）和非受检异常（RuntimeException及其子类，如NullPointerException）。}}

{{知识点18}}{{Java异常处理：try-catch-finally结构。try块放可能抛异常的代码，catch捕获处理，finally无论是否异常都执行（用于资源释放）。throws声明方法可能抛出的异常，throw手动抛出异常。}}

{{知识点19}}{{Java自定义异常：继承Exception（受检异常）或RuntimeException（非受检异常）。通常提供无参构造和带消息的构造方法。业务异常建议继承RuntimeException。}}

{{知识点20}}{{Java try-with-resources：Java 7引入，自动关闭实现AutoCloseable接口的资源。语法：try(资源声明){} 。多个资源按声明逆序关闭。抑制异常通过getSuppressed()获取。}}

{{知识点21}}{{Java泛型：泛型提供编译时类型安全检查，避免强制类型转换。泛型类、泛型接口、泛型方法。类型擦除：编译后泛型信息被擦除为原始类型。通配符：?（无界）、? extends T（上界）、? super T（下界，PECS原则）。}}

{{知识点22}}{{Java集合框架-List：ArrayList基于动态数组，随机访问O(1)，插入删除O(n)。LinkedList基于双向链表，插入删除O(1)，随机访问O(n)。Vector是线程安全的ArrayList（已过时）。ListIterator支持双向遍历。}}

{{知识点23}}{{Java集合框架-Set：HashSet基于HashMap，无序不重复，O(1)查找。LinkedHashSet维护插入顺序。TreeSet基于红黑树，有序不重复，O(log n)操作。元素需正确实现hashCode()和equals()。}}

{{知识点24}}{{Java集合框架-Map：HashMap基于数组+链表+红黑树（Java 8+），负载因子0.75。链表长度≥8转红黑树，≤6转回链表。LinkedHashMap维护插入/访问顺序。TreeMap按键排序。Hashtable线程安全但过时，推荐ConcurrentHashMap。}}

{{知识点25}}{{Java集合框架-Queue/Deque：Queue接口，LinkedList和ArrayDeque实现。PriorityQueue优先队列（最小堆）。Deque双端队列，ArrayDeque性能优于Stack。offer/poll/peek是推荐方法（不抛异常）。}}

{{知识点26}}{{Java集合框架-Collections工具类：Collections.sort()排序、reverse()反转、shuffle()打乱、binarySearch()二分查找、synchronizedList()线程安全包装、unmodifiableList()不可变包装、emptyList()空集合。}}

{{知识点27}}{{Java迭代器模式：Iterator接口，hasNext()、next()、remove()方法。for-each循环是Iterator的语法糖。ConcurrentModificationException：遍历时修改集合（非Iterator.remove()）会抛此异常。ListIterator支持双向遍历和add操作。}}

{{知识点28}}{{Java Stream API：Java 8引入，支持函数式操作集合。中间操作（filter、map、flatMap、sorted、distinct、limit、skip）惰性求值。终端操作（forEach、collect、reduce、count、anyMatch、allMatch）触发实际计算。}}

{{知识点29}}{{Java Stream收集器：Collectors工具类提供toList()、toSet()、toMap()、groupingBy()、partitioningBy()、joining()、counting()、summarizingInt()等收集器。可组合实现复杂分组和聚合。}}

{{知识点30}}{{Java函数式接口：只有一个抽象方法的接口，用@FunctionalInterface注解。常见：Function<T,R>（转换）、Predicate<T>（判断）、Consumer<T>（消费）、Supplier<T>（供给）、BiFunction<T,U,R>等。}}

{{知识点31}}{{Java Lambda表达式：(参数) -> 表达式 或 (参数) -> {语句块}。是函数式接口的匿名实现。可引用外部final或等效final的局部变量。方法引用：ClassName::staticMethod、instance::method、ClassName::new。}}

{{知识点32}}{{Java Optional类：Java 8引入，优雅处理null。Optional.of(value)非空、Optional.ofNullable(value)可空、Optional.empty()空。orElse/orElseGet提供默认值，ifPresent消费值，map/flatMap链式转换。避免用get()直接取值。}}

{{知识点33}}{{Java多线程-Thread类：继承Thread类重写run()方法。start()启动线程（创建新线程调用run()），直接调用run()只是普通方法。setDaemon(true)设为守护线程。sleep()休眠、join()等待、yield()让出CPU。}}

{{知识点34}}{{Java多线程-Runnable/Callable：实现Runnable接口（无返回值）或Callable接口（有返回值，可抛异常）。Callable配合FutureTask获取结果。推荐实现接口而非继承Thread，因为Java单继承限制。}}

{{知识点35}}{{Java线程池-ExecutorService：通过Executors工厂创建（newFixedThreadPool、newCachedThreadPool、newSingleThreadExecutor、newScheduledThreadPool）。推荐使用ThreadPoolExecutor自定义参数：核心线程数、最大线程数、存活时间、工作队列、拒绝策略。}}

{{知识点36}}{{Java线程同步-synchronized：修饰方法（对象锁/类锁）或代码块（指定锁对象）。保证原子性、可见性和有序性。Java 6后优化：偏向锁→轻量级锁→重量级锁。wait()释放锁等待，notify()/notifyAll()唤醒。}}

{{知识点37}}{{Java锁机制-ReentrantLock：比synchronized更灵活，支持公平锁、可中断锁、超时锁、条件变量（Condition）。lock()/unlock()必须配对使用，建议在finally中unlock()。tryLock()非阻塞尝试获取锁。}}

{{知识点38}}{{Java并发工具类：CountDownLatch（倒计数等待）、CyclicBarrier（栅栏同步）、Semaphore（信号量控制并发数）、Exchanger（线程间交换数据）、Phaser（灵活分阶段同步）。}}

{{知识点39}}{{Java并发集合：ConcurrentHashMap（分段锁/CAS）、CopyOnWriteArrayList（写时复制）、ConcurrentLinkedQueue（无锁队列）、BlockingQueue（阻塞队列，ArrayBlockingQueue/LinkedBlockingQueue）。}}

{{知识点40}}{{Java原子类：java.util.concurrent.atomic包。AtomicInteger、AtomicLong、AtomicBoolean、AtomicReference。基于CAS（Compare And Swap）实现无锁线程安全。LongAdder在高并发下性能优于AtomicLong。}}

{{知识点41}}{{Java CompletableFuture：Java 8增强的异步编程工具。supplyAsync/runAsync异步执行。thenApply/thenAccept/thenRun链式处理。thenCombine/thenCompose组合多个异步任务。exceptionally/handle异常处理。allOf/anyOf多任务编排。}}

{{知识点42}}{{Java IO模型：BIO（同步阻塞，一连接一线程）、NIO（同步非阻塞，Channel+Buffer+Selector多路复用）、AIO（异步非阻塞，Future/CompletionHandler回调）。Netty是基于NIO的高性能网络框架。}}

{{知识点43}}{{Java IO流体系：字节流（InputStream/OutputStream）处理二进制数据，字符流（Reader/Writer）处理文本。节点流直接操作数据源（FileInputStream），处理流包装节点流（BufferedReader）。装饰器模式的典型应用。}}

{{知识点44}}{{Java NIO核心组件：Channel（通道，双向读写）、Buffer（缓冲区，flip/clear/compact切换读写模式）、Selector（选择器，一个线程监控多个Channel）。MappedByteBuffer内存映射文件。Files工具类简化文件操作。}}

{{知识点45}}{{Java序列化：实现Serializable接口，transient关键字排除字段。serialVersionUID控制版本兼容。Externalizable接口自定义序列化逻辑。JSON序列化（Jackson/Gson）更常用。Java 17+标记序列化为deprecated。}}

{{知识点46}}{{JVM内存模型：堆（对象实例，分新生代/老年代）、栈（局部变量、方法调用）、方法区/元空间（类信息、常量池）、程序计数器（当前线程执行位置）、本地方法栈。元空间（Java 8+）替代永久代，使用本地内存。}}

{{知识点47}}{{JVM垃圾回收：可达性分析判断对象存活（GC Roots引用链）。收集算法：标记-清除（碎片）、标记-整理（无碎片）、复制算法（新生代）。收集器：Serial、Parallel、CMS（已废弃）、G1（默认）、ZGC（低延迟）。}}

{{知识点48}}{{JVM类加载机制：加载→验证→准备→解析→初始化。双亲委派模型：Bootstrap→Extension→Application ClassLoader。打破双亲委派：SPI（ServiceLoader）、OSGI、Tomcat自定义ClassLoader。}}

{{知识点49}}{{JVM调优参数：-Xms/-Xmx（堆大小）、-Xmn（新生代）、-XX:MetaspaceSize（元空间）、-XX:+UseG1GC（使用G1）、-XX:MaxGCPauseMillis（最大GC停顿）。jps/jstat/jmap/jstack/jvisualvm是常用诊断工具。}}

{{知识点50}}{{Java反射机制：通过Class对象获取类信息。获取Class：Class.forName()、obj.getClass()、类名.class。可获取构造器、方法、字段并调用。性能较低但灵活，框架大量使用（Spring IoC）。setAccessible(true)绕过访问检查。}}

{{知识点51}}{{Java注解：@interface定义注解。元注解：@Target（作用范围）、@Retention（生命周期SOURCE/CLASS/RUNTIME）、@Documented、@Inherited。内置注解：@Override、@Deprecated、@SuppressWarnings、@FunctionalInterface。运行时注解可通过反射读取。}}

{{知识点52}}{{Java设计模式-单例模式：确保一个类只有一个实例。实现方式：饿汉式（静态常量）、懒汉式（synchronized/双重检查锁）、静态内部类、枚举。推荐枚举方式，天然防反射和序列化破坏。}}

{{知识点53}}{{Java设计模式-工厂模式：简单工厂（一个工厂类根据参数创建不同产品）、工厂方法（每个产品对应一个工厂）、抽象工厂（创建一族相关产品）。遵循开闭原则，降低耦合。}}

{{知识点54}}{{Java设计模式-观察者模式：定义一对多依赖，当一个对象状态改变时自动通知所有依赖者。Java内置：Observer/Observable（已废弃）。现代实现：事件监听器、Spring Event、Reactor。}}

{{知识点55}}{{Java设计模式-策略模式：定义算法族，封装每个算法，使它们可互换。配合Lambda和函数式接口更简洁。Comparator就是策略模式的典型应用。}}

{{知识点56}}{{Java新特性-Records（Java 16）：不可变数据载体类。record Point(int x, int y){} 自动生成构造器、getter、equals、hashCode、toString。自动实现序列化。可添加自定义方法和紧凑构造器。}}

{{知识点57}}{{Java新特性-Sealed Classes（Java 17）：密封类限制哪些类可以继承。sealed class Shape permits Circle, Rectangle, Triangle {}。子类必须是final、sealed或non-sealed。配合switch模式匹配实现穷举检查。}}

{{知识点58}}{{Java新特性-Text Blocks（Java 15）：多行字符串字面量。用三个双引号"""包裹，自动处理缩进和换行。适合SQL、JSON、HTML等多行文本。支持\\s（保留尾部空格）和\\（取消换行）转义。}}

{{知识点59}}{{Java新特性-Switch模式匹配（Java 21）：switch支持类型模式匹配和守卫条件。case String s && s.length() > 5 ->。替代冗长的if-else instanceof链。sealed class + switch实现编译器检查的穷举。}}

{{知识点60}}{{Java新特性-Virtual Threads（Java 21）：虚拟线程是轻量级线程，由JVM管理而非操作系统。创建：Thread.ofVirtual().start(runnable) 或 Executors.newVirtualThreadPerTaskExecutor()。适合I/O密集型任务，可创建百万级线程。}}
