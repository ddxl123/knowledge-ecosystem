# 2025编译原理入门
# 领域: 计算机IT
# 学科: 编译原理
# 来源: 2025编译原理入门
# 描述: 编译原理核心知识点

{{编译器基本架构}}{
编译器将源代码翻译为目标代码，典型分为前端、中端和后端三个阶段。
前端负责词法分析、语法分析和语义分析，将源代码转换为中间表示（IR）。
中端对 IR 进行与目标无关的优化。
后端将优化后的 IR 转换为目标机器代码并进行目标相关优化。
各阶段之间通过中间表示（如三地址码、LLVM IR）进行解耦。
}

{{编译的六个经典阶段}}{
1. 词法分析（Lexical Analysis）：将字符流转换为记号（token）流。
2. 语法分析（Syntax Analysis）：根据文法规则构建语法树。
3. 语义分析（Semantic Analysis）：进行类型检查、作用域分析等。
4. 中间代码生成（Intermediate Code Generation）：生成平台无关的 IR。
5. 代码优化（Code Optimization）：对 IR 进行各种优化变换。
6. 目标代码生成（Code Generation）：将 IR 转换为目标机器码。
}

{{词法分析器（Lexer / Scanner）}}{
词法分析器读入源代码字符流，将其分割为有意义的记号（token）。
每个 token 包含类型（如关键字、标识符、字面量、运算符）和属性值。
词法分析器会跳过空白字符和注释。
常见的实现方式包括手写状态机和使用工具（如 Lex/Flex）自动生成。
}

{{正则表达式与词法分析}}{
正则表达式用于描述 token 的模式，如标识符 [a-zA-Z_][a-zA-Z0-9_]*。
正则语言是正则表达式所描述的语言集合，是词法分析的理论基础。
正则表达式可转换为 NFA（非确定有限自动机），再转换为 DFA（确定有限自动机）。
DFA 的每个状态对应识别某种 token 的一个阶段。
}

{{NFA 与 DFA}}{
NFA（非确定有限自动机）：一个状态对同一输入可以有多个转移，允许 ε 转移。
DFA（确定有限自动机）：每个状态对每个输入符号恰好有一个转移。
子集构造法（Subset Construction）可将 NFA 转换为等价的 DFA。
DFA 可进行最小化，得到状态数最少的等价 DFA。
词法分析器通常基于 DFA 实现，因为 DFA 运行效率为 O(n)。
}

{{语法分析概述}}{
语法分析器接收 token 流，根据上下文无关文法（CFG）构建语法分析树或抽象语法树（AST）。
CFG 由终结符、非终结符、产生式和起始符号四元组定义。
语法分析分为自顶向下（如递归下降、LL 分析）和自底向上（如 LR 分析）两大类。
如果存在语法错误，分析器需要进行错误恢复以继续分析。
}

{{上下文无关文法（CFG）}}{
上下文无关文法 G = (V, Σ, P, S)，其中 V 是非终结符集合，Σ 是终结符集合，P 是产生式集合，S 是起始符号。
产生式形如 A → α，A ∈ V，α ∈ (V ∪ Σ)*。
CFG 比正则表达式更强，能描述嵌套结构（如括号匹配、if-else 嵌套）。
最左推导：每一步替换最左边的非终结符；最右推导：替换最右边的非终结符。
}

{{文法的二义性}}{
二义文法：存在某个句子可以构造两棵不同的语法分析树。
经典例子：E → E + E | E * E | id，在 "id + id * id" 有两种解析方式。
消除二义性的常用方法：引入优先级和结合性规则，重写文法。
例如将表达式文法分层：Expr → Expr + Term | Term，Term → Term * Factor | Factor。
}

{{FIRST 和 FOLLOW 集}}{
FIRST(α) 是可以从 α 推导出的串的首终结符集合。若 α 可推导出 ε，则 ε ∈ FIRST(α)。
FOLLOW(A) 是在某些句型中紧跟在非终结符 A 后面的终结符集合，#（输入结束符）属于 FOLLOW(起始符号)。
FIRST/FOLLOW 集是构造 LL(1) 和 LR 分析表的基础。
计算 FIRST 集：对产生式 A → α，将 FIRST(α)\{ε} 加入 FIRST(A)。
计算 FOLLOW 集：对 A → αBβ，将 FIRST(β)\{ε} 加入 FOLLOW(B)。
}

{{LL(1) 文法与预测分析}}{
LL(1) 文法：从左到右扫描输入，最左推导，向前看 1 个符号即可确定使用哪个产生式。
LL(1) 文法的条件：对同一非终结符的任意两个产生式 A → α | β，FIRST(α) ∩ FIRST(β) = ∅。
若 β ⇒* ε，则还需 FIRST(α) ∩ FOLLOW(A) = ∅。
预测分析器使用分析表驱动，无回溯，效率为 O(n)。
}

{{递归下降分析器}}{
递归下降分析器是一种自顶向下的语法分析方法，每个非终结符对应一个函数。
函数体根据当前 token 选择对应的产生式分支（即预测/switch）。
优点：实现简单、易于调试、错误信息直观。
缺点：左递归文法会导致无限循环，需要先消除左递归。
LL(1) 文法可以直接用递归下降实现。
}

{{消除左递归}}{
直接左递归：A → Aα | β，消除后变为 A → βA'，A' → αA' | ε。
间接左递归：通过代入和消除直接左递归的步骤解决。
消除左递归是将左递归文法转换为可被自顶向下分析器处理的必要步骤。
消除后文法等价但产生式结构改变，生成的分析树形状不同。
}

{{LR 分析概述}}{
LR 分析是最强大的自底向上语法分析方法，L 表示从左到右扫描，R 表示最右推导的逆。
LR(k) 分析器向前看 k 个符号，k=0 或 1 最常用。
LR 分析的变体：SLR(1)、LALR(1)、规范 LR(1)。
LR 分析器使用移进-归约策略，通过栈和状态驱动。
比 LL 分析器能处理更大的文法类，但构造更复杂。
}

{{LR 分析表与移进-归约}}{
LR 分析表由 ACTION 和 GOTO 两部分组成。
ACTION[s, a] 告诉分析器在状态 s 遇到终结符 a 时的动作：shift（移进）、reduce（归约）、accept（接受）、error（错误）。
GOTO[s, X] 告诉在状态 s 归约后非终结符 X 对应的下一状态。
分析过程：移进将 token 压栈，归约将栈顶若干符号替换为产生式左部非终结符。
}

{{SLR、LALR 与规范 LR 的区别}}{
SLR(1)：最简单，使用 FOLLOW 集合决定归约，文法覆盖最窄。
LALR(1)：对规范 LR(1) 的项目集进行合并，状态数与 SLR 相同但能力更强。
规范 LR(1)：每个项目带有精确的展望符，状态数最多，能力最强。
实践中 LALR(1) 最常用（如 yacc/bison 生成的分析器），兼顾能力与效率。
}

{{语义分析概述}}{
语义分析在语法分析之后进行，确保程序的语义正确性。
主要任务：类型检查、作用域解析、变量声明检查、函数调用参数匹配等。
语义分析通常使用属性文法或语法制导翻译来实现。
符号表（Symbol Table）是语义分析的核心数据结构。
}

{{符号表}}{
符号表存储程序中所有标识符的信息：名称、类型、作用域、存储位置等。
作用域管理：进入新作用域时创建新符号表层，退出时销毁。
嵌套作用域通过链表或栈结构实现，查找时从当前作用域向外逐层查找。
符号表在编译的多个阶段都会被使用。
}

{{类型系统与类型检查}}{
类型检查确保表达式和语句中的类型使用正确。
静态类型检查在编译时完成（如 C、Java），动态类型检查在运行时完成（如 Python）。
类型推导：编译器自动推断表达式的类型（如 ML、Haskell 的 Hindley-Milner 类型系统）。
类型转换包括隐式转换（如 int → float）和显式强制转换。
类型等价性分为结构等价和名等价两种。
}

{{属性文法与语法制导翻译}}{
属性文法为文法符号附加属性，为产生式附加语义规则。
综合属性：从子节点向父节点传递信息（自底向上计算）。
继承属性：从父节点或兄弟节点向子节点传递信息（自顶向下计算）。
语法制导定义（SDD）描述"做什么"，语法制导翻译（SDT）描述"怎么做"。
}

{{中间代码生成}}{
中间代码（IR）是源代码的一种抽象表示，与具体目标机器无关。
常见 IR 形式：三地址码（Three-Address Code）、四元式、SSA（静态单赋值形式）。
三地址码每条指令最多三个操作数：x = y op z。
SSA 形式中每个变量只被赋值一次，有利于优化分析。
LLVM IR 是工业界广泛使用的 SSA 形式中间表示。
}

{{三地址码}}{
三地址码指令的一般形式：x = y op z（二元运算）或 x = op y（一元运算）。
条件跳转：if x goto L 或 ifFalse x goto L。
标签 L 标记跳转目标位置。
数组访问：x = y[i]，x[i] = y。
过程调用：param x，call p, n，x = call p, n。
三地址码可以通过语法树翻译或语法制导翻译生成。
}

{{代码优化概述}}{
代码优化的目标：在保持语义不变的前提下，提高代码的执行效率或减小代码体积。
优化分为机器无关优化（在 IR 上进行）和机器相关优化（在目标代码上进行）。
优化还可以分为局部优化（基本块内）、循环优化和全局优化（过程内）。
常见优化：常量折叠、死代码消除、公共子表达式消除、循环不变量外提等。
}

{{基本块与控制流图（CFG）}}{
基本块（Basic Block）是一段顺序执行的指令序列，只有一个入口和一个出口。
控制流图（CFG，注意区分上下文无关文法的缩写）由基本块作为节点、控制转移作为有向边构成。
通过分析跳转指令和标签来划分基本块。
CFG 是许多代码优化算法的基础数据结构。
}

{{数据流分析}}{
数据流分析是一组在 CFG 上分析信息传播的技术。
到达定义分析（Reaching Definitions）：哪些变量的定义可能到达程序的某一点。
活跃变量分析（Live Variables）：在某一点哪些变量的值后续可能被使用。
可用表达式分析（Available Expressions）：哪些表达式在某一点已被计算过且未被修改。
这些分析通常使用迭代算法或基于格理论的框架求解。
}

{{常量传播与常量折叠}}{
常量传播：如果变量 x 被赋值为常量 c，且在后续使用前未被修改，则将 x 的使用替换为 c。
常量折叠：在编译时计算常量表达式的值，如将 3 + 4 直接替换为 7。
这两个优化经常配合使用，效果显著。
实现时需要追踪变量是否为常量，考虑分支路径的合并。
}

{{死代码消除}}{
死代码消除移除不会影响程序输出的代码。
变量级别的死代码消除：如果变量被赋值后从未被使用，该赋值可以删除。
不可达代码消除：移除从程序入口不可达的基本块。
死代码消除通常在活跃变量分析的基础上进行。
}

{{循环优化}}{
循环不变量外提（Loop-Invariant Code Motion）：将循环内计算结果不变的表达式提到循环外。
循环展开（Loop Unrolling）：减少循环控制开销，增加指令级并行。
归纳变量强度消减（Induction Variable Strength Reduction）：用简单运算替换乘法运算。
循环交换与循环分块：改善数据局部性和缓存命中率。
}

{{公共子表达式消除}}{
如果表达式 E 在之前已被计算过，且其操作数的值未改变，则 E 是公共子表达式，可复用之前的结果。
局部公共子表达式消除在基本块内进行，使用哈希表追踪已计算的表达式。
全局公共子表达式消除基于可用表达式数据流分析。
消除后引入临时变量保存中间结果。
}

{{目标代码生成}}{
目标代码生成将 IR 转换为目标机器的汇编代码或机器码。
主要任务：指令选择、寄存器分配、指令调度。
指令选择：将 IR 操作映射为目标机器指令，可使用树模式匹配或动态规划。
寄存器分配：决定哪些变量值保存在寄存器中，使用图着色算法。
指令调度：重排指令顺序以利用流水线，避免数据冒险。
}

{{寄存器分配}}{
寄存器分配决定变量值何时保存在寄存器中、何时溢出到内存。
图着色寄存器分配：构建干涉图（变量同时活跃则连边），用图着色算法分配寄存器。
溢出（Spill）：当颜色数不够时，将某些变量保存到栈上。
活跃区间（Live Range）分析是寄存器分配的基础。
SSA 形式简化了寄存器分配，因为每个变量只有一个定义点。
}

{{指令调度}}{
指令调度重排指令顺序以最大化 CPU 流水线利用率。
关键问题：避免数据冒险（RAW、WAR、WAW）和控制冒险。
列表调度（List Scheduling）：贪心算法，按优先级选择就绪指令。
循环中的软件流水线（Software Pipelining）：重叠不同迭代的指令执行。
乱序执行是硬件层面的指令调度手段。
}

{{垃圾回收机制}}{
自动内存管理通过垃圾回收器（GC）回收不再使用的对象。
引用计数：维护每个对象的引用计数，归零时回收；缺点是无法处理循环引用。
标记-清除（Mark-Sweep）：从根集遍历标记可达对象，清除未标记对象。
复制算法（Copying）：将存活对象复制到另一半空间，消除碎片。
分代收集（Generational GC）：按对象年龄分代，年轻代频繁收集，老年代低频收集。
现代语言（Java、Go、Rust）各有不同的 GC 策略。
}

{{编译器构建工具}}{
Lex/Flex：词法分析器生成器，根据正则表达式规则自动生成词法分析器。
Yacc/Bison：语法分析器生成器，根据 CFG 规则自动生成 LALR(1) 或 GLR 分析器。
ANTLR：支持 LL(*) 的解析器生成器，能处理更大的文法类。
LLVM：模块化编译器基础设施，提供 IR、优化框架和后端代码生成。
GCC：GNU 编译器集合，支持多种语言和目标平台。
}
