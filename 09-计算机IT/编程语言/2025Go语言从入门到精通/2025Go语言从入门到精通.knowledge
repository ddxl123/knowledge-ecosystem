# Go语言从入门到精通
# 领域: 计算机IT
# 学科: 编程语言
# 来源: Go语言从入门到精通（2025版）
# 描述: Go语言核心知识与实战开发，涵盖基础语法、并发编程、标准库、Web开发及工程实践

{{Go语言概述}}{{Go是Google开发的编译型、静态类型编程语言。核心设计哲学：简洁、并发、高效。特点：垃圾回收、快速编译、原生并发(goroutine)、静态链接。go命令工具链：go build/run/test/mod/get/fmt。Go Module依赖管理。没有类和继承，通过接口实现多态。错误处理通过返回值而非异常。代码风格强制统一(gofmt)。}}

{{基本类型与变量}}{{基本类型：bool、string、int/int8/16/32/64、uint/uint8(byte)/16/32/64、float32/float64、complex64/complex128。变量声明：var name type 或 name := value(短变量声明)。常量：const。零值机制(int=0, string="", bool=false, pointer=nil)。类型转换：T(v)显式转换。iota枚举。数组([N]T)是值类型，切片([]T)是引用类型。map[K]V字典。}}

{{控制流}}{{if-else条件判断(条件无需括号)。switch-case(默认break，fallthrough穿透)。for循环(唯一循环关键字)：for init; condition; post {}、for condition {}、for range {}。range遍历：数组/切片(索引+值)、map(键+值)、字符串(索引+Unicode码点)、channel。defer延迟执行(栈式LIFO)。goto(不推荐)。标签(label)配合break/continue。}}

{{函数与方法}}{{func name(params) (returns) {}。多返回值：func divide(a, b int) (int, error)。命名返回值。可变参数：func sum(nums ...int) int。函数是一等公民，可赋值给变量、作为参数传递、作为返回值。方法：func (r Rectangle) Area() float64，值接收者和指针接收者区别。init()初始化函数(自动执行)。闭包捕获外部变量。}}

{{结构体与接口}}{{struct定义结构体(无类概念)。字段标签(Tag)用于JSON/ORM映射。组合(Composition)替代继承：嵌入结构体。接口(Interface)隐式实现：只要实现了接口的所有方法就实现了该接口。空接口interface{}可持有任意值(类似Object)。类型断言：v, ok := i.(T)。类型switch：switch v := i.(type)。io.Reader/Writer是最核心的接口。}}

{{错误处理}}{{Go没有异常机制，通过error接口返回错误。errors.New创建错误。fmt.Errorf格式化错误(使用%w包装)。errors.Is/As/Unwrap解包和比较错误链。自定义错误类型实现error接口。错误处理模式：if err != nil { return err }。panic/recover用于不可恢复的错误(如数组越界)。defer+recover实现类似try-catch的效果。哨兵错误(sentinel errors)。}}

{{goroutine与channel}}{{goroutine是Go的轻量级协程，go关键字启动(仅几KB栈)。channel是goroutine间的通信管道。无缓冲channel(同步)和有缓冲channel(异步)。ch := make(chan int, 10)创建缓冲channel。select多路复用(类似switch但针对channel)。channel方向：chan<-只写、<-chan只读。关闭channel：close(ch)。range遍历channel直到关闭。}}

{{sync包并发原语}}{{sync.Mutex互斥锁(保护共享资源)。sync.RWMutex读写锁(读多写少场景)。sync.WaitGroup等待一组goroutine完成(Add/Done/Wait)。sync.Once确保函数只执行一次。sync.Map并发安全的map。sync.Pool对象池减少GC。sync.Cond条件变量。atomic包原子操作：AddInt64/LoadInt64/StoreInt64/CompareAndSwap。避免数据竞争(race detector)。}}

{{标准库常用包}}{{fmt(格式化I/O)、os(操作系统接口)、io/io/ioutil(IO操作)、net/http(HTTP客户端和服务端)、encoding/json(JSON编解码)、strings(字符串操作)、strconv(字符串转换)、time(时间日期)、math(数学函数)、sort(排序)、context(上下文控制)、log(日志)、testing(测试)、flag/pflag(命令行参数)、path/filepath(文件路径操作)。}}

{{JSON处理}}{{encoding/json包。序列化：json.Marshal(v)结构体→JSON字节。反序列化：json.Unmarshal(data, &v)JSON→结构体。结构体字段标签：`json:"name,omitempty"`控制JSON键名和零值忽略。json.Encoder/Decoder流式编解码。自定义MarshalJSON/UnmarshalJSON处理特殊类型。json.RawMessage延迟解析。map[string]interface{}处理未知结构JSON。}}

{{Web开发(Gin框架)}}{{Gin是Go最流行的Web框架。路由：r.GET/POST/PUT/DELETE定义路由。路由分组：r.Group("/api")。中间件：r.Use(authMiddleware)。参数获取：c.Param(路径参数)、c.Query(查询参数)、c.Bind(请求体绑定)。响应：c.JSON(状态码, 数据)。模板渲染：c.HTML()。静态文件服务。Gin中间件生态：Logger、Recovery、CORS、JWT。}}

{{数据库操作}}{{database/sql标准接口。sql.Open创建连接池。db.Query/QueryRow执行查询。db.Exec执行增删改。sql.Rows迭代结果集。prepare预编译语句防SQL注入。事务：db.Begin()→tx.Exec()→tx.Commit()/Rollback()。GORM：Go ORM框架，AutoMigrate自动迁移、CRUD操作、关联查询、钩子函数。sqlx扩展库简化数据库操作。连接池配置。}}

{{Go并发模式}}{{Fan-out/Fan-in：多个goroutine并行处理，汇总结果。Pipeline：多阶段处理管道。Worker Pool：固定数量的goroutine处理任务队列。Context控制：context.WithTimeout/WithCancel/WithValue控制goroutine生命周期。errgroup.Group并发执行并收集错误。Rate Limiting限流：time.Ticker。Semaphore信号量：带缓冲channel。Graceful Shutdown优雅停机。}}

{{反射(Reflection)}}{{reflect包在运行时检查类型和值。reflect.TypeOf()获取类型信息。reflect.ValueOf()获取值信息。reflect.Value的Kind()、Interface()、Set()方法。通过反射修改值需要传指针(ValueOf(&v).Elem().Set())。结构体字段遍历和标签解析。应用场景：JSON序列化、ORM映射、依赖注入。反射性能较差，应谨慎使用。}}

{{Go Module依赖管理}}{{go.mod文件定义模块路径和依赖版本。go.mod中require声明依赖。go.sum校验文件。go get添加/更新依赖。go mod tidy清理无用依赖。go mod vendor将依赖复制到vendor目录。语义版本(SemVer)。replace替换模块(本地开发)。私有模块：GOPRIVATE环境变量。Module Proxy：GOPROXY配置。Workspaces(Go 1.18+)多模块开发。}}

{{泛型(Generics)}}{{Go 1.18引入泛型。类型参数：func Map[T any, R any](s []T, f func(T) R) []R。类型约束(constraint)：自定义接口约束类型参数。comparable约束：可比较类型。any约束：任意类型(原interface{})。标准库约束：constraints包。泛型函数、泛型类型、泛型接口。类型推断简化调用。泛型使用场景：容器、算法、工具函数。}}

{{测试与基准测试}}{{go test运行测试。测试文件：_test.go后缀。测试函数：func TestXxx(t *testing.T)。t.Error/Fatal/Log报告。t.Run子测试。表格驱动测试(Table-Driven Tests)。TestMain控制测试生命周期。基准测试：func BenchmarkXxx(b *testing.B)，b.N循环。模糊测试(Fuzzing)：func FuzzXxx(f *testing.F)。覆盖率：go test -cover。httptest测试HTTP服务。}}

{{Go工具链}}{{go fmt格式化代码。go vet静态检查。go vet + staticcheck更全面的静态分析。go doc查看文档。go generate代码生成。go tool pprof性能分析(CPU/内存)。go tool trace程序追踪。go build -race竞态检测。交叉编译：GOOS/GOARCH。Go Playground在线运行。Delve(dlv)调试器。golangci-lint聚合多个linter。}}

{{微服务开发}}{{Go微服务框架：Go-kit(工具集)、Go-micro(框架)、Kratos(B站)、go-zero(好未来)。gRPC：Protocol Buffers定义服务、gRPC四种通信模式(Unary/Server-stream/Client-stream/Bidirectional)。服务注册与发现：etcd/Consul/Nacos。配置中心：Viper/Nacos。链路追踪：OpenTelemetry/Jaeger。限流熔断：gobreaker/ratelimit。服务网格集成。}}

{{部署与容器化}}{{Go编译为单一静态二进制文件。CGO_ENABLED=0禁用CGO确保纯Go编译。多阶段Docker构建：golang镜像编译→scratch/alpine运行。UPX压缩二进制体积。Kubernetes部署：Deployment/Service/ConfigMap/Secret。健康检查端点。优雅停机：signal.Notify捕获SIGTERM。配置管理：环境变量/Viper。CI/CD自动化。边缘部署。}}
