# 基础语法

## 变量与类型
- 声明方式：var x int（显式类型）、x := 10（短声明，函数内）
- 基本类型：bool、int/int8/16/32/64、uint、float32/64、complex64/128
- string：不可变字节序列，len()返回字节数，utf8.RuneCountInString()返回字符数
- 零值：数值为0，bool为false，string为空串，指针/slice/map/channel为nil
- 类型转换：Type(value)，无隐式转换，不同类型不能直接运算

## 流程控制
- if-else：条件不需要括号，可以有初始化语句 if err := f(); err != nil {}
- switch：默认不fall through，可省略条件（switch {}替代if-else链）
- for：唯一的循环关键字，支持三种形式：for init;cond;post {}、for cond {}、for range
- for range：遍历数组/slice/map/channel，返回索引和值
- goto/label：存在但不推荐使用

## 函数
- 多返回值：func f() (int, error) {}，Go惯用模式
- 命名返回值：func f() (result int, err error) {}，裸return
- 可变参数：func f(args ...int) {}，本质是slice
- 函数是一等公民：可以赋值、作为参数、作为返回值
- defer：函数返回前执行，多个defer按LIFO顺序执行
- init函数：包级别初始化，每个包可有多个init，自动调用

## 数组与切片
- 数组：[5]int，值类型，长度固定，赋值和传参会复制
- 切片(slice)：[]int，引用类型，底层数组+长度+容量
- 切片操作：append追加（可能触发扩容）、copy复制、s[low:high]截取
- 切片扩容策略：容量<1024翻倍，>=1024增长1.25倍
- nil切片 vs 空切片：var s []int（nil，len=0）vs s := []int{}（非nil）

## Map
- 声明：m := map[string]int{"key": 1} 或 make(map[string]int)
- 操作：m[key] = value、delete(m, key)、v, ok := m[key]（ok判断是否存在）
- 无序：遍历顺序随机，不保证插入顺序
- 非线程安全：并发读写需加锁(sync.Mutex)或使用sync.Map
- key类型：必须支持==运算，不能是slice、map、function
