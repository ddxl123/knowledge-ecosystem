# 微服务开发

## 微服务架构
- 单体 vs 微服务：独立部署、技术异构、弹性扩展、故障隔离
- 服务拆分：按业务领域（DDD限界上下文）、按变更频率
- 通信方式：同步（HTTP/gRPC）、异步（消息队列）
- API网关：统一入口、路由、认证、限流、熔断
- 服务网格(Service Mesh)：Istio/Linkerd，透明代理管理服务通信

## 服务发现与注册
- Consul：服务注册与发现、健康检查、KV存储
- etcd：分布式KV存储，Kubernetes使用的服务发现
- 注册流程：服务启动→注册地址→定期心跳→消费者发现
- 客户端发现 vs 服务端发现：客户端直接查询 vs 负载均衡器查询
- DNS服务发现：通过DNS SRV记录解析服务地址

## 熔断与限流
- 熔断器模式：关闭→打开→半开，防止级联故障
- Hystrix/s：Go熔断器库，支持并发、超时、降级
- 限流算法：令牌桶、漏桶、滑动窗口
- 限流实现：golang.org/x/time/rate（令牌桶）
- 降级策略：返回缓存数据、默认值、错误提示

## 消息队列
- Kafka：高吞吐分布式消息系统，分区+副本保证可靠性
- RabbitMQ：AMQP协议，支持多种消息模式
- NATS：轻量级消息系统，适合微服务通信
- 消息模式：点对点、发布-订阅、请求-回复
- 消息保证：At-most-once、At-least-once、Exactly-once

## 部署与运维
- Docker：容器化部署，Dockerfile构建镜像
- Kubernetes：容器编排，Deployment/Service/Ingress
- 健康检查：/healthz端点，liveness和readiness探针
- 分布式追踪：OpenTelemetry、Jaeger，链路追踪
- 配置中心：Nacos、Apollo，动态配置管理
