# 并发编程

## Goroutine
- 启动：go func() {}()，轻量级协程，初始栈仅几KB
- GMP模型：G(Goroutine)、M(OS线程)、P(逻辑处理器)
- 调度器：P的数量由GOMAXPROCS决定（默认CPU核数）
- goroutine泄漏：忘记关闭channel或goroutine阻塞无法退出
- context包：控制goroutine生命周期，WithCancel/WithTimeout/WithDeadline

## Channel
- 创建：ch := make(chan int)（无缓冲）、ch := make(chan int, 10)（有缓冲）
- 发送：ch <- value；接收：v := <-ch；关闭：close(ch)
- 无缓冲channel：发送和接收同步阻塞，强制通信双方就绪
- 有缓冲channel：缓冲区满时发送阻塞，空时接收阻塞
- 单向channel：chan<- T（只发送）、<-chan T（只接收），用于函数参数限制方向

## Select多路复用
- select { case v := <-ch1: ... case ch2 <- x: ... default: ... }
- 随机选择：多个case就绪时随机选择一个执行
- default分支：非阻塞操作，避免永久阻塞
- 超时控制：case <-time.After(3 * time.Second):
- 永久阻塞：select {} 等待所有channel关闭

## sync包
- Mutex/RWMutex：互斥锁/读写锁，Lock/Unlock
- WaitGroup：等待一组goroutine完成，Add/Done/Wait
- Once：确保函数只执行一次，Do(func)
- Pool：对象池，减少GC压力，Get/Put
- Map：并发安全的map，Load/Store/Delete/Range

## 并发模式
- 生产者-消费者：通过channel传递数据
- 扇入(Fan-in)：多个channel合并到一个channel
- 扇出(Fan-out)：一个channel分发到多个goroutine处理
- 管道(Pipeline)：多个阶段通过channel串联
- 信号量模式：用带缓冲channel限制并发数量
