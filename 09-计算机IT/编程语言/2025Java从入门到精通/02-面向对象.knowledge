# 面向对象

## 类与对象
- 类是模板，对象是实例；使用new关键字创建对象
- 构造方法：与类同名、无返回类型，支持重载，未定义时有默认无参构造
- this关键字：引用当前对象，用于区分同名变量、调用其他构造器
- static成员：属于类而非对象，通过类名直接访问，只加载一次
- 匿名对象：new ClassName()，只使用一次，常作为方法参数

## 封装、继承、多态
- 封装：private字段 + public getter/setter，隐藏实现细节
- 继承：extends关键字，Java单继承，所有类继承Object类
- 方法重写(Override)：子类覆盖父类方法，签名相同，访问权限不能更严格
- 多态：父类引用指向子类对象，运行时决定调用哪个方法（动态绑定）
- instanceof运算符：判断对象是否为某类的实例

## 抽象类与接口
- 抽象类：abstract修饰，不能实例化，可包含抽象方法和具体方法
- 接口：interface定义，JDK 8+支持default和static方法
- JDK 9+接口支持private方法；一个类可实现多个接口
- 接口 vs 抽象类：接口定义能力(has-a)，抽象类定义本质(is-a)
- 函数式接口：只有一个抽象方法的接口，可用Lambda表达式实现

## 内部类与枚举
- 成员内部类：可访问外部类所有成员，持有外部类引用
- 静态内部类：不依赖外部类实例，只能访问外部类静态成员
- 局部内部类：定义在方法中，作用域仅限该方法
- 匿名内部类：new Interface/AbstractClass() {}，常用于事件处理
- 枚举：enum定义，可有字段、方法、构造器，天然单例

## 常用设计模式
- 单例模式：饿汉式（静态变量）、懒汉式（双重检查锁）、枚举实现
- 工厂模式：根据参数创建不同对象，解耦创建与使用
- 观察者模式：一对多依赖，状态变化通知所有观察者
- 策略模式：定义算法族，运行时可互换
- 建造者模式：分步构建复杂对象，链式调用
