# 网络编程

## 网络基础
- OSI七层模型 vs TCP/IP四层模型：应用层、传输层、网络层、网络接口层
- IP地址：IPv4（32位）和IPv6（128位），InetAddress类表示
- 端口号：0-65535，0-1023为知名端口，常用：HTTP=80,HTTPS=443,MySQL=3306
- URL/URI：统一资源定位符，URL类可解析协议、主机、端口、路径

## TCP编程
- Socket（客户端）：new Socket(host, port)，getInputStream/getOutputStream
- ServerSocket（服务端）：new ServerSocket(port)，accept()阻塞等待连接
- TCP三次握手：SYN→SYN+ACK→ACK，保证可靠连接
- TCP四次挥手：FIN→ACK→FIN→ACK，确保双方数据发送完毕
- 粘包/拆包：TCP是字节流协议，需要应用层自行界定消息边界

## UDP编程
- DatagramSocket：发送和接收UDP数据包
- DatagramPacket：数据包封装，包含数据、地址、端口
- UDP特点：无连接、不可靠、速度快，适合视频流、DNS查询
- 广播和组播：UDP支持一对多通信
- 适用场景：实时性要求高、允许少量丢包的应用

## HTTP编程
- HttpURLConnection：JDK自带，使用繁琐但无额外依赖
- HttpClient（JDK 11+）：现代HTTP客户端，支持同步和异步请求
- GET vs POST：GET参数在URL中（有长度限制），POST在请求体中
- HTTP状态码：200成功、301/302重定向、404未找到、500服务器错误
- HTTPS = HTTP + SSL/TLS，加密传输数据

## 网络框架
- Netty：高性能异步事件驱动网络框架，零拷贝、内存池
- BIO/NIO/AIO：阻塞IO、非阻塞IO、异步IO三种模型
- Netty核心组件：EventLoop、Channel、ChannelHandler、Pipeline
- 应用：RPC框架（Dubbo）、消息中间件（RocketMQ）底层通信
- WebSocket：全双工通信协议，适合实时聊天、推送场景
