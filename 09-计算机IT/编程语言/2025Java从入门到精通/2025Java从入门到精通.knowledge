# Java从入门到精通
# 领域: 计算机IT
# 学科: 编程语言
# 来源: Java从入门到精通（2025版）
# 描述: Java编程语言核心知识与高级特性，涵盖面向对象、集合框架、并发编程、JVM及现代Java特性

{{Java基础}}{{Java是面向对象的编译型语言，跨平台(Write Once, Run Anywhere)。JVM(Java虚拟机)执行字节码(.class)。基本类型：byte(1B)/short(2B)/int(4B)/long(8B)/float(4B)/double(8B)/char(2B)/boolean。引用类型：类、接口、数组。类型转换：自动拓宽和强制缩窄。运算符、控制流(if/switch/for/while)。String不可变类，StringBuilder可变。}}

{{面向对象编程}}{{封装(Encapsulation)：private字段+getter/setter。继承(Inheritance)：extends单继承，Object是所有类的根。多态(Polymorphism)：方法重载(编译时)和方法重写(运行时)。抽象类(abstract)和接口(interface)。Java 8+接口支持默认方法(default)和静态方法。构造函数链(this()/super())。instanceof模式匹配(Java 16+)。record不可变数据类(Java 16+)。}}

{{异常处理}}{{Throwable→Error(不可恢复)+Exception。受检异常(Checked Exception)：必须处理(try-catch或throws)。非受检异常(RuntimeException)：可选择处理。try-with-resources(Java 7)自动关闭资源(AutoCloseable)。多个catch合并(Java 7)：catch(IOException | SQLException e)。自定义异常。异常链。throw抛出，throws声明。finally总是执行。}}

{{集合框架}}{{Collection接口：List(ArrayList/LinkedList)、Set(HashSet/TreeSet/LinkedHashSet)、Queue(LinkedList/PriorityQueue)。Map：HashMap(哈希表)、TreeMap(红黑树)、LinkedHashMap、ConcurrentHashMap。Iterator迭代器。Collections工具类。不可变集合：List.of()/Map.of()(Java 9+)。Arrays工具类。集合选择：有序ArrayList，去重HashSet，排序TreeSet，键值对HashMap。}}

{{泛型}}{{泛型提供编译时类型安全。泛型类：class Box<T> {}。泛型方法：<T> T getFirst(List<T> list)。类型通配符：<?>无界通配、<? extends T>上界通配、<? super T>下界通配(PECS原则：Producer Extends, Consumer Super)。类型擦除：运行时泛型信息被擦除。泛型数组限制。桥接方法。泛型与反射。}}

{{Lambda与Stream API}}{{Lambda表达式：(a, b) -> a + b。函数式接口(@FunctionalInterface)：Runnable、Callable、Comparator、Predicate、Function、Consumer、Supplier。方法引用：类::静态方法、对象::实例方法、类::new。Stream API：stream()创建流→中间操作(filter/map/flatMap/sorted/distinct/limit/skip)→终端操作(collect/reduce/forEach/count/toArray)。Collectors工具类。并行流parallelStream。}}

{{并发编程}}{{Thread类和Runnable接口。线程状态：NEW→RUNNABLE→BLOCKED/WAITING/TIMED_WAITING→TERMINATED。synchronized关键字(内置锁)。volatile保证可见性。java.util.concurrent包：ExecutorService线程池、Future/CompletableFuture异步编程、CountDownLatch/CyclicBarrier/Semaphore同步工具、ConcurrentHashMap并发容器、ReadWriteLock读写锁。JUC的Lock接口(ReentrantLock)。原子变量(AtomicInteger等)。}}

{{JVM内存模型}}{{JVM内存区域：堆(Heap，对象实例)、方法区/元空间(Metaspace，类信息)、虚拟机栈(栈帧，局部变量)、程序计数器、本地方法栈。垃圾回收算法：标记-清除、标记-整理、复制算法。GC收集器：Serial、Parallel、CMS(已废弃)、G1(默认)、ZGC(低延迟)、Shenandoah。GC调优：-Xms/-Xmx堆大小、新生代/老年代比例、GC日志分析。内存泄漏排查。}}

{{Java IO与NIO}}{{IO流：字节流(InputStream/OutputStream)、字符流(Reader/Writer)。缓冲流(Buffered*)。对象序列化(Serializable)。NIO(New IO)：Channel、Buffer、Selector(多路复用)。ByteBuffer操作。NIO.2(Java 7)：Path、Files、FileSystem。Files工具类读写文件。try-with-resources自动关闭。内存映射文件(MappedByteBuffer)。零拷贝传输(transferTo)。}}

{{Java 8-21新特性}}{{Java 8：Lambda、Stream、Optional、新日期API(java.time)。Java 9：模块系统(JPMS)、JShell。Java 10：var局部变量类型推断。Java 11：HTTP Client、String增强。Java 14：switch表达式/箭头语法。Java 15：Text Block文本块。Java 16：record、instanceof模式匹配。Java 17：密封类(sealed)。Java 21：虚拟线程(Virtual Threads)、switch模式匹配、Record Patterns、Sequenced Collections。}}

{{Stream高级操作}}{{Stream分组：Collectors.groupingBy()。分区：Collectors.partitioningBy()。聚合：Collectors.summingInt/averagingInt/joining/reducing。收集到Map：Collectors.toMap()。下游收集器嵌套。flatMap展平嵌套集合。takeWhile/dropWhile(Java 9+)。Stream.ofNullable(Java 9+)。自定义收集器(Collector接口)。并行流性能注意事项。}}

{{Optional类}}{{Optional<T>优雅处理可能为null的值。Optional.of(value)/ofNullable(null)/empty()。isPresent()/isEmpty()(Java 11+)。orElse(default)/orElseGet(supplier)/orElseThrow()。map/flatMap链式转换。filter过滤。ifPresent(consumer)。Optional避免NullPointerException，替代null检查。最佳实践：不作为方法参数和字段类型。}}

{{反射机制}}{{java.lang.reflect包。Class对象：getClass()、Class.forName()、类名.class。获取构造器/方法/字段：getConstructor/getMethod/getDeclaredField。setAccessible(true)访问私有成员。反射调用：method.invoke(obj, args)。注解处理通过反射实现。框架核心：Spring IoC、Hibernate ORM。反射性能开销。应用：动态代理、工厂模式、测试框架。}}

{{注解(Annotation)}}{{内置注解：@Override、@Deprecated、@SuppressWarnings、@FunctionalInterface、@SafeVarargs。元注解：@Target(作用位置)、@Retention(保留策略)、@Documented、@Inherited、@Repeatable。自定义注解。注解处理器(Annotation Processor)在编译时处理注解。Lombok通过注解生成代码。运行时注解通过反射读取。Spring大量使用注解配置。}}

{{设计模式应用}}{{单例：枚举实现(最佳)、静态内部类、双重检查锁。工厂：简单工厂/工厂方法/抽象工厂。建造者(Builder)：链式构建复杂对象。策略：Lambda替代策略接口。观察者：事件机制。装饰者：IO流包装。代理：静态代理和动态代理(JDK/CGLIB)。模板方法：定义算法骨架。Java 8 Lambda简化很多设计模式。}}

{{构建工具Maven/Gradle}}{{Maven：pom.xml定义项目结构、依赖、插件。依赖管理：groupId:artifactId:version。依赖范围：compile/test/provided/runtime。传递依赖和依赖排除。生命周期：compile→test→package→install→deploy。Gradle：build.gradle(Groovy/Kotlin DSL)。增量构建、构建缓存。依赖解析策略。多模块项目。Gradle Wrapper确保构建一致性。}}

{{测试(JUnit 5)}}{{JUnit 5核心：@Test、@BeforeEach/@AfterEach、@BeforeAll/@AfterAll。断言：assertEquals/assertNull/assertThrows/assertAll。参数化测试：@ParameterizedTest + @ValueSource/@CsvSource/@MethodSource。嵌套测试：@Nested。标签：@Tag。重复测试：@RepeatedTest。Mockito：Mock/Verify/Spy。AssertJ流式断言。测试覆盖率：JaCoCo。}}

{{JVM调优实战}}{{JVM参数分类：标准(-)、非标准(-X)、不稳定(-XX)。堆配置：-Xms/-Xmx/-Xmn。GC选择：-XX:+UseG1GC/-XX:+UseZGC。GC日志：-Xlog:gc*。JVM监控工具：jps/jstat/jmap/jstack/jinfo。JFR(Java Flight Recorder)低开销性能分析。Arthas在线诊断工具。内存分析：MAT(Memory Analyzer Tool)。常见问题：OOM、CPU飙升、死锁排查。}}

{{Java网络编程}}{{Socket编程：ServerSocket/Socket。多线程服务器。NIO Selector实现高并发。HTTP客户端(Java 11+)：HttpClient。WebSocket支持。Netty框架：高性能网络编程。gRPC-Java：RPC框架。OkHttp/Retrofit：HTTP客户端库。网络协议：TCP/UDP/HTTP/HTTPS。序列化：JSON(Jackson/Gson)、Protobuf、Avro。超时和重试策略。}}

{{Java生态与框架}}{{Spring生态：Spring Framework(IoC/AOP)、Spring Boot(快速开发)、Spring Cloud(微服务)、Spring Data(数据访问)。持久层：MyBatis、Hibernate/JPA。构建：Maven/Gradle。Web框架：Spring MVC、Quarkus、Micronaut。消息中间件：Spring Kafka、Spring AMQP。响应式：Project Reactor、RxJava。Java是企业级应用首选语言。}}
