{{知识点名称}}文件读写基础{{知识点内容}}open(file, mode, encoding='utf-8')返回文件对象。模式：'r'读（默认）、'w'写（覆盖）、'a'追加、'x'创建、'b'二进制、't'文本（默认）。组合：'rb'读二进制、'wb'写二进制、'r+'读写。with语句自动关闭文件（推荐）。encoding='utf-8'指定编码避免乱码
{{知识点名称}}文本文件操作{{知识点内容}}f.read()读取全部内容字符串；f.readline()读一行（含换行符）；f.readlines()返回行列表；for line in f逐行遍历内存友好。f.write('text')写入字符串；f.writelines(lines)写入行列表；mode='a'追加模式。print('text', file=f)用print函数写入文件
{{知识点名称}}二进制文件操作{{知识点内容}}'rb'、'wb'、'ab'处理图片/音频/视频等非文本文件。f.read()返回bytes对象；f.write(b'binary data')写入字节。struct模块打包/解包二进制数据。pickle模块序列化Python对象：pickle.dump(obj, f) / pickle.load(f)。JSON序列化：json.dump(obj, f, ensure_ascii=False) / json.load(f)
{{知识点名称}}os与pathlib模块{{知识点内容}}os模块：os.path.join()拼接路径、exists()检查存在、makedirs()创建目录、listdir()列出目录、remove()删除、rename()重命名。pathlib模块（推荐）：from pathlib import Path。Path操作：Path('dir') / 'file.txt'拼接、.exists()、.is_file()、.is_dir()。创建：p.mkdir(parents=True, exist_ok=True)递归创建。遍历：p.glob('*.py')匹配、p.rglob('**/*.py')递归匹配。读写：p.read_text()、p.write_text()、p.read_bytes()、p.write_bytes()
{{知识点名称}}CSV与Excel文件{{知识点内容}}csv模块：csv.reader(f)返回行列表、csv.DictReader(f)返回字典列表、csv.writer(f).writerows(data)写入。openpyxl库读写Excel：load_workbook('file.xlsx')打开、ws.active获取活动表、ws['A1'].value读取、ws.append([1,2,3])写入、wb.save()保存。pandas：pd.read_csv()、pd.read_excel()、df.to_csv()强大数据处理