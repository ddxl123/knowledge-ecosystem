{{知识点名称}}变量与数据类型{{知识点内容}}Python是动态类型语言，变量无需声明类型，赋值即定义。基本类型：int（任意精度整数）、float（浮点数）、str（字符串）、bool（True/False）、None。复合类型：list、tuple、dict、set。类型转换用int()、float()、str()、bool()等。类型检查推荐isinstance(x, int)。变量命名约定snake_case，常量约定全大写如MAX_SIZE = 100
{{知识点名称}}字符串操作{{知识点内容}}定义：单引号、双引号、三引号（多行）。f-string格式化：f"Name: {name}" 推荐方式。常用方法：upper()、lower()、strip()、split()、join()、replace()、find()、startswith()、endswith()。切片s[start:stop:step]，s[::-1]反转。字符串不可变，修改操作返回新字符串。encode('utf-8')转字节、decode()字节转字符串
{{知识点名称}}运算符{{知识点内容}}算术：+、-、*、/（真除法）、//（整除）、%（取模）、**（幂）。比较：==、!=、>、<、>=、<=。逻辑：and、or、not（关键字）。赋值：=、+=、-=、*=、/=等。成员：in、not in。身份：is、is not判断是否同一对象。海象运算符（Python 3.8+）：if (n := len(data)) > 10: 赋值并判断
{{知识点名称}}控制流程{{知识点内容}}if-elif-else条件不需要括号，用缩进表示代码块。三元表达式：x = a if condition else b。for循环遍历可迭代对象。while循环条件为真时执行。列表推导式：[x**2 for x in range(10) if x % 2 == 0]。字典推导式：{k: v for k, v in items.items()}。生成器表达式：(x**2 for x in range(10))惰性求值。match-case（Python 3.10+）模式匹配
{{知识点名称}}数据结构{{知识点内容}}list有序可变序列：append/pop/insert/sort/reverse。tuple有序不可变序列：解包a, b, c = (1, 2, 3)。dict键值对映射：get/update/pop/keys/values/items。set无序不重复集合：add/remove/union/intersection/difference。collections模块：deque双端队列、Counter计数器、defaultdict默认字典、OrderedDict。排序：sorted()返回新列表、list.sort()原地排序，key参数自定义