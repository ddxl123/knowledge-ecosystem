# Python 基础语法


## 收集指南

> ⚠️ **严格范围限定：** 仅收集本章（第1章 基础语法）相关内容，禁止跨章节引用。例如：不得将其他章节的知识点混入本章。

### 条目描述
本文件是《2026Python从入门到精通》中关于「Python 基础语法」的知识原子文件，属于编程语言方向。

### 知识结构
- 核心知识点
- 2026年更新要点

### 待收集原子知识点
- 核心知识点：核心概念、关键术语、原理机制、实践用法
- 2026年更新要点：核心概念、关键术语、原理机制、实践用法

## 核心知识点

### 一、Python简介
```python
# Python的特点
# 1. 简洁易读
# 2. 解释型语言
# 3. 动态类型
# 4. 跨平台
# 5. 丰富的标准库和第三方库

# Python的应用领域
# - Web开发（Django, Flask）
# - 数据科学（NumPy, Pandas）
# - 人工智能（TensorFlow, PyTorch）
# - 自动化运维
# - 网络爬虫
```

### 二、变量与数据类型
```python
# 变量命名规则
# 1. 由字母、数字、下划线组成
# 2. 不能以数字开头
# 3. 区分大小写
# 4. 不能使用关键字

# 基本数据类型
name = "Alice"          # 字符串 str
age = 25                # 整数 int
height = 1.68           # 浮点数 float
is_student = True       # 布尔值 bool
score = None            # 空值 NoneType

# 类型转换
int("123")      # 字符串转整数
str(123)        # 整数转字符串
float("3.14")   # 字符串转浮点数
bool(1)         # 转布尔值
```

### 三、运算符
```python
# 算术运算符
+   # 加法
-   # 减法
*   # 乘法
/   # 除法
//  # 整除
%   # 取余
**  # 幂运算

# 比较运算符
==  # 等于
!=  # 不等于
>   # 大于
<   # 小于
>=  # 大于等于
<=  # 小于等于

# 逻辑运算符
and  # 与
or   # 或
not  # 非

# 赋值运算符
=   # 赋值
+=  # 加等于
-=  # 减等于
*=  # 乘等于
/=  # 除等于
```

### 四、流程控制
```python
# 条件语句
if condition:
    pass
elif condition:
    pass
else:
    pass

# for循环
for i in range(10):
    print(i)

# while循环
while condition:
    pass

# break和continue
for i in range(10):
    if i == 5:
        break
    if i % 2 == 0:
        continue
    print(i)
```

### 五、数据结构
```python
# 列表 list
fruits = ["apple", "banana", "cherry"]
fruits.append("orange")
fruits.remove("banana")
fruits[0]

# 元组 tuple
point = (1, 2, 3)

# 字典 dict
person = {"name": "Alice", "age": 25}
person["name"]
person.get("name")

# 集合 set
numbers = {1, 2, 3, 4, 5}
numbers.add(6)
numbers.remove(1)
```

## 2026年更新要点
- 新增了Python 3.13的新特性
- 增加了match-case模式匹配的详细讲解
- 更新了类型注解（Type Hints）的最新用法
- 新增了f-string的高级用法
