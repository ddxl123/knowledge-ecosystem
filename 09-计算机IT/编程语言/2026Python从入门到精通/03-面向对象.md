# 面向对象编程


## 收集指南

> ⚠️ **严格范围限定：** 仅收集本章（第3章 面向对象）相关内容，禁止跨章节引用。例如：不得将其他章节的知识点混入本章。

### 条目描述
本文件是《2026Python从入门到精通》中关于「面向对象编程」的知识原子文件，属于编程语言方向。

### 知识结构
- 核心知识点
- 2026年更新要点

### 待收集原子知识点
- 核心知识点：核心概念、关键术语、原理机制、实践用法
- 2026年更新要点：核心概念、关键术语、原理机制、实践用法

## 核心知识点

### 一、类与对象
```python
# 定义类
class Dog:
    # 类属性
    species = "Canis familiaris"

    # 构造方法
    def __init__(self, name, age):
        self.name = name  # 实例属性
        self.age = age

    # 实例方法
    def bark(self):
        return f"{self.name} says Woof!"

# 创建对象
dog = Dog("Buddy", 3)
print(dog.bark())
```

### 二、继承
```python
# 单继承
class Animal:
    def __init__(self, name):
        self.name = name

    def speak(self):
        raise NotImplementedError

class Dog(Animal):
    def speak(self):
        return f"{self.name} says Woof!"

# 多继承
class Flyable:
    def fly(self):
        return "Flying"

class Swimmable:
    def swim(self):
        return "Swimming"

class Duck(Animal, Flyable, Swimmable):
    pass
```

### 三、多态
```python
# 多态
class Cat(Animal):
    def speak(self):
        return f"{self.name} says Meow!"

def animal_speak(animal):
    return animal.speak()

dog = Dog("Buddy")
cat = Cat("Kitty")
print(animal_speak(dog))  # Buddy says Woof!
print(animal_speak(cat))  # Kitty says Meow!
```

### 四、封装
```python
class BankAccount:
    def __init__(self, balance=0):
        self.__balance = balance  # 私有属性

    def deposit(self, amount):
        if amount > 0:
            self.__balance += amount

    def get_balance(self):
        return self.__balance

    @property
    def balance(self):
        return self.__balance
```

### 五、魔法方法
```python
class Point:
    def __init__(self, x, y):
        self.x = x
        self.y = y

    def __str__(self):
        return f"Point({self.x}, {self.y})"

    def __repr__(self):
        return f"Point({self.x}, {self.y})"

    def __add__(self, other):
        return Point(self.x + other.x, self.y + other.y)

    def __eq__(self, other):
        return self.x == other.x and self.y == other.y

    def __len__(self):
        return int((self.x**2 + self.y**2)**0.5)
```

### 六、抽象类与接口
```python
from abc import ABC, abstractmethod

class Shape(ABC):
    @abstractmethod
    def area(self):
        pass

    @abstractmethod
    def perimeter(self):
        pass

class Circle(Shape):
    def __init__(self, radius):
        self.radius = radius

    def area(self):
        return 3.14 * self.radius ** 2

    def perimeter(self):
        return 2 * 3.14 * self.radius
```

## 2026年更新要点
- 新增了dataclass装饰器的详细讲解
- 增加了类的类型注解
- 更新了__slots__的性能优化
- 新增了Protocol类（结构化子类型）
