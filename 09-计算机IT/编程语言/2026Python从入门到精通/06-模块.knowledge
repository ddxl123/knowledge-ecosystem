{{知识点名称}}模块基础{{知识点内容}}一个.py文件就是一个模块，避免命名冲突。导入：import module、from module import func、from module import *（不推荐）。别名：import numpy as np。模块搜索路径为sys.path列表。import后缓存在sys.modules中，再次import不重新执行。__name__：直接运行时为'__main__'，被导入时为模块名。if __name__ == '__main__': 模块入口保护
{{知识点名称}}包{{知识点内容}}包含__init__.py的目录，Python 3.3+可省略（隐式命名空间包）。目录结构：mypackage/__init__.py、module1.py、module2.py。导入：import mypackage.module1 或 from mypackage import module1。__init__.py可定义__all__控制from package import *行为。相对导入：from . import module（同级）、from .. import module（上级）。绝对导入推荐使用
{{知识点名称}}常用标准库{{知识点内容}}os操作系统接口；sys解释器参数（path、argv、exit）；datetime日期时间（date、time、datetime、timedelta）；json编解码（dumps、loads、dump、load）；re正则表达式（match、search、findall、sub）；collections高级数据结构（Counter、defaultdict、deque、namedtuple）；itertools迭代器工具（chain、product、permutations）；functools函数工具（lru_cache、partial、reduce、wraps）；pathlib面向对象路径（推荐替代os.path）；logging日志模块
{{知识点名称}}第三方包管理{{知识点内容}}pip install/uninstall管理包。requirements.txt：pip freeze > requirements.txt导出、pip install -r requirements.txt安装。虚拟环境：python -m venv env创建、source env/bin/activate激活。conda适合数据科学。pyproject.toml（PEP 621）现代项目配置文件。发布：python -m build构建、twine upload dist/*发布到PyPI
{{知识点名称}}生成器与迭代器{{知识点内容}}迭代器协议：实现__iter__和__next__方法。iter()和next()手动迭代。生成器函数使用yield暂停和恢复执行。生成器表达式：(x**2 for x in range(10))惰性求值。send()向生成器发送值、throw()抛异常、close()关闭。itertools常用：count()无限计数、cycle()无限循环、islice()切片、chain()连接