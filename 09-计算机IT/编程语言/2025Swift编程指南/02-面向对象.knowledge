# 面向对象

## 类与结构体
- class：引用类型，支持继承、析构器、类型检查
- struct：值类型，自动获得成员初始化器，推荐优先使用
- 值类型赋值时复制（写时复制优化），引用类型共享同一实例
- 类有析构器deinit，结构体没有
- mutating方法：结构体方法修改自身属性需标记mutating

## 属性
- 存储属性：var name: String，存储实际值
- 计算属性：var fullName: String { get { } set { } }
- 属性观察器：willSet（设置前）、didSet（设置后）
- 类型属性：static var count = 0，属于类型而非实例
- lazy属性：lazy var x = ...，首次访问时初始化

## 方法
- 实例方法：定义在类型内部，通过实例调用
- 类型方法：static func / class func，通过类型调用
- self：引用当前实例
- mutating：结构体和枚举中修改自身的值类型方法
- class方法可被子类重写（override），static不可

## 继承与多态
- class SubClass: SuperClass { }，单继承
- override重写方法和属性，super调用父类实现
- final class/method：阻止继承和重写
- 类型检查：is（判断类型）、as?（条件转换）、as!（强制转换）
- 协议多态：通过协议类型调用具体实现

## 初始化
- 指定初始化器：init，必须初始化所有未赋默认值的存储属性
- 便捷初始化器：convenience init，必须调用同一类的指定初始化器
- 两段式初始化：第一阶段初始化所有属性，第二阶段可访问self
- 可失败初始化器：init?，可能返回nil
- 必要初始化器：required init，所有子类必须实现
