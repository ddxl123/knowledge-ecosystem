# SwiftUI

## 基础视图
- Text：文本显示，支持样式修饰符
- Image：图片显示，SF Symbols、本地图片、网络图片
- Button：按钮，action闭包+label视图
- TextField/TextEditor：文本输入
- 修饰符链：.font().padding().background()，顺序影响效果

## 布局系统
- HStack/VStack/ZStack：水平/垂直/层叠布局
- Spacer：弹性空间，推动视图到两端
- GeometryReader：读取父视图尺寸，自适应布局
- LazyVStack/LazyHStack：懒加载，适合长列表
- ScrollView：滚动视图，支持水平和垂直方向

## 状态管理
- @State：视图私有的可变状态，值类型
- @Binding：子视图绑定父视图的状态
- @ObservedObject：观察ObservableObject的引用类型
- @StateObject：视图拥有ObservableObject实例
- @EnvironmentObject：从环境中获取共享对象
- @Environment：系统环境值，如colorScheme、locale

## 导航与列表
- NavigationView/NavigationStack：导航容器
- List：列表视图，支持分组和编辑
- ForEach：动态生成视图列表
- NavigationLink：导航链接，跳转到目标视图
- Sheet/FullScreenCover：模态呈现

## 动画与效果
- .animation(.easeInOut, value: someValue)：隐式动画
- withAnimation { }：显式动画控制
- Transition：.opacity、.slide、.scale、.asymmetric
- matchedGeometryEffect：共享元素过渡动画
- TimelineView/Canvas：自定义绘制和时间驱动动画
