# JavaScript从入门到精通
# 领域: 计算机IT
# 学科: 编程语言
# 来源: JavaScript从入门到精通（2025版）
# 描述: JavaScript语言核心知识与现代开发实践，涵盖语法基础、异步编程、DOM操作、ES6+特性及Node.js应用

{{JavaScript基础}}{{JavaScript是动态类型的解释型脚本语言。基本类型：string、number、bigint、boolean、undefined、null、symbol。引用类型：Object(Array/Function/Date/RegExp/Map/Set)。typeof操作符。var/let/const声明变量(let块作用域、const不可重赋值)。类型转换：隐式(==)和显式(parseInt/String/Number)。模板字符串：`Hello ${name}`。解构赋值：对象和数组。}}

{{函数与作用域}}{{函数声明(提升)和函数表达式(不提升)。箭头函数：(a, b) => a + b，无自己的this。闭包(Closure)：函数记住其词法作用域。IIFE(立即执行函数表达式)。this绑定规则：默认(全局/undefined)、隐式(对象方法)、显式(call/apply/bind)、new(构造函数)。箭头函数的this继承外层。作用域链和变量查找。块作用域(let/const)。}}

{{异步编程}}{{JavaScript是单线程事件循环模型。回调函数(Callback)→回调地狱。Promise：pending/fulfilled/rejected三状态。链式调用：.then().catch().finally()。Promise.all(全部成功)/race(第一个完成)/any(任一成功)/allSettled(全部完成)。async/await：以同步方式写异步代码。错误处理：try-catch配合await。微任务(Promise)和宏任务(setTimeout)执行顺序。}}

{{ES6+核心特性}}{{let/const、箭头函数、模板字符串、解构赋值、展开运算符(...)、默认参数、剩余参数。类(Class)语法：constructor、extends、super、static。模块系统：import/export。Symbol唯一标识。Map/Set数据结构。WeakMap/WeakSet弱引用。迭代器(Iterator)和生成器(Generator function*)。Proxy和Reflect。可选链(?.)和空值合并(??)。}}

{{数组方法}}{{高阶数组方法：map(映射)、filter(过滤)、reduce(归约)、forEach(遍历)、find(查找)、some/every(判定)、sort(排序)、flat/flatMap(扁平化)、includes(包含)。数组创建：Array.from()、Array.of()。at()支持负索引。toSorted/toReversed/toSpliced(非变异方法)。reduce强大用法：计数、分组、扁平化、管道组合。}}

{{对象操作}}{{对象创建：字面量{}、Object.create()、new Object()。属性访问：点号和方括号。Object.keys/values/entries获取键值。Object.assign合并对象。Object.freeze/seal冻结/密封。展开运算符浅拷贝：{...obj}。解构：const {a, b} = obj。getter/setter存取器属性。对象属性描述符：writable/enumerable/configurable。in操作符和hasOwnProperty。}}

{{原型与继承}}{{每个对象有__proto__指向其原型。构造函数的prototype属性。原型链：对象→prototype→Object.prototype→null。ES6 class语法糖底层仍是原型继承。继承实现：class Child extends Parent。instanceof检查原型链。Object.getPrototypeOf()获取原型。原型污染风险和防护。组合继承、寄生组合继承模式。}}

{{DOM操作}}{{DOM(Document Object Model)是HTML的编程接口。获取元素：getElementById/querySelector/querySelectorAll。创建/删除：createElement/removeChild。修改：innerHTML/innerText/textContent、setAttribute/classList。事件处理：addEventListener/removeEventListener。事件冒泡和捕获。事件委托(Event Delegation)。DOM性能：DocumentFragment批量操作。虚拟DOM原理。}}

{{BOM与Web API}}{{BOM(Browser Object Model)：window、navigator、location、history、screen。定时器：setTimeout/setInterval/clearTimeout/clearInterval。requestAnimationFrame动画。fetch API网络请求。LocalStorage/SessionStorage/IndexedDB存储。Geolocation定位。Notification通知。Clipboard剪贴板。IntersectionObserver/ResizeObserver/MutationObserver。Web Workers多线程。}}

{{错误处理}}{{try-catch-finally捕获和处理错误。throw抛出自定义错误。Error类型：Error/TypeError/RangeError/SyntaxError/ReferenceError。自定义错误类：class AppError extends Error。Error.stack堆栈追踪。window.onerror/addEventListener('error')全局错误捕获。unhandledrejection事件。错误边界(React)。Sentry等错误监控服务。防御性编程。}}

{{模块系统}}{{CommonJS(Node.js)：require()导入，module.exports导出。ES Modules(ESM)：import/export，浏览器和Node.js都支持。静态分析：export default和命名export。动态导入：import()返回Promise。模块解析：相对路径/包名/别名。Tree Shaking(摇树优化)：移除未使用代码。UMD兼容CommonJS和AMD。模块打包器：Webpack、Vite、Rollup、esbuild。}}

{{正则表达式}}{{正则表达式：/pattern/flags。元字符：. \d \w \s \b ^ $。量词：* + ? {n} {n,m}。字符类：[abc] [^abc] [a-z]。分组捕获：()和反向引用\1。前瞻/后顾：(?=) (?!。字符串方法：match/test/replace/search/split。RegExp对象：new RegExp()。全局(g)、不区分大小写(i)、多行(m)标志。命名捕获组(?<name>)。性能：避免灾难性回溯。}}

{{Proxy与元编程}}{{Proxy拦截对象操作：get/set/deleteProperty/has/apply等13种陷阱(trap)。const proxy = new Proxy(target, handler)。Reflect API提供默认行为。应用场景：数据绑定、验证、日志、惰性加载、不可变数据。Vue 3使用Proxy实现响应式。Symbol内置符号：Symbol.iterator、Symbol.toPrimitive、Symbol.hasInstance。Well-Known Symbols。}}

{{Web安全}}{{XSS(跨站脚本)：DOM型/反射型/存储型，防护：转义输出、CSP。CSRF(跨站请求伪造)：SameSite Cookie、CSRF Token。点击劫持：X-Frame-Options。内容安全策略(CSP)。HTTPS。避免eval和innerHTML。输入验证。CORS(跨域资源共享)：简单请求和预检请求。Cookie安全属性：HttpOnly/Secure/SameSite。依赖安全：npm audit。}}

{{性能优化}}{{减少DOM操作(批量更新)。事件委托减少事件监听器。防抖(Debounce)和节流(Throttle)。requestAnimationFrame优化动画。Web Workers处理CPU密集任务。代码分割和懒加载(import())。Tree Shaking减少包体积。图片优化(懒加载/响应式/格式选择)。CDN加速。缓存策略(Service Worker/HTTP缓存)。内存泄漏排查(DevTools)。}}

{{TypeScript基础}}{{TypeScript是JavaScript的超集，添加静态类型。基本类型注解：string/number/boolean/any/unknown/void/never。接口(interface)和类型别名(type)。泛型：function identity<T>(arg: T): T。联合类型：string | number。字面量类型。类型断言：as Type或<Type>。枚举(enum)。类型守卫：typeof/instanceof/in。映射类型和条件类型。Utility Types：Partial/Required/Pick/Omit/Record。}}

{{Fetch与HTTP}}{{fetch()是浏览器原生HTTP API。fetch(url, options)返回Promise<Response>。GET/POST/PUT/DELETE请求。请求头设置：Headers对象。请求体：JSON.stringify()、FormData、Blob。响应处理：response.json()、response.text()。错误处理：网络错误和HTTP错误(status判断)。AbortController取消请求。请求拦截和响应拦截(axios)。并发请求：Promise.all。}}

{{迭代器与生成器}}{{迭代器协议：对象实现next()方法返回{value, done}。可迭代协议：实现[Symbol.iterator]()方法。for...of遍历可迭代对象。生成器函数：function* yield暂停和恢复。生成器是特殊的迭代器。应用：惰性求值、无限序列、异步迭代(for await...of)。async生成器：async function*。自定义可迭代类。展开运算符、解构、Array.from都支持可迭代对象。}}

{{现代JavaScript开发工具}}{{包管理器：npm/yarn/pnpm。构建工具：Vite(开发首选)、Webpack(成熟)、esbuild(极速)、Turbopack。代码质量：ESLint(代码检查)、Prettier(格式化)。测试：Jest、Vitest、Playwright(E2E)。类型检查：TypeScript。版本管理：Git + Husky + lint-staged。Monorepo：pnpm workspace/Turborepo/Nx。CI/CD：GitHub Actions。}}
