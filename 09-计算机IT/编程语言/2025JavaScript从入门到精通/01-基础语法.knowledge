# 基础语法

## 变量与数据类型
- var：函数作用域，存在变量提升（hoisting），可重复声明
- let：块级作用域，不存在提升（暂时性死区TDZ），不可重复声明
- const：块级作用域，声明时必须初始化，引用不可变（对象内部可变）
- 基本类型：number、string、boolean、null、undefined、symbol（ES6）、bigint（ES2020）
- 引用类型：object（包括数组、函数、日期、正则等）

## 运算符
- 算术运算符：+、-、*、/、%、**（幂，ES7）、++、--
- 比较运算符：==（类型转换后比较）、===（严格比较，推荐使用）
- 逻辑运算符：&&、||、??（空值合并，ES2020，null/undefined时取右值）
- 可选链：obj?.prop、arr?.[index]、func?.()（ES2020）
- 解构赋值：const {a, b} = obj; const [x, y] = arr;

## 流程控制
- if-else、switch-case（使用===严格比较）、三元运算符 ?:
- for、for-in（遍历对象可枚举属性）、for-of（遍历可迭代对象，ES6）
- while、do-while、break、continue
- 标签语句：outer: for(...) { break outer; }
- try-catch-finally，throw主动抛出异常

## 函数
- 函数声明：function name() {}，存在提升
- 函数表达式：const fn = function() {}，不存在提升
- 箭头函数（ES6）：const fn = () => {}，无自己的this、arguments、super
- 默认参数（ES6）：function f(x = 10) {}
- 剩余参数（ES6）：function f(...args) {}，替代arguments对象
- 立即执行函数IIFE：(function() { })()

## 字符串与模板
- 字符串方法：includes、startsWith、endsWith、padStart、padEnd、repeat
- 模板字面量（ES6）：反引号`` ` ``，支持多行和插值 `${expression}`
- 标签模板（Tagged Templates）：tag`string ${expr}`
- String.raw：获取原始字符串，不处理转义字符
- Unicode支持：codePointAt、fromCodePoint、for-of遍历码点
