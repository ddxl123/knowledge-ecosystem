# 集合框架

## Collection体系
- List：ArrayList（动态数组）、LinkedList（双向链表）、Vector（已过时）
- Set：HashSet（哈希表）、LinkedHashSet（有序）、TreeSet（红黑树排序）
- Queue/Deque：ArrayDeque（推荐替代Stack）、PriorityQueue（堆）
- 不可变集合（JDK 10+）：List.copyOf()、Set.copyOf()、Map.copyOf()
- 集合工厂方法（JDK 9+）：List.of()、Set.of()、Map.of()、Map.ofEntries()

## Map体系
- HashMap：数组+链表+红黑树（JDK 8+），容量总是2的幂
- HashMap扩容：容量翻倍，重新哈希，负载因子0.75
- ConcurrentHashMap：JDK 7分段锁，JDK 8 CAS+synchronized锁单个桶
- TreeMap：红黑树，O(logN)操作，支持NavigableMap接口
- WeakHashMap：弱引用键，GC时自动清理，适合缓存场景

## Stream API增强
- 基本类型流：IntStream、LongStream、DoubleStream，避免装箱开销
- 收集器增强：Collectors.toUnmodifiableList()（JDK 10+）
- takeWhile/dropWhile（JDK 9+）：按条件截断流
- ofNullable（JDK 9+）：Stream.ofNullable()处理可能为null的元素
- Stream.toList()（JDK 16+）：返回不可修改的List，替代collect(Collectors.toList())

## 迭代与遍历增强
- Iterator的forEachRemaining()方法
- ListIterator：双向迭代，add/set操作
- Iterable.forEach()：配合Lambda简洁遍历
- Spliterator：并行流的分割迭代器，trySplit/tryAdvance
- Enumeration：遗留接口，Vector/Hashtable使用

## 实用数据结构
- BitSet：位集合，高效存储布尔值集合
- EnumSet/EnumMap：枚举专用集合，位向量实现，性能最优
- IdentityHashMap：用==而非equals比较键，特殊场景使用
- Properties：属性文件读取，继承Hashtable
- Collections.unmodifiableXxx() vs List.of()：后者更简洁且真正不可变
