# IO流

## 传统IO流
- 字节流：InputStream/OutputStream，适合二进制数据
- 字符流：Reader/Writer，适合文本数据，自动处理编码
- 缓冲流：BufferedXxx，8KB默认缓冲区，显著提升性能
- 转换流：InputStreamReader/OutputStreamWriter，字节→字符+编码
- 打印流：PrintStream/PrintWriter，格式化输出，不会抛IO异常

## 文件操作增强
- Files工具类（JDK 7+）：readString/writeString（JDK 11+）、readAllLines/write
- Path/Paths：路径操作，resolve()、relativize()、normalize()
- FileVisitor接口：遍历文件树，preVisitDirectory/postVisitDirectory/visitFile
- WatchService：文件系统监控，监听目录变化事件
- 临时文件：Files.createTempFile()、Files.createTempDirectory()

## NIO与NIO.2
- Buffer：ByteBuffer（direct/heap）、CharBuffer，flip/clear/compact操作
- Channel：FileChannel（文件）、SocketChannel（TCP）、DatagramChannel（UDP）
- Selector：多路复用，一个线程管理多个Channel
- 内存映射文件：FileChannel.map()，MappedByteBuffer
- Scatter/Gather：分散读取/聚集写入，多个Buffer操作一个Channel

## 序列化增强
- Serializable+serialVersionUID：版本兼容性控制
- transient关键字：排除字段序列化
- Externalizable：自定义readExternal/writeExternal
- JSON序列化：Jackson/Gson库，替代Java原生序列化
- Protocol Buffers：跨语言、高效的二进制序列化

## 新IO特性
- HttpClient（JDK 11+）：替代HttpURLConnection，支持HTTP/2
- 异步HTTP：HttpClient.sendAsync()，返回CompletableFuture
- Reactive Streams：Flow API（JDK 9+），发布-订阅模式
- Foreign Memory API（JDK 14+）：安全高效的堆外内存访问
- Zip文件系统：FileSystem.newFileSystem()直接操作ZIP文件
