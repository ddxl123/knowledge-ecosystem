# 面向对象

## 类与对象
- 构造器：主构造器和次构造器，this()调用链
- 初始化块：init块在构造器前执行，静态初始化块只执行一次
- 数据类：Record（JDK 16+）自动生成getter/equals/hashCode/toString
- 不可变对象：final类+final字段+无修改方法，天然线程安全
- var关键字：局部变量类型推断，var list = new ArrayList<String>()

## 封装、继承、多态
- 访问控制：private→default(包)→protected(包+子类)→public
- final类不能继承，final方法不能重写，final变量不能修改
- 密封类（Sealed Class）：permits关键字限制哪些类可以继承
- 模式匹配多态（JDK 21+）：switch(obj) { case Circle c -> ... case Square s -> ... }
- 协变返回类型：重写方法可返回父类返回类型的子类型

## 抽象类与接口增强
- 抽象类：可有构造器、字段、具体方法和抽象方法
- 接口default方法（JDK 8+）：提供默认实现，不影响已有实现类
- 接口static方法（JDK 8+）：工具方法直接定义在接口中
- 接口private方法（JDK 9+）：提取default方法中的公共逻辑
- 接口 vs 密封类 vs 抽象类：接口定义能力，密封类定义封闭层次

## 注解与反射
- 元注解：@Target、@Retention、@Documented、@Inherited、@Repeatable
- 内置注解：@Override、@Deprecated、@SuppressWarnings、@FunctionalInterface
- 自定义注解：@interface定义，可有default值
- 反射API：Class<T>、Method、Field、Constructor
- 注解处理器：编译时生成代码（APT），如Lombok

## 新特性深度
- Record模式解构（JDK 21+预览）：if (obj instanceof Point(int x, int y))
- 值类型预览（Project Valhalla）：减少对象头开销，扁平化内存布局
- Foreign Function和Memory API（JDK 22+）：替代JNI调用本地代码
- 虚拟线程与结构化并发（JDK 21+）：StructuredTaskScope管理子任务
- 作用域值（Scoped Values，JDK 21+预览）：替代ThreadLocal，虚拟线程友好
