# 所有权系统

## 所有权规则
- 每个值有且只有一个所有者（Owner）
- 同一时刻只能有一个所有者，离开作用域自动drop释放
- 所有权转移（Move）：let s2 = s1后，s1不再有效
- Copy类型：基本类型（i32、bool、f64等）实现了Copy trait，赋值时复制
- Clone：显式深拷贝，.clone()方法

## 借用规则
- 不可变借用(&T)：可同时存在多个，只读访问
- 可变借用(&mut T)：同一时刻只能有一个，独占访问
- 不可变借用和可变借用不能同时存在（读写互斥）
- 借用不能比所有者活得久（生命周期约束）
- NLL(Non-Lexical Lifetimes)：借用在最后一次使用后结束

## 生命周期
- 语法：'a标注生命周期参数，fn f<'a>(x: &'a str) -> &'a str
- 生命周期省略规则：输入生命周期→单一输入→输出借用自&self
- 'static：整个程序运行期间有效，如字符串字面量
- 结构体中的生命周期：struct S<'a> { data: &'a str }
- 多个生命周期：编译器取交集（最短的生命周期）

## 切片与字符串
- &str：字符串切片，不可变引用，指向某处UTF-8字节序列
- String：堆上分配，可增长的UTF-8字符串
- String::from / .to_string()：创建String
- &s[..]：切片语法，借用String的一部分
- 字符串不能按索引访问（UTF-8变长编码），用chars()/bytes()迭代

## 内存安全
- 无GC：所有权系统在编译期管理内存，零运行时开销
- 无空指针：Option<T>类型显式处理None情况
- 无悬垂引用：编译器通过生命周期检查确保引用有效
- 无数据竞争：借用规则在编译期防止并发数据竞争
- RAII：Drop trait在值离开作用域时自动释放资源
