# 生命周期进阶

## 生命周期基础回顾
- 生命周期是引用有效的作用域，编译器通过借用检查器验证
- 函数签名中的生命周期标注描述输入输出引用的关系
- 'a不是某个引用的寿命，而是描述多个引用间的关系
- 生命周期标注不改变引用的实际寿命，只是帮助编译器检查

## 结构体中的生命周期
- 包含引用的结构体必须标注生命周期：struct Excerpt<'a> { part: &'a str }
- 结构体实例的生命周期不能超过其引用数据的生命周期
- 多个引用字段可使用相同或不同的生命周期参数
- 通常建议结构体拥有数据（String而非&str）避免生命周期复杂性

## 生命周期省略规则
- 规则一：每个引用参数获得独立的生命周期
- 规则二：只有一个输入生命周期时，输出生命周期与之相同
- 规则三：方法中&self的生命周期赋给所有输出引用
- 三条规则不满足时必须手动标注，编译器无法推断

## 高级生命周期
- 'static：全局生命周期，字符串字面量、泄漏的Box
- 生命周期子类型：'a: 'b 表示'a至少和'b一样长
- 生命周期型变：协变（可以变短）、逆变（可以变长）
- Higher-Ranked Trait Bounds (HRTB)：for<'a> Fn(&'a T) -> &'a U
- 匿名生命周期'_：让编译器推断，fn f(x: &str) -> &'_ str

## 生命周期与泛型结合
- 泛型+生命周期：fn f<'a, T>(x: &'a T) -> &'a T
- trait对象中的生命周期：Box<dyn Trait + 'a>
- where子句中的生命周期约束：where T: 'a
- 常见错误：返回局部变量的引用，永远无法编译通过
- 解决方案：返回拥有所有权的类型，或确保引用来源的有效期
