# 错误处理

## Result与Option
- Result<T, E>：Ok(T)表示成功，Err(E)表示错误
- Option<T>：Some(T)表示有值，None表示无值
- unwrap()/expect()：快速获取值，失败时panic（仅用于原型/测试）
- 模式匹配：match result { Ok(v) => ..., Err(e) => ... }
- if let / while let：简化的模式匹配

## 错误传播
- ?运算符：result?，错误时提前返回Err（自动From转换）
- 链式调用：file.read_to_string(&mut s)?简洁传播错误
- 函数签名：fn f() -> Result<T, Box<dyn Error>> 返回任意错误
- map_err：转换错误类型，.map_err(|e| MyError::from(e))
- and_then：链式Result操作，类似flatMap

## 自定义错误类型
- 实现std::fmt::Display和std::error::Error trait
- 枚举错误：enum MyError { Io(io::Error), Parse(ParseError) }
- thiserror库：派生宏自动实现Error trait
- #[error("描述{0}")]：自定义错误消息
- #[from]：自动实现From转换

## 错误处理策略
- 顶层（main/入口）：统一处理所有错误，打印用户友好的消息
- 库代码：返回Result，不panic，让调用者决定如何处理
- 不可恢复错误：panic!宏，如数组越界、逻辑错误
- 可恢复错误：Result类型，如文件不存在、网络超时
- anyhow库：应用级错误处理，anyhow::Result简化错误传播

## 错误处理最佳实践
- 优先使用?运算符而非unwrap()，保持代码简洁
- 为每个模块/crate定义自己的错误枚举类型
- 使用thiserror（库）和anyhow（应用）简化错误处理
- 错误信息要有上下文，帮助调试
- 不要吞掉错误（忽略Err），至少记录日志
