# 协程

## 协程基础
- 协程是轻量级线程，由Kotlin编译器和运行时管理
- suspend函数：挂起函数，可在不阻塞线程的情况下暂停执行
- 协程构建器：launch（启动不返回结果）、async（返回Deferred结果）
- CoroutineScope：协程作用域，管理协程生命周期
- GlobalScope：全局作用域，生命周期跟随应用（慎用）

## 协程上下文与调度器
- Dispatchers.Default：CPU密集型任务，线程数=CPU核数
- Dispatchers.IO：IO密集型任务，共享线程池（默认64线程）
- Dispatchers.Main：UI线程（Android），更新界面
- withContext(Dispatcher)：切换协程上下文，非阻塞挂起
- CoroutineContext：协程上下文，包含Job、Dispatcher、异常处理器

## 结构化并发
- coroutineScope {}：等待所有子协程完成，异常传播
- supervisorScope {}：子协程失败不影响其他子协程
- 取消传播：父协程取消时所有子协程自动取消
- 协作取消：检查isActive，使用ensureActive()
- 超时：withTimeout(1000L) { } 或 withTimeoutOrNull

## Flow
- 冷流：flow { emit(value) }，收集时才执行
- 操作符：map、filter、take、drop、combine、zip
- 终端操作符：collect、toList、first、reduce
- StateFlow：有初始值的状态容器，始终有值
- SharedFlow：可配置缓存和重放的热流

## 异常处理
- launch中的异常会传播到CoroutineScope
- async中的异常在await()时抛出
- CoroutineExceptionHandler：全局异常处理
- try-catch包裹collect处理Flow中的异常
- supervisorScope中子协程异常不会传播到父级
