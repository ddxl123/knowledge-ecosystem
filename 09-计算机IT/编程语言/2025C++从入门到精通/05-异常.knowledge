# 异常处理

## 异常基础
- try-catch块：try { } catch (ExceptionType e) { }
- 抛出异常：throw ExceptionType("message")
- catch(...)：捕获所有异常，通常放在最后
- 异常对象：按值捕获或按引用捕获（推荐引用，避免切片）
- 栈展开：异常抛出时逐层退出函数调用栈，调用析构函数

## 异常规范
- C++98动态异常规范：throw(Type1, Type2)（C++17移除）
- noexcept关键字（C++11）：承诺函数不抛异常
- noexcept运算符：noexcept(expr)编译期判断表达式是否noexcept
- 析构函数默认noexcept，不应抛异常
- 移动操作应标记noexcept以启用优化

## 标准异常类
- exception基类：what()虚函数返回异常描述
- logic_error：逻辑错误，程序设计问题（invalid_argument、domain_error等）
- runtime_error：运行时错误（overflow_error、underflow_error等）
- bad_alloc：new分配失败抛出
- bad_cast：dynamic_cast引用转换失败抛出

## 异常安全保证
- 基本保证：异常后对象仍处于有效状态
- 强烈保证：异常后状态回滚到操作前（事务语义）
- 不抛保证（nothrow）：操作保证不抛异常
- RAII是实现异常安全的关键：智能指针、lock_guard
- copy-and-swap惯用法：实现强烈的异常安全保证

## 最佳实践
- 不要在析构函数中抛异常
- 使用RAII管理资源，减少try-catch
- 按引用捕获异常：catch (const std::exception& e)
- 自定义异常继承std::exception，重写what()
- 性能敏感场景考虑错误码替代异常（零开销异常：C++23）
