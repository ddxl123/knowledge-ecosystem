# 模板编程

## 函数模板
- 语法：template <typename T> T max(T a, T b)
- 类型推导：编译器根据实参自动推导模板参数
- 显式指定：max<int>(3, 5)
- 模板特化：template <> int max<int>(int a, int b)
- 非类型参数：template <int N> void f()，编译期常量

## 类模板
- 语法：template <typename T> class Stack { ... };
- 类外定义成员：template <typename T> void Stack<T>::push(T val) {}
- 类模板特化：template <> class Stack<bool> { ... }
- 偏特化：template <typename T> class Stack<T*> { ... }
- 默认模板参数：template <typename T, typename Alloc = std::allocator<T>>

## 模板进阶
- 可变参数模板（C++11）：template <typename... Args> void f(Args... args)
- 参数包展开：递归展开、折叠表达式（C++17）
- SFINAE：Substitution Failure Is Not An Error，替换失败不是错误
- enable_if：条件启用模板重载，基于类型特征
- if constexpr（C++17）：编译期条件分支，替代SFINAE

## Concepts（C++20）
- 概念定义：template <typename T> concept Sortable = requires(T t) { ... }
- 约束模板参数：template <Sortable T> void sort(T& container)
- requires表达式：检查类型是否满足特定操作
- 标准概念库：<concepts>头文件，Sortable、EqualityComparable等
- 简化模板错误：编译期给出清晰的约束失败信息

## 模板元编程
- 编译期计算：模板递归实现阶乘、斐波那契
- type_traits：<type_traits>头文件，is_integral、is_pointer等
- 类型变换：remove_const、add_pointer、conditional
- constexpr函数（C++11/14/20）：编译期求值，替代简单元编程
- 编译期if（C++17）：if constexpr 替代模板特化的条件分支
