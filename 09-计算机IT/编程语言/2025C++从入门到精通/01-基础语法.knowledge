# 基础语法

## 变量与数据类型
- 基本类型：int(通常32位)、short(16位)、long(至少32位)、long long(64位)
- 浮点类型：float(32位)、double(64位)、long double(80/128位)
- 字符类型：char(8位)、wchar_t(16/32位)、char16_t、char32_t
- 布尔类型：bool(true/false)，void类型：void*通用指针
- auto关键字（C++11）：编译器自动推导类型，auto x = 42;
- decltype：获取表达式类型，decltype(x + y) result;

## 运算符
- 算术运算符：+、-、*、/、%（整数取模）
- 赋值运算符：=、+=、-=、*=、/=、%=、&=、|=、^=、<<=、>>=
- 逻辑运算符：&&、||、!，支持短路求值
- 位运算符：&、|、^、~、<<、>>
- 作用域解析运算符：::，访问命名空间或类成员

## 控制流
- if-else、switch-case（支持枚举类型，必须break或fallthrough）
- for循环、range-based for（C++11）：for (auto& x : container)
- while、do-while、break、continue、return
- 结构化绑定（C++17）：auto [x, y] = std::make_pair(1, 2);
- constexpr if（C++17）：编译期条件分支

## 函数
- 函数声明与定义分离：头文件声明(.h)，源文件定义(.cpp)
- 默认参数：void f(int x = 10)，从右向左设置
- 函数重载：同名不同参数列表（类型或个数不同）
- 内联函数：inline关键字，建议编译器内联展开
- Lambda表达式（C++11）：[capture](params) -> return_type { body }

## 引用与指针
- 引用：int& ref = x，必须初始化，不能重新绑定，本质是别名
- 指针：int* p = &x，可以为空(nullptr)，可以重新指向
- const引用：const int& ref，防止通过引用修改值
- 右值引用（C++11）：int&& rv = 42，用于移动语义
- 智能指针（C++11）：unique_ptr、shared_ptr、weak_ptr
