# Laravel框架

## 核心架构
- 服务容器：IoC容器，自动解析依赖注入
- 服务提供者：注册绑定到容器，引导应用
- Facade：静态代理，简化服务容器调用
- 中间件：请求过滤层，认证、CORS、日志
- Artisan命令行：php artisan make:model/migrate/serve

## 路由与控制器
- 路由定义：Route::get('/users', [UserController::class, 'index'])
- 路由参数：Route::get('/user/{id}')，Route Model Binding
- 路由分组：中间件、前缀、命名空间
- 资源路由：Route::resource('posts', PostController::class)
- 控制器：__invoke单动作控制器，API资源控制器

## Eloquent ORM
- 模型定义：class User extends Model { protected $fillable = []; }
- CRUD：User::create()、User::find()、$user->update()、$user->delete()
- 关联：hasOne、hasMany、belongsTo、belongsToMany、hasManyThrough
- 查询构建器：where、orderBy、groupBy、join、select
- 软删除：SoftDeletes trait，不实际删除数据

## Blade模板
- 模板继承：@extends('layout')、@yield('content')
- 组件：@component('alert')、slot插槽
- 条件与循环：@if/@elseif/@else、@foreach/@forelse
- 表单：@csrf、@method('PUT')
- Include：@include('partials.nav')

## API开发
- API路由：Route::apiResource()，自动排除create/edit路由
- API Resource：UserResource格式化响应数据
- 分页：->paginate()，自动生成分页链接
- 认证：Sanctum（SPA/Token）、Passport（OAuth2）
- 速率限制：Throttle中间件，RateLimiter门面
