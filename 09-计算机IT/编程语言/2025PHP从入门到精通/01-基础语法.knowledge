# 基础语法

## 变量与类型
- 变量：$前缀，如 $name = "PHP"，弱类型语言
- 标量类型：int、float、string、bool
- 复合类型：array、object、callable、iterable
- 特殊类型：null、resource
- 类型声明：function add(int $a, int $b): int {}

## 字符串
- 单引号：不解析变量和转义（仅\和\'）
- 双引号：解析变量 "$name" 和转义字符
- Heredoc：<<<EOT ... EOT，类似双引号
- Nowdoc：<<<'EOT' ... EOT，类似单引号
- 常用函数：strlen、strpos、substr、str_replace、explode、implode

## 数组
- 索引数组：$arr = [1, 2, 3] 或 array(1, 2, 3)
- 关联数组：$arr = ["name" => "Tom", "age" => 18]
- 数组函数：array_push、array_pop、array_merge、array_map、array_filter
- 数组遍历：foreach ($arr as $key => $value) {}
- 数组解构：[$a, $b] = [1, 2]（PHP 7.1+）

## 流程控制
- if-elseif-else、switch-case（使用===比较）
- for、foreach、while、do-while
- match表达式（PHP 8.0+）：严格比较，返回值
- 三元运算符、??（空合并运算符）、<=>（太空船运算符）
- match vs switch：match使用===严格比较，必须穷尽

## 函数
- 函数定义：function add(int $a, int $b): int { return $a + $b; }
- 默认参数：function f($x = 10) {}
- 可变参数：function f(...$args) {}
- 匿名函数：$fn = function($x) { return $x * 2; };
- 箭头函数（PHP 7.4+）：$fn = fn($x) => $x * 2;，自动捕获外部变量
- 命名参数（PHP 8.0+）：f(name: "Tom", age: 18)
