# 面向对象

## 类与对象
- 类定义：class Person { public $name; public function __construct($name) {} }
- 对象创建：$p = new Person("Tom")
- 访问控制：public、protected、private
- 属性类型声明（PHP 7.4+）：public string $name;
- 构造器：__construct()，支持参数提升（PHP 8.0+）

## 继承与多态
- 继承：class Child extends Parent {}
- 方法重写：子类定义同名方法覆盖父类
- parent::：调用父类方法
- 抽象类：abstract class，可有抽象方法和具体方法
- 接口：interface定义，implements实现，支持多实现

## Trait
- trait：代码复用机制，解决PHP单继承限制
- use TraitName：在类中使用trait
- 多个trait：use Trait1, Trait2;
- 冲突解决：insteadof（选择哪个）和as（起别名）
- trait可定义属性、方法、抽象方法

## 魔术方法
- __construct/__destruct：构造器/析构器
- __get/__set：属性重载，动态属性访问
- __call/__callStatic：方法重载，调用不存在的方法
- __toString：对象转字符串
- __invoke：对象当作函数调用

## PHP 8新特性
- 命名参数：f(name: "Tom", age: 18)
- 联合类型：function f(int|string $x) {}
- Match表达式：$result = match($x) { 1 => "one", default => "other" };
- 属性（Attributes）：#[Route("/api")]，替代注解
- Fibers（PHP 8.1+）：协程原语，用于异步编程
