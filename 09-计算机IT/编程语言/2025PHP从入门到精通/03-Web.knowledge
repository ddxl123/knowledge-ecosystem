# Web开发

## HTTP基础
- $_GET/$_POST/$_REQUEST：获取请求参数
- $_SERVER：服务器和执行环境信息
- header()：发送HTTP响应头
- setcookie()：设置Cookie
- session_start() / $_SESSION：会话管理

## 表单处理
- 表单提交：method="GET/POST"，enctype="multipart/form-data"
- 数据过滤：filter_input()、filter_var()、FILTER_SANITIZE_*
- CSRF防护：Token验证
- 文件上传：$_FILES、move_uploaded_file()
- 数据验证：正则表达式、filter_var验证过滤器

## 数据库操作
- PDO：PHP数据对象，统一数据库接口
- 预处理语句：$stmt = $pdo->prepare("SELECT * FROM users WHERE id = ?")
- 参数绑定：$stmt->execute([$id])，防止SQL注入
- 事务：beginTransaction()、commit()、rollBack()
- ORM：Doctrine、Eloquent（Laravel内置）

## 会话与认证
- Session：session_start()，$_SESSION读写，session_destroy()
- Cookie：setcookie()、$_COOKIE读取
- JWT：json web token无状态认证
- OAuth2.0：授权码流程、密码模式
- 密码处理：password_hash()、password_verify()

## 模板与视图
- 原生PHP模板：<?php include 'header.php'; ?>
- Twig：Symfony模板引擎，自动转义、模板继承
- Blade：Laravel模板引擎，@if/@foreach/@section
- 模板继承：@extends + @yield + @section
- 组件化：可复用的UI组件
