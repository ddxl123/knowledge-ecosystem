# 面向对象


## 收集指南

> ⚠️ **严格范围限定：** 仅收集"2025黑马程序员Python教程"中"面向对象"的内容，禁止跨文件、跨目录引用。若本文件为某章节，则只收集该章节内容。

### 条目描述
本文件是《2025黑马程序员Python教程》中关于「面向对象」的知识原子文件，属于编程语言方向。

### 知识结构
- 面向对象概述
- 类的定义和使用
- 封装
- 继承
- 多态
- 特殊方法（魔术方法）
- 类方法和静态方法
- 属性和方法的访问

### 待收集原子知识点
- 面向对象概述：核心概念、关键术语、原理机制、实践用法
- 类的定义和使用：核心概念、关键术语、原理机制、实践用法
- 封装：核心概念、关键术语、原理机制、实践用法
- 继承：核心概念、关键术语、原理机制、实践用法
- 多态：核心概念、关键术语、原理机制、实践用法
- 特殊方法（魔术方法）：核心概念、关键术语、原理机制、实践用法

## 面向对象概述

- 面向对象编程（OOP）的基本概念
- 面向对象的三大特征：封装、继承、多态
- 类和对象的关系：类是对象的模板，对象是类的实例

## 类的定义和使用

- 类的定义：
```python
class 类名:
    def __init__(self, 参数):
        self.属性 = 参数
    
    def 方法名(self):
        方法体
```
- 对象的创建：对象名 = 类名(参数)
- __init__方法：构造方法，创建对象时自动调用
- self参数：代表当前实例对象

## 封装

- 封装的定义：将属性和方法封装在类中
- 访问控制：
  - 公有属性：直接访问
  - 私有属性：以双下划线__开头，外部不能直接访问
  - 受保护属性：以单下划线_开头，约定为受保护
- 属性的getter和setter方法
- @property装饰器：将方法变成属性访问

## 继承

- 继承的定义：子类继承父类的属性和方法
- 继承的语法：
```python
class 子类名(父类名):
    pass
```
- 方法重写（Override）：子类重新定义父类的方法
- super()函数：调用父类的方法
- 多继承：一个类可以继承多个父类
- MRO（方法解析顺序）：决定多继承时方法的查找顺序

## 多态

- 多态的定义：同一个方法在不同类中有不同的实现
- 多态的条件：有继承关系、子类重写父类方法、父类引用指向子类对象
- Python的鸭子类型：不关注对象类型，只关注对象行为

## 特殊方法（魔术方法）

- __init__：构造方法
- __str__：定义对象的字符串表示（print时调用）
- __repr__：定义对象的官方字符串表示
- __len__：定义len()函数的行为
- __getitem__：定义索引操作
- __setitem__：定义赋值操作
- __del__：析构方法
- __call__：使对象可调用
- __eq__、__lt__、__gt__：比较操作
- __add__、__sub__：算术操作

## 类方法和静态方法

- 实例方法：第一个参数为self，操作实例属性
- 类方法：@classmethod装饰器，第一个参数为cls，操作类属性
- 静态方法：@staticmethod装饰器，无默认参数，独立于实例和类

## 属性和方法的访问

- 实例属性：每个实例独有的属性
- 类属性：所有实例共享的属性
- 实例方法：操作实例属性的方法
- 类方法：操作类属性的方法
- 静态方法：与类相关但不操作类或实例属性的方法
