# SQL注入

## 注入原理
- 用户输入未过滤直接拼接到SQL语句
- 攻击者构造恶意输入改变SQL语义
- 危害：数据泄露、数据篡改、身份绕过、远程命令执行
- 经典示例：' OR 1=1 -- 绕过登录

## 注入类型
- 联合注入：UNION SELECT获取其他表数据
- 报错注入：利用数据库报错信息回显数据
- 布尔盲注：通过页面返回差异判断条件真假
- 时间盲注：通过页面响应时间判断条件真假
- 堆叠注入：执行多条SQL语句（分号分隔）

## 数据库特性
- MySQL：information_schema、LIMIT、GROUP_CONCAT
- PostgreSQL：pg_catalog、字符串连接||
- SQL Server：sysobjects、TOP、WAITFOR DELAY
- Oracle：ALL_TABLES、ROWNUM、ROWID
- SQLite：sqlite_master

## 防御措施
- 参数化查询（预编译语句）：最有效防御
- ORM框架：MyBatis #{}、Hibernate参数绑定
- 输入验证：白名单过滤，限制输入格式
- 最小权限：数据库用户只授予必要权限
- WAF：Web应用防火墙拦截恶意请求

## SQLMap工具
- 自动化SQL注入检测和利用
- sqlmap -u "url?id=1" --dbs：获取数据库列表
- --tables、--dump：获取表结构和数据
- --os-shell：获取操作系统shell
- 绕过WAF：tamper脚本、随机UA、延迟
