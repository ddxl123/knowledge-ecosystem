# 文件上传漏洞

## 漏洞原理
- 上传功能未严格验证文件类型和内容
- 攻击者上传WebShell（如.php、.jsp）获取服务器控制权
- 危害：远程代码执行、服务器沦陷、内网渗透
- 常见场景：头像上传、文件管理、导入功能

## 绕过技术
- 前端验证绕过：修改请求包，删除JS验证
- MIME类型绕过：修改Content-Type为image/jpeg
- 后缀名绕过：.php5、.phtml、.php.jpg（Apache解析漏洞）
- 大小写绕过：.PhP、.PHP
- 双写绕过：.pphphp（过滤后变成.php）
- %00截断：文件名截断（旧版本PHP）

## 防御措施
- 白名单验证：只允许特定后缀（.jpg、.png、.pdf）
- 文件类型检测：读取文件头（Magic Number）判断真实类型
- 重命名文件：随机文件名+正确后缀
- 存储隔离：上传目录不解析脚本
- 文件大小限制：防止DoS
- 病毒扫描：上传前扫描恶意代码

## 上传目录安全
- 禁止脚本执行：Nginx/Apache配置禁止上传目录执行脚本
- 独立存储：上传文件存储在Web根目录之外
- CDN存储：阿里云OSS、AWS S3
- 权限设置：上传目录只读权限
- 目录不可浏览：禁止目录列表

## 二次渲染
- 图片压缩/缩放后存储，破坏嵌入的恶意代码
- ImageMagick处理：convert重绘图片
- 文件内容检查：检测文件中是否包含恶意代码
- 沙箱检测：在隔离环境中执行文件检测行为
