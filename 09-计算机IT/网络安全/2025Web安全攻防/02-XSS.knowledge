# XSS跨站脚本攻击

## XSS类型
- 反射型XSS：恶意代码在URL中，服务器返回时执行
- 存储型XSS：恶意代码存储在数据库，用户访问时执行
- DOM型XSS：前端JavaScript直接操作DOM触发
- 危害：窃取Cookie、会话劫持、钓鱼、键盘记录

## 攻击向量
- script标签：<script>alert(1)</script>
- 事件属性：<img src=x onerror=alert(1)>
- 伪协议：javascript:alert(1)
- SVG：<svg onload=alert(1)>
- CSS表达式（旧浏览器）

## 攻击场景
- Cookie窃取：document.cookie发送到攻击者服务器
- 会话劫持：利用Cookie模拟用户登录
- 钓鱼攻击：修改页面内容诱导用户输入敏感信息
- 键盘记录：addEventListener捕获按键
- 内网探测：利用浏览器发起内网请求

## 防御措施
- 输出编码：HTML实体编码、JavaScript编码、URL编码
- CSP（Content-Security-Policy）：限制脚本来源
- HttpOnly Cookie：禁止JavaScript访问Cookie
- 输入验证：白名单允许的HTML标签和属性
- DOMPurify：前端HTML消毒库

## CSP配置
- Content-Security-Policy: script-src 'self'
- nonce：动态生成的随机数，允许特定脚本
- strict-dynamic：信任由受信任脚本加载的脚本
- 报告模式：report-uri接收违规报告
- 逐步部署：Content-Security-Policy-Report-Only
