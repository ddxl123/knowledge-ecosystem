# CSRF跨站请求伪造

## 攻击原理
- 用户已登录目标网站，浏览器携带Cookie
- 诱导用户访问恶意页面，自动发起请求
- 浏览器自动附加Cookie，请求被信任
- 用户在不知情的情况下执行操作

## 攻击方式
- GET型：img标签自动发起GET请求
- POST型：隐藏表单自动提交
- 链接诱导：点击触发请求
- JSON劫持：利用script标签跨域读取JSON
- CORS误配置：允许任意源访问

## 防御措施
- CSRF Token：服务端生成随机Token，表单提交时验证
- SameSite Cookie：Strict/Lax/None限制跨站发送
- 验证Referer/Origin头：检查请求来源
- 自定义请求头：X-Requested-With
- 双重Cookie验证：请求参数中携带Cookie值

## SameSite Cookie
- Strict：完全禁止跨站发送Cookie
- Lax：GET请求允许跨站发送（默认）
- None：允许跨站发送（必须配合Secure）
- 浏览器默认：Chrome默认Lax
- 兼容性：现代浏览器普遍支持

## Token机制实现
- 服务端生成随机Token存入Session
- 表单中嵌入隐藏字段<input type="hidden" name="_token" value="xxx">
- AJAX请求：X-CSRF-Token请求头
- 双重提交：Cookie+请求参数中的Token比对
- Token一次性：每次请求后刷新Token
