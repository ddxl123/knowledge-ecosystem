# 安全防御体系

## 核心知识点

### 一、HTTPS配置

```nginx
# Nginx SSL配置
server {
    listen 443 ssl http2;
    server_name example.com;

    ssl_certificate /etc/letsencrypt/live/example.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/example.com/privkey.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256;
    ssl_prefer_server_ciphers off;

    # HSTS
    add_header Strict-Transport-Security "max-age=63072000" always;

    # HTTP重定向
    return 301 https://$host$request_uri;
}
```

### 二、安全头部

```nginx
add_header X-Content-Type-Options "nosniff" always;
add_header X-Frame-Options "SAMEORIGIN" always;
add_header X-XSS-Protection "1; mode=block" always;
add_header Referrer-Policy "strict-origin-when-cross-origin" always;
add_header Permissions-Policy "camera=(), microphone=(), geolocation=()" always;
add_header Content-Security-Policy "default-src 'self'; script-src 'self'" always;
```

### 三、WAF配置

```python
# ModSecurity规则示例
SecRule REQUEST_HEADERS:Content-Type "text/xml" \
    "id:'200000',phase:1,t:none,t:lowercase,deny,status:403"

# 防SQL注入
SecRule ARGS "@detectSQLi" \
    "id:'942100',phase:2,deny,status:403,msg:'SQL Injection Attack'"

# 防XSS
SecRule ARGS "@detectXSS" \
    "id:'941100',phase:2,deny,status:403,msg:'XSS Attack'"
```

### 四、代码审计

```python
# 危险函数检查清单
dangerous_patterns = {
    'python': [
        'eval(', 'exec(', 'os.system(', 'subprocess.call(',
        'pickle.loads(', 'yaml.load('
    ],
    'php': [
        'eval(', 'system(', 'exec(', 'shell_exec(',
        'unserialize(', 'include($_GET'
    ],
    'javascript': [
        'eval(', 'innerHTML', 'document.write(',
        'Function(', 'setTimeout(string'
    ]
}
```

### 五、安全开发流程

```yaml
# 安全检查清单
development:
  - 输入验证
  - 输出编码
  - 参数化查询
  - 最小权限原则
  - 错误处理（不暴露敏感信息）

deployment:
  - HTTPS强制
  - 安全头部
  - CSP策略
  - 依赖更新
  - 日志监控

monitoring:
  - WAF日志
  - 异常请求检测
  - 入侵检测系统
  - 定期漏洞扫描
  - 安全审计
```

### 六、日志与监控

```python
# 安全日志记录
import logging

security_logger = logging.getLogger('security')

def log_attack(request, attack_type, details):
    security_logger.warning({
        'type': attack_type,
        'ip': request.remote_addr,
        'url': request.url,
        'user_agent': request.user_agent.string,
        'details': details,
        'timestamp': datetime.utcnow().isoformat()
    })

# 异常检测
def detect_brute_force(ip, threshold=10, window=300):
    """检测暴力破解：5分钟内失败超过10次"""
    key = f"login_fail:{ip}"
    count = redis.incr(key)
    if count == 1:
        redis.expire(key, window)
    return count >= threshold
```
