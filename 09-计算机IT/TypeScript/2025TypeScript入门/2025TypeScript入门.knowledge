# 2025TypeScript入门
# 领域: 计算机IT
# 学科: TypeScript
# 来源: 2025TypeScript入门
# 描述: TypeScript核心知识点，涵盖类型系统、接口、泛型、装饰器、模块系统、高级类型等

{{TypeScript是什么}}{{TypeScript是由微软开发的开源编程语言，是JavaScript的超集，添加了可选的静态类型和基于类的面向对象编程。TypeScript代码需要编译为JavaScript才能在浏览器或Node.js中运行。2025年TypeScript已发展到5.x版本，成为前端和全栈开发的主流选择。}}

{{为什么使用TypeScript}}{{TypeScript提供静态类型检查，能在编译阶段发现错误，减少运行时bug。它增强了IDE的智能提示和重构能力，提升了大型项目的可维护性。TypeScript支持最新的ECMAScript特性，并通过类型系统提供更好的代码文档化。}}

{{安装TypeScript}}{{通过npm全局安装：npm install -g typescript。使用tsc命令编译.ts文件为.js文件。通过tsconfig.json配置编译选项。项目中通常作为开发依赖安装：npm install -D typescript。2025年推荐使用tsconfig.json的extends继承共享配置。}}

{{基本类型 - string、number、boolean}}{{TypeScript的基本类型包括string（字符串）、number（数字）、boolean（布尔值）。声明方式：let name: string = "Alice"、let age: number = 25、let isActive: boolean = true。这些是TypeScript类型系统的基础构建块。}}

{{类型推断}}{{TypeScript具有强大的类型推断能力。当变量初始化时，编译器会自动推断其类型，无需显式标注。例如let x = 10会被推断为number类型。但在函数参数和复杂场景中，显式类型标注仍是推荐做法。}}

{{any类型与unknown类型}}{{any类型表示任意类型，关闭类型检查，应尽量避免使用。unknown类型是类型安全的any，使用前必须进行类型检查或类型断言。推荐用unknown替代any，保持类型安全性。}}

{{数组类型}}{{数组类型有两种声明方式：let nums: number[] = [1,2,3]或let nums: Array<number> = [1,2,3]。TypeScript支持只读数组ReadonlyArray<T>，防止数组被修改。元组Tuple允许固定长度和类型的数组：[string, number]。}}

{{元组类型}}{{元组是固定长度和类型的数组。声明方式：let tuple: [string, number] = ["hello", 10]。可选元组元素使用?:标注。具名元组提升可读性：type User = [name: string, age: number]。剩余元素使用...语法：[string, ...number[]]。}}

{{枚举类型}}{{枚举用enum关键字定义，分为数字枚举和字符串枚举。数字枚举默认从0递增：enum Direction { Up, Down }。字符串枚举每个成员必须初始化：enum Color { Red = "RED" }。const枚举在编译时内联，不生成额外代码。2025年推荐使用as const对象替代枚举。}}

{{联合类型}}{{联合类型表示一个值可以是多种类型之一，使用|分隔。例如：let id: string | number。使用联合类型时需要通过类型缩窄来安全访问特定类型的方法。Type守卫（typeof、instanceof、in）用于在运行时区分联合类型的成员。}}

{{类型别名 type}}{{type关键字定义类型别名，为类型创建新名称。type ID = string | number。支持对象类型：type Point = { x: number; y: number }。支持交叉类型：type AB = A & B。type可以定义联合类型、元组类型等复杂类型，但不能被extends或implements。}}

{{接口 interface}}{{interface定义对象的结构契约。interface User { name: string; age: number; }。可选属性使用?:标记，只读属性使用readonly标记。接口支持继承：interface Admin extends User { role: string; }。2025年推荐优先使用interface定义对象类型，type用于联合类型和复杂类型操作。}}

{{type与interface的区别}}{{interface可以被extends继承和implements实现，支持声明合并（同名interface自动合并）。type不能被继承，但支持联合类型、交叉类型、映射类型等高级类型操作。对象类型优先用interface，其他场景用type。两者在对象类型上可以互换。}}

{{函数类型}}{{函数类型定义参数和返回值类型。function add(a: number, b: number): number { return a+b; }。函数类型别名：type Fn = (x: number) => string。可选参数用?:，默认参数用=赋值。剩余参数用...args: number[]。函数重载通过多个签名声明实现。}}

{{函数重载}}{{函数重载允许一个函数有多个调用签名。先声明重载签名，再实现兼容所有签名的实现签名。例如：function parse(input: string): object; function parse(input: Buffer): object; function parse(input: string | Buffer): object { ... }。TypeScript根据参数类型选择正确的重载。}}

{{类型断言}}{{类型断言告诉编译器"我知道这个值的类型"。两种语法：let len = (someValue as string).length或let len = (<string>someValue).length。推荐使用as语法，兼容JSX。非空断言!告诉编译器值不是null/undefined：el!.focus()。应尽量用类型守卫替代断言。}}

{{类 class}}{{TypeScript类支持public、private、protected访问修饰符。public成员在任何地方可访问，private仅类内部，protected子类也可访问。readonly属性只能在构造函数中赋值。参数属性简写：constructor(public name: string)自动创建并赋值属性。}}

{{抽象类}}{{abstract class定义不能直接实例化的基类。抽象方法用abstract关键字声明，没有实现，子类必须实现。抽象类可以包含具体实现和属性。适用于定义通用接口和模板方法模式。interface定义契约，abstract class定义部分实现的基类。}}

{{泛型基础}}{{泛型允许创建可复用的组件，支持多种类型。function identity<T>(arg: T): T { return arg; }。泛型可约束类型：T extends HasLength。多个泛型参数：<T, U>。泛型接口：interface Box<T> { value: T; }。泛型类：class Stack<T> { ... }。}}

{{泛型约束}}{{通过extends关键字约束泛型类型。interface HasLength { length: number; } function log<T extends HasLength>(arg: T): void。keyof约束：function get<T, K extends keyof T>(obj: T, key: K): T[K]。多重约束使用交叉类型：T extends A & B。}}

{{条件类型}}{{条件类型根据类型关系选择不同类型：T extends U ? X : Y。分布式条件类型在联合类型上自动分发。结合infer关键字可以在条件类型中提取类型。例如：type ReturnType<T> = T extends (...args: any[]) => infer R ? R : any。}}

{{infer关键字}}{{infer在条件类型的extends子句中声明待推断的类型变量。用于提取函数返回类型：type Return<T> = T extends (...args: any[]) => infer R ? R : never。提取元组第一个元素：type Head<T> = T extends [infer H, ...any[]] ? H : never。提取Promise内层类型：type Awaited<T> = T extends Promise<infer U> ? Awaited<U> : T。}}

{{映射类型}}{{映射类型基于已有类型创建新类型。{ [K in keyof T]: T[K] }遍历T的所有属性。内置映射类型：Partial<T>（所有属性可选）、Required<T>（所有属性必选）、Readonly<T>（所有属性只读）、Record<K,V>（键值映射）。可通过+/-修饰符控制readonly和optional。}}

{{模板字面量类型}}{{模板字面量类型在类型层面操作字符串。type Greeting = `hello ${string}`。支持联合类型分发：type Color = "red" | "blue"; type Msg = `${Color}_msg`。内置字符串操作类型：Uppercase<T>、Lowercase<T>、Capitalize<T>、Uncapitalize<T>。}}

{{索引类型与keyof}}{{keyof T获取类型T的所有属性名组成的联合类型。索引访问T[K]获取属性K的类型。配合泛型实现类型安全的属性访问：function getProp<T, K extends keyof T>(obj: T, key: K): T[K]。可变字符串索引签名：{ [key: string]: number }。}}

{{类型守卫 typeof、instanceof}}{{类型守卫在运行时缩窄类型。typeof检查基本类型：if (typeof x === "string")。instanceof检查类实例：if (x instanceof Date)。in操作符检查属性存在：if ("name" in obj)。自定义类型守卫使用类型谓词：function isString(x: any): x is string。}}

{{类型谓词（自定义类型守卫）}}{{类型谓词函数返回类型标注为param is Type的形式。function isString(val: unknown): val is string { return typeof val === "string"; }。调用后TypeScript自动缩窄类型。广泛用于数据验证、API响应类型判断等场景。}}

{{可辨识联合}}{{可辨识联合通过共同的字面量类型属性区分联合成员。interface Circle { kind: "circle"; radius: number; } interface Square { kind: "square"; side: number; } type Shape = Circle | Square; 通过switch(shape.kind)进行穷尽检查。配合never类型确保处理所有情况。}}

{{模块系统 - ES Modules}}{{TypeScript使用ES Module语法：import/export。export导出声明，export default导出默认值。import { fn } from "./module"具名导入，import fn from "./module"默认导入。支持export * as ns from "./module"重导出。2025年ES Modules是标准模块系统。}}

{{命名空间 namespace}}{{命名空间namespace用于组织代码，避免全局污染。namespace Utils { export function log() {} }。访问：Utils.log()。现代TypeScript中已被ES Modules取代，但在类型声明（.d.ts）中仍有使用价值。2025年新项目不推荐使用namespace。}}

{{声明文件 .d.ts}}{{.d.ts文件为JavaScript库提供类型信息。declare声明不生成代码，只描述类型。declare function jQuery(selector: string): JQuery;。@types/*包提供第三方库的类型声明。declare module "lib"扩展无类型模块。通过declare global扩展全局类型。}}

{{装饰器基础}}{{装饰器是一种特殊的声明，可以附加到类声明、方法、属性、参数上。使用@expression形式。类装饰器接收构造函数参数。方法装饰器接收(target, key, descriptor)参数。2025年装饰器已进入TC39 Stage 3，语法为标准JavaScript装饰器。}}

{{装饰器类型}}{{五种装饰器：类装饰器（Class Decorator）、方法装饰器（Method Decorator）、属性装饰器（Property Decorator）、参数装饰器（Parameter Decorator）、访问器装饰器（Accessor Decorator）。每种接收不同参数，可返回修改后的描述符。实验性装饰器需开启experimentalDecorators选项。}}

{{实用类型 Utility Types}}{{TypeScript内置实用类型：Partial<T>（可选属性）、Required<T>（必选属性）、Readonly<T>（只读）、Pick<T,K>（选取部分属性）、Omit<T,K>（排除部分属性）、Record<K,V>（键值映射）、Exclude<T,U>（排除联合成员）、Extract<T,U>（提取联合成员）、NonNullable<T>（排除null/undefined）、ReturnType<T>（获取函数返回类型）、Parameters<T>（获取函数参数类型）、Awaited<T>（解包Promise类型）。}}

{{tsconfig.json配置}}{{tsconfig.json是TypeScript项目配置文件。compilerOptions常用选项：target（编译目标ES版本）、module（模块系统）、strict（严格模式开关）、esModuleInterop（ES模块互操作）、outDir（输出目录）、rootDir（根目录）、declaration（生成.d.ts）、sourceMap（生成源映射）。include/exclude控制编译文件范围。}}

{{strict模式}}{{strict模式启用所有严格类型检查选项。包括strictNullChecks（null检查）、noImplicitAny（禁止隐式any）、strictFunctionTypes（函数参数双变检查）、strictBindCallApply、strictPropertyInitialization、noImplicitThis、alwaysStrict。2025年新项目强烈建议开启strict: true。}}

{{空值处理 null和undefined}}{{TypeScript中null和undefined有各自的类型。strictNullChecks开启后，可选类型变为T | undefined。空值合并运算符??提供默认值：let x = val ?? "default"。可选链?.安全访问：obj?.prop?.method()。非空断言!跳过null检查。}}

{{类型缩窄（Narrowing）}}{{类型缩窄是TypeScript在特定代码路径中自动收窄类型的能力。通过typeof、instanceof、in、相等性检查、真值检查等方式实现。控制流分析自动跟踪类型变化。自定义类型守卫函数扩展缩窄能力。是编写类型安全代码的核心机制。}}

{{交叉类型 &}}{{交叉类型将多个类型合并为一个类型，使用&连接。type AB = A & B，AB同时拥有A和B的所有属性。常用于mixins模式和组合类型。如果交叉的类型有同名但不兼容的属性，该属性类型变为never。}}

{{协变与逆变}}{{协变：子类型关系保持方向（返回值类型）。逆变：子类型关系反转（函数参数类型）。TypeScript的函数参数默认双向协变（--strictFunctionTypes开启后为逆变）。理解协变逆变对设计泛型接口和回调类型至关重要。}}

{{模块解析策略}}{{TypeScript有两种模块解析策略：classic（旧式，相对路径查找）和node（模拟Node.js查找逻辑）。默认使用node策略。paths配置路径别名：@components/*映射到src/components/*。baseUrl设置基础路径。模块解析影响import语句如何找到目标文件。}}
