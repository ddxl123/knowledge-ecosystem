# 进程管理

## 进程基础
- 进程：程序的运行实例，有独立的PID和内存空间
- 父子进程：fork创建子进程，PPID标识父进程
- 进程状态：R运行、S睡眠、D不可中断、Z僵尸、T停止
- 前台/后台：命令后加&放入后台，jobs查看后台任务
- 守护进程：后台运行的系统服务进程

## 进程查看
- ps aux：查看所有进程详细信息
- top/htop：实时监控进程和系统资源
- pstree：进程树
- pidof：查找进程PID
- /proc/[PID]：进程详细信息

## 进程控制
- kill：发送信号，kill -9 PID强制终止
- 常用信号：SIGTERM(15)优雅终止、SIGKILL(9)强制、SIGHUP(1)重载配置
- nohup：忽略挂断信号，终端关闭后继续运行
- bg/fg：后台/前台切换
- Ctrl+Z：暂停前台进程

## 计划任务
- crontab：定时任务，分 时 日 月 周 命令
- crontab -e：编辑定时任务
- crontab -l：查看定时任务
- at：一次性计划任务
- systemd timer：systemd定时器

## 系统资源监控
- vmstat：虚拟内存统计
- iostat：IO统计
- free：内存使用
- df：磁盘空间
- du：目录大小
- sar：系统活动报告
