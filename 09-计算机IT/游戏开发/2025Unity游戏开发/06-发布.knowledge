# Unity发布与优化

## 构建设置
- Build Settings：选择目标平台（PC/Mobile/WebGL/Console）
- Player Settings：公司名、产品名、版本号、图标
- 质量设置：画质等级、阴影、抗锯齿
- 分辨率：全屏/窗口、分辨率列表
- 图标和启动画面：自定义应用图标和启动页

## 移动平台发布
- Android：需要Android SDK、JDK、Gradle
- iOS：需要Xcode、Apple开发者账号
- APK/AAB：Android安装包格式
- App Store提交：审核流程、隐私政策
- 签名：Android Keystore、iOS证书

## 性能优化
- 批处理(Batching)：合并Draw Call，减少渲染开销
- 图集(Atlas)：合并小纹理减少Draw Call
- LOD(Level of Detail)：根据距离切换模型精度
- 遮挡剔除(Occlusion Culling)：不渲染被遮挡的物体
- 对象池：复用GameObject，减少Instantiate/Destroy

## 内存优化
- 纹理压缩：ASTC、ETC2、PVRTC
- 资源卸载：Resources.UnloadUnusedAssets()
- AssetBundle：按需加载资源
- 异步加载：SceneManager.LoadSceneAsync
- Profiler：分析内存使用

## 发布后维护
- 崩溃报告：Unity Cloud Diagnostics、Firebase Crashlytics
- 热更新：Addressable Assets远程更新
- 数据分析：Unity Analytics、自建数据埋点
- A/B测试：不同版本效果对比
- 版本管理：语义化版本号
