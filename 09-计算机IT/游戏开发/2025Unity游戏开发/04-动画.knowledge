# Unity动画系统

## Animator组件
- Animator Controller：状态机，管理动画状态和过渡
- 状态(State)：每个动画片段对应一个状态
- 过渡(Transition)：状态之间的切换条件
- 参数(Parameter)：Bool/Int/Float/Trigger控制过渡
- 混合树(Blend Tree)：多个动画平滑混合

## 动画片段(Animation Clip)
- 关键帧动画：在时间线上设置关键帧
- 曲线编辑器：调整动画曲线（缓入缓出）
- 动画事件(Animation Event)：在特定帧调用函数
- 循环设置：Loop Time控制动画是否循环
- 人形动画(Humanoid)：通用骨骼重定向

## 状态机设计
- Entry → 默认状态
- Any State：任何状态都可以切换到的目标状态
- Exit：退出状态机
- 子状态机(Sub-State Machine)：组织复杂动画
- 状态行为(State Behaviour)：状态进入/退出时执行脚本

## 动画混合
- 1D Blend Tree：一个参数混合两个以上动画
- 2D Blend Tree：两个参数混合动画（如移动方向+速度）
- 动画层(Layer)：多个动画层叠加（如上半身射击+下半身跑步）
- 遮罩(Avatar Mask)：控制动画层影响的身体部位
- 权重：层的混合权重

## IK与高级动画
- 反向动力学(IK)：根据末端位置计算关节角度
- 脚部IK：脚部自动贴合地面
- Look At IK：角色头部/眼睛看向目标
- 程序化动画：代码控制骨骼
- 有限状态机(FSM)：管理复杂角色行为
