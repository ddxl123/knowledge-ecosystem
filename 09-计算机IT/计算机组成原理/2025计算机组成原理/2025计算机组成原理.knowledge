# 2025计算机组成原理
# 领域: 计算机IT
# 学科: 计算机组成原理
# 来源: 2025计算机组成原理
# 描述: 计算机组成原理核心知识点

{{知识点1}}{{冯·诺依曼计算机的基本特点：存储程序、以运算器为中心。由运算器、控制器、存储器、输入设备和输出设备五大部分组成。指令和数据以二进制形式存储在同一存储器中，按地址访问。指令由操作码和地址码组成，顺序存放、顺序执行。}}

{{知识点2}}{{计算机系统层次结构：从下到上依次为——硬件逻辑层、微程序层、指令系统层、操作系统层、汇编语言层、高级语言层。每层向上层提供抽象接口，隐藏底层实现细节。}}

{{知识点3}}{{IEEE 754浮点数标准：单精度32位（1位符号+8位阶码+23位尾数），双精度64位（1位符号+11位阶码+52位尾数）。阶码采用移码表示，隐含前导1。特殊值：±0、±∞、NaN。}}

{{知识点4}}{{原码、反码、补码的转换：正数三码相同。负数原码→反码：符号位不变，数值位取反。负数反码→补码：加1。补码的优势在于加减法统一，零的表示唯一。8位补码表示范围：-128~+127。}}

{{知识点5}}{{定点数与浮点数：定点数小数点位置固定，分为定点整数和定点小数。浮点数表示为 N = M × 2^E，M为尾数，E为阶码。浮点数表示范围远大于定点数，但精度随数值增大而降低。}}

{{知识点6}}{{溢出检测方法：单符号位法——两个同号数相加结果符号位与之不同则溢出。双符号位法（变形补码）——00正/01上溢/10下溢/11负。进位判别法——最高数值位进位与符号位进位异或。}}

{{知识点7}}{{BCD码：用4位二进制表示1位十进制。8421码是最常用的有权BCD码，0~9对应0000~1001，1010~1111为非法码。余3码是无权码，对应8421码加3。2421码是另一种有权BCD码。}}

{{知识点8}}{{海明码校验：在数据位中插入k个校验位，满足 2^k ≥ n+k+1（n为数据位数）。校验位放在2的幂次位置。每个校验位覆盖特定数据位。可检测2位错、纠正1位错。SECDED码增加1位总校验位可检测2位错。}}

{{知识点9}}{{CRC循环冗余校验：将数据视为多项式，除以生成多项式，余数作为校验码附加在数据后。常用生成多项式如CRC-16、CRC-32。只能检错不能纠错。检错能力取决于生成多项式的选取。}}

{{知识点10}}{{ALU算术逻辑单元：执行算术运算（加减乘除）和逻辑运算（与或非异或）。核心是加法器。先行进位加法器（CLA）通过并行计算进位信号加速运算，将进位延迟从O(n)降低到O(log n)。}}

{{知识点11}}{{补码加减法运算：[X+Y]补 = [X]补 + [Y]补，[X-Y]补 = [X]补 + [-Y]补。[-Y]补的求法：将[Y]补连同符号位一起取反加1。加减法统一使用加法器实现，无需额外减法电路。}}

{{知识点12}}{{原码一位乘法：逐位检查乘数，为1则加被乘数，为0则加0，每次部分积右移一位。共n轮（n为乘数位数）。符号位单独处理（异或）。 Booth算法改进：减少加法次数，适用于补码乘法。}}

{{知识点13}}{{原码一位除法（恢复余数法）：余数左移后减除数，若余数为负则恢复余数（加回除数），商0；若余数为非负则商1。加减交替法（不恢复余数法）更高效：余数为负时不恢复，下一步加除数。}}

{{知识点14}}{{浮点数加减运算步骤：1.对阶（小阶向大阶看齐，尾数右移）；2.尾数加减运算；3.规格化处理（左规或右规）；4.舍入处理；5.溢出判断（阶码溢出）。对阶和规格化是关键步骤。}}

{{知识点15}}{{存储器层次结构：寄存器→高速缓存(Cache)→主存→外存。速度递减、容量递增、价格递减。局部性原理是层次结构的理论基础：时间局部性（最近访问的数据可能再次访问）和空间局部性（相邻地址可能被访问）。}}

{{知识点16}}{{SRAM与DRAM：SRAM用触发器存储，速度快、不需刷新、集成度低、成本高，用于Cache。DRAM用电容存储，需要定期刷新（通常64ms）、速度较慢、集成度高、成本低，用于主存。DRAM有异步和同步(SDRAM)之分。}}

{{知识点17}}{{ROM类型：掩模ROM（出厂固化）、PROM（一次可编程）、EPROM（紫外线擦除可重编程）、EEPROM（电擦除可重编程）、Flash（闪存，按块擦写）。闪存广泛用于SSD和U盘，读快写慢。}}

{{知识点18}}{{主存容量扩展：位扩展——增加数据位宽（芯片并联），地址线不变。字扩展——增加存储单元数（芯片串联），数据线不变。字位同时扩展——两者结合。片选信号由高位地址线译码产生。}}

{{知识点19}}{{Cache工作原理：利用局部性原理，将主存中活跃数据块复制到Cache。主存地址分为Tag（标记）、Index（索引）、Offset（块内偏移）。CPU访问时先查Cache，命中则直接读取，未命中则从主存调入。}}

{{知识点20}}{{Cache映射方式：直接映射——主存块只能映射到Cache的固定行，简单但冲突率高。全相联映射——主存块可映射到任意Cache行，灵活但比较电路多。组相联映射——折中方案，组间直接映射、组内全相联。}}

{{知识点21}}{{Cache替换算法：FIFO（先进先出）——替换最早进入的块，可能替换频繁使用的块。LRU（最近最少使用）——替换最久未被访问的块，命中率高但实现复杂。随机替换——实现最简单，性能接近LRU。}}

{{知识点22}}{{Cache写策略：写直达（Write Through）——同时写Cache和主存，一致性好但速度慢。写回（Write Back）——只写Cache，被替换时才写回主存，速度快但一致性维护复杂。写分配与非写分配决定未命中时的行为。}}

{{知识点23}}{{虚拟存储器：由主存和外存构成，给程序提供远大于物理内存的逻辑地址空间。页式虚拟存储——逻辑地址空间分页，物理地址空间分帧，通过页表映射。缺页中断——访问的页面不在主存时触发，从外存调入。}}

{{知识点24}}{{TLB快表：页表的高速缓存，存储最近使用的页表项。CPU访存时先查TLB，命中则直接获得物理地址；未命中则查页表并更新TLB。TLB命中率通常很高（>99%），极大减少页表访问开销。}}

{{知识点25}}{{指令格式：操作码字段指定操作类型，地址码字段指定操作数来源。按地址码数量分为：三地址指令、二地址指令、一地址指令、零地址指令。操作码长度可固定或可变（扩展操作码技术）。}}

{{知识点26}}{{寻址方式：立即寻址——操作数在指令中。直接寻址——地址码为操作数地址。间接寻址——地址码指向的存储单元存放操作数地址。寄存器寻址——操作数在寄存器中。寄存器间接寻址——寄存器中存放操作数地址。基址/变址/相对寻址用于程序重定位和数组访问。}}

{{知识点27}}{{CISC与RISC：CISC指令系统复杂、指令长度不等、指令数目多、微程序控制、访存指令多。RISC指令系统精简、指令长度固定、指令数目少、硬布线控制、只有Load/Store访存。RISC更利于流水线实现。}}

{{知识点28}}{{CPU的基本功能：指令控制——保证按序取指和执行。操作控制——产生各操作的控制信号。时间控制——对操作定时。数据加工——完成算术逻辑运算。中断处理——响应和处理异常/中断。}}

{{知识点29}}{{CPU数据通路：由ALU、寄存器组、多路选择器、总线等组成。单总线数据通路——简单但速度慢（每条指令需多个时钟周期）。专用数据通路——速度快但硬件复杂。现代CPU多采用多总线或交叉开关方案。}}

{{知识点30}}{{硬布线控制器：由组合逻辑电路直接产生控制信号。速度快但设计复杂、修改困难。控制信号是指令操作码、时序信号和状态标志的函数。适合RISC处理器，因为指令格式规整、控制逻辑较简单。}}

{{知识点31}}{{微程序控制器：将每条机器指令的执行过程编写为一段微程序，存放在控制存储器（CM）中。微指令包含控制字段和下地址字段。执行指令时依次读出微指令，产生控制信号。灵活易修改但速度较慢。}}

{{知识点32}}{{流水线基本原理：将指令执行分为多个阶段（IF取指、ID译码、EX执行、MEM访存、WB写回），各阶段并行工作。理想情况下n级流水线加速比趋近n。吞吐率 = n/(k+n-1)×t，k为流水线级数。}}

{{知识点33}}{{流水线冒险：结构冒险——硬件资源冲突（如单端口存储器同时取指和访存）。数据冒险——后续指令依赖前序指令结果（RAW/WAR/WAW）。控制冒险——分支指令改变程序流，导致已进入流水线的指令无效。}}

{{知识点34}}{{数据冒险解决方案：数据旁路/转发（Forwarding）——将结果直接从EX/MEM级转发到下一条指令的EX输入。插入气泡（Stall）——暂停流水线等待数据就绪。编译器调度——重排指令避免依赖。记分牌和Tomasulo算法用于动态调度。}}

{{知识点35}}{{分支预测技术：静态预测——总预测不跳转/总预测跳转/基于方向预测。动态预测——分支历史表(BHT)记录分支结果，两位饱和计数器提高准确率。分支目标缓冲(BTB)缓存分支目标地址。现代处理器分支预测准确率可达95%以上。}}

{{知识点36}}{{总线分类：片内总线——CPU内部连接各部件。系统总线——CPU、主存、I/O接口间通信，分为数据总线、地址总线、控制总线。I/O总线——连接I/O设备（如PCIe）。通信总线——计算机系统间通信。}}

{{知识点37}}{{总线仲裁方式：集中式仲裁——由中央仲裁器分配总线使用权。链式查询——设备按优先级串行连接，简单但低优先级设备可能饥饿。计数器定时查询——公平性好。独立请求——响应速度快，但控制线多。分布式仲裁——各设备自行判断。}}

{{知识点38}}{{总线传输方式：同步总线——由统一时钟控制，时序简单但速度受限于最慢设备。异步总线——用握手信号协调，可连接不同速度设备。半同步总线——结合两者优点，增加Ready/Wait信号。分离事务总线——将请求和应答分开，提高总线利用率。}}

{{知识点39}}{{总线带宽计算：总线带宽 = 总线宽度(位) × 总线频率(Hz) / 8 (B/s)。例：64位总线、频率800MHz，带宽 = 64×800M/8 = 6.4GB/s。实际带宽还需考虑传输效率（非数据传输的开销）。突发传输可提高连续数据传输效率。}}

{{知识点40}}{{I/O控制方式——程序查询方式：CPU主动查询设备状态，忙等待。实现简单但CPU利用率极低，适合低速设备。CPU与外设串行工作，不能并行。}))

{{知识点41}}{{I/O控制方式——程序中断方式：设备就绪后主动通知CPU。CPU响应中断，保存现场，执行中断服务程序，恢复现场。CPU与外设可部分并行。中断优先级和中断屏蔽用于管理多个中断源。}}

{{知识点42}}{{I/O控制方式——DMA方式：直接内存访问，数据在I/O设备和主存间直接传输，不经过CPU。DMA控制器接管总线，CPU仅在传输开始和结束时介入。三种传送方式：CPU停止法、周期挪用法、交替访问法。适合高速批量数据传输。}}

{{知识点43}}{{I/O控制方式——通道方式：通道是专用处理器，有自己的指令系统（通道指令）。CPU启动通道后，通道独立执行通道程序控制I/O操作。三种通道类型：字节多路通道（低速设备）、数组选择通道（高速设备）、数组多路通道（综合）。}}

{{知识点44}}{{中断系统：中断源分为内中断（异常）和外中断。中断响应条件：中断允许位置1、有中断请求、当前指令执行完毕。中断处理过程：关中断→保存断点→识别中断源→保存现场→执行服务程序→恢复现场→开中断→返回断点。}}

{{知识点45}}{{中断优先级与嵌套：多个中断同时请求时按优先级处理。硬件实现——排队器。软件实现——查询顺序决定优先级。中断嵌套——高优先级中断可打断低优先级中断处理。中断屏蔽字用于动态调整优先级。}}

{{知识点46}}{{DMA与中断的关系：DMA传输完成后通过中断通知CPU。DMA优先级高于中断（因为DMA请求是周期性的，延迟会导致数据丢失）。DMA方式下CPU交出总线控制权，中断方式下CPU不交出总线控制权。}}

{{知识点47}}{{指令流水线性能指标：吞吐率TP = n/(k+n-1)t₀，n为指令数，k为级数，t₀为时钟周期。加速比S = k·n/(k+n-1)。效率E = S/k。流水线级数越多，理论上吞吐率越高，但流水线寄存器延迟和冒险处理开销也增大。}}

{{知识点48}}{{存储器带宽计算：存储器带宽 = 存储周期频率 × 存储字长。提高带宽的方法：双端口存储器（两个独立端口同时访问）、多体交叉存储器（低位交叉/高位交叉，连续访问分布在不同体中，减少冲突）、宽字长存储。}}

{{知识点49}}{{多体交叉存储器：低位交叉编址——地址低位选体号，高位选体内地址，连续地址分布在不同体，可实现流水式访问。n体交叉存储器带宽最高为单体的n倍。高位交叉编址——地址高位选体号，利于扩大容量但不能提高带宽。}}

{{知识点50}}{{指令流水线中的异常和中断处理：精确异常——保证异常指令之前的所有指令都完成，之后的指令都不执行。实现方法：在WB阶段才允许修改状态，或使用重排序缓冲(ROB)。乱序执行处理器中精确异常实现更复杂。}}

{{知识点51}}{{微指令格式：水平型微指令——控制字段直接控制数据通路，一条微指令可同时控制多个微操作，并行度高但字长长。垂直型微指令——类似机器指令格式，字长短但并行度低，需要更多微指令。混合型——折中方案。}}

{{知识点52}}{{USB总线特点：串行通信标准，支持热插拔和即插即用。USB采用轮询式通信，主机为主设备。USB有多种速率等级：USB 2.0(480Mbps)、USB 3.0(5Gbps)、USB 3.2(20Gbps)。传输类型：控制传输、批量传输、中断传输、等时传输。}}

{{知识点53}}{{PCIe总线：高速串行点对点连接标准。采用分层架构：物理层、数据链路层、事务层。支持多通道（x1/x4/x8/x16），每通道双向独立。PCIe 4.0单通道带宽约2GB/s，PCIe 5.0翻倍。取代了并行PCI总线。}}

{{知识点54}}{{CPU性能公式：CPU时间 = 指令数 × CPI × 时钟周期。CPI（每条指令周期数）受指令集、流水线效率、Cache命中率等因素影响。Amdahl定律：系统加速比 = 1/((1-f)+f/S)，f为可改进比例，S为局部加速比。}}

{{知识点55}}{{输入输出接口功能：数据缓冲——解决CPU与外设速度不匹配。信号转换——电平/格式转换。设备选择——地址译码选择设备。命令/状态寄存器——CPU向设备发命令、读取设备状态。中断逻辑——向CPU发送中断请求。数据格式转换——串并转换等。}}

{{知识点56}}{{程序中断方式详解：CPU与外设并行工作。每次中断只能传送一个数据字，CPU开销大。适用于低速设备。中断请求寄存器记录各设备中断请求。中断服务程序完成数据传送。比程序查询方式效率高，比DMA方式简单。}}

{{知识点57}}{{Cache性能分析：平均访存时间 = 命中时间 + 缺失率 × 缺失代价。降低缺失率：增大Cache容量、提高相联度、优化替换策略、硬件预取。降低缺失代价：多级Cache、关键字优先、提前重启。降低命中时间：小而简单的Cache、路预测。}}

{{知识点58}}{{指令系统设计原则：完备性——提供足够指令完成各种操作。有效性——指令执行效率高。规整性——指令格式和寻址方式统一。兼容性——不同机器间指令系统向上兼容。正交性——操作码与寻址方式独立。}}

{{知识点59}}{{浮点数规格化：左规——尾数最高有效位不在正确位置时，尾数左移、阶码减小。右规——尾数溢出时，尾数右移、阶码增大。上溢（阶码过大）→溢出异常。下溢（阶码过小）→结果为0或非规格化数。}}

{{知识点60}}{{计算机系统性能评价：MIPS（百万条指令/秒）——受指令集影响，不同机器不可比。MFLOPS（百万浮点运算/秒）——评价浮点性能。基准测试程序(Benchmark)——SPEC CPU、CoreMark等。吞吐量和响应时间是系统级评价指标。}}
