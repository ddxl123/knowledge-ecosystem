# 2025计算机网络基础
# 领域: 计算机IT
# 学科: 计算机网络
# 来源: 2025计算机网络基础
# 描述: 计算机网络核心知识点

{{知识点1}}{{OSI七层模型从下到上依次为：物理层、数据链路层、网络层、传输层、会话层、表示层、应用层。每层各司其职，下层为上层提供服务，是理解网络通信的基础框架。}}

{{知识点2}}{{物理层负责在物理介质上传输原始比特流，定义电气、机械、时序接口和物理传输介质（如双绞线、光纤、无线电波），不涉及数据的含义。}}

{{知识点3}}{{数据链路层负责将比特组装成帧（Frame），提供节点到节点的传输，处理物理寻址（MAC地址）、差错检测与纠正。常见协议：以太网（Ethernet）、PPP、HDLC。}}

{{知识点4}}{{网络层负责将数据包从源主机传送到目的主机（端到端），核心功能是路由选择和逻辑寻址（IP地址）。主要协议：IP、ICMP、ARP、OSPF、BGP。}}

{{知识点5}}{{传输层提供端到端的可靠或不可靠数据传输服务，核心协议为TCP和UDP。TCP提供可靠、面向连接的服务；UDP提供不可靠、无连接的服务。}}

{{知识点6}}{{会话层负责建立、管理和终止应用程序之间的会话。表示层负责数据的编码/解码、加密/解密和压缩/解压缩。应用层直接为用户应用进程提供网络服务。}}

{{知识点7}}{{TCP/IP四层模型从下到上为：网络接口层、网际层、传输层、应用层。与OSI模型的对应关系为：网络接口层≈物理层+数据链路层，应用层≈会话层+表示层+应用层。}}

{{知识点8}}{{IP地址是网络层的逻辑地址，IPv4地址为32位，分为A、B、C、D、E五类。A类（1-126）用于大型网络，B类（128-191）用于中型网络，C类（192-223）用于小型网络。}}

{{知识点9}}{{子网掩码用于区分IP地址中的网络部分和主机部分。CIDR记法（如192.168.1.0/24）中"/24"表示前24位为网络位。子网划分可以将一个大网络分割为多个小网络，提高IP地址利用率。}}

{{知识点10}}{{私有IP地址范围：A类10.0.0.0~10.255.255.255，B类172.16.0.0~172.31.255.255，C类192.168.0.0~192.168.255.255。私有地址不能直接在互联网上路由，需通过NAT转换。}}

{{知识点11}}{{IPv6地址为128位，采用十六进制表示，用冒号分隔为8组。IPv6解决了IPv4地址耗尽问题，支持自动配置、简化头部格式、内置IPsec安全机制。}}

{{知识点12}}{{TCP是面向连接的传输层协议，通过三次握手建立连接：客户端发SYN→服务器回SYN+ACK→客户端发ACK。四次挥手断开连接：FIN→ACK→FIN→ACK。保证数据可靠、有序传输。}}

{{知识点13}}{{TCP的可靠传输机制包括：序列号和确认号（保证有序和确认）、超时重传、滑动窗口（流量控制）、拥塞控制（慢启动、拥塞避免、快重传、快恢复）。}}

{{知识点14}}{{UDP是无连接的传输层协议，头部仅8字节（源端口、目的端口、长度、校验和），开销小、速度快。适用于实时音视频、DNS查询、DHCP等对实时性要求高、可容忍少量丢包的场景。}}

{{知识点15}}{{TCP三次握手中，第三次ACK的作用是确认服务器的初始序列号。若无第三次ACK，服务器不知道客户端是否收到了SYN+ACK，可能导致过期连接请求被服务器误接受。}}

{{知识点16}}{{HTTP（超文本传输协议）是应用层协议，基于请求-响应模型，默认端口80。HTTP/1.0为非持久连接，HTTP/1.1引入持久连接和管道机制，HTTP/2引入多路复用和头部压缩。}}

{{知识点17}}{{HTTPS = HTTP + SSL/TLS，在HTTP基础上增加加密层，默认端口443。TLS握手过程：客户端发送支持的加密套件→服务器选择并发送证书→客户端验证证书并生成预主密钥→双方生成会话密钥。}}

{{知识点18}}{{HTTP常见状态码：200 OK（成功）、301永久重定向、302临时重定向、304未修改、400请求错误、401未授权、403禁止访问、404未找到、500服务器内部错误、502网关错误、503服务不可用。}}

{{知识点19}}{{DNS（域名系统）将域名解析为IP地址。解析过程：浏览器缓存→操作系统缓存→本地DNS服务器→根DNS→顶级域DNS→权威DNS。DNS使用UDP协议，端口53（大于512字节时用TCP）。}}

{{知识点20}}{{ARP（地址解析协议）将IP地址解析为MAC地址。工作过程：主机广播ARP请求→目标主机单播ARP应答。ARP缓存可以减少广播次数。ARP欺骗是常见的局域网攻击方式。}}

{{知识点21}}{{DHCP（动态主机配置协议）自动分配IP地址。过程：客户端广播DHCP Discover→服务器回应DHCP Offer→客户端请求DHCP Request→服务器确认DHCP ACK。使用UDP，客户端端口68，服务器端口67。}}

{{知识点22}}{{路由选择分为静态路由和动态路由。动态路由协议：RIP（距离向量，最大15跳）、OSPF（链路状态，使用Dijkstra算法）、BGP（路径向量，用于自治系统间）。默认路由（0.0.0.0/0）用于没有更具体路由时。}}

{{知识点23}}{{NAT（网络地址转换）将私有IP转换为公有IP，解决IPv4地址不足。类型：静态NAT（一对一）、动态NAT（多对多）、NAPT/PAT（多对一，使用端口号区分）。NAT穿透是P2P通信的难点。}}

{{知识点24}}{{交换机工作在数据链路层，根据MAC地址表进行帧转发。交换机学习MAC地址：收到帧后记录源MAC与端口的映射。若目标MAC未知，则洪泛（除入端口外所有端口转发）。}}

{{知识点25}}{{路由器工作在网络层，根据路由表进行数据包转发。路由器连接不同网络，实现跨网络通信。路由器的每个接口属于不同的广播域。}}

{{知识点26}}{{VLAN（虚拟局域网）将物理网络逻辑划分为多个广播域，减少广播流量，提高安全性和灵活性。交换机通过VLAN标签（802.1Q）区分不同VLAN的流量。Trunk端口可承载多个VLAN。}}

{{知识点27}}{{TCP拥塞控制算法：慢启动（指数增长）→拥塞避免（线性增长）→检测到丢包后：快速重传（收到3个重复ACK立即重传）→快速恢复（将阈值减半，从新阈值开始拥塞避免）。}}

{{知识点28}}{{滑动窗口机制：发送方维护发送窗口，窗口内的数据可以连续发送而不必等待确认。接收方通过TCP头部的窗口字段通告自己的接收能力（流量控制），防止发送方发送过快导致接收方缓冲区溢出。}}

{{知识点29}}{{ICMP（互联网控制报文协议）用于网络层的差错报告和诊断。常见类型：Echo Request/Reply（ping使用）、Destination Unreachable、Time Exceeded（traceroute使用）。ICMP不传输用户数据，是IP的辅助协议。}}

{{知识点30}}{{ping命令使用ICMP Echo Request/Reply测试主机可达性和往返时间（RTT）。traceroute利用TTL递增和ICMP Time Exceeded报文发现到目标的路径上每一跳路由器。}}

{{知识点31}}{{网络安全基础：对称加密（AES、DES，速度快，密钥分发难）、非对称加密（RSA、ECC，公钥加密私钥解密，速度慢但安全）、哈希函数（SHA-256、MD5，不可逆，用于完整性验证）。}}

{{知识点32}}{{数字证书由CA（证书颁发机构）签发，包含公钥、持有者信息、CA签名。客户端验证证书链直到根CA。证书吊销通过CRL（证书吊销列表）或OCSP（在线证书状态协议）实现。}}

{{知识点33}}{{防火墙分为包过滤防火墙（基于IP、端口）、状态检测防火墙（跟踪连接状态）、应用层防火墙/代理防火墙（深度包检测）。下一代防火墙（NGFW）集成入侵防御、应用识别等功能。}}

{{知识点34}}{{SYN Flood攻击：攻击者发送大量伪造源IP的SYN包，使服务器维护大量半开连接，耗尽资源。防御方法：SYN Cookie、限制半连接数、使用防火墙过滤。}}

{{知识点35}}{{Socket编程：TCP Socket通信流程——服务器：socket()→bind()→listen()→accept()→read()/write()→close()；客户端：socket()→connect()→read()/write()→close()。UDP使用sendto()/recvfrom()。}}

{{知识点36}}{{WebSocket是全双工通信协议，基于TCP，通过HTTP升级握手建立连接。相比HTTP轮询，WebSocket实现服务器主动推送，适合实时聊天、在线游戏、股票行情等场景。默认端口8765（ws）或443（wss）。}}

{{知识点37}}{{CDN（内容分发网络）通过在全球部署边缘节点，将内容缓存到离用户最近的节点，减少延迟、提高访问速度。关键技术：DNS智能解析、负载均衡、内容缓存、回源策略。}}

{{知识点38}}{{负载均衡将流量分发到多台服务器，提高系统可用性和性能。算法：轮询、加权轮询、最少连接、IP哈希。实现层次：四层负载均衡（L4，基于IP+端口）、七层负载均衡（L7，基于HTTP内容）。}}

{{知识点39}}{{网络延迟的组成部分：传播延迟（信号在介质中传播的时间）、传输延迟（数据发送到链路的时间=数据量/带宽）、处理延迟（路由器处理时间）、排队延迟（在队列中等待的时间）。总延迟=四者之和。}}

{{知识点40}}{{带宽（Bandwidth）是链路的最大数据传输速率（bps），吞吐量是实际的数据传输速率，通常小于带宽。信道利用率=帧发送时间/(帧发送时间+往返传播延迟)。}}

{{知识点41}}{{CSMA/CD（载波侦听多路访问/冲突检测）是以太网的介质访问控制协议。工作流程：先听后发、边发边听、冲突停发、随机重发。全双工以太网不使用CSMA/CD。无线网络使用CSMA/CA（冲突避免）。}}

{{知识点42}}{{HTTP/3基于QUIC协议（运行在UDP之上），解决了HTTP/2的队头阻塞问题。QUIC内置TLS 1.3加密，支持0-RTT快速连接建立，连接迁移（基于Connection ID而非IP+端口）。}}

{{知识点43}}{{RESTful API设计原则：资源用URI标识、使用标准HTTP方法（GET获取、POST创建、PUT更新、DELETE删除）、无状态、统一接口。JSON是最常用的数据交换格式。}}

{{知识点44}}{{网络攻击常见类型：DDoS（分布式拒绝服务）、中间人攻击（MITM）、SQL注入、XSS（跨站脚本）、CSRF（跨站请求伪造）、DNS劫持、ARP欺骗。防御需要多层安全策略。}}

{{知识点45}}{{VPN（虚拟专用网络）通过公共网络建立加密隧道，实现安全的远程访问。协议：IPsec（网络层）、SSL/TLS VPN（应用层）、WireGuard（新型，高性能）。企业常用VPN连接分支机构。}}

{{知识点46}}{{STP（生成树协议）解决交换网络中的环路问题，通过阻塞冗余端口形成无环拓扑。RSTP（快速生成树协议）缩短了收敛时间。链路聚合（LACP）将多条物理链路捆绑为一条逻辑链路，增加带宽和冗余。}}

{{知识点47}}{{FTP（文件传输协议）使用双通道：控制连接（端口21，持久）和数据连接（端口20或随机高端口，按需建立）。主动模式：服务器主动连接客户端；被动模式：客户端连接服务器的数据端口。}}

{{知识点48}}{{端口号范围0~65535，其中0~1023为知名端口（Well-Known Ports），1024~49151为注册端口，49152~65535为动态/私有端口。常见端口：HTTP 80、HTTPS 443、FTP 21、SSH 22、SMTP 25、DNS 53。}}

{{知识点49}}{{网络性能指标：带宽（最大传输速率）、延迟（数据往返时间）、丢包率（丢失数据包占比）、抖动（延迟的变化量）、吞吐量（实际传输速率）。不同应用对指标敏感度不同。}}

{{知识点50}}{{零拷贝技术（如Linux的sendfile、mmap）减少用户空间与内核空间之间的数据拷贝次数，显著提升网络数据传输效率。传统方式需要4次拷贝和4次上下文切换，零拷贝可减少到2次拷贝。}}
