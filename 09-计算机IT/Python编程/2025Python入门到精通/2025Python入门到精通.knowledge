# 2025Python入门到精通
# 领域: 计算机IT
# 学科: Python编程
# 来源: 2025Python入门到精通
# 描述: Python编程核心知识点与实战技巧

{{知识点1}}{{Python的特点是什么？
Python是一种高级、解释型、通用编程语言，具有以下特点：
- 语法简洁清晰，接近自然语言
- 动态类型系统，无需声明变量类型
- 自动内存管理（垃圾回收机制）
- 丰富的标准库和第三方库
- 跨平台运行（Windows、macOS、Linux）
- 支持面向对象、函数式、过程式编程范式
- 开源免费，社区活跃}}

{{知识点2}}{{Python变量命名规则
- 由字母、数字、下划线组成，不能以数字开头
- 区分大小写（name 和 Name 是不同变量）
- 不能使用Python关键字（如 if、for、class 等）
- 推荐使用蛇形命名法（snake_case），如 my_variable
- 类名推荐使用大驼峰命名法（PascalCase），如 MyClass
- 私有变量约定以单下划线开头，如 _private_var
- 避免使用单字符变量名（循环变量 i、j 除外）}}

{{知识点3}}{{Python基本数据类型
Python有以下基本数据类型：
- int（整数）：如 42、-10、0
- float（浮点数）：如 3.14、-0.5、1.0
- bool（布尔）：True、False
- str（字符串）：如 "hello"、'world'
- None：表示空值，是 NoneType 的唯一实例
- complex（复数）：如 3+4j
可以用 type() 函数查看变量类型，isinstance() 判断类型}}

{{知识点4}}{{字符串常用操作
- 切片：s[start:end:step]，如 "hello"[1:4] → "ell"
- 查找：find()、index()、count()
- 替换：replace(old, new)
- 分割：split(sep)、rsplit()
- 连接：sep.join(iterable)
- 去除空白：strip()、lstrip()、rstrip()
- 大小写：upper()、lower()、title()、capitalize()
- 判断：isdigit()、isalpha()、isalnum()、startswith()、endswith()
- 格式化：f-string（f"{name}"）、format()、% 操作符（推荐f-string）}}

{{知识点5}}{{列表（list）操作
列表是有序可变序列，常用操作：
- 创建：[] 或 list()
- 添加：append(x)、insert(i, x)、extend(iterable)
- 删除：remove(x)、pop(i)、del lst[i]、clear()
- 排序：sort()（原地排序）、sorted()（返回新列表）
- 切片：lst[start:end:step]
- 推导式：[x**2 for x in range(10) if x % 2 == 0]
- 常用函数：len()、max()、min()、sum()、enumerate()、zip()
- 复制：lst.copy() 或 lst[:]（浅拷贝）}}

{{知识点6}}{{元组（tuple）特性
元组是有序不可变序列：
- 创建：() 或 tuple()，单元素需加逗号 (1,)
- 不可变：创建后不能修改元素
- 可作为字典的键（因为可哈希）
- 支持切片、索引、遍历
- 解包：a, b, c = (1, 2, 3)
- 常用方法：count()、index()
- 比列表更节省内存，访问速度更快
- 适用于不需要修改的数据集合}}

{{知识点7}}{{字典（dict）操作
字典是键值对的无序集合（Python 3.7+保持插入顺序）：
- 创建：{}、dict()、dict(key=value)
- 访问：d[key]、d.get(key, default)
- 添加/修改：d[key] = value、update()
- 删除：del d[key]、pop(key)、popitem()、clear()
- 遍历：keys()、values()、items()
- 推导式：{k: v for k, v in items if condition}
- 合并：d1 | d2（Python 3.9+）
- setdefault(key, default)：获取或设置默认值}}

{{知识点8}}{{集合（set）操作
集合是无序不重复元素集：
- 创建：{}（非空）、set()、set(iterable)
- 添加：add()、update()
- 删除：remove()（不存在报错）、discard()（不存在不报错）、pop()
- 集合运算：
  - 并集：s1 | s2 或 s1.union(s2)
  - 交集：s1 & s2 或 s1.intersection(s2)
  - 差集：s1 - s2 或 s1.difference(s2)
  - 对称差集：s1 ^ s2 或 s1.symmetric_difference(s2)
- 推导式：{x for x in iterable}
- 适用于去重和成员检测（O(1)时间复杂度）}}

{{知识点9}}{{条件语句 if-elif-else
基本语法：
if 条件1:
    代码块1
elif 条件2:
    代码块2
else:
    代码块3

- 条件可以是比较表达式、逻辑表达式、布尔值
- 逻辑运算符：and、or、not
- 比较运算符：==、!=、<、>、<=、>=
- 链式比较：0 < x < 10
- 三元表达式：x if condition else y
- 真值判断：0、None、空序列、空字典为False，其余为True
- match-case（Python 3.10+）：模式匹配语句}}

{{知识点10}}{{for循环与迭代
基本语法：
for 变量 in 可迭代对象:
    代码块

- range(start, stop, step)：生成数字序列
- enumerate(iterable)：同时获取索引和值
- zip(iter1, iter2)：并行遍历多个可迭代对象
- for-else：循环正常结束时执行else块
- 嵌套循环与break/continue
- 可迭代对象：list、tuple、str、dict、set、range、generator
- 迭代器协议：__iter__() 和 __next__() 方法
- iter() 和 next() 内置函数}}

{{知识点11}}{{while循环
基本语法：
while 条件:
    代码块

- 条件为True时持续执行
- 必须有退出条件，避免死循环
- while-else：循环条件为False时执行else块
- break：跳出整个循环
- continue：跳过当前迭代，继续下次循环
- 适用场景：不确定循环次数时使用
- 可以用 while True + break 模拟do-while}}

{{知识点12}}{{函数定义与调用
def 函数名(参数):
    函数体
    return 返回值

- 位置参数：def f(a, b)
- 关键字参数：f(a=1, b=2)
- 默认参数：def f(a, b=10)，默认参数必须在最后
- 可变位置参数：*args，收集为元组
- 可变关键字参数：**kwargs，收集为字典
- 返回值：可以返回多个值（实际是元组）
- 文档字符串：函数体内第一行用三引号包裹
- 类型注解：def f(a: int) -> str:（不强制，仅提示）}}

{{知识点13}}{{作用域与LEGB规则
Python变量查找遵循LEGB规则：
- L（Local）：函数内部的局部变量
- E（Enclosing）：外层嵌套函数的作用域
- G（Global）：模块级别的全局变量
- B（Built-in）：内置命名空间

- global：在函数内声明使用全局变量
- nonlocal：在内层函数中修改外层函数变量
- 局部变量优先级高于全局变量
- 函数内只能读取全局变量，不能直接修改（需声明global）
- 类的作用域与函数不同，类变量对方法是可见的}}

{{知识点14}}{{Lambda匿名函数
语法：lambda 参数: 表达式

- 适用于简单的单行函数
- 常与 sorted()、map()、filter() 配合使用
- 示例：
  - sorted(lst, key=lambda x: x[1])
  - map(lambda x: x**2, lst)
  - filter(lambda x: x > 0, lst)
- 可以赋值给变量（但不推荐，建议用def）
- 不支持多条语句和复杂逻辑
- 可以有多个参数：lambda x, y: x + y}}

{{知识点15}}{{装饰器（Decorator）
装饰器是修改函数行为的高阶函数：
- 基本语法：@decorator 放在函数定义前
- 本质：decorator(func) 返回一个新函数
- 带参数的装饰器：需要三层嵌套
- 常用内置装饰器：@property、@staticmethod、@classmethod、@functools.wraps
- 用途：日志记录、权限校验、性能测量、缓存
- @functools.wraps(f)：保留原函数的元信息
- 类装饰器：用类实现装饰器，实现__call__方法
- 多个装饰器从下往上执行（靠近函数的先执行）}}

{{知识点16}}{{列表推导式与生成器表达式
列表推导式：[表达式 for 变量 in 可迭代对象 if 条件]
- 示例：[x**2 for x in range(10) if x % 2 == 0]
- 嵌套：[(x, y) for x in range(3) for y in range(3)]
- 可以有多层条件过滤

生成器表达式：(表达式 for 变量 in 可迭代对象 if 条件)
- 返回生成器对象，惰性求值，节省内存
- 适用于大数据集或只需遍历一次的场景
- tuple()、sum()、any()、all() 等可以直接接收生成器

字典推导式：{k: v for k, v in iterable}
集合推导式：{表达式 for 变量 in 可迭代对象}}

{{知识点17}}{{类与面向对象基础
class 类名:
    类变量 = 值
    
    def __init__(self, 参数):
        self.实例变量 = 参数

- self：实例方法的第一个参数，指向实例本身
- 实例变量：每个实例独有的数据
- 类变量：所有实例共享的数据
- 实例方法：操作实例数据，第一个参数是self
- 类方法：@classmethod，第一个参数是cls，操作类数据
- 静态方法：@staticmethod，无默认第一个参数，与类/实例无关
- 属性访问：obj.attr，属性查找顺序：实例→类→父类}}

{{知识点18}}{{特殊方法（魔术方法）
常用的特殊方法：
- __init__(self)：构造方法，初始化实例
- __str__(self)：str()和print()调用，面向用户
- __repr__(self)：repr()调用，面向开发者，应返回可重建对象的字符串
- __len__(self)：len()调用
- __getitem__(self, key)：支持下标访问
- __setitem__(self, key, value)：支持下标赋值
- __del__(self)：析构方法，对象被销毁时调用
- __eq__、__lt__、__gt__ 等：比较运算符
- __add__、__mul__ 等：算术运算符
- __enter__、__exit__：上下文管理器协议
- __iter__、__next__：迭代器协议
- __call__：使实例可调用}}

{{知识点19}}{{继承与多态
- 继承语法：class Child(Parent)
- 多继承：class Child(Parent1, Parent2)
- super()：调用父类方法，如 super().__init__()
- 方法重写（Override）：子类重新定义父类方法
- 多态：不同类的对象对同一方法有不同实现
- isinstance(obj, Class)：检查对象是否为某类的实例
- issubclass(Child, Parent)：检查是否为子类
- MRO（方法解析顺序）：C3线性化算法，可通过__mro__查看
- 鸭子类型：不关心类型，只关心行为（"如果它像鸭子叫..."）}}

{{知识点20}}{{文件操作
打开文件：open(file, mode, encoding)
- 模式：'r'读、'w'写（覆盖）、'a'追加、'b'二进制、'x'创建、't'文本
- 推荐使用 with 语句自动关闭文件

读取操作：
- read()：读取全部内容
- readline()：读取一行
- readlines()：读取所有行，返回列表
- for line in f：逐行迭代（推荐，内存友好）

写入操作：
- write(string)：写入字符串
- writelines(lines)：写入字符串列表

其他：
- seek(offset)：移动文件指针
- tell()：返回当前指针位置
- with open('f.txt', encoding='utf-8') as f: data = f.read()}}

{{知识点21}}{{异常处理
try:
    可能出错的代码
except 异常类型 as e:
    处理代码
else:
    无异常时执行
finally:
    无论是否异常都执行

- 常见异常：ValueError、TypeError、KeyError、IndexError、FileNotFoundError、AttributeError
- 捕获多个异常：except (TypeError, ValueError)
- 不指定异常类型会捕获所有异常（不推荐）
- raise：主动抛出异常
- 自定义异常：继承Exception类
- 异常链：raise NewError() from original_error
- assert：调试断言，assert 条件, "错误信息"
- 上下文管理器也可以处理异常}}

{{知识点22}}{{模块与包
- 模块：一个 .py 文件就是一个模块
- 包：包含 __init__.py 的目录
- 导入方式：
  - import module
  - from module import name
  - from module import *（不推荐）
  - import module as alias
- 模块搜索路径：sys.path
- __name__ == "__main__"：判断是否为主模块运行
- __init__.py：包的初始化文件，可以为空
- 相对导入：from . import module（包内导入）
- 绝对导入：from package import module
- dir(module)：查看模块所有属性
- 常用标准库：os、sys、math、datetime、json、re、collections、itertools}}

{{知识点23}}{{常用内置函数
- len()：获取长度
- type()：查看类型
- isinstance()：类型判断
- int()、float()、str()、bool()：类型转换
- range()：生成数字序列
- enumerate()：带索引迭代
- zip()：并行迭代
- map()、filter()：函数式编程
- sorted()：排序，返回新列表
- min()、max()、sum()：聚合函数
- abs()：绝对值
- round()：四舍五入
- any()、all()：逻辑判断
- input()：用户输入
- print()：输出
- open()：打开文件
- hasattr()、getattr()、setattr()：属性操作
- callable()：判断是否可调用
- id()：获取对象内存地址
- hash()：获取哈希值}}

{{知识点24}}{{collections模块常用类
- Counter：计数器，统计元素出现次数
  - Counter("abracadabra") → {'a': 5, 'b': 2, ...}
  - most_common(n)：返回出现次数最多的n个
- defaultdict：带默认值的字典
  - defaultdict(int)：不存在的键默认值为0
  - defaultdict(list)：不存在的键默认值为空列表
- namedtuple：具名元组，可通过名称访问字段
  - Point = namedtuple('Point', ['x', 'y'])
- deque：双端队列，两端操作都是O(1)
  - appendleft()、popleft()、rotate()
- OrderedDict：有序字典（Python 3.7+普通dict已保持顺序）}}

{{知识点25}}{{itertools模块常用函数
- count(start, step)：无限计数器
- cycle(iterable)：无限循环迭代
- repeat(obj, times)：重复元素
- chain(iter1, iter2)：连接多个可迭代对象
- combinations(iterable, r)：组合（不重复）
- permutations(iterable, r)：排列
- product(iter1, iter2)：笛卡尔积
- groupby(iterable, key)：分组
- islice(iterable, stop)：切片迭代器
- starmap(func, iterable)：星号映射
- takewhile/dropwhile：条件截取/丢弃
- pairwise(iterable)：成对迭代（Python 3.10+）}}

{{知识点26}}{{正则表达式（re模块）
- re.match()：从字符串开头匹配
- re.search()：搜索第一个匹配
- re.findall()：找到所有匹配，返回列表
- re.sub()：替换匹配内容
- re.split()：按模式分割
- re.compile()：预编译正则表达式，提高效率
- 常用模式：
  - \d：数字，\w：字母数字下划线，\s：空白字符
  - .：任意字符（除换行）
  - *：0次或多次，+：1次或多次，?：0次或1次
  - {n,m}：n到m次
  - []：字符集，[^]：否定字符集
  - ()：捕获组，(?:)：非捕获组
  - ^：开头，$：结尾，|：或
- 标志：re.IGNORECASE、re.MULTILINE、re.DOTALL}}

{{知识点27}}{{JSON数据处理（json模块）
- json.dumps(obj)：Python对象→JSON字符串
- json.loads(str)：JSON字符串→Python对象
- json.dump(obj, file)：写入文件
- json.load(file)：从文件读取
- 常用参数：
  - ensure_ascii=False：支持中文
  - indent=2：格式化缩进
  - sort_keys=True：按键排序
- 类型映射：
  - dict → object
  - list/tuple → array
  - str → string
  - int/float → number
  - True/False → true/false
  - None → null
- 自定义编解码：default参数和object_hook参数}}

{{知识点28}}{{日期时间处理（datetime模块）
- datetime.datetime.now()：获取当前时间
- datetime.date.today()：获取当前日期
- datetime.datetime(2025, 1, 1)：创建指定日期时间
- 格式化：
  - strftime(format)：日期→字符串
  - strptime(string, format)：字符串→日期
  - 常用格式：%Y-%m-%d %H:%M:%S
- 时间差：datetime.timedelta(days=7)
- 时间戳：timestamp() 和 fromtimestamp()
- 时区：datetime.timezone.utc、zoneinfo模块（Python 3.9+）
- 常用操作：加减时间差、比较日期、计算间隔}}

{{知识点29}}{{os模块常用操作
- os.getcwd()：获取当前工作目录
- os.chdir(path)：切换目录
- os.listdir(path)：列出目录内容
- os.mkdir(path)：创建目录
- os.makedirs(path)：递归创建目录
- os.remove(path)：删除文件
- os.rmdir(path)：删除空目录
- os.removedirs(path)：递归删除空目录
- os.rename(old, new)：重命名
- os.path.exists(path)：判断是否存在
- os.path.isfile/isdir()：判断文件/目录
- os.path.join()：路径拼接（跨平台）
- os.path.basename/dirname()：文件名/目录名
- os.path.getsize()：获取文件大小
- os.environ：环境变量字典
- os.walk()：遍历目录树}}

{{知识点30}}{{pathlib模块（Python 3.4+）
面向对象的路径操作：
- from pathlib import Path
- Path("dir/file.txt")：创建路径对象
- Path.cwd()：当前目录
- Path.home()：用户主目录
- 路径操作：
  - p / "sub" / "file.txt"：路径拼接（推荐）
  - p.name：文件名
  - p.stem：无扩展名的文件名
  - p.suffix：扩展名
  - p.parent：父目录
  - p.exists()：是否存在
  - p.is_file()、p.is_dir()：判断类型
  - p.mkdir(parents=True)：创建目录
  - p.read_text()、p.write_text()：读写文本
  - p.read_bytes()、p.write_bytes()：读写字节
  - p.glob("*.py")：通配符搜索
  - p.rglob("*.py")：递归搜索
- 比os.path更现代、更易用}}

{{知识点31}}{{logging模块日志记录
- import logging
- logging.basicConfig(level=logging.DEBUG)
- 日志级别（从低到高）：DEBUG < INFO < WARNING < ERROR < CRITICAL
- 基本方法：logging.debug()、info()、warning()、error()、critical()
- 自定义logger：logger = logging.getLogger(__name__)
- Handler：控制日志输出位置（控制台、文件等）
- Formatter：控制日志输出格式
- 配置示例：
  logging.basicConfig(
      level=logging.INFO,
      format='%(asctime)s - %(name)s - %(levelname)s - %(message)s',
      filename='app.log'
  )
- 比print更适合生产环境，可控制级别和输出格式}}

{{知识点32}}{{虚拟环境与包管理
- 创建虚拟环境：python -m venv env_name
- 激活虚拟环境：
  - Windows：env_name\Scripts\activate
  - macOS/Linux：source env_name/bin/activate
- 退出虚拟环境：deactivate
- pip常用命令：
  - pip install package：安装包
  - pip install package==version：安装指定版本
  - pip install -r requirements.txt：批量安装
  - pip freeze > requirements.txt：导出依赖
  - pip list：列出已安装包
  - pip uninstall package：卸载包
  - pip install --upgrade package：升级包
- Python 3.12+推荐使用内置venv
- 其他工具：poetry、pipenv、conda}}

{{知识点33}}{{类的高级特性——property
@property装饰器将方法变为属性访问：
class Circle:
    def __init__(self, radius):
        self._radius = radius
    
    @property
    def radius(self):
        return self._radius
    
    @radius.setter
    def radius(self, value):
        if value < 0:
            raise ValueError("半径不能为负")
        self._radius = value
    
    @property
    def area(self):
        return 3.14159 * self._radius ** 2

- getter：@property
- setter：@属性名.setter
- deleter：@属性名.deleter
- 只读属性：只定义getter不定义setter
- 用于封装、验证、计算属性}}

{{知识点34}}{{数据类（dataclasses模块，Python 3.7+）
from dataclasses import dataclass, field

@dataclass
class Point:
    x: float
    y: float
    label: str = "origin"

- 自动生成__init__、__repr__、__eq__方法
- @dataclass(order=True)：生成比较方法
- @dataclass(frozen=True)：不可变数据类
- field(default_factory=list)：可变默认值
- __post_init__：初始化后处理
- dataclass替代简单的类定义，减少样板代码
- 比namedtuple更灵活，比普通类更简洁
- Python 3.10+支持slots=True节省内存}}

{{知识点35}}{{上下文管理器与with语句
两种实现方式：
1. 类实现__enter__和__exit__方法
2. contextlib.contextmanager装饰器

class MyContext:
    def __enter__(self):
        # 进入时执行
        return self
    
    def __exit__(self, exc_type, exc_val, exc_tb):
        # 退出时执行（即使发生异常）
        return False  # True会吞掉异常

from contextlib import contextmanager
@contextmanager
def my_context():
    # 进入时
    yield value  # 返回给as变量
    # 退出时

- 文件操作、数据库连接、锁等常用with语句
- contextlib.suppress()：忽略特定异常
- contextlib.redirect_stdout()：重定向标准输出}}

{{知识点36}}{{迭代器与生成器
迭代器协议：
- __iter__()：返回迭代器对象
- __next__()：返回下一个元素，无元素时抛出StopIteration
- iter(obj) 和 next(obj) 是对应的内置函数

生成器（Generator）：
- 使用yield关键字的函数
- 惰性求值，节省内存
- 示例：
  def fibonacci():
      a, b = 0, 1
      while True:
          yield a
          a, b = b, a + b
- send(value)：向生成器发送值
- throw()：向生成器抛出异常
- close()：关闭生成器
- yield from：委托给子生成器
- 适用于大数据处理、流式读取、无限序列}}

{{知识点37}}{{协程与异步编程（asyncio）
async def 函数名():
    await 异步操作

- async def：定义协程函数
- await：等待协程完成
- asyncio.run()：运行顶层协程
- asyncio.gather()：并发运行多个协程
- asyncio.create_task()：创建任务
- asyncio.sleep()：异步休眠
- async with：异步上下文管理器
- async for：异步迭代
- 事件循环（Event Loop）：调度协程执行
- 适用场景：IO密集型任务（网络请求、文件操作）
- 注意：协程中不能直接调用同步阻塞函数}}

{{知识点38}}{{线程与多进程
threading模块（线程）：
- t = threading.Thread(target=func, args=(arg,))
- t.start()：启动线程
- t.join()：等待线程完成
- threading.Lock()：互斥锁，防止数据竞争
- threading.Semaphore()：信号量
- threading.Event()：事件通知
- 适用：IO密集型任务

multiprocessing模块（多进程）：
- p = multiprocessing.Process(target=func)
- p.start()、p.join()
- multiprocessing.Pool(n)：进程池
- map(func, iterable)：并行映射
- 适用：CPU密集型任务

- GIL（全局解释器锁）限制了Python线程的并行性
- CPU密集型任务推荐使用多进程
- concurrent.futures：高级接口，统一线程和进程池}}

{{知识点39}}{{类型提示（Type Hints，Python 3.5+）
- 基本类型：int、str、float、bool、None
- 容器类型：list[int]、dict[str, int]、tuple[int, ...]、set[str]
- Optional[X]：等价于 Union[X, None]
- Union[X, Y]：X或Y类型（Python 3.10+可用 X | Y）
- Any：任意类型
- Callable[[参数类型], 返回类型]：函数类型
- TypeVar：泛型变量
- Protocol：结构化子类型（Python 3.8+）
- TypeAlias：类型别名（Python 3.10+）
- 不影响运行时行为，仅用于类型检查工具（mypy、pyright）
- 好处：提高代码可读性、IDE自动补全、静态检查}}

{{知识点40}}{{单元测试（unittest模块）
import unittest

class TestMath(unittest.TestCase):
    def test_add(self):
        self.assertEqual(1 + 1, 2)
    
    def test_raises(self):
        with self.assertRaises(ZeroDivisionError):
            1 / 0
    
    def setUp(self):  # 每个测试前执行
        pass
    
    def tearDown(self):  # 每个测试后执行
        pass

- assertEqual、assertNotEqual、assertTrue、assertFalse
- assertIn、assertIs、assertIsNone
- assertRaises：异常测试
- setUp/tearDown：测试前后准备/清理
- setUpClass/tearDownClass：类级别
- python -m unittest discover：自动发现并运行测试
- pytest：第三方测试框架，更简洁的语法（推荐）}}

{{知识点41}}{{pytest测试框架
- 安装：pip install pytest
- 测试文件命名：test_*.py 或 *_test.py
- 测试函数命名：def test_xxx()
- 断言：直接使用 assert 语句
- fixture：测试前的准备和清理
  @pytest.fixture
  def sample_data():
      return [1, 2, 3]
- 参数化：@pytest.mark.parametrize("input,expected", [(1,2), (3,4)])
- 标记：@pytest.mark.skip、@pytest.mark.xfail
- 运行：pytest、pytest test_file.py、pytest -v
- 插件：pytest-cov（覆盖率）、pytest-xdist（并行）
- 比unittest更简洁、更Pythonic}}

{{知识点42}}{{虚拟环境管理工具对比
- venv：Python内置，轻量，适合简单项目
- virtualenv：功能更丰富，支持更多Python版本
- conda：跨语言包管理，适合数据科学
- poetry：依赖管理+打包发布，pyproject.toml
- pipenv：结合pip和virtualenv，Pipfile
- uv：Rust实现，极快的包管理器（2024年新工具）
- 推荐方案：
  - 小项目：venv + pip
  - 库开发：poetry
  - 数据科学：conda
  - 追求速度：uv}}

{{知识点43}}{{字符串格式化详解
三种方式：
1. f-string（Python 3.6+，推荐）：
   f"{name} is {age} years old"
   f"{value:.2f}"  # 保留两位小数
   f"{number:>10}"  # 右对齐，宽度10
   f"{number:0>10}"  # 右对齐，补零
   f"{number:,}"  # 千位分隔符
   f"{value:.2%}"  # 百分比格式
   f"{obj!r}"  # 调用repr()

2. str.format()：
   "{} is {}".format(name, age)
   "{0} {1} {0}".format("hello", "world")
   "{name} is {age}".format(name="Alice", age=30)

3. %格式化（旧式，不推荐）：
   "%s is %d years old" % (name, age)

- f-string性能最好，可读性最强
- f-string中可以嵌入表达式：f"{func(x) + 1}"}}

{{知识点44}}{{深拷贝与浅拷贝
- 赋值：只是创建引用，不复制对象
  a = [1, 2, 3]; b = a  # a和b指向同一对象
- 浅拷贝：复制第一层，内层仍是引用
  import copy
  b = copy.copy(a)
  b = a.copy()、b = a[:]、b = list(a)
- 深拷贝：完全递归复制，完全独立
  b = copy.deepcopy(a)

区别：
- 浅拷贝：外层是新对象，内层嵌套对象仍是共享的
- 深拷贝：所有层级都是新对象，完全独立
- 不可变对象（str、int、tuple）不需要深拷贝
- 嵌套可变对象（如列表中包含列表）需要深拷贝才能完全独立}}

{{知识点45}}{{Python内存管理
- 引用计数：每个对象维护被引用的次数
  - sys.getrefcount(obj)：查看引用计数
  - 引用计数为0时立即释放内存
- 垃圾回收：处理循环引用
  - gc模块：gc.collect()、gc.get_stats()
  - 分代回收：0、1、2三代
- 内存池：小对象（<512字节）使用内存池（pymalloc）
- 优化建议：
  - 使用__slots__限制实例属性，减少内存
  - 使用生成器替代列表，节省内存
  - 及时释放不需要的引用
  - 使用弱引用：weakref模块
  - 对于大型数据，考虑numpy数组
- id(obj)：返回对象内存地址
- is：判断是否为同一对象（比较地址）}}

{{知识点46}}{{Python性能优化技巧
1. 使用内置函数和库（C实现，比纯Python快）
2. 列表推导式比for循环快
3. 字符串拼接用join()而非+=
4. 使用集合(set)进行成员检测
5. 局部变量比全局变量访问快
6. 避免在循环中重复计算（提前计算常量）
7. 使用生成器处理大数据集
8. 使用lru_cache缓存重复计算
9. 使用__slots__减少内存和加速属性访问
10. 使用numpy处理数值计算
11. 使用Cython或C扩展优化热点代码
12. profiling工具：cProfile、line_profiler、memory_profiler
13. timeit模块：微基准测试
- 优化原则：先测量，再优化，不要过早优化}}

{{知识点47}}{{上下文管理器实用案例
1. 文件操作：
   with open('file.txt') as f: data = f.read()

2. 数据库连接：
   with sqlite3.connect('db.sqlite') as conn: ...

3. 计时器：
   @contextmanager
   def timer():
       start = time.time()
       yield
       print(f"耗时: {time.time() - start:.2f}s")

4. 临时修改目录：
   with tempfile.TemporaryDirectory() as tmpdir: ...

5. 重定向输出：
   with open('log.txt', 'w') as f:
       with contextlib.redirect_stdout(f):
           print("这会写入文件")

6. 锁管理：
   with threading.Lock(): ...

7. 信号量：
   with semaphore: ...}}

{{知识点48}}{{Python包结构最佳实践
my_package/
├── __init__.py        # 包的入口，定义__all__
├── core.py           # 核心功能
├── utils.py          # 工具函数
├── exceptions.py     # 自定义异常
├── sub_package/      # 子包
│   ├── __init__.py
│   └── module.py
└── py.typed          # 类型提示标记文件

- __init__.py中控制导出：__all__ = ["func1", "Class1"]
- pyproject.toml：现代包配置（替代setup.py）
- setup.py/setuptools：传统打包方式
- MANIFEST.in：指定额外打包文件
- 相对导入：from .module import func
- 延迟导入：在函数内部导入，减少启动时间
- 循环导入解决方案：延迟导入或重构代码}}

{{知识点49}}{{Python设计模式常用实现
1. 单例模式：
   class Singleton:
       _instance = None
       def __new__(cls):
           if cls._instance is None:
               cls._instance = super().__new__(cls)
           return cls._instance

2. 工厂模式：
   def create_animal(kind):
       animals = {"dog": Dog, "cat": Cat}
       return animals[kind]()

3. 观察者模式：
   使用信号库或自定义事件系统

4. 策略模式：
   将算法封装为函数，通过参数传递

5. 装饰器模式：
   Python原生支持装饰器语法

6. 上下文管理器模式：
   实现__enter__和__exit__

- Python推崇鸭子类型，很多设计模式可以简化
- 组合优于继承
- 函数是一等公民，很多场景用函数代替类}}

{{知识点50}}{{Python 3.10+新特性
- match-case模式匹配：
  match command:
      case "quit": ...
      case ["go", direction]: ...
      case _ if x > 0: ...  # 守卫条件

- 类型联合运算符：int | str（替代Union[int, str]）
- ParamSpec和TypeVarTuple：更强大的泛型
- 结构化模式匹配：匹配字典、列表、对象结构
- 更好的错误信息：精确定位语法错误
- Python 3.11+：
  - ExceptionGroup和except*：多个异常处理
  - tomllib：内置TOML解析
  - 速度提升10-60%
- Python 3.12+：
  - 类型参数语法：type Alias = int | str
  - 更好的f-string（支持引号等）
  - 从标准库移除distutils}}

{{知识点51}}{{常用第三方库推荐
Web开发：
- Flask：轻量Web框架
- Django：全功能Web框架
- FastAPI：高性能API框架，支持异步

数据处理：
- pandas：数据处理与分析
- numpy：数值计算
- matplotlib/seaborn：数据可视化

爬虫：
- requests：HTTP请求
- BeautifulSoup4：HTML解析
- Scrapy：爬虫框架
- Selenium/Playwright：浏览器自动化

机器学习：
- scikit-learn：机器学习
- PyTorch/TensorFlow：深度学习

其他：
- Pillow：图像处理
- SQLAlchemy：ORM框架
- Celery：分布式任务队列
- Typer/Click：命令行工具
- Rich：终端美化输出
- Pydantic：数据验证}}

{{知识点52}}{{Python代码规范（PEP 8）
- 缩进：4个空格，不用Tab
- 行宽：最大79字符（代码），72字符（文档）
- 空行：顶层函数/类之间空2行，方法之间空1行
- 导入顺序：标准库→第三方库→本地模块，各组之间空一行
- 命名规范：
  - 变量/函数：snake_case
  - 类：PascalCase
  - 常量：UPPER_CASE
  - 私有：_leading_underscore
- 空格：运算符两侧、逗号后加空格
- 注释：行注释用#，文档字符串用三引号
- 工具：
  - black：自动格式化
  - isort：导入排序
  - flake8/pylint：代码检查
  - mypy：类型检查
  - ruff：All-in-one linter（推荐）}}

{{知识点53}}{{Python调试技巧
1. print调试：简单直接，临时使用
2. breakpoint()（Python 3.7+）：内置断点函数
3. pdb调试器：
   - python -m pdb script.py
   - n(下一行)、s(进入函数)、c(继续)
   - p(打印)、l(列出代码)、w(调用栈)
4. ipdb：增强版pdb，支持语法高亮和自动补全
5. IDE调试：PyCharm、VSCode内置调试器
6. logging模块：比print更正式
7. traceback模块：格式化异常信息
8. faulthandler：定位段错误
9. memory_profiler：内存分析
10. py-spy：采样分析器，无需修改代码
11. icecream库：ic()替代print()，显示变量名和值
12. rich.pretty：美化输出复杂数据结构}}

{{知识点54}}{{Python打包与发布
1. 准备pyproject.toml：
   [build-system]
   requires = ["setuptools>=61.0"]
   build-backend = "setuptools.backends._legacy:_Backend"
   
   [project]
   name = "my-package"
   version = "0.1.0"
   description = "My package description"

2. 构建：
   python -m build

3. 发布到PyPI：
   pip install twine
   twine upload dist/*

4. 使用poetry（推荐）：
   poetry init、poetry build、poetry publish

5. 版本号规范（语义化版本）：
   - MAJOR.MINOR.PATCH（如 1.2.3）
   - MAJOR：不兼容的API变更
   - MINOR：向下兼容的功能新增
   - PATCH：向下兼容的问题修正

6. LICENSE：选择开源许可证（MIT、Apache 2.0等）}}

{{知识点55}}{{Python常见陷阱与最佳实践
1. 可变默认参数陷阱：
   def f(lst=[]): lst.append(1); return lst
   # 正确：def f(lst=None): lst = lst or []

2. 循环中修改列表：
   for i in lst: lst.remove(i)  # 不安全
   # 正确：lst = [x for x in lst if condition]

3. 浅拷贝陷阱：
   a = [[]] * 3  # 三个引用指向同一列表
   # 正确：a = [[] for _ in range(3)]

4. 浮点数精度：0.1 + 0.2 != 0.3
   # 使用decimal模块或round()

5. is vs ==：is比较地址，==比较值

6. try-except范围过大：只捕获可能抛出异常的代码

7. 避免使用eval()和exec()

8. with语句确保资源释放

9. 使用enumerate()而非手动管理索引

10. 优先使用列表推导式，但避免过长的推导式}}
