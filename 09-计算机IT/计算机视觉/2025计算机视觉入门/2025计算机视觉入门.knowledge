# 2025计算机视觉入门
# 领域: 计算机IT
# 学科: 计算机视觉
# 来源: 2025计算机视觉入门
# 描述: 计算机视觉核心知识点

{{像素与图像表示}}
图像是由像素（Pixel）组成的二维矩阵。灰度图每个像素用一个0-255的值表示亮度；彩色图通常使用RGB三通道，每个通道各一个0-255的值。常见格式包括BMP（无压缩）、JPEG（有损压缩）、PNG（无损压缩）、TIFF（高质量存储）。图像分辨率用像素数表示，如1920×1080即约200万像素。2025年，高分辨率图像（4K/8K）和HDR图像在CV任务中越来越普遍。

{{色彩空间}}
色彩空间是描述颜色的数学模型。RGB是最常见的加色模型，适用于屏幕显示；HSV（色相-饱和度-明度）更符合人类直觉，便于颜色分割；YCbCr将亮度与色度分离，广泛用于视频编码；Lab色彩空间具有感知均匀性，适合颜色差异计算。在CV任务中，选择合适的色彩空间能显著提升特征提取效果。

{{图像预处理基础}}
图像预处理是CV流水线的第一步。常见操作包括：灰度化（将彩色图转为灰度图）、归一化（将像素值缩放到0-1或标准化为零均值单位方差）、去噪（高斯滤波、中值滤波、双边滤波）、直方图均衡化（增强对比度）、图像缩放与裁剪。良好的预处理能提升后续模型的准确性和鲁棒性。

{{图像滤波与卷积}}
图像滤波通过卷积核（Kernel）对图像进行局部运算。均值滤波用于平滑降噪；高斯滤波保留边缘的同时去除高斯噪声；Sobel算子和Prewitt算子用于边缘检测；Laplacian算子检测二阶导数变化；锐化滤波增强图像细节。卷积是CNN的核心操作基础，理解传统卷积有助于深入理解深度学习中的卷积层。

{{边缘检测}}
边缘检测是识别图像中亮度急剧变化区域的技术。Canny边缘检测器是最经典的方法，包括高斯平滑、梯度计算、非极大值抑制和双阈值边缘跟踪四个步骤。Sobel算子分别计算水平和垂直方向的梯度。Hough变换可从边缘点中检测直线和圆等几何形状。边缘信息是目标识别和图像分割的重要特征。

{{形态学操作}}
形态学操作基于形状对二值图像进行处理。腐蚀（Erosion）缩小白色区域，消除小噪点；膨胀（Dilation）扩大白色区域，填补小孔洞；开运算（先腐蚀后膨胀）去除小物体；闭运算（先膨胀后腐蚀）填充小孔洞；形态学梯度（膨胀减腐蚀）提取边缘轮廓。这些操作在图像分割后处理中非常常用。

{{特征检测与描述}}
传统特征检测方法包括：Harris角点检测（基于自相关矩阵的特征值判断角点）、SIFT（尺度不变特征变换，对旋转和尺度具有不变性）、SURF（加速稳健特征，比SIFT更快）、ORB（Oriented FAST and Rotated BRIEF，免费且高效）。特征描述子将局部区域编码为向量，用于图像匹配、拼接和目标识别。

{{图像变换}}
常见的图像变换包括：傅里叶变换（将图像从空间域转到频率域，分析频率成分）、霍夫变换（检测直线和圆等几何形状）、仿射变换（平移、旋转、缩放、剪切，保持平行性）、透视变换（模拟视角变化，用于文档校正和增强现实）、小波变换（多分辨率分析，适用于图像压缩和去噪）。

{{直方图与图像分析}}
图像直方图统计像素值分布，是图像分析的基础工具。灰度直方图反映亮度分布；颜色直方图描述颜色组成。直方图可用于图像分割（阈值选取）、图像检索（直方图比较）、对比度评估。常用比较方法包括相关性、卡方检验、交叉和巴氏距离。直方图反投影可用于目标跟踪。

{{图像分类基础}}
图像分类是将图像分配到预定义类别的任务。传统方法包括基于特征提取（SIFT/HOG）+分类器（SVM/KNN）的流水线。2025年，深度学习方法已成为主流，CNN及其变体在图像分类任务上远超传统方法。评估指标包括准确率、精确率、召回率、F1分数、混淆矩阵和ROC-AUC曲线。

{{卷积神经网络（CNN）基础}}
CNN是计算机视觉的核心架构。基本组件包括：卷积层（提取局部特征）、激活函数（引入非线性，常用ReLU）、池化层（降维，常用最大池化和平均池化）、全连接层（分类决策）。卷积操作的三个关键参数是卷积核大小、步长（Stride）和填充（Padding）。感受野（Receptive Field）描述每个输出神经元对应的输入区域大小。

{{经典CNN架构}}
里程碑式的CNN架构：LeNet-5（1998，手写数字识别）→ AlexNet（2012，ReLU和Dropout）→ VGGNet（2014，小卷积核堆叠）→ GoogLeNet/Inception（2014，多尺度并行结构）→ ResNet（2015，残差连接解决梯度消失）→ DenseNet（2017，密集连接）→ EfficientNet（2019，复合缩放）→ ConvNeXt（2022，现代化CNN）。每代架构都推动了CV领域的进展。

{{残差连接与跳跃连接}}
ResNet引入的残差连接（Skip Connection）是深度学习最重要的创新之一。通过恒等映射 F(x) + x，使网络可以训练到数百甚至上千层。残差连接缓解了梯度消失问题，加速了训练收敛。U-Net中的跳跃连接将编码器特征直接传递给解码器，保留了空间细节信息，对语义分割至关重要。

{{迁移学习}}
迁移学习利用在大规模数据集（如ImageNet）上预训练的模型，将其知识迁移到新任务。常见策略：特征提取（冻结预训练层，只训练新分类头）和微调（解冻部分或全部预训练层，用较小学习率继续训练）。迁移学习大幅减少了对标注数据的需求，是2025年CV项目中最常用的起步策略。

{{数据增强}}
数据增强通过对训练图像施加变换来扩充数据集，提升模型泛化能力。基本增强包括：随机翻转、旋转、裁剪、缩放、颜色抖动、亮度对比度调整。高级增强：Mixup（混合两张图像）、CutMix（区域替换）、Mosaic（四图拼接）、Cutout/Random Erasing（随机遮挡）。2025年，自动增强（AutoAugment）策略可根据数据集自动搜索最优增强方案。

{{目标检测概述}}
目标检测是在图像中定位并分类感兴趣物体的任务，输出为边界框（Bounding Box）+类别+置信度。评估指标采用mAP（mean Average Precision），基于IoU（交并比）阈值判断检测是否正确。发展历程从传统方法（Viola-Jones、HOG+SVM）到深度学习方法，分为两阶段和单阶段两大类。

{{两阶段目标检测器}}
两阶段检测器先生成候选区域，再进行分类和回归。R-CNN（2014）使用选择性搜索提取候选区域，CNN提取特征，SVM分类。Fast R-CNN引入RoI Pooling，共享卷积计算。Faster R-CNN（2015）引入区域提议网络（RPN），实现端到端训练。FPN（特征金字塔网络）通过多尺度特征融合提升小目标检测效果。

{{单阶段目标检测器}}
单阶段检测器直接在特征图上预测边界框和类别，速度更快。YOLO系列是代表：YOLOv1开创单阶段范式；YOLOv3引入多尺度预测；YOLOv5工程化优化；YOLOv8（2023）采用Anchor-Free设计；YOLOv11/YOLO-World（2024-2025）支持开放词汇检测。SSD在多尺度特征图上检测。RetinaNet引入Focal Loss解决正负样本不均衡问题。

{{Anchor机制与Anchor-Free}}
Anchor-Based方法预定义一组不同大小和长宽比的锚框，模型预测相对于锚框的偏移量。优点是召回率高，缺点是超参数多、计算量大。Anchor-Free方法（如FCOS、CenterNet、CornerNet）直接预测关键点或中心点，无需锚框，结构更简洁。2025年主流检测器多采用Anchor-Free设计，性能与效率兼顾。

{{目标检测后处理}}
NMS（非极大值抑制）是目标检测的标准后处理步骤：按置信度排序，抑制IoU超过阈值的冗余框。Soft-NMS通过降低分数替代直接抑制，改善密集场景检测。DIoU-NMS考虑中心点距离。测试时增强（TTA）通过对测试图像进行多尺度翻转等变换并融合预测结果来提升检测精度。

{{语义分割}}
语义分割对图像中的每个像素进行分类，输出与输入同尺寸的分割图。经典架构：FCN（全卷积网络，将分类网络改为全卷积）、U-Net（编码器-解码器+跳跃连接，医学图像分割标杆）、DeepLab系列（空洞卷积+ASPP多尺度特征融合+CRF后处理）。评估指标包括像素准确率、mIoU（平均交并比）和Dice系数。

{{实例分割}}
实例分割同时解决检测和分割问题，区分同一类别的不同实例。Mask R-CNN在Faster R-CNN基础上增加分割分支，是经典方法。YOLACT提出实时实例分割。SOLOv2基于位置分割。2025年，SAM（Segment Anything Model）等基础模型实现了零样本分割，可对任意物体生成高质量掩码，极大推动了分割技术的普及。

{{全景分割}}
全景分割统一了语义分割和实例分割，为每个像素分配一个语义标签和实例ID。Stuff类（天空、道路等不可数区域）只标注语义标签，Thing类（汽车、行人等可数物体）还需区分实例。Panoptic FPN和MaskFormer是代表性方法。Panoptic Quality（PQ）是标准评估指标，综合考虑分割质量和识别质量。

{{OCR文字识别}}
OCR（光学字符识别）将图像中的文字转换为可编辑文本。流程通常包括：文本检测（定位文字区域，如EAST、CRAFT）→ 文本识别（识别字符序列，如CRNN+CTC、Attention-based方法）。2025年，端到端OCR模型（如PaddleOCR、EasyOCR、TrOCR）可直接从图像输出文本，支持多语言、手写体和场景文字识别。

{{图像生成基础}}
图像生成模型学习数据分布并生成新的图像。主要方法：GAN（生成对抗网络，生成器与判别器博弈训练）、VAE（变分自编码器，学习隐空间分布）、扩散模型（Diffusion Model，通过逐步去噪生成图像）。2025年，扩散模型（如Stable Diffusion、DALL-E 3、Midjourney）已成为图像生成的主流范式，生成质量极高。

{{GAN基础}}
GAN由Goodfellow于2014年提出。生成器G从噪声生成假图像，判别器D判断图像真假。两者对抗训练，目标是生成器生成以假乱真的图像。经典变体：DCGAN（卷积架构）、WGAN（Wasserstein距离改进训练稳定性）、StyleGAN（风格控制，高质量人脸生成）、Pix2Pix（图像翻译）、CycleGAN（无配对图像风格转换）。GAN训练不稳定和模式崩塌是主要挑战。

{{扩散模型原理}}
扩散模型通过前向过程逐步向图像添加高斯噪声，再通过反向过程逐步去噪恢复图像。训练时学习去噪网络（通常为U-Net），推理时从纯噪声出发迭代去噪生成图像。关键技术：DDPM（去噪扩散概率模型）、DDIM（加速采样）、Latent Diffusion（在隐空间操作，大幅降低计算量）、Classifier-Free Guidance（提升条件生成质量）。

{{图像超分辨率}}
图像超分辨率从低分辨率图像重建高分辨率图像。传统方法包括双线性/双三次插值。深度学习方法：SRCNN（开创性CNN方法）、ESPCN（亚像素卷积）、EDSR（增强深度超分）、Real-ESRGAN（真实世界超分）、SwinIR（基于Transformer）。2025年的超分模型可生成8倍甚至更高倍率的高质量放大结果。

{{视频分析基础}}
视频分析将CV从静态图像扩展到时间维度。基本任务包括：动作识别（识别视频中的动作类别）、视频目标检测（在视频帧中检测物体）、视频目标跟踪（跟踪物体在帧间的运动轨迹）、视频分割（对视频帧进行语义/实例分割）。关键挑战是时间建模和计算效率，因为视频数据量远大于单张图像。

{{目标跟踪}}
目标跟踪在视频序列中持续定位目标物体。传统方法：KCF（核化相关滤波器）、MOSSE（最小输出平方误差和）。深度学习方法：SORT/DeepSORT（检测+关联，使用卡尔曼滤波和匈牙利算法）、TransT（Transformer跟踪器）、OSTrack（单流Transformer）。评估指标包括跟踪精度（Precision）和成功率（Success Rate），常用数据集有OTB、LaSOT、GOT-10k。

{{动作识别}}
动作识别是理解视频内容的核心任务。方法分为空间流（单帧外观）和时间流（光流运动信息）的双流架构、3D卷积网络（C3D、I3D直接在时空维度卷积）、时序建模（TSN分段采样、SlowFast快慢路径）。2025年，Video Vision Transformer（Video ViT）和预训练视频基础模型成为主流，在Kinetics等数据集上达到极高准确率。

{{3D视觉基础}}
3D视觉从2D图像或传感器数据中恢复三维结构。主要数据表示：点云（Point Cloud，无序3D点集合）、深度图（Depth Map，每个像素记录距离）、体素（Voxel，3D网格）、网格（Mesh，顶点+面片）、NeRF（神经辐射场）。传感器包括双目相机、结构光相机（如Kinect）、LiDAR激光雷达和ToF相机。

{{点云处理}}
点云是3D视觉的核心数据格式。PointNet开创了直接处理点云的深度学习方法，通过共享MLP和对称函数（最大池化）保证置换不变性。PointNet++引入层次化特征学习。2025年，Point Transformer和基于Transformer的点云模型广泛应用。点云任务包括分类、分割、检测和配准，应用涵盖自动驾驶和机器人导航。

{{深度估计}}
深度估计从单张或多张图像中预测每个像素的深度。单目深度估计最具挑战性，MiDaS和Depth Anything等模型在2025年实现了强大的零样本深度估计能力。双目深度估计通过立体匹配计算视差。多视图深度估计利用多张图像的几何关系。深度信息对自动驾驶、增强现实和机器人抓取至关重要。

{{相机模型与几何}}
针孔相机模型描述了3D点到2D图像的投影关系，包含内参矩阵（焦距、主点）和外参矩阵（旋转、平移）。畸变模型包括径向畸变和切向畸变。对极几何描述了两视图间的约束关系，本质矩阵（E）和基础矩阵（F）编码了这种约束。相机标定（如棋盘格法）是3D视觉的基础步骤。

{{视觉Transformer（ViT）}}
Vision Transformer将NLP中的Transformer架构引入视觉领域。ViT将图像分割为固定大小的Patch，展平为序列后输入标准Transformer编码器。核心组件：多头自注意力（MHSA）捕获全局关系，位置编码保留空间信息。后续发展：DeiT（数据高效训练）、Swin Transformer（窗口注意力+层次化结构）、BEiT（自监督预训练）。2025年，ViT已成为与CNN并列的主流视觉骨干网络。

{{视觉语言模型}}
视觉语言模型（VLM）连接视觉和语言模态。CLIP通过对比学习对齐图像和文本表示，实现零样本分类。BLIP/BLIP-2统一图像描述和视觉问答。LLaVA等多模态大模型将视觉编码器与LLM结合，实现视觉理解和对话。2025年，GPT-4V、Gemini等多模态模型能理解复杂视觉场景并进行推理，推动了通用视觉智能的发展。

{{视觉基础模型}}
2023-2025年，视觉基础模型（Foundation Model）成为趋势。SAM（Segment Anything）可分割任意物体；DINOv2学习通用视觉特征；Grounding DINO结合文本实现开放词汇检测；SAM 2扩展到视频分割。这些模型在大规模数据上预训练，可通过提示（Prompt）适配各种下游任务，减少了对任务特定模型的需求。

{{自监督学习}}
自监督学习利用无标注数据学习视觉表示。对比学习方法（SimCLR、MoCo）通过正负样本对学习不变性。掩码图像建模（MAE、BEiT）遮挡图像区域并重建。DINO/DINOv2通过自蒸馏学习语义特征。自监督预训练减少了对标注数据的依赖，学到的特征在下游任务中表现出色，是2025年CV研究的热点方向。

{{模型评估与指标}}
CV模型评估需选择合适的指标。分类任务：准确率、精确率、召回率、F1、Top-k准确率。检测任务：mAP@IoU（通常IoU=0.5或0.5:0.95）。分割任务：mIoU、Dice系数、像素准确率。生成任务：FID（Fréchet Inception Distance）、IS（Inception Score）。跟踪任务：HOTA。还需要关注推理速度（FPS）、模型大小（参数量/MACs）和鲁棒性。

{{深度学习框架与工具}}
2025年主流CV开发工具：PyTorch（最流行的深度学习框架，动态图）、TensorFlow/Keras（Google生态）、JAX（函数式编程，适合研究）。高级库：Hugging Face Transformers（预训练模型）、OpenMMLab（MMDetection/MMSegmentation等）、Ultralytics（YOLOv8/v11）、PaddlePaddle/PaddleOCR（百度生态）。数据标注工具：LabelImg、CVAT、Roboflow。

{{部署与优化}}
模型部署将训练好的模型投入实际应用。优化技术：量化（INT8/FP16降低精度换速度）、剪枝（移除冗余参数）、知识蒸馏（大模型指导小模型训练）。推理引擎：ONNX Runtime、TensorRT（NVIDIA GPU优化）、OpenVINO（Intel优化）、CoreML（Apple设备）、NCNN（移动端）。边缘部署需考虑算力、功耗和延迟的平衡。

{{数据集与基准}}
经典CV数据集：ImageNet（1000类分类，120万图像）、COCO（目标检测和分割，33万图像）、Pascal VOC（20类检测和分割）、ADE20K（150类语义分割）、Cityscapes（城市街景分割）、KITTI（自动驾驶，检测+深度+跟踪）、VOT（目标跟踪）。2025年，大规模多模态数据集（如LAION）支撑了基础模型的预训练。

{{自动驾驶中的CV}}
自动驾驶是CV最重要的应用领域之一。核心任务：2D/3D目标检测（车辆、行人、交通标志）、车道线检测、可行驶区域分割、交通标志识别、深度估计。传感器方案：纯视觉（Tesla）、多传感器融合（LiDAR+Camera）。BEV（鸟瞰图）感知范式（如BEVFormer、UniAD）在2025年成为主流，统一了多个感知任务。

{{医学图像分析}}
医学图像分析是CV的重要应用方向。任务包括：病变检测（肺结节、肿瘤）、器官分割（心脏、肝脏）、医学图像分类（皮肤癌筛查、视网膜病变分级）。常用模态：X光、CT、MRI、超声、病理切片。挑战包括标注数据稀缺、类别不均衡和领域迁移。U-Net及其变体仍是医学图像分割的主力架构。

{{人脸技术}}
人脸相关CV技术包括：人脸检测（MTCNN、RetinaFace）、人脸对齐（关键点定位）、人脸识别（ArcFace、CosFace，基于度量学习）、人脸属性分析（年龄、性别、表情）、人脸生成（StyleGAN系列）、人脸编辑（表情/年龄变换）。2025年的人脸技术已高度成熟，同时也引发了隐私和伦理方面的广泛讨论。

{{图像检索}}
图像检索从数据库中找到与查询图像相似的图像。基于内容的图像检索（CBIR）提取全局或局部特征进行相似度匹配。深度学习方法使用CNN提取紧凑的特征向量，通过余弦距离或欧氏距离检索。哈希学习将高维特征编码为短二进制码，加速大规模检索。CLIP等模型实现了文本-图像跨模态检索。

{{光学与成像基础}}
理解成像原理对CV至关重要。关键概念：焦距（决定视角和放大倍率）、光圈（控制进光量和景深）、快门速度（影响运动模糊和曝光）、ISO感光度（影响噪点）、景深（清晰范围）。畸变类型包括桶形畸变和枕形畸变。了解这些有助于处理实际成像问题，如低光增强、去雾和去模糊。

{{模型可解释性}}
模型可解释性帮助理解CV模型的决策过程。方法包括：Grad-CAM（可视化卷积层关注区域）、注意力可视化（显示Transformer的注意力权重）、SHAP/LIME（特征重要性分析）、对抗样本分析（发现模型脆弱点）。2025年，可解释性在医疗、自动驾驶等安全敏感领域越来越重要，也是AI伦理和监管的要求。
