# MySQL从入门到精通
# 领域: 计算机IT
# 学科: 数据库
# 来源: MySQL从入门到精通（2025版）
# 描述: MySQL关系型数据库从基础到高级应用，涵盖SQL语法、索引优化、事务机制、锁机制及高可用架构

{{SQL基础语法}}{{SQL分为DDL(数据定义：CREATE/ALTER/DROP)、DML(数据操作：INSERT/UPDATE/DELETE/SELECT)、DCL(数据控制：GRANT/REVOKE)、TCL(事务控制：COMMIT/ROLLBACK)。SELECT基本语法：SELECT columns FROM table WHERE condition ORDER BY column LIMIT n。聚合函数：COUNT/SUM/AVG/MAX/MIN配合GROUP BY。HAVING过滤分组结果。JOIN连接：INNER/LEFT/RIGHT/CROSS JOIN。}}

{{数据类型}}{{整数类型：TINYINT(1B)/SMALLINT(2B)/MEDIUMINT(3B)/INT(4B)/BIGINT(8B)。浮点：FLOAT(4B)/DOUBLE(8B)，DECIMAL精确小数。字符串：CHAR(定长)/VARCHAR(变长)/TEXT/BLOB。日期时间：DATE/TIME/DATETIME/TIMESTAMP(自动更新)。JSON类型(MySQL 5.7+)。ENUM枚举类型。选择原则：够用就好，越小越好。UNSIGNED无符号扩大正数范围。}}

{{表设计与约束}}{{PRIMARY KEY主键约束(唯一+非空)。UNIQUE唯一约束。NOT NULL非空约束。DEFAULT默认值。CHECK检查约束(MySQL 8.0+)。FOREIGN KEY外键约束(引用完整性)。AUTO_INCREMENT自增。范式化设计：1NF(原子性)→2NF(完全依赖)→3NF(消除传递依赖)。反范式化：适当冗余提升查询性能。ER图设计工具。}}

{{索引原理与优化}}{{B+树索引是MySQL默认索引结构(叶子节点有序链表)。聚簇索引(主键)vs非聚簇索引(二级索引)。覆盖索引：查询字段都在索引中，避免回表。联合索引遵循最左前缀原则。索引选择性：区分度高的列适合建索引。EXPLAIN分析查询计划：type(访问类型)、key(使用的索引)、rows(扫描行数)、Extra(额外信息)。避免索引失效：函数操作、隐式类型转换、LIKE '%前缀'、OR条件。}}

{{查询优化}}{{慢查询日志：slow_query_log=ON，long_query_time设置阈值。EXPLAIN/EXPLAIN ANALYZE分析执行计划。优化策略：避免SELECT *、合理使用索引、减少子查询改用JOIN、分页优化(延迟关联)、批量INSERT、COUNT优化。MySQL 8.0+窗口函数：ROW_NUMBER/RANK/DENSE_RANK/LAG/LEAD。CTE(公共表表达式)：WITH RECURSIVE递归查询。查询缓存(MySQL 8.0已移除)。}}

{{事务机制}}{{事务ACID特性：Atomicity(原子性)、Consistency(一致性)、Isolation(隔离性)、Durability(持久性)。隔离级别：READ UNCOMMITTED(脏读)→READ COMMITTED(不可重复读)→REPEATABLE READ(默认，幻读部分解决)→SERIALIZABLE(串行化)。MVCC(多版本并发控制)通过Undo Log实现。事务命令：START TRANSACTION/COMMIT/ROLLBACK。SAVEPOINT保存点。}}

{{锁机制}}{{MySQL锁粒度：表锁(MyISAM)、行锁(InnoDB)、页锁。InnoDB行锁类型：Record Lock(记录锁)、Gap Lock(间隙锁)、Next-Key Lock(临键锁=记录+间隙)。共享锁(S锁，SELECT ... LOCK IN SHARE MODE)和排他锁(X锁，SELECT ... FOR UPDATE)。乐观锁：版本号机制。死锁检测和处理：innodb_deadlock_detect。锁等待超时：innodb_lock_wait_timeout。}}

{{存储引擎}}{{InnoDB(默认)：支持事务、行锁、外键、MVCC、聚簇索引。MyISAM：不支持事务、表锁、全文索引(已被InnoDB取代)。Memory：内存引擎，重启数据丢失。Archive：高压缩比归档引擎。选择建议：绝大多数场景使用InnoDB。InnoDB架构：Buffer Pool(缓冲池)、Change Buffer、Log Buffer、Redo Log、Undo Log。}}

{{MySQL 8.0+新特性}}{{窗口函数、CTE、JSON增强函数、不可见索引(隐藏索引测试)、降序索引、角色(Role)权限管理、原子DDL、数据字典改进(INnoDB存储)、资源组管理、CHECK约束、默认字符集utf8mb4。MySQL 8.4 LTS长期支持版本。Hash Join优化。EXPLAIN ANALYZE实际执行分析。Instant DDL即时加列。多值索引(Multi-Valued Index)支持JSON数组。}}

{{备份与恢复}}{{逻辑备份：mysqldump(全库/单库/单表)、mysqlpump(并行导出)、mydumper/myloader(多线程)。物理备份：xtrabackup(Percona，支持增量备份)、MySQL Enterprise Backup。备份策略：完全备份+增量备份+binlog恢复。binlog：二进制日志记录所有数据变更(statement/row/mixed格式)。mysqlbinlog工具解析binlog。时间点恢复(PITR)。}}

{{主从复制}}{{异步复制：binlog从主库传到从库的relay log，SQL线程重放。半同步复制：至少一个从库确认接收binlog后主库才提交。GTID复制：全局事务标识简化复制管理。多源复制：从库从多个主库复制。延迟复制：CHANGE MASTER TO MASTER_DELAY。复制过滤：replicate-do-db/replicate-ignore-db。复制监控：SHOW SLAVE STATUS。}}

{{高可用架构}}{{MySQL Group Replication：多主/单主组复制，自动故障转移。MySQL InnoDB Cluster：Group Replication + MySQL Shell + MySQL Router。MySQL NDB Cluster：内存数据库集群。ProxySQL：智能代理，读写分离、查询路由、连接池。Orchestrator：拓扑管理和故障转移。MHA(MySQL High Availability)传统方案。Galera Cluster同步复制。}}

{{分库分表}}{{垂直分库：按业务拆分到不同数据库。垂直分表：大表拆分为多表(主表+扩展表)。水平分表：按规则(哈希/范围)将数据分散到多个表。分片中间件：ShardingSphere(Java生态首选)、Vitess(YouTube开源)、ProxySQL。分片键选择。跨分片查询和排序。分布式ID生成：Snowflake、UUID、数据库序列。数据迁移和扩容策略。}}

{{SQL注入防护}}{{SQL注入是最常见的Web安全漏洞。防护措施：参数化查询(PreparedStatement)、ORM框架(自动参数化)、输入验证和转义、最小权限原则(应用用户不给DROP权限)、WAF(Web应用防火墙)。注入类型：联合注入、布尔盲注、时间盲注、报错注入。安全编码：不要拼接SQL字符串。定期安全审计。MySQL安全配置：密码策略、权限最小化。}}

{{性能调优}}{{InnoDB Buffer Pool大小设置(物理内存的70-80%)。innodb_flush_log_at_trx_commit(1最安全/2性能优先)。sync_binlog(1最安全)。连接池配置：最大连接数、超时时间。临时表：tmp_table_size/max_heap_table_size。排序缓冲：sort_buffer_size。慢查询分析和优化。Performance Schema性能监控。sys schema简化监控查询。SHOW PROCESSLIST查看连接状态。}}

{{JSON数据操作}}{{MySQL 5.7+支持JSON类型。JSON函数：JSON_EXTRACT/->/->>(提取)、JSON_INSERT(插入)、JSON_REPLACE(替换)、JSON_REMOVE(删除)、JSON_SET(设置)、JSON_ARRAY/JSON_OBJECT(创建)。JSON表函数：JSON_TABLE将JSON转为关系表。多值索引优化JSON数组查询。JSON Schema验证。JSON聚合：JSON_ARRAYAGG/JSON_OBJECTAGG。}}

{{全文检索}}{{MySQL全文索引(FULLTEXT)支持InnoDB和MyISAM。CREATE FULLTEXT INDEX创建索引。MATCH ... AGAINST搜索。自然语言模式和布尔模式。ngram解析器支持中文全文检索。全文索引限制和优化。与Elasticsearch对比：MySQL全文检索适合简单场景，复杂搜索需求使用ES。Meilisearch/Typesense作为轻量替代。}}

{{MySQL与应用程序连接}}{{连接器/驱动：mysql-connector-java(Java)、mysql-connector-python(Python)、mysql2(Node.js)、go-sql-driver(Go)。连接池：HikariCP(Java)、Druid(阿里，监控强大)。连接参数：charset=utf8mb4、serverTimezone=UTC。ORM框架集成：Hibernate/JPA、SQLAlchemy、Prisma、GORM。读写分离配置。连接池监控和调优。}}

{{监控与运维}}{{SHOW STATUS查看服务器状态变量。SHOW VARIABLES查看配置变量。Performance Schema记录性能数据。sys schema提供易读视图。MySQL Enterprise Monitor商业监控。开源方案：PMM(Percona Monitoring and Management)、Zabbix、Prometheus+mysqld_exporter。关键指标：QPS/TPS、连接数、Buffer Pool命中率、锁等待、复制延迟。告警阈值设置。}}

{{字符集与排序规则}}{{utf8mb4是MySQL推荐字符集(完整UTF-8，支持emoji)。utf8是3字节UTF-8(不完整，已不推荐)。排序规则(collation)：utf8mb4_general_ci(不区分大小写)、utf8mb4_unicode_ci(Unicode标准)、utf8mb4_0900_ai_ci(MySQL 8.0默认)。字符集设置层级：服务器→数据库→表→列。连接字符集：SET NAMES utf8mb4。乱码问题排查。}}
