# 索引

## 索引类型
- B+Tree索引：默认索引类型，适合范围查询和排序
- 哈希索引：Memory引擎，等值查询O(1)，不支持范围
- 全文索引：FULLTEXT INDEX，文本搜索
- 空间索引：GIS数据索引
- 联合索引：多列组合索引，遵循最左前缀原则

## 索引优化
- 最左前缀原则：联合索引(a,b,c)，查询必须包含a
- 覆盖索引：查询字段都在索引中，避免回表
- 索引选择性：选择性高的列更适合建索引（唯一值/总行数）
- 前缀索引：对长字符串取前n个字符建索引
- 索引下推（ICP）：MySQL 5.6+，减少回表次数

## EXPLAIN分析
- type列：system>const>eq_ref>ref>range>index>ALL
- key列：实际使用的索引
- rows列：预估扫描行数
- Extra列：Using index（覆盖索引）、Using filesort（文件排序）
- possible_keys vs key：可能使用 vs 实际使用的索引

## 索引失效
- 对索引列使用函数：WHERE YEAR(create_time) = 2025
- 隐式类型转换：WHERE varchar_col = 123
- LIKE左模糊：WHERE name LIKE '%Tom'
- OR条件：部分条件无索引时全表扫描
- 不等于：!=、NOT IN可能不走索引

## 索引设计原则
- 选择区分度高的列作为索引
- 联合索引将区分度高的列放前面
- 避免过多索引：影响写入性能
- 长字符串用前缀索引
- 定期分析和优化索引：ANALYZE TABLE
