# SQL语法

## 数据定义（DDL）
- CREATE DATABASE/TABLE：创建数据库和表
- ALTER TABLE：ADD/MODIFY/DROP/RENAME COLUMN，修改表结构
- DROP TABLE/DATABASE：删除表/数据库
- 数据类型：INT、VARCHAR(n)、TEXT、DATE、DATETIME、DECIMAL、BLOB
- 约束：PRIMARY KEY、FOREIGN KEY、UNIQUE、NOT NULL、DEFAULT、CHECK

## 数据操作（DML）
- INSERT INTO：插入数据，支持批量插入
- UPDATE SET WHERE：更新数据，必须加WHERE避免全表更新
- DELETE FROM WHERE：删除数据，必须加WHERE
- REPLACE INTO：存在则更新，不存在则插入
- LOAD DATA INFILE：批量导入数据

## 数据查询（DQL）
- SELECT ... FROM ... WHERE ... GROUP BY ... HAVING ... ORDER BY ... LIMIT
- 聚合函数：COUNT、SUM、AVG、MAX、MIN
- 连接查询：INNER JOIN、LEFT JOIN、RIGHT JOIN、CROSS JOIN
- 子查询：WHERE/IN/EXISTS中的子查询
- UNION/UNION ALL：合并多个查询结果

## 函数与表达式
- 字符串：CONCAT、SUBSTRING、LENGTH、REPLACE、TRIM
- 数学：ROUND、CEIL、FLOOR、MOD、ABS
- 日期：NOW()、CURDATE()、DATE_FORMAT、DATEDIFF、DATE_ADD
- 条件：IF、CASE WHEN THEN ELSE END、IFNULL、COALESCE
- 窗口函数（MySQL 8.0+）：ROW_NUMBER、RANK、DENSE_RANK、LAG、LEAD

## 视图与存储过程
- 视图：CREATE VIEW AS SELECT，虚拟表，简化复杂查询
- 存储过程：CREATE PROCEDURE，封装SQL逻辑，减少网络交互
- 触发器：CREATE TRIGGER，INSERT/UPDATE/DELETE时自动执行
- 游标：DECLARE CURSOR，逐行处理结果集
- 事件调度器：CREATE EVENT，定时执行SQL
