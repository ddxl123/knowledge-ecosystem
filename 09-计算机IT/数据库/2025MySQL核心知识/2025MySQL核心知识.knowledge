# 2025MySQL核心知识
# 领域: 计算机IT
# 学科: 数据库
# 来源: 2025MySQL核心知识
# 描述: MySQL数据库核心知识点

{{知识点1}}{{MySQL的存储引擎有哪些，各自特点是什么？
MySQL主要存储引擎包括：InnoDB（支持事务、行级锁、外键，MySQL 5.5+默认引擎）、MyISAM（不支持事务，表级锁，读性能好）、Memory（数据存内存，重启丢失）、Archive（只支持INSERT和SELECT，适合归档）、NDB（集群引擎，高可用）。生产环境首选InnoDB。}}

{{知识点2}}{{MySQL中VARCHAR和CHAR的区别？
CHAR是定长字符串，最大255字符，存储时右侧补空格，检索时去除尾部空格，适合长度固定的字段（如MD5值）。VARCHAR是变长字符串，最大65535字节，需要1-2字节记录长度，适合长度不固定的字段。CHAR查询速度略快，VARCHAR更节省空间。}}

{{知识点3}}{{MySQL整型类型有哪些？
TINYINT(1字节)、SMALLINT(2字节)、MEDIUMINT(3字节)、INT/INTEGER(4字节)、BIGINT(8字节)。每种都有UNSIGNED可选，表示无符号。INT(11)中的11只是显示宽度，不影响存储范围。}}

{{知识点4}}{{MySQL中FLOAT、DOUBLE和DECIMAL的区别？
FLOAT占4字节，精度约7位；DOUBLE占8字节，精度约15位；DECIMAL是定点数，精度由用户指定（如DECIMAL(10,2)），用于存储精确数值如金额。FLOAT和DOUBLE存在浮点精度问题，不适合财务计算。}}

{{知识点5}}{{什么是MySQL的三大范式？
第一范式（1NF）：字段不可再分，每列原子性。第二范式（2NF）：在1NF基础上，非主键字段完全依赖于主键（消除部分依赖）。第三范式（3NF）：在2NF基础上，非主键字段不能传递依赖于主键（消除传递依赖）。实际设计中会适当反范式化以提高查询性能。}}

{{知识点6}}{{MySQL中DROP、DELETE和TRUNCATE的区别？
DROP删除表结构和数据，不可回滚，释放空间。DELETE删除行数据，可加WHERE条件，DML操作可回滚，不释放空间。TRUNCATE清空表数据，DDL操作不可回滚，重置自增ID，释放空间。性能：DROP > TRUNCATE > DELETE。}}

{{知识点7}}{{MySQL中UNION和UNION ALL的区别？
UNION对结果集去重排序，有性能开销。UNION ALL直接合并所有结果不去重，性能更好。如果确定结果无重复或不需要去重，优先使用UNION ALL。}}

{{知识点8}}{{MySQL中JOIN有哪些类型？
INNER JOIN（内连接，返回两表匹配行）、LEFT JOIN（左连接，返回左表所有行+右表匹配行）、RIGHT JOIN（右连接，返回右表所有行+左表匹配行）、CROSS JOIN（交叉连接，笛卡尔积）、FULL OUTER JOIN（MySQL不直接支持，可通过UNION模拟）。自连接是表与自身的连接。}}

{{知识点9}}{{MySQL索引的数据结构是什么？
InnoDB使用B+树索引。B+树特点：非叶子节点只存key不存data，叶子节点存所有数据且用双向链表连接，适合范围查询和磁盘IO优化。聚簇索引的叶子节点存完整行数据，非聚簇索引（二级索引）叶子节点存主键值（需回表）。}}

{{知识点10}}{{什么是聚簇索引和非聚簇索引？
聚簇索引：叶子节点包含完整行数据，InnoDB表只有一个聚簇索引（通常是主键），数据按主键顺序物理存储。非聚簇索引（二级索引）：叶子节点存储主键值，查询非索引列时需要回表。覆盖索引可以避免回表。}}

{{知识点11}}{{什么是覆盖索引？
覆盖索引是指查询所需的列都包含在索引中，无需回表查询主键索引。例如索引(name, age)，查询SELECT name, age FROM user WHERE name='Tom'只需扫描索引即可，EXPLAIN中显示Using index。可显著减少IO，是常用优化手段。}}

{{知识点12}}{{什么是索引下推（ICP）？
Index Condition Pushdown是MySQL 5.6引入的优化。在使用联合索引时，即使索引不能完全确定查询条件，也可以在存储引擎层利用索引中的列提前过滤，减少回表次数。EXPLAIN中显示Using index condition。}}

{{知识点13}}{{索引失效的常见场景有哪些？
1) 对索引列使用函数或运算；2) 隐式类型转换（如varchar列用int查询）；3) LIKE以%开头；4) OR条件中有非索引列；5) 联合索引未遵循最左前缀原则；6) 使用!=或NOT IN（可能）；7) IS NULL/IS NOT NULL（视数据分布）；8) 数据量小时优化器选择全表扫描。}}

{{知识点14}}{{什么是联合索引的最左前缀原则？
联合索引(a,b,c)相当于创建了(a)、(a,b)、(a,b,c)三个索引。查询条件必须从最左列开始连续使用才能命中索引。WHERE a=1 AND b=2可命中，WHERE b=2 AND c=3不能命中（跳过了a）。WHERE a=1 AND c=3只能命中a。}}

{{知识点15}}{{什么是事务？事务的四大特性（ACID）？
事务是一组不可分割的数据库操作序列。ACID：Atomicity（原子性，全部成功或全部回滚）、Consistency（一致性，事务前后数据满足约束）、Isolation（隔离性，事务间互不干扰）、Durability（持久性，提交后永久生效）。InnoDB通过undo log保证原子性，redo log保证持久性，锁和MVCC保证隔离性。}}

{{知识点16}}{{MySQL的四种事务隔离级别？
1) READ UNCOMMITTED（读未提交，可能脏读）；2) READ COMMITTED（读已提交，解决脏读，可能不可重复读，Oracle默认）；3) REPEATABLE READ（可重复读，解决不可重复读，InnoDB默认，通过MVCC+间隙锁部分解决幻读）；4) SERIALIZABLE（串行化，完全隔离，性能最差）。}}

{{知识点17}}{{什么是脏读、不可重复读和幻读？
脏读：读到其他事务未提交的数据。不可重复读：同一事务内两次读同一行数据结果不同（被其他事务UPDATE）。幻读：同一事务内两次相同范围查询结果行数不同（被其他事务INSERT）。InnoDB默认RR级别通过MVCC+Next-Key Lock解决大部分幻读。}}

{{知识点18}}{{MySQL的MVCC机制是什么？
Multi-Version Concurrency Control，多版本并发控制。InnoDB每行数据有两个隐藏列：trx_id（最近修改的事务ID）和roll_pointer（指向undo log的指针）。通过undo log版本链和ReadView（快照读时生成）实现非锁定一致性读，读不加锁，提高并发性能。}}

{{知识点19}}{{ReadView的原理是什么？
ReadView包含：m_ids（生成时活跃事务ID列表）、min_trx_id（最小活跃事务ID）、max_trx_id（下一个分配的事务ID）、creator_trx_id（创建者事务ID）。判断可见性：trx_id < min_trx_id则可见；trx_id >= max_trx_id则不可见；在m_ids中则不可见（事务未提交）。RC每次读创建新ReadView，RR只在第一次读时创建。}}

{{知识点20}}{{InnoDB有哪些锁类型？
1) 共享锁（S Lock，读锁）和排他锁（X Lock，写锁）；2) 意向锁（IS、IX，表级，标识事务意图）；3) 记录锁（Record Lock，锁定单行）；4) 间隙锁（Gap Lock，锁定索引记录间的间隙，防止幻读）；5) 临键锁（Next-Key Lock = Record Lock + Gap Lock，左开右闭）；6) 插入意向锁（INSERT时使用）。}}

{{知识点21}}{{什么是死锁？如何解决和预防？
死锁是两个或多个事务互相等待对方释放锁。InnoDB自动检测死锁并回滚代价较小的事务。预防：1) 按固定顺序访问表和行；2) 保持事务短小；3) 使用合理索引减少锁范围；4) 降低隔离级别；5) 设置锁等待超时（innodb_lock_wait_timeout）。通过SHOW ENGINE INNODB STATUS查看死锁信息。}}

{{知识点22}}{{MySQL中乐观锁和悲观锁是什么？
悲观锁：假设会冲突，操作前先加锁（SELECT ... FOR UPDATE），适合写多读少。乐观锁：假设不冲突，提交时检查版本号或时间戳，冲突则重试，适合读多写少。乐观锁实现通常在表中加version字段，UPDATE时WHERE version=旧值。}}

{{知识点23}}{{EXPLAIN执行计划中各字段含义？
type（访问类型，从优到差：system>const>eq_ref>ref>range>index>ALL）、key（实际使用的索引）、rows（预估扫描行数）、Extra（重要信息：Using index=覆盖索引、Using filesort=文件排序、Using temporary=使用临时表、Using index condition=索引下推）、possible_keys（可能使用的索引）、filtered（过滤百分比）。}}

{{知识点24}}{{MySQL查询优化的常见手段？
1) 避免SELECT *，只查需要的列；2) 优化JOIN，小表驱动大表；3) 合理创建索引，覆盖高频查询；4) 避免在WHERE中对索引列做函数运算；5) 分页优化（延迟关联或游标分页）；6) 避免OR，改用UNION ALL；7) 合理使用缓存；8) 大表分库分表。}}

{{知识点25}}{{如何优化大表分页查询？
传统LIMIT offset, size在offset大时性能差（需扫描offset+size行后丢弃前offset行）。优化方案：1) 游标分页：WHERE id > last_id LIMIT size；2) 延迟关联：先查主键再JOIN获取数据；3) 业务层面限制翻页深度；4) 使用Elasticsearch等搜索引擎辅助。}}

{{知识点26}}{{什么是慢查询？如何定位和优化？
慢查询是执行时间超过long_query_time（默认10秒）的SQL。开启慢查询日志：slow_query_log=ON。定位：1) 分析慢查询日志；2) 使用mysqldumpslow工具；3) EXPLAIN分析执行计划。优化：添加合适索引、优化SQL写法、优化表结构、读写分离、分库分表。}}

{{知识点27}}{{MySQL的Buffer Pool是什么？
Buffer Pool是InnoDB在内存中开辟的缓冲区，用于缓存数据页和索引页，减少磁盘IO。默认128MB，生产环境通常设为物理内存的60%-80%。通过LRU（最近最少使用）算法管理，使用改进的LRU（young区+old区）避免预读和全表扫描污染缓存。可通过SHOW ENGINE INNODB STATUS查看命中率。}}

{{知识点28}}{{redo log和undo log的作用？
redo log（重做日志）：记录数据页的物理修改，用于崩溃恢复，保证持久性（WAL机制：先写日志再写磁盘）。循环写入，空间固定。undo log（回滚日志）：记录数据修改前的值，用于事务回滚和MVCC版本链。逻辑日志，产生undo log时也会产生redo log。binlog是Server层的逻辑日志，用于主从复制和数据恢复。}}

{{知识点29}}{{binlog的三种格式？
1) Statement：记录SQL语句，日志量小但可能主从不一致（如NOW()）；2) Row：记录行数据变更，日志量大但一致性好，推荐用于主从复制；3) Mixed：默认Statement，不安全时自动切Row。生产推荐Row格式。binlog用于主从复制和基于时间点的数据恢复。}}

{{知识点30}}{{MySQL主从复制原理？
三个线程协作：1) Master的Binlog Dump Thread发送binlog事件；2) Slave的IO Thread接收并写入relay log；3) Slave的SQL Thread读取relay log并重放。异步复制有延迟，半同步复制（Semi-sync）保证至少一个slave确认收到。GTID复制简化了故障切换和位点管理。}}

{{知识点31}}{{MySQL主从延迟的原因和解决方案？
原因：从库单线程重放、大事务、从库性能差、网络延迟。解决方案：1) 开启并行复制（slave_parallel_workers）；2) 从库配置更好的硬件；3) 避免大事务拆分小事务；4) 半同步复制保证数据一致性；5) 读写分离+强制走主库的关键读；6) 使用GTID复制。}}

{{知识点32}}{{MySQL分库分表的策略？
垂直拆分：按业务模块拆库（如用户库、订单库），按字段拆表（如text/blob字段分表）。水平拆分：按行拆分，策略包括：范围分片（如按时间或ID范围）、哈希分片（如user_id % N）、一致性哈希。中间件：ShardingSphere、MyCat。分片后跨库JOIN和分布式事务是难点。}}

{{知识点33}}{{InnoDB的表空间结构？
表空间由段（segment）、区（extent，1MB=64个16KB页）、页（page，16KB基本单位）、行组成。系统表空间（ibdata1）存储数据字典、undo log等。独立表空间（innodb_file_per_table=ON，每表一个.ibd文件）是默认配置。Undo表空间和临时表空间可独立管理。}}

{{知识点34}}{{MySQL的binlog、redo log、undo log三种日志的区别？
binlog：Server层，逻辑日志，记录SQL或行变更，用于复制和恢复。redo log：InnoDB引擎层，物理日志，记录页修改，用于崩溃恢复（WAL）。undo log：InnoDB引擎层，逻辑日志，记录反向操作，用于回滚和MVCC。两阶段提交保证redo log和binlog一致性。}}

{{知识点35}}{{什么是两阶段提交（2PC）？
InnoDB写入redo log分两步：prepare阶段写redo log并标记prepare状态，commit阶段先写binlog再标记redo log为commit。崩溃恢复时：redo log为prepare且binlog存在则提交，否则回滚。保证redo log和binlog的一致性。}}

{{知识点36}}{{MySQL中的临时表使用场景和注意事项？
使用场景：UNION中间结果、子查询物化、GROUP BY/ORDER BY无法使用索引时、派生表。内部临时表可使用Memory引擎（tmp_table_size限制）或TempTable引擎（MySQL 8.0默认，支持更多类型）。注意：避免大数据量的临时表，会导致磁盘临时表，性能下降。EXPLAIN的Extra中出现Using temporary需关注。}}

{{知识点37}}{{MySQL中GROUP BY的优化？
1) 确保GROUP BY字段有索引（利用索引有序性避免排序）；2) 避免使用SELECT *，只查需要的列和聚合函数；3) 使用SQL_BIG_RESULT提示优化器直接使用磁盘临时表；4) 考虑是否真的需要GROUP BY，可能DISTINCT更合适；5) 关闭ONLY_FULL_GROUP_BY或使用ANY_VALUE()函数处理非聚合列。}}

{{知识点38}}{{MySQL中ORDER BY的优化？
1) 利用索引的有序性（联合索引的排序方向需一致或MySQL 8.0支持DESC索引）；2) 避免filesort，EXPLAIN中Using filesort表示需要额外排序；3) 增大sort_buffer_size；4) 只SELECT需要的列减少排序数据量；5) LIMIT配合ORDER BY时可用索引优化。}}

{{知识点39}}{{MySQL的连接池是什么？如何配置？
连接池是维护一组数据库连接的复用机制，避免频繁创建销毁连接的开销。应用层连接池（如HikariCP、Druid）和MySQL的thread_cache_size（线程缓存）配合使用。关键参数：max_connections（最大连接数）、wait_timeout（空闲超时）、interactive_timeout。连接数设置需根据服务器资源合理评估，过多连接会导致资源争抢。}}

{{知识点40}}{{MySQL 8.0有哪些重要新特性？
1) 窗口函数（ROW_NUMBER、RANK、DENSE_RANK等）；2) 公用表表达式CTE（WITH RECURSIVE递归查询）；3) JSON增强（JSON_TABLE等）；4) 不可见索引（ALTER TABLE t ALTER INDEX idx INVISIBLE）；5) 直方图统计（ANALYZE TABLE t UPDATE HISTOGRAM）；6) 默认字符集改为utf8mb4；7) 去除查询缓存（Query Cache）；8) 原子DDL；9) Resource Group资源组；10) InnoDB Cluster高可用方案。}}

{{知识点41}}{{什么是MySQL的查询缓存？为什么8.0移除了？
查询缓存（Query Cache）缓存SELECT语句及其结果集，相同查询直接返回缓存结果。问题：1) 表任何数据变更会导致该表所有缓存失效；2) 在多核高并发下QC锁成为瓶颈；3) 命中率在OLTP场景通常很低。MySQL 8.0彻底移除。替代方案：应用层缓存（Redis等）。}}

{{知识点42}}{{MySQL的数据备份方式有哪些？
1) 逻辑备份：mysqldump（单线程，适合小库）、mydumper（多线程并行）；2) 物理备份：xtrabackup（Percona，支持增量备份，不锁表）、mysqlbackup（Oracle官方）；3) 基于binlog的增量备份和时间点恢复（PITR）；4) 快照备份（LVM、ZFS、云快照）。生产推荐xtrabackup+binlog的组合方案。}}

{{知识点43}}{{xtrabackup备份恢复的原理？
备份阶段：拷贝InnoDB数据文件（物理复制）同时持续监控并记录redo log变化。prepare阶段：将redo log应用到数据文件，使数据一致性。恢复阶段：将备份文件复制到数据目录，apply-log后启动MySQL。支持全量和增量备份，增量备份基于LSN（Log Sequence Number）差异。}}

{{知识点44}}{{如何实现MySQL的高可用？
1) 主从复制+VIP漂移（简单但手动切换）；2) MHA（Master High Availability，自动故障切换）；3) MySQL Group Replication（组复制，单主/多主模式）；4) InnoDB Cluster（MySQL Shell + Group Replication + MySQL Router）；5) Orchestrator（拓扑管理和自动故障转移）；6) ProxySQL/MySQL Router读写分离代理。}}

{{知识点45}}{{什么是MySQL的GTID？
Global Transaction Identifier，全局事务标识符。格式：server_uuid:transaction_id。每提交一个事务分配唯一GTID。优势：1) 简化主从切换，无需手动指定binlog位点；2) 简化故障恢复；3) 天然支持并行复制。开启：gtid_mode=ON，enforce_gtid_consistency=ON。}}

{{知识点46}}{{MySQL中的字符集和排序规则？
字符集定义字符编码（utf8mb4支持完整Unicode包括emoji，utf8只支持3字节）。排序规则（collation）定义比较规则，utf8mb4_general_ci（不区分大小写，性能好但不精确）、utf8mb4_unicode_ci（更精确的Unicode排序）、utf8mb4_0900_ai_ci（MySQL 8.0默认）。建议统一使用utf8mb4。}}

{{知识点47}}{{MySQL中的JSON类型怎么用？
MySQL 5.7+支持JSON类型，支持JSON_EXTRACT(->)、JSON_SET、JSON_INSERT、JSON_REMOVE、JSON_CONTAINS等函数。可创建虚拟列（Generated Column）并建索引。MySQL 8.0新增JSON_TABLE将JSON转为关系表、JSON_ARRAYAGG/JSON_OBJECTAGG聚合函数。适合存储半结构化数据，但频繁查询建议拆为关系表。}}

{{知识点48}}{{MySQL中的窗口函数有哪些？
MySQL 8.0支持：ROW_NUMBER()（行号）、RANK()（排名可重复跳号）、DENSE_RANK()（排名可重复不跳号）、NTILE(n)（分组排名）、LAG/LEAD（前后偏移取值）、FIRST_VALUE/LAST_VALUE（首尾值）、聚合函数窗口化（SUM() OVER()等）。OVER子句定义窗口：PARTITION BY分组、ORDER BY排序、ROWS/RANGE范围。}}

{{知识点49}}{{MySQL中如何处理分布式事务？
1) XA事务（两阶段提交，MySQL原生支持，性能差）；2) TCC（Try-Confirm-Cancel，业务层实现）；3) SAGA模式（长事务拆分为本地事务序列+补偿操作）；4) 本地消息表（利用本地事务+消息队列保证最终一致性）；5) Seata等分布式事务框架。实际中多采用最终一致性方案替代强一致性XA。}}

{{知识点50}}{{MySQL安全加固的最佳实践？
1) 使用强密码，定期更换；2) 最小权限原则，禁止root远程登录；3) 删除匿名账户和测试数据库；4) 开启SSL/TLS加密连接；5) 配置防火墙限制访问IP；6) 开启审计日志（audit_log插件）；7) 定期备份并测试恢复；8) 及时更新补丁；9) 使用参数化查询防SQL注入；10) 加密敏感数据（AES_ENCRYPT/列级加密）。}}
