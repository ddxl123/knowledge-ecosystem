# 2025MongoDB入门
# 领域: 计算机IT
# 学科: 数据库
# 来源: 2025MongoDB入门
# 描述: MongoDB核心知识点，涵盖文档模型、CRUD操作、索引、聚合管道、副本集、分片、Schema设计等

{{MongoDB概述}}{{MongoDB是一个基于分布式文件存储的开源NoSQL数据库系统，由C++编写。它将数据存储为灵活的JSON样式的BSON文档，而不是传统的表格关系型结构。MongoDB的核心特点包括：文档导向存储、高性能读写、自动水平扩展、丰富的查询语言和聚合框架。适用于内容管理、物联网、实时分析、移动应用等场景。2025年最新版本为MongoDB 8.0，引入了性能优化、安全增强和更好的开发者体验。}}

{{BSON数据类型}}{{BSON（Binary JSON）是MongoDB的二进制编码格式，支持比JSON更丰富的数据类型。常用类型包括：String（字符串）、Integer（32位或64位整数）、Double（双精度浮点数）、Boolean（布尔值）、Array（数组）、Object（嵌入式文档）、ObjectId（12字节唯一标识符）、Date（日期时间）、Null、Binary Data、Regular Expression、Timestamp、Decimal128、Map等。ObjectId由时间戳、机器标识、进程ID和计数器组成，保证全局唯一性。}}

{{数据库与集合}}{{MongoDB的层级结构为：数据库（Database）→ 集合（Collection）→ 文档（Document）。一个MongoDB实例可以承载多个数据库，每个数据库包含多个集合，集合是文档的容器。集合类似于关系数据库的表，但不需要预定义schema。使用 `use <dbname>` 切换/创建数据库，集合在插入第一个文档时自动创建。系统保留数据库：admin（管理操作）、local（副本集本地数据）、config（分片配置）。}}

{{文档结构}}{{MongoDB文档是BSON格式的键值对集合，最大大小为16MB。文档的key必须是字符串，value可以是任意BSON类型。支持嵌入式文档（子文档）和数组，形成树状结构。嵌入式设计模式可以将相关数据放在同一文档中，减少查询时的JOIN操作。示例：`{ name: "Alice", age: 30, address: { city: "Beijing", zip: "100000" }, hobbies: ["reading", "coding"] }`。文档的字段顺序在存储时会被保留，但查询时不关心顺序。}}

{{ObjectId详解}}{{ObjectId是MongoDB默认的主键类型，长度为12字节（24个十六进制字符）。其组成：4字节Unix时间戳（秒级）+ 5字节随机值（每个进程唯一）+ 3字节递增计数器。可以通过 `ObjectId()` 生成新ID，用 `ObjectId().getTimestamp()` 提取创建时间。在分布式系统中，ObjectId不需要中心协调即可保证唯一性。也可以使用自定义_id字段，但必须保证唯一性。}}

{{Insert插入操作}}{{插入文档的主要方法：`insertOne(doc)` 插入单个文档，返回包含 insertedId 的结果；`insertMany([doc1, doc2, ...])` 批量插入多个文档，可设置 ordered 选项（默认true，遇错停止；false则跳过错误继续）。如果未指定 _id 字段，MongoDB会自动生成ObjectId。插入操作是写入主节点，通过副本集同步到从节点。bulkWrite() 方法可以混合执行多种写操作，适合批量数据导入。}}

{{Find查询操作}}{{`find()` 是最基本的查询方法。`db.collection.find({})` 返回所有文档；`find({ field: value })` 精确匹配；`find({}, { field1: 1, field2: 1 })` 投影指定字段。常用查询操作符：`$gt/$gte/$lt/$lte`（比较）、`$eq/$ne`（相等）、`$in/$nin`（集合匹配）、`$and/$or/$not/$nor`（逻辑）、`$exists`（字段存在）、`$regex`（正则匹配）。`findOne()` 返回单个文档或null。可链式调用 `.sort()`、`.limit()`、`.skip()`、`.count()`。}}

{{Update更新操作}}{{更新方法：`updateOne(filter, update, options)` 更新匹配的第一个文档；`updateMany(filter, update, options)` 更新所有匹配文档。更新操作符：`$set`（设置字段值）、`$unset`（删除字段）、`$inc`（自增）、`$push/$pull`（数组操作）、`$addToSet`（数组去重添加）、`$rename`（重命名字段）、`$min/$max`（取最小/最大值）。options: `upsert: true` 在无匹配时插入新文档。替换操作 `replaceOne()` 会用新文档完全替换匹配文档（保留原_id）。}}

{{Delete删除操作}}{{删除方法：`deleteOne(filter)` 删除匹配的第一个文档；`deleteMany(filter)` 删除所有匹配文档。空过滤器 `{}` 会删除所有文档。`findOneAndDelete(filter)` 删除并返回被删除的文档，支持 sort 选项。注意：删除操作不可逆，生产环境建议先查询确认再删除。集合的 `drop()` 方法删除整个集合。数据库的 `db.dropDatabase()` 删除整个数据库。可以使用 `bulkWrite()` 批量混合删除和其他操作。}}

{{比较查询操作符}}{{比较操作符用于数值、日期等类型的范围查询。`$gt`（大于）、`$gte`（大于等于）、`$lt`（小于）、`$lte`（小于等于）、`$eq`（等于）、`$ne`（不等于）。示例：`db.products.find({ price: { $gt: 100, $lte: 500 } })` 查询价格在100到500之间的商品。`$in` 匹配值在指定数组中的文档，如 `{ status: { $in: ["active", "pending"] } }`；`$nin` 匹配不在数组中的文档。比较操作符对不同BSON类型有预定义的比较顺序。}}

{{逻辑查询操作符}}{{逻辑操作符组合多个查询条件。`$and: [{cond1}, {cond2}, ...]` 所有条件都满足；`$or: [{cond1}, {cond2}]` 任一条件满足；`$nor: [{cond1}, {cond2}]` 所有条件都不满足；`$not: { expr }` 取反表达式。隐式AND：`find({ age: { $gt: 20 }, status: "A" })` 等价于 `$and`。注意 `$or` 中每个条件可以使用不同字段，而 `$and` 可以对同一字段使用多个条件（如范围查询）。MongoDB会自动优化查询，利用索引提高性能。}}

{{数组查询操作符}}{{数组查询操作符针对数组类型字段。`{ tags: "java" }` 匹配tags数组包含"java"的文档。`{ tags: { $all: ["java", "python"] } }` 匹配同时包含两个元素的文档。`{ tags: { $size: 3 } }` 匹配数组长度为3的文档。`{ scores: { $elemMatch: { $gt: 80, $lt: 90 } } }` 匹配数组中至少有一个元素同时满足多个条件。点号访问嵌套数组元素：`{ "scores.0": { $gt: 90 } }` 匹配第一个分数大于90的文档。`$slice` 投影可限制返回的数组元素数量。}}

{{索引基础}}{{索引是MongoDB查询性能优化的核心机制。默认情况下MongoDB在_id字段上创建唯一索引。创建索引：`db.collection.createIndex({ field: 1 })`（升序）或 `{ field: -1 }`（降序）。索引类型：单字段索引、复合索引（多字段）、多键索引（数组）、文本索引（全文搜索）、地理空间索引（2dsphere）、哈希索引（分片用）。复合索引遵循ESR原则（Equality, Sort, Range）。使用 `explain()` 分析查询是否使用了索引及效率。}}

{{复合索引与ESR原则}}{{复合索引是在多个字段上建立的索引，字段顺序至关重要。ESR原则：将等值查询（Equality）字段放最前，排序（Sort）字段放中间，范围查询（Range）字段放最后。例如查询 `{ status: "A", date: { $gt: ISODate("2025-01-01") } }` 并按 `date` 排序，最优索引为 `{ status: 1, date: 1 }`。复合索引可以支持前缀字段的查询，即 `{ a: 1, b: 1, c: 1 }` 可以支持仅查询 `a` 或 `a, b` 的场景。索引字段顺序也影响排序效率。}}

{{索引管理与优化}}{{索引管理操作：`createIndex()` 创建索引、`dropIndex()` 删除索引、`getIndexes()` 查看所有索引。索引选项：`unique: true`（唯一索引）、`sparse: true`（稀疏索引，忽略null值）、`expireAfterSeconds`（TTL索引，自动过期删除文档）、`partialFilterExpression`（部分索引，仅索引满足条件的文档）。`db.collection.reIndex()` 重建索引。注意索引会占用存储空间并影响写入性能，需要在查询加速和写入开销之间平衡。定期使用 `$indexStats` 监控索引使用情况，删除无用索引。}}

{{文本索引与全文搜索}}{{MongoDB支持全文搜索功能。创建文本索引：`db.articles.createIndex({ content: "text", title: "text" })`，每个集合只能有一个文本索引。使用 `$text` 操作符查询：`db.articles.find({ $text: { $search: "keyword1 keyword2" } })`。支持：精确短语搜索（用引号）、排除词（用减号）、按相关性评分排序（`{ score: { $meta: "textScore" } }`）。文本索引支持权重设置和语言特定的停用词和词干提取。MongoDB Atlas还提供Atlas Search（基于Lucene），功能更强大。}}

{{聚合管道基础}}{{聚合管道（Aggregation Pipeline）是MongoDB最强大的数据处理框架。它将处理流程分为多个阶段（stage），文档像流水线一样依次经过各阶段处理。基本语法：`db.collection.aggregate([ stage1, stage2, ... ])`。常用阶段：`$match`（过滤）、`$group`（分组聚合）、`$sort`（排序）、`$project`（字段投影）、`$limit/$skip`（限制/跳过）、`$unwind`（展开数组）、`$lookup`（关联查询）、`$addFields`（添加字段）、`$count`（计数）。管道阶段可以重复使用，顺序影响性能。}}

{{$match与$project阶段}}{{`$match` 阶段在管道早期过滤文档，减少后续阶段处理的数据量，应尽量利用索引。语法与find()查询相同：`{ $match: { status: "active", age: { $gte: 18 } } }`。`$project` 阶段控制输出字段，可以包含/排除字段、重命名、计算新字段。示例：`{ $project: { name: 1, fullName: { $concat: ["$firstName", " ", "$lastName"] }, age: 1, _id: 0 } }`。支持计算表达式、条件逻辑（$cond）、类型转换等。合理使用$project可以减少网络传输数据量。}}

{{$group聚合阶段}}{{`$group` 阶段按指定字段分组并计算聚合值。语法：`{ $group: { _id: "$field", count: { $sum: 1 }, avgPrice: { $avg: "$price" } } }`。`_id` 为null时表示对所有文档做全局聚合。常用累加器操作符：`$sum`（求和）、`$avg`（平均值）、`$min/$max`（最小/最大值）、`$first/$last`（首/末值）、`$push`（收集值到数组）、`$addToSet`（收集不重复值）、`$stdDevPop/$stdDevSamp`（标准差）。支持多字段分组：`_id: { status: "$status", city: "$city" }`。}}

{{$lookup关联查询}}{{`$lookup` 阶段实现类似SQL的LEFT JOIN功能，关联两个集合。基本语法：`{ $lookup: { from: "orders", localField: "userId", foreignField: "_id", as: "userOrders" } }`。结果中的 `userOrders` 是一个数组，包含关联的文档。高级语法支持 `pipeline`，可以使用 `$match`、`$project` 等阶段进行更复杂的关联查询和过滤。`$lookup` 性能关键：确保 foreignField 上有索引。`$unwind` 常与 `$lookup` 配合使用，将结果数组展开为单个文档。替代方案：嵌入式文档设计可以避免$lookup。}}

{{$unwind展开数组}}{{`$unwind` 阶段将数组字段中的每个元素展开为独立的文档。例如文档 `{ _id: 1, tags: ["a", "b", "c"] }` 经过 `{ $unwind: "$tags" }` 后产生3个文档。options：`preserveNullAndEmptyArrays: true` 保留数组为null或空的文档。常用于：统计数组元素频率、与 `$group` 结合做分组聚合、展开嵌套数组后关联查询。多层数组展开需多次使用 $unwind，顺序影响结果。展开后文档数量会增加，注意性能影响。}}

{{聚合管道优化}}{{聚合管道优化技巧：1）尽早使用 `$match` 减少数据量，MongoDB会将$match推到管道最前面并利用索引；2）尽早 `$project` 减少字段传输；3）`$sort` 阶段如果能利用索引会大幅提速；4）`$limit` 可与 `$sort` 结合使用 `top-N` 优化；5）使用 `allowDiskUse: true` 处理超过100MB内存限制的大数据集；6）避免在 `$group` 中使用 `$push` 收集大量数据；7）使用 `$facet` 并行执行多个聚合分支。使用 `explain("executionStats")` 分析管道性能。}}

{{副本集概念}}{{副本集（Replica Set）是MongoDB的高可用方案，由一组mongod实例组成。一个副本集包含：一个Primary（主节点，处理所有写操作）、多个Secondary（从节点，复制主节点数据）、可选的Arbiter（仲裁节点，参与选举但不存储数据）。推荐最少3个数据节点或2个数据节点+1个仲裁节点。副本集通过oplog（操作日志）实现数据同步。当主节点故障时，自动选举新的主节点，实现故障自动转移。读操作默认发往主节点，可配置读偏好从从节点读取。}}

{{副本集选举机制}}{{副本集选举机制确保高可用性。选举触发条件：主节点心跳丢失（默认10秒超时）、主节点主动降级、新副本集初始化。选举过程：从节点发起选举请求，获得多数投票（>节点总数/2）的节点成为新主节点。优先级（priority）字段影响选举：优先级高的节点更可能被选为主节点，设为0则不能成为主。隐藏节点（hidden）和延迟节点（priority:0, hidden:true）不参与选举。选举超时默认12秒，心跳间隔默认2秒。网络分区时可能出现脑裂，MongoDB通过多数派机制避免。}}

{{副本集读写偏好}}{{读偏好（Read Preference）控制读操作路由到哪些节点。模式：`primary`（默认，只读主节点）、`primaryPreferred`（优先主节点，不可用时读从节点）、`secondary`（只读从节点）、`secondaryPreferred`（优先从节点）、`nearest`（延迟最低的节点）。设置方法：`db.collection.find().readPref("secondary")` 或连接字符串参数。读偏好影响：数据一致性（从节点可能有复制延迟）、负载分布、地理就近读取。Tag Set可以精细控制读取特定从节点。写操作始终发往主节点。}}

{{写关注（Write Concern）}}{{写关注（Write Concern）控制写操作的持久性保证。语法：`{ w: <number>, j: <boolean>, wtimeout: <number> }`。`w: 1`（默认，主节点确认即返回）、`w: 0`（不等待确认，最快但最不安全）、`w: "majority"`（多数节点确认，最安全）、`w: N`（N个节点确认）。`j: true` 要求写入journal日志后才确认，保证断电不丢失。`wtimeout` 等待超时毫秒数，超时返回错误。生产环境建议 `w: "majority"` + `j: true`。写关注与读偏好配合使用，实现不同级别的一致性。}}

{{分片概念}}{{分片（Sharding）是MongoDB的水平扩展方案，将数据分散到多个服务器（分片）上。每个分片存储数据的一部分，组成一个完整的数据集。分片集群组件：mongos（路由服务，客户端连接入口）、config server（存储集群元数据和分片映射）、shard（存储实际数据，每个分片可以是副本集）。启用分片：`sh.enableSharding("dbname")`，对集合分片：`sh.shardCollection("db.collection", { shardKey: 1 })`。分片对应用透明，mongos自动路由查询。}}

{{分片键选择}}{{分片键（Shard Key）决定数据如何分布到各分片，是分片设计最关键的决策。分片键必须是索引字段（或复合索引前缀）。选择原则：1）高基数（cardinality），值分布均匀，避免热点；2）写分布均匀，避免所有写集中在同一分片；3）查询隔离，常用查询能路由到单个分片。分片策略：Ranged Sharding（范围分片，按值范围分配）、Hashed Sharding（哈希分片，均匀分布但范围查询效率低）。注意：分片键一旦选定不可更改，MongoDB 5.0+支持refineShardKey。}}

{{Schema设计模式}}{{MongoDB的Schema设计直接影响查询性能和数据管理。关键原则：1）数据访问模式决定Schema设计（查询驱动设计）；2）嵌入 vs 引用的权衡：嵌入减少查询但增加文档大小，引用保持数据独立但需要额外查询；3）一对一和一对少量关系→嵌入；4）一对多量大→引用；5）多对多→引用。常用设计模式：内嵌子文档、引用ObjectId、反范式（冗余数据避免JOIN）、桶模式（时序数据聚合存储）。Schema验证（$jsonSchema）可以在应用层强制数据结构。}}

{{内嵌与引用模式}}{{内嵌模式（Embedding）：将相关数据放在同一文档的子文档或数组中。优点：一次查询获取所有数据、原子性更新、更好的读性能。适用：数据关系紧密、数据量小、不需要独立访问子数据。引用模式（Referencing）：通过存储引用（通常是_id）关联数据。优点：避免数据冗余、文档大小可控、独立更新。适用：数据量大、多对多关系、子数据需要独立查询。混合模式：高频访问数据内嵌，低频数据引用。实际项目中常结合使用两种模式。}}

{{反范式与数据冗余}}{{MongoDB鼓励适度的数据冗余以优化读性能。反范式（Denormalization）将相关数据冗余存储在多处，减少查询时的关联操作。示例：订单文档中冗余存储商品名称和价格，而非仅存商品ID。权衡：读性能提升 vs 写入时需同步多处数据。处理策略：1）应用层同步更新；2）变更流（Change Streams）监听数据变更自动同步；3）定期批处理修复不一致。关键冗余数据应有权威来源（source of truth），其他副本为派生数据。}}

{{Schema验证}}{{MongoDB 3.6+支持JSON Schema验证，可在集合级别强制文档结构。创建时设置：`db.createCollection("users", { validator: { $jsonSchema: { bsonType: "object", required: ["name", "email"], properties: { name: { bsonType: "string" }, age: { bsonType: "int", minimum: 0 } } } } })`。修改验证规则：`db.runCommand({ collMod: "collection", validator: {...} })`。验证级别：`strict`（默认，拒绝不合规文档）、`moderate`（仅验证已有不合规文档的更新）。验证动作：`error`（拒绝）、`warn`（警告但允许）。}}

{{MongoDB事务}}{{MongoDB 4.0+支持多文档ACID事务（副本集），4.2+扩展到分片集群。事务保证：原子性（全部成功或全部回滚）、一致性、隔离性、持久性。使用方式：`session.startTransaction()` → 执行操作（需传入session选项）→ `session.commitTransaction()` 或 `session.abortTransaction()`。注意事项：事务有60秒超时限制、多文档事务影响性能、合理设计Schema减少事务需求。Write Concern建议设为 `majority`。MongoDB推荐优先通过Schema设计（嵌入文档）减少事务需求。}}

{{Change Streams变更流}}{{Change Streams是MongoDB 3.6+的实时数据变更通知功能，基于oplog实现。使用：`db.collection.watch()` 或 `db.watch()`（数据库级）或 `client.watch()`（集群级）。返回变更事件包含操作类型（insert/update/replace/delete）、完整文档或变更内容、时间戳等。支持过滤管道：`watch([{ $match: { operationType: "insert" } }])`。选项：`fullDocument: "updateLookup"` 获取更新后的完整文档。应用场景：实时数据同步、事件驱动架构、缓存失效通知。断线后可通过resumeToken恢复。}}

{{MongoDB安全机制}}{{MongoDB安全最佳实践：1）启用认证（SCRAM、x.509证书、LDAP、Kerberos）；2）基于角色的访问控制（RBAC），内置角色：read、readWrite、dbAdmin、userAdmin、clusterAdmin等；3）网络隔离：绑定IP、使用VPC、配置防火墙；4）加密：TLS/SSL传输加密、静态加密（Enterprise版或Atlas）；5）审计日志（Enterprise版）；6）输入验证防止注入；7）定期更新版本修复安全漏洞。MongoDB Atlas提供开箱即用的安全配置。生产环境必须禁用 `--noauth` 和绑定 `0.0.0.0`。}}

{{MongoDB性能优化}}{{性能优化要点：1）索引优化：确保常用查询有合适索引，使用explain()分析；2）查询优化：只返回需要的字段（投影）、使用limit限制结果集、避免全集合扫描；3）Schema优化：合理嵌入减少查询次数、控制文档大小在16MB以内；4）连接池：合理配置连接池大小（默认100）；5）写入优化：批量写入替代逐条插入、合理设置Write Concern；6）硬件：SSD存储、足够内存（工作集应能放入内存）；7）监控：使用mongostat、mongotop、Atlas监控面板。}}

{{MongoDB驱动与连接}}{{各语言官方驱动：Node.js（mongodb）、Python（pymongo）、Java（mongodb-driver-sync）、Go（mongo-go-driver）、C#（MongoDB.Driver）。连接字符串格式：`mongodb://user:password@host1:27017,host2:27017/dbname?replicaSet=rs0&authSource=admin`。Atlas连接：`mongodb+srv://user:password@cluster.mongodb.net/dbname`。连接选项：`maxPoolSize`（连接池大小）、`retryWrites`（自动重试写操作）、`retryReads`、`readPreference`、`w`（Write Concern）、`tls`（启用TLS）。推荐使用连接池复用连接，避免频繁创建销毁。}}

{{MongoDB Atlas云服务}}{{MongoDB Atlas是MongoDB官方的全托管云数据库服务。主要功能：1）自动化部署和运维（备份、升级、扩容）；2）全球多区域部署；3）自动扩展（Auto-Scaling）；4）Atlas Search（全文搜索，基于Lucene）；5）Atlas Vector Search（向量搜索，支持AI/ML应用）；6）Data Federation（联合查询多数据源）；7）Atlas Charts（数据可视化）；8）Triggers（事件驱动函数）；9）完整安全套件（加密、网络隔离、审计）。免费层M0提供512MB存储，适合开发学习。}}

{{MongoDB与SQL对比}}{{MongoDB（NoSQL）与关系型数据库（SQL）对比：数据模型-文档(BSON) vs 行/列；Schema-灵活无固定结构 vs 严格预定义；查询语言-MQL(MongoDB Query Language) vs SQL；JOIN-$lookup(有限) vs 多表JOIN(丰富)；事务-单文档原子/多文档事务(4.0+) vs 完整ACID事务；扩展方式-水平分片为主 vs 垂直扩展+读写分离；索引-支持多种类型 vs B-Tree/Hash等；适用场景-快速迭代、大数据量、灵活Schema vs 强一致性、复杂关系查询。选择取决于具体业务需求，两者也可共存使用。}}
