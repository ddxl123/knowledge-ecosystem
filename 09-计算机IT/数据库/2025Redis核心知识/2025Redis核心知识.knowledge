# 2025Redis核心知识
# 领域: 计算机IT
# 学科: 数据库
# 来源: 2025Redis核心知识
# 描述: Redis核心知识点

{{Redis简介}}{{Redis（Remote Dictionary Server）是一个开源的、基于内存的高性能键值数据库。它支持多种数据结构，包括字符串、哈希、列表、集合、有序集合等。Redis的核心特点是数据存储在内存中，读写速度极快，单线程模型避免了上下文切换开销，同时通过IO多路复用技术实现高并发。Redis广泛用于缓存、会话管理、消息队列、排行榜、实时计数等场景。}}

{{单线程模型}}{{Redis采用单线程模型处理命令请求，这意味着所有命令都是串行执行的。单线程的优势在于：避免了多线程的锁竞争和上下文切换开销；代码实现简单，逻辑清晰；无需考虑线程安全问题。Redis 6.0引入了多线程IO（仅用于网络IO处理，命令执行仍为单线程），进一步提升了网络吞吐量。瓶颈通常在网络IO而非CPU，因此单线程足以应对大多数场景。}}

{{IO多路复用}}{{Redis使用IO多路复用技术（epoll/kqueue/select）实现单线程处理大量并发连接。IO多路复用允许单个线程同时监听多个文件描述符，当某个连接有数据到达时才进行处理，而不是为每个连接创建一个线程。这样Redis可以高效地处理成千上万的并发连接，而不会因为线程过多导致系统资源耗尽。Redis会在事件循环中轮询就绪事件并依次处理。}}

{{数据结构之String}}{{String是Redis最基本的数据结构，可以存储字符串、整数或浮点数。底层实现根据内容自动选择：int（整数且<=20位）、embstr（字符串<=44字节）、raw（字符串>44字节）。常用命令：SET/GET/MSET/MGET/INCR/DECR/INCRBY/APPEND/STRLEN/SETEX/SETNX。典型应用场景：缓存、计数器、分布式锁、Session存储。最大可存储512MB。}}

{{数据结构之Hash}}{{Hash是键值对的集合，适合存储对象。底层使用ziplist（元素少且值小时）或hashtable（元素多或值大时）实现。Redis 7.0引入listpack替代ziplist，解决了级联更新问题。常用命令：HSET/HGET/HMSET/HMGET/HGETALL/HDEL/HKEYS/HVALS/HINCRBY/HSETNX。典型应用场景：存储用户信息、商品详情等结构化数据。相比String存储JSON，Hash可以单独修改某个字段，减少网络传输。}}

{{数据结构之List}}{{List是有序的字符串列表，底层使用quicklist实现（Redis 3.2+），quicklist是ziplist和linkedlist的混合体。支持从两端推入弹出元素，可以用作队列或栈。常用命令：LPUSH/RPUSH/LPOP/RPOP/LRANGE/LLEN/LINDEX/LREM/BLPOP/BRPOP/LSET。典型应用场景：消息队列、最新消息列表、时间线。BLPOP/BRPOP支持阻塞弹出，适合简单的消息队列场景。}}

{{数据结构之Set}}{{Set是无序的、不重复的字符串集合。底层使用intset（全为整数且元素少时）或hashtable实现。支持集合运算。常用命令：SADD/SREM/SISMEMBER/SMEMBERS/SRANDMEMBER/SINTER/SUNION/SDIFF/SINTERSTORE/SCARD。典型应用场景：标签系统、共同好友、抽奖系统、去重。集合运算可以在服务端高效完成，减少客户端计算压力。}}

{{数据结构之SortedSet}}{{SortedSet（ZSet）是有序的、不重复的字符串集合，每个元素关联一个score用于排序。底层使用ziplist/listpack（元素少时）或skiplist+hashtable实现。skiplist提供O(logN)的插入和查找性能。常用命令：ZADD/ZREM/ZRANGE/ZREVRANGE/ZRANGEBYSCORE/ZRANK/ZREVRANK/ZSCORE/ZINCRBY/ZCARD/ZCOUNT。典型应用场景：排行榜、延迟队列、带权重的优先级队列。}}

{{数据结构之Bitmap}}{{Bitmap不是独立的数据结构，而是基于String的一系列位操作。可以对字符串的每一位进行设置、获取和统计。常用命令：SETBIT/GETBIT/BITCOUNT/BITOP/BITPOS/BITFIELD。典型应用场景：用户签到（每天一个bit，一年仅需365/8≈46字节）、布隆过滤器实现、在线状态统计、特征标记。Bitmap的存储效率极高，适合大量布尔值的场景。}}

{{数据结构之HyperLogLog}}{{HyperLogLog是用于基数统计的概率数据结构，无论统计多少个元素，只占用12KB内存。它允许约0.81%的误差率。常用命令：PFADD/PFCOUNT/PFMERGE。典型应用场景：统计网站UV（独立访客数）、日活用户数等需要去重计数但允许微小误差的场景。相比Set存储所有元素，HyperLogLog的内存效率极高，但不能获取具体元素。}}

{{数据结构之Stream}}{{Stream是Redis 5.0引入的日志型数据结构，支持消息持久化、消费组和消息确认。底层使用Radix Tree存储消息ID，listpack存储消息内容。常用命令：XADD/XREAD/XREADGROUP/XACK/XLEN/XRANGE/XREVRANGE/XINFO/XGROUP。典型应用场景：消息队列（替代List和Pub/Sub）、事件溯源、日志收集。相比Pub/Sub，Stream支持消息持久化和消费确认，更加可靠。}}

{{数据结构之Geospatial}}{{Geospatial类型用于存储地理位置信息，底层基于Sorted Set实现（将经纬度编码为score）。常用命令：GEOADD/GEOPOS/GEODIST/GEORADIUS/GEORADIUSBYMEMBER/GEOHASH/GEOSEARCH。典型应用场景：附近的人/店铺、距离计算、地理围栏。Redis使用Geohash编码将二维坐标转换为一维数值进行排序，支持高效的范围查询。}}

{{数据结构之Bitfield}}{{Bitfield允许在单个字符串键中存储和操作多个不同位宽的整数。支持有符号和无符号整数（1-64位），可以执行读取、设置和原子递增操作。常用命令：BITFIELD GET/SET/INCRBY/OVERFLOW。典型应用场景：紧凑型计数器、统计系统、内存敏感的计数场景。相比多个键存储，Bitfield将多个值打包到一个键中，减少了键的管理开销。}}

{{底层编码之ziplist}}{{ziplist是一种紧凑的、连续内存存储的数据结构，通过特殊编码节省内存。它将所有数据元素紧密排列在一块连续内存中，包括前一个元素长度、当前元素编码和数据。优势是内存占用小、缓存友好；劣势是插入/删除可能触发级联更新（realloc导致内存拷贝），性能退化。Redis 7.0用listpack替代ziplist，消除了级联更新问题。当元素数量或大小超过阈值时会自动转换为更高效的结构。}}

{{底层编码之skiplist}}{{skiplist（跳表）是Redis中Sorted Set的核心实现之一，与hashtable配合使用。跳表是一种多层有序链表，每一层都是下一层的子集。通过概率平衡的方式，实现O(logN)的查找、插入和删除。相比红黑树，跳表实现更简单，范围查询更高效（直接遍历底层链表即可）。Redis的skiplist支持最高32层，且允许重复分数（score相同时按字典序排序）。}}

{{底层编码之listpack}}{{listpack是Redis 7.0引入的新编码，替代ziplist解决级联更新问题。listpack中每个元素只记录自身长度（而非前一个元素长度），因此删除或插入元素不会影响其他元素的长度字段，避免了级联更新。结构上每个元素包含：编码类型、数据、以及记录自身总长的backlen字段。listpack在内存紧凑性和操作安全性之间取得了更好的平衡。}}

{{底层编码之intset}}{{intset（整数集合）是Set在只包含整数且元素较少时使用的底层编码。它是一个有序的、不重复的整数数组，支持int16、int32、int64三种类型。当插入更大范围的整数时，intset会自动升级（如从int16升级到int32），但不会降级。intset的优势在于内存紧凑，所有元素连续存储，二分查找效率高。当元素数量超过阈值（默认512）或插入非整数值时，会转换为hashtable。}}

{{RDB持久化}}{{RDB（Redis Database）是Redis的快照式持久化机制，在指定时间间隔将内存数据生成快照写入磁盘。触发方式：SAVE（阻塞主线程）、BGSAVE（fork子进程，利用COW机制）、自动触发（配置文件中的save规则）。优势：文件紧凑、恢复速度快、适合备份和灾难恢复。劣势：可能丢失最后一次快照后的数据、fork大内存时可能阻塞。RDB文件是二进制格式，包含数据库状态的完整快照。}}

{{AOF持久化}}{{AOF（Append Only File）持久化记录每一条写命令，以追加方式写入日志文件。写入策略：always（每条命令都同步，最安全但最慢）、everysec（每秒同步，推荐折中方案）、no（由操作系统决定，最快但最不安全）。AOF重写机制（BGREWRITEAOF）可以压缩AOF文件，去除冗余命令。Redis 7.0引入Multi-Part AOF，将AOF拆分为base和增量文件，优化了重写流程和数据安全性。}}

{{RDB与AOF混合持久化}}{{Redis 4.0引入混合持久化模式（aof-use-rdb-preamble yes），在AOF重写时，将RDB格式数据写入AOF文件头部，增量命令以AOF格式追加在尾部。这样既利用了RDB的快速恢复特性，又保留了AOF的数据完整性。加载时先解析RDB部分快速恢复大部分数据，再重放AOF部分恢复增量数据。混合持久化是生产环境的推荐配置，兼顾了恢复速度和数据安全。}}

{{Redis内存管理}}{{Redis将所有数据存储在内存中，内存管理至关重要。内存分配器默认使用jemalloc（也支持libc和tcl）。内存碎片率（mem_fragmentation_ratio）>1.5时需关注。内存回收策略包括：LRU（近似最近最少使用）、LFU（近似最不经常使用，Redis 4.0+）、随机淘汰、TTL淘汰。内存优化技巧：使用Hash代替多个String键、合理设置过期时间、使用压缩编码（ziplist/listpack）、避免大Key。}}

{{缓存穿透}}{{缓存穿透是指查询一个数据库中一定不存在的数据，由于缓存未命中，请求直接打到数据库。恶意攻击者可能利用此漏洞大量请求不存在的数据来压垮数据库。解决方案：1）缓存空值并设置较短过期时间；2）布隆过滤器（Bloom Filter）预判key是否存在；3）接口层做参数校验和合法性检查。布隆过滤器是空间效率极高的概率数据结构，可能有误判（false positive）但不会有漏判（false negative）。}}

{{缓存击穿}}{{缓存击穿是指某个热点key在过期的瞬间，大量并发请求同时到达，全部打到数据库造成压力骤增。与缓存雪崩不同，缓存击穿是单个热点key的问题。解决方案：1）使用互斥锁（如SETNX），只允许一个请求重建缓存，其他请求等待；2）热点数据永不过期，异步更新；3）使用逻辑过期时间，在value中存储过期时间，由应用层判断是否需要异步刷新；4）提前预热热点数据。}}

{{缓存雪崩}}{{缓存雪崩是指大量缓存key在同一时间过期，或Redis服务宕机，导致大量请求直接打到数据库，可能造成数据库崩溃。解决方案：1）设置随机过期时间（在基础过期时间上加随机偏移量），避免同时失效；2）Redis高可用架构（哨兵、集群）防止单点故障；3）本地缓存（如Caffeine/Guava）作为二级缓存；4）限流降级机制保护数据库；5）缓存预热，在系统启动时提前加载热点数据。}}

{{缓存与数据库一致性}}{{缓存与数据库的双写一致性是分布式系统中的经典问题。常见策略：1）Cache Aside Pattern（旁路缓存）：读时先查缓存，未命中再查数据库并写缓存；写时先更新数据库，再删除缓存（而非更新缓存）。2）Write Behind Pattern：只更新缓存，异步批量更新数据库。3）延迟双删：先删缓存→更新数据库→延迟一段时间→再删缓存。删除缓存比更新缓存更安全，因为下次读取时会自动重建正确的缓存值。}}

{{Redis分布式锁之SETNX实现}}{{基于Redis实现分布式锁的核心命令是SET key value NX EX（Redis 2.6.24+支持在SET命令中直接设置NX和过期时间，保证原子性）。释放锁时必须先比较value是否为自己的标识（推荐使用Lua脚本保证原子性），防止误删其他客户端的锁。常见问题：1）锁过期但业务未完成→看门狗机制自动续期；2）Redis主从切换导致锁丢失→Redlock算法。单节点分布式锁适用于对一致性要求不高的场景。}}

{{Redis分布式锁之Redlock算法}}{{Redlock是Redis作者Antirez提出的分布式锁算法，适用于对可靠性要求更高的场景。基本原理：客户端向N个（通常5个）独立的Redis实例依次请求锁，如果在大多数（N/2+1）实例上成功获取锁，且总耗时未超过锁的过期时间，则认为获取锁成功。Redlock的争议：Martin Kleppmann指出在网络分区、GC暂停等场景下Redlock的安全性无法保证。实际生产中，单节点锁+看门狗机制通常已能满足大多数需求。}}

{{Redis事务}}{{Redis事务通过MULTI、EXEC、DISCARD、WATCH命令实现。MULTI开启事务后，命令依次入队，EXEC时按顺序原子执行。与关系数据库事务不同，Redis事务不支持回滚——如果某条命令执行失败，其他命令仍会继续执行。WATCH命令提供乐观锁机制，在EXEC前检查被监视的key是否被修改，如果被修改则事务失败（CAS机制）。Redis事务保证的是命令的顺序性和不可中断性，而非ACID。}}

{{Redis Lua脚本}}{{Redis支持在服务端执行Lua脚本（EVAL/EVALSHA），脚本在服务端原子执行，期间不会有其他命令插入。Lua脚本的优势：1）减少网络往返次数（多条命令合并为一次脚本调用）；2）原子性保证（无需MULTI/EXEC）；3）支持条件逻辑。常用命令：EVAL script numkeys key [key...] arg [arg...]、SCRIPT LOAD、SCRIPT EXISTS、SCRIPT FLUSH。EVALSHA通过脚本SHA1摘要避免重复传输脚本内容。典型应用：分布式锁释放、限流器、原子计数器。}}

{{Redis Pub/Sub}}{{Pub/Sub（发布/订阅）是Redis的消息通信模式。发布者通过PUBLISH命令向频道发送消息，所有订阅该频道的订阅者都会收到消息。支持模式匹配订阅（PSUBSCRIBE）。局限性：1）消息不持久化，订阅者离线期间的消息会丢失；2）不支持消息确认；3）不支持消息回放。因此Pub/Sub适合实时通知、事件广播等不需要消息可靠性的场景。需要可靠消息传递应使用Stream。}}

{{Redis Stream消息队列}}{{Redis Stream是Redis 5.0引入的持久化消息队列，提供消息ID自动生成、消费组、消费确认等完整功能。消息通过XADD追加，消费者通过XREAD/XREADGROUP读取。消费组（Consumer Group）支持多个消费者协作消费，每条消息只会分配给组内的一个消费者。XACK用于确认消息已处理。XPENDING查看待处理消息。XCLAIM转移超时未确认的消息。Stream是Redis中替代专业消息队列的轻量级方案。}}

{{Redis主从复制}}{{Redis主从复制是实现高可用和读写分离的基础。从节点通过PSYNC命令与主节点同步数据，支持全量同步（首次连接或复制偏移量不匹配时发送RDB快照）和增量同步（基于复制积压缓冲区的偏移量同步）。主节点将写命令异步发送给从节点，因此存在短暂的数据不一致。复制积压缓冲区（repl-backlog-size）大小决定了增量同步的成功率。配置方式：replicaof <masterip> <masterport>。}}

{{Redis哨兵模式}}{{Redis Sentinel（哨兵）是Redis的高可用方案。哨兵节点负责监控主从节点的健康状态，在主节点下线时自动执行故障转移（将某个从节点提升为新主节点）并通知客户端。故障转移流程：1）主观下线（单个哨兵认为主节点不可达）；2）客观下线（多数哨兵达成共识）；3）选举领导者哨兵；4）选择最优从节点晋升为主节点；5）重新配置其他从节点指向新主节点。至少需要3个哨兵节点以确保多数投票。}}

{{Redis集群原理}}{{Redis Cluster是Redis的分布式方案，数据自动分片到多个节点。采用哈希槽（Hash Slot）机制，将数据分成16384个槽，每个节点负责一部分槽。客户端发送命令时，节点检查key对应的槽是否在自己负责的范围内，如果不是则返回MOVED重定向。客户端通常会缓存槽与节点的映射关系。支持在线扩缩容（槽迁移）。每个主节点可以有从节点用于故障转移。集群内节点通过Gossip协议通信。}}

{{Redis集群之哈希标签}}{{Redis集群使用CRC16(key) % 16384计算key所属的槽位。哈希标签（Hash Tag）允许强制多个key映射到同一个槽位，规则是key中包含{...}时，只对花括号内的内容计算哈希。例如{user}:1:name和{user}:1:email会被分配到同一个槽。这在需要对多个key执行事务或Lua脚本时非常有用（Redis集群要求事务/Lua中的所有key必须在同一个槽）。注意滥用哈希标签可能导致数据分布不均。}}

{{Redis集群之故障转移}}{{Redis集群的故障检测和转移基于Gossip协议和投票机制。每个节点定期向其他节点发送PING，如果在cluster-node-timeout内未收到PONG，则将该节点标记为PFAIL（可能下线）。当集群中多数主节点都将某个主节点标记为PFAIL时，该节点被标记为FAIL（确认下线）。此时该主节点的从节点发起选举，获得多数主节点投票的从节点晋升为新主节点。整个过程自动完成，无需人工干预。}}

{{Redis过期策略}}{{Redis采用惰性删除+定期删除的过期策略。惰性删除：在访问key时检查是否过期，过期则删除。定期删除：每100ms随机抽取一批设置了过期时间的key进行检查，删除已过期的key，并根据过期比例调整下次检查频率。这种组合策略在CPU使用和内存回收之间取得平衡。注意：过期删除释放的内存不会立即返还给操作系统（取决于jemalloc的配置），可能需要重启Redis才能回收。}}

{{Redis内存淘汰策略}}{{当Redis内存使用达到maxmemory上限时，根据配置的淘汰策略（maxmemory-policy）决定如何处理新写入的数据。策略包括：noeviction（不淘汰，返回错误）、allkeys-lru（所有key中淘汰最近最少使用的）、volatile-lru（只在设置了过期时间的key中淘汰LRU）、allkeys-lfu（Redis 4.0+，淘汰最不经常使用的）、volatile-lfu、allkeys-random、volatile-random、volatile-ttl（淘汰即将过期的key）。生产环境推荐allkeys-lfu或volatile-lru。}}

{{Redis Pipeline}}{{Pipeline是Redis客户端优化批量操作的技术。通常情况下，客户端发送一条命令后需要等待响应才能发送下一条命令（RTT开销累积）。Pipeline允许客户端一次性发送多条命令，不需要逐条等待响应，最后一次性读取所有响应。Pipeline不是原子操作，期间可能插入其他客户端的命令。Pipeline可以显著减少网络往返次数，提升批量操作的吞吐量。使用时注意控制Pipeline的批量大小（建议不超过1000条），避免阻塞服务端。}}

{{Redis慢查询日志}}{{Redis慢查询日志记录执行时间超过指定阈值的命令。配置参数：slowlog-log-slower-than（微秒，10000=10ms，0记录所有，-1禁用）、slowlog-max-len（日志队列最大长度）。命令：SLOWLOG GET N（获取N条慢查询）、SLOWLOG LEN（查看慢查询数量）、SLOWLOG RESET（清空）。慢查询日志存储在内存中，不写磁盘。生产环境建议设置10ms阈值，定期检查并优化慢查询。常见慢查询原因：大Key操作、KEYS命令、SORT命令、O(N)的集合操作。}}

{{Redis大Key问题}}{{大Key是指存储了大量数据或占用大量内存的Key。常见类型：大String（>10KB）、大Hash/Set/ZSet/List（元素数>5000或总大小>10MB）。大Key的危害：1）读写延迟高，阻塞其他请求；2）网络带宽消耗大；3）主从同步延迟；4）内存分布不均；5）删除时可能阻塞（DEL O(N)）。排查工具：redis-cli --bigkeys、MEMORY USAGE key、SCAN+TYPE。删除大Key应使用UNLINK（异步删除）而非DEL，或分批删除（HSCAN/SSCAN+HDEL/SREM）。}}

{{Redis监控指标}}{{关键监控指标：1）内存：used_memory、mem_fragmentation_ratio、evicted_keys；2）性能：instantaneous_ops_per_sec、latency、hit_rate（keyspace_hits/(keyspace_hits+keyspace_misses)）；3）连接：connected_clients、blocked_clients、rejected_connections；4）持久化：rdb_last_bgsave_status、aof_last_bgrewrite_status；5）复制：master_link_status、slave_repl_offset；6）集群：cluster_state、cluster_slots_ok。推荐监控工具：Redis INFO命令、redis-stat、Prometheus+Redis Exporter、RedisInsight。}}

{{Redis安全配置}}{{Redis安全最佳实践：1）设置requirepass密码认证；2）禁用危险命令（rename-command FLUSHALL ""、rename-command CONFIG ""）；3）绑定内网IP（bind 127.0.0.1）；4）启用TLS加密通信（Redis 6.0+）；5）使用ACL（Access Control List）控制用户权限（Redis 6.0+）；6）禁止以root用户运行Redis；7）定期更新Redis版本修复安全漏洞；8）网络层面使用防火墙限制访问端口。Redis默认没有密码且监听所有接口，生产环境必须配置安全措施。}}

{{Redis ACL访问控制}}{{Redis 6.0引入ACL（Access Control List）访问控制系统，取代了简单的密码认证。ACL可以创建多个用户，每个用户设置独立的密码和命令/键的访问权限。命令：ACL SETUSER/DELUSER/LIST/WHOAMI/GETUSER/SETUSER/LOAD/SAVE。权限粒度：命令级别（+@all、+get、-flushall）、键级别（~*、~user:*）、频道级别（&*）。默认用户为default，可以通过ACL文件（aclfile）持久化用户配置。ACL使得多租户Redis共享成为可能。}}

{{Redis Cluster扩缩容}}{{Redis Cluster支持在线扩缩容。扩容流程：1）启动新节点；2）CLUSTER MEET加入集群；3）CLUSTER ADDSLOTS分配槽位（或使用redis-cli --cluster reshard重新分配）；4）自动迁移数据。缩容流程：1）CLUSTER DELSLOTS移除槽位；2）将槽位迁移到其他节点；3）CLUSTER FORGET移除节点。迁移过程中，正在迁移的槽位的请求会被ASK重定向到目标节点。建议使用redis-cli --cluster工具自动化扩缩容操作，避免手动操作出错。}}

{{Redis客户端优化}}{{Redis客户端优化要点：1）使用连接池避免频繁创建/销毁连接；2）合理设置连接池大小（建议CPU核数*2）；3）使用Pipeline批量操作减少RTT；4）避免在主循环中执行阻塞命令（KEYS、大集合操作）；5）使用序列化压缩减少数据传输量；6）合理使用本地缓存减少Redis访问；7）注意客户端超时设置；8）使用连接池的健康检查机制。主流客户端：Jedis（Java）、Lettuce（Java，支持响应式）、redis-py（Python）、go-redis（Go）。}}

{{Redis生产环境部署建议}}{{生产环境部署建议：1）内存建议预留30%用于fork操作和碎片；2）单实例内存建议不超过20GB（避免fork延迟）；3）关闭THP（Transparent Huge Pages）以减少内存延迟；4）设置vm.overcommit_memory=1避免fork失败；5）TCP backlog设置为较大值（tcp-backlog 511）；6）合理设置timeout关闭空闲连接；7）开启AOF+RDB混合持久化；8）使用哨兵或集群保证高可用；9）定期备份RDB文件；10）使用Redis 7.0+版本获取最新性能优化和安全修复。}}
