# 数据可视化

## Matplotlib基础
- 折线图：plt.plot(x, y)
- 柱状图：plt.bar(x, y)
- 散点图：plt.scatter(x, y)
- 饼图：plt.pie(sizes, labels=labels)
- 子图：plt.subplot()、fig, axes = plt.subplots()

## Seaborn
- 统计图表：sns.histplot()、sns.boxplot()、sns.violinplot()
- 关系图：sns.scatterplot()、sns.lineplot()、sns.relplot()
- 分布图：sns.displot()、sns.kdeplot()
- 热力图：sns.heatmap()展示相关性矩阵
- 主题样式：sns.set_theme()设置全局样式

## Plotly交互式图表
- plotly.express：高级API，一行代码创建交互图表
- 散点图：px.scatter(df, x='col1', y='col2', color='category')
- 地图：px.choropleth()地理数据可视化
- 3D图表：px.scatter_3d()三维散点图
- 仪表盘：Dash框架，基于Plotly的Web应用

## 图表设计原则
- 选择合适的图表类型：趋势→折线、比较→柱状、分布→直方图、关系→散点
- 颜色使用：一致的配色方案，考虑色盲友好
- 标注：标题、轴标签、图例、数据标签
- 简洁：避免过多装饰（chartjunk）
- 数据墨水比：最大化数据展示，最小化非数据元素

## 报告与展示
- Jupyter Notebook：代码+文本+图表，交互式分析报告
- HTML导出：nbconvert转为HTML/PDF
- Streamlit：快速创建数据应用Web页面
- Plotly Dash：交互式数据仪表盘
- 数据故事：数据→洞察→行动的叙述方式
