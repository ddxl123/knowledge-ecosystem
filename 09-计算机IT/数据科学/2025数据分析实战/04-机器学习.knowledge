# 机器学习入门

## 监督学习
- 分类：逻辑回归、决策树、随机森林、SVM、KNN
- 回归：线性回归、岭回归、Lasso回归、随机森林回归
- 评估指标：准确率、精确率、召回率、F1、AUC-ROC
- 交叉验证：K-Fold交叉验证评估模型泛化能力
- 过拟合/欠拟合：正则化、增加数据、特征选择

## 无监督学习
- 聚类：K-Means、DBSCAN、层次聚类
- 降维：PCA主成分分析、t-SNE可视化
- 关联规则：Apriori算法
- 异常检测：Isolation Forest、One-Class SVM
- 评估：轮廓系数、Davies-Bouldin指数

## 特征工程
- 特征缩放：StandardScaler、MinMaxScaler
- 编码：One-Hot、Label Encoding、Target Encoding
- 特征选择：方差阈值、互信息、递归特征消除
- 特征构造：多项式特征、交互特征、时间特征
- 缺失值处理：均值/中位数/众数填充、KNN填充

## Scikit-learn
- 统一API：fit()训练、predict()预测、transform()转换
- Pipeline：串联预处理和模型
- GridSearchCV：网格搜索超参数调优
- 模型持久化：joblib.dump()/load()
- 数据集：train_test_split划分训练/测试集

## 实战流程
- 问题定义：明确业务目标和评估指标
- 数据探索：EDA，理解数据分布和关系
- 数据清洗：缺失值、异常值、重复值处理
- 特征工程：构建和选择特征
- 模型训练→评估→调优→部署
