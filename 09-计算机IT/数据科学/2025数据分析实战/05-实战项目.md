# 实战项目

## 核心知识点

### 一、数据分析流程

```python
# 完整分析流程
import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 数据加载
df = pd.read_csv('sales_data.csv')

# 2. 数据探索
print(df.info())
print(df.describe())
print(df.isnull().sum())

# 3. 数据清洗
df['date'] = pd.to_datetime(df['date'])
df = df.dropna(subset=['amount'])
df['amount'] = df['amount'].clip(lower=0)

# 4. 特征工程
df['month'] = df['date'].dt.month
df['weekday'] = df['date'].dt.weekday
df['quarter'] = df['date'].dt.quarter

# 5. 分析
monthly_sales = df.groupby('month')['amount'].sum()
category_sales = df.groupby('category')['amount'].agg(['sum', 'mean', 'count'])

# 6. 可视化
fig, axes = plt.subplots(2, 2, figsize=(14, 10))
monthly_sales.plot(ax=axes[0, 0], kind='line', marker='o')
category_sales['sum'].plot(ax=axes[0, 1], kind='bar')
plt.tight_layout()
plt.savefig('analysis_report.png')
```

### 二、ETL流程

```python
# Extract
def extract_data():
    """从多个数据源提取数据"""
    sales = pd.read_csv('sales.csv')
    customers = pd.read_sql('SELECT * FROM customers', conn)
    products = pd.read_json('products.json')
    return sales, customers, products

# Transform
def transform_data(sales, customers, products):
    """数据清洗与转换"""
    # 合并数据
    df = sales.merge(customers, on='customer_id') \
              .merge(products, on='product_id')

    # 计算指标
    df['revenue'] = df['quantity'] * df['unit_price']
    df['profit'] = df['revenue'] - df['cost']

    # 聚合
    result = df.groupby(['region', 'product_category']).agg({
        'revenue': 'sum',
        'profit': 'sum',
        'order_id': 'nunique'
    }).reset_index()

    return result

# Load
def load_data(df):
    """加载到数据仓库"""
    df.to_sql('sales_summary', engine, if_exists='replace', index=False)
    df.to_csv('output/sales_summary.csv', index=False)

# 执行ETL
sales, customers, products = extract_data()
result = transform_data(sales, customers, products)
load_data(result)
```

### 三、业务分析报告

```python
class SalesAnalyzer:
    def __init__(self, data_path):
        self.df = pd.read_csv(data_path)
        self.prepare_data()

    def prepare_data(self):
        self.df['date'] = pd.to_datetime(self.df['date'])
        self.df['revenue'] = self.df['quantity'] * self.df['price']

    def kpi_summary(self):
        """关键指标汇总"""
        return {
            'total_revenue': self.df['revenue'].sum(),
            'total_orders': self.df['order_id'].nunique(),
            'avg_order_value': self.df.groupby('order_id')['revenue'].sum().mean(),
            'customer_count': self.df['customer_id'].nunique(),
        }

    def trend_analysis(self, period='M'):
        """趋势分析"""
        return self.df.set_index('date').resample(period)['revenue'].sum()

    def cohort_analysis(self):
        """留存分析"""
        self.df['first_purchase'] = self.df.groupby('customer_id')['date'].transform('min')
        self.df['cohort'] = self.df['first_purchase'].dt.to_period('M')
        # ...
        return cohort_data

    def rfm_analysis(self):
        """RFM分析"""
        snapshot_date = self.df['date'].max() + pd.Timedelta(days=1)
        rfm = self.df.groupby('customer_id').agg({
            'date': lambda x: (snapshot_date - x.max()).days,
            'order_id': 'nunique',
            'revenue': 'sum'
        })
        rfm.columns = ['Recency', 'Frequency', 'Monetary']
        return rfm

    def generate_report(self):
        """生成分析报告"""
        kpis = self.kpi_summary()
        trend = self.trend_analysis()

        fig, axes = plt.subplots(2, 2, figsize=(14, 10))
        # 绘制图表...
        plt.savefig('report.png')
```

### 四、数据采集

```python
import requests
from bs4 import BeautifulSoup

def scrape_product_data(url):
    """网页数据采集"""
    response = requests.get(url)
    soup = BeautifulSoup(response.text, 'html.parser')

    products = []
    for item in soup.select('.product-card'):
        products.append({
            'name': item.select_one('.name').text.strip(),
            'price': float(item.select_one('.price').text.replace('¥', '')),
            'rating': float(item.select_one('.rating').text)
        })

    return pd.DataFrame(products)

# API数据采集
def fetch_api_data(endpoint, params):
    """API数据采集"""
    response = requests.get(endpoint, params=params)
    data = response.json()
    return pd.DataFrame(data['results'])
```
