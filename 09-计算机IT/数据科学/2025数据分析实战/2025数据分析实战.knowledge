# 数据分析实战
# 领域: 计算机IT
# 学科: 数据科学
# 来源: 数据分析实战（2025版）
# 描述: 数据分析全流程实战，涵盖数据清洗、探索性分析、统计方法、数据可视化及机器学习应用

{{数据分析流程}}{{数据分析标准流程：定义问题→数据收集→数据清洗→探索性分析(EDA)→特征工程→建模分析→结果解释→报告呈现。CRISP-DM是数据挖掘标准方法论。分析类型：描述性分析(发生了什么)、诊断性分析(为什么)、预测性分析(将发生什么)、规范性分析(该怎么做)。数据素养是核心能力。}}

{{Python数据分析工具栈}}{{NumPy：数组计算基础库，ndarray多维数组、广播机制、向量化运算。Pandas：数据分析核心库，Series(一维)和DataFrame(二维)数据结构。Matplotlib：基础绑图库。Seaborn：统计可视化库。SciPy：科学计算库(优化、插值、统计)。Scikit-learn：机器学习库。Jupyter Notebook交互式开发环境。Anaconda发行版一站式安装。}}

{{Pandas数据操作}}{{DataFrame创建：从CSV(pd.read_csv)、Excel(pd.read_excel)、SQL、JSON读取。数据选择：loc(标签)、iloc(位置)、条件筛选。数据清洗：dropna(缺失值)、drop_duplicates(重复值)、fillna(填充)、replace(替换)、astype(类型转换)。数据转换：apply(自定义函数)、map(映射)、groupby(分组聚合)、pivot_table(透视表)、merge/join(合并)。时间序列：pd.to_datetime、resample(重采样)。}}

{{数据清洗}}{{常见数据质量问题：缺失值、异常值、重复数据、格式不一致、类型错误。缺失值处理：删除(dropna)、均值/中位数/众数填充、插值法、KNN填充。异常值检测：Z-score(|z|>3)、IQR方法(Q1-1.5*IQR ~ Q3+1.5*IQR)、箱线图可视化。数据标准化：Min-Max归一化、Z-score标准化。字符串清洗：strip、正则表达式。数据验证和质量报告。}}

{{探索性数据分析(EDA)}}{{EDA目标：了解数据分布、发现模式、验证假设。单变量分析：直方图(分布)、箱线图(离散度)、频率统计。双变量分析：散点图(相关性)、分组箱线图、交叉表。多变量分析：热力图(相关矩阵)、配对图(pairplot)。描述统计：mean/std/min/max/percentiles。偏度(skewness)和峰度(kurtosis)。假设检验前置分析。}}

{{数据可视化}}{{Matplotlib基础：figure/axes对象模型，plot/scatter/bar/hist/pie等图表类型。Seaborn高级：sns.histplot/sns.boxplot/sns.heatmap/sns.pairplot。Plotly交互式：可缩放、悬停显示详情。图表选择指南：比较用柱状图、趋势用折线图、分布用直方图/箱线图、关系用散点图、构成用饼图/堆叠图。可视化原则：简洁、准确、有洞察力。配色方案和图表美化。}}

{{统计分析基础}}{{描述统计：集中趋势(均值、中位数、众数)、离散程度(方差、标准差、IQR)、分布形状(偏度、峰度)。概率分布：正态分布、二项分布、泊松分布。中心极限定理。假设检验：t检验(均值差异)、卡方检验(独立性)、ANOVA(多组均值)。p值和显著性水平(α=0.05)。置信区间。相关分析：Pearson(线性)、Spearman(秩次)。统计功效和样本量计算。}}

{{特征工程}}{{特征工程将原始数据转换为模型可用特征。数值特征：标准化、归一化、对数变换、多项式特征。类别特征：One-Hot编码、Label编码、Target编码、频率编码。时间特征：提取年/月/日/星期/小时、时间差、周期性编码。文本特征：TF-IDF、词袋模型、Word2Vec。特征选择：方差过滤、相关性过滤、递归特征消除(RFE)、L1正则化。降维：PCA、t-SNE、UMAP。}}

{{回归分析}}{{线性回归：y = β₀ + β₁x₁ + ... + βₙxₙ。评估指标：R²(决定系数)、MSE/RMSE(均方误差)、MAE(平均绝对误差)。多重共线性检测：VIF(方差膨胀因子)。正则化回归：Ridge(L2)、Lasso(L1)、ElasticNet(L1+L2)。非线性回归：多项式回归。假设检验：残差正态性、同方差性、独立性。Scikit-learn API：fit→predict→score。}}

{{分类分析}}{{逻辑回归(Logistic Regression)：二分类基础算法。决策树(Decision Tree)：可解释性强，信息增益/基尼系数。随机森林(Random Forest)：集成多棵决策树。SVM(支持向量机)：寻找最大间隔超平面。KNN(K近邻)：基于距离的惰性学习。评估指标：准确率、精确率、召回率、F1-Score、AUC-ROC曲线。混淆矩阵。交叉验证(K-Fold)。}}

{{聚类分析}}{{K-Means：基于距离的划分聚类，需预设K值，肘部法则/轮廓系数选择K。DBSCAN：基于密度的聚类，能发现任意形状簇，自动处理噪声点。层次聚类：凝聚/分裂方法，树状图可视化。GMM(高斯混合模型)：概率聚类。聚类评估：轮廓系数(Silhouette)、Calinski-Harabasz指数、Davies-Bouldin指数。聚类结果可视化和解释。}}

{{时间序列分析}}{{时间序列组成：趋势(Trend)、季节性(Seasonality)、残差(Residual)。平稳性检验：ADF检验。差分使序列平稳。自相关(ACF)和偏自相关(PACF)分析。ARIMA模型：AR(自回归)+I(差分)+MA(移动平均)。SARIMA：加入季节性成分。Prophet(Facebook)：自动处理趋势、季节性、节假日。评估：MAPE、RMSE。预测区间。}}

{{A/B测试}}{{A/B测试是因果推断的金标准。流程：假设→设计→随机分组→实验→分析→决策。关键指标：转化率、点击率、收入。样本量计算：统计功效分析。显著性检验：卡方检验(比例)、t检验(均值)。多重比较校正：Bonferroni校正。陷阱：辛普森悖论、选择偏差、新奇效应。序贯分析：提前终止决策。贝叶斯A/B测试。}}

{{数据报告与故事叙述}}{{数据报告结构：背景→方法→发现→建议→附录。数据故事叙述：设定情境→展示数据→揭示洞察→建议行动。可视化叙事：选择合适的图表、突出关键信息、避免误导。工具：Jupyter Notebook(分析+报告)、Tableau/Power BI(仪表盘)、Streamlit/Dash(数据应用)。一页纸报告(Executive Summary)。演示技巧。}}

{{大数据分析入门}}{{大数据特征：Volume(量大)、Velocity(速度快)、Variety(多样)、Value(价值密度低)、Veracity(真实性)。Hadoop生态：HDFS(分布式文件)、MapReduce(分布式计算)、YARN(资源管理)。Spark：内存计算框架，比MapReduce快100倍。PySpark：Python接口。SQL-on-Hadoop：Hive、Presto/Trino。数据仓库：ClickHouse、Snowflake。数据湖：Delta Lake、Iceberg。}}

{{机器学习实战}}{{监督学习：分类和回归。无监督学习：聚类和降维。Scikit-learn标准流程：train_test_split→model.fit→model.predict→评估。特征工程→模型选择→超参调优(GridSearchCV/RandomizedSearchCV)→交叉验证→模型评估→模型部署。集成方法：Bagging(随机森林)、Boosting(XGBoost/LightGBM)、Stacking。模型解释：SHAP值、特征重要性。}}

{{数据采集与爬虫}}{{数据来源：API接口、数据库导出、文件导入、网络爬虫。Python爬虫库：requests(HTTP)、BeautifulSoup(HTML解析)、Scrapy(爬虫框架)、Selenium(动态页面)。数据格式：CSV、JSON、XML、Parquet、Excel。API调用：REST API、GraphQL。爬虫礼仪：robots.txt、请求频率限制、User-Agent。数据采集法律合规。增量爬取策略。}}

{{SQL数据分析}}{{SQL是数据分析师必备技能。高级查询：窗口函数(ROW_NUMBER/RANK/SUM OVER)、CTE(公共表表达式)、子查询、CASE条件表达式。分组分析：GROUP BY + 聚合函数。时间序列SQL：DATE_FORMAT/EXTRACT、LAG/LEAD。留存分析：用户次日/7日/30日留存率。漏斗分析：各步骤转化率。RFM模型：Recency/Frequency/Monetary。}}

{{数据隐私与伦理}}{{数据隐私法规：GDPR(欧盟)、个人信息保护法(中国)。数据脱敏：泛化、抑制、扰动、k-匿名。差分隐私(Differential Privacy)：在数据中添加噪声保护个体。数据伦理：算法偏见检测和缓解、公平性指标。知情同意和数据最小化原则。数据安全：访问控制、加密、审计日志。隐私计算：联邦学习、安全多方计算。}}
