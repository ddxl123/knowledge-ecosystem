# 2025算法竞赛入门
# 领域: 计算机IT
# 学科: 算法竞赛
# 来源: 2025算法竞赛入门
# 描述: 算法竞赛入门核心知识点与解题技巧

{{知识点1}}{{时间复杂度分析
时间复杂度用大O表示法衡量算法执行时间随输入规模增长的趋势。常见复杂度从低到高：O(1) < O(log n) < O(n) < O(n log n) < O(n²) < O(2ⁿ)。竞赛中一般要求 n≤10⁶ 时选择 O(n log n) 或更优的算法，n≤10³ 可用 O(n²)。空间复杂度同理，需注意内存限制（通常256MB）。}}

{{知识点2}}{{空间换时间的思想
用额外的存储空间来减少计算时间。典型手段：预处理数组、哈希表存储中间结果、记忆化搜索。例如用一个布尔数组标记已访问节点，避免重复遍历。核心权衡：空间复杂度增加但时间复杂度降低。}}

{{知识点3}}{{贪心算法
每一步都选择当前最优解，期望最终得到全局最优。适用于具有"贪心选择性质"和"最优子结构"的问题。经典应用：区间调度问题（按结束时间排序后贪心选取）、哈夫曼编码、最小生成树（Prim/Kruskal）。注意：贪心不保证全局最优，需证明正确性。}}

{{知识点4}}{{二分查找
在有序序列中查找目标值，每次将搜索范围缩小一半。时间复杂度 O(log n)。关键细节：mid = l + (r - l) / 2 防溢出；边界条件 l <= r 还是 l < r 取决于区间定义（闭区间/左闭右开）。变体：查找第一个满足条件的位置（二分答案）。}}

{{知识点5}}{{二分答案
当答案具有单调性时，将"求最优解"转化为"判定某个值是否可行"。步骤：确定答案范围 → 二分猜测答案 → 用 check 函数验证 → 调整边界。典型题：最小化最大值、最大化最小值、Koko吃香蕉等。check 函数的复杂度决定总复杂度。}}

{{知识点6}}{{排序算法
常见排序：快速排序（平均 O(n log n)，不稳定）、归并排序（稳定 O(n log n)，可求逆序对）、堆排序（O(n log n)，原地）。STL sort 使用内省排序（introsort），综合快排、堆排、插入排序。自定义比较函数：返回 bool，严格弱序。}}

{{知识点7}}{{前缀和
一维前缀和：s[i] = a[1] + a[2] + ... + a[i]，区间和 [l,r] = s[r] - s[l-1]。二维前缀和：s[i][j] 为左上角 (1,1) 到 (i,j) 的矩形和，子矩阵和需容斥原理。构建 O(n)，查询 O(1)，是处理区间和问题的基础工具。}}

{{知识点8}}{{差分数组
差分是前缀和的逆运算。d[i] = a[i] - a[i-1]。对区间 [l,r] 统一加 val：d[l] += val, d[r+1] -= val。最后对 d 求前缀和还原数组。适用于多次区间修改、最后统一查询的场景，将 O(n) 的区间修改降为 O(1)。}}

{{知识点9}}{{双指针技巧
两个指针在序列上按规则移动，常见模式：①对撞指针（有序数组两数之和）②快慢指判环③滑动窗口（维护区间性质）。核心：利用单调性使每个元素最多被访问常数次，总复杂度 O(n)。典型题：三数之和、接雨水、最长无重复子串。}}

{{知识点10}}{{滑动窗口
维护一个满足条件的连续子区间。右端点扩展时更新状态，左端点收缩以维持性质。模板：for r in range(n): 更新; while 不满足: 左移l并更新。时间复杂度 O(n)。应用：最小覆盖子串、最长无重复子串、定长子数组最大和。}}

{{知识点11}}{{栈的应用
后进先出（LIFO）。经典应用：①括号匹配②表达式求值（调度场算法）③单调栈（找下一个更大/更小元素）④DFS 非递归实现。单调栈：维护栈内元素单调递增或递减，每个元素最多入栈出栈各一次，O(n)。典型题：柱状图最大矩形。}}

{{知识点12}}{{队列与单调队列
先进先出（FIFO）。双端队列 deque 支持两端操作。单调队列：维护滑动窗口内的最值，队头为当前最值，入队时弹出不满足单调性的队尾元素。应用：滑动窗口最大/最小值、优化 DP（如多重背包）。}}

{{知识点13}}{{链表
由节点组成，每个节点包含数据和指向下一节点的指针。单链表、双链表、循环链表。竞赛中常用数组模拟链表（静态链表）：e[i] 存值，ne[i] 存下一节点下标。优势：避免动态内存分配的开销。应用：LRU 缓存、邻接表。}}

{{知识点14}}{{哈希表
通过哈希函数将键映射到数组下标，实现 O(1) 平均查找。冲突处理：链地址法、开放寻址法。C++ 中 unordered_map/unordered_set。竞赛技巧：手动实现哈希表（拉链法或开放寻址）可大幅提速。应用：计数、去重、判重。}}

{{知识点15}}{{二叉树基础
每个节点最多两个子节点。遍历方式：前序（根左右）、中序（左根右）、后序（左右根）、层序（BFS）。完全二叉树可用数组存储：节点 i 的左子为 2i，右子为 2i+1，父为 i/2。二叉搜索树（BST）：左子 < 根 < 右子，中序遍历有序。}}

{{知识点16}}{{堆（优先队列）
完全二叉树，满足堆性质：最大堆（父 ≥ 子）或最小堆（父 ≤ 子）。C++ priority_queue 默认最大堆。支持 O(log n) 插入和取最值。应用：Dijkstra 算法、合并 K 个有序链表、任务调度。建堆 O(n)，逐个插入 O(n log n)。}}

{{知识点17}}{{并查集（Union-Find）
处理不相交集合的合并与查询。两个操作：Find（查找元素所属集合的根，路径压缩）、Union（合并两个集合，按秩合并）。路径压缩 + 按秩合并后近似 O(α(n))，α 为反阿克曼函数，几乎为常数。应用：连通分量、Kruskal 最小生成树。}}

{{知识点18}}{{BFS（广度优先搜索）
从起点开始，逐层扩展访问所有相邻节点。用队列实现。适用于：最短路径（无权图）、层序遍历、迷宫最短距离。时间复杂度 O(V+E)。变体：双向 BFS（从起点和终点同时搜索，适用于知道终点的情况）、0-1 BFS（deque 实现）。}}

{{知识点19}}{{DFS（深度优先搜索）
沿一条路径尽可能深入，回溯到上一节点继续探索。用递归或栈实现。适用于：全排列、组合、连通分量、拓扑排序、迷宫路径。注意递归深度限制（可手动设置栈大小）。DFS 序和时间戳可用于子树问题。}}

{{知识点20}}{{最短路径 - Dijkstra 算法
求单源最短路径，适用于非负权图。贪心思想：每次取未访问的距离最小节点松弛邻居。朴素版 O(V²)，优先队列优化 O((V+E) log V)。注意：不能处理负权边。堆优化版中 dist 数组需配合 visited 数组或懒删除。}}

{{知识点21}}{{最短路径 - Bellman-Ford 与 SPFA
Bellman-Ford：松弛所有边 V-1 次，可检测负权环，O(VE)。SPFA 是其队列优化：只将距离变小的节点入队，平均 O(E)，最坏 O(VE)。2025 年竞赛中 SPFA 可能被卡（构造数据退化），优先用 Dijkstra。}}

{{知识点22}}{{最短路径 - Floyd-Warshall
全源最短路径，三重循环枚举中间点 k、起点 i、终点 j。O(V³)。适用于 V≤400 的小图。可检测负权环（对角线为负）。也可用于传递闭包（判断连通性）。实现简单，适合邻接矩阵存储。}}

{{知识点23}}{{拓扑排序
对有向无环图（DAG）进行线性排序，使得所有边 u→v 中 u 排在 v 前面。BFS（Kahn 算法）：入度为 0 的节点入队。可用于判环（排序后节点数 < 总数则有环）。应用：任务调度、DP 顺序、关键路径。}}

{{知识点24}}{{最小生成树（MST）
Prim 算法：类似 Dijkstra，从任一点出发每次取最小边扩展，适合稠密图 O(V²) 或堆优化 O(E log V)。Kruskal 算法：将所有边按权值排序，依次加入不形成环的边（并查集判环），适合稀疏图 O(E log E)。}}

{{知识点25}}{{动态规划基础
将问题分解为重叠子问题，通过填表避免重复计算。要素：状态定义、状态转移方程、边界条件、计算顺序。步骤：明确状态 → 找转移方程 → 确定初始值 → 确定遍历顺序。时间复杂度 = 状态数 × 单次转移代价。}}

{{知识点26}}{{背包问题
01背包：n 件物品，每件选或不选，容量 W 的最大价值。dp[j] = max(dp[j], dp[j-w[i]] + v[i])，一维倒序遍历。完全背包：每件无限选，正序遍历。多重背包：每件有限个，二进制拆分或单调队列优化。分组背包：每组选一个。}}

{{知识点27}}{{最长公共子序列（LCS）
给两个序列，求最长公共子序列长度。dp[i][j] 表示前 i 和前 j 个元素的 LCS。若 a[i]==b[j]，dp[i][j]=dp[i-1][j-1]+1；否则 dp[i][j]=max(dp[i-1][j], dp[i][j-1])。时间 O(nm)，空间可优化到 O(min(n,m))。}}

{{知识点28}}{{最长递增子序列（LIS）
在序列中找最长的严格递增子序列。朴素 O(n²)：dp[i] 为以 a[i] 结尾的 LIS。贪心+二分 O(n log n)：维护数组 d，d[k] 表示长度为 k 的 LIS 的最小末尾，对每个元素二分查找替换位置。}}

{{知识点29}}{{区间 DP
在区间上进行动态规划。dp[i][j] 表示区间 [i,j] 的最优解。枚举分割点 k ∈ [i,j)，dp[i][j] = min/max(dp[i][k] + dp[k+1][j] + cost)。遍历顺序：先短后长（len 从小到大）。典型题：矩阵链乘法、石子合并、戳气球。}}

{{知识点30}}{{数位 DP
按位统计满足条件的数字个数。通常用记忆化搜索：dfs(pos, state, tight, lead) 表示当前处理第 pos 位，状态为 state，是否受上界限制，是否有前导零。典型题：计数不含某数字的数、数字之和为某值的数的个数。}}

{{知识点31}}{{状态压缩 DP
用二进制表示集合状态。dp[S] 表示已选集合为 S 时的最优解。典型题：旅行商问题（TSP）——dp[S][i] 表示已访问城市集合 S、当前在城市 i 的最短路径，O(2ⁿ·n)。适用于 n≤20 的小规模集合问题。}}

{{知识点32}}{{树形 DP
在树上进行动态规划。通常后序遍历（自底向上）。dp[u] 表示以 u 为根的子树的某种最优值。状态转移考虑子节点的贡献。典型题：树的直径、树的重心、树上最大独立集、树上背包。换根 DP：两次遍历处理以每个节点为根的情况。}}

{{知识点33}}{{质数与筛法
判断质数：试除法 O(√n)。埃拉托斯特尼筛法：O(n log log n)，标记合数。线性筛（欧拉筛）：O(n)，每个合数只被最小质因子筛一次，同时记录最小质因子。质数定理：≤n 的质数约 n/ln(n) 个。}}

{{知识点34}}{{GCD 与扩展欧几里得
辗转相除法求最大公约数：gcd(a,b) = gcd(b, a%b)，O(log(min(a,b)))。扩展欧几里得：求 ax + by = gcd(a,b) 的整数解 (x,y)。可用于求模逆元：当 gcd(a,m)=1 时，ax ≡ 1 (mod m) 的解为 x。}}

{{知识点35}}{{快速幂
计算 a^b mod p，O(log b)。将 b 二进制分解，逐位累乘。递推式：若 b 为偶数，a^b = (a^(b/2))²；若奇数，a^b = a·(a^(b/2))²。注意中间结果可能溢出，用 long long 或 __int128。矩阵快速幂可优化线性递推。}}

{{知识点36}}{{模运算与逆元
(a + b) mod p = ((a mod p) + (b mod p)) mod p，乘法同理。除法需转为乘以逆元：a/b mod p = a × b⁻¹ mod p。求逆元：费马小定理（p 为质数时 b⁻¹ = b^(p-2) mod p）或扩展欧几里得。预处理阶乘逆元可批量求组合数。}}

{{知识点37}}{{组合数学 - 组合数
C(n,k) = n! / (k! × (n-k)!)。杨辉三角递推：C(n,k) = C(n-1,k-1) + C(n-1,k)。Lucas 定理：大 n 小 p 时将 n,k 转 p 进制逐位求组合数再相乘。预处理阶乘及其逆元，O(1) 查询单个组合数。}}

{{知识点38}}{{字符串哈希
将字符串映射为数值，快速比较子串是否相等。常用多项式哈希：H(s) = Σ s[i] × base^(n-i-1) mod mod。双哈希降低冲突概率。子串哈希：通过前缀哈希 O(1) 计算任意子串哈希值。应用：字符串匹配、最长公共子串、回文判断。}}

{{知识点39}}{{KMP 算法
字符串匹配算法，O(n+m)。核心：next 数组（失配函数），记录模式串每个前缀的最长相等前后缀长度。匹配时失配不回退主串指针，而是根据 next 数组跳转模式串指针。应用：求出现次数、最短循环节（len - next[len]）。}}

{{知识点40}}{{Trie（字典树）
多叉树结构，每条边对应一个字符，从根到叶的路径表示一个字符串。支持 O(L) 插入和查询（L 为字符串长度）。应用：前缀查询、字符串集合判重、最大 XOR 对（01-Trie）。空间可用数组模拟：trie[N][26]。}}

{{知识点41}}{{线段树
支持区间查询和区间修改的高级数据结构，O(log n)。每个节点代表一个区间，维护区间和/最值等信息。懒标记（lazy propagation）：延迟更新，查询时下推。实现复杂但功能强大。应用：区间加/乘、区间最值、动态开点线段树。}}

{{知识点42}}{{树状数组（Fenwick Tree）
支持单点修改和前缀查询，O(log n)。比线段树代码更短、常数更小。lowbit(x) = x & (-x)。区间修改+区间查询需两个树状数组。应用：求逆序对、动态排名、前缀和维护。二维树状数组支持子矩阵查询。}}

{{知识点43}}{{ST 表（稀疏表）
用于静态区间最值查询（RMQ），O(n log n) 预处理，O(1) 查询。st[i][j] 表示从 i 开始长度为 2^j 的区间最值。查询时取两个重叠区间覆盖目标区间。不支持修改。应用：LCA（欧欧序+RMQ）、静态区间 GCD。}}

{{知识点44}}{{单调栈技巧
维护栈内元素单调递增或递减。应用：①每个元素左边/右边第一个更大/更小元素②柱状图最大矩形面积③接雨水④每日温度（下一个更高温度天数）。每个元素最多入栈出栈一次，总体 O(n)。注意处理相等元素的策略。}}

{{知识点45}}{{位运算技巧
常用技巧：①n & (n-1) 去掉最低位 1 ②n & (-n) 取最低位 1 ③枚举子集：for(int s=sub; s; s=(s-1)&sub) ④判断 2 的幂：n > 0 && (n & (n-1)) == 0 ⑤异或性质：a^a=0, a^0=a（找唯一出现一次的数）。位运算常用于状态压缩和优化。}}

{{知识点46}}{{递归与回溯
递归：函数调用自身，需有终止条件。回溯：在递归过程中尝试所有可能的选择，发现当前选择不可行时撤销（回退）继续尝试。模板：选择 → 递归 → 撤销选择。应用：全排列、N 皇后、数独、子集枚举。剪枝可大幅减少搜索空间。}}

{{知识点47}}{{BFS 求最短路（网格图）
在网格/迷宫中用 BFS 求最短距离。方向数组 dx[] = {-1,1,0,0}, dy[] = {0,0,-1,1}。注意边界判断和已访问标记。多源 BFS：多个起点同时入队，求每个点到最近源点的距离。0-1 BFS：边权为 0 或 1 时用 deque 代替优先队列。}}

{{知识点48}}{{构造题技巧
构造题要求输出满足条件的任意解。常见策略：①从小规模找规律②贪心构造③分治构造④利用数学性质（奇偶性、模运算）。先手写小规模样例观察模式，再推广。注意题目的充分必要条件，有时需证明构造的正确性。}}

{{知识点49}}{{输入输出优化
大数据量时 cin/cout 较慢。优化方法：①ios::sync_with_stdio(false); cin.tie(0) ②使用 scanf/printf ③手写快读：逐字符读取并转换为整数。Python 中用 sys.stdin.read() 一次性读入。输出时避免频繁 flush。大量数据建议一次性读入后处理。}}

{{知识点50}}{{常见调试技巧
①assert 断言检查中间状态②对拍：暴力解与优化解对比随机数据③打印中间变量定位错误④边界测试：n=0, n=1, 最大值⑤特判：注意溢出（int vs long long）、数组越界、初始化。竞赛中先写暴力保底，再优化。}}
