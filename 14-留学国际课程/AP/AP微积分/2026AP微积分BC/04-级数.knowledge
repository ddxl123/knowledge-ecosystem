# 级数（Series）

## 收集指南

> ⚠️ **严格范围限定：** 仅收集本章（第4章 级数）相关内容，禁止跨章节引用。例如：不得将其他章节的知识点混入本章。

### 条目描述
无穷级数是AP微积分BC的特色内容，包括数列、级数和幂级数。

### 知识结构
- 数列 - 收敛和发散
- 级数 - 收敛判别法
- 幂级数 - 泰勒级数

### 待收集原子知识点
- 数列收敛：数列极限的概念和判断方法
- 级数收敛：部分和、收敛判别法
- 幂级数：收敛半径、泰勒级数和麦克劳林级数
- 常用展开：e^x、sin(x、cos(x)的幂级数展开

## 核心知识点

### 一、数项级数
```
1. 级数的概念
- 无穷级数：Σ(n=1→∞) aₙ
- 部分和：Sₙ = a₁ + a₂ + ... + aₙ
- 收敛：lim(n→∞) Sₙ 存在
- 发散：lim(n→∞) Sₙ 不存在

2. 收敛判别法
- 第n项判别法：lim(n→∞) aₙ ≠ 0，则级数发散
- 几何级数：Σ rⁿ 收敛当且仅当 |r| < 1
- p-级数：Σ 1/nᵖ 收敛当且仅当 p > 1
- 比较判别法
- 比值判别法：lim(n→∞) |aₙ₊₁/aₙ| = L
- 根值判别法：lim(n→∞) ⁿ√|aₙ| = L
- 积分判别法

3. 交错级数
- Σ (-1)ⁿ aₙ
- 莱布尼茨判别法：aₙ递减且lim aₙ = 0
```

### 二、幂级数
```
1. 幂级数的概念
- Σ(n=0→∞) cₙ(x-a)ⁿ
- 收敛半径R
- 收敛区间

2. 收敛半径的求法
- 比值法：1/R = lim(n→∞) |cₙ₊₁/cₙ|
- 根值法：1/R = lim(n→∞) ⁿ√|cₙ|

3. 幂级数的运算
- 加减法
- 乘法
- 逐项求导
- 逐项积分
```

### 三、泰勒级数
```
1. 泰勒公式
- f(x) = Σ(n=0→∞) f⁽ⁿ⁾(a)/n! · (x-a)ⁿ
- 麦克劳林级数：a = 0

2. 常见函数的麦克劳林级数
- eˣ = Σ xⁿ/n! = 1 + x + x²/2! + x³/3! + ...
- sin x = Σ (-1)ⁿ x²ⁿ⁺¹/(2n+1)! = x - x³/3! + x⁵/5! - ...
- cos x = Σ (-1)ⁿ x²ⁿ/(2n)! = 1 - x²/2! + x⁴/4! - ...
- 1/(1-x) = Σ xⁿ = 1 + x + x² + x³ + ... (|x| < 1)
- ln(1+x) = Σ (-1)ⁿ⁺¹ xⁿ/n = x - x²/2 + x³/3 - ... (-1 < x ≤ 1)
- arctan x = Σ (-1)ⁿ x²ⁿ⁺¹/(2n+1) = x - x³/3 + x⁵/5 - ... (|x| ≤ 1)

3. 泰勒级数的应用
- 近似计算
- 求极限
- 求不定积分
```

### 四、参数方程与极坐标
```
1. 参数方程
- x = f(t), y = g(t)
- dy/dx = (dy/dt)/(dx/dt)
- 弧长：L = ∫ √[(dx/dt)² + (dy/dt)²] dt

2. 极坐标
- r = f(θ)
- x = r cos θ, y = r sin θ
- 面积：A = ½ ∫ r² dθ
- 弧长：L = ∫ √[r² + (dr/dθ)²] dθ

3. 向量函数
- r(t) = ⟨x(t), y(t)⟩
- 速度：v(t) = r'(t)
- 加速度：a(t) = r''(t)
- 速率：|v(t)|
```

### 五、BC特有内容
```
1. 洛必达法则的扩展
- 适用于0/0型和∞/∞型
- 可以重复使用

2. 反常积分的收敛性
- 比较判别法
- 极限比较判别法

3. 欧拉方法
- 数值求解微分方程
- y_{n+1} = y_n + h f(x_n, y_n)

4. 逻辑斯蒂模型
- dP/dt = kP(M - P)
- P(t) = M / (1 + Ae^(-kMt))
```

## 2026年更新要点
- 新增了级数的图形化理解
- 增加了泰勒级数的实际应用
- 更新了参数方程和极坐标的考查方式
- 新增了向量函数的综合应用
