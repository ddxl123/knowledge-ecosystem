# AP 微积分 BC — 无穷级数

## 数列与级数

### 定义
数列是有序的数的列表，级数是数列各项的和。

### 核心概念
- **数列极限：** lim(n→∞) aₙ = L，数列收敛于 L
- **级数定义：** Σ(n=1 to ∞) aₙ = a₁ + a₂ + a₃ + ...
- **部分和：** Sₙ = Σ(k=1 to n) aₙ，级数收敛 ⟺ lim(n→∞) Sₙ 存在
- **级数收敛的必要条件：** 若 Σaₙ 收敛，则 lim(n→∞) aₙ = 0（反之不成立）

### 关键要点
- lim aₙ = 0 是必要条件，不是充分条件（如调和级数发散）
- 部分和序列的极限决定级数的敛散性
- AP 考试常考判断级数是否收敛

### 典型例题
调和级数 Σ1/n 发散，尽管 lim(n→∞) 1/n = 0

## 常用级数

### 定义
AP 微积分 BC 中需要掌握的几种重要级数。

### 核心概念
- **几何级数：** Σarⁿ，|r| < 1 时收敛于 a/(1-r)，|r| ≥ 1 时发散
- **p-级数：** Σ1/nᵖ，p > 1 收敛，p ≤ 1 发散
- **调和级数：** Σ1/n（p = 1 的 p-级数），发散
- **交错调和级数：** Σ(-1)^(n+1)/n，条件收敛于 ln 2
- **幂级数的基本形式：** Σcₙ(x-a)ⁿ，以 a 为中心

### 关键要点
- 几何级数的收敛条件必须牢记
- p-级数的分界点是 p = 1
- 这些级数常作为比较判别法的参照

### 典型例题
Σ(n=0 to ∞) (1/2)ⁿ = 1/(1-1/2) = 2（几何级数，r = 1/2）

## 收敛判别法

### 定义
判断无穷级数收敛或发散的各种方法。

### 核心概念
- **比较判别法：** 0 ≤ aₙ ≤ bₙ，若 Σbₙ 收敛则 Σaₙ 收敛；若 Σaₙ 发散则 Σbₙ 发散
- **极限比较判别法：** lim(n→∞) aₙ/bₙ = c（有限正数），则两者同敛散
- **比值判别法：** lim(n→∞) |aₙ₊₁/aₙ| = L，L < 1 收敛，L > 1 发散，L = 1 不确定
- **根值判别法：** lim(n→∞) ⁿ√|aₙ| = L，判别标准同比值判别法
- **积分判别法：** aₙ = f(n)，∫[1,∞] f(x)dx 与 Σaₙ 同敛散

### 关键要点
- 比值判别法对含阶乘和幂的级数特别有效
- L = 1 时比值判别法失效，需用其他方法
- AP 考试中比值判别法使用频率最高

### 典型例题
Σn!/nⁿ，用比值判别法：lim |aₙ₊₁/aₙ| = lim (n+1)!/(n+1)^(n+1) · nⁿ/n! = lim (n/(n+1))ⁿ = 1/e < 1，收敛

## 绝对收敛与条件收敛

### 定义
绝对收敛指级数各项取绝对值后仍收敛，条件收敛指原级数收敛但取绝对值后发散。

### 核心概念
- **绝对收敛：** Σ|aₙ| 收敛，则 Σaₙ 收敛
- **条件收敛：** Σaₙ 收敛但 Σ|aₙ| 发散
- **交错级数判别法（莱布尼茨判别法）：** Σ(-1)ⁿbₙ，若 bₙ 单调递减趋于 0，则收敛
- **重排定理：** 绝对收敛级数可任意重排；条件收敛级数重排可能改变和

### 关键要点
- 绝对收敛 ⟹ 收敛，反之不成立
- 交错级数判别法仅判断收敛，不给出和的值
- AP 考试常考交错级数的条件收敛

### 典型例题
Σ(-1)^(n+1)/n² 绝对收敛（因为 Σ1/n² 是 p=2 的 p-级数，收敛）

## 泰勒级数与麦克劳林级数

### 定义
泰勒级数是用多项式逼近函数的无穷级数表示。

### 核心概念
- **泰勒级数：** f(x) = Σ(n=0 to ∞) [f⁽ⁿ⁾(a)/n!] (x-a)ⁿ
- **麦克劳林级数：** a = 0 时的泰勒级数，f(x) = Σf⁽ⁿ⁾(0)/n! · xⁿ
- **常用麦克劳林级数：**
  - eˣ = Σxⁿ/n! = 1 + x + x²/2! + x³/3! + ...
  - sin x = Σ(-1)ⁿx^(2n+1)/(2n+1)! = x - x³/3! + x⁵/5! - ...
  - cos x = Σ(-1)ⁿx^(2n)/(2n)! = 1 - x²/2! + x⁴/4! - ...
  - 1/(1-x) = Σxⁿ = 1 + x + x² + x³ + ...（|x| < 1）
  - ln(1+x) = Σ(-1)^(n+1)xⁿ/n = x - x²/2 + x³/3 - ...（-1 < x ≤ 1）

### 关键要点
- 必须熟记 eˣ、sin x、cos x、1/(1-x) 的麦克劳林级数
- 泰勒级数的收敛半径需要单独判断
- AP 考试中常考级数的前几项近似

### 典型例题
用麦克劳林级数前 3 项近似 e^0.1 ≈ 1 + 0.1 + 0.01/2 = 1.105

## 幂级数

### 定义
幂级数是以 (x-a)ⁿ 为通项的级数，是函数的级数表示。

### 核心概念
- **收敛半径 R：** |x-a| < R 时收敛，|x-a| > R 时发散
- **求 R 的方法：** R = 1/L，其中 L = lim|cₙ₊₁/cₙ|（比值法）
- **收敛区间：** (a-R, a+R)，端点需单独检验
- **逐项求导：** (Σcₙ(x-a)ⁿ)' = Σncₙ(x-a)^(n-1)，收敛半径不变
- **逐项积分：** ∫Σcₙ(x-a)ⁿ dx = Σcₙ(x-a)^(n+1)/(n+1) + C，收敛半径不变

### 关键要点
- 收敛半径相同，但端点收敛性可能不同
- 逐项求导和积分不改变收敛半径
- AP 考试中幂级数是 BC 独有的高频考点

### 典型例题
求 Σxⁿ/n 的收敛半径和区间：R = 1/lim(1/(n+1)·n) = 1，收敛区间 (-1,1]，x=1 发散，x=-1 收敛
