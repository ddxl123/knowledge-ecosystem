# AP 微积分 BC — 微分方程

## 一阶可分离变量方程

### 定义
可分离变量微分方程是指可以将所有 y 项和 dy 移到一边，所有 x 项和 dx 移到另一边的方程。

### 核心概念
- **标准形式：** dy/dx = f(x)·g(y)
- **求解步骤：** 分离变量 → 两边积分 → 整理得通解
- **即：** ∫(1/g(y))dy = ∫f(x)dx + C
- **特解：** 利用初始条件确定常数 C

### 关键要点
- 分离时注意 g(y) = 0 的常数解
- 积分后可能需要隐式表达
- AP 考试中常给出初始条件求特解

### 典型例题
dy/dx = xy，分离：dy/y = x dx，积分：ln|y| = x²/2 + C，通解：y = Ce^(x²/2)

## 一阶线性方程

### 定义
一阶线性微分方程的标准形式为 dy/dx + P(x)y = Q(x)。

### 核心概念
- **标准形式：** y' + P(x)y = Q(x)
- **积分因子法：** μ(x) = e^(∫P(x)dx)
- **求解公式：** y = (1/μ(x))·∫μ(x)·Q(x)dx
- **步骤：** 求积分因子 → 两边乘积分因子 → 左边为 (μy)' → 积分求解

### 关键要点
- 积分因子法是通用解法
- 当 Q(x) = 0 时为齐次方程，解为 y = Ce^(-∫P(x)dx)
- 注意积分因子中不定积分不加常数

### 典型例题
y' + 2y = 4，μ = e^(∫2dx) = e^(2x)，(e^(2x)y)' = 4e^(2x)，y = 2 + Ce^(-2x)

## 斜率场

### 定义
斜率场（方向场）是通过在 xy 平面上各点画短斜线段来可视化微分方程解的图形方法。

### 核心概念
- **绘制方法：** 在各点 (x,y) 处画斜率为 dy/dx = f(x,y) 的短线段
- **解曲线：** 与斜率场相切的曲线即为微分方程的解
- **等斜线：** dy/dx = k（常数）的曲线，其上所有短线段斜率相同
- **平衡解：** dy/dx = 0 对应的水平线

### 关键要点
- 斜率场能直观展示解的行为趋势
- AP 考试要求从斜率场判断解的性质
- 稳定平衡解：附近的解趋向它；不稳定平衡解：附近的解远离它

### 典型例题
dy/dx = y(1-y)，平衡解 y = 0（不稳定）和 y = 1（稳定），斜率场显示解趋向 y = 1

## 欧拉方法

### 定义
欧拉方法是用切线近似来数值求解微分方程的逐步逼近方法。

### 核心概念
- **递推公式：** y_{n+1} = y_n + h·f(x_n, y_n)
- **步长 h：** 越小越精确，计算量越大
- **几何意义：** 每步沿切线方向前进 h 个单位
- **误差：** 局部截断误差 O(h²)，全局截断误差 O(h)

### 关键要点
- 欧拉方法是 AP 考试必考内容
- 步长减半可提高精度但计算量翻倍
- 结果通常是近似值，会低估或高估真实值

### 典型例题
dy/dx = x + y，y(0) = 1，步长 h = 0.5：y₁ = 1 + 0.5·(0+1) = 1.5，y₂ = 1.5 + 0.5·(0.5+1.5) = 2.5

## 增长与衰减模型

### 定义
微分方程在描述自然增长和衰减过程中的应用。

### 核心概念
- **指数增长：** dy/dx = ky（k > 0），解为 y = Ce^(kt)
- **指数衰减：** dy/dx = ky（k < 0），解为 Ce^(kt)，趋向 0
- **受限增长（逻辑斯蒂）：** dy/dt = ky(1-y/M)，M 为承载力
- **牛顿冷却定律：** dT/dt = k(T - T_env)，物体温度趋向环境温度
- **半衰期：** t₁/₂ = ln2/|k|

### 关键要点
- 逻辑斯蒂模型的 S 形曲线是 AP 高频考点
- 承载力 M 是 y 的上界
- 初始条件决定特解

### 典型例题
人口模型 dP/dt = 0.02P(1 - P/1000)，P(0) = 100，承载力为 1000

## FRQ 常见题型

### 定义
AP 微积分 BC 考试中微分方程部分的自由回答题常见类型。

### 核心概念
- **斜率场画图：** 给定方程画斜率场，画出过特定点的解曲线
- **欧拉方法计算：** 给定步长，逐步计算近似值
- **分离变量求解：** 求通解或特解
- **逻辑斯蒂模型应用：** 给定数据拟合模型，预测未来值
- **验证解：** 给定函数验证是否满足微分方程

### 关键要点
- FRQ 通常结合斜率场和欧拉方法
- 要求用数学语言解释推理过程
- 注意计算器使用的限制（部分不能用计算器）

### 典型例题
给定 dy/dx = xy，(a) 画斜率场 (b) 用欧拉方法 h=0.5 求 y(1) (c) 求精确解
