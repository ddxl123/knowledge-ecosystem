# A-Level 统计（Statistics）

## 收集指南

> ⚠️ **严格范围限定：** 仅收集"2025A-Level数学"中"统计"的内容，禁止跨文件、跨目录引用。若本文件为某章节，则只收集该章节内容。

### 条目描述
统计学是A-Level数学的重要组成部分。

### 知识结构
- 描述统计 - 集中趋势、离散程度
- 概率 - 基本概率、条件概率
- 推断 - 假设检验

### 待收集原子知识点
- 描述统计：均值、中位数、标准差
- 概率：条件概率、独立事件
- 分布：二项分布、正态分布
- 推断：假设检验、置信区间

## 1. 数据表示（Data Representation）
### 数据类型
- 离散数据：可数的（如人数）
- 连续数据：不可数的（如身高、体重）

### 图表类型
- 频率分布表
- 直方图（Histogram）
- 频率多边图（Frequency Polygon）
- 累积频率图（Cumulative Frequency Graph）
- 箱线图（Box Plot）
- 茎叶图（Stem and Leaf Diagram）

## 2. 描述统计（Descriptive Statistics）
### 集中趋势
- 均值（Mean）：x̄ = Σx / n
- 中位数（Median）：排序后中间值
- 众数（Mode）：出现次数最多的值

### 离散程度
- 极差（Range）：最大值 - 最小值
- 四分位距（IQR）：Q3 - Q1
- 方差（Variance）：σ² = Σ(x-x̄)² / n
- 标准差（Standard Deviation）：σ = √方差

### 位置参数
- 四分位数：Q1, Q2, Q3
- 百分位数

## 3. 概率（Probability）
### 基本概念
- 样本空间（Sample Space）
- 事件（Event）
- 概率范围：0 ≤ P(A) ≤ 1

### 概率法则
- 加法法则：P(A∪B) = P(A) + P(B) - P(A∩B)
- 乘法法则：P(A∩B) = P(A) × P(B|A)
- 条件概率：P(B|A) = P(A∩B) / P(A)
- 互斥事件：P(A∩B) = 0
- 独立事件：P(A∩B) = P(A) × P(B)

### 全概率公式
- P(B) = Σ P(Ai) × P(B|Ai)

### 贝叶斯定理
- P(A|B) = P(B|A) × P(A) / P(B)

## 4. 概率分布（Probability Distributions）
### 离散随机变量
- 概率分布表
- 期望值：E(X) = Σx·P(X=x)
- 方差：Var(X) = E(X²) - [E(X)]²

### 二项分布（Binomial Distribution）
- 条件：固定次数、独立、成功概率恒定
- X ~ B(n, p)
- P(X=r) = C(n,r) × p^r × (1-p)^(n-r)
- E(X) = np
- Var(X) = np(1-p)

### 泊松分布（Poisson Distribution）
- 条件：稀有事件、单位时间/空间
- X ~ Po(λ)
- P(X=r) = (e^(-λ) × λ^r) / r!
- E(X) = λ
- Var(X) = λ

## 5. 正态分布（Normal Distribution）
### 基本性质
- 钟形曲线
- 对称分布
- X ~ N(μ, σ²)
- 68-95-99.7法则

### 标准化
- Z = (X - μ) / σ
- Z ~ N(0, 1)

### 常见概率
- P(-1 < Z < 1) ≈ 0.6826
- P(-2 < Z < 2) ≈ 0.9544
- P(-3 < Z < 3) ≈ 0.9974

## 6. 统计推断（Statistical Inference）
### 抽样方法
- 简单随机抽样
- 系统抽样
- 分层抽样
- 整群抽样

### 假设检验
- 零假设（H₀）和备择假设（H₁）
- 显著性水平（α）
- p值
- 单尾检验和双尾检验

### 置信区间
- 均值的置信区间
- 置信水平

## 核心公式
- 均值：x̄ = Σx / n
- 方差：σ² = Σ(x-x̄)² / n
- 二项分布：P(X=r) = C(n,r) p^r (1-p)^(n-r)
- 泊松分布：P(X=r) = (e^(-λ) λ^r) / r!
- 标准化：Z = (X-μ) / σ

## 备考建议
1. 掌握计算器统计功能
2. 理解公式推导过程
3. 大量练习真题
4. 注意书写规范和步骤完整
