# 04-动态规划

## 4.1 DP基础

### 4.1.1 动态规划思想
- 最优子结构：问题的最优解包含子问题的最优解
- 重叠子问题：不同路径会重复求解相同的子问题
- 状态转移方程：描述状态之间转移关系的核心公式

### 4.1.2 DP的一般步骤
- 定义状态：确定DP数组的含义，明确状态维度
- 写出转移方程：根据问题结构推导状态间的递推关系
- 确定初始条件和边界：最小子问题的解
- 确定计算顺序：保证计算某个状态时其依赖的状态已计算

## 4.2 线性DP

### 4.2.1 最长递增子序列（LIS）
- O(n²)算法：dp[i] = max(dp[j]+1)（j < i 且 a[j] < a[i]）
- O(n log n)二分优化：维护一个辅助数组，用二分查找确定每个元素的位置

### 4.2.2 最长公共子序列（LCS）
- 经典DP解法：dp[i][j]表示s₁前i个字符与s₂前j个字符的LCS长度
- 滚动数组优化：将空间从O(nm)优化到O(min(n,m))

### 4.2.3 数字三角形
- 自顶向下：dp[i][j] = max(dp[i-1][j-1], dp[i-1][j]) + a[i][j]
- 自底向上：从最后一行开始向上递推，最终答案在dp[1][1]

## 4.3 背包问题

### 4.3.1 01背包
- 二维DP：dp[i][j]表示前i个物品容量j时的最大价值
- 一维滚动数组优化：逆序遍历容量，dp[j] = max(dp[j], dp[j-w]+v)

### 4.3.2 完全背包
- 状态转移方程：dp[j] = max(dp[j], dp[j-w]+v)，正序遍历容量
- 优化：与01背包的区别在于遍历方向

### 4.3.3 多重背包
- 朴素算法：将每个物品拆成若干个01背包物品
- 二进制拆分优化：将数量按二进制拆分，减少物品数
- 单调队列优化：O(nm)，利用单调队列优化转移

### 4.3.4 分组背包
- 每组物品中最多选一个，先枚举组，再枚举容量，最后枚举组内物品

## 4.4 区间DP
- 石子合并：dp[i][j] = min/max(dp[i][k]+dp[k+1][j]+cost(i,j))
- 矩阵链乘法：dp[i][j] = min(dp[i][k]+dp[k+1][j]+p[i]*p[k]*p[j])
- 戳气球：逆向思维，dp[i][j]表示开区间(i,j)内戳完气球的最大得分

## 4.5 树形DP
- 树上最长路径：dp[u]表示以u为根的子树中最长路径
- 树的直径：两次DFS或树形DP求解
- 树上背包：在树上进行背包DP，子树间选择

## 4.6 状态压缩DP
- 位运算基础：与(&)、或(|)、异或(^)、左移(<<)、右移(>>)、取反(~)
- 旅行商问题（TSP）：dp[S][i]表示已访问集合S且当前在城市i的最短路径
- 棋盘覆盖问题：轮廓线DP，用二进制表示当前行的覆盖状态

## 4.7 数位DP
- 基本框架：逐位枚举，维护是否达到上界的状态
- 计数问题：统计满足条件的数的个数
- 不含连续特定数字：如不含连续两个1的数的个数

## 4.8 DP优化

### 4.8.1 单调队列优化
- 滑动窗口最值：维护队列单调性，队首即为最值
- 单调队列优化DP：将转移区间用单调队列维护，降低时间复杂度

### 4.8.2 斜率优化（凸壳优化）
- 斜率优化DP的基本思想：将转移方程变形为斜率形式，利用凸壳快速决策
- 典型例题：任务安排、玩具装箱

### 4.8.3 四边形不等式优化
- 区间DP的优化：当决策具有单调性时，利用四边形不等式减少枚举量
- 决策单调性：opt[i][j-1] ≤ opt[i][j] ≤ opt[i+1][j]

## NOIP提示
动态规划是NOIP最重要的算法类型，背包问题和线性DP是基础，状态压缩和数位DP是进阶。斜率优化和四边形不等式是高级优化技巧。
