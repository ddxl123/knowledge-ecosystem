# 03-图论

## 3.1 图的基本概念

### 3.1.1 图的存储
- 邻接矩阵：O(1)查询边，O(n²)空间，适合稠密图
- 邻接表：每个节点维护一个邻接列表，O(n+m)空间，适合稀疏图
- 链式前向星：用数组模拟邻接表，常数小，竞赛中常用

### 3.1.2 图的遍历
- DFS遍历：递归或栈实现，时间O(n+m)
- BFS遍历：队列实现，时间O(n+m)
- 连通分量：无向图中通过DFS/BFS标记连通分量

## 3.2 最短路径

### 3.2.1 单源最短路径
- Dijkstra算法（堆优化）：O((n+m)log n)，适用于非负权边
- Bellman-Ford算法：O(nm)，可检测负权环
- SPFA算法：Bellman-Ford的队列优化，平均O(m)，最坏O(nm)

### 3.2.2 全源最短路径
- Floyd-Warshall算法：O(n³)，动态规划思想，d[i][j] = min(d[i][j], d[i][k]+d[k][j])
- Johnson算法：利用势函数重赋权消除负权边，再对每个节点跑Dijkstra

### 3.2.3 特殊最短路径
- 0-1 BFS：边权只有0和1时，用双端队列实现O(n+m)
- 分层图最短路：将图复制多层，层间连特殊边，处理有限次免费通行等问题

## 3.3 最小生成树
- Kruskal算法：O(m log m)，按边权排序，用并查集判断是否形成环
- Prim算法：O((n+m)log n)，类似Dijkstra，每次选择距已选集合最近的点
- 最小生成树的性质：切割性质（最小边一定在某棵MST中）、环性质（最大边一定不在MST中）

## 3.4 拓扑排序
- BFS实现：维护入度为0的节点队列，每次取出并更新邻接点入度
- DFS实现：DFS完成后按完成时间逆序排列
- 应用：DAG上DP（在拓扑序上进行动态规划）

## 3.5 强连通分量
- Tarjan算法：一次DFS，利用dfn和low值识别强连通分量
- Kosaraju算法：两次DFS，第一次确定完成顺序，第二次在反图上按顺序DFS
- 缩点：将每个强连通分量缩为一个点，得到DAG

## 3.6 网络流

### 3.6.1 最大流
- Ford-Fulkerson方法：不断寻找增广路，更新残量网络
- Dinic算法：O(n²m)，利用层次图和多路增广优化
- 最小割定理：最大流 = 最小割

### 3.6.2 费用流
- 最小费用最大流：在满足最大流的前提下使总费用最小
- SPFA求费用流：在残量网络上用SPFA寻找最短增广路

## 3.7 二分图
- 二分图判定：DFS染色法，相邻节点染不同颜色
- 匈牙利算法（最大匹配）：O(nm)，寻找增广路扩大匹配
- König定理：二分图中最大匹配数 = 最小点覆盖数

## NOIP提示
图论是NOIP复赛的重点和难点，最短路径和网络流是高频考点。掌握Dijkstra、SPFA、Tarjan等核心算法。
