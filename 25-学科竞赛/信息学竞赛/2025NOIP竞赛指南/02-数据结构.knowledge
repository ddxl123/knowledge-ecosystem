# 02-数据结构

## 2.1 线性数据结构

### 2.1.1 数组与链表
- 静态数组与动态数组：静态数组大小固定，动态数组（如vector）可自动扩容
- 单链表、双链表：单链表只能单向遍历，双链表可双向遍历
- 循环链表：尾节点指向头节点，适用于环形结构

### 2.1.2 栈与队列
- 栈的应用：表达式求值（后缀表达式）、括号匹配、DFS的实现
- 队列的应用：BFS的实现、任务调度
- 单调栈：维护栈内元素单调递增/递减，用于求每个元素左/右第一个更大/小元素
- 单调队列：维护队列内元素单调性，用于滑动窗口最值问题

## 2.2 树形数据结构

### 2.2.1 二叉树
- 二叉树的遍历：前序（根-左-右）、中序（左-根-右）、后序（左-右-根）、层序（BFS）
- 二叉搜索树（BST）：左子树所有节点 < 根 < 右子树所有节点，中序遍历有序
- 平衡二叉树（AVL）：任意节点左右子树高度差不超过1，通过旋转维持平衡

### 2.2.2 堆
- 二叉堆（大根堆/小根堆）：完全二叉树，父节点 ≥（或 ≤）子节点
- 堆的操作：插入O(log n)、删除堆顶O(log n)、建堆O(n)
- 优先队列：基于堆实现，支持插入和弹出最值

### 2.2.3 字典树（Trie）
- 基本结构与操作：每个节点代表一个字符，从根到叶的路径构成字符串
- 01字典树：用于处理二进制数的异或问题，每个节点只有0和1两个子节点

## 2.3 并查集
- 基本操作：Find（查找所属集合的根）、Union（合并两个集合）
- 路径压缩：Find时将路径上所有节点直接连到根，均摊O(α(n))
- 按秩合并：Union时将较矮的树连到较高的树下，减少树高增长
- 带权并查集：边上带权值，维护节点间的相对关系

## 2.4 线段树

### 2.4.1 基本线段树
- 建树：O(n)，将区间信息存储在树节点中
- 区间查询：O(log n)，查询任意区间的聚合信息（和、最大值等）
- 区间修改（懒标记）：O(log n)，延迟更新，查询时下推标记

### 2.4.2 权值线段树
- 动态开点：只在需要时创建节点，节省空间
- 区间第k大：结合二分思想，利用权值线段树在线查询

## 2.5 树状数组（BIT）
- 基本原理：利用二进制低位运算维护前缀信息
- 单点修改、区间查询：修改O(log n)，查询前缀和O(log n)
- 区间修改、单点查询：利用差分数组，将区间修改转化为单点修改

## 2.6 哈希表
- 哈希函数：将键映射到数组下标，理想情况下O(1)访问
- 冲突处理：链地址法（拉链法）、开放定址法（线性探测、二次探测）
- 字符串哈希：将字符串映射为整数，用于字符串匹配和去重

## NOIP提示
数据结构是NOIP的核心，线段树和树状数组是复赛难题的常见工具。并查集和字典树也经常出现。
