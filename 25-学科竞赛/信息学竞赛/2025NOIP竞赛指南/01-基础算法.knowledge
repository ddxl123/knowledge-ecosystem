# 01-基础算法

## 1.1 排序算法

### 1.1.1 基本排序
- 冒泡排序：O(n²)，稳定，相邻元素比较交换
- 选择排序：O(n²)，不稳定，每次选最小放前面
- 插入排序：O(n²)，稳定，将元素插入已排序部分的正确位置

### 1.1.2 高效排序
- 归并排序：O(n log n)，稳定，分治思想，需要额外空间
- 快速排序：O(n log n)平均，不稳定，选取基准元素分区
- 堆排序：O(n log n)，不稳定，利用堆结构选择最值
- 计数排序：O(n+k)，稳定，适用于值域较小的整数排序
- 基数排序：O(d·(n+k))，稳定，按位依次排序

### 1.1.3 排序应用
- 自定义比较函数：通过重载比较运算符或传入比较函数实现自定义排序
- 离散化：将大值域映射到小值域，保持相对大小关系
- 逆序对计数：利用归并排序在O(n log n)时间内统计逆序对数量

## 1.2 搜索算法

### 1.2.1 深度优先搜索（DFS）
- 递归实现：利用系统调用栈，或手动维护栈
- 回溯法：尝试-失败-回退的搜索策略，用于求解组合问题
- 剪枝技巧：可行性剪枝（提前排除不可能的分支）、最优性剪枝（排除不可能优于当前最优解的分支）

### 1.2.2 广度优先搜索（BFS）
- 队列实现：先进先出，逐层扩展
- 最短路径（无权图）：BFS天然求解无权图最短路径
- 双向BFS：从起点和终点同时搜索，相遇时得到最短路径

### 1.2.3 启发式搜索
- A*算法：f(n) = g(n) + h(n)，g为已走代价，h为启发函数估计剩余代价
- 迭代加深搜索（IDDFS）：限制深度的DFS，逐步增加深度限制
- IDA*：迭代加深的A*算法，使用f值作为深度限制

## 1.3 二分算法

### 1.3.1 二分查找
- 整数二分：在有序整数序列中查找目标值，注意边界处理
- 浮点二分：在连续值域上查找，精度可控
- 二分答案：将求最优解问题转化为判定问题，通过二分答案范围求解

### 1.3.2 三分法
- 单峰函数极值：三分区间，比较两个三分点的函数值
- 单谷函数极值：类似三分法，适用于单谷函数

## 1.4 贪心算法
- 贪心策略的选择：每步选择局部最优，期望得到全局最优
- 区间调度问题：按结束时间排序，贪心选择不重叠的最多区间
- 哈夫曼编码：每次合并权值最小的两个节点，构造最优前缀编码
- 活动安排问题：贪心选择最早结束的活动

## 1.5 分治算法
- 归并排序：分（对半分）→治（递归排序）→合（合并两个有序序列）
- 快速幂：O(log n)计算a^n，利用二进制分解指数
- 平面最近点对：分治法在O(n log n)时间内求解

## NOIP提示
基础算法是NOIP初赛和复赛的基础，二分和贪心是高频考点。掌握各种排序的时间复杂度和稳定性特征。
