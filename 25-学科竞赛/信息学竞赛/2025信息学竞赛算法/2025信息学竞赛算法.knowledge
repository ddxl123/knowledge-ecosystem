# 2025信息学竞赛算法
# 领域: 学科竞赛
# 学科: 信息学竞赛
# 来源: 2025信息学竞赛算法

{{知识点1}}{{时间复杂度分析：大O记号用于描述算法运行时间的上界。常见复杂度从小到大依次为O(1)、O(log n)、O(√n)、O(n)、O(n log n)、O(n²)、O(n³)、O(2ⁿ)、O(n!)。竞赛中n≤10^18时一般只能用O(log n)或O(1)算法，n≤10^6时O(n log n)可接受，n≤5000时O(n²)通常可行。}}

{{知识点2}}{{空间换时间思想：用额外内存降低时间复杂度。典型应用包括预处理前缀和数组（O(1)区间查询）、哈希表（O(1)查找）、DP表（避免重复计算）。竞赛中常用long long数组开10^6级别，注意MLE限制。}}

{{知识点3}}{{快速排序与归并排序：快速排序平均O(n log n)，最坏O(n²)，随机化pivot可避免最坏情况。归并排序稳定O(n log n)，可用于求逆序对数量。C++中std::sort使用内省排序（introsort），结合快排、堆排和插入排序。}}

{{知识点4}}{{二分查找：在有序数组中查找目标值，时间O(log n)。变体包括：查找第一个≥目标值的下界（lower_bound）、查找第一个>目标值的上界（upper_bound）。整数二分注意边界处理，浮点二分注意精度控制。}}

{{知识点5}}{{二分答案：当答案具有单调性时，将求解问题转化为判定问题。典型题型：最小化最大值、最大化最小值、求满足条件的第k小/大值。判定函数通常O(n)或O(n log n)，整体O(n log V)。}}

{{知识点6}}{{双指针算法：利用单调性，两个指针在序列上同步移动，将O(n²)暴力优化到O(n)。典型应用：两数之和、三数之和、滑动窗口最值、最长无重复子串、接雨水问题。}}

{{知识点7}}{{前缀和与差分：前缀和sum[i]=a[1]+...+a[i]，可O(1)求区间和。二维前缀和sum[i][j]用于矩阵区间查询。差分数组d[i]=a[i]-a[i-1]，支持O(1)区间加操作，最终前缀和还原原数组。}}

{{知识点8}}{{栈与单调栈：栈用于括号匹配、表达式求值、DFS遍历。单调栈维护栈内元素单调递增或递减，用于求每个元素左/右第一个更大/更小元素的位置，典型题：柱状图最大矩形、接雨水。时间O(n)。}}

{{知识点9}}{{队列与单调队列：普通队列用于BFS。单调队列维护滑动窗口内最值，队首始终为当前窗口最值，入队时弹出不满足单调性的队尾元素。时间复杂度O(n)，典型应用：滑动窗口最大值。}}

{{知识点10}}{{链表：动态数据结构，支持O(1)插入和删除（已知节点位置）。竞赛中常用静态数组模拟链表（邻接表存图）。双向链表可从前驱/后继遍历。}}

{{知识点11}}{{哈希表：通过哈希函数将键映射到数组下标，平均O(1)查找/插入/删除。冲突处理：链地址法和开放寻址法。竞赛中常用unordered_map/set，或手写哈希表提升性能。注意哈希冲突可能导致退化。}}

{{知识点12}}{{二叉堆（优先队列）：完全二叉树结构，支持O(log n)插入和删除最值，O(1)取最值。C++中priority_queue默认最大堆。典型应用：Dijkstra算法、哈夫曼编码、合并K个有序链表、任务调度。}}

{{知识点13}}{{并查集（Union-Find）：维护不相交集合，支持合并和查询连通性。路径压缩+按秩合并使单次操作近O(α(n))。典型应用：连通分量计数、最小生成树Kruskal算法、判断图中是否有环、动态连通性。}}

{{知识点14}}{{字典树（Trie）：用于高效存储和查找字符串集合。每个节点代表一个字符，根到叶路径构成一个字符串。支持O(L)插入、查找和前缀查询（L为字符串长度）。典型应用：自动补全、异或最大值（01-Trie）。}}

{{知识点15}}{{线段树：支持区间查询和区间修改的高级数据结构。基本操作（区间求和、最值）时间O(log n)。懒标记（lazy propagation）优化区间修改。可扩展为动态开点线段树处理大范围离散区间。空间O(4n)。}}

{{知识点16}}{{树状数组（Fenwick Tree/BIT）：比线段树更轻量的区间/单点操作数据结构。支持单点修改+区间查询O(log n)，或区间修改+单点查询（差分树状数组）。二维树状数组支持矩阵区间操作。常数小，代码短。}}

{{知识点17}}{{平衡树：维护有序集合，支持O(log n)插入、删除、查找第k大、排名查询。常见实现：Treap（随机化平衡）、Splay（伸展树）、AVL、红黑树。C++中set/map底层为红黑树，但不支持排名查询。}}

{{知识点18}}{{DFS（深度优先搜索）：图/树的遍历方式，使用栈（递归或显式）实现。典型应用：连通分量、拓扑排序、强连通分量（Tarjan）、欧拉路径、回溯搜索。注意递归深度可能导致栈溢出。}}

{{知识点19}}{{BFS（广度优先搜索）：使用队列实现，按层遍历。无权图最短路径O(V+E)。典型应用：最短路径、迷宫问题、层序遍历、双向BFS优化。0-1BFS使用双端队列处理边权为0或1的图。}}

{{知识点20}}{{Dijkstra最短路算法：贪心思想，每次扩展距离最小的未确定节点。适用于非负权图。朴素实现O(V²)，优先队列优化O((V+E)log V)。不能处理负权边。典型应用：单源最短路。}}

{{知识点21}}{{Bellman-Ford与SPFA：Bellman-Ford可处理负权边，O(VE)。SPFA是其队列优化版本，平均O(E)，最坏O(VE)。可检测负环（某节点入队超过V次）。2025年竞赛中SPFA常被卡复杂度，需谨慎使用。}}

{{知识点22}}{{Floyd-Warshall全源最短路：动态规划思想，三重循环枚举中间节点k。O(V³)求所有点对最短路。可处理负权边（但不能有负环）。常用于小规模图（V≤500）或求传递闭包。}}

{{知识点23}}{{最小生成树（MST）：Kruskal算法按边权排序后贪心加入不形成环的边，配合并查集O(E log E)。Prim算法类似Dijkstra，从一点扩展O((V+E)log V)。Borůvka算法适合分布式场景。}}

{{知识点24}}{{拓扑排序：对有向无环图（DAG）进行线性排序，使所有边从前指向后。BFS实现（Kahn算法）使用入度数组，O(V+E)。典型应用：任务调度、判断DAG、DP on DAG。}}

{{知识点25}}{{强连通分量（SCC）：Tarjan算法通过DFS和栈在O(V+E)内求出所有SCC。Kosaraju算法需要两次DFS。缩点后DAG上可进行DP。典型应用：2-SAT问题、可达性分析。}}

{{知识点26}}{{二分图判定与匹配：二分图可用染色法（BFS/DFS）判定。最大匹配：匈牙利算法O(VE)，Hopcroft-Karp算法O(E√V)。König定理：最大匹配=最小顶点覆盖。典型应用：配对问题、棋盘覆盖。}}

{{知识点27}}{{网络流最大流：Ford-Fulkerson方法，DFS找增广路。Dinic算法O(V²E)效率更高，使用BFS分层+DFS多路增广。ISAP算法进一步优化。典型应用：二分图匹配、最小割、项目选择。}}

{{知识点28}}{{最小费用最大流（MCMF）：在网络流基础上考虑每条边的费用。SPFA找最短增广路（最小费用路径）。Successive Shortest Path算法。典型应用：运输问题、分配问题、最小权匹配。}}

{{知识点29}}{{动态规划基础：将问题分解为重叠子问题，记忆化搜索（自顶向下）或递推（自底向上）。核心要素：状态定义、转移方程、边界条件、计算顺序。优化手段：滚动数组降维、矩阵快速幂加速线性递推。}}

{{知识点30}}{{背包问题：0-1背包（每件物品选或不选）O(nW)，完全背包（无限次选取）O(nW)，多重背包（有限数量）可用二进制拆分优化。分组背包每组选一个。背包问题的DP状态通常为dp[i][w]表示前i件物品容量w的最大价值。}}

{{知识点31}}{{区间DP：在区间上定义状态dp[i][j]表示区间[i,j]的最优解，枚举分割点k转移。O(n³)。典型题：矩阵链乘法、石子合并、戳气球。环形问题可断环成链。}}

{{知识点32}}{{树形DP：在树结构上进行动态规划。典型应用：树的直径、树的重心、树上背包、没有上司的舞会（独立集）。通常DFS后序遍历，子节点结果向父节点转移。O(V)或O(V²)。}}

{{知识点33}}{{数位DP：按位处理数字的每一位，统计满足某种性质的数的个数。状态通常包括位置pos、前导零标记、是否达到上界tight、其他约束条件。典型题：不含某数字的数、数位和为某值的数。O(位数×状态数×10)。}}

{{知识点34}}{{状压DP：用二进制状态压缩表示集合。dp[S]表示集合S的最优解。典型题：旅行商问题（TSP）O(2ⁿ·n²)、哈密顿路径、棋盘覆盖。适用于n≤20的小规模集合问题。}}

{{知识点35}}{{斜率优化DP：当转移方程可化为dp[i]=min{dp[j]+f(j)·g(i)+h(i)}形式时，将转移看作平面上的直线，用凸包维护最优决策点。可将O(n²)优化到O(n)或O(n log n)。典型题：任务安排、玩具装箱。}}

{{知识点36}}{{分治算法：将问题分成若干子问题递归求解再合并。归并排序、快速排序是经典分治。CDQ分治处理三维偏序问题。整体二分用于离线处理带修改的区间查询。时间复杂度通过主定理分析。}}

{{知识点37}}{{贪心算法：每步选择当前最优的局部解。正确性需要严格证明（交换论证或归纳法）。典型应用：活动选择、区间调度（按结束时间排序）、哈夫曼编码、最小延迟调度。不能用于所有最优化问题。}}

{{知识点38}}{{KMP字符串匹配：预处理模式串的next/fail数组，匹配时利用已匹配信息跳过不必要的比较。时间O(n+m)。next[i]表示模式串[0,i)的最长相等前后缀长度。典型应用：循环节判断、字符串匹配。}}

{{知识点39}}{{字符串哈希：将字符串映射为整数，O(1)比较子串是否相等。常用多项式哈希：H(s)=Σs[i]·base^i mod p。双哈希降低冲突概率。典型应用：子串匹配、回文判断、最长公共子串。}}

{{知识点40}}{{后缀数组（SA）：将字符串的所有后缀按字典序排序。SA[i]表示排名第i的后缀起始位置。height数组记录相邻排名后缀的LCP长度。倍增法构建O(n log n)。典型应用：最长重复子串、不同子串计数。}}

{{知识点41}}{{Manacher算法：求字符串中最长回文子串，时间O(n)。核心思想：利用已知回文信息减少计算，维护最右回文边界和其中心。每个位置的回文半径可在O(1)均摊时间内确定。}}

{{知识点42}}{{欧拉函数与莫比乌斯函数：欧拉函数φ(n)表示[1,n]中与n互质的数的个数。莫比乌斯函数μ(n)用于容斥原理和数论函数反演。莫比乌斯反演：若f(n)=Σg(d)，则g(n)=Σμ(d)·f(n/d)。筛法预处理O(n)。}}

{{知识点43}}{{快速幂与矩阵快速幂：快速幂在O(log n)时间内计算a^n mod p。矩阵快速幂用于加速线性递推（如斐波那契数列），将O(n)递推优化到O(k³ log n)，k为矩阵维度。}}

{{知识点44}}{{素数筛法：埃拉托斯特尼筛法O(n log log n)筛出[2,n]所有素数。线性筛（欧拉筛）O(n)，每个合数只被其最小质因子筛掉。可用于预处理积性函数（欧拉函数、莫比乌斯函数等）。}}

{{知识点45}}{{扩展欧几里得算法（exGCD）：求ax+by=gcd(a,b)的整数解。可用于求乘法逆元（ax≡1 mod p时x=a⁻¹ mod p）。逆元在组合数取模计算中必不可少。时间O(log(min(a,b)))。}}

{{知识点46}}{{中国剩余定理（CRT）：求解一元线性同余方程组x≡aᵢ(mod mᵢ)。当模数两两互质时有唯一解。扩展CRT处理模数不互质的情况。典型应用：大数分解、RSA加密基础。}}

{{知识点47}}{{组合数学基础：排列数P(n,k)、组合数C(n,k)=n!/(k!(n-k)!)。Lucas定理处理大组合数取素数模。卡特兰数C(n)=C(2n,n)/(n+1)计数括号序列、二叉树形态等。容斥原理处理集合计数。}}

{{知识点48}}{{博弈论：Nim游戏和SG函数。Nim游戏先手必败当且仅当所有堆异或和为0。SG函数将任意公平组合游戏转化为Nim：SG(x)=mex{SG(y)|y为x的后继状态}。Sprague-Grundy定理：组合游戏的SG值等于子游戏SG值的异或。}}

{{知识点49}}{{计算几何基础：向量叉积判断左右转、面积计算。点到直线距离、线段相交判断。凸包用Graham扫描或Andrew算法O(n log n)求解。旋转卡壳求最远点对。半平面交求多边形交集。}}

{{知识点50}}{{随机化算法：随机化快排（随机选pivot避免最坏情况）、Miller-Rabin素性测试（概率算法，多次测试降低错误率）、随机化Treap（用随机优先级维护平衡）。随机化在竞赛中是处理最坏情况的有效手段。}}

{{知识点51}}{{构造与思维题：竞赛中常见的一类题型，要求根据条件构造满足要求的解。技巧包括：贪心构造、归纳构造、利用奇偶性、利用模运算性质、特判边界情况。通常不需要复杂算法，重点在于思维深度。}}

{{知识点52}}{{交互题：程序与评测系统交互，通过询问获取信息再作答。策略包括：二分查找优化询问次数、分治、利用约束条件减少搜索空间。注意刷新输出缓冲区（endl或fflush）。典型IOI赛制题型。}}

{{知识点53}}{{模拟退火：一种随机化优化算法，用于求解NP-hard问题的近似解。从初始解出发，以一定概率接受更差的解（温度高时概率大），随温度降低逐渐收敛到最优解附近。常用于几何、调度等问题。}}

{{知识点54}}{{LCA（最近公共祖先）：树上两个节点的最近公共祖先。倍增法预处理O(n log n)，查询O(log n)。Tarjan离线算法O(n+q)。树剖法也可求LCA。典型应用：树上距离计算、路径信息查询。}}

{{知识点55}}{{树链剖分：将树分解为若干条链，用线段树/树状数组维护链上信息。重链剖分保证任意节点到根的路径上轻边数不超过O(log n)。支持路径修改、路径查询、子树修改、子树查询。时间O(log²n)每次操作。}}

{{知识点56}}{{虚树：在树上多组查询中，只保留关键节点及其LCA构建虚树，在虚树上DP。虚树大小不超过2k-1（k为关键节点数）。将O(nq)优化到O(k log k + k)。典型应用：树上多次询问的优化。}}

{{知识点57}}{{分块算法：将数组分为√n块，每块维护聚合信息。单点修改O(1)，区间查询O(√n)。整块直接查预处理值，散块暴力。莫队算法基于分块，将离线区间查询排序后O(n√n)处理。实现简单，常数小。}}

{{知识点58}}{{可持久化数据结构：支持历史版本查询。可持久化线段树（主席树）在每次修改时只新建O(log n)个节点。典型应用：区间第k大（O(n log n)预处理+O(log n)查询）、历史版本查询。空间O(n log n)。}}

{{知识点59}}{{Link-Cut Tree（LCT）：维护动态树结构的高级数据结构。支持link、cut、换根、路径查询等操作，均摊O(log n)。基于Splay实现。典型应用：动态树上路径查询、动态连通性、动态MST。}}

{{知识点60}}{{FFT与NTT：快速傅里叶变换在O(n log n)时间内计算多项式乘法。NTT（数论变换）在模意义下进行，避免浮点误差。典型应用：大整数乘法、生成函数、卷积问题。竞赛中常用NTT，模数998244353。}}
