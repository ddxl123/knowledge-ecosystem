# 03-数论

## 3.1 整数的性质

### 3.1.1 整除
- 整除的基本性质：若a|b且b|c则a|c；若a|b且a|c则a|(bx+cy)
- 带余除法：a = bq + r，0 ≤ r < |b|
- 最大公因数与最小公倍数：gcd(a,b)·lcm(a,b) = a·b
- 欧几里得算法：辗转相除法求gcd，扩展欧几里得求ax + by = gcd(a,b)的整数解

### 3.1.2 质数与合数
- 质数的性质：大于1的正整数，只有1和自身两个因数；质数有无穷多个
- 算术基本定理（唯一分解定理）：任何大于1的正整数可唯一表示为质数幂的乘积
- 质数分布：质数定理 π(n) ~ n/ln(n)

### 3.1.3 同余
- 同余的定义与性质：a ≡ b (mod m) 当且仅当 m | (a-b)
- 模运算：模加、模乘、模幂的运算规则
- 线性同余方程：ax ≡ b (mod m) 有解条件 gcd(a,m)|b，解的个数
- 中国剩余定理（孙子定理）：一组同余方程在模互质条件下有唯一解

## 3.2 数论函数

### 3.2.1 欧拉函数
- 欧拉函数φ(n)的定义：1到n中与n互质的正整数个数
- 欧拉定理：若gcd(a,n)=1，则 a^φ(n) ≡ 1 (mod n)
- 费马小定理：若p为质数且p∤a，则 a^(p-1) ≡ 1 (mod p)

### 3.2.2 莫比乌斯函数
- 定义：μ(1)=1，若n有平方因子则μ(n)=0，若n为k个不同质数之积则μ(n)=(-1)^k
- 莫比乌斯反演公式：若g(n) = Σ_{d|n} f(d)，则f(n) = Σ_{d|n} μ(d)·g(n/d)

### 3.2.3 因数和与因数个数
- σ(n)：所有因数之和，σ(p^a) = (p^(a+1)-1)/(p-1)
- τ(n)：因数个数，τ(p^a) = a+1
- 积性函数：f(mn) = f(m)f(n)（当gcd(m,n)=1时），欧拉函数、σ、τ均为积性函数

## 3.3 不定方程

### 3.3.1 线性不定方程
- 二元一次不定方程：ax + by = c 有解条件 gcd(a,b)|c
- 多元一次不定方程：a₁x₁ + a₂x₂ + … + aₙxₙ = c 的求解方法

### 3.3.2 高次不定方程
- 勾股数：x² + y² = z² 的正整数解，本原勾股数的参数化表示
- 费马大定理简介：xⁿ + yⁿ = zⁿ (n≥3) 无正整数解
- 无穷递降法：假设存在最小解，构造更小的解导出矛盾

## 3.4 数论中的特殊方法
- 取整函数 [x] 的应用：高斯函数的性质，n!中质因数p的指数
- 末位数字与进位制：模10运算，不同进制下的表示
- 构造法与反证法：通过构造特例或反例解决问题

## 竞赛提示
数论是数学竞赛的四大板块之一，同余和不定方程是联赛二试的高频考点。费马小定理和中国剩余定理是最常用的数论工具。
