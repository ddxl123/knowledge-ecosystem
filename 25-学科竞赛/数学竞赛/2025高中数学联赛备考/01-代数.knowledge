# 01-代数

## 1.1 集合与函数

### 1.1.1 集合论基础
- 集合的运算与性质：交集、并集、补集、差集的基本运算律
- 容斥原理：|A∪B| = |A| + |B| - |A∩B|，推广到多个集合
- 子集与幂集：含n个元素的集合有2ⁿ个子集

### 1.1.2 函数性质
- 奇偶性：f(-x) = f(x)为偶函数，f(-x) = -f(x)为奇函数
- 单调性：判断函数增减性的方法，复合函数单调性判断
- 周期性：存在T使f(x+T) = f(x)
- 函数方程：通过代入特殊值、构造法求解
- 反函数与复合函数：反函数存在条件，复合函数定义域求法

## 1.2 不等式

### 1.2.1 基本不等式
- AM-GM不等式（均值不等式）：(a₁+a₂+…+aₙ)/n ≥ ⁿ√(a₁a₂…aₙ)，等号成立条件
- 柯西不等式：(a₁²+a₂²+…+aₙ²)(b₁²+b₂²+…+bₙ²) ≥ (a₁b₁+a₂b₂+…+aₙbₙ)²
- 排序不等式：顺序和 ≥ 乱序和 ≥ 逆序和

### 1.2.2 不等式证明方法
- 比较法：作差比较、作商比较
- 综合法：由已知条件出发逐步推导
- 分析法：从结论出发逆推条件
- 放缩法：适当放大或缩小进行证明
- 数学归纳法：适用于与自然数相关的不等式
- 反证法：假设结论不成立导出矛盾

### 1.2.3 经典不等式
- 伯努利不等式：(1+x)ⁿ ≥ 1+nx（x > -1, n为自然数）
- 幂平均不等式：不同阶幂平均之间的大小关系
- 舒尔不等式：a(a-b)(a-c) + b(b-a)(b-c) + c(c-a)(c-b) ≥ 0

## 1.3 多项式

### 1.3.1 多项式基本理论
- 韦达定理及其推广：根与系数的关系，对称多项式的表示
- 因式定理与余式定理：f(a)=0 ⇔ (x-a)|f(x)，余式定理的计算
- 对称多项式：初等对称多项式，对称多项式基本定理

### 1.3.2 多项式方程
- 整系数多项式的有理根：有理根定理（p/q型根的判定）
- 不可约多项式：艾森斯坦判别法
- 多项式的根与系数关系：Vieta定理的系统应用

## 1.4 数列与递推

### 1.4.1 等差数列与等比数列
- 通项公式：等差 aₙ = a₁ + (n-1)d；等比 aₙ = a₁·qⁿ⁻¹
- 求和公式：等差 Sₙ = n(a₁+aₙ)/2；等比 Sₙ = a₁(1-qⁿ)/(1-q)
- 性质与应用：等差中项、等比中项，数列在实际问题中的建模

### 1.4.2 递推数列
- 特征方程法：线性递推数列的通项求法
- 不动点法：x = f(x)的不动点用于简化递推关系
- 数列不等式：通过递推关系建立不等式，结合放缩法证明

## 竞赛提示
代数是数学联赛一试的重点板块，不等式和函数方程是高频考点。AM-GM不等式是最基本也最重要的不等式工具，需熟练掌握其各种变形和应用。
