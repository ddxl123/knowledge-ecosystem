# 04-组合

## 4.1 计数原理

### 4.1.1 基本计数原理
- 加法原理与乘法原理：分类计数用加法，分步计数用乘法
- 排列与组合：P(n,k) = n!/(n-k)!，C(n,k) = n!/[k!(n-k)!]
- 多重集的排列与组合：含重复元素的排列数 = n!/(n₁!n₂!…nₖ!)

### 4.1.2 特殊计数方法
- 隔板法：将n个相同球放入k个不同盒子（每盒至少一个）= C(n-1,k-1)
- 捆绑法与插空法：相邻元素捆绑为一个整体；不相邻元素先排其余再插空
- 环形排列：n个不同元素的环形排列数 = (n-1)!
- 圆排列：考虑翻转等价时为 (n-1)!/2

## 4.2 二项式定理与组合恒等式

### 4.2.1 二项式定理
- 二项式系数：(a+b)ⁿ = Σ C(n,k)·aⁿ⁻ᵏ·bᵏ
- 杨辉三角：C(n,k) = C(n-1,k-1) + C(n-1,k)
- 二项式系数的性质：对称性 C(n,k) = C(n,n-k)，最大值在中间

### 4.2.2 组合恒等式
- 范德蒙德恒等式：C(m+n,r) = Σ C(m,i)·C(n,r-i)
- 朱世杰恒等式：Σ C(k,r) = C(n+1,r+1)（k从r到n求和）
- 卡特兰数：Cₙ = C(2n,n)/(n+1)，应用：括号匹配、路径计数

## 4.3 递推与生成函数

### 4.3.1 递推关系
- 斐波那契数列：F(n) = F(n-1) + F(n-2)，通项公式（Binet公式）
- 递推关系的建立与求解：通过分析问题结构建立递推，特征方程法求解

### 4.3.2 生成函数
- 普通生成函数：G(x) = Σ aₙxⁿ，用于求解计数问题
- 指数生成函数：G(x) = Σ aₙxⁿ/n!，用于带标号的计数
- 生成函数在计数中的应用：整数分拆、递推关系求解

## 4.4 组合几何

### 4.4.1 覆盖与划分
- 图形的覆盖问题：用最少/最多图形覆盖给定区域
- 区域划分：n条直线最多将平面分为 (n²+n+2)/2 个区域

### 4.4.2 染色问题
- 棋盘染色：棋盘上放置棋子的约束条件
- 区域染色：相邻区域不同色的染色方案数

### 4.4.3 极值问题
- 凸包：平面点集的凸包构造
- 海莱定理：n个凸集中任取d+1个有交则全体有交

## 4.5 图论基础

### 4.5.1 基本概念
- 图的定义与术语：顶点、边、度、子图、补图
- 路与连通性：路径、回路、连通图、连通分量
- 树与生成树：树的性质（n个顶点n-1条边），最小生成树

### 4.5.2 欧拉图与哈密顿图
- 欧拉路径与欧拉回路：所有顶点度为偶数（回路），恰好两个奇度顶点（路径）
- 哈密顿路径与哈密顿回路：经过每个顶点恰好一次的路径/回路

## 竞赛提示
组合数学灵活多变，计数方法和递推关系是核心，需要大量练习培养直觉。卡特兰数和生成函数是高级工具。
